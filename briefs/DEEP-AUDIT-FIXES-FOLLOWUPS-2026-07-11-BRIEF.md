<!-- SPDX: Copyright 2026 Michal Planicka · SPDX-License-Identifier: Apache-2.0 -->
**Status:** PROPOSED · **Created:** 2026-07-11 · **Follows:** [`DEEP-AUDIT-FIXES-2026-07-11-BRIEF.md`](DEEP-AUDIT-FIXES-2026-07-11-BRIEF.md) · **Findings:** [`../audits/DEEP-AUDIT-FINDINGS-2026-07-11.md`](../audits/DEEP-AUDIT-FINDINGS-2026-07-11.md)

# Deep-audit fixes — follow-ups (2026-07-11)

Residue discovered while executing `DEEP-AUDIT-FIXES-2026-07-11`: one deferred finding that needs the
raw corpus, one verification caveat, and the latent hypotheses the findings pass parked.

## §1 — F4 (MED, contract): ECGDex `apnea` + `hrvStability` never reach the Integrator via the automated path
**Deferred from the fixes pass because it is EXPORT-MOVING** — extending the shared `ecgBuildNodeExport`
rich branch (`ecgdex-dsp.js`) to emit `apnea`/`hrvStability`/whole-record `hrv` changes the ECGDex
`compute()` output, so the committed `ECGDex_2026-06-27_equiv` fixture must be **regenerated by re-running
the app on its raw `_ECG.txt` input** (never re-stamped blind — `CLAUDE.md §🔏`). The raw input is
gitignored in the audit checkout, so this must run in an environment with the real corpus. Two options
(decide in execution): **(a)** extend the rich builder to mirror `buildV2`'s `apnea`+`hrvStability` so
`emitEcgNodeExport`/`exportGanglior` feed `fuseAutonomicGlycemic` (rule 3) and the apnea authority; or
**(b)** if light-by-design, have the Integrator emit a visible warning when an ECGDex rec has events but
null `hrv`/`apnea`/slope, and add an assertion pinning the intended feed. Gate cost: ECGDex re-bundle +
**regenerate the ECGDex equiv fixture** (GATE B/C) + re-run both gates.

## §2 — F5 verification caveat: confirm the PulseDex equiv leg on a full-corpus run
`artifactClean`'s `2200 → 2000` ms bound was reasoned export-inert (replace-with-median, no sub-30 bpm
beats in a resting clip) but **not** re-confirmed against the raw PulseDex clip headless (no raw input).
On the next full-corpus run, open `Dex-Test-Suite.html?full` and confirm the PulseDex `env.equiv` leg is
green; if it reds, the committed `PulseDex_2026-06-25_equiv` fixture must be regenerated (the clip *does*
contain a `[2000,2200]` ms beat) — otherwise no action.

## §3 — Latent hypotheses (from the findings doc; none reachable in production today)
- **HRVDex carried fields emit `0`, not `null`, on absent source** (`hrvdex-dsp.js:757-758`) — the
  `_envToSeed` `n(v)=finite?v:0` coercion surfaces `sdnn:0`/`hr:0` for an absent envelope cell,
  contradicting the file's own "stay null when absent" comment. Same class as 2026-07-01 Finding 1, one
  layer over (carried fields vs rolling baseline). Trigger: re-ingesting a partial envelope.
- **HRVDex `meanRR_s` outside the Baevsky guard** (`hrvdex-dsp.js:378,385`) — `d_csi`/`d_cvi` assume
  Mean RR is ms unguarded; a seconds-unit vendor would inflate `d_csi` ~1000×. Close the asymmetry.
- **HRVDex `d_csi` fallback ms/s mismatch when `DexUnits` absent** (`hrvdex-dsp.js:385`) — one-line
  defensive fix (`r._mxdmn/1000`); unreachable in a co-loaded bundle.
- **OxyDex 1-Hz-proxy Poincaré divisor mix** (`oxydex-dsp.js:4268` `÷m` vs `:4274` `÷(m−1)`) — a
  `÷N`/`÷(N−1)` inconsistency on the oximeter pulse-rate proxy (not beat HRV); out of the four-node
  HRV scope but same bug-class — worth a separate look.

If any of §3 is accepted, it lands as its own dated brief and honors the re-bundle + gate ritual.
