<!-- Copyright 2026 Michal Planicka · SPDX-License-Identifier: Apache-2.0 -->
<!-- GENERATED by tools/mutation-suite.mjs --inventory — do not hand-edit; re-run the tool. -->
# Mutation inventory — what the suite knows about these tests

**Status:** REFERENCE (living — regenerated by the tool) · **last-verified:** 2026-09-04

## Where this comes from, and how to reproduce it

| | |
|---|---|
| suite driver | `tools/mutation-suite.mjs` |
| regenerate this file | `node tools/mutation-suite.mjs --inventory` |
| run a sweep | `node tools/mutation-suite.mjs --file <name>-dsp.js` |
| stop a running sweep | `node tools/mutation-suite.mjs --kill` (by PID — never `pkill`) |
| live state | `node tools/mutation-suite.mjs --status` |
| build the speed-up map | `node tools/mutation-suite.mjs --build-map` |
| mutation engine | `tools/mutate.mjs` · statement deletion `tools/stmt-delete.mjs` · XMT `tools/extreme-mutate.mjs` |
| coverage map | `/home/michal/Tepna/.git/tepna-mutation/per-group.json` |

A **surviving mutant is the finding**: the code was changed and the whole suite stayed green,
so nothing tests that line — whatever the coverage number says. A survivor is NOT proof of a
bug; some are legitimately untestable. Triage is the reader’s.

## Totals

**Scope: the JavaScript DSPs only.** `capture-host/` is a separate lane (mutmut, ~74.6 % at last
audit) and none of its mutants are counted here — do not read the fleet row as a project-wide figure.

- **open** — survivors with no recorded classification: the actual outstanding work.
- **classified** — survivors already proven to have no distinguishing input (`tools/mutate-equivalence.json`).
  They cannot be killed by any test, so writing one for them is wasted effort.
- **invalid** — mutants that failed to run. They leave the denominator SILENTLY, so a kill rate
  computed beside a large invalid count is provisional (one file once ran 73 % invalid).
- **state** — `complete` is recorded on a clean exit; `unknown` means nothing here can vouch for it.
- **stale classifications** — ledger entries that no longer match any survivor. The ledger is keyed
  by LINE, and lines move, so a classification silently stops applying when the file is edited.
  A VOID file (its canary survived) measured nothing at all and its counts must not be quoted.

| file | tested | killed | survived | **open** | classified | invalid | kill rate | state |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| `cpapdex-dsp.js` | 911 | 437 | 474 | **445** | 29 | 0 | 48 % | unknown |
| `ecgdex-dsp.js` | 813 | 331 | 476 | **476** | 0 | 6 | 41 % | unknown |
| `glucodex-dsp.js` | 847 | 409 | 433 | **388** | 45 | 5 | 48 % | complete |
| `hrvdex-dsp.js` | 489 | 326 | 163 | **119** | 44 | 0 | 67 % | complete |
| `integrator-dsp.js` | 978 | 512 | 466 | **466** | 0 | 0 | 52 % | unknown |
| `motiondex-dsp.js` | 469 | 217 | 243 | **143** | 100 | 9 | 46 % | complete |
| `oxydex-dsp.js` | 2937 | 1598 | 1323 | **1323** | 0 | 16 | 54 % | unknown |
| `ppgdex-dsp.js` | 1367 | 564 | 789 | **661** | 128 | 14 | 41 % | unknown |
| `pulsedex-dsp.js` | 577 | 304 | 271 | **229** | 42 | 2 | 53 % | complete |
| **fleet** | **9388** | **4698** | **4638** | **4250** | 388 | 52 | **50 %** | **includes 5 unconfirmed** |

> ⚠ **47 recorded classification(s) no longer match any survivor.** The equivalence ledger is
> keyed by line number, and lines move — so triage work that was really done has silently stopped
> applying. Those mutants are counted as **open** here, which over-states the work rather than
> hiding a gap, but the classifications need re-anchoring to be worth anything again.

> ⚠ **`cpapdex-dsp.js`, `ecgdex-dsp.js`, `integrator-dsp.js`, `oxydex-dsp.js`, `ppgdex-dsp.js` are not confirmed-complete sweeps.** A row marked
> `in flight` is a snapshot of a running sweep — the counts will grow and the rate will move.
> A row marked `unknown` came from a journal this suite did not finish itself, so its
> completeness cannot be asserted; it is not a claim that the file is unfinished, only that
> nothing here can vouch for it. Re-run `--inventory` after a sweep to resolve either.
> The fleet row inherits that caveat.

## The other two lanes — different UNITS, never summed with the mutant table above

Pseudo (XMT/Descartes) classifies **functions**; statement deletion judges **statements**.
Neither is a mutant count, and no combined total exists on purpose.

### Pseudo lane (`extreme-mutate.mjs`) — unit: **functions**

_No recorded run found in either state location. This is an absent INPUT — the lane may
have run without `--resume` (no persistent ledger) or not at all; it is NOT a clean bill._

### Statement-deletion lane (`stmt-delete.mjs`) — unit: **statements**

_No recorded run found in either state location. This is an absent INPUT — the lane may
have run without `--resume` (no persistent ledger) or not at all; it is NOT a clean bill._

## `cpapdex-dsp.js` — 474 survivor(s)

| line | operator | source |
|---:|---|---|
| 37 | `bool && → \|\|` | `var _EDF = typeof require !== 'undefined' ? /** @type {any} */ (require)` |
| 41 | `cmp < → <=` | `return (n < 10 ? '0' : '') + n;` |
| 41 | `num → 0` | `return (n < 10 ? '0' : '') + n;` |
| 71 | `bool \|\| → &&` | `if (dd.getUTCFullYear() !== y \|\| dd.getUTCMonth() !== mo0 \|\| dd.getUTCDa` |
| 71 | `bool \|\| → &&` | `if (dd.getUTCFullYear() !== y \|\| dd.getUTCMonth() !== mo0 \|\| dd.getUTCDa` |
| 72 | `bool && → \|\|` | `if (h === 24 && mi === 0 && se === 0 && ms === 0) return day0 + 86400000` |
| 72 | `eq === → !==` | `if (h === 24 && mi === 0 && se === 0 && ms === 0) return day0 + 86400000` |
| 72 | `eq === → !==` | `if (h === 24 && mi === 0 && se === 0 && ms === 0) return day0 + 86400000` |
| 72 | `eq === → !==` | `if (h === 24 && mi === 0 && se === 0 && ms === 0) return day0 + 86400000` |
| 72 | `eq === → !==` | `if (h === 24 && mi === 0 && se === 0 && ms === 0) return day0 + 86400000` |
| 72 | `bool && → \|\|` | `if (h === 24 && mi === 0 && se === 0 && ms === 0) return day0 + 86400000` |
| 72 | `num → 0` | `if (h === 24 && mi === 0 && se === 0 && ms === 0) return day0 + 86400000` |
| 72 | `num → 0` | `if (h === 24 && mi === 0 && se === 0 && ms === 0) return day0 + 86400000` |
| 73 | `cmp > → >=` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 73 | `cmp > → >=` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 73 | `cmp > → >=` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 73 | `cmp < → <=` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 73 | `cmp > → >=` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 73 | `bool \|\| → &&` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 73 | `cmp < → <=` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 73 | `cmp < → <=` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 73 | `bool \|\| → &&` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 73 | `bool \|\| → &&` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 73 | `bool \|\| → &&` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 73 | `bool \|\| → &&` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 73 | `bool \|\| → &&` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 73 | `bool \|\| → &&` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 98 | `cmp < → <=` | `for (var i = 0; i < a.length; i++) {` |
| 100 | `bool && → \|\|` | `if (v != null && isFinite(v)) o.push(v);` |
| 121 | `bool \|\| → &&` | `return m === 0 \|\| isNaN(m) ? NaN : (_sd(a) / Math.abs(m)) * 100;` |
| 131 | `eq === → !==` | `if (lo === hi) return f[lo];` |
| 139 | `cmp < → <=` | `for (var i = 0; i < a.length; i++) if (pred(a[i])) c++;` |
| 144 | `cmp < → <=` | `for (var i = 0; i < a.length; i++) if (mask[i]) o.push(a[i]);` |
| 189 | `cmp <= → <` | `for (var i = 0; i + n <= pressureMaskOn.length; i += n) out.push(_p(pres` |
| 189 | `num → 0` | `for (var i = 0; i + n <= pressureMaskOn.length; i += n) out.push(_p(pres` |
| 219 | `bool && → \|\|` | `return v != null && isFinite(v);` |
| 221 | `cmp < → <=` | `if (q.length < MODE_MIN_NIGHTS) return { mode: null, envIqr: null, night` |
| 225 | `cmp >= → >` | `mode: med <= MODE_CPAP_MAX ? 'CPAP' : med >= MODE_APAP_MIN ? 'APAP' : nu` |
| 225 | `cmp <= → <` | `mode: med <= MODE_CPAP_MAX ? 'CPAP' : med >= MODE_APAP_MIN ? 'APAP' : nu` |
| 236 | `cmp < → <=` | `if (env.length < MODE_MIN_WINDOWS) return { mode: null, envIqr: null, nW` |
| 240 | `cmp >= → >` | `mode: q <= MODE_CPAP_MAX ? 'CPAP' : q >= MODE_APAP_MIN ? 'APAP' : null,` |
| 240 | `cmp <= → <` | `mode: q <= MODE_CPAP_MAX ? 'CPAP' : q >= MODE_APAP_MIN ? 'APAP' : null,` |
| 266 | `cmp > → >=` | `for (var i = 0; i < n; i++) maskOn[i] = raw.pressure[i] > 0 ? 1 : 0;` |
| 280 | `cmp > → >=` | `return v > 0;` |
| 288 | `cmp > → >=` | `if (!(denomHours > 0)) return NaN;` |
| 312 | `cmp < → <=` | `for (var i = 0; i < d.pressure.length; i++)` |
| 320 | `num → 0` | `medianPressure: _p(d.pressureMaskOn, 50),` |
| 321 | `num → 0` | `p95Pressure: _p(d.pressureMaskOn, 95),` |
| 329 | `num → 0` | `medianLeak: _p(d.leakMaskOn, 50),` |
| 330 | `num → 0` | `p95Leak: _p(d.leakMaskOn, 95),` |
| 333 | `cmp > → >=` | `return v > 24;` |
| 336 | `num → 0` | `100` |
| 345 | `bool \|\| → &&` | `if (ll == null \|\| isNaN(ll)) return 1;` |
| 360 | `num → 0` | `endMs: prepared.t0Ms + durMin * 60000,` |
| 398 | `cmp < → <=` | `for (var i = 0; i < arr.length; i++) if (arr[i] != null && isFinite(arr[` |
| 398 | `bool && → \|\|` | `for (var i = 0; i < arr.length; i++) if (arr[i] != null && isFinite(arr[` |
| 400 | `cmp < → <=` | `if (n < 2) return null;` |
| 412 | `cmp < → <=` | `if (Math.abs(den) < 1e-12) return null;` |
| 412 | `num → 0` | `if (Math.abs(den) < 1e-12) return null;` |
| 435 | `cmp >= → >` | `return h != null && h >= 4;` |
| 452 | `num → 0` | `.slice(-30)` |
| 455 | `bool && → \|\|` | `return v != null && isFinite(v);` |
| 457 | `cmp > → >=` | `var ahiTrend30d = last30.length ? { mean: +_mean(last30).toFixed(2), sd:` |
| 467 | `eq !== → ===` | `return typeof require !== 'undefined' ? /** @type {any} */ (require)('./` |
| 472 | `bool && → \|\|` | `var crossNight = CROSS && CROSS.crossNightBlock ? CROSS.crossNightBlock(` |
| 514 | `cmp < → <=` | `for (var i = 0; i < ch.data.length; i++) out[i] = ch.data[i] * k;` |
| 538 | `bool && → \|\|` | `return da > 0 && db > 0 ? num / Math.sqrt(da * db) : NaN;` |
| 538 | `cmp > → >=` | `return da > 0 && db > 0 ? num / Math.sqrt(da * db) : NaN;` |
| 538 | `cmp > → >=` | `return da > 0 && db > 0 ? num / Math.sqrt(da * db) : NaN;` |
| 553 | `num → 0` | `WIN_SEC: 10, // ± window (s) around the desat onset at 1 Hz` |
| 566 | `bool \|\| → &&` | `if (onset == null \|\| !pulseSeries \|\| !pulseSeries.length) return desat;` |
| 566 | `bool \|\| → &&` | `if (onset == null \|\| !pulseSeries \|\| !pulseSeries.length) return desat;` |
| 574 | `cmp <= → <` | `for (var i = lo; i <= hi; i++) {` |
| 577 | `cmp >= → >` | `if (p != null && isFinite(p) && p >= SELFGATE.PULSE_MIN && p <= SELFGATE` |
| 577 | `cmp <= → <` | `if (p != null && isFinite(p) && p >= SELFGATE.PULSE_MIN && p <= SELFGATE` |
| 579 | `cmp > → >=` | `var pulseValid = tot > 0 ? valid / tot : 0;` |
| 582 | `bool && → \|\|` | `if (spo2Series && spo2Series.length) {` |
| 585 | `cmp <= → <` | `for (var k = a; k <= b; k++) {` |
| 587 | `cmp > → >=` | `if (d > fallRate) fallRate = d;` |
| 589 | `bool && → \|\|` | `} else if (desat.depth != null && desat.duration) {` |
| 594 | `cmp < → <=` | `if (nadir != null && nadir < N) {` |
| 594 | `bool && → \|\|` | `if (nadir != null && nadir < N) {` |
| 597 | `cmp >= → >` | `var beforeOk = pBefore != null && isFinite(pBefore) && pBefore >= SELFGA` |
| 597 | `cmp <= → <` | `var beforeOk = pBefore != null && isFinite(pBefore) && pBefore >= SELFGA` |
| 597 | `bool && → \|\|` | `var beforeOk = pBefore != null && isFinite(pBefore) && pBefore >= SELFGA` |
| 597 | `bool && → \|\|` | `var beforeOk = pBefore != null && isFinite(pBefore) && pBefore >= SELFGA` |
| 597 | `bool && → \|\|` | `var beforeOk = pBefore != null && isFinite(pBefore) && pBefore >= SELFGA` |
| 598 | `cmp > → >=` | `var atBad = pAt == null \|\| !isFinite(pAt) \|\| pAt < SELFGATE.PULSE_MIN \|\|` |
| 598 | `cmp >= → >` | `var atBad = pAt == null \|\| !isFinite(pAt) \|\| pAt < SELFGATE.PULSE_MIN \|\|` |
| 598 | `cmp < → <=` | `var atBad = pAt == null \|\| !isFinite(pAt) \|\| pAt < SELFGATE.PULSE_MIN \|\|` |
| 598 | `bool \|\| → &&` | `var atBad = pAt == null \|\| !isFinite(pAt) \|\| pAt < SELFGATE.PULSE_MIN \|\|` |
| 598 | `bool \|\| → &&` | `var atBad = pAt == null \|\| !isFinite(pAt) \|\| pAt < SELFGATE.PULSE_MIN \|\|` |
| 598 | `bool \|\| → &&` | `var atBad = pAt == null \|\| !isFinite(pAt) \|\| pAt < SELFGATE.PULSE_MIN \|\|` |
| 598 | `bool \|\| → &&` | `var atBad = pAt == null \|\| !isFinite(pAt) \|\| pAt < SELFGATE.PULSE_MIN \|\|` |
| 601 | `cmp > → >=` | `if (fallRate > SELFGATE.FALL_RATE_MAX) {` |
| 604 | `num → 0` | `desat.sqi = 0.2;` |
| 605 | `cmp < → <=` | `} else if (pulseValid < SELFGATE.PULSE_VALID_FLOOR \|\| edgeCollapse) {` |
| 608 | `num → 0` | `desat.sqi = 0.2;` |
| 624 | `num → 0` | `var OXI = { SPO2_MIN: 50, SPO2_MAX: 100, DROP: 3, MIN_DUR: 10, BASE_WIN:` |
| 624 | `num → 0` | `var OXI = { SPO2_MIN: 50, SPO2_MAX: 100, DROP: 3, MIN_DUR: 10, BASE_WIN:` |
| 624 | `num → 0` | `var OXI = { SPO2_MIN: 50, SPO2_MAX: 100, DROP: 3, MIN_DUR: 10, BASE_WIN:` |
| 626 | `cmp <= → <` | `return v != null && isFinite(v) && v >= OXI.SPO2_MIN && v <= OXI.SPO2_MA` |
| 626 | `cmp >= → >` | `return v != null && isFinite(v) && v >= OXI.SPO2_MIN && v <= OXI.SPO2_MA` |
| 629 | `bool \|\| → &&` | `drop = drop \|\| OXI.DROP;` |
| 635 | `num → 0` | `nad = 100,` |
| 637 | `num → 0` | `base = 95;` |
| 639 | `cmp >= → >` | `if (inEv && end - st >= OXI.MIN_DUR) events.push({ startIdx: st, onset: ` |
| 642 | `cmp < → <=` | `for (var i = 0; i < n; i++) {` |
| 649 | `cmp < → <=` | `for (var j = lo; j < i; j++) {` |
| 651 | `cmp > → >=` | `if (_spo2Valid(v) && v > bmax) bmax = v;` |
| 654 | `cmp <= → <` | `if (spo2[i] <= bmax - drop) {` |
| 661 | `cmp < → <=` | `} else if (spo2[i] < nad) {` |
| 675 | `bool \|\| → &&` | `if (!spo2Ch \|\| !spo2Ch.data \|\| !spo2Ch.data.length) return { available: ` |
| 680 | `cmp < → <=` | `for (var i = 0; i < n; i++) if (_spo2Valid(spo2[i])) valid++;` |
| 682 | `bool \|\| → &&` | `if (valid === 0 \|\| coverage < OXI.COVERAGE_FLOOR)` |
| 697 | `bool \|\| → &&` | `var spanSec = durSec \|\| n;` |
| 698 | `cmp > → >=` | `var hours = n > 0 ? (valid * (spanSec / n)) / 3600 : 0;` |
| 712 | `cmp < → <=` | `for (var k = 0; k < n; k++) {` |
| 717 | `cmp < → <=` | `if (s < 90) below90++;` |
| 717 | `num → 0` | `if (s < 90) below90++;` |
| 718 | `cmp < → <=` | `if (s < nadir) nadir = s;` |
| 722 | `cmp < → <=` | `for (var m = 0; m < pulse.length; m++) {` |
| 724 | `cmp >= → >` | `if (p >= SELFGATE.PULSE_MIN && p <= SELFGATE.PULSE_MAX) pv.push(p);` |
| 724 | `cmp <= → <` | `if (p >= SELFGATE.PULSE_MIN && p <= SELFGATE.PULSE_MAX) pv.push(p);` |
| 724 | `bool && → \|\|` | `if (p >= SELFGATE.PULSE_MIN && p <= SELFGATE.PULSE_MAX) pv.push(p);` |
| 729 | `cmp > → >=` | `odi: hours > 0 ? +(real.length / hours).toFixed(2) : null,` |
| 735 | `cmp > → >=` | `analyzedHours: hours > 0 ? +hours.toFixed(4) : null,` |
| 738 | `num → 0` | `t90Pct: vArr.length ? +((below90 / vArr.length) * 100).toFixed(2) : null` |
| 741 | `num → 0` | `pulseMedian: pv.length ? +_p(pv, 50).toFixed(0) : null,` |
| 742 | `num → 0` | `pulseRange: pv.length ? +(_p(pv, 95) - _p(pv, 5)).toFixed(0) : null,` |
| 757 | `num → 0` | `fs = flowCh.fs \|\| 25,` |
| 757 | `bool \|\| → &&` | `fs = flowCh.fs \|\| 25,` |
| 768 | `num → 0` | `noise = (noise / n) * 0.15; // 15% of mean \|flow\|` |
| 771 | `cmp < → <=` | `for (var a = 0; a < n; a++) ps[a + 1] = ps[a] + flow[a];` |
| 773 | `num → 0` | `minBreathSamp = Math.round(fs * 1.2); // ≥1.2 s/breath (≤50 brpm ceiling` |
| 774 | `cmp < → <=` | `for (var i = 0; i < n; i++) {` |
| 779 | `cmp > → >=` | `if (d > noise) {` |
| 781 | `cmp >= → >` | `if (lastSign <= 0 && (lastCrossIdx < 0 \|\| i - lastCrossIdx >= minBreathS` |
| 781 | `cmp <= → <` | `if (lastSign <= 0 && (lastCrossIdx < 0 \|\| i - lastCrossIdx >= minBreathS` |
| 781 | `cmp < → <=` | `if (lastSign <= 0 && (lastCrossIdx < 0 \|\| i - lastCrossIdx >= minBreathS` |
| 786 | `cmp < → <=` | `} else if (d < -noise) {` |
| 791 | `cmp > → >=` | `var breathRate = durSec > 0 ? +(breaths / (durSec / 60)).toFixed(1) : nu` |
| 792 | `cmp > → >=` | `var ieRatio = expSamp > 0 ? +(inspSamp / expSamp).toFixed(2) : null;` |
| 828 | `num → 0` | `var timeSec = a.tMs != null ? (a.tMs - sessionT0Ms) / 1000 : a.onsetSec;` |
| 829 | `bool \|\| → &&` | `out.push({ type: type, timeSec: timeSec, durSec: a.durSec \|\| 0, class: a` |
| 890 | `cmp > → >=` | `if (sessionDurSec > 0 && sessionDurSec > open) {` |
| 890 | `bool && → \|\|` | `if (sessionDurSec > 0 && sessionDurSec > open) {` |
| 890 | `cmp > → >=` | `if (sessionDurSec > 0 && sessionDurSec > open) {` |
| 909 | `bool \|\| → &&` | `meta = meta \|\| {};` |
| 911 | `bool \|\| → &&` | `var therapy = set.PLD \|\| set.BRP \|\| set.SA2; // therapy clock source` |
| 914 | `bool \|\| → &&` | `var recDur = therapy.recDurSec \|\| 60;` |
| 914 | `num → 0` | `var recDur = therapy.recDurSec \|\| 60;` |
| 930 | `num → 0` | `var fs = pressCh ? pressCh.fs : 0.5;` |
| 935 | `cmp > → >=` | `for (var i = 0; i < pressure.length; i++) maskOn[i] = pressure[i] > 0 ? ` |
| 935 | `cmp < → <=` | `for (var i = 0; i < pressure.length; i++) maskOn[i] = pressure[i] > 0 ? ` |
| 940 | `cmp > → >=` | `return v > 0;` |
| 942 | `num → 0` | `((fs \|\| 0.5) * 3600);` |
| 942 | `bool \|\| → &&` | `((fs \|\| 0.5) * 3600);` |
| 953 | `bool && → \|\|` | `if (eprCh && pressCh) {` |
| 956 | `cmp > → >=` | `for (var e = 0; e < nn; e++) if (pressCh.data[e] > 0) diffs.push(pressCh` |
| 956 | `cmp < → <=` | `for (var e = 0; e < nn; e++) if (pressCh.data[e] > 0) diffs.push(pressCh` |
| 957 | `num → 0` | `eprDelta = diffs.length ? +_p(diffs, 50).toFixed(1) : null;` |
| 965 | `num → 0` | `var epap95 = epapMaskOn.length ? +_p(epapMaskOn, 95).toFixed(2) : null; ` |
| 992 | `cmp > → >=` | `for (var i = 0; i < pressure.length; i++) if (pressure[i] > 0) return +(` |
| 992 | `cmp < → <=` | `for (var i = 0; i < pressure.length; i++) if (pressure[i] > 0) return +(` |
| 992 | `num → 0` | `for (var i = 0; i < pressure.length; i++) if (pressure[i] > 0) return +(` |
| 996 | `num → 0` | `medianPressure: +_p(pressureMaskOn, 50).toFixed(2),` |
| 997 | `num → 0` | `p95Pressure: +_p(pressureMaskOn, 95).toFixed(2),` |
| 1007 | `cmp > → >=` | `obstructiveIndex: usageHours > 0 ? +(aCount('OA') / usageHours).toFixed(` |
| 1008 | `cmp > → >=` | `centralIndex: usageHours > 0 ? +(aCount('CA') / usageHours).toFixed(2) :` |
| 1009 | `cmp > → >=` | `hypopneaIndex: usageHours > 0 ? +(aCount('H') / usageHours).toFixed(2) :` |
| 1024 | `cmp > → >=` | `reraIndex: usageHours > 0 && aCount('RE') > 0 ? +(aCount('RE') / usageHo` |
| 1039 | `cmp > → >=` | `periodicBreathingPct: pbObserved && durSec > 0 ? +((pbSec / durSec) * 10` |
| 1042 | `num → 0` | `medianLeak: leak ? +_p(leakMaskOn, 50).toFixed(2) : null,` |
| 1043 | `num → 0` | `p95Leak: leak ? +_p(leakMaskOn, 95).toFixed(2) : null,` |
| 1048 | `cmp > → >=` | `return v > 24;` |
| 1048 | `num → 0` | `return v > 24;` |
| 1051 | `num → 0` | `100` |
| 1056 | `num → 0` | `respRateMedian: rrMaskOn.length ? +_p(rrMaskOn, 50).toFixed(1) : null,` |
| 1058 | `num → 0` | `tidVolMedian: tvMaskOn.length ? +_p(tvMaskOn, 50).toFixed(2) : null,` |
| 1059 | `num → 0` | `minVentMedian: mvMaskOn.length ? +_p(mvMaskOn, 50).toFixed(2) : null,` |
| 1066 | `cmp > → >=` | `return v > 0.3;` |
| 1066 | `num → 0` | `return v > 0.3;` |
| 1069 | `num → 0` | `100` |
| 1076 | `cmp > → >=` | `return v > 0.2;` |
| 1076 | `num → 0` | `return v > 0.2;` |
| 1079 | `num → 0` | `100` |
| 1082 | `bool && → \|\|` | `snorePressureCorr: snMaskOn.length && pressureMaskOn.length ? +_pearson(` |
| 1096 | `bool \|\| → &&` | `fname: meta.fname \|\| null,` |
| 1175 | `cmp < → <=` | `for (var i = 0; i < p.pressureMaskOn.length; i++) P.push(p.pressureMaskO` |
| 1176 | `cmp < → <=` | `if (p.epapMaskOn) for (var k = 0; k < p.epapMaskOn.length; k++) E.push(p` |
| 1177 | `cmp < → <=` | `for (var j = 0; j < p.leakMaskOn.length; j++) L.push(p.leakMaskOn[j]);` |
| 1193 | `cmp < → <=` | `if (p.rrMaskOn) for (var a = 0; a < p.rrMaskOn.length; a++) RR.push(p.rr` |
| 1194 | `cmp < → <=` | `if (p.tvMaskOn) for (var b = 0; b < p.tvMaskOn.length; b++) TV.push(p.tv` |
| 1195 | `cmp < → <=` | `if (p.mvMaskOn) for (var c = 0; c < p.mvMaskOn.length; c++) MV.push(p.mv` |
| 1196 | `cmp < → <=` | `if (p.snMaskOn) for (var d = 0; d < p.snMaskOn.length; d++) SN.push(p.sn` |
| 1197 | `cmp < → <=` | `if (p.flMaskOn) for (var e = 0; e < p.flMaskOn.length; e++) FL.push(p.fl` |
| 1198 | `bool && → \|\|` | `if (p.eprDelta != null && isFinite(p.eprDelta)) eprs.push(p.eprDelta);` |
| 1199 | `bool && → \|\|` | `if (p.breathCount != null && isFinite(p.breathCount)) {` |
| 1204 | `bool && → \|\|` | `if (p.ieRatio != null && isFinite(p.ieRatio)) {` |
| 1214 | `bool && → \|\|` | `maskOnLatency: pools[0].maskOnLatency != null && isFinite(pools[0].maskO` |
| 1215 | `cmp > → >=` | `residualAHI: totHours > 0 ? +(nA / totHours).toFixed(2) : null,` |
| 1216 | `cmp > → >=` | `obstructiveIndex: totHours > 0 ? +(nOA / totHours).toFixed(2) : null,` |
| 1217 | `cmp > → >=` | `centralIndex: totHours > 0 ? +(nCA / totHours).toFixed(2) : null,` |
| 1218 | `cmp > → >=` | `hypopneaIndex: totHours > 0 ? +(nH / totHours).toFixed(2) : null,` |
| 1222 | `cmp > → >=` | `reraIndex: totHours > 0 && nRE > 0 ? +(nRE / totHours).toFixed(2) : null` |
| 1222 | `bool && → \|\|` | `reraIndex: totHours > 0 && nRE > 0 ? +(nRE / totHours).toFixed(2) : null` |
| 1222 | `cmp > → >=` | `reraIndex: totHours > 0 && nRE > 0 ? +(nRE / totHours).toFixed(2) : null` |
| 1230 | `num → 0` | `medianPressure: +_p(P, 50).toFixed(2),` |
| 1231 | `num → 0` | `p95Pressure: +_p(P, 95).toFixed(2),` |
| 1259 | `bool && → \|\|` | `return v != null && isFinite(v);` |
| 1261 | `num → 0` | `return q.length ? +_p(q, 50).toFixed(2) : null;` |
| 1265 | `num → 0` | `eprDelta: eprs.length ? +_p(eprs, 50).toFixed(1) : null,` |
| 1266 | `num → 0` | `epap95: E.length ? +_p(E, 95).toFixed(2) : null,` |
| 1267 | `num → 0` | `medianLeak: L.length ? +_p(L, 50).toFixed(2) : null,` |
| 1268 | `num → 0` | `p95Leak: L.length ? +_p(L, 95).toFixed(2) : null,` |
| 1273 | `cmp > → >=` | `return v > 24;` |
| 1273 | `num → 0` | `return v > 24;` |
| 1276 | `num → 0` | `100` |
| 1282 | `num → 0` | `respRateMedian: RR.length ? +_p(RR, 50).toFixed(1) : null,` |
| 1284 | `num → 0` | `tidVolMedian: TV.length ? +_p(TV, 50).toFixed(2) : null,` |
| 1285 | `num → 0` | `minVentMedian: MV.length ? +_p(MV, 50).toFixed(2) : null,` |
| 1292 | `num → 0` | `return v > 0.3;` |
| 1292 | `cmp > → >=` | `return v > 0.3;` |
| 1295 | `num → 0` | `100` |
| 1302 | `cmp > → >=` | `return v > 0.2;` |
| 1302 | `num → 0` | `return v > 0.2;` |
| 1305 | `num → 0` | `100` |
| 1310 | `bool && → \|\|` | `snorePressureCorr: SN.length && SN.length === P.length ? +_pearson(SN, P` |
| 1310 | `eq === → !==` | `snorePressureCorr: SN.length && SN.length === P.length ? +_pearson(SN, P` |
| 1314 | `cmp > → >=` | `breathRate: haveBreaths && durSec > 0 ? +(breaths / (durSec / 60)).toFix` |
| 1314 | `bool && → \|\|` | `breathRate: haveBreaths && durSec > 0 ? +(breaths / (durSec / 60)).toFix` |
| 1314 | `num → 0` | `breathRate: haveBreaths && durSec > 0 ? +(breaths / (durSec / 60)).toFix` |
| 1315 | `cmp > → >=` | `ieRatio: ieW > 0 ? +(ieWsum / ieW).toFixed(2) : null` |
| 1332 | `cmp < → <=` | `for (var i = 0; i < n; i++) {` |
| 1333 | `cmp < → <=` | `pressure[i] = i < startBlank ? 0 : 10 + Math.sin(i / 500) * 0.05; // ~10` |
| 1333 | `num → 0` | `pressure[i] = i < startBlank ? 0 : 10 + Math.sin(i / 500) * 0.05; // ~10` |
| 1344 | `num → 0` | `return { t0Ms: Date.UTC(2026, 5, 13, 23, 14, 33), fs: fs, pressure: pres` |
| 1344 | `num → 0` | `return { t0Ms: Date.UTC(2026, 5, 13, 23, 14, 33), fs: fs, pressure: pres` |
| 1344 | `num → 0` | `return { t0Ms: Date.UTC(2026, 5, 13, 23, 14, 33), fs: fs, pressure: pres` |
| 1344 | `num → 0` | `return { t0Ms: Date.UTC(2026, 5, 13, 23, 14, 33), fs: fs, pressure: pres` |
| 1344 | `num → 0` | `return { t0Ms: Date.UTC(2026, 5, 13, 23, 14, 33), fs: fs, pressure: pres` |
| 1352 | `cmp < → <=` | `for (var i = 0; i < n; i++) a[i] = fn(i);` |
| 1353 | `num → 0` | `return { data: a, fs: fs, dim: dim, _spr: Math.round(fs * 60) };` |
| 1360 | `num → 0` | `t0 = Date.UTC(2026, 5, 12, 22, 28, 30);` |
| 1360 | `num → 0` | `t0 = Date.UTC(2026, 5, 12, 22, 28, 30);` |
| 1360 | `num → 0` | `t0 = Date.UTC(2026, 5, 12, 22, 28, 30);` |
| 1360 | `num → 0` | `t0 = Date.UTC(2026, 5, 12, 22, 28, 30);` |
| 1360 | `num → 0` | `t0 = Date.UTC(2026, 5, 12, 22, 28, 30);` |
| 1374 | `num → 0` | `return 10 + Math.sin(i / 50) * 0.1;` |
| 1376 | `num → 0` | `0.5,` |
| 1384 | `num → 0` | `0.5,` |
| 1392 | `num → 0` | `0.5,` |
| 1398 | `num → 0` | `return 14;` |
| 1400 | `num → 0` | `0.5,` |
| 1406 | `num → 0` | `return 0.5;` |
| 1408 | `num → 0` | `0.5,` |
| 1416 | `num → 0` | `0.5,` |
| 1422 | `num → 0` | `return 0.05;` |
| 1424 | `num → 0` | `0.5,` |
| 1430 | `num → 0` | `return 0.1;` |
| 1432 | `num → 0` | `0.5,` |
| 1440 | `num → 0` | `1 / 60,` |
| 1457 | `num → 0` | `25,` |
| 1463 | `num → 0` | `return 10;` |
| 1465 | `num → 0` | `25,` |
| 1473 | `cmp < → <=` | `for (var i = 0; i < n1; i++) {` |
| 1477 | `cmp < → <=` | `for (var k = 200; k < 220; k++) spo2[k] = 96 - (k - 200) * 0.35; // gent` |
| 1478 | `num → 0` | `for (var k2 = 220; k2 < 240; k2++) spo2[k2] = 89 + (k2 - 220) * 0.35;` |
| 1478 | `cmp < → <=` | `for (var k2 = 220; k2 < 240; k2++) spo2[k2] = 89 + (k2 - 220) * 0.35;` |
| 1478 | `num → 0` | `for (var k2 = 220; k2 < 240; k2++) spo2[k2] = 89 + (k2 - 220) * 0.35;` |
| 1481 | `cmp < → <=` | `for (var s = 400; s < 404; s++) spo2[s] = 96 - (s - 399) * 7; // ~28%/s ` |
| 1481 | `num → 0` | `for (var s = 400; s < 404; s++) spo2[s] = 96 - (s - 399) * 7; // ~28%/s ` |
| 1481 | `num → 0` | `for (var s = 400; s < 404; s++) spo2[s] = 96 - (s - 399) * 7; // ~28%/s ` |
| 1481 | `num → 0` | `for (var s = 400; s < 404; s++) spo2[s] = 96 - (s - 399) * 7; // ~28%/s ` |
| 1482 | `cmp < → <=` | `for (var s2 = 404; s2 < 420; s2++) {` |
| 1486 | `num → 0` | `pulse[400] = -1;` |
| 1487 | `num → 0` | `pulse[401] = -1;` |
| 1488 | `num → 0` | `pulse[402] = -1;` |
| 1489 | `num → 0` | `pulse[403] = -1;` |
| 1492 | `cmp < → <=` | `for (var z = 0; z < n1; z++) {` |
| 1502 | `num → 0` | `'SpO2.1s': { data: spo2, fs: 1, dim: '%', _spr: 60 },` |
| 1503 | `num → 0` | `'Pulse.1s': { data: pulse, fs: 1, dim: 'bpm', _spr: 60 }` |
| 1512 | `num → 0` | `{ class: 'Obstructive Apnea', durSec: 15, onsetSec: 100, tMs: t0 + 10000` |
| 1512 | `num → 0` | `{ class: 'Obstructive Apnea', durSec: 15, onsetSec: 100, tMs: t0 + 10000` |
| 1512 | `num → 0` | `{ class: 'Obstructive Apnea', durSec: 15, onsetSec: 100, tMs: t0 + 10000` |
| 1513 | `num → 0` | `{ class: 'Obstructive Apnea', durSec: 18, onsetSec: 200, tMs: t0 + 20000` |
| 1513 | `num → 0` | `{ class: 'Obstructive Apnea', durSec: 18, onsetSec: 200, tMs: t0 + 20000` |
| 1513 | `num → 0` | `{ class: 'Obstructive Apnea', durSec: 18, onsetSec: 200, tMs: t0 + 20000` |
| 1514 | `num → 0` | `{ class: 'Central Apnea', durSec: 12, onsetSec: 300, tMs: t0 + 300000 },` |
| 1514 | `num → 0` | `{ class: 'Central Apnea', durSec: 12, onsetSec: 300, tMs: t0 + 300000 },` |
| 1514 | `num → 0` | `{ class: 'Central Apnea', durSec: 12, onsetSec: 300, tMs: t0 + 300000 },` |
| 1515 | `num → 0` | `{ class: 'Hypopnea', durSec: 20, onsetSec: 400, tMs: t0 + 400000 }` |
| 1515 | `num → 0` | `{ class: 'Hypopnea', durSec: 20, onsetSec: 400, tMs: t0 + 400000 }` |
| 1515 | `num → 0` | `{ class: 'Hypopnea', durSec: 20, onsetSec: 400, tMs: t0 + 400000 }` |
| 1519 | `num → 0` | `.concat(opts.noRera ? [] : [{ class: 'RERA', durSec: 8, onsetSec: 500, t` |
| 1519 | `num → 0` | `.concat(opts.noRera ? [] : [{ class: 'RERA', durSec: 8, onsetSec: 500, t` |
| 1528 | `num → 0` | `{ class: 'Cheyne-Stokes', durSec: 0, onsetSec: 50, text: 'CSR Start', bo` |
| 1529 | `num → 0` | `{ class: 'Cheyne-Stokes', durSec: 0, onsetSec: 170, text: 'CSR End', bou` |
| 1532 | `num → 0` | `? [{ class: 'Cheyne-Stokes', durSec: 120, onsetSec: 50, tMs: t0 + 50000 ` |
| 1532 | `num → 0` | `? [{ class: 'Cheyne-Stokes', durSec: 120, onsetSec: 50, tMs: t0 + 50000 ` |
| 1566 | `num → 0` | `ok('near() REJECTS a value just outside the tolerance', !near(10, 10.75,` |
| 1566 | `num → 0` | `ok('near() REJECTS a value just outside the tolerance', !near(10, 10.75,` |
| 1566 | `num → 0` | `ok('near() REJECTS a value just outside the tolerance', !near(10, 10.75,` |
| 1575 | `num → 0` | `ok('maskOnLatency ≈ 0.5 min (30s)', near(m.maskOnLatency, 0.5, 0.05), m.` |
| 1584 | `num → 0` | `ok('largeLeakPct = 0 (no >24)', near(m.largeLeakPct, 0, 0.01), m.largeLe` |
| 1602 | `num → 0` | `ok('that off-mask gap ≈ 20 min', near((night.offMaskGaps[0] \|\| {}).gapMi` |
| 1605 | `cmp > → >=` | `ok('overnight stays monotonic (gap > 0)', night.offMaskGaps[0].gapMin > ` |
| 1608 | `num → 0` | `var lng = buildLongitudinal([{ therapyHours: 7 }, { therapyHours: 3.5 },` |
| 1609 | `num → 0` | `ok('compliancePct = 75 (3/4 nights ≥4h)', near(lng.compliancePct, 75, 0.` |
| 1613 | `num → 0` | `return { t0Ms: Date.UTC(2026, 5, day, 22, 30, 0), therapyHours: hrs, met` |
| 1613 | `num → 0` | `return { t0Ms: Date.UTC(2026, 5, day, 22, 30, 0), therapyHours: hrs, met` |
| 1613 | `num → 0` | `return { t0Ms: Date.UTC(2026, 5, day, 22, 30, 0), therapyHours: hrs, met` |
| 1615 | `num → 0` | `var hist = [mkN(1, 5, 8), mkN(2, 5.5, 7), mkN(3, 6, 6), mkN(4, 6.5, 5), ` |
| 1615 | `num → 0` | `var hist = [mkN(1, 5, 8), mkN(2, 5.5, 7), mkN(3, 6, 6), mkN(4, 6.5, 5), ` |
| 1617 | `cmp > → >=` | `ok('longitudinal: usageTrend7d > 0 (usage rising)', lng2.usageTrend7d > ` |
| 1619 | `num → 0` | `ok('longitudinal: ahiTrend30d mean ≈ 5 over 7 nights', near((lng2.ahiTre` |
| 1627 | `bool \|\| → &&` | `ok('longitudinal: crossNight block present (CPAPCross resolved in this r` |
| 1627 | `bool \|\| → &&` | `ok('longitudinal: crossNight block present (CPAPCross resolved in this r` |
| 1648 | `cmp < → <=` | `for (var i = 0; i < 30; i++) {` |
| 1649 | `num → 0` | `spA.push(98);` |
| 1650 | `num → 0` | `puA.push(60);` |
| 1652 | `num → 0` | `spA.push(82);` |
| 1654 | `num → 0` | `spA.push(67);` |
| 1656 | `cmp < → <=` | `for (var i = 32; i < 50; i++) {` |
| 1656 | `num → 0` | `for (var i = 32; i < 50; i++) {` |
| 1656 | `num → 0` | `for (var i = 32; i < 50; i++) {` |
| 1657 | `num → 0` | `spA.push(67);` |
| 1660 | `num → 0` | `var evA = { onset: 30, startIdx: 30, nadirIdx: 31, endIdx: 49, depth: 31` |
| 1660 | `num → 0` | `var evA = { onset: 30, startIdx: 30, nadirIdx: 31, endIdx: 49, depth: 31` |
| 1660 | `num → 0` | `var evA = { onset: 30, startIdx: 30, nadirIdx: 31, endIdx: 49, depth: 31` |
| 1660 | `num → 0` | `var evA = { onset: 30, startIdx: 30, nadirIdx: 31, endIdx: 49, depth: 31` |
| 1660 | `num → 0` | `var evA = { onset: 30, startIdx: 30, nadirIdx: 31, endIdx: 49, depth: 31` |
| 1667 | `cmp < → <=` | `for (var i = 0; i < 20; i++) {` |
| 1667 | `num → 0` | `for (var i = 0; i < 20; i++) {` |
| 1668 | `num → 0` | `spB.push(96);` |
| 1669 | `num → 0` | `puB.push(60);` |
| 1671 | `cmp < → <=` | `for (var i = 0; i < 30; i++) {` |
| 1672 | `num → 0` | `spB.push(96 - i * 0.27);` |
| 1675 | `cmp < → <=` | `for (var i = 0; i < 20; i++) {` |
| 1675 | `num → 0` | `for (var i = 0; i < 20; i++) {` |
| 1676 | `num → 0` | `spB.push(88 + i * 0.4);` |
| 1676 | `num → 0` | `spB.push(88 + i * 0.4);` |
| 1677 | `num → 0` | `puB.push(60);` |
| 1679 | `num → 0` | `var evB = { onset: 20, startIdx: 20, nadirIdx: 49, endIdx: 50, depth: 8,` |
| 1679 | `num → 0` | `var evB = { onset: 20, startIdx: 20, nadirIdx: 49, endIdx: 50, depth: 8,` |
| 1679 | `num → 0` | `var evB = { onset: 20, startIdx: 20, nadirIdx: 49, endIdx: 50, depth: 8,` |
| 1679 | `num → 0` | `var evB = { onset: 20, startIdx: 20, nadirIdx: 49, endIdx: 50, depth: 8,` |
| 1679 | `num → 0` | `var evB = { onset: 20, startIdx: 20, nadirIdx: 49, endIdx: 50, depth: 8,` |
| 1686 | `num → 0` | `var evC = { onset: 20, startIdx: 20, nadirIdx: 49, endIdx: 50, depth: 8,` |
| 1686 | `num → 0` | `var evC = { onset: 20, startIdx: 20, nadirIdx: 49, endIdx: 50, depth: 8,` |
| 1686 | `num → 0` | `var evC = { onset: 20, startIdx: 20, nadirIdx: 49, endIdx: 50, depth: 8,` |
| 1686 | `num → 0` | `var evC = { onset: 20, startIdx: 20, nadirIdx: 49, endIdx: 50, depth: 8,` |
| 1686 | `num → 0` | `var evC = { onset: 20, startIdx: 20, nadirIdx: 49, endIdx: 50, depth: 8,` |
| 1698 | `num → 0` | `{ class: 'Obstructive Apnea', durSec: 10, tMs: 1000, onsetSec: 1 },` |
| 1698 | `num → 0` | `{ class: 'Obstructive Apnea', durSec: 10, tMs: 1000, onsetSec: 1 },` |
| 1699 | `num → 0` | `{ class: 'Cheyne-Stokes', durSec: 60, tMs: 2000, onsetSec: 2 }` |
| 1699 | `num → 0` | `{ class: 'Cheyne-Stokes', durSec: 60, tMs: 2000, onsetSec: 2 }` |
| 1709 | `num → 0` | `{ class: 'Hypopnea', durSec: 10 }` |
| 1726 | `num → 0` | `{ class: 'PeriodicBreathing', durSec: 40, onsetSec: 300 }` |
| 1739 | `num → 0` | `var pbNoDur = periodicBreathingSpans([{ class: 'Cheyne-Stokes', durSec: ` |
| 1742 | `num → 0` | `var pbOrphan = periodicBreathingSpans([{ class: 'Cheyne-Stokes', durSec:` |
| 1742 | `num → 0` | `var pbOrphan = periodicBreathingSpans([{ class: 'Cheyne-Stokes', durSec:` |
| 1751 | `num → 0` | `500` |
| 1776 | `cmp < → <=` | `for (var i = 0; i < n; i++) a.push(fn(i));` |
| 1782 | `num → 0` | `pressureMaskOn: [10, 10, 10],` |
| 1782 | `num → 0` | `pressureMaskOn: [10, 10, 10],` |
| 1782 | `num → 0` | `pressureMaskOn: [10, 10, 10],` |
| 1792 | `num → 0` | `durSec: 14400,` |
| 1793 | `num → 0` | `maskOnLatency: 0.5,` |
| 1795 | `num → 0` | `pressureEnvIqr: 0.1` |
| 1802 | `eq === → !==` | `return i % 2 === 0 ? 10 : 7;` |
| 1802 | `num → 0` | `return i % 2 === 0 ? 10 : 7;` |
| 1804 | `cmp > → >=` | `ok('§F2 the OLD bare-IQR rule misfires on EPR (raw IQR > 1 on a FIXED ma` |
| 1804 | `num → 0` | `ok('§F2 the OLD bare-IQR rule misfires on EPR (raw IQR > 1 on a FIXED ma` |
| 1806 | `cmp <= → <` | `ok('§F2 … and its envelope IQR is ~0 despite a raw IQR of 3', /** @type ` |
| 1811 | `eq === → !==` | `return i % 2 === 0 ? base : base - 3;` |
| 1820 | `num → 0` | `return 10 + (1.5 * Math.floor(i / WIN)) / 7;` |
| 1824 | `cmp > → >=` | `ok('§F2 …and that envelope really is above the CPAP ceiling', bMode.envI` |
| 1825 | `cmp < → <=` | `ok('§F2 …and below the APAP floor — i.e. genuinely in the dead band', bM` |
| 1832 | `num → 0` | `return 10;` |
| 1850 | `bool \|\| → &&` | `ok('§1 night mode is ALWAYS null — even when every session agrees (a nig` |
| 1852 | `bool \|\| → &&` | `ok('§1 night mode is null when sessions disagree too', (nmSplit \|\| {}).m` |
| 1855 | `bool && → \|\|` | `nmAgree && nmAgree.pressureEnvIqr != null && isFinite(nmAgree.pressureEn` |
| 1855 | `bool && → \|\|` | `nmAgree && nmAgree.pressureEnvIqr != null && isFinite(nmAgree.pressureEn` |
| 1856 | `bool && → \|\|` | `nmAgree && nmAgree.pressureEnvIqr` |
| 1869 | `num → 0` | `ok('§1 longitudinal: < 7 nights ⇒ null (not enough evidence for a SETTIN` |
| 1869 | `num → 0` | `ok('§1 longitudinal: < 7 nights ⇒ null (not enough evidence for a SETTIN` |
| 1870 | `num → 0` | `ok('§1 longitudinal: a run of auto-titrating nights ⇒ APAP', classifyMod` |
| 1870 | `num → 0` | `ok('§1 longitudinal: a run of auto-titrating nights ⇒ APAP', classifyMod` |
| 1871 | `num → 0` | `ok('§1 longitudinal: a run of flat-envelope nights ⇒ CPAP', classifyMode` |
| 1871 | `num → 0` | `ok('§1 longitudinal: a run of flat-envelope nights ⇒ CPAP', classifyMode` |
| 1871 | `num → 0` | `ok('§1 longitudinal: a run of flat-envelope nights ⇒ CPAP', classifyMode` |
| 1872 | `num → 0` | `ok('§1 longitudinal: a run in the dead-band ⇒ null (still refuses to gue` |
| 1872 | `num → 0` | `ok('§1 longitudinal: a run in the dead-band ⇒ null (still refuses to gue` |
| 1872 | `num → 0` | `ok('§1 longitudinal: a run in the dead-band ⇒ null (still refuses to gue` |
| 1877 | `num → 0` | `var noisy = runOf(1.4, 8).concat([mkNight(0.2), mkNight(0.3)]); // 2 of ` |
| 1877 | `num → 0` | `var noisy = runOf(1.4, 8).concat([mkNight(0.2), mkNight(0.3)]); // 2 of ` |
| 1884 | `num → 0` | `ok('§1 buildLongitudinal on ONE night ⇒ mode null (a night is not a sett` |
| 1885 | `num → 0` | `ok('EDF session: EPR delta ≈ 3', near(sess.metrics.eprDelta, 3, 0.3), se` |
| 1886 | `num → 0` | `ok('EDF session: epap95 present (expiratory P95 ≈ 7)', near(sess.metrics` |
| 1887 | `num → 0` | `ok('EDF session: medianLeak ≈ 4.8 L/min (L/s×60)', near(sess.metrics.med` |
| 1888 | `num → 0` | `ok('EDF session: AHI = 4 apneas+hypopnea / hr', near(sess.metrics.residu` |
| 1889 | `num → 0` | `ok('EDF session: obstructiveIndex from 2 OA', near(sess.metrics.obstruct` |
| 1890 | `num → 0` | `ok('EDF session: reraIndex from 1 RERA', near(sess.metrics.reraIndex, 1 ` |
| 1898 | `bool && → \|\|` | `_noRera.metrics.residualAHI != null && _noRera.metrics.centralIndex != n` |
| 1901 | `num → 0` | `ok('EDF session: periodicBreathingPct = 20% (120/600 s)', near(sess.metr` |
| 1911 | `num → 0` | `ok('EDF session: spo2Nadir ≈ 89', near(sessO.oximetry.spo2Nadir, 89, 1.5` |
| 1968 | `bool && → \|\|` | `if (!sets && (input.PLD \|\| input.BRP \|\| input.SA2 \|\| input.EVE)) sets = ` |
| 1968 | `bool \|\| → &&` | `if (!sets && (input.PLD \|\| input.BRP \|\| input.SA2 \|\| input.EVE)) sets = ` |
| 1968 | `bool \|\| → &&` | `if (!sets && (input.PLD \|\| input.BRP \|\| input.SA2 \|\| input.EVE)) sets = ` |
| 1971 | `cmp < → <=` | `for (var i = 0; i < sets.length; i++) {` |
| 1972 | `bool \|\| → &&` | `var s = buildSessionFromEdf(sets[i], { fname: (sets[i] && sets[i]._fname` |
| 1972 | `bool && → \|\|` | `var s = buildSessionFromEdf(sets[i], { fname: (sets[i] && sets[i]._fname` |
| 1980 | `bool \|\| → &&` | `opts = opts \|\| {};` |
| 1982 | `negate: drop !` | `if (!night) throw new Error('CPAPDex.compute: need a cpap SignalFrame (w` |
| 1985 | `bool && → \|\|` | `var build = root && root.CpapFusion && root.CpapFusion.cpapBuildExport;` |
| 1985 | `bool && → \|\|` | `var build = root && root.CpapFusion && root.CpapFusion.cpapBuildExport;` |
| 1986 | `eq !== → ===` | `if (typeof build !== 'function') throw new Error('CPAPDex.compute: CpapF` |
| 2005 | `bool && → \|\|` | `return edf && edf.signals && edf.signals[label] ? edf.signals[label] : n` |
| 2005 | `bool && → \|\|` | `return edf && edf.signals && edf.signals[label] ? edf.signals[label] : n` |
| 2009 | `bool \|\| → &&` | `if (!sig \|\| !sig.data) return null;` |
| 2011 | `cmp < → <=` | `return idx < sig.data.length ? sig.data[idx] : null;` |
| 2015 | `bool \|\| → &&` | `if (!dateSig \|\| !dateSig.data \|\| !dateSig.data.length) return [];` |
| 2038 | `cmp < → <=` | `for (var r = 0; r < nRec; r++) {` |
| 2040 | `cmp >= → >` | `if (!(days >= 0) \|\| days > 24836) continue; // out-of-range Date ⇒ drop ` |
| 2040 | `cmp > → >=` | `if (!(days >= 0) \|\| days > 24836) continue; // out-of-range Date ⇒ drop ` |
| 2040 | `bool \|\| → &&` | `if (!(days >= 0) \|\| days > 24836) continue; // out-of-range Date ⇒ drop ` |
| 2048 | `cmp < → <=` | `if (on == null \|\| off == null \|\| on < 0 \|\| off < 0) continue;` |
| 2048 | `bool \|\| → &&` | `if (on == null \|\| off == null \|\| on < 0 \|\| off < 0) continue;` |
| 2048 | `cmp < → <=` | `if (on == null \|\| off == null \|\| on < 0 \|\| off < 0) continue;` |
| 2048 | `bool \|\| → &&` | `if (on == null \|\| off == null \|\| on < 0 \|\| off < 0) continue;` |
| 2048 | `bool \|\| → &&` | `if (on == null \|\| off == null \|\| on < 0 \|\| off < 0) continue;` |
| 2049 | `num → 0` | `sessions.push({ onMs: dateMs + STR_NOON_MS + Math.round(on) * 60000, off` |
| 2057 | `bool \|\| → &&` | `prescription[key] = s ? _strAt(s, r, s._spr \|\| 1, 0) : null;` |
| 2064 | `bool && → \|\|` | `deviceRera: rin != null && isFinite(rin) ? +rin.toFixed(2) : null,` |
| 2065 | `bool && → \|\|` | `deviceCsr: csr != null && isFinite(csr) ? +csr.toFixed(2) : null,` |
| 2095 | `negate: drop !` | `if (!nights \|\| !nights.length) return nights;` |
| 2095 | `bool \|\| → &&` | `if (!nights \|\| !nights.length) return nights;` |
| 2095 | `negate: drop !` | `if (!nights \|\| !nights.length) return nights;` |
| 2096 | `cmp < → <=` | `for (var n = 0; n < nights.length; n++) {` |
| 2098 | `negate: drop !` | `if (!night \|\| !night.sessions \|\| !night.sessions.length) continue;` |
| 2098 | `negate: drop !` | `if (!night \|\| !night.sessions \|\| !night.sessions.length) continue;` |
| 2100 | `bool && → \|\|` | `var off = ev && ev.clock_offset;` |
| 2101 | `bool && → \|\|` | `var sec = off && off.offset_sec;` |
| 2102 | `bool \|\| → &&` | `if (sec == null \|\| !isFinite(sec)) continue; // UNKNOWN ⇒ no corrected v` |
| 2102 | `negate: drop !` | `if (sec == null \|\| !isFinite(sec)) continue; // UNKNOWN ⇒ no corrected v` |
| 2103 | `num → 0` | `var ms = sec * 1000;` |
| 2105 | `cmp < → <=` | `for (var i = 0; i < night.sessions.length; i++) {` |
| 2113 | `bool \|\| → &&` | `reference: off.reference \|\| null,` |
| 2114 | `bool \|\| → &&` | `method: off.method \|\| null,` |
| 2137 | `negate: drop !` | `if (!nights \|\| !nights.length \|\| !bag) return nights;` |
| 2137 | `bool \|\| → &&` | `if (!nights \|\| !nights.length \|\| !bag) return nights;` |
| 2137 | `bool \|\| → &&` | `if (!nights \|\| !nights.length \|\| !bag) return nights;` |
| 2137 | `negate: drop !` | `if (!nights \|\| !nights.length \|\| !bag) return nights;` |
| 2137 | `negate: drop !` | `if (!nights \|\| !nights.length \|\| !bag) return nights;` |
| 2140 | `negate: drop !` | `if (!Object.prototype.hasOwnProperty.call(bag, sid)) continue;` |
| 2142 | `bool && → \|\|` | `var t = ev && ev.start_time_ms;` |
| 2143 | `bool \|\| → &&` | `if (t == null \|\| !isFinite(t)) continue; // unjoinable — never guessed i` |
| 2143 | `negate: drop !` | `if (t == null \|\| !isFinite(t)) continue; // unjoinable — never guessed i` |
| 2144 | `num → 0` | `var d = Math.floor(t / 86400000);` |
| 2146 | `negate: drop !` | `if (!Object.prototype.hasOwnProperty.call(byDay, d)) byDay[d] = ev;` |
| 2148 | `cmp < → <=` | `for (var n = 0; n < nights.length; n++) {` |
| 2150 | `bool && → \|\|` | `var t0 = night && (night.t0Ms != null ? night.t0Ms : night.stats && nigh` |
| 2152 | `num → 0` | `var day = Math.floor(t0 / 86400000);` |
| 2182 | `num → 0` | `var band = Math.max(2, 0.5 * Math.max(deviceCsr, pbPct));` |
| 2184 | `cmp < → <=` | `if (deviceCsr < CSR_PB_FLOOR && pbPct < CSR_PB_FLOOR) verdict = 'both-ne` |
| 2184 | `cmp < → <=` | `if (deviceCsr < CSR_PB_FLOOR && pbPct < CSR_PB_FLOOR) verdict = 'both-ne` |
| 2185 | `cmp > → >=` | `else if (delta > band) verdict = 'discrepancy';` |
| 2186 | `cmp < → <=` | `else if (delta < -band) verdict = 'pb-broader';` |
| 2192 | `bool \|\| → &&` | `if (!nights \|\| !nights.length \|\| !summary \|\| !summary.length) return nig` |
| 2195 | `cmp < → <=` | `for (var n = 0; n < nights.length; n++) {` |
| 2197 | `bool && → \|\|` | `var t0 = night && (night.t0Ms != null ? night.t0Ms : night.stats && nigh` |
| 2201 | `num → 0` | `var rec = byDay[Math.floor(t0 / 86400000)] \|\| byDay[Math.floor(t0 / 8640` |

## `ecgdex-dsp.js` — 476 survivor(s)

| line | operator | source |
|---:|---|---|
| 30 | `cmp < → <=` | `if (a.length < 2) return 0;` |
| 68 | `cmp < → <=` | `for (let i = 1; i < a.length; i++) if (Math.abs(a[i] - a[i - 1]) > 50) n` |
| 68 | `cmp > → >=` | `for (let i = 1; i < a.length; i++) if (Math.abs(a[i] - a[i - 1]) > 50) n` |
| 69 | `cmp > → >=` | `return a.length > 1 ? (n / (a.length - 1)) * 100 : 0;` |
| 73 | `num → 0` | `for (let i = 1; i < a.length; i++) if (Math.abs(a[i] - a[i - 1]) > 50) n` |
| 73 | `cmp > → >=` | `for (let i = 1; i < a.length; i++) if (Math.abs(a[i] - a[i - 1]) > 50) n` |
| 73 | `cmp < → <=` | `for (let i = 1; i < a.length; i++) if (Math.abs(a[i] - a[i - 1]) > 50) n` |
| 83 | `negate: drop !` | `if (!a.length) return 0;` |
| 92 | `cmp < → <=` | `for (let i = 0; i < a.length; i++) if (a[i] < m) m = a[i];` |
| 92 | `cmp < → <=` | `for (let i = 0; i < a.length; i++) if (a[i] < m) m = a[i];` |
| 97 | `cmp > → >=` | `for (let i = 0; i < a.length; i++) if (a[i] > m) m = a[i];` |
| 97 | `cmp < → <=` | `for (let i = 0; i < a.length; i++) if (a[i] > m) m = a[i];` |
| 129 | `cmp < → <=` | `if (v < mn) mn = v;` |
| 130 | `cmp > → >=` | `if (v > mx) mx = v;` |
| 140 | `cmp > → >=` | `if (bins[b] > modeCnt) {` |
| 234 | `num → 0` | `const base = 730 - 90 * Math.min(1, (t / durSec) * 1.2);` |
| 234 | `num → 0` | `const base = 730 - 90 * Math.min(1, (t / durSec) * 1.2);` |
| 234 | `num → 0` | `const base = 730 - 90 * Math.min(1, (t / durSec) * 1.2);` |
| 235 | `num → 0` | `return { base, vagal: 0.3, stage: 'Wake' };` |
| 240 | `cmp < → <=` | `if (t < 7 * 60) {` |
| 245 | `cmp < → <=` | `else if (ph < 0.18) {` |
| 249 | `num → 0` | `} else if (ph < 0.4) {` |
| 249 | `cmp < → <=` | `} else if (ph < 0.4) {` |
| 253 | `cmp < → <=` | `} else if (ph < 0.66) {` |
| 258 | `cmp < → <=` | `else if (ph < 0.82) {` |
| 265 | `num → 0` | `vagal = 0.42;` |
| 276 | `num → 0` | `{ t0: 38 * 60, t1: 66 * 60, cyc: 28, depth: 0.15 }, // moderate cluster` |
| 276 | `num → 0` | `{ t0: 38 * 60, t1: 66 * 60, cyc: 28, depth: 0.15 }, // moderate cluster` |
| 276 | `num → 0` | `{ t0: 38 * 60, t1: 66 * 60, cyc: 28, depth: 0.15 }, // moderate cluster` |
| 277 | `num → 0` | `{ t0: 120 * 60, t1: 152 * 60, cyc: 24, depth: 0.2 } // stronger cluster` |
| 277 | `num → 0` | `{ t0: 120 * 60, t1: 152 * 60, cyc: 24, depth: 0.2 } // stronger cluster` |
| 277 | `num → 0` | `{ t0: 120 * 60, t1: 152 * 60, cyc: 24, depth: 0.2 } // stronger cluster` |
| 278 | `cmp < → <=` | `].filter((w) => w.t0 < durSec);` |
| 282 | `num → 0` | `{ t0: 88 * 60, t1: 88 * 60 + 40, kind: 'noise' },` |
| 282 | `num → 0` | `{ t0: 88 * 60, t1: 88 * 60 + 40, kind: 'noise' },` |
| 282 | `num → 0` | `{ t0: 88 * 60, t1: 88 * 60 + 40, kind: 'noise' },` |
| 283 | `num → 0` | `{ t0: 175 * 60, t1: 175 * 60 + 55, kind: 'flat' }` |
| 283 | `num → 0` | `{ t0: 175 * 60, t1: 175 * 60 + 55, kind: 'flat' }` |
| 283 | `num → 0` | `{ t0: 175 * 60, t1: 175 * 60 + 55, kind: 'flat' }` |
| 284 | `cmp < → <=` | `].filter((w) => w.t0 < durSec);` |
| 299 | `cmp <= → <` | `const respHz0 = opts.respHz != null && opts.respHz >= 4 / 60 && opts.res` |
| 299 | `cmp >= → >` | `const respHz0 = opts.respHz != null && opts.respHz >= 4 / 60 && opts.res` |
| 322 | `num → 0` | `return s === 'REM' ? 0.055 : s === 'Wake' ? 0.045 : s === 'N1' ? 0.022 :` |
| 328 | `num → 0` | `let sinusRR = 900; // running "expected" sinus interval` |
| 331 | `num → 0` | `while (t < durSec - 0.4) {` |
| 331 | `cmp < → <=` | `while (t < durSec - 0.4) {` |
| 354 | `cmp >= → >` | `if (t >= w.t0 && t < w.t1) {` |
| 354 | `cmp < → <=` | `if (t >= w.t0 && t < w.t1) {` |
| 355 | `num → 0` | `const ramp = Math.min(1, (t - w.t0) / 40) * Math.min(1, (w.t1 - t) / 40)` |
| 355 | `num → 0` | `const ramp = Math.min(1, (t - w.t0) / 40) * Math.min(1, (w.t1 - t) / 40)` |
| 366 | `cmp < → <=` | `else if (rnd() < 0.0017) {` |
| 372 | `cmp < → <=` | `} else if (rnd() < 0.0011) {` |
| 372 | `num → 0` | `} else if (rnd() < 0.0011) {` |
| 375 | `num → 0` | `const coupling = sinusRR * (0.58 + 0.12 * rnd());` |
| 375 | `num → 0` | `const coupling = sinusRR * (0.58 + 0.12 * rnd());` |
| 379 | `num → 0` | `rr = Math.max(360, Math.min(1700, rr));` |
| 391 | `cmp < → <=` | `for (let i = 0; i < N; i++) {` |
| 393 | `num → 0` | `ecg[i] = 45 * Math.sin(2 * Math.PI * 0.22 * ti) + 30 * Math.sin(2 * Math` |
| 393 | `num → 0` | `ecg[i] = 45 * Math.sin(2 * Math.PI * 0.22 * ti) + 30 * Math.sin(2 * Math` |
| 393 | `num → 0` | `ecg[i] = 45 * Math.sin(2 * Math.PI * 0.22 * ti) + 30 * Math.sin(2 * Math` |
| 398 | `num → 0` | `[-185, 95, 20], // P` |
| 398 | `num → 0` | `[-185, 95, 20], // P` |
| 399 | `num → 0` | `[-28, -115, 9], // Q` |
| 399 | `num → 0` | `[-28, -115, 9], // Q` |
| 401 | `num → 0` | `[28, -240, 11], // S` |
| 401 | `num → 0` | `[28, -240, 11], // S` |
| 402 | `num → 0` | `[240, 255, 54] // T (later + wider → physiological QT/QTc)` |
| 402 | `num → 0` | `[240, 255, 54] // T (later + wider → physiological QT/QTc)` |
| 404 | `cmp < → <=` | `for (let k = 0; k < beatT.length; k++) {` |
| 408 | `eq === → !==` | `const isPAC = gtType[k] === 'S';` |
| 416 | `cmp > → >=` | `a = cms === 0 ? amp * 1.5 * respAmp : cms > 40 ? amp * 1.9 : amp * 0.35;` |
| 416 | `num → 0` | `a = cms === 0 ? amp * 1.5 * respAmp : cms > 40 ? amp * 1.9 : amp * 0.35;` |
| 416 | `num → 0` | `a = cms === 0 ? amp * 1.5 * respAmp : cms > 40 ? amp * 1.9 : amp * 0.35;` |
| 416 | `num → 0` | `a = cms === 0 ? amp * 1.5 * respAmp : cms > 40 ? amp * 1.9 : amp * 0.35;` |
| 417 | `eq === → !==` | `if (cms === 0 \|\| cms === 26 \|\| cms === -28) ww = w * 1.9; // broaden QRS` |
| 417 | `eq === → !==` | `if (cms === 0 \|\| cms === 26 \|\| cms === -28) ww = w * 1.9; // broaden QRS` |
| 417 | `eq === → !==` | `if (cms === 0 \|\| cms === 26 \|\| cms === -28) ww = w * 1.9; // broaden QRS` |
| 417 | `bool \|\| → &&` | `if (cms === 0 \|\| cms === 26 \|\| cms === -28) ww = w * 1.9; // broaden QRS` |
| 417 | `num → 0` | `if (cms === 0 \|\| cms === 26 \|\| cms === -28) ww = w * 1.9; // broaden QRS` |
| 417 | `bool \|\| → &&` | `if (cms === 0 \|\| cms === 26 \|\| cms === -28) ww = w * 1.9; // broaden QRS` |
| 417 | `num → 0` | `if (cms === 0 \|\| cms === 26 \|\| cms === -28) ww = w * 1.9; // broaden QRS` |
| 420 | `bool && → \|\|` | `if (isPAC && cms === -200) {` |
| 420 | `eq === → !==` | `if (isPAC && cms === -200) {` |
| 420 | `num → 0` | `if (isPAC && cms === -200) {` |
| 421 | `num → 0` | `a = amp * 0.7 * respAmp;` |
| 425 | `cmp <= → <` | `for (let i = lo; i <= hi; i++) {` |
| 432 | `cmp < → <=` | `for (let i = 0; i < N; i++) ecg[i] += gauss() * 8;` |
| 438 | `eq === → !==` | `if (w.kind === 'flat') {` |
| 439 | `bool \|\| → &&` | `const v = ecg[s] \|\| 0;` |
| 440 | `cmp < → <=` | `for (let i = s; i < e; i++) ecg[i] = v;` |
| 443 | `cmp < → <=` | `for (let i = s; i < e; i++) ecg[i] = (rnd() - 0.5) * 2600;` |
| 443 | `num → 0` | `for (let i = s; i < e; i++) ecg[i] = (rnd() - 0.5) * 2600;` |
| 443 | `num → 0` | `for (let i = s; i < e; i++) ecg[i] = (rnd() - 0.5) * 2600;` |
| 449 | `cmp < → <=` | `for (let i = 0; i < N; i++) {` |
| 451 | `cmp > → >=` | `if (v > 32767) v = 32767;` |
| 451 | `num → 0` | `if (v > 32767) v = 32767;` |
| 452 | `cmp < → <=` | `if (v < -32768) v = -32768;` |
| 452 | `num → 0` | `if (v < -32768) v = -32768;` |
| 457 | `num → 0` | `const t0 = ambulatory ? Date.UTC(2026, 5, 1, 12, 14, 0) : Date.UTC(2026,` |
| 457 | `num → 0` | `const t0 = ambulatory ? Date.UTC(2026, 5, 1, 12, 14, 0) : Date.UTC(2026,` |
| 457 | `num → 0` | `const t0 = ambulatory ? Date.UTC(2026, 5, 1, 12, 14, 0) : Date.UTC(2026,` |
| 457 | `num → 0` | `const t0 = ambulatory ? Date.UTC(2026, 5, 1, 12, 14, 0) : Date.UTC(2026,` |
| 457 | `num → 0` | `const t0 = ambulatory ? Date.UTC(2026, 5, 1, 12, 14, 0) : Date.UTC(2026,` |
| 457 | `num → 0` | `const t0 = ambulatory ? Date.UTC(2026, 5, 1, 12, 14, 0) : Date.UTC(2026,` |
| 458 | `num → 0` | `const devRR = gtRR.map((r, k) => ({ tsMs: t0 + Math.round(beatT[k] * 100` |
| 465 | `cmp < → <=` | `for (let s = 0; s < Mhr; s++) {` |
| 466 | `cmp < → <=` | `while (_bi < beatT.length - 1 && beatT[_bi + 1] <= s) _bi++;` |
| 466 | `cmp <= → <` | `while (_bi < beatT.length - 1 && beatT[_bi + 1] <= s) _bi++;` |
| 466 | `bool && → \|\|` | `while (_bi < beatT.length - 1 && beatT[_bi + 1] <= s) _bi++;` |
| 467 | `num → 0` | `let hr = 60000 / gtRR[Math.min(_bi, gtRR.length - 1)] + gauss() * 0.7;` |
| 467 | `num → 0` | `let hr = 60000 / gtRR[Math.min(_bi, gtRR.length - 1)] + gauss() * 0.7;` |
| 468 | `num → 0` | `devHR[s] = { tsMs: t0 + s * 1000, hr };` |
| 470 | `num → 0` | `for (let s = 1; s < Mhr; s++) devHR[s].hr = 0.55 * devHR[s].hr + 0.45 * ` |
| 470 | `num → 0` | `for (let s = 1; s < Mhr; s++) devHR[s].hr = 0.55 * devHR[s].hr + 0.45 * ` |
| 485 | `cmp < → <=` | `for (let i = 0; i < Macc; i++) {` |
| 487 | `num → 0` | `const breath = 30 * Math.sin(respPhase(ti));` |
| 488 | `num → 0` | `const phase = (ti % 600) / 600; // 10-min macro-cycle` |
| 488 | `num → 0` | `const phase = (ti % 600) / 600; // 10-min macro-cycle` |
| 491 | `cmp < → <=` | `if (phase < 0.12) {` |
| 491 | `num → 0` | `if (phase < 0.12) {` |
| 495 | `cmp < → <=` | `else if (phase < 0.39) {` |
| 495 | `num → 0` | `else if (phase < 0.39) {` |
| 496 | `num → 0` | `stepHz = 1.83;` |
| 499 | `num → 0` | `stepHz = 1.5;` |
| 501 | `num → 0` | `const step = moving ? 190 * Math.sin(2 * Math.PI * stepHz * ti) : 0;` |
| 502 | `num → 0` | `const sway = moving ? 80 * Math.sin(2 * Math.PI * stepHz * ti * 0.5) : 0` |
| 502 | `num → 0` | `const sway = moving ? 80 * Math.sin(2 * Math.PI * stepHz * ti * 0.5) : 0` |
| 503 | `num → 0` | `const nz = moving ? 26 : 6;` |
| 505 | `num → 0` | `tsMs: t0 + Math.round(ti * 1000),` |
| 506 | `num → 0` | `x: Math.round(40 + sway + gauss() * nz),` |
| 507 | `num → 0` | `y: Math.round(120 + sway * 0.6 + breath * 0.4 + gauss() * nz),` |
| 507 | `num → 0` | `y: Math.round(120 + sway * 0.6 + breath * 0.4 + gauss() * nz),` |
| 507 | `num → 0` | `y: Math.round(120 + sway * 0.6 + breath * 0.4 + gauss() * nz),` |
| 508 | `num → 0` | `z: Math.round(980 + step + breath * 0.5 + gauss() * nz)` |
| 508 | `num → 0` | `z: Math.round(980 + step + breath * 0.5 + gauss() * nz)` |
| 518 | `num → 0` | `[25, -18, 990],` |
| 518 | `num → 0` | `[25, -18, 990],` |
| 519 | `num → 0` | `[970, -40, 90],` |
| 519 | `num → 0` | `[970, -40, 90],` |
| 520 | `num → 0` | `[-965, 30, 70],` |
| 520 | `num → 0` | `[-965, 30, 70],` |
| 520 | `num → 0` | `[-965, 30, 70],` |
| 521 | `num → 0` | `[60, 35, -985]` |
| 521 | `num → 0` | `[60, 35, -985]` |
| 524 | `cmp < → <=` | `for (let i = 0; i < Macc; i++) {` |
| 537 | `eq === → !==` | `const shiftP = sa.stage === 'Wake' ? 0.0016 : sa.stage === 'REM' ? 0.000` |
| 537 | `eq === → !==` | `const shiftP = sa.stage === 'Wake' ? 0.0016 : sa.stage === 'REM' ? 0.000` |
| 537 | `num → 0` | `const shiftP = sa.stage === 'Wake' ? 0.0016 : sa.stage === 'REM' ? 0.000` |
| 537 | `num → 0` | `const shiftP = sa.stage === 'Wake' ? 0.0016 : sa.stage === 'REM' ? 0.000` |
| 537 | `num → 0` | `const shiftP = sa.stage === 'Wake' ? 0.0016 : sa.stage === 'REM' ? 0.000` |
| 538 | `cmp < → <=` | `if (rnd() < (shiftP / ACCfs) * 4) posture = POSTURES[Math.floor(rnd() * ` |
| 539 | `eq === → !==` | `let act = sa.stage === 'Wake' ? 1.0 : sa.stage === 'N1' ? 0.32 : sa.stag` |
| 539 | `num → 0` | `let act = sa.stage === 'Wake' ? 1.0 : sa.stage === 'N1' ? 0.32 : sa.stag` |
| 539 | `num → 0` | `let act = sa.stage === 'Wake' ? 1.0 : sa.stage === 'N1' ? 0.32 : sa.stag` |
| 539 | `num → 0` | `let act = sa.stage === 'Wake' ? 1.0 : sa.stage === 'N1' ? 0.32 : sa.stag` |
| 543 | `cmp < → <=` | `if (sa.stage === 'REM' && rnd() < 0.25 / ACCfs) act += 0.45;` |
| 543 | `eq === → !==` | `if (sa.stage === 'REM' && rnd() < 0.25 / ACCfs) act += 0.45;` |
| 543 | `bool && → \|\|` | `if (sa.stage === 'REM' && rnd() < 0.25 / ACCfs) act += 0.45;` |
| 543 | `num → 0` | `if (sa.stage === 'REM' && rnd() < 0.25 / ACCfs) act += 0.45;` |
| 543 | `num → 0` | `if (sa.stage === 'REM' && rnd() < 0.25 / ACCfs) act += 0.45;` |
| 545 | `bool && → \|\|` | `if (ti >= w.t0 && ti < w.t1) {` |
| 545 | `cmp >= → >` | `if (ti >= w.t0 && ti < w.t1) {` |
| 545 | `cmp < → <=` | `if (ti >= w.t0 && ti < w.t1) {` |
| 547 | `cmp > → >=` | `if (ph > w.cyc - 3) act += 0.55;` |
| 547 | `num → 0` | `if (ph > w.cyc - 3) act += 0.55;` |
| 551 | `num → 0` | `devACC[i] = { tsMs: t0 + Math.round(ti * 1000), x: Math.round(posture[0]` |
| 551 | `num → 0` | `devACC[i] = { tsMs: t0 + Math.round(ti * 1000), x: Math.round(posture[0]` |
| 551 | `num → 0` | `devACC[i] = { tsMs: t0 + Math.round(ti * 1000), x: Math.round(posture[0]` |
| 551 | `num → 0` | `devACC[i] = { tsMs: t0 + Math.round(ti * 1000), x: Math.round(posture[0]` |
| 577 | `cmp < → <=` | `for (tt = 0; tt < durSec; tt += step) {` |
| 586 | `bool \|\| → &&` | `scenario: opts.scenario \|\| 'overnight'` |
| 603 | `cmp < → <=` | `for (let i = 0; i < N; i++) {` |
| 613 | `cmp < → <=` | `for (let i = 0; i < N; i++) {` |
| 624 | `cmp < → <=` | `for (let i = 2; i < N - 2; i++) d[i] = 2 * bp[i + 1] + bp[i + 2] - bp[i ` |
| 626 | `cmp < → <=` | `for (let i = 0; i < N; i++) sq[i] = d[i] * d[i];` |
| 630 | `cmp < → <=` | `for (let i = 0; i < N; i++) {` |
| 632 | `cmp >= → >` | `if (i >= win) acc -= sq[i - win];` |
| 648 | `num → 0` | `const stride = Math.max(1, Math.floor(N / 20000)),` |
| 650 | `cmp < → <=` | `for (let i = 0; i < N; i += stride) {` |
| 652 | `bool && → \|\|` | `if (isFinite(v) && v > 0) s.push(v);` |
| 652 | `cmp > → >=` | `if (isFinite(v) && v > 0) s.push(v);` |
| 659 | `cmp > → >=` | `if (p > 0) return p;` |
| 663 | `cmp < → <=` | `for (let i = 0; i < initN; i++) if (integ[i] > mx) mx = integ[i];` |
| 663 | `cmp > → >=` | `for (let i = 0; i < initN; i++) if (integ[i] > mx) mx = integ[i];` |
| 682 | `num → 0` | `let SPKI = 0.5 * init,` |
| 683 | `num → 0` | `NPKI = 0.1 * init,` |
| 684 | `num → 0` | `THRI = NPKI + 0.25 * (SPKI - NPKI);` |
| 711 | `cmp < → <=` | `for (let i = 1; i < N - 1; i++) {` |
| 712 | `cmp > → >=` | `if (integ[i] > integ[i - 1] && integ[i] >= integ[i + 1] && integ[i] > TH` |
| 712 | `cmp >= → >` | `if (integ[i] > integ[i - 1] && integ[i] >= integ[i + 1] && integ[i] > TH` |
| 712 | `cmp > → >=` | `if (integ[i] > integ[i - 1] && integ[i] >= integ[i + 1] && integ[i] > TH` |
| 713 | `cmp > → >=` | `if (i - last > refractory) {` |
| 718 | `cmp <= → <` | `for (let j = Math.max(0, i - w); j <= Math.min(N - 1, i + w); j++) {` |
| 719 | `cmp > → >=` | `if (int16[j] > bv) {` |
| 725 | `cmp >= → >` | `if (last >= 0) {` |
| 727 | `cmp > → >=` | `if (d > 0) {` |
| 729 | `cmp > → >=` | `if (rr.length > 8) rr.shift();` |
| 731 | `cmp < → <=` | `for (let k = 0; k < rr.length; k++) s += rr[k];` |
| 738 | `num → 0` | `NPKI = 0.125 * integ[i] + 0.875 * NPKI;` |
| 738 | `num → 0` | `NPKI = 0.125 * integ[i] + 0.875 * NPKI;` |
| 740 | `cmp >= → >` | `} else if (integ[i] > integ[i - 1] && integ[i] >= integ[i + 1]) {` |
| 740 | `cmp > → >=` | `} else if (integ[i] > integ[i - 1] && integ[i] >= integ[i + 1]) {` |
| 740 | `bool && → \|\|` | `} else if (integ[i] > integ[i - 1] && integ[i] >= integ[i + 1]) {` |
| 748 | `num → 0` | `THRI = NPKI + 0.25 * (SPKI - NPKI);` |
| 777 | `cmp > → >=` | `if (!(mx > 0)) return 0;` |
| 781 | `cmp < → <=` | `for (let s0 = 0; s0 < N; s0 += W) {` |
| 784 | `cmp < → <=` | `for (let i = s0; i < e; i++) {` |
| 786 | `cmp > → >=` | `if (a > m) m = a;` |
| 788 | `cmp >= → >` | `if (e - s0 >= W / 2) maxima.push(m);` |
| 805 | `num → 0` | `refractory = Math.round(0.22 * fs);` |
| 807 | `cmp < → <=` | `for (let i = 0; i < N; i++) {` |
| 809 | `cmp > → >=` | `if (a > mx) mx = a;` |
| 814 | `num → 0` | `let thr = 0.35 * ref,` |
| 819 | `cmp < → <=` | `for (let i = 1; i < N - 1; i++) {` |
| 821 | `num → 0` | `env = 0.999 * env + 0.001 * a;` |
| 821 | `num → 0` | `env = 0.999 * env + 0.001 * a;` |
| 822 | `num → 0` | `const t = Math.max(0.3 * env, 0.18 * ref);` |
| 823 | `cmp >= → >` | `if (bp[i] > bp[i - 1] && bp[i] >= bp[i + 1] && bp[i] > t && i - last > r` |
| 823 | `cmp > → >=` | `if (bp[i] > bp[i - 1] && bp[i] >= bp[i + 1] && bp[i] > t && i - last > r` |
| 823 | `cmp > → >=` | `if (bp[i] > bp[i - 1] && bp[i] >= bp[i + 1] && bp[i] > t && i - last > r` |
| 823 | `cmp > → >=` | `if (bp[i] > bp[i - 1] && bp[i] >= bp[i + 1] && bp[i] > t && i - last > r` |
| 838 | `cmp < → <=` | `for (let k = 0; k < peaks.length; k++) {` |
| 844 | `cmp <= → <` | `for (let j = Math.max(1, i - w); j <= Math.min(bp.length - 2, i + w); j+` |
| 845 | `cmp > → >=` | `if (bp[j] > bv) {` |
| 856 | `cmp > → >=` | `if (!isFinite(delta) \|\| Math.abs(delta) > 1) delta = 0;` |
| 856 | `bool \|\| → &&` | `if (!isFinite(delta) \|\| Math.abs(delta) > 1) delta = 0;` |
| 882 | `cmp > → >=` | `return m2 > 0 ? m4 / (m2 * m2) : 0;` |
| 894 | `cmp < → <=` | `for (let k = 1; k < n; k++) rr[k] = (times[k] - times[k - 1]) * 1000;` |
| 895 | `num → 0` | `rr[0] = rr[1] \|\| 1000;` |
| 900 | `cmp < → <=` | `for (let k = 0; k < n; k++) {` |
| 902 | `cmp < → <=` | `while (bp2 < peaksB.length && peaksB[bp2] < target - tolB) bp2++;` |
| 902 | `cmp < → <=` | `while (bp2 < peaksB.length && peaksB[bp2] < target - tolB) bp2++;` |
| 905 | `cmp <= → <` | `for (let j = Math.max(0, bp2 - 2); j < peaksB.length && peaksB[j] <= tar` |
| 905 | `cmp < → <=` | `for (let j = Math.max(0, bp2 - 2); j < peaksB.length && peaksB[j] <= tar` |
| 905 | `bool && → \|\|` | `for (let j = Math.max(0, bp2 - 2); j < peaksB.length && peaksB[j] <= tar` |
| 906 | `cmp <= → <` | `if (Math.abs(peaksB[j] - target) <= tolB) {` |
| 933 | `cmp < → <=` | `for (let j = s + 1; j < e; j++) {` |
| 934 | `cmp < → <=` | `if (Math.abs(int16[j] - prev) < 2) {` |
| 936 | `cmp > → >=` | `if (flatRun > maxFlat) maxFlat = flatRun;` |
| 938 | `cmp > → >=` | `if (Math.abs(int16[j]) > 31000) railHit++;` |
| 941 | `cmp > → >=` | `const flatBad = maxFlat > 0.2 * fs \|\| railHit > 3; // >200 ms flat` |
| 941 | `bool \|\| → &&` | `const flatBad = maxFlat > 0.2 * fs \|\| railHit > 3; // >200 ms flat` |
| 941 | `cmp > → >=` | `const flatBad = maxFlat > 0.2 * fs \|\| railHit > 3; // >200 ms flat` |
| 947 | `cmp >= → >` | `const rrOK = rrk >= 300 && rrk <= 2000;` |
| 947 | `bool && → \|\|` | `const rrOK = rrk >= 300 && rrk <= 2000;` |
| 947 | `cmp <= → <` | `const rrOK = rrk >= 300 && rrk <= 2000;` |
| 947 | `num → 0` | `const rrOK = rrk >= 300 && rrk <= 2000;` |
| 949 | `cmp < → <=` | `if (k > 1 && k < n - 1) {` |
| 949 | `bool && → \|\|` | `if (k > 1 && k < n - 1) {` |
| 949 | `cmp > → >=` | `if (k > 1 && k < n - 1) {` |
| 957 | `cmp < → <=` | `for (let j = s; j < e; j++) {` |
| 958 | `cmp < → <=` | `if (int16[j] < mn) mn = int16[j];` |
| 959 | `cmp > → >=` | `if (int16[j] > mx) mx = int16[j];` |
| 962 | `cmp > → >=` | `const ampOK = amp > 180 && amp < 6000 ? 1 : amp <= 180 ? 0 : 0.4;` |
| 962 | `cmp <= → <` | `const ampOK = amp > 180 && amp < 6000 ? 1 : amp <= 180 ? 0 : 0.4;` |
| 962 | `cmp < → <=` | `const ampOK = amp > 180 && amp < 6000 ? 1 : amp <= 180 ? 0 : 0.4;` |
| 965 | `num → 0` | `if (flatBad) q *= 0.15;` |
| 985 | `num → 0` | `flatBadPct: n > 0 ? +((100 * nFlat) / n).toFixed(2) : null` |
| 996 | `num → 0` | `sqiThr = sqiThr == null ? 0.3 : sqiThr;` |
| 1018 | `cmp >= → >` | `if (j !== k && sqi[j] >= sqiThr && rr[j] >= 300 && rr[j] <= 2000) seg.pu` |
| 1018 | `cmp >= → >` | `if (j !== k && sqi[j] >= sqiThr && rr[j] >= 300 && rr[j] <= 2000) seg.pu` |
| 1018 | `cmp <= → <` | `if (j !== k && sqi[j] >= sqiThr && rr[j] >= 300 && rr[j] <= 2000) seg.pu` |
| 1018 | `num → 0` | `if (j !== k && sqi[j] >= sqiThr && rr[j] >= 300 && rr[j] <= 2000) seg.pu` |
| 1023 | `cmp < → <=` | `const rangeBad = sqi[k] < sqiThr \|\| nn[k] < 300 \|\| nn[k] > 2000;` |
| 1023 | `bool \|\| → &&` | `const rangeBad = sqi[k] < sqiThr \|\| nn[k] < 300 \|\| nn[k] > 2000;` |
| 1023 | `cmp < → <=` | `const rangeBad = sqi[k] < sqiThr \|\| nn[k] < 300 \|\| nn[k] > 2000;` |
| 1023 | `num → 0` | `const rangeBad = sqi[k] < sqiThr \|\| nn[k] < 300 \|\| nn[k] > 2000;` |
| 1024 | `cmp > → >=` | `const ectopic = med && dev > ectopyThr;` |
| 1026 | `bool \|\| → &&` | `nn[k] = med \|\| nn[k + 1] \|\| nn[k - 1] \|\| 1000;` |
| 1026 | `bool \|\| → &&` | `nn[k] = med \|\| nn[k + 1] \|\| nn[k - 1] \|\| 1000;` |
| 1026 | `num → 0` | `nn[k] = med \|\| nn[k + 1] \|\| nn[k - 1] \|\| 1000;` |
| 1028 | `negate: drop !` | `if (ectopic && !rangeBad) nEctopy++;` |
| 1028 | `bool && → \|\|` | `if (ectopic && !rangeBad) nEctopy++;` |
| 1034 | `cmp >= → >` | `for (let k = 0; k < n; k++) if (sqi[k] >= sqiThr) nGood++;` |
| 1045 | `cmp <= → <` | `if (d <= 0) continue;` |
| 1046 | `cmp > → >=` | `if (d > GAP_S) {` |
| 1051 | `cmp > → >=` | `const spanSec = n > 1 ? tt[n - 1] - tt[0] : 0;` |
| 1055 | `num → 0` | `const analyzablePct = +(cleanBeatPct * Math.min(1, coveragePct / 100)).t` |
| 1060 | `num → 0` | `correctionRate: +((nCorr / n) * 100).toFixed(2),` |
| 1096 | `cmp < → <=` | `if (n < 20) {` |
| 1096 | `num → 0` | `if (n < 20) {` |
| 1097 | `cmp < → <=` | `for (let k = 0; k < n; k++) out.set(secAbs(k), 1);` |
| 1103 | `cmp < → <=` | `if (S < 1) return out;` |
| 1107 | `cmp < → <=` | `for (let k = 0; k < n; k++) {` |
| 1109 | `cmp < → <=` | `if (s >= 0 && s < S) {` |
| 1109 | `bool && → \|\|` | `if (s >= 0 && s < S) {` |
| 1111 | `bool && → \|\|` | `qsum[s] += sqi && Number.isFinite(sqi[k]) ? sqi[k] : 1;` |
| 1122 | `cmp < → <=` | `for (let i = 0; i < S; i++) {` |
| 1136 | `cmp > → >=` | `winSqi[i] = cAcc > 0 ? qAcc / cAcc : 0;` |
| 1140 | `cmp > → >=` | `for (let i = 0; i < S; i++) if (winCnt[i] > 0) active.push(i);` |
| 1140 | `cmp < → <=` | `for (let i = 0; i < S; i++) if (winCnt[i] > 0) active.push(i);` |
| 1158 | `cmp >= → >` | `const trust = (z) => (z <= 0 ? 1 : z >= C ? 0 : (1 - (z / C) * (z / C)) ` |
| 1158 | `cmp <= → <` | `const trust = (z) => (z <= 0 ? 1 : z >= C ? 0 : (1 - (z / C) * (z / C)) ` |
| 1159 | `cmp < → <=` | `for (let i = 0; i < S; i++) {` |
| 1160 | `cmp <= → <` | `if (winCnt[i] <= 0) continue;` |
| 1174 | `cmp < → <=` | `if (!peaks \|\| peaks.length < 20) return new Map();` |
| 1174 | `num → 0` | `if (!peaks \|\| peaks.length < 20) return new Map();` |
| 1177 | `cmp < → <=` | `for (let k = 0; k < peaks.length; k++) times[k] = peaks[k] / fs; // seco` |
| 1194 | `cmp < → <=` | `if (N < 12) return { tp: null, vlf: null, lf: null, hf: null, lfhf: null` |
| 1219 | `cmp > → >=` | `const fNyq = _meanRRs > 0 ? 1 / (2 * _meanRRs) : fHi;` |
| 1229 | `cmp < → <=` | `for (let kf = 0; kf < nfFull; kf++) {` |
| 1259 | `cmp < → <=` | `if (f < fHi) {` |
| 1261 | `cmp < → <=` | `if (f < 0.04) vlf += e;` |
| 1262 | `cmp < → <=` | `else if (f < 0.15) lf += e;` |
| 1265 | `cmp > → >=` | `if (P > peakP) {` |
| 1273 | `cmp > → >=` | `sc = tpFull > 0 ? variance / tpFull : 1;` |
| 1292 | `cmp > → >=` | `lfhf: hf > 0 ? +(lf / hf).toFixed(3) : null,` |
| 1294 | `cmp < → <=` | `hfAboveNyquist: fNyq < fHi,` |
| 1301 | `num → 0` | `if (N < 16) return null;` |
| 1305 | `cmp < → <=` | `for (let i = 0; i < N; i++) {` |
| 1339 | `cmp < → <=` | `if (N < m + 2) return null;` |
| 1342 | `cmp < → <=` | `for (let i = 0; i < N - m; i++) {` |
| 1343 | `cmp < → <=` | `for (let j = i + 1; j < N - m; j++) {` |
| 1345 | `cmp <= → <` | `while (k < m && Math.abs(a[i + k] - a[j + k]) <= r) k++;` |
| 1348 | `cmp <= → <` | `if (Math.abs(a[i + m] - a[j + m]) <= r) A++;` |
| 1352 | `bool \|\| → &&` | `if (B === 0 \|\| A === 0) return null;` |
| 1363 | `cmp > → >=` | `if (f[k] > maxC) maxC = f[k];` |
| 1372 | `cmp < → <=` | `for (let i = L; i < N - L; i++) {` |
| 1373 | `cmp > → >=` | `const isAnchor = sign > 0 ? a[i] > a[i - 1] : a[i] < a[i - 1];` |
| 1373 | `cmp < → <=` | `const isAnchor = sign > 0 ? a[i] > a[i - 1] : a[i] < a[i - 1];` |
| 1375 | `cmp > → >=` | `if (Math.abs(a[i] - a[i - 1]) / a[i - 1] > 0.05) continue;` |
| 1378 | `cmp < → <=` | `if (win.length < 3) return null;` |
| 1380 | `cmp < → <=` | `for (let k = 0; k < 5; k++) {` |
| 1390 | `cmp < → <=` | `if (N < 4) return null;` |
| 1392 | `cmp < → <=` | `for (let i = 1; i < N; i++) d.push(a[i] - a[i - 1]);` |
| 1393 | `cmp > → >=` | `const s = d.map((v) => (v > 0 ? 1 : v < 0 ? -1 : 0));` |
| 1393 | `cmp < → <=` | `const s = d.map((v) => (v > 0 ? 1 : v < 0 ? -1 : 0));` |
| 1394 | `cmp < → <=` | `for (let i = 0; i < s.length; i++) {` |
| 1395 | `cmp > → >=` | `if (s[i] === 0) s[i] = i > 0 ? s[i - 1] : 1;` |
| 1446 | `cmp <= → <` | `for (let s0 = w0; s0 + SUB <= w1 + 1e-6; s0 += SUB) {` |
| 1450 | `cmp < → <=` | `for (let k = 0; k < segT.length; k++) {` |
| 1451 | `cmp >= → >` | `if (segT[k] >= s0 && segT[k] < s1) {` |
| 1451 | `cmp < → <=` | `if (segT[k] >= s0 && segT[k] < s1) {` |
| 1457 | `num → 0` | `if (sSeg.length < 30) continue;` |
| 1457 | `cmp < → <=` | `if (sSeg.length < 30) continue;` |
| 1458 | `num → 0` | `const r = lombScargle(sSeg, sT, 160).respRate;` |
| 1459 | `cmp > → >=` | `if (r != null && isFinite(r) && r > 0) rates.push(r);` |
| 1459 | `bool && → \|\|` | `if (r != null && isFinite(r) && r > 0) rates.push(r);` |
| 1459 | `bool && → \|\|` | `if (r != null && isFinite(r) && r > 0) rates.push(r);` |
| 1461 | `cmp < → <=` | `if (rates.length < 3) return null;` |
| 1463 | `cmp > → >=` | `if (!(m > 0)) return null;` |
| 1472 | `bool \|\| → &&` | `winSec = winSec \|\| 300;` |
| 1472 | `num → 0` | `winSec = winSec \|\| 300;` |
| 1477 | `cmp <= → <` | `for (let w0 = 0; w0 <= tEnd; w0 += winSec) {` |
| 1482 | `cmp < → <=` | `while (i < N && tt[i] < w1) {` |
| 1482 | `cmp < → <=` | `while (i < N && tt[i] < w1) {` |
| 1489 | `cmp >= → >` | `if (seg.length >= 20) {` |
| 1489 | `num → 0` | `if (seg.length >= 20) {` |
| 1491 | `num → 0` | `const ls = lombScargle(seg, segT, 160);` |
| 1543 | `cmp < → <=` | `if (!epochs \|\| epochs.length < 12) return null; // need ≥ ~1 h` |
| 1551 | `cmp >= → >` | `if (cur.length >= 3) windows.push({ tMin: wStart, epochs: cur });` |
| 1558 | `cmp < → <=` | `if (windows.length < 3) return null;` |
| 1572 | `num → 0` | `const xs = pts.map((p) => p.tMin / 60); // hours` |
| 1588 | `cmp < → <=` | `if (sdSlope < -0.015) {` |
| 1588 | `num → 0` | `if (sdSlope < -0.015) {` |
| 1591 | `cmp > → >=` | `} else if (sdSlope > 0.015) {` |
| 1591 | `num → 0` | `} else if (sdSlope > 0.015) {` |
| 1662 | `cmp < → <=` | `for (let i = 0; i < N; i++) {` |
| 1665 | `cmp <= → <` | `for (let k = -half; k <= half; k++) {` |
| 1681 | `cmp < → <=` | `for (let i = 0; i < M; i++) {` |
| 1683 | `cmp < → <=` | `while (j < N - 2 && xs[j + 1] < g) j++;` |
| 1683 | `cmp < → <=` | `while (j < N - 2 && xs[j + 1] < g) j++;` |
| 1688 | `cmp > → >=` | `const f = x1 > x0 ? (g - x0) / (x1 - x0) : 0;` |
| 1696 | `cmp < → <=` | `for (let i = 0; i < N; i++) {` |
| 1701 | `cmp >= → >` | `if (u >= 0 && u < N) {` |
| 1712 | `num → 0` | `const hi = _maHalf(x, Math.max(1, Math.round(0.3 * fs))); // drop > ~0.4` |
| 1715 | `cmp < → <=` | `for (let i = 0; i < x.length; i++) o[i] = hi[i] - lo[i];` |
| 1720 | `num → 0` | `const w0 = 2 * Math.PI * Math.max(0.05, f0),` |
| 1723 | `cmp < → <=` | `for (let i = 0; i < N; i++) {` |
| 1724 | `cmp > → >=` | `const xm = i > 0 ? x[i - 1] : x[i],` |
| 1725 | `cmp < → <=` | `xp = i < N - 1 ? x[i + 1] : x[i];` |
| 1733 | `bool \|\| → &&` | `if (n < 60 \|\| !refIdx \|\| refIdx.length < n) return null;` |
| 1733 | `bool \|\| → &&` | `if (n < 60 \|\| !refIdx \|\| refIdx.length < n) return null;` |
| 1733 | `num → 0` | `if (n < 60 \|\| !refIdx \|\| refIdx.length < n) return null;` |
| 1736 | `cmp < → <=` | `for (let k = 0; k < n; k++) {` |
| 1739 | `cmp <= → <` | `for (let j = Math.max(0, c - 2); j <= Math.min(int16.length - 1, c + 2);` |
| 1739 | `cmp > → >=` | `for (let j = Math.max(0, c - 2); j <= Math.min(int16.length - 1, c + 2);` |
| 1744 | `cmp < → <=` | `for (let k = 0; k < n; k++) hrAbs[k] = 60000 / nn[k];` |
| 1744 | `num → 0` | `for (let k = 0; k < n; k++) hrAbs[k] = 60000 / nn[k];` |
| 1745 | `num → 0` | `const hrR = _detrendMov(hrAbs, 40); // resp-band HR oscillation (RSA)` |
| 1761 | `cmp > → >=` | `if (!isFinite(t1 - t0) \|\| t1 - t0 > CVHR_MAX_SPAN_S) return null;` |
| 1762 | `num → 0` | `const M = Math.max(16, Math.floor((t1 - t0) * FS));` |
| 1763 | `cmp < → <=` | `if (M < 16) return null;` |
| 1763 | `num → 0` | `if (M < 16) return null;` |
| 1765 | `cmp < → <=` | `for (let i = 0; i < M; i++) grid[i] = t0 + i / FS;` |
| 1787 | `num → 0` | `const edrPeriod = _autocorrPeriod(edrB, FS, 2.5, 10);` |
| 1788 | `cmp >= → >` | `const respFromEDR = edrPeriod ? +(60 / edrPeriod).toFixed(1) : respHint ` |
| 1788 | `cmp <= → <` | `const respFromEDR = edrPeriod ? +(60 / edrPeriod).toFixed(1) : respHint ` |
| 1788 | `bool && → \|\|` | `const respFromEDR = edrPeriod ? +(60 / edrPeriod).toFixed(1) : respHint ` |
| 1788 | `bool && → \|\|` | `const respFromEDR = edrPeriod ? +(60 / edrPeriod).toFixed(1) : respHint ` |
| 1788 | `num → 0` | `const respFromEDR = edrPeriod ? +(60 / edrPeriod).toFixed(1) : respHint ` |
| 1788 | `num → 0` | `const respFromEDR = edrPeriod ? +(60 / edrPeriod).toFixed(1) : respHint ` |
| 1789 | `cmp >= → >` | `const f0 = respFromEDR >= 6 && respFromEDR <= 24 ? respFromEDR / 60 : 0.` |
| 1789 | `cmp <= → <` | `const f0 = respFromEDR >= 6 && respFromEDR <= 24 ? respFromEDR / 60 : 0.` |
| 1789 | `bool && → \|\|` | `const f0 = respFromEDR >= 6 && respFromEDR <= 24 ? respFromEDR / 60 : 0.` |
| 1789 | `num → 0` | `const f0 = respFromEDR >= 6 && respFromEDR <= 24 ? respFromEDR / 60 : 0.` |
| 1789 | `num → 0` | `const f0 = respFromEDR >= 6 && respFromEDR <= 24 ? respFromEDR / 60 : 0.` |
| 1789 | `num → 0` | `const f0 = respFromEDR >= 6 && respFromEDR <= 24 ? respFromEDR / 60 : 0.` |
| 1794 | `num → 0` | `const wN = Math.max(16, Math.round(60 * FS)),` |
| 1794 | `num → 0` | `const wN = Math.max(16, Math.round(60 * FS)),` |
| 1795 | `num → 0` | `wStep = Math.max(8, Math.round(30 * FS));` |
| 1800 | `cmp < → <=` | `for (let i = s; i < e; i++) {` |
| 1812 | `cmp <= → <` | `for (let s = 0; s + wN <= M; s += wStep) {` |
| 1816 | `cmp > → >=` | `if (lp > bestPLV) {` |
| 1826 | `num → 0` | `const rsaAmp = Math.max(0, quant(hrBarr, 0.92) - quant(hrBarr, 0.08));` |
| 1826 | `num → 0` | `const rsaAmp = Math.max(0, quant(hrBarr, 0.92) - quant(hrBarr, 0.08));` |
| 1827 | `cmp > → >=` | `const rsaRatio = meanHRabs - rsaAmp / 2 > 0 ? (meanHRabs + rsaAmp / 2) /` |
| 1828 | `num → 0` | `const rsaAmpNorm = Math.min(1, rsaAmp / (0.1 * meanHRabs \|\| 1));` |
| 1828 | `bool \|\| → &&` | `const rsaAmpNorm = Math.min(1, rsaAmp / (0.1 * meanHRabs \|\| 1));` |
| 1829 | `num → 0` | `const couplingStrength = Math.max(0, Math.min(1, 0.65 * plv + 0.35 * rsa` |
| 1829 | `num → 0` | `const couplingStrength = Math.max(0, Math.min(1, 0.65 * plv + 0.35 * rsa` |
| 1831 | `num → 0` | `const NB = 16;` |
| 1834 | `cmp < → <=` | `for (let i = bestS; i < Math.min(M, bestS + wN); i++) {` |
| 1836 | `cmp < → <=` | `if (ph < 0) ph += 2 * Math.PI;` |
| 1842 | `cmp < → <=` | `for (let b = 0; b < NB; b++) {` |
| 1843 | `cmp >= → >` | `phaseCurve.push(binN[b] >= 2 ? +(binSum[b] / binN[b]).toFixed(1) : null)` |
| 1847 | `bool && → \|\|` | `if (epochs && epochs.length) {` |
| 1849 | `num → 0` | `const w0 = e.tMin * 60,` |
| 1850 | `num → 0` | `w1 = w0 + 300;` |
| 1853 | `cmp < → <=` | `if (s1 - s0 < wN) {` |
| 1858 | `cmp <= → <` | `for (let s = s0; s + wN <= s1; s += wStep) {` |
| 1899 | `cmp < → <=` | `if (N < 60) return { events: [], index: null, hrSeries: [] };` |
| 1916 | `cmp > → >=` | `if (!isFinite(tEnd) \|\| tEnd > CVHR_MAX_SPAN_S) return { events: [], inde` |
| 1919 | `cmp < → <=` | `for (let s = 0; s < M; s++) {` |
| 1920 | `cmp < → <=` | `while (j < N - 1 && tt[j + 1] < s) j++;` |
| 1920 | `cmp < → <=` | `while (j < N - 1 && tt[j + 1] < s) j++;` |
| 1925 | `cmp < → <=` | `for (let s = 0; s < M; s++) {` |
| 1928 | `cmp <= → <` | `for (let k = -2; k <= 2; k++) {` |
| 1930 | `bool && → \|\|` | `if (u >= 0 && u < M) {` |
| 1930 | `cmp >= → >` | `if (u >= 0 && u < M) {` |
| 1930 | `cmp < → <=` | `if (u >= 0 && u < M) {` |
| 1942 | `cmp < → <=` | `for (let s = 0; s < M; s++) {` |
| 1944 | `cmp > → >=` | `if (s > 2 * half) acc -= src[s - 2 * half - 1];` |
| 1950 | `cmp < → <=` | `for (let s = 0; s < M; s++) {` |
| 1953 | `cmp <= → <` | `for (let k = -half; k <= half; k++) {` |
| 1955 | `cmp < → <=` | `if (u >= 0 && u < M) {` |
| 1955 | `cmp >= → >` | `if (u >= 0 && u < M) {` |
| 1955 | `bool && → \|\|` | `if (u >= 0 && u < M) {` |
| 1967 | `cmp < → <=` | `for (let s = 0; s < M; s++) res[s] = hiCut[s] - lo[s]; // apnea-band sig` |
| 1970 | `cmp < → <=` | `for (let s = 0; s < M; s++) {` |
| 1973 | `cmp <= → <` | `for (let k = -12; k <= 12; k++) {` |
| 1973 | `num → 0` | `for (let k = -12; k <= 12; k++) {` |
| 1973 | `num → 0` | `for (let k = -12; k <= 12; k++) {` |
| 1975 | `cmp >= → >` | `if (u >= 0 && u < M) {` |
| 1975 | `cmp < → <=` | `if (u >= 0 && u < M) {` |
| 1975 | `bool && → \|\|` | `if (u >= 0 && u < M) {` |
| 1985 | `num → 0` | `let lastT = -100;` |
| 1986 | `cmp < → <=` | `for (let s = 8; s < M - 8; s++) {` |
| 1987 | `cmp < → <=` | `if (env[s] < ENV_ON) continue; // not a sustained oscillation → skip (re` |
| 1988 | `cmp <= → <` | `if (res[s] < res[s - 1] && res[s] <= res[s + 1] && res[s] < -2.4) {` |
| 1988 | `cmp < → <=` | `if (res[s] < res[s - 1] && res[s] <= res[s + 1] && res[s] < -2.4) {` |
| 1988 | `cmp < → <=` | `if (res[s] < res[s - 1] && res[s] <= res[s + 1] && res[s] < -2.4) {` |
| 1988 | `bool && → \|\|` | `if (res[s] < res[s - 1] && res[s] <= res[s + 1] && res[s] < -2.4) {` |
| 1988 | `bool && → \|\|` | `if (res[s] < res[s - 1] && res[s] <= res[s + 1] && res[s] < -2.4) {` |
| 1988 | `num → 0` | `if (res[s] < res[s - 1] && res[s] <= res[s + 1] && res[s] < -2.4) {` |
| 1991 | `cmp < → <=` | `for (let u = s + 8; u < Math.min(M, s + 48); u++) {` |
| 1992 | `cmp > → >=` | `if (res[u] > pk) {` |
| 1999 | `cmp > → >=` | `if (amp >= 5 && period >= 14 && period <= 46 && s - lastT > 14) {` |
| 1999 | `cmp <= → <` | `if (amp >= 5 && period >= 14 && period <= 46 && s - lastT > 14) {` |
| 1999 | `cmp >= → >` | `if (amp >= 5 && period >= 14 && period <= 46 && s - lastT > 14) {` |
| 1999 | `cmp >= → >` | `if (amp >= 5 && period >= 14 && period <= 46 && s - lastT > 14) {` |
| 1999 | `bool && → \|\|` | `if (amp >= 5 && period >= 14 && period <= 46 && s - lastT > 14) {` |
| 1999 | `num → 0` | `if (amp >= 5 && period >= 14 && period <= 46 && s - lastT > 14) {` |
| 1999 | `bool && → \|\|` | `if (amp >= 5 && period >= 14 && period <= 46 && s - lastT > 14) {` |
| 1999 | `bool && → \|\|` | `if (amp >= 5 && period >= 14 && period <= 46 && s - lastT > 14) {` |
| 1999 | `num → 0` | `if (amp >= 5 && period >= 14 && period <= 46 && s - lastT > 14) {` |
| 2006 | `num → 0` | `const hours = tEnd / 3600;` |
| 2007 | `cmp > → >=` | `const index = hours > 0 ? +(events.length / hours).toFixed(1) : 0;` |
| 2024 | `bool \|\| → &&` | `const fs = accFs \|\| 4;` |
| 2025 | `cmp < → <=` | `if (!deviceACC \|\| !epochs \|\| !epochs.length \|\| deviceACC.length < fs * 3` |
| 2025 | `bool \|\| → &&` | `if (!deviceACC \|\| !epochs \|\| !epochs.length \|\| deviceACC.length < fs * 3` |
| 2025 | `num → 0` | `if (!deviceACC \|\| !epochs \|\| !epochs.length \|\| deviceACC.length < fs * 3` |
| 2030 | `cmp >= → >` | `const off = baseOffset >= -2 && baseOffset <= durSec ? baseOffset : 0;` |
| 2030 | `cmp <= → <` | `const off = baseOffset >= -2 && baseOffset <= durSec ? baseOffset : 0;` |
| 2030 | `bool && → \|\|` | `const off = baseOffset >= -2 && baseOffset <= durSec ? baseOffset : 0;` |
| 2034 | `cmp < → <=` | `for (let i = 1; i < N; i++) dmv[i] = Math.abs(vm[i] - vm[i - 1]);` |

## `glucodex-dsp.js` — 433 survivor(s)

| line | operator | source |
|---:|---|---|
| 23 | `cmp < → <=` | `for (let i = 0; i < a.length; i++) {` |
| 35 | `cmp < → <=` | `for (let i = 0; i < a.length; i++) {` |
| 41 | `cmp > → >=` | `return n > 1 ? Math.sqrt(s / (n - 1)) : 0;` |
| 48 | `eq === → !==` | `if (lo === hi) return sorted[lo];` |
| 53 | `num → 0` | `return quantile(s, 0.5);` |
| 70 | `num → 0` | `return new Date(instantMs).getTimezoneOffset() * 60000;` |
| 73 | `negate: drop !` | `if (!isFinite(n)) return null;` |
| 74 | `num → 0` | `if (n < 1e11) n = n * 1000;` |
| 74 | `cmp < → <=` | `if (n < 1e11) n = n * 1000;` |
| 75 | `cmp < → <=` | `if (n < 1e11 \|\| n > 4e12) return null;` |
| 75 | `bool \|\| → &&` | `if (n < 1e11 \|\| n > 4e12) return null;` |
| 75 | `cmp > → >=` | `if (n < 1e11 \|\| n > 4e12) return null;` |
| 77 | `num → 0` | `return { tMs: n - off, offsetMin: -off / 60000 };` |
| 81 | `eq === → !==` | `var sign = zs[0] === '-' ? -1 : 1;` |
| 82 | `num → 0` | `return sign * (parseInt(zs.slice(1, 3), 10) * 60 + parseInt(zs.slice(3, ` |
| 82 | `num → 0` | `return sign * (parseInt(zs.slice(1, 3), 10) * 60 + parseInt(zs.slice(3, ` |
| 82 | `num → 0` | `return sign * (parseInt(zs.slice(1, 3), 10) * 60 + parseInt(zs.slice(3, ` |
| 93 | `cmp >= → >` | `return lmo >= 1 && lmo <= 12 && ld >= 1 && ld <= 31 ? { d: ld, mo: lmo }` |
| 93 | `cmp <= → <` | `return lmo >= 1 && lmo <= 12 && ld >= 1 && ld <= 31 ? { d: ld, mo: lmo }` |
| 93 | `cmp >= → >` | `return lmo >= 1 && lmo <= 12 && ld >= 1 && ld <= 31 ? { d: ld, mo: lmo }` |
| 93 | `bool && → \|\|` | `return lmo >= 1 && lmo <= 12 && ld >= 1 && ld <= 31 ? { d: ld, mo: lmo }` |
| 93 | `bool && → \|\|` | `return lmo >= 1 && lmo <= 12 && ld >= 1 && ld <= 31 ? { d: ld, mo: lmo }` |
| 93 | `cmp <= → <` | `return lmo >= 1 && lmo <= 12 && ld >= 1 && ld <= 31 ? { d: ld, mo: lmo }` |
| 93 | `bool && → \|\|` | `return lmo >= 1 && lmo <= 12 && ld >= 1 && ld <= 31 ? { d: ld, mo: lmo }` |
| 95 | `cmp > → >=` | `if (a > 12) return { d: a, mo: b };` |
| 95 | `num → 0` | `if (a > 12) return { d: a, mo: b };` |
| 96 | `num → 0` | `if (b > 12) return { d: b, mo: a };` |
| 96 | `cmp > → >=` | `if (b > 12) return { d: b, mo: a };` |
| 105 | `eq !== → ===` | `var pref = preferDMY !== false,` |
| 117 | `cmp < → <=` | `for (var i = 0; i < list.length; i++) {` |
| 125 | `bool \|\| → &&` | `var mD = mA \|\| mC ? null : s.match(RE_D); // §5.5 — bare date (nutrition` |
| 136 | `cmp > → >=` | `if (a > 12) sawDMY = true;` |
| 136 | `num → 0` | `if (a > 12) sawDMY = true;` |
| 137 | `cmp > → >=` | `if (b > 12) sawMDY = true;` |
| 138 | `bool && → \|\|` | `if (sawDMY && sawMDY) break;` |
| 154 | `bool \|\| → &&` | `se = se \|\| 0;` |
| 158 | `bool \|\| → &&` | `if (dd.getUTCFullYear() !== y \|\| dd.getUTCMonth() !== mo0 \|\| dd.getUTCDa` |
| 158 | `bool \|\| → &&` | `if (dd.getUTCFullYear() !== y \|\| dd.getUTCMonth() !== mo0 \|\| dd.getUTCDa` |
| 159 | `eq === → !==` | `if (h === 24 && mi === 0 && se === 0 && ms === 0) return day0 + 86400000` |
| 159 | `eq === → !==` | `if (h === 24 && mi === 0 && se === 0 && ms === 0) return day0 + 86400000` |
| 159 | `eq === → !==` | `if (h === 24 && mi === 0 && se === 0 && ms === 0) return day0 + 86400000` |
| 159 | `num → 0` | `if (h === 24 && mi === 0 && se === 0 && ms === 0) return day0 + 86400000` |
| 160 | `cmp > → >=` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 160 | `cmp > → >=` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 160 | `bool \|\| → &&` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 160 | `bool \|\| → &&` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 160 | `bool \|\| → &&` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 160 | `bool \|\| → &&` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 160 | `bool \|\| → &&` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 160 | `bool \|\| → &&` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 160 | `bool \|\| → &&` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 166 | `eq === → !==` | `var dmyLocked = opts.dmyLocked === true; // set by _ckResolveDMY — order` |
| 174 | `num → 0` | `if (/^\\d{10,13}$/.test(s)) return _ckNumEpoch(parseInt(s, 10));` |
| 174 | `num → 0` | `if (/^\\d{10,13}$/.test(s)) return _ckNumEpoch(parseInt(s, 10));` |
| 189 | `negate: drop !` | `if (!dm) return null; // row contradicts the file's proven order → hones` |
| 251 | `cmp > → >=` | `if (counts[k] > bn) {` |
| 268 | `cmp < → <=` | `for (let c = 0; c < ncol; c++) {` |
| 276 | `cmp >= → >` | `if (c >= cells.length) continue;` |
| 280 | `bool && → \|\|` | `if (isFinite(parseTimestamp(cell)) && /[:\\-/]/.test(cell) && cell.length` |
| 280 | `cmp >= → >` | `if (isFinite(parseTimestamp(cell)) && /[:\\-/]/.test(cell) && cell.length` |
| 280 | `bool && → \|\|` | `if (isFinite(parseTimestamp(cell)) && /[:\\-/]/.test(cell) && cell.length` |
| 282 | `bool && → \|\|` | `if (isFinite(num) && /^[\\s+-]*[\\d.,]+\\s*$/.test(cell)) {` |
| 285 | `cmp >= → >` | `if ((num >= 2 && num <= 30) \|\| (num >= 30 && num <= 600)) inBand++;` |
| 285 | `cmp >= → >` | `if ((num >= 2 && num <= 30) \|\| (num >= 30 && num <= 600)) inBand++;` |
| 285 | `cmp <= → <` | `if ((num >= 2 && num <= 30) \|\| (num >= 30 && num <= 600)) inBand++;` |
| 285 | `cmp <= → <` | `if ((num >= 2 && num <= 30) \|\| (num >= 30 && num <= 600)) inBand++;` |
| 285 | `num → 0` | `if ((num >= 2 && num <= 30) \|\| (num >= 30 && num <= 600)) inBand++;` |
| 285 | `num → 0` | `if ((num >= 2 && num <= 30) \|\| (num >= 30 && num <= 600)) inBand++;` |
| 288 | `cmp < → <=` | `if (total < 3) continue;` |
| 289 | `cmp > → >=` | `if (dateHits / total > 0.6 && tsCol < 0) tsCol = c;` |
| 289 | `cmp < → <=` | `if (dateHits / total > 0.6 && tsCol < 0) tsCol = c;` |
| 289 | `bool && → \|\|` | `if (dateHits / total > 0.6 && tsCol < 0) tsCol = c;` |
| 289 | `num → 0` | `if (dateHits / total > 0.6 && tsCol < 0) tsCol = c;` |
| 292 | `num → 0` | `if (numHits / total > 0.6 && sc > gScore) {` |
| 292 | `cmp > → >=` | `if (numHits / total > 0.6 && sc > gScore) {` |
| 292 | `bool && → \|\|` | `if (numHits / total > 0.6 && sc > gScore) {` |
| 292 | `cmp > → >=` | `if (numHits / total > 0.6 && sc > gScore) {` |
| 297 | `cmp < → <=` | `if (tsCol < 0) tsCol = 0;` |
| 315 | `cmp < → <=` | `return tsCol < cells.length ? (cells[tsCol] \|\| '').trim() : '';` |
| 324 | `cmp >= → >` | `if (gCol >= cells.length) continue;` |
| 338 | `cmp < → <=` | `if (T.length < 10) throw new Error('Parsed only ' + T.length + ' valid r` |
| 349 | `cmp < → <=` | `if (med < 30) {` |
| 379 | `num → 0` | `const sustainedCells = Math.max(3, Math.round(15 / cadence)); // ≥~15 mi` |
| 381 | `cmp < → <=` | `for (let k = i; k < j; k++) {` |
| 382 | `cmp < → <=` | `if (gF[k] === FLAG.OK && gV[k] < 70) below70++;` |
| 382 | `bool && → \|\|` | `if (gF[k] === FLAG.OK && gV[k] < 70) below70++;` |
| 384 | `cmp < → <=` | `if (below70 < sustainedCells) return false; // too brief to be a clinica` |
| 392 | `cmp > → >=` | `if (lo > 70) return false; // nadir not in the hypo band → ambiguous, le` |
| 395 | `cmp <= → <` | `for (let k = Math.max(1, i - 2); k <= Math.min(N - 1, i + 1); k++) {` |
| 397 | `cmp > → >=` | `if (d > maxDropStep) maxDropStep = d;` |
| 400 | `cmp <= → <` | `for (let k = Math.max(1, j - 1); k <= Math.min(N - 1, j + 1); k++) {` |
| 402 | `cmp > → >=` | `if (d > maxRiseStep) maxRiseStep = d;` |
| 405 | `cmp >= → >` | `if (maxDropStep >= VERTICAL && maxRiseStep >= VERTICAL) return false; //` |
| 405 | `cmp >= → >` | `if (maxDropStep >= VERTICAL && maxRiseStep >= VERTICAL) return false; //` |
| 411 | `num → 0` | `progress && progress(20, 'Detecting cadence & cleaning…');` |
| 415 | `cmp < → <=` | `for (let i = 1; i < n; i++) {` |
| 417 | `cmp < → <=` | `if (d > 0 && d < 240) dts.push(d);` |
| 417 | `cmp > → >=` | `if (d > 0 && d < 240) dts.push(d);` |
| 417 | `bool && → \|\|` | `if (d > 0 && d < 240) dts.push(d);` |
| 420 | `num → 0` | `const cadence = dts.length ? +quantile(dts, 0.5).toFixed(2) : 5;` |
| 421 | `num → 0` | `const gapThresh = Math.max(cadence * 2.5, cadence + 3); // minutes` |
| 446 | `cmp < → <=` | `for (let i = 0; i < N; i++) {` |
| 449 | `cmp < → <=` | `while (j < n - 1 && tMs[j + 1] <= t) j++;` |
| 449 | `cmp <= → <` | `while (j < n - 1 && tMs[j + 1] <= t) j++;` |
| 458 | `cmp <= → <` | `if (Math.min(gapL, gapR) <= cadence * 0.75) {` |
| 459 | `cmp <= → <` | `gV[i] = gapL <= gapR ? vL : vR; // snap to the real reading` |
| 461 | `cmp > → >=` | `} else if (tR > tL && gapL < gapThresh && gapR < gapThresh) {` |
| 461 | `cmp < → <=` | `} else if (tR > tL && gapL < gapThresh && gapR < gapThresh) {` |
| 461 | `cmp < → <=` | `} else if (tR > tL && gapL < gapThresh && gapR < gapThresh) {` |
| 468 | `cmp > → >=` | `const f = tR > tL ? (t - tL) / (tR - tL) : 0;` |
| 469 | `cmp > → >=` | `gV[i] = tR > tL ? vL + (vR - vL) * f : vL;` |
| 477 | `num → 0` | `const warmCells = Math.min(N, Math.round(60 / cadence));` |
| 480 | `cmp < → <=` | `for (let i = 0; i < warmCells; i++) {` |
| 481 | `eq === → !==` | `if (gF[i] === FLAG.OK) {` |
| 486 | `num → 0` | `warmMean = wc ? warmMean / wc : 999;` |
| 488 | `cmp < → <=` | `for (let i = warmCells; i < Math.min(N, warmCells * 3); i++) if (gF[i] =` |
| 488 | `eq === → !==` | `for (let i = warmCells; i < Math.min(N, warmCells * 3); i++) if (gF[i] =` |
| 490 | `cmp > → >=` | `if (wc > 4 && warmMean < 55 && warmMean < postMean - 25) {` |
| 490 | `cmp < → <=` | `if (wc > 4 && warmMean < 55 && warmMean < postMean - 25) {` |
| 490 | `cmp < → <=` | `if (wc > 4 && warmMean < 55 && warmMean < postMean - 25) {` |
| 490 | `bool && → \|\|` | `if (wc > 4 && warmMean < 55 && warmMean < postMean - 25) {` |
| 490 | `num → 0` | `if (wc > 4 && warmMean < 55 && warmMean < postMean - 25) {` |
| 490 | `num → 0` | `if (wc > 4 && warmMean < 55 && warmMean < postMean - 25) {` |
| 491 | `cmp < → <=` | `for (let i = 0; i < warmCells; i++) gF[i] = FLAG.WARMUP;` |
| 504 | `cmp < → <=` | `for (let i = 0; i < N; i++) {` |
| 505 | `eq === → !==` | `if (gF[i] === FLAG.OK) okv.push(gV[i]);` |
| 512 | `num → 0` | `: 100;` |
| 514 | `num → 0` | `const shoulderFloor = Math.max(72, baseline * 0.86); // shoulders must s` |
| 515 | `num → 0` | `const minRun = Math.max(3, Math.round(25 / cadence)); // ≥~25 min sustai` |
| 516 | `num → 0` | `const ctxW = Math.max(4, Math.round(120 / cadence)); // 2-h context shou` |
| 519 | `cmp < → <=` | `return h >= 23 \|\| h < 8;` |
| 519 | `cmp >= → >` | `return h >= 23 \|\| h < 8;` |
| 519 | `num → 0` | `return h >= 23 \|\| h < 8;` |
| 523 | `cmp < → <=` | `for (let k = Math.max(0, a); k < Math.min(N, b); k++) {` |
| 529 | `num → 0` | `0.5` |
| 534 | `cmp < → <=` | `while (i < N) {` |
| 535 | `cmp < → <=` | `if (gF[i] === FLAG.OK && isNight(gT[i]) && gV[i] < dipCeil) {` |
| 538 | `cmp < → <=` | `while (j < N && gV[j] < shoulderFloor && isNight(gT[j])) {` |
| 538 | `cmp < → <=` | `while (j < N && gV[j] < shoulderFloor && isNight(gT[j])) {` |
| 544 | `cmp >= → >` | `if (runLen >= minRun && runMin <= 150) {` |
| 544 | `bool && → \|\|` | `if (runLen >= minRun && runMin <= 150) {` |
| 544 | `cmp <= → <` | `if (runLen >= minRun && runMin <= 150) {` |
| 549 | `cmp > → >=` | `if (before != null && after != null && before > shoulderFloor && after >` |
| 549 | `cmp > → >=` | `if (before != null && after != null && before > shoulderFloor && after >` |
| 549 | `cmp > → >=` | `if (before != null && after != null && before > shoulderFloor && after >` |
| 549 | `cmp > → >=` | `if (before != null && after != null && before > shoulderFloor && after >` |
| 549 | `num → 0` | `if (before != null && after != null && before > shoulderFloor && after >` |
| 549 | `num → 0` | `if (before != null && after != null && before > shoulderFloor && after >` |
| 550 | `cmp < → <=` | `for (let k = i; k < j; k++) if (gF[k] === FLAG.OK) gF[k] = FLAG.COMPRESS` |
| 564 | `cmp < → <=` | `for (let i = 0; i < N; i++) {` |
| 569 | `cmp >= → >` | `if (run >= 2) nGaps++;` |
| 573 | `cmp >= → >` | `if (run >= 2) nGaps++;` |
| 577 | `cmp < → <=` | `for (let i = 0; i < N; i++) {` |
| 597 | `num → 0` | `spanMin: Math.round(spanMs / 60000),` |
| 598 | `num → 0` | `spanSec: spanMs / 1000,` |
| 664 | `num → 0` | `const boundaryGapMin = 90; // a ≥90-min gap ⇒ likely sensor change` |
| 669 | `cmp < → <=` | `while (i < c.N) {` |
| 672 | `cmp < → <=` | `while (j < c.N && _absent(c, j)) j++;` |
| 673 | `cmp >= → >` | `if ((j - i) * c.cadence >= boundaryGapMin) {` |
| 674 | `cmp > → >=` | `if (i - start > minSessionCells) bounds.push([start, i]);` |
| 678 | `cmp > → >=` | `} else if (c.gF[i] === c.FLAG.WARMUP && i > start + minSessionCells) {` |
| 685 | `cmp > → >=` | `if (c.N - start > minSessionCells) bounds.push([start, c.N]);` |
| 700 | `num → 0` | `if (ys.length < 10)` |
| 700 | `cmp < → <=` | `if (ys.length < 10)` |
| 701 | `num → 0` | `return { idx: k + 1, s, e, startMs: c.gT[s], endMs: c.gT[e - 1], days: +` |
| 717 | `cmp >= → >` | `if (xRange >= 0.5) {` |
| 717 | `num → 0` | `if (xRange >= 0.5) {` |
| 726 | `cmp > → >=` | `if (den > 0) {` |
| 728 | `num → 0` | `slope = Math.max(-40, Math.min(40, slope));` |
| 764 | `cmp < → <=` | `for (let p = sess.s; p < sess.e; p++) {` |
| 781 | `num → 0` | `const TIR_CUT = { vlow: 54, low: 70, high: 180, vhigh: 250 }; // mg/dL c` |
| 781 | `num → 0` | `const TIR_CUT = { vlow: 54, low: 70, high: 180, vhigh: 250 }; // mg/dL c` |
| 797 | `cmp <= → <` | `else if (g <= TIR_CUT.high) tir++;` |
| 798 | `cmp <= → <` | `else if (g <= TIR_CUT.vhigh) tar1++;` |
| 817 | `num → 0` | `p10: round(quantile(sorted, 0.1), 0),` |
| 818 | `num → 0` | `p25: round(quantile(sorted, 0.25), 0),` |
| 819 | `num → 0` | `median: round(quantile(sorted, 0.5), 0),` |
| 821 | `num → 0` | `p90: round(quantile(sorted, 0.9), 0),` |
| 836 | `cmp < → <=` | `if (n < 4) return null;` |
| 839 | `cmp < → <=` | `for (let i = 1; i < n; i++) {` |
| 842 | `cmp > → >=` | `if (Math.abs(d) > 0) {` |
| 845 | `eq !== → ===` | `} else if (Math.sign(v[i] - v[i - 1]) !== 0 && Math.sign(v[i] - v[i - 1]` |
| 853 | `cmp < → <=` | `for (let i = 1; i < tp.length; i++) {` |
| 863 | `cmp < → <=` | `if (lag < 1) return null;` |
| 865 | `cmp < → <=` | `for (let i = lag; i < c.N; i++) {` |
| 866 | `bool \|\| → &&` | `if (!_ana(c, i) \|\| !_ana(c, i - lag)) continue;` |
| 869 | `cmp < → <=` | `if (diffs.length < 5) return null;` |
| 876 | `cmp < → <=` | `if (lag < 1 \|\| c.N <= lag) return null;` |
| 876 | `cmp <= → <` | `if (lag < 1 \|\| c.N <= lag) return null;` |
| 876 | `bool \|\| → &&` | `if (lag < 1 \|\| c.N <= lag) return null;` |
| 878 | `cmp < → <=` | `for (let i = lag; i < c.N; i++) {` |
| 882 | `cmp < → <=` | `if (diffs.length < 5) return null;` |
| 897 | `cmp > → >=` | `return L0 > 0 ? round((L / L0 - 1) * 100, 1) : null;` |
| 906 | `cmp <= → <` | `if (bg <= 0) continue;` |
| 909 | `cmp < → <=` | `if (f < 0) rl += r;` |
| 938 | `bool \|\| → &&` | `if (!_ana(c, i) \|\| !_ana(c, i - 1)) continue;` |
| 942 | `cmp > → >=` | `return hrs > 0 ? round(sum / hrs, 1) : null;` |
| 954 | `cmp <= → <` | `if (mmol <= 1) continue;` |
| 960 | `cmp < → <=` | `if (bg < 70) hypo += g;` |
| 961 | `cmp <= → <` | `else if (bg <= 140) eu += g;` |
| 966 | `num → 0` | `return { score: round(sum / n, 1), hypoPct: round((hypo / tot) * 100, 0)` |
| 981 | `cmp <= → <` | `if (bg <= 0) continue;` |
| 984 | `cmp < → <=` | `if (f < 0) lrMax = Math.max(lrMax, r);` |
| 988 | `cmp > → >=` | `if (cnt > 20) {` |
| 988 | `num → 0` | `if (cnt > 20) {` |
| 993 | `cmp >= → >` | `return nd >= 2 ? round(sum / nd, 1) : null;` |
| 1012 | `cmp >= → >` | `if (rel >= -30 && rel < 0) base.push(c.gV[i]);` |
| 1013 | `cmp <= → <` | `if (rel >= 0 && rel <= 180) seq.push({ rel, v: c.gV[i] });` |
| 1013 | `cmp >= → >` | `if (rel >= 0 && rel <= 180) seq.push({ rel, v: c.gV[i] });` |
| 1015 | `cmp < → <=` | `if (seq.length < 6 \|\| base.length < 2) continue;` |
| 1015 | `cmp < → <=` | `if (seq.length < 6 \|\| base.length < 2) continue;` |
| 1021 | `cmp > → >=` | `if (s.v > peak) {` |
| 1030 | `cmp < → <=` | `if (dd < best) {` |
| 1035 | `cmp <= → <` | `const ret = seq.some((s) => s.rel > ttp && s.v <= baseline + 15);` |
| 1035 | `cmp > → >=` | `const ret = seq.some((s) => s.rel > ttp && s.v <= baseline + 15);` |
| 1035 | `num → 0` | `const ret = seq.some((s) => s.rel > ttp && s.v <= baseline + 15);` |
| 1035 | `bool && → \|\|` | `const ret = seq.some((s) => s.rel > ttp && s.v <= baseline + 15);` |
| 1053 | `num → 0` | `returnedPct: pm.ret.length ? round(mean(pm.ret) * 100, 0) : null` |
| 1078 | `cmp < → <=` | `for (let i = 0; i < c.N; i++) {` |
| 1096 | `cmp >= → >` | `if (h >= 3 && h < 6) nadir = Math.min(nadir, c.gV[i]);` |
| 1096 | `cmp < → <=` | `if (h >= 3 && h < 6) nadir = Math.min(nadir, c.gV[i]);` |
| 1097 | `cmp >= → >` | `else if (h >= 6 && h < 8) pre.push(c.gV[i]);` |
| 1097 | `cmp < → <=` | `else if (h >= 6 && h < 8) pre.push(c.gV[i]);` |
| 1099 | `cmp >= → >` | `if (isFinite(nadir) && pre.length >= 2) {` |
| 1099 | `bool && → \|\|` | `if (isFinite(nadir) && pre.length >= 2) {` |
| 1109 | `cmp >= → >` | `return h >= 6 && h < 8;` |
| 1109 | `cmp < → <=` | `return h >= 6 && h < 8;` |
| 1109 | `bool && → \|\|` | `return h >= 6 && h < 8;` |
| 1114 | `bool && → \|\|` | `return h >= 6 && h < 8;` |
| 1114 | `cmp >= → >` | `return h >= 6 && h < 8;` |
| 1114 | `cmp < → <=` | `return h >= 6 && h < 8;` |
| 1123 | `cmp >= → >` | `return { present: md >= 20, medianDelta: md, days: deltas, nDays: deltas` |
| 1131 | `cmp < → <=` | `for (let i = 0; i < c.N; i++) {` |
| 1133 | `cmp < → <=` | `const night = h < 6;` |
| 1144 | `cmp >= → >` | `if (run && run.e - run.s + 1 >= minCells) eps.push({ startMs: c.gT[run.s` |
| 1159 | `cmp < → <=` | `if (a.length < 5) return { cv: null, mean: null, n: a.length };` |
| 1176 | `num → 0` | `const w = Math.max(1, Math.round(15 / c.cadence));` |
| 1189 | `cmp > → >=` | `if (diff < -20 \|\| diff > 75) continue;` |
| 1189 | `cmp < → <=` | `if (diff < -20 \|\| diff > 75) continue;` |
| 1189 | `num → 0` | `if (diff < -20 \|\| diff > 75) continue;` |
| 1190 | `cmp < → <=` | `if (Math.abs(diff) < bd) {` |
| 1197 | `cmp < → <=` | `while (i < c.N - w) {` |
| 1206 | `cmp <= → <` | `for (let k = i; k <= maxAhead; k++) {` |
| 1211 | `cmp < → <=` | `if (c.gV[k] < trough - 10) break;` |
| 1215 | `cmp > → >=` | `const rate = durMin > 0 ? rise / durMin : 0;` |
| 1216 | `cmp >= → >` | `if (rise >= 45 && rate >= 1.0 && c.gV[i] < 160) {` |
| 1216 | `cmp < → <=` | `if (rise >= 45 && rate >= 1.0 && c.gV[i] < 160) {` |
| 1216 | `cmp >= → >` | `if (rise >= 45 && rate >= 1.0 && c.gV[i] < 160) {` |
| 1250 | `cmp < → <=` | `if (arr.length < 2) return { h: b / 2, n: arr.length, p10: null, p25: nu` |
| 1255 | `num → 0` | `p10: round(quantile(s, 0.1), 0),` |
| 1273 | `cmp < → <=` | `if (vals.length < 6) continue;` |
| 1294 | `cmp < → <=` | `if (activeDays < 1) return { tier: 'partial', label: 'Partial day', msg:` |
| 1295 | `cmp < → <=` | `if (activeDays < 14)` |
| 1313 | `num → 0` | `if (d.delta >= 20) ev.push({ t: hhmm(d.atMs), tMs: d.atMs, impulse: 'daw` |
| 1313 | `cmp >= → >` | `if (d.delta >= 20) ev.push({ t: hhmm(d.atMs), tMs: d.atMs, impulse: 'daw` |
| 1330 | `cmp < → <=` | `ev.sort((a, b) => (a.tMs != null && b.tMs != null ? a.tMs - b.tMs : a.t ` |
| 1330 | `bool && → \|\|` | `ev.sort((a, b) => (a.tMs != null && b.tMs != null ? a.tMs - b.tMs : a.t ` |
| 1350 | `cmp < → <=` | `for (let i = 0; i < n; i++) {` |
| 1353 | `cmp < → <=` | `if (v < lo) lo = v;` |
| 1354 | `cmp > → >=` | `if (v > hi) hi = v;` |
| 1356 | `cmp <= → <` | `if (!isFinite(lo) \|\| !isFinite(hi) \|\| hi <= lo) return empty;` |
| 1356 | `bool \|\| → &&` | `if (!isFinite(lo) \|\| !isFinite(hi) \|\| hi <= lo) return empty;` |
| 1356 | `bool \|\| → &&` | `if (!isFinite(lo) \|\| !isFinite(hi) \|\| hi <= lo) return empty;` |
| 1387 | `num → 0` | `const CLIP_RATIO = 2.5; // a pile-up worth investigating` |
| 1390 | `cmp <= → <` | `const nearLingoFloor = lo >= 53 && lo <= 57,` |
| 1390 | `cmp >= → >` | `const nearLingoFloor = lo >= 53 && lo <= 57,` |
| 1390 | `num → 0` | `const nearLingoFloor = lo >= 53 && lo <= 57,` |
| 1391 | `cmp >= → >` | `nearLingoCeil = hi >= 195 && hi <= 205;` |
| 1391 | `bool && → \|\|` | `nearLingoCeil = hi >= 195 && hi <= 205;` |
| 1391 | `cmp <= → <` | `nearLingoCeil = hi >= 195 && hi <= 205;` |
| 1391 | `num → 0` | `nearLingoCeil = hi >= 195 && hi <= 205;` |
| 1391 | `num → 0` | `nearLingoCeil = hi >= 195 && hi <= 205;` |
| 1396 | `cmp < → <=` | `for (let i = 0; i < n; i++) {` |
| 1400 | `cmp < → <=` | `if (Math.abs(d) < 0.5) {` |
| 1410 | `cmp >= → >` | `const pileUp = at >= 3 && pct >= 0.3 && ratio >= CLIP_RATIO;` |
| 1410 | `bool && → \|\|` | `const pileUp = at >= 3 && pct >= 0.3 && ratio >= CLIP_RATIO;` |
| 1410 | `cmp >= → >` | `const pileUp = at >= 3 && pct >= 0.3 && ratio >= CLIP_RATIO;` |
| 1410 | `cmp >= → >` | `const pileUp = at >= 3 && pct >= 0.3 && ratio >= CLIP_RATIO;` |
| 1410 | `num → 0` | `const pileUp = at >= 3 && pct >= 0.3 && ratio >= CLIP_RATIO;` |
| 1410 | `bool && → \|\|` | `const pileUp = at >= 3 && pct >= 0.3 && ratio >= CLIP_RATIO;` |
| 1411 | `cmp >= → >` | `const saturated = pileUp && (inVendorBand \|\| ratio >= HARD_RAIL_RATIO);` |
| 1417 | `bool && → \|\|` | `if (floor.saturated && ceiling.saturated && nearLingoFloor && nearLingoC` |
| 1417 | `bool && → \|\|` | `if (floor.saturated && ceiling.saturated && nearLingoFloor && nearLingoC` |
| 1418 | `bool && → \|\|` | `else if ((floor.saturated && nearLingoFloor) \|\| (ceiling.saturated && ne` |
| 1418 | `bool && → \|\|` | `else if ((floor.saturated && nearLingoFloor) \|\| (ceiling.saturated && ne` |
| 1428 | `bool && → \|\|` | `(lows && highs ? ' and' : '') +` |
| 1430 | `eq === → !==` | `(vendor === 'lingo' ? ' (Abbott Lingo exports only 55–200 mg/dL)' : '') ` |
| 1433 | `bool && → \|\|` | `(lows && highs ? ' and ' : '') +` |
| 1446 | `bool \|\| → &&` | `const biasOffset = +opts.biasOffset \|\| 0;` |
| 1450 | `cmp < → <=` | `for (let i = 0; i < c.N; i++) c.gV[i] = Math.max(20, c.gV[i] + biasOffse` |
| 1450 | `num → 0` | `for (let i = 0; i < c.N; i++) c.gV[i] = Math.max(20, c.gV[i] + biasOffse` |
| 1456 | `bool \|\| → &&` | `const sessionsOut = sessionCorr.leveled \|\| sessionCorr.deDrifted ? detec` |
| 1457 | `num → 0` | `progress && progress(45, 'Computing glycemic metrics…');` |
| 1459 | `cmp < → <=` | `if (ana.vals.length < 6) throw new Error('Too few analyzable readings af` |
| 1463 | `num → 0` | `progress && progress(60, 'Variability suite…');` |
| 1466 | `num → 0` | `progress && progress(72, 'Pattern detection…');` |
| 1476 | `num → 0` | `progress && progress(84, 'AGP aggregation…');` |
| 1502 | `cmp >= → >` | `longRec: activeDays >= 1,` |
| 1512 | `bool \|\| → &&` | `mealMarkers: meals \|\| [],` |
| 1527 | `num → 0` | `progress && progress(96, 'Done');` |
| 1539 | `num → 0` | `const t0 = Date.now() - days * 86400000;` |
| 1549 | `eq === → !==` | `const dawnAmp = profile === 'predm' ? 32 : 16;` |
| 1549 | `num → 0` | `const dawnAmp = profile === 'predm' ? 32 : 16;` |
| 1549 | `num → 0` | `const dawnAmp = profile === 'predm' ? 32 : 16;` |
| 1550 | `num → 0` | `const mealPeak = profile === 'predm' ? { b: 78, l: 70, d: 88 } : { b: 42` |
| 1550 | `num → 0` | `const mealPeak = profile === 'predm' ? { b: 78, l: 70, d: 88 } : { b: 42` |
| 1550 | `num → 0` | `const mealPeak = profile === 'predm' ? { b: 78, l: 70, d: 88 } : { b: 42` |
| 1550 | `num → 0` | `const mealPeak = profile === 'predm' ? { b: 78, l: 70, d: 88 } : { b: 42` |
| 1550 | `num → 0` | `const mealPeak = profile === 'predm' ? { b: 78, l: 70, d: 88 } : { b: 42` |
| 1550 | `num → 0` | `const mealPeak = profile === 'predm' ? { b: 78, l: 70, d: 88 } : { b: 42` |
| 1551 | `eq === → !==` | `const mealDecay = profile === 'predm' ? 150 : 95; // min back toward bas` |
| 1551 | `num → 0` | `const mealDecay = profile === 'predm' ? 150 : 95; // min back toward bas` |
| 1559 | `eq === → !==` | `while (v === 0) v = Math.random();` |
| 1565 | `cmp < → <=` | `for (let dd = 0; dd < days; dd++) {` |
| 1566 | `cmp < → <=` | `if (Math.random() < 0.3) gapDays.add(dd);` |
| 1566 | `num → 0` | `if (Math.random() < 0.3) gapDays.add(dd);` |
| 1569 | `cmp < → <=` | `for (let i = 0; i < N; i++) {` |
| 1578 | `num → 0` | `g -= 6 * Math.exp(-Math.pow((h - 2.5) / 2.2, 2));` |
| 1580 | `num → 0` | `g += (dawnAmp / (1 + Math.exp(-(h - 4.5) * 1.6))) * Math.exp(-Math.pow(M` |
| 1580 | `num → 0` | `g += (dawnAmp / (1 + Math.exp(-(h - 4.5) * 1.6))) * Math.exp(-Math.pow(M` |
| 1584 | `num → 0` | `{ t: 7.8 + 0.4 * Math.sin(dayIdx * 1.7), amp: mealPeak.b },` |
| 1584 | `num → 0` | `{ t: 7.8 + 0.4 * Math.sin(dayIdx * 1.7), amp: mealPeak.b },` |
| 1584 | `num → 0` | `{ t: 7.8 + 0.4 * Math.sin(dayIdx * 1.7), amp: mealPeak.b },` |
| 1585 | `num → 0` | `{ t: 12.8 + 0.4 * Math.sin(dayIdx * 2.3), amp: mealPeak.l },` |
| 1585 | `num → 0` | `{ t: 12.8 + 0.4 * Math.sin(dayIdx * 2.3), amp: mealPeak.l },` |
| 1585 | `num → 0` | `{ t: 12.8 + 0.4 * Math.sin(dayIdx * 2.3), amp: mealPeak.l },` |
| 1586 | `num → 0` | `{ t: 19.3 + 0.5 * Math.sin(dayIdx * 1.1), amp: mealPeak.d }` |
| 1586 | `num → 0` | `{ t: 19.3 + 0.5 * Math.sin(dayIdx * 1.1), amp: mealPeak.d }` |
| 1586 | `num → 0` | `{ t: 19.3 + 0.5 * Math.sin(dayIdx * 1.1), amp: mealPeak.d }` |
| 1589 | `cmp >= → >` | `if (h >= m.t) {` |
| 1592 | `num → 0` | `const rise = Math.exp(-Math.pow((dm - 45) / 30, 2));` |
| 1592 | `num → 0` | `const rise = Math.exp(-Math.pow((dm - 45) / 30, 2));` |
| 1594 | `num → 0` | `g += m.amp * Math.max(rise, decay * 0.9);` |
| 1598 | `cmp > → >=` | `if (profile === 'healthy' && h > 16.5 && h < 17.5 && Math.random() < 0.0` |
| 1598 | `cmp < → <=` | `if (profile === 'healthy' && h > 16.5 && h < 17.5 && Math.random() < 0.0` |
| 1598 | `cmp < → <=` | `if (profile === 'healthy' && h > 16.5 && h < 17.5 && Math.random() < 0.0` |
| 1598 | `eq === → !==` | `if (profile === 'healthy' && h > 16.5 && h < 17.5 && Math.random() < 0.0` |
| 1598 | `bool && → \|\|` | `if (profile === 'healthy' && h > 16.5 && h < 17.5 && Math.random() < 0.0` |
| 1598 | `num → 0` | `if (profile === 'healthy' && h > 16.5 && h < 17.5 && Math.random() < 0.0` |
| 1598 | `num → 0` | `if (profile === 'healthy' && h > 16.5 && h < 17.5 && Math.random() < 0.0` |
| 1598 | `bool && → \|\|` | `if (profile === 'healthy' && h > 16.5 && h < 17.5 && Math.random() < 0.0` |
| 1598 | `bool && → \|\|` | `if (profile === 'healthy' && h > 16.5 && h < 17.5 && Math.random() < 0.0` |
| 1598 | `num → 0` | `if (profile === 'healthy' && h > 16.5 && h < 17.5 && Math.random() < 0.0` |
| 1598 | `num → 0` | `if (profile === 'healthy' && h > 16.5 && h < 17.5 && Math.random() < 0.0` |
| 1602 | `eq === → !==` | `g = Math.max(45, Math.min(profile === 'predm' ? 260 : 210, g));` |
| 1602 | `num → 0` | `g = Math.max(45, Math.min(profile === 'predm' ? 260 : 210, g));` |
| 1605 | `cmp > → >=` | `if (h > 1 && h < 4 && Math.random() < 0.0008) g = Math.max(42, g - 55);` |
| 1605 | `cmp < → <=` | `if (h > 1 && h < 4 && Math.random() < 0.0008) g = Math.max(42, g - 55);` |
| 1605 | `cmp < → <=` | `if (h > 1 && h < 4 && Math.random() < 0.0008) g = Math.max(42, g - 55);` |
| 1605 | `bool && → \|\|` | `if (h > 1 && h < 4 && Math.random() < 0.0008) g = Math.max(42, g - 55);` |
| 1605 | `num → 0` | `if (h > 1 && h < 4 && Math.random() < 0.0008) g = Math.max(42, g - 55);` |
| 1605 | `num → 0` | `if (h > 1 && h < 4 && Math.random() < 0.0008) g = Math.max(42, g - 55);` |
| 1605 | `num → 0` | `if (h > 1 && h < 4 && Math.random() < 0.0008) g = Math.max(42, g - 55);` |
| 1613 | `num → 0` | `const gapStartH = 2 + Math.random() * 18;` |
| 1614 | `num → 0` | `const gapLen = 40 + Math.random() * 70;` |
| 1614 | `num → 0` | `const gapLen = 40 + Math.random() * 70;` |
| 1616 | `num → 0` | `const e = Math.round((dd * 1440 + gapStartH * 60 + gapLen) / cadenceMin)` |
| 1616 | `num → 0` | `const e = Math.round((dd * 1440 + gapStartH * 60 + gapLen) / cadenceMin)` |
| 1617 | `cmp < → <=` | `for (let i = s; i < e && i < N; i++) removed.add(i);` |
| 1617 | `cmp < → <=` | `for (let i = s; i < e && i < N; i++) removed.add(i);` |
| 1620 | `num → 0` | `const warm = Math.round(55 / cadenceMin);` |
| 1621 | `num → 0` | `for (let i = 0; i < warm; i++) vMgdl[i] = Math.max(40, 50 - i * 1.5 + ga` |
| 1621 | `cmp < → <=` | `for (let i = 0; i < warm; i++) vMgdl[i] = Math.max(40, 50 - i * 1.5 + ga` |
| 1621 | `num → 0` | `for (let i = 0; i < warm; i++) vMgdl[i] = Math.max(40, 50 - i * 1.5 + ga` |
| 1621 | `num → 0` | `for (let i = 0; i < warm; i++) vMgdl[i] = Math.max(40, 50 - i * 1.5 + ga` |
| 1621 | `num → 0` | `for (let i = 0; i < warm; i++) vMgdl[i] = Math.max(40, 50 - i * 1.5 + ga` |
| 1663 | `bool \|\| → &&` | `if (dx === 0 \|\| dy === 0) return null;` |
| 1676 | `cmp < → <=` | `if (lines.length < 2) throw new Error('Nutrition file has too few rows.'` |
| 1684 | `negate: drop !` | `q = !q;` |
| 1707 | `bool \|\| → &&` | `carbs: header.findIndex((h) => h.startsWith('carbs') \|\| h.includes('carb` |
| 1707 | `negate: drop !` | `carbs: header.findIndex((h) => h.startsWith('carbs') \|\| h.includes('carb` |
| 1707 | `bool \|\| → &&` | `carbs: header.findIndex((h) => h.startsWith('carbs') \|\| h.includes('carb` |
| 1708 | `negate: drop !` | `sugars: header.findIndex((h) => h.includes('sugar') && !h.includes('adde` |
| 1712 | `eq === → !==` | `fat: header.findIndex((h) => h === 'fat (g)' \|\| h.startsWith('fat ('))` |
| 1712 | `bool \|\| → &&` | `fat: header.findIndex((h) => h === 'fat (g)' \|\| h.startsWith('fat ('))` |
| 1716 | `cmp >= → >` | `if (i < 0 \|\| i >= cells.length) return null;` |
| 1716 | `cmp < → <=` | `if (i < 0 \|\| i >= cells.length) return null;` |
| 1716 | `bool \|\| → &&` | `if (i < 0 \|\| i >= cells.length) return null;` |
| 1721 | `cmp >= → >` | `ci.time >= 0 &&` |
| 1721 | `bool && → \|\|` | `ci.time >= 0 &&` |
| 1724 | `bool && → \|\|` | `return c[ci.time] && /\\d{1,2}:\\d{2}/.test(c[ci.time]);` |
| 1737 | `bool \|\| → &&` | `if (c.length < 2 \|\| !c[ci.date]) continue;` |
| 1737 | `cmp < → <=` | `if (c.length < 2 \|\| !c[ci.date]) continue;` |
| 1738 | `bool && → \|\|` | `nutStamps.push(hasTime && c[ci.time] ? c[ci.date] + ' ' + c[ci.time] : c` |
| 1750 | `cmp >= → >` | `const grp = (ci.group >= 0 ? cells[ci.group] \|\| '' : '').toLowerCase();` |
| 1750 | `bool \|\| → &&` | `const grp = (ci.group >= 0 ? cells[ci.group] \|\| '' : '').toLowerCase();` |
| 1754 | `bool && → \|\|` | `const _hasT = hasTime && cells[ci.time];` |
| 1772 | `bool \|\| → &&` | `servings.push({ ms, minOfDay: d.getUTCHours() * 60 + d.getUTCMinutes(), ` |
| 1774 | `bool \|\| → &&` | `if (!hasTime \|\| grp.includes('total') \|\| ci.group < 0) {` |
| 1774 | `cmp < → <=` | `if (!hasTime \|\| grp.includes('total') \|\| ci.group < 0) {` |
| 1774 | `negate: drop !` | `if (!hasTime \|\| grp.includes('total') \|\| ci.group < 0) {` |
| 1799 | `cmp > → >=` | `const carbs = Math.round(m.carbs / Math.max(1, [...byMeal.values()].leng` |
| 1809 | `cmp > → >=` | `.filter((d) => d._n > 0)` |
| 1821 | `bool \|\| → &&` | `fat: Math.round(d.fat) \|\| null` |
| 1824 | `bool && → \|\|` | `return { shape: hasTime && mealMarkers && mealMarkers.length ? 'servings` |
| 1856 | `cmp < → <=` | `if (matched.length < 3) return { matchedDays: matched.length, note: 'Few` |
| 1863 | `cmp < → <=` | `for (let i = 0; i < matched.length; i++) {` |
| 1864 | `bool && → \|\|` | `if (isFinite(a[i]) && b[i] != null) {` |
| 1914 | `bool \|\| → &&` | `opts = opts \|\| {};` |
| 1937 | `bool && → \|\|` | `var nadir = e && e.meta ? e.meta.minMgdl : null;` |
| 1938 | `bool \|\| → &&` | `if (nadir == null \|\| !isFinite(nadir) \|\| _floorVal == null) return true;` |
| 1938 | `bool \|\| → &&` | `if (nadir == null \|\| !isFinite(nadir) \|\| _floorVal == null) return true;` |
| 1957 | `bool \|\| → &&` | `? { version: (opts.kernel.version != null ? opts.kernel.version : opts.k` |
| 1957 | `bool \|\| → &&` | `? { version: (opts.kernel.version != null ? opts.kernel.version : opts.k` |
| 1966 | `bool \|\| → &&` | `provenance: opts.provenance \|\| null,` |
| 2007 | `bool && → \|\|` | `var _F = r.series && r.series.FLAG ? r.series.FLAG : null;` |
| 2008 | `bool && → \|\|` | `var _cadS = r.series && isFinite(r.series.cadence) && r.series.cadence >` |
| 2008 | `cmp > → >=` | `var _cadS = r.series && isFinite(r.series.cadence) && r.series.cadence >` |
| 2008 | `bool && → \|\|` | `var _cadS = r.series && isFinite(r.series.cadence) && r.series.cadence >` |
| 2011 | `bool \|\| → &&` | `if (_F == null \|\| _cadS == null) return { kind: 'continuous', spanSec: _` |
| 2040 | `bool && → \|\|` | `floor: cs.floor && cs.floor.saturated && cs.floor.value != null ? cs.flo` |
| 2040 | `bool && → \|\|` | `floor: cs.floor && cs.floor.saturated && cs.floor.value != null ? cs.flo` |
| 2094 | `bool && → \|\|` | `if (input && Array.isArray(input.tMs) && Array.isArray(input.vMgdl)) {` |
| 2096 | `eq === → !==` | `} else if (input && Array.isArray(input.samples) && input.samples.length` |
| 2096 | `eq === → !==` | `} else if (input && Array.isArray(input.samples) && input.samples.length` |
| 2111 | `bool \|\| → &&` | `unit: input.unit \|\| 'mg/dL',` |
| 2116 | `eq === → !==` | `typeof input === 'string' ? input : input && typeof input.text === 'stri` |
| 2116 | `bool && → \|\|` | `typeof input === 'string' ? input : input && typeof input.text === 'stri` |
| 2116 | `bool && → \|\|` | `typeof input === 'string' ? input : input && typeof input.text === 'stri` |
| 2116 | `bool && → \|\|` | `typeof input === 'string' ? input : input && typeof input.text === 'stri` |
| 2168 | `bool \|\| → &&` | `message: 'This is a ' + (node \|\| 'non-GlucoDex') + ' export \\u2014 open ` |
| 2168 | `bool \|\| → &&` | `message: 'This is a ' + (node \|\| 'non-GlucoDex') + ' export \\u2014 open ` |
| 2175 | `bool && → \|\|` | `return ((a && a.tMs) \|\| 0) - ((b && b.tMs) \|\| 0);` |
| 2175 | `bool \|\| → &&` | `return ((a && a.tMs) \|\| 0) - ((b && b.tMs) \|\| 0);` |
| 2175 | `bool && → \|\|` | `return ((a && a.tMs) \|\| 0) - ((b && b.tMs) \|\| 0);` |
| 2184 | `bool && → \|\|` | `generated: (json.schema && json.schema.generated) \|\| null,` |
| 2185 | `bool && → \|\|` | `derivedFrom: (json.schema && json.schema.derivedFrom) \|\| null,` |
| 2185 | `bool \|\| → &&` | `derivedFrom: (json.schema && json.schema.derivedFrom) \|\| null,` |
| 2186 | `bool \|\| → &&` | `kernel: json.kernel \|\| null,` |
| 2189 | `bool && → \|\|` | `scrubbed: !!(json.schema && json.schema.scrubbed),` |
| 2199 | `eq === → !==` | `if (global.DexExport && typeof global.DexExport.scrubExport === 'functio` |
| 2199 | `bool && → \|\|` | `if (global.DexExport && typeof global.DexExport.scrubExport === 'functio` |
| 2200 | `eq === → !==` | `if (typeof global.dexScrubExport === 'function') return global.dexScrubE` |

## `hrvdex-dsp.js` — 163 survivor(s)

| line | operator | source |
|---:|---|---|
| 118 | `bool \|\| → &&` | `if (v == null \|\| typeof v !== 'number' \|\| !isFinite(v)) return false;` |
| 118 | `bool \|\| → &&` | `if (v == null \|\| typeof v !== 'number' \|\| !isFinite(v)) return false;` |
| 134 | `cmp < → <=` | `for (var i = 0; i < arguments.length; i++) {` |
| 155 | `cmp < → <=` | `if (_sc.length < 5) continue;` |
| 156 | `bool && → \|\|` | `const _sd = _dIdx >= 0 && _sc[_dIdx] ? _sc[_dIdx].trim().replace(/\\r/g, ` |
| 157 | `cmp >= → >` | `const _st = _tIdx >= 0 && _sc[_tIdx] ? _sc[_tIdx].trim().replace(/\\r/g, ` |
| 157 | `bool && → \|\|` | `const _st = _tIdx >= 0 && _sc[_tIdx] ? _sc[_tIdx].trim().replace(/\\r/g, ` |
| 158 | `bool && → \|\|` | `_stamps.push(_st && !/\\d{1,2}:\\d{2}/.test(_sd) ? (_sd + ' ' + _st).trim(` |
| 158 | `bool \|\| → &&` | `_stamps.push(_st && !/\\d{1,2}:\\d{2}/.test(_sd) ? (_sd + ' ' + _st).trim(` |
| 160 | `bool && → \|\|` | `const _order = typeof DexClock !== 'undefined' && DexClock.resolveDMY ? ` |
| 165 | `cmp < → <=` | `if (cells.length < 5) continue;` |
| 174 | `bool \|\| → &&` | `var _combined = _rawT && !/\\d{1,2}:\\d{2}/.test(_rawD) ? (_rawD + ' ' + _` |
| 188 | `bool \|\| → &&` | `r._mode = numOrNull(r['Mode'] \|\| r['Mode RR']);` |
| 189 | `bool \|\| → &&` | `r._totalPow = numOrNull(r['Total power'] \|\| r['Total Power']);` |
| 212 | `bool && → \|\|` | `r._hrv = numOrNull(r['HRV Score'] != null && String(r['HRV Score']).trim` |
| 219 | `bool \|\| → &&` | `return commitRows(_hrvParseSummaryRows(text), opts \|\| {});` |
| 277 | `cmp >= → >` | `return !!(row && typeof row._spanMin === 'number' && isFinite(row._spanM` |
| 295 | `bool && → \|\|` | `s[k] = typeof v === 'number' && isFinite(v) ? v : null;` |
| 295 | `eq === → !==` | `s[k] = typeof v === 'number' && isFinite(v) ? v : null;` |
| 315 | `bool && → \|\|` | `r[k] = typeof s[k] === 'number' && isFinite(s[k]) ? s[k] : null;` |
| 327 | `negate: drop !` | `if (!allRows \|\| !allRows.length) {` |
| 344 | `cmp > → >=` | `while (kept.length > 1) {` |
| 371 | `negate: drop !` | `if (!Array.isArray(seeds) \|\| !seeds.length) return false;` |
| 373 | `negate: drop !` | `if (!rows.length) return false;` |
| 380 | `bool \|\| → &&` | `opts = opts \|\| {};` |
| 381 | `bool \|\| → &&` | `newRows = (newRows \|\| []).filter((r) => isFinite(r._tMs));` |
| 396 | `negate: drop !` | `if (!allRows.length) {` |
| 404 | `num → 0` | `_ui.setProgress(100);` |
| 414 | `eq === → !==` | `const skip = dup ? ' · ' + dup + ' duplicate' + (dup === 1 ? '' : 's') +` |
| 415 | `eq === → !==` | `_ui.setStatus('✅ Added ' + added + ' measurement' + (added === 1 ? '' : ` |
| 457 | `bool && → \|\|` | `if (pat && allRows && allRows.length) {` |
| 457 | `bool && → \|\|` | `if (pat && allRows && allRows.length) {` |
| 470 | `bool && → \|\|` | `if (sdc && sdi && allRows && allRows.length) {` |
| 470 | `bool && → \|\|` | `if (sdc && sdi && allRows && allRows.length) {` |
| 470 | `bool && → \|\|` | `if (sdc && sdi && allRows && allRows.length) {` |
| 472 | `bool && → \|\|` | `sdi.innerHTML = allRows.length + ' sessions &bull; 85 metrics<br>' + (al` |
| 490 | `bool && → \|\|` | `const tMs = rec.startEpochMs != null && isFinite(rec.startEpochMs) ? +re` |
| 498 | `bool && → \|\|` | `const n = (v) => (typeof v === 'number' && isFinite(v) ? v : null);` |
| 512 | `cmp > → >=` | `if (t > 0) spanMin = t;` |
| 552 | `bool && → \|\|` | `else if (data && Array.isArray(data.recordings))` |
| 554 | `bool \|\| → &&` | `else if (data && (data.recording \|\| data.hrv)) envs = [data]; // single ` |
| 554 | `bool && → \|\|` | `else if (data && (data.recording \|\| data.hrv)) envs = [data]; // single ` |
| 556 | `negate: drop !` | `if (!seeds.length) {` |
| 561 | `bool \|\| → &&` | `commitRows(rows, opts \|\| {});` |
| 598 | `bool && → \|\|` | `var _baev = typeof DexUnits !== 'undefined' && DexUnits && DexUnits.guar` |
| 621 | `eq !== → ===` | `var _meanRRs0 = typeof DexUnits !== 'undefined' && DexUnits && DexUnits.` |
| 621 | `bool && → \|\|` | `var _meanRRs0 = typeof DexUnits !== 'undefined' && DexUnits && DexUnits.` |
| 632 | `cmp > → >=` | `const _hasNu = r._totalPow > 0 && r._vlf != null && r._totalPow > r._vlf` |
| 633 | `cmp > → >=` | `const _hasBands = r._hf > 0 && r._lf > 0 && r._vlf > 0;` |
| 633 | `cmp > → >=` | `const _hasBands = r._hf > 0 && r._lf > 0 && r._vlf > 0;` |
| 633 | `cmp > → >=` | `const _hasBands = r._hf > 0 && r._lf > 0 && r._vlf > 0;` |
| 633 | `bool && → \|\|` | `const _hasBands = r._hf > 0 && r._lf > 0 && r._vlf > 0;` |
| 633 | `bool && → \|\|` | `const _hasBands = r._hf > 0 && r._lf > 0 && r._vlf > 0;` |
| 669 | `bool && → \|\|` | `const meanRR_s = typeof DexUnits !== 'undefined' && DexUnits && DexUnits` |
| 679 | `eq !== → ===` | `const rmssd_s = typeof DexUnits !== 'undefined' && DexUnits && DexUnits.` |
| 679 | `bool && → \|\|` | `const rmssd_s = typeof DexUnits !== 'undefined' && DexUnits && DexUnits.` |
| 688 | `cmp > → >=` | `r.d_abs = _all(r._sns, r._psns) && r._sns + r._psns > 0 ? (r._psns - r._` |
| 713 | `cmp > → >=` | `if (r._vlf > 0 && r._lf > 0 && r._hf > 0) {` |
| 713 | `cmp > → >=` | `if (r._vlf > 0 && r._lf > 0 && r._hf > 0) {` |
| 713 | `bool && → \|\|` | `if (r._vlf > 0 && r._lf > 0 && r._hf > 0) {` |
| 713 | `cmp > → >=` | `if (r._vlf > 0 && r._lf > 0 && r._hf > 0) {` |
| 713 | `bool && → \|\|` | `if (r._vlf > 0 && r._lf > 0 && r._hf > 0) {` |
| 722 | `cmp > → >=` | `r.d_plaw = den_s > 0 ? num_s / den_s : NaN;` |
| 735 | `cmp >= → >` | `r.d_pns_eff = _all(r._rmssd, r._sdnn, r._pnn50) && r._pnn50 >= 1 && r._s` |
| 739 | `negate: drop !` | `r.d_otr_sat = !isNaN(r.d_otr) && r.d_otr >= 499 ? true : false; // satur` |
| 739 | `cmp >= → >` | `r.d_otr_sat = !isNaN(r.d_otr) && r.d_otr >= 499 ? true : false; // satur` |
| 748 | `cmp > → >=` | `const circAdj = _hrvIsAllNight(r) ? 1.0 : mHour < 10 ? 1.08 : mHour > 16` |
| 748 | `cmp < → <=` | `const circAdj = _hrvIsAllNight(r) ? 1.0 : mHour < 10 ? 1.08 : mHour > 16` |
| 763 | `cmp > → >=` | `const _hrRestR = p_prof.hrrest_manual > 0 ? p_prof.hrrest_manual : r._hr` |
| 764 | `cmp > → >=` | `const hrmax_tanaka = p_prof.hrmax_manual > 0 && p_prof.hrmax_manual >= 1` |
| 764 | `cmp > → >=` | `const hrmax_tanaka = p_prof.hrmax_manual > 0 && p_prof.hrmax_manual >= 1` |
| 765 | `num → 0` | `const _altFR = p_prof.elev <= 1500 ? 1 : Math.max(0.55, 1 - ((p_prof.ele` |
| 765 | `cmp <= → <` | `const _altFR = p_prof.elev <= 1500 ? 1 : Math.max(0.55, 1 - ((p_prof.ele` |
| 785 | `cmp > → >=` | `const vo2_win = _vo2win7.map((x) => x.d_vo2_hrv).filter((v) => !isNaN(v)` |
| 844 | `bool && → \|\|` | `const dayGap = isFinite(r._tMs) && isFinite(prev._tMs) ? Math.round((r._` |
| 845 | `bool && → \|\|` | `r.d_rmssd_delta_pct = dayGap === 1 && prev._rmssd > 0 && !isNaN(r._rmssd` |
| 845 | `num → 0` | `r.d_rmssd_delta_pct = dayGap === 1 && prev._rmssd > 0 && !isNaN(r._rmssd` |
| 845 | `bool && → \|\|` | `r.d_rmssd_delta_pct = dayGap === 1 && prev._rmssd > 0 && !isNaN(r._rmssd` |
| 845 | `cmp > → >=` | `r.d_rmssd_delta_pct = dayGap === 1 && prev._rmssd > 0 && !isNaN(r._rmssd` |
| 854 | `cmp < → <=` | `for (let j = i; j >= 0 && window7.length < 7; j--) {` |
| 856 | `bool && → \|\|` | `if (_dk && !_seen7.has(_dk)) {` |
| 872 | `cmp > → >=` | `r.d_rmssd_cv7 = rmssd7.length > 1 ? (std(rmssd7) / mean7rmssd) * 100 : N` |
| 872 | `num → 0` | `r.d_rmssd_cv7 = rmssd7.length > 1 ? (std(rmssd7) / mean7rmssd) * 100 : N` |
| 875 | `cmp > → >=` | `const stdSDNN7 = sdnn7.length > 1 ? std(sdnn7) : NaN;` |
| 883 | `cmp < → <=` | `for (let j = i; j >= 0 && _win14ac.length < 14; j--) {` |
| 893 | `cmp < → <=` | `for (let j = 0; j < ac_raw.length - 1; j++) {` |
| 905 | `cmp > → >=` | `if (pnn507.length > 2) {` |
| 915 | `cmp < → <=` | `for (let j = i; j >= 0 && window14.length < 14; j--) {` |
| 930 | `negate: drop !` | `const ari14 = window14.map((x) => x.d_ari).filter((v) => !isNaN(v));` |
| 931 | `num → 0` | `r.d_recovery_debt = ari14.filter((v) => v < 0.9).length;` |
| 931 | `cmp < → <=` | `r.d_recovery_debt = ari14.filter((v) => v < 0.9).length;` |
| 966 | `cmp > → >=` | `return dx * dy > 0 ? num / Math.sqrt(dx * dy) : NaN;` |
| 977 | `eq === → !==` | `if (k === 0) return arr;` |
| 992 | `negate: drop !` | `if (!el) return;` |
| 993 | `eq !== → ===` | `var n = typeof windowDays !== 'undefined' ? windowDays : 7;` |
| 995 | `eq === → !==` | `var mo = sc === 'morning' ? ', mornings only' : sc === 'allnight' ? ', a` |
| 995 | `eq === → !==` | `var mo = sc === 'morning' ? ', mornings only' : sc === 'allnight' ? ', a` |
| 996 | `eq === → !==` | `var scope = n >= 999 ? 'all measurements' : 'last ' + n + ' day' + (n ==` |
| 996 | `cmp >= → >` | `var scope = n >= 999 ? 'all measurements' : 'last ' + n + ' day' + (n ==` |
| 996 | `num → 0` | `var scope = n >= 999 ? 'all measurements' : 'last ' + n + ' day' + (n ==` |
| 1008 | `bool && → \|\|` | `if (sel && sel.value) return sel.value;` |
| 1010 | `bool && → \|\|` | `return legacy && /** @type {HTMLInputElement} */ (legacy).checked ? 'mor` |
| 1017 | `eq === → !==` | `if (scope === 'morning') {` |
| 1020 | `cmp < → <=` | `rows = rows.filter((r) => !_hrvIsAllNight(r) && r._date && r._date.getUT` |
| 1020 | `bool && → \|\|` | `rows = rows.filter((r) => !_hrvIsAllNight(r) && r._date && r._date.getUT` |
| 1020 | `negate: drop !` | `rows = rows.filter((r) => !_hrvIsAllNight(r) && r._date && r._date.getUT` |
| 1020 | `bool && → \|\|` | `rows = rows.filter((r) => !_hrvIsAllNight(r) && r._date && r._date.getUT` |
| 1020 | `num → 0` | `rows = rows.filter((r) => !_hrvIsAllNight(r) && r._date && r._date.getUT` |
| 1021 | `eq === → !==` | `} else if (scope === 'allnight') {` |
| 1024 | `cmp < → <=` | `if (windowDays < 999) {` |
| 1024 | `num → 0` | `if (windowDays < 999) {` |
| 1026 | `num → 0` | `const lastMs = allRows[allRows.length - 1]._tMs - (windowDays - 1) * 864` |
| 1027 | `cmp >= → >` | `rows = rows.filter((r) => r._tMs >= lastMs);` |
| 1046 | `cmp > → >=` | `if (r._rmssd > 0) {` |
| 1050 | `cmp >= → >` | `if (_all(r._pnn50) && r._pnn50 >= 0) {` |
| 1051 | `num → 0` | `paraScore += Math.min(100, r._pnn50 * 4);` |
| 1054 | `cmp > → >=` | `if (r._hf > 0) {` |
| 1061 | `cmp > → >=` | `if (r._hf > 0 && r._lf > 0) {` |
| 1061 | `bool && → \|\|` | `if (r._hf > 0 && r._lf > 0) {` |
| 1061 | `cmp > → >=` | `if (r._hf > 0 && r._lf > 0) {` |
| 1063 | `num → 0` | `sympPenalty = Math.max(0, (lfhf - 1.0) * 15); // penalty grows above LF/` |
| 1063 | `num → 0` | `sympPenalty = Math.max(0, (lfhf - 1.0) * 15); // penalty grows above LF/` |
| 1088 | `cmp < → <=` | `return (n < 10 ? '0' : '') + n;` |
| 1097 | `bool \|\| → &&` | `if (!r \|\| !isFinite(r._tMs)) return;` |
| 1099 | `cmp > → >=` | `if (isFinite(r._rmssd) && r._rmssd > 0 && r._rmssd < 20)` |
| 1116 | `num → 0` | `conf: +Math.max(0.4, Math.min(0.9, (r._stress - 50) / 50)).toFixed(2),` |
| 1139 | `bool && → \|\|` | `return r && isFinite(r._tMs);` |
| 1148 | `bool && → \|\|` | `var kfmt = opts.kernel ? { version: opts.kernel.VERSION, hash: opts.kern` |
| 1167 | `bool && → \|\|` | `typeof SignalFrame !== 'undefined' && SignalFrame && SignalFrame.compute` |
| 1167 | `bool && → \|\|` | `typeof SignalFrame !== 'undefined' && SignalFrame && SignalFrame.compute` |
| 1167 | `bool && → \|\|` | `typeof SignalFrame !== 'undefined' && SignalFrame && SignalFrame.compute` |
| 1183 | `bool && → \|\|` | `spanDays: t0 != null && last ? Math.round((last._tMs - t0) / 864e5) + 1 ` |
| 1195 | `bool && → \|\|` | `spanSec: t0 != null && last && last._tMs != null ? Math.round((last._tMs` |
| 1197 | `bool && → \|\|` | `var d = r._spanMin != null && isFinite(r._spanMin) ? Math.round(r._spanM` |
| 1197 | `num → 0` | `var d = r._spanMin != null && isFinite(r._spanMin) ? Math.round(r._spanM` |
| 1206 | `bool && → \|\|` | `return r._spanMin != null && isFinite(r._spanMin);` |
| 1211 | `num → 0` | `return a + r._spanMin * 60;` |
| 1217 | `bool && → \|\|` | `return r._spanMin != null && isFinite(r._spanMin);` |
| 1227 | `cmp > → >=` | `var sd1 = r._rmssd > 0 ? r._rmssd / Math.SQRT2 : null;` |
| 1228 | `bool && → \|\|` | `var sd2 = sd1 != null && r._sdnn > 0 ? Math.sqrt(Math.max(0, 2 * r._sdnn` |
| 1228 | `cmp > → >=` | `var sd2 = sd1 != null && r._sdnn > 0 ? Math.sqrt(Math.max(0, 2 * r._sdnn` |
| 1287 | `bool \|\| → &&` | `message: 'This is a ' + (node \|\| 'non-HRVDex') + ' export \\u2014 open it` |
| 1287 | `bool \|\| → &&` | `message: 'This is a ' + (node \|\| 'non-HRVDex') + ' export \\u2014 open it` |
| 1294 | `bool && → \|\|` | `return ((a && a.tMs) \|\| 0) - ((b && b.tMs) \|\| 0);` |
| 1294 | `bool \|\| → &&` | `return ((a && a.tMs) \|\| 0) - ((b && b.tMs) \|\| 0);` |
| 1294 | `bool && → \|\|` | `return ((a && a.tMs) \|\| 0) - ((b && b.tMs) \|\| 0);` |
| 1294 | `bool \|\| → &&` | `return ((a && a.tMs) \|\| 0) - ((b && b.tMs) \|\| 0);` |
| 1303 | `bool && → \|\|` | `generated: (json.schema && json.schema.generated) \|\| null,` |
| 1304 | `bool \|\| → &&` | `derivedFrom: (json.schema && json.schema.derivedFrom) \|\| null,` |
| 1304 | `bool && → \|\|` | `derivedFrom: (json.schema && json.schema.derivedFrom) \|\| null,` |
| 1305 | `bool \|\| → &&` | `kernel: json.kernel \|\| null,` |
| 1308 | `bool && → \|\|` | `scrubbed: !!(json.schema && json.schema.scrubbed),` |
| 1315 | `bool && → \|\|` | `var HRVDex = typeof HRVDex !== 'undefined' && HRVDex ? HRVDex : {};` |
| 1315 | `eq !== → ===` | `var HRVDex = typeof HRVDex !== 'undefined' && HRVDex ? HRVDex : {};` |
| 1317 | `bool \|\| → &&` | `opts = opts \|\| {};` |
| 1319 | `bool \|\| → &&` | `if (!rows \|\| !rows.length) return null;` |
| 1339 | `eq === → !==` | `if (typeof DexExport !== 'undefined' && DexExport && typeof DexExport.sc` |
| 1339 | `eq !== → ===` | `if (typeof DexExport !== 'undefined' && DexExport && typeof DexExport.sc` |
| 1339 | `bool && → \|\|` | `if (typeof DexExport !== 'undefined' && DexExport && typeof DexExport.sc` |
| 1339 | `bool && → \|\|` | `if (typeof DexExport !== 'undefined' && DexExport && typeof DexExport.sc` |
| 1340 | `eq === → !==` | `if (typeof dexScrubExport === 'function') return dexScrubExport(env);` |
| 1436 | `eq !== → ===` | `})(/** @type {any} */ (typeof globalThis !== 'undefined' ? globalThis : ` |
| 1436 | `eq !== → ===` | `})(/** @type {any} */ (typeof globalThis !== 'undefined' ? globalThis : ` |

## `integrator-dsp.js` — 466 survivor(s)

| line | operator | source |
|---:|---|---|
| 86 | `cmp < → <=` | `if (t < t0Ms - 60000) t += 86400000; // F3: the event window is [t0 − 60` |
| 94 | `cmp < → <=` | `for (var i = 0; i < confs.length; i++) {` |
| 108 | `negate: drop !` | `if (!ivs \|\| !ivs.length) return 0;` |
| 116 | `cmp <= → <` | `if (a[i][0] <= curE) {` |
| 138 | `bool \|\| → &&` | `if (!e \|\| e.conf == null \|\| !isFinite(e.conf)) return null;` |
| 138 | `bool \|\| → &&` | `if (!e \|\| e.conf == null \|\| !isFinite(e.conf)) return null;` |
| 138 | `negate: drop !` | `if (!e \|\| e.conf == null \|\| !isFinite(e.conf)) return null;` |
| 138 | `negate: drop !` | `if (!e \|\| e.conf == null \|\| !isFinite(e.conf)) return null;` |
| 139 | `bool \|\| → &&` | `var q = e.sqi == null \|\| !isFinite(e.sqi) ? 1 : Math.max(0, Math.min(1, ` |
| 139 | `negate: drop !` | `var q = e.sqi == null \|\| !isFinite(e.sqi) ? 1 : Math.max(0, Math.min(1, ` |
| 210 | `bool \|\| → &&` | `return NODE_COLORS[n] \|\| NODE_COLORS.Unknown;` |
| 215 | `cmp >= → >` | `return BUS_ALIASES.indexOf(String(v).toLowerCase()) >= 0;` |
| 228 | `bool && → \|\|` | `if (Array.isArray(json) && json[0] && (json[0].desatProfile \|\| json[0].h` |
| 228 | `bool && → \|\|` | `if (Array.isArray(json) && json[0] && (json[0].desatProfile \|\| json[0].h` |
| 228 | `bool \|\| → &&` | `if (Array.isArray(json) && json[0] && (json[0].desatProfile \|\| json[0].h` |
| 228 | `bool \|\| → &&` | `if (Array.isArray(json) && json[0] && (json[0].desatProfile \|\| json[0].h` |
| 234 | `bool \|\| → &&` | `var t0Ms = (json.recording && json.recording.startEpochMs) \|\| json.start` |
| 234 | `bool \|\| → &&` | `var t0Ms = (json.recording && json.recording.startEpochMs) \|\| json.start` |
| 240 | `bool && → \|\|` | `return e && typeof e.tMs === 'number' ? e.tMs : null;` |
| 240 | `eq === → !==` | `return e && typeof e.tMs === 'number' ? e.tMs : null;` |
| 248 | `cmp < → <=` | `for (var i = 0; i < raw.length; i++) {` |
| 254 | `bool \|\| → &&` | `t: e.t \|\| fmtClockS(tMs),` |
| 256 | `bool \|\| → &&` | `node: e.node \|\| node,` |
| 303 | `bool && → \|\|` | `if (_last && _last.startMs != null && isFinite(_last.startMs)) {` |
| 303 | `bool && → \|\|` | `if (_last && _last.startMs != null && isFinite(_last.startMs)) {` |
| 304 | `bool && → \|\|` | `var _cEnd = _last.startMs + (_last.durSec != null && isFinite(_last.durS` |
| 304 | `num → 0` | `var _cEnd = _last.startMs + (_last.durSec != null && isFinite(_last.durS` |
| 312 | `bool && → \|\|` | `if (events[i].meta && events[i].meta.offsetMin != null) {` |
| 333 | `bool \|\| → &&` | `summary.ambulatory = !!(json.recording && json.recording.ambulatory) \|\| ` |
| 333 | `bool && → \|\|` | `summary.ambulatory = !!(json.recording && json.recording.ambulatory) \|\| ` |
| 333 | `bool \|\| → &&` | `summary.ambulatory = !!(json.recording && json.recording.ambulatory) \|\| ` |
| 365 | `cmp > → >=` | `summary.respRateBrpm = _hf.respRate != null && _hf.respRate > 0 ? _hf.re` |
| 365 | `bool && → \|\|` | `summary.respRateBrpm = _hf.respRate != null && _hf.respRate > 0 ? _hf.re` |
| 366 | `bool \|\| → &&` | `summary.respRateMethod = summary.respRateBrpm != null ? _hf.respRateMeth` |
| 377 | `cmp > → >=` | `if (!_sleepSuppressed && json.sleep && json.sleep.stageMinutes && json.s` |
| 398 | `bool \|\| → &&` | `_dig(json, ['glucose', 'cv']) \|\| _dig(json, ['glycemic', 'cv']) \|\| _dig(` |
| 398 | `bool \|\| → &&` | `_dig(json, ['glucose', 'cv']) \|\| _dig(json, ['glycemic', 'cv']) \|\| _dig(` |
| 398 | `bool \|\| → &&` | `_dig(json, ['glucose', 'cv']) \|\| _dig(json, ['glycemic', 'cv']) \|\| _dig(` |
| 405 | `bool \|\| → &&` | `_dig(json, ['glucose', 'dawn', 'riseMgdl']) \|\|` |
| 406 | `bool \|\| → &&` | `_dig(json, ['fusion', 'dawnSurge']) \|\|` |
| 407 | `bool \|\| → &&` | `_dig(json, ['dawn', 'surge']) \|\|` |
| 408 | `bool \|\| → &&` | `(json.patterns && json.patterns.dawnPhenomenon ? json.patterns.dawnPheno` |
| 410 | `bool && → \|\|` | `summary.glucoseAutonomicCorrelation = json.reserved && json.reserved.glu` |
| 411 | `bool && → \|\|` | `summary.autonomicInstabilitySlope = json.reserved && json.reserved.auton` |
| 419 | `bool \|\| → &&` | `? { vendor: _clamp.vendor \|\| null, floor: _clamp.floor != null ? _clamp.` |
| 422 | `cmp < → <=` | `for (var _ei = 0; _ei < events.length; _ei++) {` |
| 444 | `bool && → \|\|` | `return { tMs: cl.tMs != null ? cl.tMs : t0c != null && cl.i != null && c` |
| 444 | `bool && → \|\|` | `return { tMs: cl.tMs != null ? cl.tMs : t0c != null && cl.i != null && c` |
| 444 | `num → 0` | `return { tMs: cl.tMs != null ? cl.tMs : t0c != null && cl.i != null && c` |
| 447 | `bool && → \|\|` | `return cl.tMs != null && cl.v != null;` |
| 462 | `bool \|\| → &&` | `summary.rmssd = _dig(json, ['hrv', 'time', 'rmssd']) \|\| _dig(json, ['hrv` |
| 462 | `bool \|\| → &&` | `summary.rmssd = _dig(json, ['hrv', 'time', 'rmssd']) \|\| _dig(json, ['hrv` |
| 463 | `bool \|\| → &&` | `summary.sdnn = _dig(json, ['hrv', 'time', 'sdnn']) \|\| _dig(json, ['hrv',` |
| 463 | `bool \|\| → &&` | `summary.sdnn = _dig(json, ['hrv', 'time', 'sdnn']) \|\| _dig(json, ['hrv',` |
| 464 | `bool \|\| → &&` | `summary.lfhf = _dig(json, ['hrv', 'frequency', 'lfhf']) \|\| _dig(json, ['` |
| 464 | `bool \|\| → &&` | `summary.lfhf = _dig(json, ['hrv', 'frequency', 'lfhf']) \|\| _dig(json, ['` |
| 480 | `bool && → \|\|` | `return m && m.sdnn != null;` |
| 487 | `bool && → \|\|` | `return m && m.rmssd != null;` |
| 502 | `bool \|\| → &&` | `if (summary.rmssd != null \|\| summary.sdnn != null) {` |
| 525 | `bool \|\| → &&` | `summary.site = _dig(json, ['recording', 'site']) \|\| 'wrist';` |
| 555 | `cmp < → <=` | `for (var _pi = 0; _pi < events.length; _pi++) {` |
| 579 | `bool && → \|\|` | `summary.therapyHours = json.recording && json.recording.therapyHours != ` |
| 614 | `bool \|\| → &&` | `summary.dwellFrac = mo.dwellFrac \|\| null;` |
| 631 | `bool && → \|\|` | `if (summary.posture && summary.posture.length) summary.postureSource = '` |
| 644 | `bool \|\| → &&` | `if (tMin == null \|\| !isFinite(tMin)) return null;` |
| 648 | `bool && → \|\|` | `rmssd: e.rmssd != null && isFinite(e.rmssd) ? e.rmssd : null,` |
| 649 | `bool && → \|\|` | `hr: e.hr != null && isFinite(e.hr) ? e.hr : null,` |
| 656 | `bool && → \|\|` | `motion: e.motionIndex != null && isFinite(e.motionIndex) ? e.motionIndex` |
| 681 | `bool && → \|\|` | `var _hz = _sp.hz != null && isFinite(_sp.hz) && _sp.hz > 0 ? _sp.hz : 1;` |
| 681 | `cmp > → >=` | `var _hz = _sp.hz != null && isFinite(_sp.hz) && _sp.hz > 0 ? _sp.hz : 1;` |
| 681 | `bool && → \|\|` | `var _hz = _sp.hz != null && isFinite(_sp.hz) && _sp.hz > 0 ? _sp.hz : 1;` |
| 736 | `bool \|\| → &&` | `coverage: (json.recording && json.recording.coverage) \|\| null,` |
| 753 | `bool \|\| → &&` | `raw: { ganglior_events: json.ganglior_events \|\| json.events \|\| null },` |
| 770 | `bool \|\| → &&` | `if (!json \|\| t0Ms == null \|\| !isFinite(t0Ms)) return null;` |
| 770 | `bool \|\| → &&` | `if (!json \|\| t0Ms == null \|\| !isFinite(t0Ms)) return null;` |
| 773 | `bool \|\| → &&` | `if (!ser \|\| !ser.tSec \|\| !ser.tSec.length) return null;` |
| 778 | `cmp < → <=` | `for (var i = 0; i < src.length; i++) {` |
| 780 | `bool \|\| → &&` | `if (s == null \|\| !isFinite(s)) continue;` |
| 804 | `bool && → \|\|` | `var mo = (json && json.motion) \|\| {};` |
| 806 | `negate: drop !` | `if (!Array.isArray(src) \|\| !src.length) return null;` |
| 807 | `num → 0` | `var cadMs = (mo.effortCadenceSec != null ? mo.effortCadenceSec : 10) * 1` |
| 807 | `num → 0` | `var cadMs = (mo.effortCadenceSec != null ? mo.effortCadenceSec : 10) * 1` |
| 809 | `cmp < → <=` | `for (var i = 0; i < src.length; i++) {` |
| 810 | `bool \|\| → &&` | `var e = src[i] \|\| {};` |
| 850 | `cmp < → <=` | `for (var i = 0; i < evs.length; i++) {` |
| 852 | `bool \|\| → &&` | `if (!e \|\| e.impulse !== 'posture_change') continue;` |
| 853 | `bool && → \|\|` | `var tMs = e.tMs != null ? e.tMs : t0Ms != null && e.t ? reconstructEvent` |
| 855 | `bool && → \|\|` | `if (tMs != null && pos && pos !== 'unknown') steps.push({ tMs: tMs, pos:` |
| 855 | `bool && → \|\|` | `if (tMs != null && pos && pos !== 'unknown') steps.push({ tMs: tMs, pos:` |
| 862 | `cmp < → <=` | `for (i = 0; i < steps.length && out.length < _MOTION_POS_MAX; i++) {` |
| 868 | `cmp < → <=` | `for (var t = from; t < to && out.length < _MOTION_POS_MAX; t += _MOTION_` |
| 868 | `cmp < → <=` | `for (var t = from; t < to && out.length < _MOTION_POS_MAX; t += _MOTION_` |
| 877 | `bool && → \|\|` | `var mo = (json && json.motion) \|\| {};` |
| 879 | `negate: drop !` | `if (!Array.isArray(src) \|\| !src.length) return null;` |
| 880 | `num → 0` | `var cadMs = (mo.activityCadenceSec != null ? mo.activityCadenceSec : 30)` |
| 880 | `num → 0` | `var cadMs = (mo.activityCadenceSec != null ? mo.activityCadenceSec : 30)` |
| 882 | `cmp < → <=` | `for (var i = 0; i < src.length; i++) {` |
| 883 | `bool \|\| → &&` | `var e = src[i] \|\| {};` |
| 891 | `bool \|\| → &&` | `var acc = json.acc \|\| (json.timeseries && json.timeseries.acc) \|\| null;` |
| 891 | `bool \|\| → &&` | `var acc = json.acc \|\| (json.timeseries && json.timeseries.acc) \|\| null;` |
| 894 | `bool && → \|\|` | `var tMs = t0Ms != null && tMin != null ? t0Ms + tMin * 60000 : null;` |
| 894 | `num → 0` | `var tMs = t0Ms != null && tMin != null ? t0Ms + tMin * 60000 : null;` |
| 899 | `bool \|\| → &&` | `push(a.tMin != null ? a.tMin : a.t, a.position \|\| a.pos \|\| a.bodyPositio` |
| 899 | `bool \|\| → &&` | `push(a.tMin != null ? a.tMin : a.t, a.position \|\| a.pos \|\| a.bodyPositio` |
| 914 | `bool \|\| → &&` | `var p = e.meta && (e.meta.position \|\| e.meta.pos \|\| e.meta.bodyPosition)` |
| 914 | `bool \|\| → &&` | `var p = e.meta && (e.meta.position \|\| e.meta.pos \|\| e.meta.bodyPosition)` |
| 922 | `bool \|\| → &&` | `return (a.tMs \|\| 0) - (b.tMs \|\| 0);` |
| 922 | `bool \|\| → &&` | `return (a.tMs \|\| 0) - (b.tMs \|\| 0);` |
| 936 | `bool && → \|\|` | `var _topKernel = json && !Array.isArray(json) && json.kernel ? json.kern` |
| 936 | `bool && → \|\|` | `var _topKernel = json && !Array.isArray(json) && json.kernel ? json.kern` |
| 936 | `negate: drop !` | `var _topKernel = json && !Array.isArray(json) && json.kernel ? json.kern` |
| 941 | `negate: drop !` | `var _topT0 = json && !Array.isArray(json) && json.recording && json.reco` |
| 949 | `bool && → \|\|` | `var s = nn && nn.t0Ms != null ? nn.t0Ms : nn && nn.stats && nn.stats.t0M` |
| 949 | `bool && → \|\|` | `var s = nn && nn.t0Ms != null ? nn.t0Ms : nn && nn.stats && nn.stats.t0M` |
| 949 | `bool && → \|\|` | `var s = nn && nn.t0Ms != null ? nn.t0Ms : nn && nn.stats && nn.stats.t0M` |
| 949 | `bool && → \|\|` | `var s = nn && nn.t0Ms != null ? nn.t0Ms : nn && nn.stats && nn.stats.t0M` |
| 950 | `bool && → \|\|` | `var dm = nn && nn.stats && nn.stats.durationMin != null ? nn.stats.durat` |
| 950 | `bool && → \|\|` | `var dm = nn && nn.stats && nn.stats.durationMin != null ? nn.stats.durat` |
| 950 | `num → 0` | `var dm = nn && nn.stats && nn.stats.durationMin != null ? nn.stats.durat` |
| 951 | `bool && → \|\|` | `return { s: s, e: s != null && dm != null ? s + dm : null };` |
| 962 | `cmp < → <=` | `if (w.s == null \|\| e.tMs < w.s) continue;` |
| 962 | `bool \|\| → &&` | `if (w.s == null \|\| e.tMs < w.s) continue;` |
| 963 | `bool && → \|\|` | `if (w.e != null && e.tMs > w.e) continue;` |
| 963 | `cmp > → >=` | `if (w.e != null && e.tMs > w.e) continue;` |
| 964 | `cmp > → >=` | `if (w.s > bestS) {` |
| 970 | `cmp < → <=` | `if (best < 0) {` |
| 971 | `cmp < → <=` | `for (var wj = 0; wj < _wins.length; wj++) {` |
| 973 | `cmp >= → >` | `if (w2.s != null && e.tMs >= w2.s && w2.s > bestS) {` |
| 973 | `cmp > → >=` | `if (w2.s != null && e.tMs >= w2.s && w2.s > bestS) {` |
| 973 | `bool && → \|\|` | `if (w2.s != null && e.tMs >= w2.s && w2.s > bestS) {` |
| 973 | `bool && → \|\|` | `if (w2.s != null && e.tMs >= w2.s && w2.s > bestS) {` |
| 984 | `bool \|\| → &&` | `if (!n \|\| typeof n !== 'object') return;` |
| 987 | `bool \|\| → &&` | `var nSamp = stats.n \|\| 0;` |
| 989 | `bool && → \|\|` | `var dt = durMs && nSamp ? durMs / nSamp : 1000; // O2Ring ≈ 1 Hz` |
| 989 | `num → 0` | `var dt = durMs && nSamp ? durMs / nSamp : 1000; // O2Ring ≈ 1 Hz` |
| 990 | `bool && → \|\|` | `var endMs = t0Ms != null && durMs != null ? t0Ms + durMs : null;` |
| 1006 | `bool \|\| → &&` | `if (idx == null \|\| t0Ms == null) return;` |
| 1009 | `num → 0` | `var conf = clamp(0.45 + (depth != null ? Math.min(depth, 12) / 24 : 0.1)` |
| 1009 | `num → 0` | `var conf = clamp(0.45 + (depth != null ? Math.min(depth, 12) / 24 : 0.1)` |
| 1009 | `num → 0` | `var conf = clamp(0.45 + (depth != null ? Math.min(depth, 12) / 24 : 0.1)` |
| 1009 | `num → 0` | `var conf = clamp(0.45 + (depth != null ? Math.min(depth, 12) / 24 : 0.1)` |
| 1009 | `num → 0` | `var conf = clamp(0.45 + (depth != null ? Math.min(depth, 12) / 24 : 0.1)` |
| 1009 | `num → 0` | `var conf = clamp(0.45 + (depth != null ? Math.min(depth, 12) / 24 : 0.1)` |
| 1024 | `bool && → \|\|` | `typeof DexClock !== 'undefined' && DexClock.resolveDMY` |
| 1034 | `bool && → \|\|` | `var tMs = p ? p.tMs : sp.idx != null && t0Ms != null ? t0Ms + sp.idx * d` |
| 1036 | `bool && → \|\|` | `var rise = sp.peak != null && sp.baseline != null ? sp.peak - sp.baselin` |
| 1037 | `num → 0` | `var conf = clamp(0.4 + (rise != null ? Math.min(rise, 40) / 80 : 0.15), ` |
| 1037 | `num → 0` | `var conf = clamp(0.4 + (rise != null ? Math.min(rise, 40) / 80 : 0.15), ` |
| 1037 | `num → 0` | `var conf = clamp(0.4 + (rise != null ? Math.min(rise, 40) / 80 : 0.15), ` |
| 1037 | `num → 0` | `var conf = clamp(0.4 + (rise != null ? Math.min(rise, 40) / 80 : 0.15), ` |
| 1037 | `num → 0` | `var conf = clamp(0.4 + (rise != null ? Math.min(rise, 40) / 80 : 0.15), ` |
| 1037 | `num → 0` | `var conf = clamp(0.4 + (rise != null ? Math.min(rise, 40) / 80 : 0.15), ` |
| 1071 | `eq === → !==` | `return e.impulse === 'spo2_desaturation' \|\| e.impulse === 'desat_event';` |
| 1071 | `eq === → !==` | `return e.impulse === 'spo2_desaturation' \|\| e.impulse === 'desat_event';` |
| 1071 | `bool \|\| → &&` | `return e.impulse === 'spo2_desaturation' \|\| e.impulse === 'desat_event';` |
| 1083 | `cmp > → >=` | `var _remImplausible = !!_sp && (_sp.plausible === false \|\| (_sp.remProxy` |
| 1083 | `bool \|\| → &&` | `var _remImplausible = !!_sp && (_sp.plausible === false \|\| (_sp.remProxy` |
| 1085 | `num → 0` | `summary.remFraction = +(_sp.remProxyPct / 100).toFixed(3);` |
| 1086 | `num → 0` | `summary.deepFraction = _sp.nremDeepPct != null ? +(_sp.nremDeepPct / 100` |
| 1097 | `bool \|\| → &&` | `summary.stagingSuppressed = _sp.plausibilityNote \|\| 'oximetry REM proxy ` |
| 1100 | `bool \|\| → &&` | `uid: 'OxyDex@' + (t0Ms \|\| 'n' + ni),` |
| 1102 | `bool \|\| → &&` | `label: 'OxyDex · ' + (t0Ms != null ? fmtDayShort(t0Ms) : n.date \|\| 'nigh` |
| 1103 | `bool \|\| → &&` | `dateStr: n.date \|\| (t0Ms != null ? fmtDate(t0Ms) : null),` |
| 1111 | `bool \|\| → &&` | `kernelHash: _kernelHash(n.kernel \|\| _topKernel),` |
| 1112 | `bool \|\| → &&` | `kernelVersion: _kernelVersion(n.kernel \|\| _topKernel),` |
| 1155 | `cmp < → <=` | `if (node !== 'Unknown' && KNOWN_NODES.indexOf(node) < 0) {` |
| 1173 | `bool && → \|\|` | `json.t0Ms != null &&` |
| 1174 | `bool \|\| → &&` | `(Array.isArray(json.windows) \|\| json.nWindows != null \|\| json.durMin != ` |
| 1174 | `bool \|\| → &&` | `(Array.isArray(json.windows) \|\| json.nWindows != null \|\| json.durMin != ` |
| 1178 | `bool \|\| → &&` | `(filename \|\| node) +` |
| 1212 | `bool && → \|\|` | `if (Array.isArray(json[_c.key]) && json.schema && json.schema[_c.flag]) ` |
| 1222 | `bool && → \|\|` | `if (_nightRecs && _nightRecs.length) mnRecs = mnRecs.concat(_nightRecs);` |
| 1230 | `bool \|\| → &&` | `warnings: warnings.concat(['"' + (filename \|\| 'file') + '" — ' + node + ` |
| 1240 | `bool \|\| → &&` | `return { recs: [], warnings: warnings.concat(['"' + (filename \|\| 'file')` |
| 1255 | `bool \|\| → &&` | `var raw = r.raw \|\| {};` |
| 1256 | `bool \|\| → &&` | `var ev = raw.ganglior_events \|\| raw.events \|\| r.events \|\| [];` |
| 1256 | `bool \|\| → &&` | `var ev = raw.ganglior_events \|\| raw.events \|\| r.events \|\| [];` |
| 1256 | `bool \|\| → &&` | `var ev = raw.ganglior_events \|\| raw.events \|\| r.events \|\| [];` |
| 1257 | `bool \|\| → &&` | `var first = ev.length ? ev[0].t \|\| ev[0].tMs \|\| '' : '';` |
| 1257 | `bool \|\| → &&` | `var first = ev.length ? ev[0].t \|\| ev[0].tMs \|\| '' : '';` |
| 1258 | `bool \|\| → &&` | `var last = ev.length ? ev[ev.length - 1].t \|\| ev[ev.length - 1].tMs \|\| '` |
| 1258 | `bool \|\| → &&` | `var last = ev.length ? ev[ev.length - 1].t \|\| ev[ev.length - 1].tMs \|\| '` |
| 1259 | `bool && → \|\|` | `var rm = r.summary && r.summary.rmssd != null ? r.summary.rmssd : '';` |
| 1260 | `bool && → \|\|` | `var sd = r.summary && r.summary.sdnn != null ? r.summary.sdnn : '';` |
| 1274 | `bool && → \|\|` | `if (e.contentId && nr.contentId) return e.contentId === nr.contentId;` |
| 1275 | `bool && → \|\|` | `if (e.t0Ms != null && nr.t0Ms != null) return Math.abs(e.t0Ms - nr.t0Ms)` |
| 1275 | `cmp <= → <` | `if (e.t0Ms != null && nr.t0Ms != null) return Math.abs(e.t0Ms - nr.t0Ms)` |
| 1276 | `bool && → \|\|` | `if (e.t0Ms == null && nr.t0Ms == null) return _recSig(e) === nsig; // st` |
| 1302 | `bool \|\| → &&` | `if (!cov \|\| !Array.isArray(cov.segments) \|\| !cov.segments.length) return` |
| 1304 | `cmp < → <=` | `for (var i = 0; i < cov.segments.length; i++) {` |
| 1306 | `bool \|\| → &&` | `if (!sg \|\| sg.startMs == null \|\| !isFinite(sg.startMs)) continue;` |
| 1306 | `bool \|\| → &&` | `if (!sg \|\| sg.startMs == null \|\| !isFinite(sg.startMs)) continue;` |
| 1309 | `bool && → \|\|` | `var dur = sg.durSec != null && isFinite(sg.durSec) && sg.durSec > 0 ? sg` |
| 1309 | `cmp > → >=` | `var dur = sg.durSec != null && isFinite(sg.durSec) && sg.durSec > 0 ? sg` |
| 1309 | `bool && → \|\|` | `var dur = sg.durSec != null && isFinite(sg.durSec) && sg.durSec > 0 ? sg` |
| 1326 | `bool \|\| → &&` | `if (!sa \|\| !sb) return null;` |
| 1332 | `cmp > → >=` | `if (e > s) totalMs += e - s;` |
| 1340 | `bool \|\| → &&` | `if (!wa \|\| !wb) return null;` |
| 1347 | `eq !== → ===` | `if (a.offsetMin != null && b.offsetMin != null && a.offsetMin !== b.offs` |
| 1347 | `bool && → \|\|` | `if (a.offsetMin != null && b.offsetMin != null && a.offsetMin !== b.offs` |
| 1347 | `bool && → \|\|` | `if (a.offsetMin != null && b.offsetMin != null && a.offsetMin !== b.offs` |
| 1348 | `num → 0` | `sa -= a.offsetMin * 60000;` |
| 1349 | `num → 0` | `ea -= a.offsetMin * 60000;` |
| 1350 | `num → 0` | `sb -= b.offsetMin * 60000;` |
| 1351 | `num → 0` | `eb -= b.offsetMin * 60000;` |
| 1356 | `cmp <= → <` | `if (e <= s) return null;` |
| 1376 | `negate: drop !` | `if (!sa && !sb) {` |
| 1382 | `bool \|\| → &&` | `if (!wa \|\| !wb) return [];` |
| 1389 | `eq !== → ===` | `if (a.offsetMin != null && b.offsetMin != null && a.offsetMin !== b.offs` |
| 1389 | `bool && → \|\|` | `if (a.offsetMin != null && b.offsetMin != null && a.offsetMin !== b.offs` |
| 1389 | `bool && → \|\|` | `if (a.offsetMin != null && b.offsetMin != null && a.offsetMin !== b.offs` |
| 1390 | `num → 0` | `shA = a.offsetMin * 60000;` |
| 1391 | `num → 0` | `shB = b.offsetMin * 60000;` |
| 1398 | `cmp > → >=` | `if (en > st) out.push([st + shA, en + shA]);` |
| 1412 | `bool \|\| → &&` | `if (!recWindow(a) \|\| !recWindow(b)) return true; // unknown ⇒ cannot dis` |
| 1420 | `bool && → \|\|` | `return !!(recWindow(a) && recWindow(b) && overlapInterval(a, b));` |
| 1420 | `bool && → \|\|` | `return !!(recWindow(a) && recWindow(b) && overlapInterval(a, b));` |
| 1476 | `bool && → \|\|` | `var expectSurge = depth != null && depth >= CONSEQUENCE.EXPECT_DEPTH_PCT` |
| 1476 | `cmp >= → >` | `var expectSurge = depth != null && depth >= CONSEQUENCE.EXPECT_DEPTH_PCT` |
| 1486 | `cmp <= → <` | `return t != null && t >= w0 && t <= w1;` |
| 1486 | `bool && → \|\|` | `return t != null && t >= w0 && t <= w1;` |
| 1486 | `bool && → \|\|` | `return t != null && t >= w0 && t <= w1;` |
| 1486 | `cmp >= → >` | `return t != null && t >= w0 && t <= w1;` |
| 1541 | `num → 0` | `var APNEA_TYPE_LEAD_MS = 15000; // look this far BEFORE the desat — SpO₂` |
| 1556 | `num → 0` | `cadMs = 10000;` |
| 1560 | `num → 0` | `if (r.summary.effortCadenceSec != null) cadMs = r.summary.effortCadenceS` |
| 1596 | `bool && → \|\|` | `var durMs = (d.ev.meta && d.ev.meta.durSec != null ? d.ev.meta.durSec : ` |
| 1596 | `num → 0` | `var durMs = (d.ev.meta && d.ev.meta.durSec != null ? d.ev.meta.durSec : ` |
| 1596 | `num → 0` | `var durMs = (d.ev.meta && d.ev.meta.durSec != null ? d.ev.meta.durSec : ` |
| 1600 | `cmp < → <=` | `for (var i = 0; i < eps.length; i++) {` |
| 1601 | `cmp <= → <` | `if (eps[i].tMs + cadMs <= lo) continue;` |
| 1602 | `cmp >= → >` | `if (eps[i].tMs >= hi) break;` |
| 1672 | `cmp <= → <` | `if (cur[0] <= last[1])` |
| 1698 | `eq !== → ===` | `if (b.hours !== a.hours) return b.hours - a.hours; // more of the night ` |
| 1699 | `num → 0` | `var ra = DESAT_OBSERVER_AUTHORITY[a.node] != null ? DESAT_OBSERVER_AUTHO` |
| 1700 | `num → 0` | `var rb = DESAT_OBSERVER_AUTHORITY[b.node] != null ? DESAT_OBSERVER_AUTHO` |
| 1701 | `eq !== → ===` | `if (ra !== rb) return ra - rb;` |
| 1702 | `cmp > → >=` | `return a.node < b.node ? -1 : a.node > b.node ? 1 : 0;` |
| 1702 | `cmp < → <=` | `return a.node < b.node ? -1 : a.node > b.node ? 1 : 0;` |
| 1722 | `cmp < → <=` | `if (r && recSegments(r) && declaredBy.indexOf(r.node) < 0) declaredBy.pu` |
| 1731 | `cmp > → >=` | `recordedFrac: envHrs > 0 ? +(totHrs / envHrs).toFixed(3) : null,` |
| 1732 | `bool && → \|\|` | `segments: u && u.merged ? u.merged.length : 0,` |
| 1755 | `bool \|\| → &&` | `if (!oxyAll.length \|\| !cardiac.length) return null;` |
| 1765 | `bool \|\| → &&` | `gate = gate \|\| {};` |
| 1766 | `num → 0` | `var leadMaxSec = gate.leadMaxSec != null ? gate.leadMaxSec : 15;` |
| 1767 | `num → 0` | `var trailMaxSec = gate.trailMaxSec != null ? gate.trailMaxSec : 60;` |
| 1796 | `cmp <= → <` | `if (tMs >= merged[i][0] - dtMs && tMs <= merged[i][1] + dtMs) return tru` |
| 1796 | `cmp >= → >` | `if (tMs >= merged[i][0] - dtMs && tMs <= merged[i][1] + dtMs) return tru` |
| 1796 | `bool && → \|\|` | `if (tMs >= merged[i][0] - dtMs && tMs <= merged[i][1] + dtMs) return tru` |
| 1840 | `cmp > → >=` | `if (lat < -leadMaxSec \|\| lat > trailMaxSec) return; // directionality ga` |
| 1840 | `cmp < → <=` | `if (lat < -leadMaxSec \|\| lat > trailMaxSec) return; // directionality ga` |
| 1840 | `bool \|\| → &&` | `if (lat < -leadMaxSec \|\| lat > trailMaxSec) return; // directionality ga` |
| 1842 | `cmp < → <=` | `if (dd < bd) {` |
| 1852 | `bool \|\| → &&` | `durSec: (d.meta && d.meta.durSec) \|\| null,` |
| 1852 | `bool && → \|\|` | `durSec: (d.meta && d.meta.durSec) \|\| null,` |
| 1861 | `bool \|\| → &&` | `nodes: [d.node \|\| 'OxyDex', s.node \|\| 'ECGDex'],` |
| 1863 | `bool \|\| → &&` | `{ node: d.node \|\| 'OxyDex', impulse: d.impulse, tMs: d.tMs, conf: d.conf` |
| 1864 | `bool \|\| → &&` | `{ node: s.node \|\| 'ECGDex', impulse: s.impulse, tMs: s.tMs, conf: s.conf` |
| 1864 | `bool \|\| → &&` | `{ node: s.node \|\| 'ECGDex', impulse: s.impulse, tMs: s.tMs, conf: s.conf` |
| 1866 | `bool && → \|\|` | `meta: { desatDepth: d.meta && d.meta.depth, nadir: d.meta && d.meta.nadi` |
| 1866 | `bool && → \|\|` | `meta: { desatDepth: d.meta && d.meta.depth, nadir: d.meta && d.meta.nadi` |
| 1866 | `bool \|\| → &&` | `meta: { desatDepth: d.meta && d.meta.depth, nadir: d.meta && d.meta.nadi` |
| 1866 | `num → 0` | `meta: { desatDepth: d.meta && d.meta.depth, nadir: d.meta && d.meta.nadi` |
| 1874 | `negate: drop !` | `return !usedSurge.has(si);` |
| 1891 | `bool \|\| → &&` | `var k = s.node \|\| 'ECGDex';` |
| 1904 | `bool \|\| → &&` | `hrWitness: (pickHRAuthority(hrNodesLive) \|\| {}).node \|\| null,` |
| 1907 | `eq === → !==` | `return d.verdict === 'artifact-no-consequence';` |
| 1910 | `eq === → !==` | `return d.verdict === 'unconfirmed-desat';` |
| 1920 | `num → 0` | `var unionSec = totHrs * 3600;` |
| 1937 | `bool \|\| → &&` | `_surgeByNode[n] = (_surgeByNode[n] \|\| 0) + 1;` |
| 1940 | `num → 0` | `var ra = HR_AUTHORITY[a] != null ? HR_AUTHORITY[a] : 99,` |
| 1941 | `num → 0` | `rb = HR_AUTHORITY[b] != null ? HR_AUTHORITY[b] : 99;` |
| 1942 | `eq !== → ===` | `if (ra !== rb) return ra - rb;` |
| 1943 | `eq !== → ===` | `if (_surgeByNode[b] !== _surgeByNode[a]) return _surgeByNode[b] - _surge` |
| 1944 | `cmp < → <=` | `return a < b ? -1 : 1;` |
| 1948 | `cmp > → >=` | `var surgeRate = unionSec > 0 ? _rateCount / unionSec : 0; // surges per ` |
| 1954 | `cmp >= → >` | `var belowChance = nConf === 0 \|\| nConf <= lambda \|\| pAtLeast >= 0.05;` |
| 1954 | `cmp <= → <` | `var belowChance = nConf === 0 \|\| nConf <= lambda \|\| pAtLeast >= 0.05;` |
| 1954 | `eq === → !==` | `var belowChance = nConf === 0 \|\| nConf <= lambda \|\| pAtLeast >= 0.05;` |
| 1954 | `bool \|\| → &&` | `var belowChance = nConf === 0 \|\| nConf <= lambda \|\| pAtLeast >= 0.05;` |
| 1954 | `bool \|\| → &&` | `var belowChance = nConf === 0 \|\| nConf <= lambda \|\| pAtLeast >= 0.05;` |
| 1954 | `num → 0` | `var belowChance = nConf === 0 \|\| nConf <= lambda \|\| pAtLeast >= 0.05;` |
| 1971 | `eq !== → ===` | `var _EC = (typeof EventCoupling !== 'undefined' && EventCoupling) \|\| (ty` |
| 1971 | `eq !== → ===` | `var _EC = (typeof EventCoupling !== 'undefined' && EventCoupling) \|\| (ty` |
| 1971 | `bool \|\| → &&` | `var _EC = (typeof EventCoupling !== 'undefined' && EventCoupling) \|\| (ty` |
| 1971 | `bool && → \|\|` | `var _EC = (typeof EventCoupling !== 'undefined' && EventCoupling) \|\| (ty` |
| 1971 | `bool \|\| → &&` | `var _EC = (typeof EventCoupling !== 'undefined' && EventCoupling) \|\| (ty` |
| 1973 | `bool && → \|\|` | `if (_EC && typeof _EC.coupling === 'function' && desats.length && surges` |
| 1973 | `bool && → \|\|` | `if (_EC && typeof _EC.coupling === 'function' && desats.length && surges` |
| 1973 | `bool && → \|\|` | `if (_EC && typeof _EC.coupling === 'function' && desats.length && surges` |
| 1973 | `bool && → \|\|` | `if (_EC && typeof _EC.coupling === 'function' && desats.length && surges` |
| 1981 | `bool && → \|\|` | `var usable = !ec.underpowered && !ec.saturated;` |
| 2005 | `cmp < → <=` | `real: usable && isFinite(ec.pPerm) && ec.pPerm < COUPLING_ALPHA` |
| 2010 | `cmp > → >=` | `var ahi = totHrs > 0 ? +(nConf / totHrs).toFixed(2) : null;` |
| 2014 | `bool && → \|\|` | `confirmedAHIReportable: !belowChance && nConf > 0,` |
| 2014 | `cmp > → >=` | `confirmedAHIReportable: !belowChance && nConf > 0,` |
| 2014 | `negate: drop !` | `confirmedAHIReportable: !belowChance && nConf > 0,` |
| 2033 | `num → 0` | `surgeRatePerHr: +(surgeRate * 3600).toFixed(1),` |
| 2068 | `cmp <= → <` | `agreement: confirmedIndex != null ? (confirmedIndex <= dev * 1.2 ? 'cons` |
| 2068 | `num → 0` | `agreement: confirmedIndex != null ? (confirmedIndex <= dev * 1.2 ? 'cons` |
| 2077 | `cmp <= → <` | `if (lam <= 0) return k <= 0 ? 1 : 0;` |
| 2077 | `cmp <= → <` | `if (lam <= 0) return k <= 0 ? 1 : 0;` |
| 2080 | `cmp < → <=` | `for (var i = 0; i < k; i++) {` |
| 2089 | `bool \|\| → &&` | `if (!apneaResult \|\| !apneaResult.findings.length) return null;` |
| 2110 | `bool && → \|\|` | `if (g.summary && g.summary.posture) posture = posture.concat(g.summary.p` |
| 2141 | `cmp < → <=` | `if (d < bd) {` |
| 2146 | `cmp <= → <` | `return best && bd <= 10 * 60000 ? /** @type {any} */ (best).pos : null;` |
| 2159 | `cmp > → >=` | `var rate = supine + nonsupine > 0 ? supine / (supine + nonsupine) : null` |
| 2160 | `cmp >= → >` | `var positional = rate != null && supine >= 3 && rate >= 0.7 && (nonsupin` |
| 2160 | `cmp >= → >` | `var positional = rate != null && supine >= 3 && rate >= 0.7 && (nonsupin` |
| 2160 | `eq === → !==` | `var positional = rate != null && supine >= 3 && rate >= 0.7 && (nonsupin` |
| 2189 | `cmp < → <=` | `if (n < 4) return Math.round(sd);` |
| 2193 | `cmp < → <=` | `for (i = 1; i < n; i++) {` |
| 2195 | `eq === → !==` | `if (dir === 0) {` |
| 2196 | `cmp > → >=` | `if (Math.abs(d) > 0) dir = d > 0 ? 1 : -1;` |
| 2196 | `cmp > → >=` | `if (Math.abs(d) > 0) dir = d > 0 ? 1 : -1;` |
| 2199 | `cmp > → >=` | `var sg = s > 0 ? 1 : s < 0 ? -1 : 0;` |
| 2199 | `cmp < → <=` | `var sg = s > 0 ? 1 : s < 0 ? -1 : 0;` |
| 2200 | `eq !== → ===` | `if (sg !== 0 && sg !== dir) {` |
| 2200 | `eq !== → ===` | `if (sg !== 0 && sg !== dir) {` |
| 2200 | `bool && → \|\|` | `if (sg !== 0 && sg !== dir) {` |
| 2208 | `cmp < → <=` | `for (i = 1; i < tp.length; i++) {` |
| 2210 | `cmp > → >=` | `if (amp > sd) exc.push(amp);` |
| 2212 | `negate: drop !` | `if (!exc.length) return Math.round(sd);` |
| 2214 | `cmp < → <=` | `for (i = 0; i < exc.length; i++) sum += exc[i];` |
| 2219 | `num → 0` | `var minCov = opts.minCoverage != null ? opts.minCoverage : 0.5;` |
| 2220 | `num → 0` | `var minMin = opts.minWindowMin != null ? opts.minWindowMin : 60; // reje` |
| 2222 | `cmp <= → <` | `if (!cells.length \|\| startMs == null \|\| endMs == null \|\| endMs <= startM` |
| 2222 | `bool \|\| → &&` | `if (!cells.length \|\| startMs == null \|\| endMs == null \|\| endMs <= startM` |
| 2222 | `bool \|\| → &&` | `if (!cells.length \|\| startMs == null \|\| endMs == null \|\| endMs <= startM` |
| 2222 | `bool \|\| → &&` | `if (!cells.length \|\| startMs == null \|\| endMs == null \|\| endMs <= startM` |
| 2223 | `cmp > → >=` | `var cadMin = cgmRec.series.cadenceMin != null && cgmRec.series.cadenceMi` |
| 2223 | `bool && → \|\|` | `var cadMin = cgmRec.series.cadenceMin != null && cgmRec.series.cadenceMi` |
| 2225 | `cmp < → <=` | `if (nMin < minMin) return null; // honest: overlap too short to characte` |
| 2229 | `cmp <= → <` | `if (cells[i].tMs >= startMs && cells[i].tMs <= endMs) win.push(cells[i])` |
| 2229 | `cmp >= → >` | `if (cells[i].tMs >= startMs && cells[i].tMs <= endMs) win.push(cells[i])` |
| 2229 | `bool && → \|\|` | `if (cells[i].tMs >= startMs && cells[i].tMs <= endMs) win.push(cells[i])` |
| 2255 | `cmp > → >=` | `var coverage = expected > 0 ? vals.length / expected : 0;` |
| 2256 | `cmp < → <=` | `if (vals.length < 3 \|\| coverage < minCov) return null; // honest: too th` |
| 2256 | `cmp < → <=` | `if (vals.length < 3 \|\| coverage < minCov) return null; // honest: too th` |
| 2262 | `cmp < → <=` | `for (i = 0; i < vals.length; i++) {` |
| 2266 | `cmp > → >=` | `var sd = vals.length > 1 ? Math.sqrt(ss / (vals.length - 1)) : 0;` |
| 2267 | `cmp > → >=` | `var cv = m > 0 ? (sd / m) * 100 : 0;` |
| 2267 | `num → 0` | `var cv = m > 0 ? (sd / m) * 100 : 0;` |
| 2273 | `cmp < → <=` | `for (i = 0; i < vals.length; i++) {` |
| 2275 | `bool && → \|\|` | `if (g >= 70 && g <= 140) tt++;` |
| 2275 | `cmp >= → >` | `if (g >= 70 && g <= 140) tt++;` |
| 2275 | `cmp <= → <` | `if (g >= 70 && g <= 140) tt++;` |
| 2275 | `num → 0` | `if (g >= 70 && g <= 140) tt++;` |
| 2275 | `num → 0` | `if (g >= 70 && g <= 140) tt++;` |
| 2276 | `num → 0` | `if (g < 70) below++;` |
| 2276 | `cmp < → <=` | `if (g < 70) below++;` |
| 2277 | `cmp < → <=` | `if (g < nadir) {` |
| 2286 | `cmp < → <=` | `for (i = 0; i < vals.length; i++) {` |
| 2288 | `cmp >= → >` | `if (h >= 3 && h < 6) {` |
| 2288 | `cmp < → <=` | `if (h >= 3 && h < 6) {` |
| 2288 | `bool && → \|\|` | `if (h >= 3 && h < 6) {` |
| 2289 | `cmp < → <=` | `if (vals[i] < dawnNadir) dawnNadir = vals[i];` |
| 2290 | `cmp >= → >` | `} else if (h >= 6 && h < 8) {` |
| 2290 | `cmp < → <=` | `} else if (h >= 6 && h < 8) {` |
| 2290 | `bool && → \|\|` | `} else if (h >= 6 && h < 8) {` |
| 2291 | `cmp > → >=` | `if (vals[i] > dawnMax) {` |
| 2297 | `bool && → \|\|` | `var dawnRise = isFinite(dawnNadir) && isFinite(dawnMax) ? Math.round(daw` |
| 2308 | `num → 0` | `tir70_140: +((tt / vals.length) * 100).toFixed(1),` |
| 2319 | `bool \|\| → &&` | `if (!ecg.length \|\| !glu.length) return null;` |
| 2325 | `bool && → \|\|` | `var slope = g.summary && g.summary.autonomicInstabilitySlope;` |
| 2333 | `bool && → \|\|` | `var gm = glucoseMetricsInWindow(c, win.startMs, win.endMs, { minCoverage` |
| 2334 | `negate: drop !` | `if (!gm) return; // thin window → skip, honest (no fabricated CV)` |
| 2342 | `bool && → \|\|` | `dawn = c.summary && c.summary.dawnSurge;` |
| 2357 | `cmp >= → >` | `var r = xs.length >= 3 ? pearson(xs, ys) : null;` |
| 2365 | `bool && → \|\|` | `if (r == null && pairs.length) {` |
| 2369 | `num → 0` | `if (p0) directional = clamp(0.5 + clamp(p0.slope, -0.5, 0.5), 0, 1);` |
| 2369 | `num → 0` | `if (p0) directional = clamp(0.5 + clamp(p0.slope, -0.5, 0.5), 0, 1);` |
| 2369 | `num → 0` | `if (p0) directional = clamp(0.5 + clamp(p0.slope, -0.5, 0.5), 0, 1);` |
| 2399 | `eq !== → ===` | `return (typeof IntegratorTCH !== 'undefined' && IntegratorTCH) \|\| (typeo` |
| 2399 | `eq !== → ===` | `return (typeof IntegratorTCH !== 'undefined' && IntegratorTCH) \|\| (typeo` |
| 2399 | `bool && → \|\|` | `return (typeof IntegratorTCH !== 'undefined' && IntegratorTCH) \|\| (typeo` |
| 2399 | `bool \|\| → &&` | `return (typeof IntegratorTCH !== 'undefined' && IntegratorTCH) \|\| (typeo` |
| 2399 | `bool \|\| → &&` | `return (typeof IntegratorTCH !== 'undefined' && IntegratorTCH) \|\| (typeo` |
| 2409 | `bool && → \|\|` | `return e && e.tMs != null && isFinite(e.tMs) ? Math.round(e.tMs / 300000` |
| 2409 | `bool && → \|\|` | `return e && e.tMs != null && isFinite(e.tMs) ? Math.round(e.tMs / 300000` |
| 2414 | `bool && → \|\|` | `return e && e.tMin != null && e.rmssd != null;` |
| 2414 | `bool && → \|\|` | `return e && e.tMin != null && e.rmssd != null;` |
| 2424 | `bool && → \|\|` | `return e && e.tMin != null && e.hr != null;` |
| 2424 | `bool && → \|\|` | `return e && e.tMin != null && e.hr != null;` |
| 2435 | `bool \|\| → &&` | `var eps = (s.series && s.series.hrvEpochs) \|\| [];` |
| 2438 | `bool && → \|\|` | `return e && set[_epKey(e)] && e.motion != null;` |
| 2438 | `bool && → \|\|` | `return e && set[_epKey(e)] && e.motion != null;` |
| 2443 | `negate: drop !` | `if (!vs.length) return null;` |
| 2455 | `bool && → \|\|` | `if (e && e.tMin != null && e.motion != null) m[_epKey(e)] = e.motion;` |
| 2455 | `bool && → \|\|` | `if (e && e.tMin != null && e.motion != null) m[_epKey(e)] = e.motion;` |
| 2470 | `cmp < → <=` | `for (i = 0; i < a.length; i++) {` |
| 2471 | `bool && → \|\|` | `if (a[i] != null && b[i] != null && isFinite(a[i]) && isFinite(b[i])) {` |
| 2471 | `bool && → \|\|` | `if (a[i] != null && b[i] != null && isFinite(a[i]) && isFinite(b[i])) {` |
| 2471 | `bool && → \|\|` | `if (a[i] != null && b[i] != null && isFinite(a[i]) && isFinite(b[i])) {` |
| 2477 | `cmp < → <=` | `if (n < 4) return null;` |
| 2496 | `cmp < → <=` | `if (sxx < 1e-12 \|\| syy < 1e-12) return null;` |
| 2496 | `cmp < → <=` | `if (sxx < 1e-12 \|\| syy < 1e-12) return null;` |
| 2496 | `bool \|\| → &&` | `if (sxx < 1e-12 \|\| syy < 1e-12) return null;` |
| 2496 | `num → 0` | `if (sxx < 1e-12 \|\| syy < 1e-12) return null;` |
| 2496 | `num → 0` | `if (sxx < 1e-12 \|\| syy < 1e-12) return null;` |
| 2511 | `cmp < → <=` | `if (ms.length < 2) return null;` |
| 2514 | `cmp < → <=` | `for (var i = 0; i < ms.length; i++)` |
| 2527 | `cmp < → <=` | `for (var k = 0; k < ms[i].m.length; k++) {` |
| 2530 | `bool && → \|\|` | `if (a != null && b != null && isFinite(a) && isFinite(b)) nOv++;` |
| 2530 | `bool && → \|\|` | `if (a != null && b != null && isFinite(a) && isFinite(b)) nOv++;` |
| 2530 | `bool && → \|\|` | `if (a != null && b != null && isFinite(a) && isFinite(b)) nOv++;` |
| 2562 | `cmp > → >=` | `denom > 0` |
| 2606 | `num → 0` | `return ptsFn(s).length >= 12;` |
| 2619 | `cmp < → <=` | `for (var i = 0; i < ws.length; i++)` |
| 2620 | `cmp < → <=` | `for (var j = i + 1; j < ws.length; j++)` |
| 2623 | `cmp > → >=` | `if (!best \|\| al.keys.length > best.al.keys.length) best = { A: ws[i], B:` |
| 2625 | `num → 0` | `if (!best \|\| best.al.keys.length < 12) return { ok: false, metric: metri` |
| 2625 | `bool \|\| → &&` | `if (!best \|\| best.al.keys.length < 12) return { ok: false, metric: metri` |
| 2670 | `bool \|\| → &&` | `corr: scr.corr \|\| null,` |
| 2697 | `cmp < → <=` | `if (seen[base] < 2) return base;` |
| 2698 | `bool \|\| → &&` | `used[base] = (used[base] \|\| 0) + 1;` |
| 2702 | `bool && → \|\|` | `var tag = sv.deviceKey \|\| sv.file \|\| sv.fname \|\| (sv.recording && sv.rec` |
| 2702 | `bool \|\| → &&` | `var tag = sv.deviceKey \|\| sv.file \|\| sv.fname \|\| (sv.recording && sv.rec` |
| 2702 | `bool \|\| → &&` | `var tag = sv.deviceKey \|\| sv.file \|\| sv.fname \|\| (sv.recording && sv.rec` |
| 2702 | `bool \|\| → &&` | `var tag = sv.deviceKey \|\| sv.file \|\| sv.fname \|\| (sv.recording && sv.rec` |
| 2702 | `bool \|\| → &&` | `var tag = sv.deviceKey \|\| sv.file \|\| sv.fname \|\| (sv.recording && sv.rec` |
| 2706 | `num → 0` | `var opts = { labels: _cornerIds, minN: 12 };` |
| 2707 | `cmp > → >=` | `if (rho && rho.value > 0) opts.rho = rho.value;` |
| 2776 | `cmp < → <=` | `for (var _i = 1; _i < _keys.length; _i++) _gaps.push(_keys[_i] - _keys[_` |
| 2803 | `bool && → \|\|` | `r.quietOrderUncertain = _quiet.length >= 2 && _quiet[0].v > 0 && _quiet[` |
| 2803 | `cmp > → >=` | `r.quietOrderUncertain = _quiet.length >= 2 && _quiet[0].v > 0 && _quiet[` |
| 2803 | `cmp > → >=` | `r.quietOrderUncertain = _quiet.length >= 2 && _quiet[0].v > 0 && _quiet[` |
| 2803 | `cmp < → <=` | `r.quietOrderUncertain = _quiet.length >= 2 && _quiet[0].v > 0 && _quiet[` |
| 2803 | `bool && → \|\|` | `r.quietOrderUncertain = _quiet.length >= 2 && _quiet[0].v > 0 && _quiet[` |
| 2828 | `bool && → \|\|` | `return r.summary && Array.isArray(r.summary.activitySeries) && r.summary` |
| 2832 | `cmp >= → >` | `return HRV_SOURCE_NODES.indexOf(r.node) >= 0 && !r.dateUnknown && r.summ` |
| 2832 | `bool && → \|\|` | `return HRV_SOURCE_NODES.indexOf(r.node) >= 0 && !r.dateUnknown && r.summ` |
| 2832 | `bool && → \|\|` | `return HRV_SOURCE_NODES.indexOf(r.node) >= 0 && !r.dateUnknown && r.summ` |
| 2832 | `bool && → \|\|` | `return HRV_SOURCE_NODES.indexOf(r.node) >= 0 && !r.dateUnknown && r.summ` |
| 2832 | `bool \|\| → &&` | `return HRV_SOURCE_NODES.indexOf(r.node) >= 0 && !r.dateUnknown && r.summ` |
| 2841 | `cmp <= → <` | `if (!isFinite(lo) \|\| !isFinite(hi) \|\| hi <= lo) return null;` |
| 2841 | `bool \|\| → &&` | `if (!isFinite(lo) \|\| !isFinite(hi) \|\| hi <= lo) return null;` |
| 2841 | `bool \|\| → &&` | `if (!isFinite(lo) \|\| !isFinite(hi) \|\| hi <= lo) return null;` |
| 2846 | `cmp >= → >` | `if (e.tMs == null \|\| e.tMs < lo \|\| e.tMs >= hi) return;` |
| 2846 | `bool \|\| → &&` | `if (e.tMs == null \|\| e.tMs < lo \|\| e.tMs >= hi) return;` |
| 2846 | `bool \|\| → &&` | `if (e.tMs == null \|\| e.tMs < lo \|\| e.tMs >= hi) return;` |
| 2858 | `cmp >= → >` | `quiet: immobileFrac >= HRV_QUIET_FRAC,` |
| 2912 | `cmp > → >=` | `if (r.node === 'OxyDex' && r.summary.pulseHr1Hz != null && isFinite(r.su` |
| 2912 | `bool && → \|\|` | `if (r.node === 'OxyDex' && r.summary.pulseHr1Hz != null && isFinite(r.su` |
| 2912 | `bool && → \|\|` | `if (r.node === 'OxyDex' && r.summary.pulseHr1Hz != null && isFinite(r.su` |
| 2912 | `bool && → \|\|` | `if (r.node === 'OxyDex' && r.summary.pulseHr1Hz != null && isFinite(r.su` |
| 2927 | `bool \|\| → &&` | `if (!wave \|\| !dev) return null;` |
| 2933 | `cmp <= → <` | `var agree = absBpm <= PULSE_AGREE_BPM;` |
| 2952 | `eq === → !==` | `(biasBpm === 0 ? 'matches' : (biasBpm > 0 ? 'reads +' : 'reads ') + bias` |
| 2952 | `cmp > → >=` | `(biasBpm === 0 ? 'matches' : (biasBpm > 0 ? 'reads +' : 'reads ') + bias` |
| 2978 | `bool && → \|\|` | `var robust = s.sdnnRobustMs != null && isFinite(s.sdnnRobustMs);` |
| 2982 | `bool && → \|\|` | `sdnnMs: robust ? +Number(s.sdnnRobustMs).toFixed(1) : s.sdnnMs != null &` |
| 2987 | `cmp > → >=` | `if (r.node === 'OxyDex' && s.rmssd1Hz != null && isFinite(s.rmssd1Hz) &&` |
| 2991 | `bool && → \|\|` | `hrVarSdBpm: s.hrVarSd1Hz != null && isFinite(s.hrVarSd1Hz) ? +Number(s.h` |
| 3003 | `cmp > → >=` | `var ratio = proxyRmssdAsMs > 0 ? +(wave.rmssdMs / proxyRmssdAsMs).toFixe` |
| 3004 | `cmp >= → >` | `var concordance = ratio == null ? null : ratio >= 1 / HRV_CONCORDANCE_FA` |
| 3004 | `cmp <= → <` | `var concordance = ratio == null ? null : ratio >= 1 / HRV_CONCORDANCE_FA` |
| 3026 | `eq === → !==` | `(wave.sdnnMetric === 'sdnnRobust' ? 'SDNN(robust) ' : 'SDNN ') +` |
| 3065 | `bool && → \|\|` | `if (r.node !== 'PpgDex' && s.cvhrIndex != null && isFinite(s.cvhrIndex) ` |
| 3065 | `bool && → \|\|` | `if (r.node !== 'PpgDex' && s.cvhrIndex != null && isFinite(s.cvhrIndex) ` |
| 3065 | `cmp >= → >` | `if (r.node !== 'PpgDex' && s.cvhrIndex != null && isFinite(s.cvhrIndex) ` |
| 3066 | `eq === → !==` | `corroborators.push({ node: r.node, cvhrIndex: +Number(s.cvhrIndex).toFix` |
| 3073 | `cmp <= → <` | `return { node: c.node, channel: c.channel, cvhrIndex: c.cvhrIndex, gapPe` |
| 3105 | `bool && → \|\|` | `(checked.length ? (anyDisagree && !anyAgree ? 'Divergence reported, neve` |
| 3125 | `bool \|\| → &&` | `if (v == null \|\| !isFinite(v) \|\| v <= 0) return;` |

## `motiondex-dsp.js` — 243 survivor(s)

| line | operator | source |
|---:|---|---|
| 56 | `cmp < → <=` | `return v < lo ? lo : v > hi ? hi : v;` |
| 56 | `cmp > → >=` | `return v < lo ? lo : v > hi ? hi : v;` |
| 62 | `cmp < → <=` | `if (w < 1) w = 1;` |
| 66 | `cmp < → <=` | `for (i = 0; i < n; i++) {` |
| 71 | `cmp >= → >` | `if (j >= 0) out[j] = acc / Math.min(w, i + 1);` |
| 74 | `cmp < → <=` | `for (i = Math.max(0, n - half); i < n; i++) out[i] = out[i] \|\| arr[i];` |
| 75 | `cmp < → <=` | `for (i = 0; i < n; i++) if (!isFinite(out[i])) out[i] = arr[i];` |
| 90 | `bool \|\| → &&` | `var h = (headerLine \|\| '').toLowerCase();` |
| 94 | `bool \|\| → &&` | `if (/dps\|deg\\/s/.test(h)) return { kind: 'gyro', unit: unit \|\| 'dps' };` |
| 100 | `eq === → !==` | `if (/µt\|ut/i.test(h) \|\| /magn/.test(h) \|\| unit === 'G') return { kind: '` |
| 100 | `bool \|\| → &&` | `if (/µt\|ut/i.test(h) \|\| /magn/.test(h) \|\| unit === 'G') return { kind: '` |
| 100 | `bool \|\| → &&` | `if (/µt\|ut/i.test(h) \|\| /magn/.test(h) \|\| unit === 'G') return { kind: '` |
| 102 | `bool && → \|\|` | `if (unit && /^mg$/i.test(unit)) return { kind: 'acc', unit: 'mg' };` |
| 117 | `bool \|\| → &&` | `var m = /_(ACC\|GYRO\|MAGN?\|MAG)\\b/i.exec(name \|\| '');` |
| 120 | `eq === → !==` | `if (t === 'ACC') return 'acc';` |
| 121 | `eq === → !==` | `if (t === 'GYRO') return 'gyro';` |
| 143 | `cmp >= → >` | `return idx.x >= 0 && idx.y >= 0 && idx.z >= 0 ? idx : null;` |
| 143 | `cmp >= → >` | `return idx.x >= 0 && idx.y >= 0 && idx.z >= 0 ? idx : null;` |
| 143 | `cmp >= → >` | `return idx.x >= 0 && idx.y >= 0 && idx.z >= 0 ? idx : null;` |
| 143 | `bool && → \|\|` | `return idx.x >= 0 && idx.y >= 0 && idx.z >= 0 ? idx : null;` |
| 143 | `bool && → \|\|` | `return idx.x >= 0 && idx.y >= 0 && idx.z >= 0 ? idx : null;` |
| 149 | `cmp < → <=` | `for (var k = 0; k < p.length; k++) {` |
| 152 | `cmp < → <=` | `if (nums.length < 3) return null;` |
| 161 | `bool && → \|\|` | `var kind = headerKind && headerKind.kind,` |
| 162 | `bool \|\| → &&` | `unit = (headerKind && headerKind.unit) \|\| '';` |
| 162 | `bool && → \|\|` | `unit = (headerKind && headerKind.unit) \|\| '';` |
| 164 | `eq === → !==` | `if (kind === 'acc')` |
| 168 | `num → 0` | `lim = /^mg$/i.test(unit) ? 16000 : unit === 'm/s2' ? 160 : unit === 'g' ` |
| 168 | `num → 0` | `lim = /^mg$/i.test(unit) ? 16000 : unit === 'm/s2' ? 160 : unit === 'g' ` |
| 169 | `eq === → !==` | `else if (kind === 'gyro') lim = 2000;` |
| 169 | `num → 0` | `else if (kind === 'gyro') lim = 2000;` |
| 170 | `eq === → !==` | `else if (kind === 'mag') lim = /^G$/.test(unit) ? 49 : 4900;` |
| 170 | `num → 0` | `else if (kind === 'mag') lim = /^G$/.test(unit) ? 49 : 4900;` |
| 172 | `cmp <= → <` | `return Math.abs(x) <= lim && Math.abs(y) <= lim && Math.abs(z) <= lim;` |
| 172 | `cmp <= → <` | `return Math.abs(x) <= lim && Math.abs(y) <= lim && Math.abs(z) <= lim;` |
| 172 | `cmp <= → <` | `return Math.abs(x) <= lim && Math.abs(y) <= lim && Math.abs(z) <= lim;` |
| 184 | `bool && → \|\|` | `if (headerKind === null && /timestamp/i.test(t)) {` |
| 208 | `bool \|\| → &&` | `if (!isFinite(y) \|\| !isFinite(z) \|\| !xyzPlausible(x, y, z, headerKind)) ` |
| 214 | `cmp >= → >` | `var b = BigInt(p[c.ns >= 0 ? c.ns : 1].trim());` |
| 215 | `eq === → !==` | `if (ns0 === null) ns0 = b;` |
| 218 | `cmp >= → >` | `var ts = parseTimestamp(p[c.phone >= 0 ? c.phone : 0]);` |
| 233 | `eq !== → ===` | `else if (inferred && inferred !== out._unit) out._unitSuspect = { declar` |
| 233 | `bool && → \|\|` | `else if (inferred && inferred !== out._unit) out._unitSuspect = { declar` |
| 237 | `bool && → \|\|` | `if (out._kind === 'mag' && out._unit === 'G') {` |
| 240 | `num → 0` | `out[gi].y *= 100;` |
| 241 | `num → 0` | `out[gi].z *= 100;` |
| 250 | `bool && → \|\|` | `if (rows) for (var i = 0; i < rows.length; i++) if (rows[i] && rows[i].t` |
| 250 | `cmp < → <=` | `if (rows) for (var i = 0; i < rows.length; i++) if (rows[i] && rows[i].t` |
| 270 | `eq === → !==` | `if (unit === 'm/s2') return v / 9.80665; // §2.2 — SI acceleration → g` |
| 270 | `num → 0` | `if (unit === 'm/s2') return v / 9.80665; // §2.2 — SI acceleration → g` |
| 283 | `bool \|\| → &&` | `if (!rows \|\| rows.length < 8) return null;` |
| 285 | `cmp < → <=` | `for (var i = 0; i < rows.length && mags.length < 2000; i += Math.max(1, ` |
| 285 | `cmp < → <=` | `for (var i = 0; i < rows.length && mags.length < 2000; i += Math.max(1, ` |
| 287 | `bool \|\| → &&` | `if (!r \|\| !isFinite(r.x) \|\| !isFinite(r.y) \|\| !isFinite(r.z)) continue;` |
| 287 | `bool \|\| → &&` | `if (!r \|\| !isFinite(r.x) \|\| !isFinite(r.y) \|\| !isFinite(r.z)) continue;` |
| 287 | `bool \|\| → &&` | `if (!r \|\| !isFinite(r.x) \|\| !isFinite(r.y) \|\| !isFinite(r.z)) continue;` |
| 292 | `cmp <= → <` | `if (!isFinite(med) \|\| med <= 0) return null;` |
| 292 | `bool \|\| → &&` | `if (!isFinite(med) \|\| med <= 0) return null;` |
| 308 | `cmp < → <=` | `if (rows.length < 3) return NaN;` |
| 315 | `cmp > → >=` | `if (prev != null && t > prev) d.push(t - prev);` |
| 315 | `bool && → \|\|` | `if (prev != null && t > prev) d.push(t - prev);` |
| 319 | `cmp > → >=` | `if (isFinite(dm) && dm > 0) return 1 / dm;` |
| 319 | `bool && → \|\|` | `if (isFinite(dm) && dm > 0) return 1 / dm;` |
| 324 | `cmp <= → <` | `if (a == null \|\| b == null \|\| b <= a) return NaN;` |
| 324 | `bool \|\| → &&` | `if (a == null \|\| b == null \|\| b <= a) return NaN;` |
| 324 | `bool \|\| → &&` | `if (a == null \|\| b == null \|\| b <= a) return NaN;` |
| 348 | `cmp > → >=` | `if (!(mag > 0.4) \|\| mag > 2.0) return 'unknown'; // not ~1g static → mov` |
| 348 | `cmp > → >=` | `if (!(mag > 0.4) \|\| mag > 2.0) return 'unknown'; // not ~1g static → mov` |
| 362 | `cmp >= → >` | `if (az >= 0.7) return gz > 0 ? 'supine' : 'prone';` |
| 362 | `cmp > → >=` | `if (az >= 0.7) return gz > 0 ? 'supine' : 'prone';` |
| 363 | `cmp >= → >` | `if (ay >= 0.55) return 'upright';` |
| 364 | `cmp < → <=` | `return gx < 0 ? 'left' : 'right';` |
| 375 | `cmp < → <=` | `for (var e = 0; e < nE; e++) {` |
| 382 | `cmp < → <=` | `if (s == null \|\| s < 0) continue;` |
| 398 | `num → 0` | `track.push({ tStartMs: t0Ms != null ? t0Ms + e * epoch * 1000 : null, po` |
| 404 | `num → 0` | `track.push({ tStartMs: t0Ms != null ? t0Ms + e * epoch * 1000 : null, po` |
| 416 | `cmp < → <=` | `if (!accRows \|\| accRows.length < 10) return { hasData: false };` |
| 419 | `bool \|\| → &&` | `if (!isFinite(hz) \|\| hz <= 0) hz = accRows.length / Math.max(1, durSec);` |
| 419 | `cmp <= → <` | `if (!isFinite(hz) \|\| hz <= 0) hz = accRows.length / Math.max(1, durSec);` |
| 441 | `bool \|\| → &&` | `if (s == null \|\| s < 0) continue;` |
| 441 | `cmp < → <=` | `if (s == null \|\| s < 0) continue;` |
| 445 | `cmp > → >=` | `if (dyn > MOVE_G) counts[idx] += dyn;` |
| 451 | `cmp < → <=` | `for (var e = 0; e < nE; e++) {` |
| 452 | `num → 0` | `var tEp = t0Ms != null ? t0Ms + e * epoch * 1000 : null;` |
| 458 | `cmp > → >=` | `var moving = counts[e] > 1.0; // counts are Σ dynamic-g in the 30 s epoc` |
| 458 | `num → 0` | `var moving = counts[e] > 1.0; // counts are Σ dynamic-g in the 30 s epoc` |
| 507 | `num → 0` | `var RR_F_LO = 0.1,` |
| 511 | `num → 0` | `RR_TAPER_W = 0.01; // soft spectral high-pass` |
| 527 | `num → 0` | `var RR_BIAS_BRPM_CORPUS = 0.58; // documented, NOT applied by default` |
| 562 | `cmp < → <=` | `for (i = 0; i < n; i++) out[i] = x[i];` |
| 571 | `cmp < → <=` | `for (i = 0; i < n; i++) {` |
| 586 | `cmp < → <=` | `for (i = 0; i < n; i++) o[i] = a[n - 1 - i];` |
| 593 | `cmp < → <=` | `if (n < 8) return Float64Array.from(x);` |
| 597 | `cmp < → <=` | `for (i = 0; i < pad; i++) ext[i] = 2 * x[0] - x[pad - i];` |
| 598 | `cmp < → <=` | `for (i = 0; i < n; i++) ext[pad + i] = x[i];` |
| 599 | `cmp < → <=` | `for (i = 0; i < pad; i++) ext[pad + n + i] = 2 * x[n - 1] - x[n - 2 - i]` |
| 612 | `cmp < → <=` | `if (i < j) {` |
| 627 | `cmp <= → <` | `for (var len = 2; len <= n; len <<= 1) {` |
| 632 | `cmp < → <=` | `for (i = 0; i < n; i += len) {` |
| 665 | `cmp < → <=` | `for (i = 1; i < Math.min(n, 4000); i++) d.push(tSec[i] - tSec[i - 1]);` |
| 667 | `cmp <= → <` | `if (!isFinite(dm) \|\| dm <= 0) return null;` |
| 667 | `bool \|\| → &&` | `if (!isFinite(dm) \|\| dm <= 0) return null;` |
| 671 | `num → 0` | `if (nOut < 64) return null;` |
| 671 | `cmp < → <=` | `if (nOut < 64) return null;` |
| 683 | `cmp < → <=` | `for (var kc = 0; kc < nOut; kc++) {` |
| 685 | `cmp < → <=` | `while (pc < n - 2 && tSec[pc + 1] < tc) pc++;` |
| 685 | `cmp < → <=` | `while (pc < n - 2 && tSec[pc + 1] < tc) pc++;` |
| 686 | `cmp <= → <` | `covered[kc] = tSec[pc + 1] - tSec[pc] <= gapMaxSec ? 1 : 0;` |
| 691 | `cmp < → <=` | `for (var a = 0; a < 3; a++) {` |
| 697 | `cmp < → <=` | `for (var k = 0; k < nOut; k++) {` |
| 699 | `cmp < → <=` | `while (p < n - 2 && tSec[p + 1] < tt) p++;` |
| 699 | `cmp < → <=` | `while (p < n - 2 && tSec[p + 1] < tt) p++;` |
| 703 | `cmp > → >=` | `if (tb > ta) u = (tt - ta) / (tb - ta);` |
| 720 | `cmp < → <=` | `for (i = 0; i < nWin; i++) mu += x[i0 + i];` |
| 725 | `cmp < → <=` | `for (i = 1; i < nWin; i++) {` |
| 726 | `cmp <= → <` | `if (x[i0 + i - 1] - mu <= 0 && x[i0 + i] - mu > 0) {` |
| 726 | `cmp > → >=` | `if (x[i0 + i - 1] - mu <= 0 && x[i0 + i] - mu > 0) {` |
| 728 | `cmp >= → >` | `if (t - last >= refractory) {` |
| 739 | `cmp <= → <` | `for (var f = RR_F_LO; f <= RR_F_HI + 1e-9; f += RR_F_STEP) g.push(f);` |
| 759 | `cmp < → <=` | `for (i = 0; i < nWin; i++) {` |
| 763 | `cmp < → <=` | `if (sd < 1e-24) continue;` |
| 764 | `cmp < → <=` | `for (i = 0; i < RR_NFFT; i++) {` |
| 776 | `cmp >= → >` | `if (j < 0 \|\| j + 1 >= RR_NFFT / 2) continue;` |
| 776 | `cmp < → <=` | `if (j < 0 \|\| j + 1 >= RR_NFFT / 2) continue;` |
| 776 | `bool \|\| → &&` | `if (j < 0 \|\| j + 1 >= RR_NFFT / 2) continue;` |
| 782 | `cmp <= → <` | `if (sum <= 0) continue;` |
| 783 | `cmp < → <=` | `for (k = 0; k < F; k++) S[k] += tmp[k] / sum;` |
| 792 | `cmp > → >=` | `if (tot > 0) for (k = 0; k < F; k++) S[k] /= tot;` |
| 792 | `cmp < → <=` | `if (tot > 0) for (k = 0; k < F; k++) S[k] /= tot;` |
| 797 | `cmp >= → >` | `if (z >= 6 && z <= 36) zs.push(z);` |
| 797 | `cmp <= → <` | `if (z >= 6 && z <= 36) zs.push(z);` |
| 797 | `bool && → \|\|` | `if (z >= 6 && z <= 36) zs.push(z);` |
| 811 | `cmp > → >=` | `if (bsum > 0) {` |
| 813 | `cmp < → <=` | `for (k = 0; k < F; k++) {` |
| 817 | `cmp < → <=` | `if (tot > 0) for (k = 0; k < F; k++) S[k] /= tot;` |
| 817 | `cmp > → >=` | `if (tot > 0) for (k = 0; k < F; k++) S[k] /= tot;` |
| 831 | `cmp < → <=` | `for (i = 0; i < F; i++) brpm[i] = grid[i] * 60;` |
| 835 | `cmp < → <=` | `for (i = 0; i < F; i++) dp[i] = Math.log(Math.max(specs[0][i], 1e-6));` |
| 839 | `cmp < → <=` | `for (i = 0; i < F; i++) {` |
| 842 | `cmp < → <=` | `for (j = 0; j < F; j++) {` |
| 845 | `cmp > → >=` | `if (v > bestV) {` |
| 854 | `cmp < → <=` | `for (i = 0; i < F; i++) dp[i] = ndp[i];` |
| 858 | `cmp < → <=` | `for (i = 1; i < F; i++) if (dp[i] > dp[best]) best = i;` |
| 858 | `cmp > → >=` | `for (i = 1; i < F; i++) if (dp[i] > dp[best]) best = i;` |
| 868 | `cmp <= → <` | `for (i = lo; i <= hi; i++) s += specs[t][i];` |
| 880 | `bool && → \|\|` | `if (typeof opts.biasBrpm === 'number' && isFinite(opts.biasBrpm)) bias =` |
| 882 | `bool && → \|\|` | `if (typeof opts.confMin === 'number' && isFinite(opts.confMin)) confMin ` |
| 883 | `cmp < → <=` | `if (!chestRows \|\| chestRows.length < 400) return { hasData: false };` |
| 883 | `num → 0` | `if (!chestRows \|\| chestRows.length < 400) return { hasData: false };` |
| 892 | `cmp < → <=` | `if (N < nWin) return { hasData: false };` |
| 894 | `cmp < → <=` | `for (var i = 0; i < nWin; i++) hann[i] = 0.5 - 0.5 * Math.cos((2 * Math.` |
| 899 | `cmp <= → <` | `for (var s = 0; s + nWin <= N; s += hop) {` |
| 907 | `cmp < → <=` | `if (cov < RR_MIN_WIN_COVERAGE) {` |
| 915 | `cmp < → <=` | `for (var q2 = 0; q2 < grid.length; q2++) flat[q2] = fv;` |
| 932 | `cmp >= → >` | `var covOk = winCov[i] >= RR_MIN_WIN_COVERAGE;` |
| 934 | `cmp >= → >` | `if (covOk && v.conf[i] >= confMin) {` |
| 943 | `cmp > → >=` | `hasData: kept.length > 0,` |
| 949 | `cmp > → >=` | `coverage: series.length - nUncovered > 0 ? kept.length / (series.length ` |
| 964 | `num → 0` | `var EFFORT_FLOOR_G = 0.004; // RMS threshold (g) for effort PRESENT vs f` |
| 975 | `cmp <= → <` | `if (!isFinite(hz) \|\| hz <= 0) hz = chestRows.length / Math.max(1, chestD` |
| 975 | `bool \|\| → &&` | `if (!isFinite(hz) \|\| hz <= 0) hz = chestRows.length / Math.max(1, chestD` |
| 979 | `cmp < → <=` | `for (var a = 0; a < 3; a++) {` |
| 984 | `cmp < → <=` | `for (i = 0; i < raw.length; i++) band[i] = raw[i] - lo[i];` |
| 985 | `num → 0` | `var sm = movavg(band, Math.max(3, Math.round(hz * 1.5))); // de-noise ca` |
| 989 | `cmp > → >=` | `if (!best \|\| v > best.var) best = { var: v, sig: sm };` |
| 995 | `num → 0` | `minSepSec = 1.2; // <50 brpm ceiling` |
| 996 | `cmp < → <=` | `for (i = 1; i < sig.length; i++) {` |
| 997 | `cmp <= → <` | `if (sig[i - 1] <= 0 && sig[i] > 0) {` |
| 997 | `cmp > → >=` | `if (sig[i - 1] <= 0 && sig[i] > 0) {` |
| 997 | `bool && → \|\|` | `if (sig[i - 1] <= 0 && sig[i] > 0) {` |
| 999 | `cmp >= → >` | `if (s2 - lastUp >= minSepSec) {` |
| 1005 | `num → 0` | `var minutes = chestDurSec / 60; // §7.3 — chest's own span, not the long` |
| 1006 | `cmp > → >=` | `var rate = minutes > 0 ? breaths / minutes : NaN;` |
| 1028 | `cmp < → <=` | `for (var e = 0; e < nE; e++) {` |
| 1031 | `num → 0` | `var tEp = t0Ms != null ? t0Ms + Math.round(e * EFFORT_CAD_SEC * 1000) : ` |
| 1032 | `cmp < → <=` | `while (kp < sig.length && tSec[kp] < w0) kp++;` |
| 1032 | `cmp < → <=` | `while (kp < sig.length && tSec[kp] < w0) kp++;` |
| 1035 | `cmp < → <=` | `for (var k = kp; k < sig.length && tSec[k] < w1; k++) {` |
| 1035 | `cmp < → <=` | `for (var k = kp; k < sig.length && tSec[k] < w1; k++) {` |
| 1039 | `cmp < → <=` | `if (cnt < 3) {` |
| 1044 | `cmp >= → >` | `series.push({ tMs: tEp, amp: Math.round(eamp * 1e4) / 1e4, present: eamp` |
| 1056 | `num → 0` | `if (isFinite(rate)) legacyRate = Math.round(rate * 10) / 10;` |
| 1056 | `num → 0` | `if (isFinite(rate)) legacyRate = Math.round(rate * 10) / 10;` |
| 1059 | `bool && → \|\|` | `if (spec.hasData && spec.medianBrpm != null) {` |
| 1065 | `cmp >= → >` | `hasData: (spec.hasData && outRate != null) \|\| (breaths >= 3 && isFinite(` |
| 1065 | `bool && → \|\|` | `hasData: (spec.hasData && outRate != null) \|\| (breaths >= 3 && isFinite(` |
| 1065 | `bool && → \|\|` | `hasData: (spec.hasData && outRate != null) \|\| (breaths >= 3 && isFinite(` |
| 1065 | `bool \|\| → &&` | `hasData: (spec.hasData && outRate != null) \|\| (breaths >= 3 && isFinite(` |
| 1086 | `cmp < → <=` | `if (!accRows \|\| accRows.length < 10) return { conf: 0, flags: ['no-data'` |
| 1098 | `cmp > → >=` | `if (m > 7.5) clip++; // >7.5 g → clip / impact-range (ACC full-scale abu` |
| 1099 | `cmp < → <=` | `if (last != null && Math.abs(m - last) < 1e-4) flat++;` |
| 1100 | `cmp < → <=` | `if (m < 0.05) staticN++; // near-zero → sensor off / detached` |
| 1100 | `num → 0` | `if (m < 0.05) staticN++; // near-zero → sensor off / detached` |
| 1106 | `cmp > → >=` | `if (clipFrac > 0.02) flags.push('clip');` |
| 1106 | `num → 0` | `if (clipFrac > 0.02) flags.push('clip');` |
| 1107 | `cmp > → >=` | `if (flatFrac > 0.5) flags.push('flatline');` |
| 1108 | `cmp > → >=` | `if (offFrac > 0.2) flags.push('sensor-off');` |
| 1108 | `num → 0` | `if (offFrac > 0.2) flags.push('sensor-off');` |
| 1142 | `cmp < → <=` | `if (!rows \|\| rows.length < 2) return null;` |
| 1144 | `cmp >= → >` | `for (var i = rows.length - 1; i >= 0; i--) {` |
| 1146 | `cmp > → >=` | `if (t != null && t > 0) return t;` |
| 1146 | `bool && → \|\|` | `if (t != null && t > 0) return t;` |
| 1155 | `cmp < → <=` | `for (var i = 0; i < list.length; i++) if (list[i] != null && (best == nu` |
| 1155 | `cmp > → >=` | `for (var i = 0; i < list.length; i++) if (list[i] != null && (best == nu` |
| 1155 | `bool && → \|\|` | `for (var i = 0; i < list.length; i++) if (list[i] != null && (best == nu` |
| 1155 | `bool \|\| → &&` | `for (var i = 0; i < list.length; i++) if (list[i] != null && (best == nu` |
| 1159 | `cmp < → <=` | `for (var i = 0; i < rowsList.length; i++) {` |
| 1174 | `bool \|\| → &&` | `var accUnit = (acc && acc._unit) \|\| 'mg',` |
| 1175 | `bool \|\| → &&` | `chestUnit = (chest && chest._unit) \|\| 'mg';` |
| 1179 | `cmp > → >=` | `var posSrc = chest && chest.length > 10 ? chest : acc;` |
| 1179 | `num → 0` | `var posSrc = chest && chest.length > 10 ? chest : acc;` |
| 1180 | `eq === → !==` | `var posUnit = posSrc === chest ? chestUnit : accUnit;` |
| 1181 | `cmp > → >=` | `var actiSrc = acc && acc.length > 10 ? acc : posSrc;` |
| 1181 | `num → 0` | `var actiSrc = acc && acc.length > 10 ? acc : posSrc;` |
| 1182 | `eq === → !==` | `var actiUnit = actiSrc === acc ? accUnit : posUnit;` |
| 1240 | `bool \|\| → &&` | `var hz = opts.hz \|\| 26,` |
| 1240 | `num → 0` | `var hz = opts.hz \|\| 26,` |
| 1241 | `num → 0` | `sec = opts.sec \|\| 120,` |
| 1242 | `num → 0` | `brpm = opts.brpm \|\| 15,` |
| 1249 | `num → 0` | `dtMs = 1000 / hz,` |
| 1268 | `num → 0` | `var pauseDur = opts.pauseDurSec == null ? 30 : opts.pauseDurSec;` |
| 1269 | `cmp < → <=` | `for (var i = 0; i < n; i++) {` |
| 1272 | `cmp >= → >` | `if (pauseAt != null && tSec >= pauseAt && tSec < pauseAt + pauseDur) res` |
| 1272 | `cmp < → <=` | `if (pauseAt != null && tSec >= pauseAt && tSec < pauseAt + pauseDur) res` |
| 1273 | `num → 0` | `var gx = 30 + (rnd() - 0.5) * 12;` |
| 1273 | `num → 0` | `var gx = 30 + (rnd() - 0.5) * 12;` |
| 1274 | `num → 0` | `var gy = 40 + (rnd() - 0.5) * 12;` |
| 1274 | `num → 0` | `var gy = 40 + (rnd() - 0.5) * 12;` |
| 1275 | `num → 0` | `var gz = 1000 + (rnd() - 0.5) * 10;` |
| 1275 | `num → 0` | `var gz = 1000 + (rnd() - 0.5) * 10;` |
| 1279 | `num → 0` | `gy = 1000 + resp * 22 + (rnd() - 0.5) * 10;` |
| 1279 | `num → 0` | `gy = 1000 + resp * 22 + (rnd() - 0.5) * 10;` |
| 1279 | `num → 0` | `gy = 1000 + resp * 22 + (rnd() - 0.5) * 10;` |
| 1280 | `num → 0` | `gz = 40 + (rnd() - 0.5) * 12;` |
| 1280 | `num → 0` | `gz = 40 + (rnd() - 0.5) * 12;` |
| 1280 | `num → 0` | `gz = 40 + (rnd() - 0.5) * 12;` |
| 1321 | `eq !== → ===` | `var kfmt = opts.kernel ? { version: opts.kernel.VERSION, hash: opts.kern` |
| 1321 | `bool && → \|\|` | `var kfmt = opts.kernel ? { version: opts.kernel.VERSION, hash: opts.kern` |
| 1326 | `bool && → \|\|` | `if (track && track.length) {` |
| 1330 | `bool && → \|\|` | `if (seg.pos !== prev && seg.pos !== 'unknown') {` |
| 1423 | `eq === → !==` | `if (global.DexExport && typeof global.DexExport.scrubExport === 'functio` |
| 1423 | `bool && → \|\|` | `if (global.DexExport && typeof global.DexExport.scrubExport === 'functio` |
| 1424 | `eq === → !==` | `if (typeof global.dexScrubExport === 'function') return global.dexScrubE` |

## `oxydex-dsp.js` — 1323 survivor(s)

| line | operator | source |
|---:|---|---|
| 50 | `bool \|\| → &&` | `window.UP = window.UP \|\| {};` |
| 128 | `num → 0` | `HR_ARTIFACT_JUMP_SOFT: 15, // node-local: BPM jump in 1 sample within ±2` |
| 166 | `cmp < → <=` | `if (now - _lastFileSelect < 500) return;` |
| 166 | `num → 0` | `if (now - _lastFileSelect < 500) return;` |
| 168 | `bool && → \|\|` | `var files = Array.from(e.target.files \|\| (e.target && e.target.files) \|\|` |
| 168 | `bool \|\| → &&` | `var files = Array.from(e.target.files \|\| (e.target && e.target.files) \|\|` |
| 168 | `bool \|\| → &&` | `var files = Array.from(e.target.files \|\| (e.target && e.target.files) \|\|` |
| 189 | `bool \|\| → &&` | `var files = Array.from(/** @type {any} */ (e.dataTransfer).files \|\| []);` |
| 204 | `cmp < → <=` | `if (!bytes \|\| bytes.length < 40) return false;` |
| 204 | `num → 0` | `if (!bytes \|\| bytes.length < 40) return false;` |
| 206 | `cmp <= → <` | `for (var i = 2; i <= 7; i++) {` |
| 207 | `eq !== → ===` | `if (bytes[i] !== 0x00) return false;` |
| 297 | `cmp < → <=` | `if (c.length < 3) continue;` |
| 299 | `bool \|\| → &&` | `if (!p \|\| !isFinite(p.tMs)) continue;` |
| 316 | `bool \|\| → &&` | `if (!night \|\| !night.stats \|\| !isFinite(night.stats.startTs) \|\| !log \|\| ` |
| 316 | `bool \|\| → &&` | `if (!night \|\| !night.stats \|\| !isFinite(night.stats.startTs) \|\| !log \|\| ` |
| 316 | `bool \|\| → &&` | `if (!night \|\| !night.stats \|\| !isFinite(night.stats.startTs) \|\| !log \|\| ` |
| 316 | `bool \|\| → &&` | `if (!night \|\| !night.stats \|\| !isFinite(night.stats.startTs) \|\| !log \|\| ` |
| 322 | `cmp <= → <` | `if (e.event === 'reset-suspect' && e.tMs >= t0 && e.tMs <= t0 + durMs) {` |
| 322 | `cmp >= → >` | `if (e.event === 'reset-suspect' && e.tMs >= t0 && e.tMs <= t0 + durMs) {` |
| 331 | `bool \|\| → &&` | `if (r.event !== 'read' \|\| r.offsetS == null) continue;` |
| 333 | `cmp > → >=` | `if (d > TOL_MS) continue;` |
| 334 | `cmp < → <=` | `if (d < bestD) {` |
| 351 | `bool \|\| → &&` | `if (!night \|\| !bag) return;` |
| 366 | `cmp <= → <` | `for (var off = 10; off + 3 <= bytes.length; off += 3) {` |
| 399 | `negate: drop !` | `if (!_rEl) return;` |
| 400 | `cmp > → >=` | `_rEl.innerHTML = '<div class="results-loading">⏳ Reading ' + files.lengt` |
| 410 | `num → 0` | `var pct = 5 + Math.round((completed / total) * 75); // 5–80% for parsing` |
| 412 | `cmp > → >=` | `_ui.setStatus('Parsed ' + completed + ' / ' + total + ' file' + (total >` |
| 424 | `num → 0` | `_ui.setProgress(85);` |
| 429 | `negate: drop !` | `if (!night \|\| !night.date) return;` |
| 429 | `bool \|\| → &&` | `if (!night \|\| !night.date) return;` |
| 429 | `negate: drop !` | `if (!night \|\| !night.date) return;` |
| 432 | `bool && → \|\|` | `if (night.stats && night.stats.startTs) {` |
| 435 | `cmp < → <=` | `return ex.stats && ex.stats.startTs && Math.abs(ex.stats.startTs - ts) <` |
| 435 | `bool && → \|\|` | `return ex.stats && ex.stats.startTs && Math.abs(ex.stats.startTs - ts) <` |
| 435 | `bool && → \|\|` | `return ex.stats && ex.stats.startTs && Math.abs(ex.stats.startTs - ts) <` |
| 435 | `num → 0` | `return ex.stats && ex.stats.startTs && Math.abs(ex.stats.startTs - ts) <` |
| 438 | `negate: drop !` | `if (!window._csvParseErrors) window._csvParseErrors = [];` |
| 439 | `bool \|\| → &&` | `window._csvParseErrors.push('Skipped duplicate recording: ' + (night.fna` |
| 457 | `eq !== → ===` | `var _rtcLog = (typeof window !== 'undefined' && window._oxyRtcLog) \|\| []` |
| 457 | `bool \|\| → &&` | `var _rtcLog = (typeof window !== 'undefined' && window._oxyRtcLog) \|\| []` |
| 457 | `bool && → \|\|` | `var _rtcLog = (typeof window !== 'undefined' && window._oxyRtcLog) \|\| []` |
| 461 | `bool && → \|\|` | `if (_n && _n._o2BinSource === true) _o2AttachRtcVerification(_n, _rtcLog` |
| 462 | `eq !== → ===` | `if (_n) _o2AttachAcqEvidence(_n, (typeof window !== 'undefined' && windo` |
| 462 | `bool \|\| → &&` | `if (_n) _o2AttachAcqEvidence(_n, (typeof window !== 'undefined' && windo` |
| 462 | `bool && → \|\|` | `if (_n) _o2AttachAcqEvidence(_n, (typeof window !== 'undefined' && windo` |
| 470 | `negate: drop !` | `if (!nights.length) {` |
| 471 | `bool && → \|\|` | `var dbg = window._csvParseErrors && window._csvParseErrors.length ? '\\n\\` |
| 479 | `num → 0` | `_ui.setProgress(95);` |
| 480 | `cmp > → >=` | `_ui.setStatus('Rendering ' + nights.length + ' night' + (nights.length >` |
| 483 | `num → 0` | `_ui.setProgress(100);` |
| 492 | `bool \|\| → &&` | `var _w2 = window._oxyW2 \|\| {};` |
| 493 | `bool \|\| → &&` | `var _w2s = window._oxyW2S \|\| {};` |
| 498 | `eq === → !==` | `var rows2 = exact ? _w2s[stem] : _wk.length === 1 && _sk.length === 1 ? ` |
| 498 | `eq === → !==` | `var rows2 = exact ? _w2s[stem] : _wk.length === 1 && _sk.length === 1 ? ` |
| 498 | `bool && → \|\|` | `var rows2 = exact ? _w2s[stem] : _wk.length === 1 && _sk.length === 1 ? ` |
| 499 | `negate: drop !` | `if (!rows2) return;` |
| 509 | `eq === → !==` | `if (typeof window.oxyRenderSpo2w === 'function') window.oxyRenderSpo2w(r` |
| 518 | `bool && → \|\|` | `if (window._csvParseErrors && window._csvParseErrors.length) {` |
| 542 | `num → 0` | `}, 30);` |
| 570 | `cmp < → <=` | `if (!_binRows \|\| _binRows.length < 60) {` |
| 570 | `bool \|\| → &&` | `if (!_binRows \|\| _binRows.length < 60) {` |
| 570 | `num → 0` | `if (!_binRows \|\| _binRows.length < 60) {` |
| 570 | `negate: drop !` | `if (!_binRows \|\| _binRows.length < 60) {` |
| 571 | `negate: drop !` | `if (!window._csvParseErrors) window._csvParseErrors = [];` |
| 574 | `bool && → \|\|` | `var _binNight = _binRows && _binRows.length >= 60 ? processNight(_binRow` |
| 574 | `cmp >= → >` | `var _binNight = _binRows && _binRows.length >= 60 ? processNight(_binRow` |
| 574 | `num → 0` | `var _binNight = _binRows && _binRows.length >= 60 ? processNight(_binRow` |
| 590 | `bool \|\| → &&` | `window._oxyRtcLog = (window._oxyRtcLog \|\| []).concat(_rtcRows);` |
| 604 | `bool && → \|\|` | `var _acqEv = _metaObj && _metaObj.acquisition_evidence;` |
| 605 | `bool && → \|\|` | `if (_acqEv && _acqEv.session_id) {` |
| 606 | `bool \|\| → &&` | `window._oxyAcqEvidence = window._oxyAcqEvidence \|\| {};` |
| 618 | `eq === → !==` | `if (text.charAt(0) === '{' \|\| text.charAt(0) === '[') {` |
| 618 | `eq === → !==` | `if (text.charAt(0) === '{' \|\| text.charAt(0) === '[') {` |
| 618 | `bool \|\| → &&` | `if (text.charAt(0) === '{' \|\| text.charAt(0) === '[') {` |
| 627 | `eq === → !==` | `if (_env && _env.schema && _env.schema.name === 'ganglior.node-export') ` |
| 627 | `bool && → \|\|` | `if (_env && _env.schema && _env.schema.name === 'ganglior.node-export') ` |
| 627 | `bool && → \|\|` | `if (_env && _env.schema && _env.schema.name === 'ganglior.node-export') ` |
| 629 | `negate: drop !` | `if (!_r.ok) {` |
| 630 | `negate: drop !` | `if (!window._csvParseErrors) window._csvParseErrors = [];` |
| 631 | `bool && → \|\|` | `window._csvParseErrors.push((file && file.name ? file.name + ': ' : '') ` |
| 661 | `bool && → \|\|` | `if (single && single.date) {` |
| 683 | `bool \|\| → &&` | `(window._oxyW2 = window._oxyW2 \|\| {})[file.name.replace(/_PPG2W\\.txt$/i,` |
| 687 | `eq === → !==` | `if (cleanText.indexOf('OxyDex Night Summary') === 0 \|\| cleanText.indexOf` |
| 687 | `eq === → !==` | `if (cleanText.indexOf('OxyDex Night Summary') === 0 \|\| cleanText.indexOf` |
| 687 | `bool \|\| → &&` | `if (cleanText.indexOf('OxyDex Night Summary') === 0 \|\| cleanText.indexOf` |
| 688 | `negate: drop !` | `if (!window._csvParseErrors) window._csvParseErrors = [];` |
| 700 | `bool && → \|\|` | `if (/_SPO2\\.csv$/i.test(file.name) && rows && rows.length) {` |
| 700 | `bool && → \|\|` | `if (/_SPO2\\.csv$/i.test(file.name) && rows && rows.length) {` |
| 701 | `bool \|\| → &&` | `(window._oxyW2S = window._oxyW2S \|\| {})[file.name.replace(/_SPO2\\.csv$/i` |
| 703 | `cmp < → <=` | `if (!rows \|\| rows.length < 60) {` |
| 703 | `bool \|\| → &&` | `if (!rows \|\| rows.length < 60) {` |
| 703 | `negate: drop !` | `if (!rows \|\| rows.length < 60) {` |
| 703 | `num → 0` | `if (!rows \|\| rows.length < 60) {` |
| 706 | `negate: drop !` | `if (!window._csvParseErrors) window._csvParseErrors = [];` |
| 707 | `num → 0` | `window._csvParseErrors.push(file.name + ': ' + rows.length + ' rows pars` |
| 709 | `cmp >= → >` | `resolve(rows && rows.length >= 60 ? processNight(rows, file.name) : null` |
| 709 | `num → 0` | `resolve(rows && rows.length >= 60 ? processNight(rows, file.name) : null` |
| 709 | `bool && → \|\|` | `resolve(rows && rows.length >= 60 ? processNight(rows, file.name) : null` |
| 711 | `negate: drop !` | `if (!window._csvParseErrors) window._csvParseErrors = [];` |
| 712 | `bool && → \|\|` | `window._csvParseErrors.push(file.name + ' ERROR: ' + (err && err.message` |
| 717 | `negate: drop !` | `if (!window._csvParseErrors) window._csvParseErrors = [];` |
| 738 | `cmp >= → >` | `if (l.indexOf('spo2') >= 0 \|\| l.indexOf('pulse') >= 0 \|\| l.indexOf('time` |
| 738 | `cmp >= → >` | `if (l.indexOf('spo2') >= 0 \|\| l.indexOf('pulse') >= 0 \|\| l.indexOf('time` |
| 738 | `cmp >= → >` | `if (l.indexOf('spo2') >= 0 \|\| l.indexOf('pulse') >= 0 \|\| l.indexOf('time` |
| 738 | `cmp >= → >` | `if (l.indexOf('spo2') >= 0 \|\| l.indexOf('pulse') >= 0 \|\| l.indexOf('time` |
| 738 | `bool \|\| → &&` | `if (l.indexOf('spo2') >= 0 \|\| l.indexOf('pulse') >= 0 \|\| l.indexOf('time` |
| 738 | `bool \|\| → &&` | `if (l.indexOf('spo2') >= 0 \|\| l.indexOf('pulse') >= 0 \|\| l.indexOf('time` |
| 738 | `bool \|\| → &&` | `if (l.indexOf('spo2') >= 0 \|\| l.indexOf('pulse') >= 0 \|\| l.indexOf('time` |
| 751 | `cmp >= → >` | `var DELIM = headerLine.indexOf(';') >= 0 && (headerLine.indexOf(';') < h` |
| 751 | `cmp < → <=` | `var DELIM = headerLine.indexOf(';') >= 0 && (headerLine.indexOf(';') < h` |
| 751 | `cmp < → <=` | `var DELIM = headerLine.indexOf(';') >= 0 && (headerLine.indexOf(';') < h` |
| 765 | `cmp >= → >` | `if ((c.indexOf('time') >= 0 \|\| c.indexOf('date') >= 0) && timeCol < 0) t` |
| 765 | `cmp >= → >` | `if ((c.indexOf('time') >= 0 \|\| c.indexOf('date') >= 0) && timeCol < 0) t` |
| 765 | `bool && → \|\|` | `if ((c.indexOf('time') >= 0 \|\| c.indexOf('date') >= 0) && timeCol < 0) t` |
| 765 | `bool \|\| → &&` | `if ((c.indexOf('time') >= 0 \|\| c.indexOf('date') >= 0) && timeCol < 0) t` |
| 765 | `cmp < → <=` | `if ((c.indexOf('time') >= 0 \|\| c.indexOf('date') >= 0) && timeCol < 0) t` |
| 766 | `cmp >= → >` | `if ((c.indexOf('spo2') >= 0 \|\| c.indexOf('o2') >= 0 \|\| c.indexOf('sao2')` |
| 766 | `cmp >= → >` | `if ((c.indexOf('spo2') >= 0 \|\| c.indexOf('o2') >= 0 \|\| c.indexOf('sao2')` |
| 766 | `cmp >= → >` | `if ((c.indexOf('spo2') >= 0 \|\| c.indexOf('o2') >= 0 \|\| c.indexOf('sao2')` |
| 766 | `cmp >= → >` | `if ((c.indexOf('spo2') >= 0 \|\| c.indexOf('o2') >= 0 \|\| c.indexOf('sao2')` |
| 766 | `cmp < → <=` | `if ((c.indexOf('spo2') >= 0 \|\| c.indexOf('o2') >= 0 \|\| c.indexOf('sao2')` |
| 766 | `bool \|\| → &&` | `if ((c.indexOf('spo2') >= 0 \|\| c.indexOf('o2') >= 0 \|\| c.indexOf('sao2')` |
| 766 | `bool \|\| → &&` | `if ((c.indexOf('spo2') >= 0 \|\| c.indexOf('o2') >= 0 \|\| c.indexOf('sao2')` |
| 766 | `bool && → \|\|` | `if ((c.indexOf('spo2') >= 0 \|\| c.indexOf('o2') >= 0 \|\| c.indexOf('sao2')` |
| 766 | `bool \|\| → &&` | `if ((c.indexOf('spo2') >= 0 \|\| c.indexOf('o2') >= 0 \|\| c.indexOf('sao2')` |
| 767 | `cmp < → <=` | `if ((c.indexOf('pulse') >= 0 \|\| c.indexOf('pr') === 0 \|\| c.indexOf('hr')` |
| 767 | `cmp >= → >` | `if ((c.indexOf('pulse') >= 0 \|\| c.indexOf('pr') === 0 \|\| c.indexOf('hr')` |
| 767 | `cmp >= → >` | `if ((c.indexOf('pulse') >= 0 \|\| c.indexOf('pr') === 0 \|\| c.indexOf('hr')` |
| 767 | `bool && → \|\|` | `if ((c.indexOf('pulse') >= 0 \|\| c.indexOf('pr') === 0 \|\| c.indexOf('hr')` |
| 767 | `bool \|\| → &&` | `if ((c.indexOf('pulse') >= 0 \|\| c.indexOf('pr') === 0 \|\| c.indexOf('hr')` |
| 768 | `cmp < → <=` | `if (c.indexOf('motion') >= 0 && motionCol < 0) motionCol = i;` |
| 770 | `cmp >= → >` | `if ((c.indexOf('pi') === 0 \|\| c.indexOf('perfus') >= 0) && piCol < 0) pi` |
| 770 | `cmp < → <=` | `if ((c.indexOf('pi') === 0 \|\| c.indexOf('perfus') >= 0) && piCol < 0) pi` |
| 775 | `cmp < → <=` | `if (timeCol < 0 \|\| spo2Col < 0 \|\| hrCol < 0) {` |
| 775 | `cmp < → <=` | `if (timeCol < 0 \|\| spo2Col < 0 \|\| hrCol < 0) {` |
| 775 | `bool \|\| → &&` | `if (timeCol < 0 \|\| spo2Col < 0 \|\| hrCol < 0) {` |
| 775 | `cmp < → <=` | `if (timeCol < 0 \|\| spo2Col < 0 \|\| hrCol < 0) {` |
| 775 | `bool \|\| → &&` | `if (timeCol < 0 \|\| spo2Col < 0 \|\| hrCol < 0) {` |
| 779 | `cmp < → <=` | `if (parts.length < 3) continue;` |
| 781 | `cmp < → <=` | `for (var j = 0; j < Math.min(parts.length, 3); j++) {` |
| 782 | `cmp >= → >` | `if (parts[j].indexOf(':') >= 0) {` |
| 786 | `cmp < → <=` | `motionCol = j + 3 < parts.length ? j + 3 : -1;` |
| 790 | `cmp >= → >` | `if (timeCol >= 0) break;` |
| 795 | `cmp < → <=` | `if (timeCol < 0) timeCol = 0;` |
| 796 | `cmp < → <=` | `if (spo2Col < 0) spo2Col = 1;` |
| 797 | `cmp < → <=` | `if (hrCol < 0) hrCol = 2;` |
| 808 | `cmp >= → >` | `if (_sp.length >= 3 && timeCol < _sp.length) _stamps.push(_sp[timeCol].t` |
| 808 | `cmp < → <=` | `if (_sp.length >= 3 && timeCol < _sp.length) _stamps.push(_sp[timeCol].t` |
| 808 | `bool && → \|\|` | `if (_sp.length >= 3 && timeCol < _sp.length) _stamps.push(_sp[timeCol].t` |
| 817 | `cmp < → <=` | `var tStr = timeCol < p.length ? p[timeCol].trim() : '';` |
| 818 | `cmp < → <=` | `var sStr = spo2Col < p.length ? p[spo2Col].trim() : '';` |
| 819 | `cmp < → <=` | `var hStr = hrCol < p.length ? p[hrCol].trim() : '';` |
| 837 | `cmp >= → >` | `var mStr = motionCol >= 0 && motionCol < p.length ? p[motionCol].trim() ` |
| 837 | `cmp < → <=` | `var mStr = motionCol >= 0 && motionCol < p.length ? p[motionCol].trim() ` |
| 838 | `bool \|\| → &&` | `if (!sStr \|\| sStr === '- -' \|\| sStr === '--' \|\| sStr === '') continue;` |
| 838 | `bool \|\| → &&` | `if (!sStr \|\| sStr === '- -' \|\| sStr === '--' \|\| sStr === '') continue;` |
| 838 | `bool \|\| → &&` | `if (!sStr \|\| sStr === '- -' \|\| sStr === '--' \|\| sStr === '') continue;` |
| 839 | `num → 0` | `var spo2 = parseInt(sStr, 10),` |
| 840 | `num → 0` | `hr = parseInt(hStr, 10),` |
| 841 | `num → 0` | `motion = mStr == null ? null : parseInt(mStr, 10) \|\| 0;` |
| 842 | `bool \|\| → &&` | `if (isNaN(spo2) \|\| isNaN(hr)) continue;` |
| 848 | `cmp >= → >` | `if (piCol >= 0 && piCol < p.length) {` |
| 848 | `cmp < → <=` | `if (piCol >= 0 && piCol < p.length) {` |
| 848 | `bool && → \|\|` | `if (piCol >= 0 && piCol < p.length) {` |
| 909 | `num → 0` | `FLOOR = 60;` |
| 910 | `cmp < → <=` | `if (n < FLOOR + MIN + 1) return { head: 0, tail: 0 }; // too short to sa` |
| 918 | `cmp >= → >` | `while (k >= 0 && k < n && rows[k].spo2 === s && rows[k].hr === h) {` |
| 928 | `cmp < → <=` | `if (hlen >= MIN && hlen <= MAX && hlen < n) {` |
| 928 | `cmp >= → >` | `if (hlen >= MIN && hlen <= MAX && hlen < n) {` |
| 928 | `cmp <= → <` | `if (hlen >= MIN && hlen <= MAX && hlen < n) {` |
| 928 | `bool && → \|\|` | `if (hlen >= MIN && hlen <= MAX && hlen < n) {` |
| 931 | `cmp >= → >` | `if (dS >= SPO2_STEP \|\| dH >= HR_STEP) head = hlen;` |
| 931 | `cmp >= → >` | `if (dS >= SPO2_STEP \|\| dH >= HR_STEP) head = hlen;` |
| 931 | `bool \|\| → &&` | `if (dS >= SPO2_STEP \|\| dH >= HR_STEP) head = hlen;` |
| 937 | `cmp >= → >` | `if (tlen >= MIN && tlen <= MAX && tlen < n - head) {` |
| 937 | `cmp <= → <` | `if (tlen >= MIN && tlen <= MAX && tlen < n - head) {` |
| 942 | `cmp >= → >` | `if (dS2 >= SPO2_STEP \|\| dH2 >= HR_STEP) tail = tlen;` |
| 942 | `cmp >= → >` | `if (dS2 >= SPO2_STEP \|\| dH2 >= HR_STEP) tail = tlen;` |
| 942 | `bool \|\| → &&` | `if (dS2 >= SPO2_STEP \|\| dH2 >= HR_STEP) tail = tlen;` |
| 946 | `cmp < → <=` | `if (n - head - tail < FLOOR) {` |
| 947 | `cmp < → <=` | `if (n - head < FLOOR) head = Math.max(0, n - FLOOR);` |
| 983 | `cmp >= → >` | `var isHard = Math.abs(rise) >= HARD; // catch both sudden rises AND drop` |
| 985 | `negate: drop !` | `if (!isHard && rise >= SOFT) {` |
| 985 | `cmp >= → >` | `if (!isHard && rise >= SOFT) {` |
| 987 | `num → 0` | `minsec = t.getUTCMinutes() + t.getUTCSeconds() / 60;` |
| 988 | `cmp >= → >` | `isSoft = minsec <= 2 \|\| minsec >= 58; // v14: full ±2min at ANY clock ho` |
| 988 | `bool \|\| → &&` | `isSoft = minsec <= 2 \|\| minsec >= 58; // v14: full ±2min at ANY clock ho` |
| 988 | `cmp <= → <` | `isSoft = minsec <= 2 \|\| minsec >= 58; // v14: full ±2min at ANY clock ho` |
| 988 | `num → 0` | `isSoft = minsec <= 2 \|\| minsec >= 58; // v14: full ±2min at ANY clock ho` |
| 995 | `cmp > → >=` | `var isRise = rise > 0;` |
| 996 | `cmp > → >=` | `while (j < n && j - i < MAX_RUN && (isRise ? rows[j].hr > baseline + REC` |
| 996 | `cmp < → <=` | `while (j < n && j - i < MAX_RUN && (isRise ? rows[j].hr > baseline + REC` |
| 996 | `cmp < → <=` | `while (j < n && j - i < MAX_RUN && (isRise ? rows[j].hr > baseline + REC` |
| 996 | `cmp < → <=` | `while (j < n && j - i < MAX_RUN && (isRise ? rows[j].hr > baseline + REC` |
| 1005 | `cmp < → <=` | `for (var k = i; k < j; k++) {` |
| 1010 | `cmp > → >=` | `i = j > i ? j : i + 1; // defensive: guarantee progress even if SOFT ≤ R` |
| 1069 | `cmp < → <=` | `if (n < 60) return null;` |
| 1081 | `cmp < → <=` | `if (n < 60) return null;` |
| 1134 | `cmp < → <=` | `return e.nadirIdx < midIdx;` |
| 1137 | `cmp >= → >` | `return e.nadirIdx >= midIdx;` |
| 1140 | `cmp > → >=` | `var clusteringIdx = total > 0 ? +(lastH / total).toFixed(2) : null; // >` |
| 1168 | `cmp <= → <` | `for (var w = 0; w + WIN <= n; w += WIN) {` |
| 1174 | `cmp >= → >` | `if (segMean >= THRESH) continue; // not an oscillating window` |
| 1178 | `cmp < → <=` | `for (var i = 1; i < seg.length; i++) {` |
| 1195 | `cmp < → <=` | `for (var i = 1; i < crossingTimes.length; i++) {` |
| 1205 | `cmp > → >=` | `if (iv > 5 && iv < 300) intervals.push(iv); // sanity: 5s–300s` |
| 1205 | `cmp < → <=` | `if (iv > 5 && iv < 300) intervals.push(iv); // sanity: 5s–300s` |
| 1227 | `cmp > → >=` | `if (cycleIntervals.length > 1) {` |
| 1249 | `bool && → \|\|` | `var pbLoad = pbCycleLen && pbAmplitude && durationHr > 0 ? +((osc.episod` |
| 1249 | `cmp > → >=` | `var pbLoad = pbCycleLen && pbAmplitude && durationHr > 0 ? +((osc.episod` |
| 1249 | `bool && → \|\|` | `var pbLoad = pbCycleLen && pbAmplitude && durationHr > 0 ? +((osc.episod` |
| 1255 | `cmp < → <=` | `return t < t1;` |
| 1258 | `cmp >= → >` | `return t >= t2;` |
| 1261 | `cmp > → >=` | `var pbFirstThirdRatio = total > 0 ? +(earlyCount / total).toFixed(2) : n` |
| 1270 | `cmp < → <=` | `if (n < 600) return null;` |
| 1270 | `num → 0` | `if (n < 600) return null;` |
| 1276 | `cmp <= → <` | `for (var i = 0; i + WIN <= n; i++) {` |
| 1289 | `cmp < → <=` | `if (sd < 5) {` |
| 1318 | `cmp < → <=` | `for (var j = 0; j < Math.min(HALF, n); j++) {` |
| 1322 | `cmp < → <=` | `for (var i = 0; i < n; i++) {` |
| 1331 | `cmp >= → >` | `if (lEdge >= 0) {` |
| 1335 | `cmp > → >=` | `hrSmooth[i] = rCnt > 0 ? rSum / rCnt : rows[i].hr;` |
| 1339 | `cmp < → <=` | `for (var i = 150; i < n - 150; i++) {` |
| 1339 | `num → 0` | `for (var i = 150; i < n - 150; i++) {` |
| 1339 | `num → 0` | `for (var i = 150; i < n - 150; i++) {` |
| 1340 | `cmp < → <=` | `if (hrSmooth[i] < hrSmooth[i - 150] && hrSmooth[i] < hrSmooth[i + 150]) ` |
| 1340 | `cmp < → <=` | `if (hrSmooth[i] < hrSmooth[i - 150] && hrSmooth[i] < hrSmooth[i + 150]) ` |
| 1341 | `cmp >= → >` | `if (!valleys.length \|\| i - valleys[valleys.length - 1] >= MIN_SEP) {` |
| 1355 | `cmp < → <=` | `if (n < 60) return { odi1Rate: 0, odi1Total: 0 };` |
| 1364 | `cmp > → >=` | `return { odi1Rate: durationHr > 0 ? +(events / durationHr).toFixed(1) : ` |
| 1376 | `cmp >= → >` | `var odiHigh = odi4Rate >= DexKernel.K.MOS_LONG;` |
| 1377 | `cmp >= → >` | `var ctHigh = ct90Min >= 5;` |
| 1378 | `cmp >= → >` | `var odiMod = odi4Rate >= DexKernel.K.MOS_SHORT;` |
| 1379 | `cmp >= → >` | `var ctMod = ct90Min >= 1;` |
| 1381 | `bool && → \|\|` | `if (odiHigh && ctHigh) score = 4;` |
| 1382 | `bool \|\| → &&` | `else if (odiHigh \|\| ctHigh) score = 3;` |
| 1417 | `cmp < → <=` | `if (n < 60 \|\| !stats) return null;` |
| 1478 | `cmp >= → >` | `if (inBurst && burstLen >= 3) motionBursts++;` |
| 1483 | `cmp < → <=` | `for (var i = 0; i < n; i++) {` |
| 1486 | `cmp > → >=` | `if (curRun > longestCleanRun) longestCleanRun = curRun;` |
| 1492 | `bool && → \|\|` | `var nadirCount = desat && desat.nadir ? desat.nadir.count : 0;` |
| 1494 | `cmp > → >=` | `var nadirDensity = durationHr > 0 ? +(nadirCount / durationHr).toFixed(2` |
| 1498 | `bool && → \|\|` | `if (stats && hb) {` |
| 1505 | `bool && → \|\|` | `if (odi4 && odi1 && odi1.odi1Rate > 0) {` |
| 1505 | `cmp > → >=` | `if (odi4 && odi1 && odi1.odi1Rate > 0) {` |
| 1505 | `bool && → \|\|` | `if (odi4 && odi1 && odi1.odi1Rate > 0) {` |
| 1530 | `cmp < → <=` | `if (n < 600) return null;` |
| 1550 | `cmp < → <=` | `if (m < worst10) worst10 = m;` |
| 1556 | `cmp <= → <` | `for (var i = 0; i + W30 <= n; i += 60) {` |
| 1562 | `cmp > → >=` | `if (pct > worstT95) worstT95 = pct;` |
| 1579 | `cmp < → <=` | `if (sd < 1) stableWins++;` |
| 1596 | `cmp < → <=` | `for (var i = 0; i < n; i++) {` |
| 1597 | `cmp > → >=` | `var newState = spo2[i] > hiThresh ? 1 : spo2[i] < loThresh ? -1 : cdiSta` |
| 1597 | `cmp < → <=` | `var newState = spo2[i] > hiThresh ? 1 : spo2[i] < loThresh ? -1 : cdiSta` |
| 1603 | `cmp > → >=` | `var cdi = durationHr > 0 ? +(cdiCross / durationHr / 2).toFixed(2) : 0; ` |
| 1613 | `cmp < → <=` | `if (postIdx < n && e.nadirIdx < n) postDipDeltas.push(hr[postIdx] - hr[e` |
| 1613 | `bool && → \|\|` | `if (postIdx < n && e.nadirIdx < n) postDipDeltas.push(hr[postIdx] - hr[e` |
| 1613 | `cmp < → <=` | `if (postIdx < n && e.nadirIdx < n) postDipDeltas.push(hr[postIdx] - hr[e` |
| 1627 | `cmp < → <=` | `for (var i = 1; i < n; i++) {` |
| 1629 | `eq === → !==` | `if (decelLen === 0) decelStartHR = hr[i - 1]; // capture baseline before` |
| 1632 | `cmp >= → >` | `if (decelLen >= 30 && decelStartHR - hr[i - 1] >= 3) hrDecelRuns++;` |
| 1632 | `cmp >= → >` | `if (decelLen >= 30 && decelStartHR - hr[i - 1] >= 3) hrDecelRuns++;` |
| 1637 | `cmp >= → >` | `if (decelLen >= 30 && decelStartHR - hr[n - 1] >= 3) hrDecelRuns++; // e` |
| 1637 | `bool && → \|\|` | `if (decelLen >= 30 && decelStartHR - hr[n - 1] >= 3) hrDecelRuns++; // e` |
| 1637 | `cmp >= → >` | `if (decelLen >= 30 && decelStartHR - hr[n - 1] >= 3) hrDecelRuns++; // e` |
| 1637 | `num → 0` | `if (decelLen >= 30 && decelStartHR - hr[n - 1] >= 3) hrDecelRuns++; // e` |
| 1648 | `cmp > → >=` | `if (dSpo2 > 0 !== dHr > 0) decoupled++;` |
| 1648 | `cmp > → >=` | `if (dSpo2 > 0 !== dHr > 0) decoupled++;` |
| 1662 | `cmp <= → <` | `if (end <= start) break;` |
| 1730 | `cmp >= → >` | `if (pbMet.pbCycleLen >= 40 && pbMet.pbCycleLen <= 130) cs++; // clinical` |
| 1730 | `num → 0` | `if (pbMet.pbCycleLen >= 40 && pbMet.pbCycleLen <= 130) cs++; // clinical` |
| 1730 | `bool && → \|\|` | `if (pbMet.pbCycleLen >= 40 && pbMet.pbCycleLen <= 130) cs++; // clinical` |
| 1730 | `cmp <= → <` | `if (pbMet.pbCycleLen >= 40 && pbMet.pbCycleLen <= 130) cs++; // clinical` |
| 1735 | `cmp >= → >` | `return f.code.indexOf('BLUNTED_AROUSAL') >= 0;` |
| 1739 | `cmp < → <=` | `if (cross && cross.crcIdx != null && cross.crcIdx < 0.2) cs++;` |
| 1741 | `cmp >= → >` | `if (odi4 && odi4.rate < 3 && osc && osc.episodeCount >= 5) cs++;` |
| 1741 | `cmp < → <=` | `if (odi4 && odi4.rate < 3 && osc && osc.episodeCount >= 5) cs++;` |
| 1748 | `cmp < → <=` | `if (pbMet.pbCycleLen < 40) uars++;` |
| 1748 | `num → 0` | `if (pbMet.pbCycleLen < 40) uars++;` |
| 1750 | `cmp >= → >` | `if (cross && cross.autoArousalIdx >= 3) uars++;` |
| 1751 | `cmp >= → >` | `if (odi4 && odi4.rate < 5 && osc && osc.episodeCount >= 3) uars++;` |
| 1751 | `cmp < → <=` | `if (odi4 && odi4.rate < 5 && osc && osc.episodeCount >= 3) uars++;` |
| 1752 | `cmp >= → >` | `if (comp && comp.sfi >= 2) uars++;` |
| 1784 | `bool && → \|\|` | `return r && r.spo2 != null ? r.spo2 : null;` |
| 1787 | `bool && → \|\|` | `return v != null && isFinite(v);` |
| 1790 | `cmp < → <=` | `if (n < 256) return null;` |
| 1790 | `num → 0` | `if (n < 256) return null;` |
| 1802 | `cmp < → <=` | `for (var i = 0; i < n; i++) {` |
| 1809 | `cmp < → <=` | `for (var s = 0; s < scales.length; s++) {` |
| 1813 | `cmp <= → <` | `for (var start = 0; start + wn <= n; start += wn) {` |
| 1836 | `cmp > → >=` | `if (count > 0) {` |
| 1842 | `cmp < → <=` | `logN.length < 2 \|\|` |
| 1842 | `bool \|\| → &&` | `logN.length < 2 \|\|` |
| 1863 | `bool \|\| → &&` | `var label = alpha1 === null \|\| !isFinite(alpha1) ? '—' : 'SpO₂ DFA (α1='` |
| 1930 | `cmp > → >=` | `if (!(den > 0)) return 0;` |
| 1934 | `cmp > → >=` | `return r > 0.999 ? 0.999 : r < 0 ? 0 : r;` |
| 1934 | `cmp < → <=` | `return r > 0.999 ? 0.999 : r < 0 ? 0 : r;` |
| 1934 | `num → 0` | `return r > 0.999 ? 0.999 : r < 0 ? 0 : r;` |
| 1950 | `cmp < → <=` | `if (n < 512) return null;` |
| 1950 | `num → 0` | `if (n < 512) return null;` |
| 1951 | `cmp > → >=` | `var RATE = fs > 0 ? fs : 1; // SpO2 rows are 1 Hz; the argument keeps th` |
| 1960 | `cmp <= → <` | `if (kHi <= kLo) return null;` |
| 1990 | `cmp > → >=` | `for (fi = 0; fi < pows.length; fi++) ratios.push(bg[fi] > 0 ? pows[fi] /` |
| 1995 | `cmp < → <=` | `for (fi = 0; fi < pows.length; fi++) {` |
| 1996 | `bool && → \|\|` | `var snr = scale > 0 && bg[fi] > 0 ? pows[fi] / (scale * bg[fi]) : 0;` |
| 1996 | `cmp > → >=` | `var snr = scale > 0 && bg[fi] > 0 ? pows[fi] / (scale * bg[fi]) : 0;` |
| 1996 | `cmp > → >=` | `var snr = scale > 0 && bg[fi] > 0 ? pows[fi] / (scale * bg[fi]) : 0;` |
| 1997 | `cmp > → >=` | `if (snr > bestSnr) {` |
| 2002 | `cmp >= → >` | `var ok = bestFreq != null && bestSnr >= thresh;` |
| 2035 | `num → 0` | `if (n < 200) return null;` |
| 2035 | `cmp < → <=` | `if (n < 200) return null;` |
| 2044 | `cmp > → >=` | `if (n > CAP) {` |
| 2062 | `num → 0` | `var r = 0.2 * stdv;` |
| 2068 | `cmp < → <=` | `for (var i = 0; i < USE - m; i++) {` |
| 2072 | `cmp > → >=` | `if (Math.abs(x[i + k] - x[j + k]) > r) {` |
| 2084 | `cmp > → >=` | `var sampEn = Bm > 0 && Am > 0 ? +(-Math.log(Am / Bm)).toFixed(4) : null;` |
| 2084 | `bool && → \|\|` | `var sampEn = Bm > 0 && Am > 0 ? +(-Math.log(Am / Bm)).toFixed(4) : null;` |
| 2084 | `cmp > → >=` | `var sampEn = Bm > 0 && Am > 0 ? +(-Math.log(Am / Bm)).toFixed(4) : null;` |
| 2085 | `cmp < → <=` | `var label = sampEn === null ? '—' : sampEn < 0.5 ? 'Low (regular)' : sam` |
| 2085 | `cmp < → <=` | `var label = sampEn === null ? '—' : sampEn < 0.5 ? 'Low (regular)' : sam` |
| 2085 | `num → 0` | `var label = sampEn === null ? '—' : sampEn < 0.5 ? 'Low (regular)' : sam` |
| 2091 | `cmp < → <=` | `if (!durationHr \|\| durationHr < 0.5) return null;` |
| 2091 | `num → 0` | `if (!durationHr \|\| durationHr < 0.5) return null;` |
| 2092 | `bool && → \|\|` | `var spikeRate = spikes && spikes.length ? spikes.length / durationHr : 0` |
| 2094 | `bool && → \|\|` | `var postDipAct = rolling && rolling.postDipHrResponse !== null ? Math.ma` |
| 2097 | `cmp < → <=` | `var label = ssi < 0.5 ? 'Low' : ssi < 1.5 ? 'Moderate' : 'High';` |
| 2097 | `cmp < → <=` | `var label = ssi < 0.5 ? 'Low' : ssi < 1.5 ? 'Moderate' : 'High';` |
| 2097 | `num → 0` | `var label = ssi < 0.5 ? 'Low' : ssi < 1.5 ? 'Moderate' : 'High';` |
| 2108 | `cmp < → <=` | `if (n < 3600) return null;` |
| 2145 | `cmp < → <=` | `if (nadirFrac < 0) nadirFrac += 1;` |
| 2156 | `num → 0` | `if (n < 200) return null;` |
| 2156 | `cmp < → <=` | `if (n < 200) return null;` |
| 2163 | `cmp > → >=` | `if (n > CAP) {` |
| 2180 | `num → 0` | `var r = 0.15 * stdv; // tighter tolerance for SpO2 (1% resolution)` |
| 2184 | `cmp < → <=` | `for (var i = 0; i < USE - m; i++) {` |
| 2190 | `cmp > → >=` | `if (k < m && diff > r) mOk = false;` |
| 2191 | `cmp > → >=` | `if (diff > r) mOk1 = false;` |
| 2197 | `cmp > → >=` | `var spo2En = Bm > 0 && Am > 0 ? +(-Math.log(Am / Bm)).toFixed(4) : null;` |
| 2197 | `cmp > → >=` | `var spo2En = Bm > 0 && Am > 0 ? +(-Math.log(Am / Bm)).toFixed(4) : null;` |
| 2197 | `bool && → \|\|` | `var spo2En = Bm > 0 && Am > 0 ? +(-Math.log(Am / Bm)).toFixed(4) : null;` |
| 2198 | `cmp < → <=` | `var label = spo2En === null ? '—' : spo2En < 0.3 ? 'Low(periodic)' : spo` |
| 2198 | `cmp < → <=` | `var label = spo2En === null ? '—' : spo2En < 0.3 ? 'Low(periodic)' : spo` |
| 2204 | `cmp < → <=` | `if (!odi3 \|\| durationHr < 0.5) return null;` |
| 2204 | `num → 0` | `if (!odi3 \|\| durationHr < 0.5) return null;` |
| 2210 | `cmp > → >=` | `if (rows && rows.length > 60) {` |
| 2210 | `num → 0` | `if (rows && rows.length > 60) {` |
| 2237 | `cmp > → >=` | `var rate = durationHr > 0 ? +(nadirCount / durationHr).toFixed(1) : odi3` |
| 2239 | `cmp < → <=` | `var label = hl < 1 ? 'Low' : hl < 5 ? 'Moderate' : 'High';` |
| 2239 | `cmp < → <=` | `var label = hl < 1 ? 'Low' : hl < 5 ? 'Moderate' : 'High';` |
| 2247 | `num → 0` | `var hrFloor = hrv.hrFloor \|\| 60;` |
| 2251 | `cmp < → <=` | `var label = vi < 0.01 ? 'Low' : vi < 0.05 ? 'Moderate' : 'High';` |
| 2251 | `cmp < → <=` | `var label = vi < 0.01 ? 'Low' : vi < 0.05 ? 'Moderate' : 'High';` |
| 2257 | `bool \|\| → &&` | `if (!slopes \|\| !slopes.meanDipSlope \|\| !slopes.meanRecSlope) return null` |
| 2260 | `cmp > → >=` | `var label = ri > 1.5 ? 'Fast' : ri > 0.8 ? 'Symmetric' : 'Slow';` |
| 2260 | `cmp > → >=` | `var label = ri > 1.5 ? 'Fast' : ri > 0.8 ? 'Symmetric' : 'Slow';` |
| 2284 | `cmp < → <=` | `var label = spi < 5 ? 'Low' : spi < 15 ? 'Moderate' : 'High';` |
| 2284 | `num → 0` | `var label = spi < 5 ? 'Low' : spi < 15 ? 'Moderate' : 'High';` |
| 2284 | `cmp < → <=` | `var label = spi < 5 ? 'Low' : spi < 15 ? 'Moderate' : 'High';` |
| 2290 | `cmp < → <=` | `if (!rows \|\| rows.length < 60) return null;` |
| 2290 | `num → 0` | `if (!rows \|\| rows.length < 60) return null;` |
| 2301 | `cmp < → <=` | `if (nadirTimes.length < 3) return null;` |
| 2314 | `cmp > → >=` | `var biCV = iMean > 0 ? +((iSD / iMean) * 100).toFixed(1) : null;` |
| 2315 | `cmp < → <=` | `var label = biCV === null ? '—' : biCV < 30 ? 'Regular' : biCV < 60 ? 'V` |
| 2315 | `cmp < → <=` | `var label = biCV === null ? '—' : biCV < 30 ? 'Regular' : biCV < 60 ? 'V` |
| 2315 | `num → 0` | `var label = biCV === null ? '—' : biCV < 30 ? 'Regular' : biCV < 60 ? 'V` |
| 2325 | `cmp < → <=` | `if (n < 60) return { oxyCrashCount: 0 };` |
| 2329 | `cmp < → <=` | `for (var i = WIN; i < n; i++) {` |
| 2330 | `cmp > → >=` | `if (cooldown > 0) {` |
| 2340 | `cmp > → >=` | `return { oxyCrashCount: crashes, oxyCrashRate: durationHr > 0 ? +(crashe` |
| 2352 | `cmp > → >=` | `var label = dip > 10 ? 'Good (intra-night)' : dip > 5 ? 'Moderate (intra` |
| 2361 | `cmp > → >=` | `var label = asym > 1.5 ? 'Abrupt (obstructive)' : asym > 0.7 ? 'Symmetri` |
| 2361 | `cmp > → >=` | `var label = asym > 1.5 ? 'Abrupt (obstructive)' : asym > 0.7 ? 'Symmetri` |
| 2361 | `num → 0` | `var label = asym > 1.5 ? 'Abrupt (obstructive)' : asym > 0.7 ? 'Symmetri` |
| 2361 | `num → 0` | `var label = asym > 1.5 ? 'Abrupt (obstructive)' : asym > 0.7 ? 'Symmetri` |
| 2377 | `cmp >= → >` | `var cat = spo2Keys.indexOf(key) >= 0 ? 'spo2' : hrKeys.indexOf(key) >= 0` |
| 2377 | `cmp >= → >` | `var cat = spo2Keys.indexOf(key) >= 0 ? 'spo2' : hrKeys.indexOf(key) >= 0` |
| 2403 | `cmp < → <=` | `n.osc.episodeCount === 0 ? 0 : n.osc.episodeCount < 3 ? 1 : n.osc.episod` |
| 2403 | `cmp < → <=` | `n.osc.episodeCount === 0 ? 0 : n.osc.episodeCount < 3 ? 1 : n.osc.episod` |
| 2403 | `cmp < → <=` | `n.osc.episodeCount === 0 ? 0 : n.osc.episodeCount < 3 ? 1 : n.osc.episod` |
| 2403 | `num → 0` | `n.osc.episodeCount === 0 ? 0 : n.osc.episodeCount < 3 ? 1 : n.osc.episod` |
| 2403 | `num → 0` | `n.osc.episodeCount === 0 ? 0 : n.osc.episodeCount < 3 ? 1 : n.osc.episod` |
| 2411 | `cmp >= → >` | `n.rolling.worst10minSpo2 >= 95 ? 0 : n.rolling.worst10minSpo2 >= 93 ? 2 ` |
| 2411 | `num → 0` | `n.rolling.worst10minSpo2 >= 95 ? 0 : n.rolling.worst10minSpo2 >= 93 ? 2 ` |
| 2411 | `cmp >= → >` | `n.rolling.worst10minSpo2 >= 95 ? 0 : n.rolling.worst10minSpo2 >= 93 ? 2 ` |
| 2411 | `cmp >= → >` | `n.rolling.worst10minSpo2 >= 95 ? 0 : n.rolling.worst10minSpo2 >= 93 ? 2 ` |
| 2411 | `num → 0` | `n.rolling.worst10minSpo2 >= 95 ? 0 : n.rolling.worst10minSpo2 >= 93 ? 2 ` |
| 2411 | `num → 0` | `n.rolling.worst10minSpo2 >= 95 ? 0 : n.rolling.worst10minSpo2 >= 93 ? 2 ` |
| 2411 | `cmp >= → >` | `n.rolling.worst10minSpo2 >= 95 ? 0 : n.rolling.worst10minSpo2 >= 93 ? 2 ` |
| 2411 | `num → 0` | `n.rolling.worst10minSpo2 >= 95 ? 0 : n.rolling.worst10minSpo2 >= 93 ? 2 ` |
| 2420 | `cmp < → <=` | `n.oxyCrash.oxyCrashRate < 1 ? 0 : n.oxyCrash.oxyCrashRate < 3 ? 3 : n.ox` |
| 2420 | `cmp < → <=` | `n.oxyCrash.oxyCrashRate < 1 ? 0 : n.oxyCrash.oxyCrashRate < 3 ? 3 : n.ox` |
| 2420 | `num → 0` | `n.oxyCrash.oxyCrashRate < 1 ? 0 : n.oxyCrash.oxyCrashRate < 3 ? 3 : n.ox` |
| 2438 | `cmp < → <=` | `if (n.motSleep.wasoPct != null) push('waso', 'WASO %', n.motSleep.wasoPc` |
| 2438 | `num → 0` | `if (n.motSleep.wasoPct != null) push('waso', 'WASO %', n.motSleep.wasoPc` |
| 2438 | `num → 0` | `if (n.motSleep.wasoPct != null) push('waso', 'WASO %', n.motSleep.wasoPc` |
| 2438 | `cmp < → <=` | `if (n.motSleep.wasoPct != null) push('waso', 'WASO %', n.motSleep.wasoPc` |
| 2438 | `cmp < → <=` | `if (n.motSleep.wasoPct != null) push('waso', 'WASO %', n.motSleep.wasoPc` |
| 2453 | `cmp > → >=` | `n.hrnDip.hrnDip > 10 ? 0 : n.hrnDip.hrnDip > 5 ? 3 : n.hrnDip.hrnDip > 2` |
| 2453 | `cmp > → >=` | `n.hrnDip.hrnDip > 10 ? 0 : n.hrnDip.hrnDip > 5 ? 3 : n.hrnDip.hrnDip > 2` |
| 2455 | `cmp > → >=` | `n.hrnDip.hrnDip > 10 ? 'g' : n.hrnDip.hrnDip > 5 ? 'w' : 'r'` |
| 2465 | `cmp > → >=` | `n.recIdx.recoveryIndex > 1.5 ? 0 : n.recIdx.recoveryIndex > 0.8 ? 2 : n.` |
| 2465 | `cmp > → >=` | `n.recIdx.recoveryIndex > 1.5 ? 0 : n.recIdx.recoveryIndex > 0.8 ? 2 : n.` |
| 2465 | `cmp > → >=` | `n.recIdx.recoveryIndex > 1.5 ? 0 : n.recIdx.recoveryIndex > 0.8 ? 2 : n.` |
| 2467 | `cmp > → >=` | `n.recIdx.recoveryIndex > 1.5 ? 'g' : n.recIdx.recoveryIndex > 0.8 ? 'w' ` |
| 2467 | `cmp > → >=` | `n.recIdx.recoveryIndex > 1.5 ? 'g' : n.recIdx.recoveryIndex > 0.8 ? 'w' ` |
| 2477 | `eq === → !==` | `_sq === 'Q5(high)' ? 10 : _sq === 'Q4' ? 7 : _sq === 'Q3' ? 4 : _sq === ` |
| 2477 | `eq === → !==` | `_sq === 'Q5(high)' ? 10 : _sq === 'Q4' ? 7 : _sq === 'Q3' ? 4 : _sq === ` |
| 2488 | `num → 0` | `_pq === 'Q5(high)' ? 10 : _pq === 'Q4' ? 7 : _pq === 'Q3' ? 4 : _pq === ` |
| 2557 | `cmp < → <=` | `if (avgScore < 2 && worstScore < 10) severity = 'Clean night';` |
| 2558 | `cmp < → <=` | `else if (avgScore < 3 && worstScore < 10) severity = 'Mild disruption';` |
| 2559 | `cmp < → <=` | `else if (avgScore < 4.5) severity = 'Moderate burden';` |
| 2561 | `cmp < → <=` | `var isolatedSevere = avgScore < 4 && worstScore >= 6; // mostly-clean ni` |
| 2561 | `bool && → \|\|` | `var isolatedSevere = avgScore < 4 && worstScore >= 6; // mostly-clean ni` |
| 2561 | `cmp >= → >` | `var isolatedSevere = avgScore < 4 && worstScore >= 6; // mostly-clean ni` |
| 2628 | `cmp > → >=` | `var supportTxt = top5.length > 1 ? (leads[top5[1].key] ? leads[top5[1].k` |
| 2654 | `cmp >= → >` | `else if (n.stab && n.stab.score != null && n.stab.score >= 80) context =` |
| 2655 | `cmp >= → >` | `else if (n.osc && n.osc.episodeCount >= 10) context = '; high PB burden'` |
| 2685 | `bool && → \|\|` | `for (var si = 0; si < rawSpikes.length; si++) if (rawSpikes[si].artifact` |
| 2692 | `bool && → \|\|` | `var _oscEpisodes = osc && osc.episodes ? osc.episodes : [];` |
| 2706 | `num → 0` | `var blArr = computeCeilingBaselineArr(spo2s, 300, 90);` |
| 2734 | `bool && → \|\|` | `if (odi4 && desat && desat.artifactCount) {` |
| 2734 | `bool && → \|\|` | `if (odi4 && desat && desat.artifactCount) {` |
| 2743 | `num → 0` | `odi4.rate = +(odi4.count / Math.max(rows.length / 3600, 0.01)).toFixed(1` |
| 3018 | `bool && → \|\|` | `typeof SignalFrame !== 'undefined' && SignalFrame && SignalFrame.compute` |
| 3018 | `bool && → \|\|` | `typeof SignalFrame !== 'undefined' && SignalFrame && SignalFrame.compute` |
| 3061 | `cmp > → >=` | `if (n > 8) {` |
| 3065 | `cmp > → >=` | `if (_dt > 0) _d.push(_dt);` |
| 3067 | `cmp > → >=` | `if (_d.length > 4) {` |
| 3074 | `cmp > → >=` | `var _durInflated = _cad != null && rawDurMs > 1.5 * n * _cad + 3600000;` |
| 3074 | `num → 0` | `var _durInflated = _cad != null && rawDurMs > 1.5 * n * _cad + 3600000;` |
| 3089 | `cmp > → >=` | `n > 0` |
| 3099 | `cmp > → >=` | `n > 0` |
| 3116 | `cmp > → >=` | `n > 0` |
| 3156 | `cmp < → <=` | `if (n < 120) return null;` |
| 3207 | `cmp <= → <` | `for (var i = 0; i + WIN <= n; i += WIN) {` |
| 3226 | `cmp > → >=` | `var rmssd = n > 1 ? +Math.sqrt(rmssd_ss / (n - 1)).toFixed(2) : 0;` |
| 3274 | `bool \|\| → &&` | `var WIN = opts.WIN \|\| 300; // 5-min clinical baseline` |
| 3274 | `num → 0` | `var WIN = opts.WIN \|\| 300; // 5-min clinical baseline` |
| 3275 | `bool \|\| → &&` | `var blArr = opts.blArr \|\| computeCeilingBaselineArr(spo2, WIN, opts.pct ` |
| 3279 | `num → 0` | `evNadir = 100,` |
| 3281 | `num → 0` | `evBaseline = 100;` |
| 3300 | `cmp < → <=` | `for (var i = 0; i < n; i++) {` |
| 3339 | `num → 0` | `return { count: events.length, rate: +(events.length / Math.max(hrs, 0.0` |
| 3346 | `cmp < → <=` | `for (var i = 10; i < n - 20; i++) {` |
| 3346 | `num → 0` | `for (var i = 10; i < n - 20; i++) {` |
| 3348 | `cmp < → <=` | `if (i - lastIdx < CFG.SPIKE_COOLDOWN_SEC) continue;` |
| 3351 | `cmp < → <=` | `for (var k = i - 10; k < i; k++) {` |
| 3352 | `cmp >= → >` | `if (k >= 0 && rows[k].motion > 0) {` |
| 3361 | `cmp >= → >` | `if (k >= 0) {` |
| 3369 | `cmp > → >=` | `for (var j = i; j < Math.min(i + 12, n); j++) if (rows[j].hr > localMax1` |
| 3371 | `cmp < → <=` | `if (localMax12 < CFG.HR_SPIKE_MIN_PEAK \|\| peakRise12 < 20) continue;` |
| 3371 | `cmp < → <=` | `if (localMax12 < CFG.HR_SPIKE_MIN_PEAK \|\| peakRise12 < 20) continue;` |
| 3373 | `cmp < → <=` | `for (var j = i; j < Math.min(i + 18, n); j++) if (rows[j].hr >= Math.max` |
| 3373 | `cmp >= → >` | `for (var j = i; j < Math.min(i + 18, n); j++) if (rows[j].hr >= Math.max` |
| 3373 | `num → 0` | `for (var j = i; j < Math.min(i + 18, n); j++) if (rows[j].hr >= Math.max` |
| 3377 | `cmp > → >=` | `for (var j = i + 12; j < Math.min(i + 40, n); j++) if (rows[j].hr > loca` |
| 3377 | `cmp < → <=` | `for (var j = i + 12; j < Math.min(i + 40, n); j++) if (rows[j].hr > loca` |
| 3377 | `num → 0` | `for (var j = i + 12; j < Math.min(i + 40, n); j++) if (rows[j].hr > loca` |
| 3377 | `num → 0` | `for (var j = i + 12; j < Math.min(i + 40, n); j++) if (rows[j].hr > loca` |
| 3379 | `cmp >= → >` | `for (var j = i; j < Math.min(i + 30, n); j++) if (rows[j].hr >= CFG.HR_S` |
| 3379 | `cmp < → <=` | `for (var j = i; j < Math.min(i + 30, n); j++) if (rows[j].hr >= CFG.HR_S` |
| 3410 | `cmp < → <=` | `for (var j = Math.max(1, i - 2); j < Math.min(i + 14, n); j++) {` |
| 3410 | `num → 0` | `for (var j = Math.max(1, i - 2); j < Math.min(i + 14, n); j++) {` |
| 3412 | `cmp > → >=` | `if (stepUp > onsetRise) onsetRise = stepUp;` |
| 3414 | `num → 0` | `var secPastHour = rows[i].t.getUTCMinutes() * 60 + rows[i].t.getUTCSecon` |
| 3415 | `cmp >= → >` | `var isArtifact = onsetRise >= CFG.HR_SPIKE_MAX_PHYSIOLOGIC_RISE;` |
| 3425 | `cmp >= → >` | `clockAligned: secPastHour <= 60 \|\| secPastHour >= 3540,` |
| 3425 | `cmp <= → <` | `clockAligned: secPastHour <= 60 \|\| secPastHour >= 3540,` |
| 3425 | `bool \|\| → &&` | `clockAligned: secPastHour <= 60 \|\| secPastHour >= 3540,` |
| 3425 | `num → 0` | `clockAligned: secPastHour <= 60 \|\| secPastHour >= 3540,` |
| 3425 | `num → 0` | `clockAligned: secPastHour <= 60 \|\| secPastHour >= 3540,` |
| 3435 | `cmp < → <=` | `if (spikes.length < 3) return null;` |
| 3439 | `cmp < → <=` | `for (var i = 1; i < times.length; i++) {` |
| 3440 | `cmp < → <=` | `while (times[i] < times[i - 1]) times[i] += 86400;` |
| 3440 | `num → 0` | `while (times[i] < times[i - 1]) times[i] += 86400;` |
| 3443 | `num → 0` | `for (var i = 1; i < times.length; i++) intervals.push(Math.round((times[` |
| 3443 | `cmp < → <=` | `for (var i = 1; i < times.length; i++) intervals.push(Math.round((times[` |
| 3444 | `cmp < → <=` | `if (intervals.length < 3) return null;` |
| 3450 | `cmp <= → <` | `return Math.abs(v - med) <= 10;` |
| 3450 | `num → 0` | `return Math.abs(v - med) <= 10;` |
| 3452 | `cmp < → <=` | `if (kept.length < 3) return null;` |
| 3461 | `cmp >= → >` | `if (regularity >= 0.8 && avgInterval >= 50 && avgInterval <= 75 && sprea` |
| 3461 | `cmp >= → >` | `if (regularity >= 0.8 && avgInterval >= 50 && avgInterval <= 75 && sprea` |
| 3461 | `cmp <= → <` | `if (regularity >= 0.8 && avgInterval >= 50 && avgInterval <= 75 && sprea` |
| 3461 | `cmp <= → <` | `if (regularity >= 0.8 && avgInterval >= 50 && avgInterval <= 75 && sprea` |
| 3461 | `bool && → \|\|` | `if (regularity >= 0.8 && avgInterval >= 50 && avgInterval <= 75 && sprea` |
| 3461 | `bool && → \|\|` | `if (regularity >= 0.8 && avgInterval >= 50 && avgInterval <= 75 && sprea` |
| 3461 | `num → 0` | `if (regularity >= 0.8 && avgInterval >= 50 && avgInterval <= 75 && sprea` |
| 3461 | `bool && → \|\|` | `if (regularity >= 0.8 && avgInterval >= 50 && avgInterval <= 75 && sprea` |
| 3461 | `num → 0` | `if (regularity >= 0.8 && avgInterval >= 50 && avgInterval <= 75 && sprea` |
| 3461 | `num → 0` | `if (regularity >= 0.8 && avgInterval >= 50 && avgInterval <= 75 && sprea` |
| 3462 | `cmp >= → >` | `else if (regularity >= 0.8 && avgInterval >= 20 && avgInterval <= 45 && ` |
| 3462 | `cmp >= → >` | `else if (regularity >= 0.8 && avgInterval >= 20 && avgInterval <= 45 && ` |
| 3462 | `bool && → \|\|` | `else if (regularity >= 0.8 && avgInterval >= 20 && avgInterval <= 45 && ` |
| 3462 | `cmp <= → <` | `else if (regularity >= 0.8 && avgInterval >= 20 && avgInterval <= 45 && ` |
| 3462 | `cmp <= → <` | `else if (regularity >= 0.8 && avgInterval >= 20 && avgInterval <= 45 && ` |
| 3462 | `bool && → \|\|` | `else if (regularity >= 0.8 && avgInterval >= 20 && avgInterval <= 45 && ` |
| 3462 | `bool && → \|\|` | `else if (regularity >= 0.8 && avgInterval >= 20 && avgInterval <= 45 && ` |
| 3462 | `num → 0` | `else if (regularity >= 0.8 && avgInterval >= 20 && avgInterval <= 45 && ` |
| 3462 | `num → 0` | `else if (regularity >= 0.8 && avgInterval >= 20 && avgInterval <= 45 && ` |
| 3462 | `num → 0` | `else if (regularity >= 0.8 && avgInterval >= 20 && avgInterval <= 45 && ` |
| 3463 | `cmp >= → >` | `else if (regularity >= 0.7 && avgInterval >= 75 && avgInterval <= 105 &&` |
| 3463 | `cmp >= → >` | `else if (regularity >= 0.7 && avgInterval >= 75 && avgInterval <= 105 &&` |
| 3463 | `bool && → \|\|` | `else if (regularity >= 0.7 && avgInterval >= 75 && avgInterval <= 105 &&` |
| 3463 | `cmp <= → <` | `else if (regularity >= 0.7 && avgInterval >= 75 && avgInterval <= 105 &&` |
| 3463 | `cmp <= → <` | `else if (regularity >= 0.7 && avgInterval >= 75 && avgInterval <= 105 &&` |
| 3463 | `bool && → \|\|` | `else if (regularity >= 0.7 && avgInterval >= 75 && avgInterval <= 105 &&` |
| 3463 | `bool && → \|\|` | `else if (regularity >= 0.7 && avgInterval >= 75 && avgInterval <= 105 &&` |
| 3463 | `num → 0` | `else if (regularity >= 0.7 && avgInterval >= 75 && avgInterval <= 105 &&` |
| 3463 | `num → 0` | `else if (regularity >= 0.7 && avgInterval >= 75 && avgInterval <= 105 &&` |
| 3463 | `num → 0` | `else if (regularity >= 0.7 && avgInterval >= 75 && avgInterval <= 105 &&` |
| 3470 | `num → 0` | `return m ? +m[1] * 3600 + +m[2] * 60 + +m[3] : 0;` |
| 3470 | `num → 0` | `return m ? +m[1] * 3600 + +m[2] * 60 + +m[3] : 0;` |
| 3490 | `cmp < → <=` | `for (var i = 0; i < n; i++) {` |
| 3491 | `cmp < → <=` | `var lo = i - half < 0 ? 0 : i - half,` |
| 3492 | `cmp > → >=` | `hi = i + half > n - 1 ? n - 1 : i + half;` |
| 3507 | `cmp < → <=` | `for (var i = 0; i < spo2.length; i++) {` |
| 3517 | `cmp > → >=` | `if (Math.abs(d) > Math.abs(extreme)) extreme = d;` |
| 3534 | `cmp < → <=` | `for (var i = 1; i < cross.length; i++) halves.push(cross[i] - cross[i - ` |
| 3537 | `cmp < → <=` | `for (var j = 0; j + 1 < halves.length; j += 2) cycles.push(halves[j] + h` |
| 3542 | `cmp < → <=` | `for (var k = 0; k < cycles.length; k++) {` |
| 3544 | `cmp <= → <` | `if (c >= cfg.PB_CYCLE_MIN_SEC && c <= cfg.PB_CYCLE_MAX_SEC) {` |
| 3544 | `cmp >= → >` | `if (c >= cfg.PB_CYCLE_MIN_SEC && c <= cfg.PB_CYCLE_MAX_SEC) {` |
| 3544 | `bool && → \|\|` | `if (c >= cfg.PB_CYCLE_MIN_SEC && c <= cfg.PB_CYCLE_MAX_SEC) {` |
| 3547 | `cmp > → >=` | `if (run > best) {` |
| 3564 | `cmp > → >=` | `if (bestLens.length > 1) {` |
| 3577 | `cmp >= → >` | `var periodic = best >= cfg.PB_MIN_CYCLES && cv !== null && cv < cfg.PB_M` |
| 3577 | `bool && → \|\|` | `var periodic = best >= cfg.PB_MIN_CYCLES && cv !== null && cv < cfg.PB_M` |
| 3577 | `cmp < → <=` | `var periodic = best >= cfg.PB_MIN_CYCLES && cv !== null && cv < cfg.PB_M` |
| 3577 | `eq !== → ===` | `var periodic = best >= cfg.PB_MIN_CYCLES && cv !== null && cv < cfg.PB_M` |
| 3590 | `cmp >= → >` | `if (runStart >= 0 && curLens.length >= cfg.PB_MIN_CYCLES) {` |
| 3601 | `cmp < → <=` | `if (vv < cfg.PB_MAX_CYCLE_CV) {` |
| 3604 | `bool && → \|\|` | `if (sIdx != null && eIdx != null) episodes.push({ startIdx: sIdx, endIdx` |
| 3610 | `cmp < → <=` | `for (var e = 0; e < cycles.length; e++) {` |
| 3611 | `cmp <= → <` | `if (cycles[e] >= cfg.PB_CYCLE_MIN_SEC && cycles[e] <= cfg.PB_CYCLE_MAX_S` |
| 3611 | `cmp >= → >` | `if (cycles[e] >= cfg.PB_CYCLE_MIN_SEC && cycles[e] <= cfg.PB_CYCLE_MAX_S` |
| 3612 | `cmp < → <=` | `if (runStart < 0) runStart = e;` |
| 3650 | `cmp < → <=` | `for (var j = ep.startIdx + 1; j <= ep.endIdx && j < n; j++) {` |
| 3652 | `cmp < → <=` | `if (rows[j].spo2 < CFG.SPO2_OSC_THRESHOLD) below++;` |
| 3656 | `cmp >= → >` | `if (motion / span >= 0.08) continue;` |
| 3713 | `cmp < → <=` | `for (var _i = 0; _i < BAD.length; _i++) if (f.indexOf(BAD[_i]) >= 0) ret` |
| 3716 | `num → 0` | `return _m && parseInt(_m[1], 10) >= 80 ? 'bad' : 'warn';` |
| 3735 | `cmp < → <=` | `for (var _j = 0; _j < WARN.length; _j++) if (f.indexOf(WARN[_j]) >= 0) r` |
| 3740 | `cmp > → >=` | `if (stats.t90pct > 1) f.push({ code: 'T90_ELEVATED', sev: 'bad' });` |
| 3741 | `cmp > → >=` | `if (stats.t95pct > 15) f.push({ code: 'T95_HIGH', sev: 'bad' });` |
| 3743 | `cmp >= → >` | `if (odi4.rate >= 5) f.push({ code: 'ODI4_ABNORMAL', sev: 'bad' });` |
| 3745 | `cmp >= → >` | `if (odi3.rate >= 15) f.push({ code: 'ODI3_ELEVATED', sev: 'warn' });` |
| 3746 | `cmp >= → >` | `if (spikes.length >= 4) f.push({ code: 'HR_SPIKES(' + spikes.length + ')` |
| 3748 | `eq === → !==` | `var sv = period.pattern === 'PLM_CANDIDATE' ? 'warn' : 'info';` |
| 3751 | `cmp >= → >` | `if (osc.episodeCount >= 4) f.push({ code: 'PERIODIC_BREATHING(' + osc.ep` |
| 3752 | `cmp > → >=` | `if (stats.maxHr > 105) f.push({ code: 'MAX_HR(' + stats.maxHr + ')', sev` |
| 3754 | `cmp < → <=` | `if (hrv.pnn3 < 0.2) f.push({ code: 'HRV_LOW_pNN3(' + hrv.pnn3 + '%)', se` |
| 3755 | `cmp > → >=` | `if (hrv.hrSlope > 1.5) f.push({ code: 'HRV_HR_RISING(' + hrv.hrSlope + '` |
| 3756 | `cmp > → >=` | `if (hrv.hrFloor > 65) f.push({ code: 'HRV_FLOOR_HIGH(' + hrv.hrFloor + '` |
| 3760 | `cmp > → >=` | `if (stats.sensorWarmupTrimmed > 0) f.push({ code: 'SENSOR_WARMUP_TRIMMED` |
| 3761 | `cmp > → >=` | `if (stats.sensorCooldownTrimmed > 0) f.push({ code: 'SENSOR_COOLDOWN_TRI` |
| 3762 | `cmp > → >=` | `if (stats.nadirArtifactExcluded > 0) f.push({ code: 'SPO2_NADIR_GATED(' ` |
| 3763 | `cmp >= → >` | `if (motion && motion.arousalIndex >= 40) f.push({ code: 'RESTLESS_NIGHT(` |
| 3764 | `cmp < → <=` | `if (stab && stab.score != null && stab.score < 50) f.push({ code: 'POOR_` |
| 3764 | `num → 0` | `if (stab && stab.score != null && stab.score < 50) f.push({ code: 'POOR_` |
| 3767 | `cmp >= → >` | `if (cross && cross.divergePct >= 75 && osc && osc.episodeCount >= 6) f.p` |
| 3767 | `cmp >= → >` | `if (cross && cross.divergePct >= 75 && osc && osc.episodeCount >= 6) f.p` |
| 3767 | `num → 0` | `if (cross && cross.divergePct >= 75 && osc && osc.episodeCount >= 6) f.p` |
| 3768 | `cmp >= → >` | `if (cross && cross.autoArousalIdx >= 5) f.push({ code: 'HIGH_AROUSAL_IDX` |
| 3769 | `cmp >= → >` | `if (comp && comp.nsi >= 80) f.push({ code: 'NOCTURNAL_STRESS(' + comp.ns` |
| 3770 | `cmp >= → >` | `else if (comp && comp.nsi >= 60) f.push({ code: 'NOCTURNAL_STRESS(' + co` |
| 3772 | `cmp > → >=` | `if (spo2Adv && spo2Adv.wtdsi > 5) f.push({ code: 'WTDSI_ELEVATED(' + spo` |
| 3792 | `cmp < → <=` | `if (rows[i].spo2 < 94) burden += 94 - rows[i].spo2;` |
| 3875 | `cmp > → >=` | `if (++run > best) best = run;` |
| 3877 | `cmp < → <=` | `if (nFinite < MOTION_STUCK_MIN_SAMPLES) return null; // indeterminate — ` |
| 3879 | `cmp >= → >` | `return best >= bar;` |
| 3953 | `cmp >= → >` | `var grade = score >= 80 ? 'Good' : score >= 60 ? 'Fair' : 'Poor';` |
| 3953 | `cmp >= → >` | `var grade = score >= 80 ? 'Good' : score >= 60 ? 'Fair' : 'Poor';` |
| 3953 | `num → 0` | `var grade = score >= 80 ? 'Good' : score >= 60 ? 'Fair' : 'Poor';` |
| 3954 | `cmp >= → >` | `var gradeClass = score >= 80 ? 'good' : score >= 60 ? 'warn' : 'bad';` |
| 3954 | `num → 0` | `var gradeClass = score >= 80 ? 'good' : score >= 60 ? 'warn' : 'bad';` |
| 3954 | `cmp >= → >` | `var gradeClass = score >= 80 ? 'good' : score >= 60 ? 'warn' : 'bad';` |
| 3954 | `num → 0` | `var gradeClass = score >= 80 ? 'good' : score >= 60 ? 'warn' : 'bad';` |
| 3977 | `num → 0` | `WIN_SEC: 10, // ± window (s) around the desat onset at 1 Hz` |
| 3990 | `bool \|\| → &&` | `if (onset == null \|\| !pulseSeries \|\| !pulseSeries.length) return desat;` |
| 3990 | `bool \|\| → &&` | `if (onset == null \|\| !pulseSeries \|\| !pulseSeries.length) return desat;` |
| 3998 | `cmp <= → <` | `for (var i = lo; i <= hi; i++) {` |
| 4001 | `cmp >= → >` | `if (p != null && isFinite(p) && p >= SELFGATE.PULSE_MIN && p <= SELFGATE` |
| 4001 | `cmp <= → <` | `if (p != null && isFinite(p) && p >= SELFGATE.PULSE_MIN && p <= SELFGATE` |
| 4003 | `cmp > → >=` | `var pulseValid = tot > 0 ? valid / tot : 0;` |
| 4006 | `bool && → \|\|` | `if (spo2Series && spo2Series.length) {` |
| 4011 | `cmp > → >=` | `if (d > fallRate) fallRate = d;` |
| 4013 | `bool && → \|\|` | `} else if (desat.depth != null && desat.duration) {` |
| 4018 | `bool && → \|\|` | `if (nadir != null && nadir < N) {` |
| 4018 | `cmp < → <=` | `if (nadir != null && nadir < N) {` |
| 4021 | `cmp >= → >` | `var beforeOk = pBefore != null && isFinite(pBefore) && pBefore >= SELFGA` |
| 4021 | `bool && → \|\|` | `var beforeOk = pBefore != null && isFinite(pBefore) && pBefore >= SELFGA` |
| 4021 | `cmp <= → <` | `var beforeOk = pBefore != null && isFinite(pBefore) && pBefore >= SELFGA` |
| 4021 | `bool && → \|\|` | `var beforeOk = pBefore != null && isFinite(pBefore) && pBefore >= SELFGA` |
| 4021 | `bool && → \|\|` | `var beforeOk = pBefore != null && isFinite(pBefore) && pBefore >= SELFGA` |
| 4022 | `cmp >= → >` | `var atBad = pAt == null \|\| !isFinite(pAt) \|\| pAt < SELFGATE.PULSE_MIN \|\|` |
| 4022 | `cmp > → >=` | `var atBad = pAt == null \|\| !isFinite(pAt) \|\| pAt < SELFGATE.PULSE_MIN \|\|` |
| 4022 | `cmp < → <=` | `var atBad = pAt == null \|\| !isFinite(pAt) \|\| pAt < SELFGATE.PULSE_MIN \|\|` |
| 4022 | `bool \|\| → &&` | `var atBad = pAt == null \|\| !isFinite(pAt) \|\| pAt < SELFGATE.PULSE_MIN \|\|` |
| 4022 | `bool \|\| → &&` | `var atBad = pAt == null \|\| !isFinite(pAt) \|\| pAt < SELFGATE.PULSE_MIN \|\|` |
| 4022 | `bool \|\| → &&` | `var atBad = pAt == null \|\| !isFinite(pAt) \|\| pAt < SELFGATE.PULSE_MIN \|\|` |
| 4022 | `bool \|\| → &&` | `var atBad = pAt == null \|\| !isFinite(pAt) \|\| pAt < SELFGATE.PULSE_MIN \|\|` |
| 4032 | `num → 0` | `desat.sqi = 0.2;` |
| 4065 | `num → 0` | `WIN = 300;` |
| 4069 | `cmp <= → <` | `for (var i = 0; i + 12 <= n; i += 12) {` |
| 4076 | `cmp > → >=` | `deltaIndex = means12.length > 1 ? +(deltaIndex / (means12.length - 1)).t` |
| 4084 | `cmp > → >=` | `var spo2CoV = meanSpo2 > 0 ? +((stdDev(spo2) / meanSpo2) * 100).toFixed(` |
| 4087 | `num → 0` | `var weights = { 95: 1, 94: 2, 93: 3, 92: 4, 91: 5, 90: 6, 89: 8, 88: 10,` |
| 4087 | `num → 0` | `var weights = { 95: 1, 94: 2, 93: 3, 92: 4, 91: 5, 90: 6, 89: 8, 88: 10,` |
| 4087 | `num → 0` | `var weights = { 95: 1, 94: 2, 93: 3, 92: 4, 91: 5, 90: 6, 89: 8, 88: 10,` |
| 4087 | `num → 0` | `var weights = { 95: 1, 94: 2, 93: 3, 92: 4, 91: 5, 90: 6, 89: 8, 88: 10,` |
| 4096 | `cmp < → <=` | `for (var i = 0; i < n; i++) {` |
| 4097 | `cmp < → <=` | `if (spo2[i] < 90) auc90 += 90 - spo2[i];` |
| 4101 | `cmp > → >=` | `var auc90Rate = durationHr > 0 ? +(auc90Total / durationHr).toFixed(2) :` |
| 4111 | `cmp < → <=` | `for (var k = e.endIdx; k < Math.min(e.endIdx + 120, n); k++) {` |
| 4176 | `cmp > → >=` | `var dip3Rate = durationHr > 0 ? +(dip3Count / durationHr).toFixed(1) : 0` |
| 4190 | `bool && → \|\|` | `e.tMs = rN && rN.tMs != null ? rN.tMs : null; // the nadir IS the event'` |
| 4191 | `bool && → \|\|` | `e.startTMs = r0 && r0.tMs != null ? r0.tMs : null;` |
| 4192 | `bool && → \|\|` | `e.endTMs = rE && rE.tMs != null ? rE.tMs : null;` |
| 4246 | `cmp <= → <` | `if (rows[0].spo2 <= CFG.NADIR_RAMP_START_MAX) {` |
| 4249 | `cmp < → <=` | `while (k < lim && rows[k].spo2 < CFG.NADIR_RAMP_RECOVER) k++;` |
| 4249 | `cmp < → <=` | `while (k < lim && rows[k].spo2 < CFG.NADIR_RAMP_RECOVER) k++;` |
| 4250 | `cmp > → >=` | `if (k > 0 && k < lim) {` |
| 4250 | `cmp < → <=` | `if (k > 0 && k < lim) {` |
| 4250 | `bool && → \|\|` | `if (k > 0 && k < lim) {` |
| 4253 | `cmp < → <=` | `for (var j = 1; j < k; j++) if (rows[j].spo2 < openMin) openMin = rows[j` |
| 4253 | `cmp < → <=` | `for (var j = 1; j < k; j++) if (rows[j].spo2 < openMin) openMin = rows[j` |
| 4254 | `cmp <= → <` | `if (rows[0].spo2 <= openMin + 1) {` |
| 4255 | `cmp < → <=` | `for (i = 0; i < k; i++) {` |
| 4267 | `cmp < → <=` | `if (rows[i].spo2 < mn) mn = rows[i].spo2;` |
| 4279 | `cmp < → <=` | `if (n < 120) return null;` |
| 4292 | `cmp <= → <` | `for (var i = 0; i + WIN60 <= n; i += 60) {` |
| 4296 | `cmp > → >=` | `if (rollingMeans.length > 1) {` |
| 4300 | `cmp < → <=` | `for (var i = 0; i < rollingMeans.length; i++) {` |
| 4302 | `cmp < → <=` | `if (pastPeak && rollingMeans[i] < minAfterPeak) minAfterPeak = rollingMe` |
| 4314 | `cmp >= → >` | `if (subN >= 20) {` |
| 4314 | `num → 0` | `if (subN >= 20) {` |
| 4320 | `cmp <= → <` | `if (N <= 0) return 0;` |
| 4327 | `cmp <= → <` | `if (maxDiff <= r) ci++;` |
| 4329 | `cmp > → >=` | `if (ci > 0) logSum += Math.log(ci / N);` |
| 4340 | `cmp < → <=` | `if (r.hr < 40) {` |
| 4340 | `num → 0` | `if (r.hr < 40) {` |
| 4342 | `eq === → !==` | `if (bradyRun === 10) bradyCount++;` |
| 4342 | `num → 0` | `if (bradyRun === 10) bradyCount++;` |
| 4350 | `cmp > → >=` | `if (r.hr > 100) {` |
| 4372 | `cmp <= → <` | `for (var i = 1800; i + WIN5 <= n; i += WIN5) {` |
| 4377 | `cmp > → >=` | `if (mc / WIN5 > 0.05) wasoWindows++;` |
| 4380 | `cmp > → >=` | `var wasoPct = totalPostOnset > 0 ? +((wasoWindows / totalPostOnset) * 10` |
| 4388 | `num → 0` | `if (shiftRun === 61) posShifts++;` |
| 4400 | `cmp > → >=` | `var autoArousalIdx = durationHr > 0 ? +((spikes.length + odi4.count) / d` |
| 4445 | `cmp > → >=` | `crcIdx = ds > 0 && dh > 0 ? +(num / Math.sqrt(ds * dh)).toFixed(3) : nul` |
| 4445 | `bool && → \|\|` | `crcIdx = ds > 0 && dh > 0 ? +(num / Math.sqrt(ds * dh)).toFixed(3) : nul` |
| 4445 | `cmp > → >=` | `crcIdx = ds > 0 && dh > 0 ? +(num / Math.sqrt(ds * dh)).toFixed(3) : nul` |
| 4450 | `cmp > → >=` | `if (osc && osc.episodeCount > 0) {` |
| 4476 | `num → 0` | `WIN = 300;` |
| 4477 | `cmp < → <=` | `if (n < 60) return null;` |
| 4477 | `num → 0` | `if (n < 60) return null;` |
| 4494 | `cmp > → >=` | `wtdsi = n > 0 ? +(wtdsi / n).toFixed(3) : 0;` |
| 4509 | `cmp > → >=` | `belowSamples.length > 0` |
| 4516 | `cmp > → >=` | `var condPct = n > 0 ? +((belowSamples.length / n) * 100).toFixed(1) : 0;` |
| 4531 | `num → 0` | `else if (nad >= 85) bins.b85_87++;` |
| 4531 | `cmp >= → >` | `else if (nad >= 85) bins.b85_87++;` |
| 4543 | `cmp < → <=` | `if (clean.length < 60) return null;` |
| 4557 | `cmp > → >=` | `var rmssd = pairs > 0 ? +Math.sqrt(ssds / pairs).toFixed(2) : 0;` |
| 4588 | `cmp >= → >` | `above = spo2Vals[0] >= CFG.SPO2_OSC_THRESHOLD;` |
| 4589 | `cmp < → <=` | `for (var j = 1; j < spo2Vals.length; j++) {` |
| 4613 | `num → 0` | `var WIN = 300;` |
| 4618 | `cmp > → >=` | `if (nadirEvents > 0 && spikes.length > 0) {` |
| 4631 | `cmp > → >=` | `var sfi = durationHr > 0 ? +((wasoWin + spikes.length) / durationHr).toF` |
| 4662 | `cmp < → <=` | `if (n < 2) return { slope: 0, r2: 0, intercept: 0 };` |
| 4674 | `bool \|\| → &&` | `if (denom === 0) return { slope: 0, r2: 0, intercept: ys[0] \|\| 0 };` |
| 4676 | `cmp > → >=` | `var intercept = n > 0 ? (sy - slope * sx) / n : 0;` |
| 4679 | `cmp > → >=` | `yMean = n > 0 ? sy / n : 0;` |
| 4694 | `cmp < → <=` | `if (n < 600) return null;` |
| 4694 | `num → 0` | `if (n < 600) return null;` |
| 4705 | `cmp < → <=` | `if (windows.length < 3) return null;` |
| 4708 | `cmp < → <=` | `return { driftSlope: lr.slope, driftPerHr: driftPerHr, driftR2: lr.r2, d` |
| 4708 | `cmp > → >=` | `return { driftSlope: lr.slope, driftPerHr: driftPerHr, driftR2: lr.r2, d` |
| 4714 | `cmp < → <=` | `if (n < 120) return null;` |
| 4729 | `cmp <= → <` | `if (winLen <= 0) continue;` |
| 4734 | `cmp > → >=` | `return { odi2Count: count, odi2Rate: durationHr > 0 ? +(count / duration` |
| 4739 | `cmp < → <=` | `if (!desat \|\| !desat.nadir \|\| !desat.nadir.count \|\| desat.nadir.count < ` |
| 4739 | `bool \|\| → &&` | `if (!desat \|\| !desat.nadir \|\| !desat.nadir.count \|\| desat.nadir.count < ` |
| 4745 | `cmp < → <=` | `for (var i = 0; i < n; i++) {` |
| 4754 | `cmp >= → >` | `if (i < 60 \|\| i >= n - 120) continue;` |
| 4754 | `num → 0` | `if (i < 60 \|\| i >= n - 120) continue;` |
| 4754 | `cmp < → <=` | `if (i < 60 \|\| i >= n - 120) continue;` |
| 4755 | `cmp < → <=` | `if (winLen < 30) continue;` |
| 4755 | `num → 0` | `if (winLen < 30) continue;` |
| 4759 | `cmp >= → >` | `if (prev < local - 1 && curr >= local - 1) {` |
| 4761 | `cmp < → <=` | `if (postSeg.length < 30) continue;` |
| 4761 | `num → 0` | `if (postSeg.length < 30) continue;` |
| 4767 | `cmp > → >=` | `if (os > 0) overshoots.push(os);` |
| 4776 | `cmp > → >=` | `return { overshootMean: mean, overshootCount: overshoots.length, oversho` |
| 4776 | `cmp > → >=` | `return { overshootMean: mean, overshootCount: overshoots.length, oversho` |
| 4811 | `cmp < → <=` | `if (n < winSec) return out;` |
| 4833 | `cmp < → <=` | `if (n < 300) return null;` |
| 4852 | `cmp > → >=` | `ac1Label: ac1 > 0.95 ? 'Sustained (hypoventilation)' : ac1 > 0.85 ? 'Per` |
| 4852 | `cmp > → >=` | `ac1Label: ac1 > 0.95 ? 'Sustained (hypoventilation)' : ac1 > 0.85 ? 'Per` |
| 4852 | `cmp < → <=` | `ac1Label: ac1 > 0.95 ? 'Sustained (hypoventilation)' : ac1 > 0.85 ? 'Per` |
| 4852 | `num → 0` | `ac1Label: ac1 > 0.95 ? 'Sustained (hypoventilation)' : ac1 > 0.85 ? 'Per` |
| 4852 | `num → 0` | `ac1Label: ac1 > 0.95 ? 'Sustained (hypoventilation)' : ac1 > 0.85 ? 'Per` |
| 4852 | `num → 0` | `ac1Label: ac1 > 0.95 ? 'Sustained (hypoventilation)' : ac1 > 0.85 ? 'Per` |
| 4900 | `cmp > → >=` | `hrLfHfLabel: lfhf === null ? 'N/A' : lfhf > 4 ? 'SNS dominant' : lfhf > ` |
| 4900 | `cmp > → >=` | `hrLfHfLabel: lfhf === null ? 'N/A' : lfhf > 4 ? 'SNS dominant' : lfhf > ` |
| 4907 | `cmp < → <=` | `if (n < 600) return null;` |
| 4907 | `num → 0` | `if (n < 600) return null;` |
| 4936 | `cmp > → >=` | `var lfhf = hfPow > 0 ? +(lfPow / hfPow).toFixed(2) : null;` |
| 4937 | `cmp > → >=` | `return { hrLfPow: lfPow, hrHfPow: hfPow, hrLfHf: lfhf, hrLfHfLabel: lfhf` |
| 4937 | `cmp > → >=` | `return { hrLfPow: lfPow, hrHfPow: hfPow, hrLfHf: lfhf, hrLfHfLabel: lfhf` |
| 4937 | `eq === → !==` | `return { hrLfPow: lfPow, hrHfPow: hfPow, hrLfHf: lfhf, hrLfHfLabel: lfhf` |
| 4974 | `cmp > → >=` | `respRateLabel: bpm < 10 ? 'Slow (<10)' : bpm > 20 ? 'Fast (>20)' : 'Norm` |
| 4974 | `cmp < → <=` | `respRateLabel: bpm < 10 ? 'Slow (<10)' : bpm > 20 ? 'Fast (>20)' : 'Norm` |
| 4981 | `cmp < → <=` | `if (n < 600) return null;` |
| 4981 | `num → 0` | `if (n < 600) return null;` |
| 4994 | `num → 0` | `bestFreq = 0.2;` |
| 4995 | `cmp < → <=` | `for (var k = 0; k < 20; k++) {` |
| 5005 | `cmp > → >=` | `if (p > bestPow) {` |
| 5015 | `num → 0` | `respRateLabel: bpm < 10 ? 'Slow (<10)' : bpm > 20 ? 'Fast (>20)' : 'Norm` |
| 5015 | `cmp > → >=` | `respRateLabel: bpm < 10 ? 'Slow (<10)' : bpm > 20 ? 'Fast (>20)' : 'Norm` |
| 5015 | `cmp < → <=` | `respRateLabel: bpm < 10 ? 'Slow (<10)' : bpm > 20 ? 'Fast (>20)' : 'Norm` |
| 5015 | `num → 0` | `respRateLabel: bpm < 10 ? 'Slow (<10)' : bpm > 20 ? 'Fast (>20)' : 'Norm` |
| 5022 | `num → 0` | `if (n < 120) return null;` |
| 5022 | `cmp < → <=` | `if (n < 120) return null;` |
| 5030 | `bool \|\| → &&` | `if (!ups.length \|\| !downs.length) return null;` |
| 5045 | `cmp > → >=` | `hrAsymLabel: asym > 1.15 ? 'Arousal-biased' : asym < 0.85 ? 'Vagally dom` |
| 5045 | `cmp < → <=` | `hrAsymLabel: asym > 1.15 ? 'Arousal-biased' : asym < 0.85 ? 'Vagally dom` |
| 5045 | `num → 0` | `hrAsymLabel: asym > 1.15 ? 'Arousal-biased' : asym < 0.85 ? 'Vagally dom` |
| 5052 | `cmp < → <=` | `if (n < 1200) return null;` |
| 5063 | `cmp >= → >` | `if (qs.indexOf(null) >= 0) return null;` |
| 5066 | `bool && → \|\|` | `var arc = qs && qs.length >= 4 ? +(q3hr - q1hr).toFixed(1) : 0;` |
| 5068 | `bool && → \|\|` | `hrQ1: qs && qs.length >= 4 ? qs[0] : null,` |
| 5069 | `bool && → \|\|` | `hrQ2: qs && qs.length >= 4 ? qs[1] : null,` |
| 5070 | `bool && → \|\|` | `hrQ3: qs && qs.length >= 4 ? qs[2] : null,` |
| 5071 | `bool && → \|\|` | `hrQ4: qs && qs.length >= 4 ? qs[3] : null,` |
| 5073 | `cmp > → >=` | `remReemergence: /** @type {number} */ (qs[3]) > /** @type {number} */ (q` |
| 5074 | `cmp > → >=` | `hrArcLabel: arc < -3 ? 'Good (declining arc)' : arc > 3 ? 'Rising (arous` |
| 5074 | `cmp < → <=` | `hrArcLabel: arc < -3 ? 'Good (declining arc)' : arc > 3 ? 'Rising (arous` |
| 5111 | `cmp < → <=` | `crossCorrLabel: lag < 10 ? 'Near-zero lag (central pattern)' : lag < 30 ` |
| 5111 | `cmp < → <=` | `crossCorrLabel: lag < 10 ? 'Near-zero lag (central pattern)' : lag < 30 ` |
| 5111 | `num → 0` | `crossCorrLabel: lag < 10 ? 'Near-zero lag (central pattern)' : lag < 30 ` |
| 5111 | `num → 0` | `crossCorrLabel: lag < 10 ? 'Near-zero lag (central pattern)' : lag < 30 ` |
| 5118 | `cmp < → <=` | `if (n < 600) return null;` |
| 5118 | `num → 0` | `if (n < 600) return null;` |
| 5143 | `cmp <= → <` | `for (var lag = 0; lag <= 120; lag++) {` |
| 5152 | `cmp > → >=` | `var cor = d1 > 0 && d2 > 0 ? num / Math.sqrt(d1 * d2) : 0;` |
| 5152 | `cmp > → >=` | `var cor = d1 > 0 && d2 > 0 ? num / Math.sqrt(d1 * d2) : 0;` |
| 5152 | `bool && → \|\|` | `var cor = d1 > 0 && d2 > 0 ? num / Math.sqrt(d1 * d2) : 0;` |
| 5153 | `cmp > → >=` | `if (cor > bestCor) {` |
| 5161 | `cmp < → <=` | `crossCorrLabel: bestLag < 10 ? 'Near-zero lag (central pattern)' : bestL` |
| 5161 | `cmp < → <=` | `crossCorrLabel: bestLag < 10 ? 'Near-zero lag (central pattern)' : bestL` |
| 5161 | `num → 0` | `crossCorrLabel: bestLag < 10 ? 'Near-zero lag (central pattern)' : bestL` |
| 5161 | `num → 0` | `crossCorrLabel: bestLag < 10 ? 'Near-zero lag (central pattern)' : bestL` |
| 5170 | `bool \|\| → &&` | `if (!sp \|\| sp.baseline == null \|\| sp.peak == null) return;` |
| 5170 | `bool \|\| → &&` | `if (!sp \|\| sp.baseline == null \|\| sp.peak == null) return;` |
| 5170 | `negate: drop !` | `if (!sp \|\| sp.baseline == null \|\| sp.peak == null) return;` |
| 5174 | `cmp > → >=` | `var startHr0 = rows.length > 0 ? rows[0].t.getUTCHours() * 60 + rows[0].` |
| 5174 | `num → 0` | `var startHr0 = rows.length > 0 ? rows[0].t.getUTCHours() * 60 + rows[0].` |
| 5174 | `num → 0` | `var startHr0 = rows.length > 0 ? rows[0].t.getUTCHours() * 60 + rows[0].` |
| 5175 | `bool \|\| → &&` | `var apxIdx = Math.round(((sp.mfm \|\| 0) - startHr0) * 60);` |
| 5176 | `num → 0` | `var s0 = Math.max(0, apxIdx - 300),` |
| 5177 | `num → 0` | `s1 = Math.min(rows.length, apxIdx + 300);` |
| 5178 | `cmp < → <=` | `for (var i = s0; i < s1; i++) {` |
| 5179 | `cmp >= → >` | `if (rows[i].hr >= sp.peak - 2 && rows[i].hr >= threshold) {` |
| 5179 | `cmp >= → >` | `if (rows[i].hr >= sp.peak - 2 && rows[i].hr >= threshold) {` |
| 5179 | `bool && → \|\|` | `if (rows[i].hr >= sp.peak - 2 && rows[i].hr >= threshold) {` |
| 5184 | `cmp < → <=` | `if (peakIdx < 0) return;` |
| 5185 | `cmp < → <=` | `for (var j = peakIdx + 1; j < Math.min(rows.length, peakIdx + 300); j++)` |
| 5185 | `num → 0` | `for (var j = peakIdx + 1; j < Math.min(rows.length, peakIdx + 300); j++)` |
| 5186 | `cmp <= → <` | `if (rows[j].hr <= threshold) {` |
| 5198 | `cmp > → >=` | `return { spikeDecayMeanS: mean, spikeDecayCount: decays.length, spikeDec` |
| 5198 | `num → 0` | `return { spikeDecayMeanS: mean, spikeDecayCount: decays.length, spikeDec` |
| 5198 | `num → 0` | `return { spikeDecayMeanS: mean, spikeDecayCount: decays.length, spikeDec` |
| 5198 | `cmp > → >=` | `return { spikeDecayMeanS: mean, spikeDecayCount: decays.length, spikeDec` |
| 5206 | `bool \|\| → &&` | `if (!sp \|\| sp.baseline == null \|\| sp.peak == null) return;` |
| 5206 | `negate: drop !` | `if (!sp \|\| sp.baseline == null \|\| sp.peak == null) return;` |
| 5206 | `bool \|\| → &&` | `if (!sp \|\| sp.baseline == null \|\| sp.peak == null) return;` |
| 5209 | `num → 0` | `var startHr1 = rows.length > 0 ? rows[0].t.getUTCHours() * 60 + rows[0].` |
| 5209 | `cmp > → >=` | `var startHr1 = rows.length > 0 ? rows[0].t.getUTCHours() * 60 + rows[0].` |
| 5209 | `num → 0` | `var startHr1 = rows.length > 0 ? rows[0].t.getUTCHours() * 60 + rows[0].` |
| 5210 | `bool \|\| → &&` | `var apxIdx1 = Math.round(((sp.mfm \|\| 0) - startHr1) * 60);` |
| 5211 | `num → 0` | `var u0 = Math.max(0, apxIdx1 - 300),` |
| 5212 | `num → 0` | `u1 = Math.min(rows.length, apxIdx1 + 300);` |
| 5213 | `cmp < → <=` | `for (var i = u0; i < u1; i++) {` |
| 5214 | `cmp >= → >` | `if (rows[i].hr >= sp.peak - 2) {` |
| 5219 | `cmp < → <=` | `if (peakIdx < 0) return;` |
| 5221 | `num → 0` | `for (var j = peakIdx + 1; j < Math.min(rows.length, peakIdx + 200); j++)` |
| 5221 | `cmp < → <=` | `for (var j = peakIdx + 1; j < Math.min(rows.length, peakIdx + 200); j++)` |
| 5222 | `cmp <= → <` | `if (rows[j].hr <= sp.baseline + 1) {` |
| 5227 | `num → 0` | `var postSeg = rows.slice(recIdx + 60, Math.min(rows.length, recIdx + 120` |
| 5227 | `num → 0` | `var postSeg = rows.slice(recIdx + 60, Math.min(rows.length, recIdx + 120` |
| 5228 | `cmp < → <=` | `if (postSeg.length < 20) return;` |
| 5228 | `num → 0` | `if (postSeg.length < 20) return;` |
| 5234 | `cmp > → >=` | `if (us > 0) undershoots.push(us);` |
| 5242 | `cmp > → >=` | `return { spikeUndershootMean: mean, spikeUndershootCount: undershoots.le` |
| 5242 | `cmp > → >=` | `return { spikeUndershootMean: mean, spikeUndershootCount: undershoots.le` |
| 5250 | `bool \|\| → &&` | `if (!sp \|\| sp.baseline == null \|\| sp.peak == null \|\| !sp.duration) retur` |
| 5250 | `bool \|\| → &&` | `if (!sp \|\| sp.baseline == null \|\| sp.peak == null \|\| !sp.duration) retur` |
| 5250 | `bool \|\| → &&` | `if (!sp \|\| sp.baseline == null \|\| sp.peak == null \|\| !sp.duration) retur` |
| 5254 | `cmp > → >=` | `if (rise > 0) rates.push(+(rise / 12).toFixed(2)); // bpm/s over ~12s de` |
| 5262 | `cmp > → >=` | `return { spikeRiseRate: mean, spikeRiseLabel: mean > 5 ? 'Abrupt (>5 bpm` |
| 5262 | `cmp > → >=` | `return { spikeRiseRate: mean, spikeRiseLabel: mean > 5 ? 'Abrupt (>5 bpm` |
| 5273 | `cmp < → <=` | `if (n < 2) return null;` |
| 5277 | `bool && → \|\|` | `var dt = rows[i].t != null && rows[i - 1].t != null ? (rows[i].t - rows[` |
| 5278 | `cmp > → >=` | `if (dt > GAP_STEP_SEC) {` |
| 5280 | `cmp > → >=` | `if (dt > maxGap) maxGap = dt;` |
| 5294 | `bool && → \|\|` | `var _spanSec = rows[n - 1].t != null && rows[0].t != null ? (rows[n - 1]` |
| 5300 | `cmp > → >=` | `gapLabel: maxGap > 120 ? 'Significant gap (>2min)' : maxGap > 10 ? 'Mino` |
| 5300 | `cmp > → >=` | `gapLabel: maxGap > 120 ? 'Significant gap (>2min)' : maxGap > 10 ? 'Mino` |
| 5300 | `num → 0` | `gapLabel: maxGap > 120 ? 'Significant gap (>2min)' : maxGap > 10 ? 'Mino` |
| 5300 | `num → 0` | `gapLabel: maxGap > 120 ? 'Significant gap (>2min)' : maxGap > 10 ? 'Mino` |
| 5313 | `cmp < → <=` | `if (!rows \|\| rows.length < 2) return null;` |
| 5313 | `bool \|\| → &&` | `if (!rows \|\| rows.length < 2) return null;` |
| 5317 | `cmp < → <=` | `for (var i = 0; i < rows.length; i++) {` |
| 5319 | `bool \|\| → &&` | `if (t == null \|\| !isFinite(t)) continue;` |
| 5321 | `cmp > → >=` | `else if ((t - prev) / 1000 > GAP_STEP_SEC) {` |
| 5327 | `bool && → \|\|` | `if (segStart !== null && prev !== null) segs.push([segStart, prev]);` |
| 5333 | `bool && → \|\|` | `return typeof DexExport !== 'undefined' && DexExport && DexExport.covera` |
| 5333 | `bool && → \|\|` | `return typeof DexExport !== 'undefined' && DexExport && DexExport.covera` |
| 5339 | `cmp < → <=` | `if (n < 60) return null;` |
| 5349 | `cmp > → >=` | `if (run > maxFlat) maxFlat = run;` |
| 5354 | `cmp >= → >` | `if (run >= 30) {` |
| 5356 | `cmp > → >=` | `if (run > maxFlat) maxFlat = run;` |
| 5358 | `cmp > → >=` | `return { flatlineCount: flatCount, maxFlatlineSec: maxFlat, flatlineLabe` |
| 5358 | `cmp > → >=` | `return { flatlineCount: flatCount, maxFlatlineSec: maxFlat, flatlineLabe` |
| 5364 | `cmp < → <=` | `if (n < 60) return null;` |
| 5375 | `cmp > → >=` | `if (run > maxCeil) maxCeil = run;` |
| 5381 | `cmp >= → >` | `if (run >= 5) {` |
| 5383 | `cmp > → >=` | `if (run > maxCeil) maxCeil = run;` |
| 5391 | `cmp > → >=` | `ceilingLabel: ceilingRuns > 3 ? 'Sensor lift likely' : ceilingRuns > 0 ?` |
| 5407 | `cmp > → >=` | `return { odri: odri, odriLabel: odri > 0.85 ? 'High (CS/PB pattern)' : o` |
| 5407 | `cmp > → >=` | `return { odri: odri, odriLabel: odri > 0.85 ? 'High (CS/PB pattern)' : o` |
| 5413 | `cmp < → <=` | `if (n < 60) return null;` |
| 5431 | `cmp < → <=` | `if (n < 60) return null;` |
| 5462 | `num → 0` | `spo2SkewLabel: skew < -0.5 ? 'Left-skewed (hypox burden)' : skew > 0.5 ?` |
| 5462 | `cmp > → >=` | `spo2SkewLabel: skew < -0.5 ? 'Left-skewed (hypox burden)' : skew > 0.5 ?` |
| 5462 | `cmp < → <=` | `spo2SkewLabel: skew < -0.5 ? 'Left-skewed (hypox burden)' : skew > 0.5 ?` |
| 5463 | `cmp > → >=` | `spo2KurtLabel: excessKurt > 2 ? 'Leptokurtic (heavy tails/events)' : exc` |
| 5463 | `cmp < → <=` | `spo2KurtLabel: excessKurt > 2 ? 'Leptokurtic (heavy tails/events)' : exc` |
| 5470 | `cmp < → <=` | `if (n < 60) return null;` |
| 5484 | `cmp <= → <` | `if (!mean \|\| mean <= 0) return null;` |
| 5484 | `bool \|\| → &&` | `if (!mean \|\| mean <= 0) return null;` |
| 5486 | `cmp > → >=` | `return { hrCV: cv, hrCVmean: +mean.toFixed(1), hrCVsd: +sd.toFixed(2), h` |
| 5486 | `cmp > → >=` | `return { hrCV: cv, hrCVmean: +mean.toFixed(1), hrCVsd: +sd.toFixed(2), h` |
| 5494 | `cmp < → <=` | `if (n < 60) return null;` |
| 5513 | `cmp < → <=` | `if (v < 94) hd94 += 94 - v;` |
| 5514 | `cmp < → <=` | `if (v < 90) hd90 += 90 - v;` |
| 5515 | `cmp < → <=` | `if (v < 88) hd88 += 88 - v;` |
| 5516 | `cmp > → >=` | `if (winLen > 0) {` |
| 5526 | `cmp > → >=` | `hd94PerHr: durationHr > 0 ? +(hd94 / durationHr).toFixed(1) : 0,` |
| 5528 | `cmp > → >=` | `hd94Label: (durationHr > 0 ? hd94 / durationHr : 0) > 200 ? 'High (>200/` |
| 5528 | `cmp > → >=` | `hd94Label: (durationHr > 0 ? hd94 / durationHr : 0) > 200 ? 'High (>200/` |
| 5528 | `cmp > → >=` | `hd94Label: (durationHr > 0 ? hd94 / durationHr : 0) > 200 ? 'High (>200/` |
| 5528 | `cmp > → >=` | `hd94Label: (durationHr > 0 ? hd94 / durationHr : 0) > 200 ? 'High (>200/` |
| 5535 | `cmp < → <=` | `if (n < 60) return null;` |
| 5540 | `cmp < → <=` | `return r.spo2 < 85;` |
| 5540 | `num → 0` | `return r.spo2 < 85;` |
| 5542 | `num → 0` | `var durationHr = n / 3600;` |
| 5549 | `num → 0` | `t85Pct: +((t85 / n) * 100).toFixed(2),` |
| 5550 | `cmp > → >=` | `t88Label: (t88 / n) * 100 > 1 ? 'Severe hypoxemia (>1%)' : (t88 / n) * 1` |
| 5550 | `num → 0` | `t88Label: (t88 / n) * 100 > 1 ? 'Severe hypoxemia (>1%)' : (t88 / n) * 1` |
| 5557 | `cmp < → <=` | `if (n < 60) return null;` |
| 5580 | `cmp < → <=` | `lcspLabel: lcspMin < 20 ? 'Severely fragmented (<20min)' : lcspMin < 45 ` |
| 5580 | `num → 0` | `lcspLabel: lcspMin < 20 ? 'Severely fragmented (<20min)' : lcspMin < 45 ` |
| 5580 | `cmp < → <=` | `lcspLabel: lcspMin < 20 ? 'Severely fragmented (<20min)' : lcspMin < 45 ` |
| 5580 | `cmp < → <=` | `lcspLabel: lcspMin < 20 ? 'Severely fragmented (<20min)' : lcspMin < 45 ` |
| 5588 | `cmp < → <=` | `if (n < 120) return null;` |
| 5597 | `cmp < → <=` | `if (m < 60) return null;` |
| 5597 | `num → 0` | `if (m < 60) return null;` |
| 5616 | `cmp > → >=` | `var ratio = sd2 > 0 ? +(sd1 / sd2).toFixed(3) : null;` |
| 5622 | `cmp > → >=` | `sd1Label: sd1 < 0.4 ? 'Low (1Hz proxy)' : sd1 > 1.0 ? 'High (1Hz proxy)'` |
| 5623 | `cmp < → <=` | `sd2Label: sd2 < 2 ? 'Low sympathetic modulation (1Hz proxy)' : 'Normal (` |
| 5633 | `cmp <= → <` | `if (!ev \|\| ev.depth == null \|\| ev.depth <= 0) return;` |
| 5633 | `bool \|\| → &&` | `if (!ev \|\| ev.depth == null \|\| ev.depth <= 0) return;` |
| 5633 | `bool \|\| → &&` | `if (!ev \|\| ev.depth == null \|\| ev.depth <= 0) return;` |
| 5636 | `cmp >= → >` | `if (nadirIdx < 0 \|\| nadirIdx >= rows.length) return;` |
| 5636 | `bool \|\| → &&` | `if (nadirIdx < 0 \|\| nadirIdx >= rows.length) return;` |
| 5636 | `cmp < → <=` | `if (nadirIdx < 0 \|\| nadirIdx >= rows.length) return;` |
| 5645 | `cmp > → >=` | `var preHR = preCnt > 0 ? preSum / preCnt : rows[Math.max(0, nadirIdx - 1` |
| 5668 | `cmp < → <=` | `return { o2hrEff: mean, o2hrEffMin: min_, o2hrEffMax: max_, o2hrEffN: ra` |
| 5668 | `cmp < → <=` | `return { o2hrEff: mean, o2hrEffMin: min_, o2hrEffMax: max_, o2hrEffN: ra` |
| 5674 | `cmp < → <=` | `if (n < 60) return null;` |
| 5702 | `eq !== → ===` | `delta !== null && delta > 1 ? 'Motion artifact likely (SpO2 lower during` |
| 5702 | `cmp < → <=` | `delta !== null && delta > 1 ? 'Motion artifact likely (SpO2 lower during` |
| 5702 | `cmp > → >=` | `delta !== null && delta > 1 ? 'Motion artifact likely (SpO2 lower during` |
| 5702 | `eq !== → ===` | `delta !== null && delta > 1 ? 'Motion artifact likely (SpO2 lower during` |
| 5712 | `bool && → \|\|` | `return ev && ev.nadir != null;` |
| 5719 | `cmp > → >=` | `var dir = lr.slope < -0.005 ? 'Worsening (REM-load)' : lr.slope > 0.005 ` |
| 5719 | `cmp < → <=` | `var dir = lr.slope < -0.005 ? 'Worsening (REM-load)' : lr.slope > 0.005 ` |
| 5719 | `num → 0` | `var dir = lr.slope < -0.005 ? 'Worsening (REM-load)' : lr.slope > 0.005 ` |
| 5719 | `num → 0` | `var dir = lr.slope < -0.005 ? 'Worsening (REM-load)' : lr.slope > 0.005 ` |
| 5725 | `cmp < → <=` | `if (!desat \|\| !desat.events \|\| desat.events.length < 3) return null;` |
| 5727 | `bool && → \|\|` | `return ev && ev.startIdx != null;` |
| 5729 | `cmp < → <=` | `if (events.length < 3) return null;` |
| 5733 | `cmp > → >=` | `if (gap > 0) intervals.push(gap);` |
| 5745 | `cmp > → >=` | `var cv = mean > 0 ? +(sd / mean).toFixed(2) : null;` |
| 5750 | `cmp < → <=` | `ieiLabel: cv !== null && cv < 0.3 ? 'Regular (PB/CS pattern)' : cv !== n` |
| 5750 | `cmp > → >=` | `ieiLabel: cv !== null && cv < 0.3 ? 'Regular (PB/CS pattern)' : cv !== n` |
| 5750 | `bool && → \|\|` | `ieiLabel: cv !== null && cv < 0.3 ? 'Regular (PB/CS pattern)' : cv !== n` |
| 5750 | `num → 0` | `ieiLabel: cv !== null && cv < 0.3 ? 'Regular (PB/CS pattern)' : cv !== n` |
| 5783 | `cmp > → >=` | `recovSlopeCVlabel: cv > 0.6 ? 'High variability (inconsistent arousal)' ` |
| 5783 | `cmp > → >=` | `recovSlopeCVlabel: cv > 0.6 ? 'High variability (inconsistent arousal)' ` |
| 5799 | `cmp < → <=` | `for (var j = 0; j <= 2 * WIN && j < n; j++) {` |
| 5799 | `cmp <= → <` | `for (var j = 0; j <= 2 * WIN && j < n; j++) {` |
| 5803 | `cmp < → <=` | `for (var i = WIN; i < n - WIN; i++) {` |
| 5804 | `cmp > → >=` | `var m = wCnt > 0 ? wSum / wCnt : rows[i].hr;` |
| 5825 | `cmp < → <=` | `hrNadirLabel: nadirFracOfNight < 0.25 ? 'Early nadir (good alignment)' :` |
| 5825 | `cmp > → >=` | `hrNadirLabel: nadirFracOfNight < 0.25 ? 'Early nadir (good alignment)' :` |
| 5825 | `num → 0` | `hrNadirLabel: nadirFracOfNight < 0.25 ? 'Early nadir (good alignment)' :` |
| 5832 | `cmp < → <=` | `if (n < 600 \|\| !desat \|\| !desat.events \|\| !desat.events.length) return n` |
| 5832 | `num → 0` | `if (n < 600 \|\| !desat \|\| !desat.events \|\| !desat.events.length) return n` |
| 5837 | `num → 0` | `return (a.nadir \|\| 99) < (b.nadir \|\| 99) ? a : b;` |
| 5837 | `num → 0` | `return (a.nadir \|\| 99) < (b.nadir \|\| 99) ? a : b;` |
| 5839 | `bool \|\| → &&` | `if (!worst \|\| worst.nadirIdx == null) return null;` |
| 5846 | `cmp > → >=` | `spo2NadirLabel: frac > 0.65 ? 'Late-night worst (REM-predominant)' : fra` |
| 5846 | `cmp < → <=` | `spo2NadirLabel: frac > 0.65 ? 'Late-night worst (REM-predominant)' : fra` |
| 5853 | `cmp < → <=` | `if (n < 1800) return null;` |
| 5853 | `num → 0` | `if (n < 1800) return null;` |
| 5860 | `cmp < → <=` | `if (seg.length < 60) {` |
| 5860 | `num → 0` | `if (seg.length < 60) {` |
| 5886 | `cmp < → <=` | `if (valid.length < 2) return null;` |
| 5914 | `cmp > → >=` | `rmssdArcLabel: lr.slope < -0.2 ? 'Declining (REM/arousal load)' : lr.slo` |
| 5914 | `num → 0` | `rmssdArcLabel: lr.slope < -0.2 ? 'Declining (REM/arousal load)' : lr.slo` |
| 5914 | `cmp < → <=` | `rmssdArcLabel: lr.slope < -0.2 ? 'Declining (REM/arousal load)' : lr.slo` |
| 5923 | `bool \|\| → &&` | `if (!sp \|\| sp.baseline == null \|\| sp.peak == null) return;` |
| 5923 | `bool \|\| → &&` | `if (!sp \|\| sp.baseline == null \|\| sp.peak == null) return;` |
| 5923 | `negate: drop !` | `if (!sp \|\| sp.baseline == null \|\| sp.peak == null) return;` |
| 5925 | `cmp <= → <` | `if (amplitude <= 0) return;` |
| 5926 | `num → 0` | `var halfTarget = sp.baseline + amplitude * 0.5;` |
| 5929 | `bool \|\| → &&` | `var spMfm = sp.mfm \|\| 0;` |
| 5930 | `cmp > → >=` | `var startHr = rows.length > 0 ? rows[0].t.getUTCHours() * 60 + rows[0].t` |
| 5930 | `num → 0` | `var startHr = rows.length > 0 ? rows[0].t.getUTCHours() * 60 + rows[0].t` |
| 5930 | `num → 0` | `var startHr = rows.length > 0 ? rows[0].t.getUTCHours() * 60 + rows[0].t` |
| 5932 | `num → 0` | `var searchStart = Math.max(0, approxIdx - 300);` |
| 5933 | `num → 0` | `var searchEnd = Math.min(rows.length, approxIdx + 300);` |
| 5934 | `cmp < → <=` | `for (var i = searchStart; i < searchEnd; i++) {` |
| 5935 | `cmp >= → >` | `if (rows[i].hr >= sp.peak - 2) {` |
| 5940 | `cmp < → <=` | `if (peakIdx < 0) return;` |
| 5941 | `cmp < → <=` | `for (var j = peakIdx + 1; j < Math.min(rows.length, peakIdx + 180); j++)` |
| 5941 | `num → 0` | `for (var j = peakIdx + 1; j < Math.min(rows.length, peakIdx + 180); j++)` |
| 5942 | `cmp <= → <` | `if (rows[j].hr <= halfTarget) {` |
| 5954 | `cmp > → >=` | `return { spike50PctRecovSec: mean, spike50PctRecovN: times.length, spike` |
| 5954 | `cmp > → >=` | `return { spike50PctRecovSec: mean, spike50PctRecovN: times.length, spike` |
| 5954 | `num → 0` | `return { spike50PctRecovSec: mean, spike50PctRecovN: times.length, spike` |
| 5954 | `num → 0` | `return { spike50PctRecovSec: mean, spike50PctRecovN: times.length, spike` |
| 5960 | `cmp < → <=` | `if (n < 1800) return null;` |
| 5990 | `cmp < → <=` | `if (segSD < 4 && segMean < hrMean - 6) {` |
| 5990 | `cmp < → <=` | `if (segSD < 4 && segMean < hrMean - 6) {` |
| 5994 | `cmp > → >=` | `else if (segSD < 3 && segMean > hrMean - 5 && segMean < hrMean + 5) remS` |
| 5994 | `cmp < → <=` | `else if (segSD < 3 && segMean > hrMean - 5 && segMean < hrMean + 5) remS` |
| 5994 | `cmp < → <=` | `else if (segSD < 3 && segMean > hrMean - 5 && segMean < hrMean + 5) remS` |
| 6008 | `num → 0` | `var REM_CEILING_PCT = 30; // adult REM ≈ 20–25 % of sleep; >30 % of the ` |
| 6011 | `cmp <= → <` | `var remPlausible = remPct <= REM_CEILING_PCT;` |
| 6024 | `cmp > → >=` | `remProxyLabel: !remPlausible ? 'Implausible — estimator unreliable' : re` |
| 6024 | `cmp < → <=` | `remProxyLabel: !remPlausible ? 'Implausible — estimator unreliable' : re` |
| 6024 | `num → 0` | `remProxyLabel: !remPlausible ? 'Implausible — estimator unreliable' : re` |
| 6024 | `num → 0` | `remProxyLabel: !remPlausible ? 'Implausible — estimator unreliable' : re` |
| 6024 | `num → 0` | `remProxyLabel: !remPlausible ? 'Implausible — estimator unreliable' : re` |
| 6024 | `num → 0` | `remProxyLabel: !remPlausible ? 'Implausible — estimator unreliable' : re` |
| 6025 | `cmp > → >=` | `nremDeepLabel: nremDeepSec / 60 < 30 ? 'Low deep sleep estimate' : nremD` |
| 6025 | `num → 0` | `nremDeepLabel: nremDeepSec / 60 < 30 ? 'Low deep sleep estimate' : nremD` |
| 6025 | `cmp < → <=` | `nremDeepLabel: nremDeepSec / 60 < 30 ? 'Low deep sleep estimate' : nremD` |
| 6025 | `num → 0` | `nremDeepLabel: nremDeepSec / 60 < 30 ? 'Low deep sleep estimate' : nremD` |
| 6047 | `num → 0` | `age = age \|\| 49; // default to space profile age` |
| 6053 | `cmp < → <=` | `return r.motion === 0 && r.hr > 30 && r.hr < 120;` |
| 6061 | `cmp < → <=` | `if (stillHR.length < 60) return null;` |
| 6063 | `num → 0` | `var p5idx = Math.floor(0.05 * stillHR.length);` |
| 6067 | `cmp > → >=` | `var hrRest = UP && UP.hrRestOverride && UP.hrRestOverride > 30 && UP.hrR` |
| 6067 | `cmp < → <=` | `var hrRest = UP && UP.hrRestOverride && UP.hrRestOverride > 30 && UP.hrR` |
| 6067 | `bool && → \|\|` | `var hrRest = UP && UP.hrRestOverride && UP.hrRestOverride > 30 && UP.hrR` |
| 6067 | `num → 0` | `var hrRest = UP && UP.hrRestOverride && UP.hrRestOverride > 30 && UP.hrR` |
| 6067 | `num → 0` | `var hrRest = UP && UP.hrRestOverride && UP.hrRestOverride > 30 && UP.hrR` |
| 6070 | `num → 0` | `if (hrRest < 30 \|\| hrRest > 100) return null;` |
| 6070 | `cmp < → <=` | `if (hrRest < 30 \|\| hrRest > 100) return null;` |
| 6083 | `cmp > → >=` | `var vo2raw = hrRest && hrRest > 0 ? +(15.3 * (hrMax / hrRest)).toFixed(1` |
| 6083 | `bool && → \|\|` | `var vo2raw = hrRest && hrRest > 0 ? +(15.3 * (hrMax / hrRest)).toFixed(1` |
| 6104 | `cmp >= → >` | `rmssdNote = 'RMSSD ' + hrv.rmssd + 'bpm (1Hz proxy) → adj ' + (rmssdAdj ` |
| 6125 | `num → 0` | `var conf = 60; // base` |
| 6126 | `cmp >= → >` | `if (hrRest >= 40 && hrRest <= 65) conf += 15; // plausible resting HR` |
| 6126 | `cmp <= → <` | `if (hrRest >= 40 && hrRest <= 65) conf += 15; // plausible resting HR` |
| 6126 | `bool && → \|\|` | `if (hrRest >= 40 && hrRest <= 65) conf += 15; // plausible resting HR` |
| 6126 | `num → 0` | `if (hrRest >= 40 && hrRest <= 65) conf += 15; // plausible resting HR` |
| 6126 | `num → 0` | `if (hrRest >= 40 && hrRest <= 65) conf += 15; // plausible resting HR` |
| 6126 | `num → 0` | `if (hrRest >= 40 && hrRest <= 65) conf += 15; // plausible resting HR` |
| 6127 | `num → 0` | `if (stillHR.length > 3600) conf += 10; // long recording` |
| 6127 | `num → 0` | `if (stillHR.length > 3600) conf += 10; // long recording` |
| 6127 | `cmp > → >=` | `if (stillHR.length > 3600) conf += 10; // long recording` |
| 6128 | `num → 0` | `if (hrv.rmssd != null) conf += 10; // RMSSD available` |
| 6130 | `num → 0` | `conf = Math.min(100, conf);` |
| 6136 | `num → 0` | `var see = 10.8; // general population SEE (trained athletes: 5.4)` |
| 6156 | `cmp >= → >` | `label: vo2est >= 42 ? 'Top-25% for age ' + age : vo2est >= 35 ? 'Above a` |
| 6156 | `cmp >= → >` | `label: vo2est >= 42 ? 'Top-25% for age ' + age : vo2est >= 35 ? 'Above a` |
| 6156 | `cmp >= → >` | `label: vo2est >= 42 ? 'Top-25% for age ' + age : vo2est >= 35 ? 'Above a` |
| 6156 | `num → 0` | `label: vo2est >= 42 ? 'Top-25% for age ' + age : vo2est >= 35 ? 'Above a` |
| 6156 | `num → 0` | `label: vo2est >= 42 ? 'Top-25% for age ' + age : vo2est >= 35 ? 'Above a` |
| 6156 | `num → 0` | `label: vo2est >= 42 ? 'Top-25% for age ' + age : vo2est >= 35 ? 'Above a` |
| 6183 | `cmp > → >=` | `var hasHrRestSource = (UP && UP.hrRestOverride && UP.hrRestOverride > 30` |
| 6183 | `bool && → \|\|` | `var hasHrRestSource = (UP && UP.hrRestOverride && UP.hrRestOverride > 30` |
| 6183 | `cmp < → <=` | `var hasHrRestSource = (UP && UP.hrRestOverride && UP.hrRestOverride > 30` |
| 6183 | `num → 0` | `var hasHrRestSource = (UP && UP.hrRestOverride && UP.hrRestOverride > 30` |
| 6183 | `num → 0` | `var hasHrRestSource = (UP && UP.hrRestOverride && UP.hrRestOverride > 30` |
| 6186 | `num → 0` | `age = age \|\| 49;` |
| 6190 | `cmp > → >=` | `if (UP && UP.hrRestOverride && UP.hrRestOverride > 30 && UP.hrRestOverri` |
| 6190 | `cmp < → <=` | `if (UP && UP.hrRestOverride && UP.hrRestOverride > 30 && UP.hrRestOverri` |
| 6190 | `bool && → \|\|` | `if (UP && UP.hrRestOverride && UP.hrRestOverride > 30 && UP.hrRestOverri` |
| 6190 | `num → 0` | `if (UP && UP.hrRestOverride && UP.hrRestOverride > 30 && UP.hrRestOverri` |
| 6190 | `num → 0` | `if (UP && UP.hrRestOverride && UP.hrRestOverride > 30 && UP.hrRestOverri` |
| 6195 | `cmp < → <=` | `if (!rows \|\| rows.length < 60) return null;` |
| 6195 | `num → 0` | `if (!rows \|\| rows.length < 60) return null;` |
| 6195 | `bool \|\| → &&` | `if (!rows \|\| rows.length < 60) return null;` |
| 6198 | `cmp > → >=` | `return r.motion === 0 && r.hr > 30 && r.hr < 120;` |
| 6198 | `cmp < → <=` | `return r.motion === 0 && r.hr > 30 && r.hr < 120;` |
| 6198 | `bool && → \|\|` | `return r.motion === 0 && r.hr > 30 && r.hr < 120;` |
| 6198 | `num → 0` | `return r.motion === 0 && r.hr > 30 && r.hr < 120;` |
| 6198 | `bool && → \|\|` | `return r.motion === 0 && r.hr > 30 && r.hr < 120;` |
| 6206 | `num → 0` | `if (stillHR.length < 60) return null;` |
| 6206 | `cmp < → <=` | `if (stillHR.length < 60) return null;` |
| 6207 | `num → 0` | `hrRest = stillHR[Math.floor(0.05 * stillHR.length)];` |
| 6209 | `cmp < → <=` | `if (hrRest < 30 \|\| hrRest > 80) return null;` |
| 6209 | `num → 0` | `if (hrRest < 30 \|\| hrRest > 80) return null;` |
| 6224 | `num → 0` | `z2: zone(0.6, 0.7),` |
| 6274 | `num → 0` | `var durationMin = n > 0 ? n / 60 : durationMinHint \|\| 360;` |
| 6292 | `cmp <= → <` | `if (hrRest <= 48) hrFloorScore = 15;` |
| 6292 | `num → 0` | `if (hrRest <= 48) hrFloorScore = 15;` |
| 6292 | `num → 0` | `if (hrRest <= 48) hrFloorScore = 15;` |
| 6293 | `num → 0` | `else if (hrRest <= 54) hrFloorScore = 12;` |
| 6293 | `cmp <= → <` | `else if (hrRest <= 54) hrFloorScore = 12;` |
| 6293 | `num → 0` | `else if (hrRest <= 54) hrFloorScore = 12;` |
| 6294 | `cmp <= → <` | `else if (hrRest <= 60) hrFloorScore = 8;` |
| 6294 | `num → 0` | `else if (hrRest <= 60) hrFloorScore = 8;` |
| 6295 | `cmp <= → <` | `else if (hrRest <= 68) hrFloorScore = 4;` |
| 6295 | `num → 0` | `else if (hrRest <= 68) hrFloorScore = 4;` |
| 6302 | `cmp < → <=` | `if (hrv.hrSlope < -0.5) hrSlopeScore = 10;` |
| 6302 | `num → 0` | `if (hrv.hrSlope < -0.5) hrSlopeScore = 10;` |
| 6302 | `num → 0` | `if (hrv.hrSlope < -0.5) hrSlopeScore = 10;` |
| 6303 | `cmp < → <=` | `else if (hrv.hrSlope < 0) hrSlopeScore = 7;` |
| 6304 | `cmp < → <=` | `else if (hrv.hrSlope < 0.5) hrSlopeScore = 4;` |
| 6304 | `num → 0` | `else if (hrv.hrSlope < 0.5) hrSlopeScore = 4;` |
| 6310 | `num → 0` | `readiness = Math.min(100, Math.max(0, readiness));` |
| 6314 | `cmp >= → >` | `if (readiness >= 85) {` |
| 6314 | `num → 0` | `if (readiness >= 85) {` |
| 6319 | `num → 0` | `} else if (readiness >= 70) {` |
| 6319 | `cmp >= → >` | `} else if (readiness >= 70) {` |
| 6324 | `cmp >= → >` | `} else if (readiness >= 55) {` |
| 6324 | `num → 0` | `} else if (readiness >= 55) {` |
| 6329 | `cmp >= → >` | `} else if (readiness >= 40) {` |
| 6329 | `num → 0` | `} else if (readiness >= 40) {` |
| 6343 | `eq === → !==` | `if (zoneRec === 'z4_z5') recZones = [zones.z3, zones.z4, zones.z5];` |
| 6344 | `eq === → !==` | `else if (zoneRec === 'z3_z4') recZones = [zones.z2, zones.z3, zones.z4];` |
| 6345 | `eq === → !==` | `else if (zoneRec === 'z2_z3') recZones = [zones.z2, zones.z3];` |
| 6346 | `eq === → !==` | `else if (zoneRec === 'z1_z2') recZones = [zones.z1, zones.z2];` |
| 6351 | `num → 0` | `var mafHR = 180 - age;` |
| 6353 | `num → 0` | `if (readiness >= 85) {` |
| 6353 | `cmp >= → >` | `if (readiness >= 85) {` |
| 6356 | `cmp < → <=` | `} else if (readiness < 55) {` |
| 6356 | `num → 0` | `} else if (readiness < 55) {` |
| 6358 | `num → 0` | `mafHR -= 10;` |
| 6363 | `num → 0` | `var lthr = Math.round(hrMax * 0.88);` |
| 6399 | `num → 0` | `WIN = 300;` |
| 6405 | `cmp <= → <` | `if (n < 60 \|\| durationHr <= 0) return { sbii: null, sbiiQ: null };` |
| 6405 | `cmp < → <=` | `if (n < 60 \|\| durationHr <= 0) return { sbii: null, sbiiQ: null };` |
| 6425 | `num → 0` | `return { depth: e.depth, duration: dur, desatArea: (e.depth * dur) / 60 ` |
| 6428 | `bool && → \|\|` | `return isFinite(e.depth) && isFinite(e.duration);` |
| 6443 | `cmp > → >=` | `var sbii = durationHr > 0 ? +(sum / durationHr).toFixed(3) : 0;` |
| 6444 | `cmp < → <=` | `var q = sbii < 2.58 ? 'Q1(low)' : sbii < 6.49 ? 'Q2' : sbii < 12.8 ? 'Q3` |
| 6444 | `cmp < → <=` | `var q = sbii < 2.58 ? 'Q1(low)' : sbii < 6.49 ? 'Q2' : sbii < 12.8 ? 'Q3` |
| 6444 | `cmp < → <=` | `var q = sbii < 2.58 ? 'Q1(low)' : sbii < 6.49 ? 'Q2' : sbii < 12.8 ? 'Q3` |
| 6444 | `cmp < → <=` | `var q = sbii < 2.58 ? 'Q1(low)' : sbii < 6.49 ? 'Q2' : sbii < 12.8 ? 'Q3` |
| 6444 | `num → 0` | `var q = sbii < 2.58 ? 'Q1(low)' : sbii < 6.49 ? 'Q2' : sbii < 12.8 ? 'Q3` |
| 6444 | `num → 0` | `var q = sbii < 2.58 ? 'Q1(low)' : sbii < 6.49 ? 'Q2' : sbii < 12.8 ? 'Q3` |
| 6444 | `num → 0` | `var q = sbii < 2.58 ? 'Q1(low)' : sbii < 6.49 ? 'Q2' : sbii < 12.8 ? 'Q3` |
| 6476 | `cmp < → <=` | `var q = pred3p < 2.78 ? 'Q1(low)' : pred3p < 6.19 ? 'Q2' : pred3p < 10.8` |
| 6476 | `cmp < → <=` | `var q = pred3p < 2.78 ? 'Q1(low)' : pred3p < 6.19 ? 'Q2' : pred3p < 10.8` |
| 6476 | `cmp < → <=` | `var q = pred3p < 2.78 ? 'Q1(low)' : pred3p < 6.19 ? 'Q2' : pred3p < 10.8` |
| 6476 | `cmp < → <=` | `var q = pred3p < 2.78 ? 'Q1(low)' : pred3p < 6.19 ? 'Q2' : pred3p < 10.8` |
| 6476 | `num → 0` | `var q = pred3p < 2.78 ? 'Q1(low)' : pred3p < 6.19 ? 'Q2' : pred3p < 10.8` |
| 6476 | `num → 0` | `var q = pred3p < 2.78 ? 'Q1(low)' : pred3p < 6.19 ? 'Q2' : pred3p < 10.8` |
| 6491 | `cmp < → <=` | `if (n < 60) return { desSev: null };` |
| 6515 | `bool && → \|\|` | `var base = e.baseline != null ? e.baseline : e.nadir != null && e.depth ` |
| 6516 | `bool \|\| → &&` | `if (base == null \|\| !isFinite(base)) return;` |
| 6519 | `cmp > → >=` | `if (d > 0) totalArea += d;` |
| 6524 | `cmp > → >=` | `var desSev = durationHr > 0 ? +(totalArea / 60 / durationHr).toFixed(2) ` |
| 6539 | `cmp < → <=` | `for (var i = 0; i < n; i++) {` |
| 6544 | `cmp < → <=` | `if (v < 85) ct85++;` |
| 6544 | `num → 0` | `if (v < 85) ct85++;` |
| 6545 | `num → 0` | `if (v < 80) ct80++;` |
| 6545 | `cmp < → <=` | `if (v < 80) ct80++;` |
| 6625 | `bool \|\| → &&` | `pnn3: obj.hrv.pnn3 \|\| 0,` |
| 6626 | `bool \|\| → &&` | `hrFloor: obj.hrv.hrFloor \|\| 0,` |
| 6628 | `bool \|\| → &&` | `rsaProxy: obj.hrv.rsaProxy \|\| 0,` |
| 6630 | `bool \|\| → &&` | `maxHr: obj.hrv.maxHr \|\| 0,` |
| 6644 | `bool \|\| → &&` | `mfm: sp.mfm \|\| (sp.time ? parseTimeStr(sp.time) / 60 : 0)` |
| 6644 | `num → 0` | `mfm: sp.mfm \|\| (sp.time ? parseTimeStr(sp.time) / 60 : 0)` |
| 6649 | `bool \|\| → &&` | `var cnt = (obj.hr_spikes && obj.hr_spikes.count) \|\| 0;` |
| 6650 | `cmp > → >=` | `return cnt > 0 ? { length: cnt } : [];` |
| 6658 | `num → 0` | `if (s.t95pct != null) idx[95] = { pct: s.t95pct, secs: Math.round((s.t95` |
| 6658 | `bool \|\| → &&` | `if (s.t95pct != null) idx[95] = { pct: s.t95pct, secs: Math.round((s.t95` |
| 6658 | `num → 0` | `if (s.t95pct != null) idx[95] = { pct: s.t95pct, secs: Math.round((s.t95` |
| 6658 | `num → 0` | `if (s.t95pct != null) idx[95] = { pct: s.t95pct, secs: Math.round((s.t95` |
| 6659 | `num → 0` | `if (s.t90pct != null) idx[90] = { pct: s.t90pct, secs: Math.round((s.t90` |
| 6659 | `bool \|\| → &&` | `if (s.t90pct != null) idx[90] = { pct: s.t90pct, secs: Math.round((s.t90` |
| 6659 | `num → 0` | `if (s.t90pct != null) idx[90] = { pct: s.t90pct, secs: Math.round((s.t90` |
| 6659 | `num → 0` | `if (s.t90pct != null) idx[90] = { pct: s.t90pct, secs: Math.round((s.t90` |
| 6667 | `bool \|\| → &&` | `stab: obj.sleepStability \|\| null,` |
| 6668 | `bool \|\| → &&` | `motSleep: obj.sleepQuality \|\| null,` |
| 6669 | `bool \|\| → &&` | `desat: obj.desatProfile \|\| null,` |
| 6670 | `bool \|\| → &&` | `hrProf: obj.hrProfile \|\| null,` |
| 6671 | `bool \|\| → &&` | `cross: obj.crossSignal \|\| null,` |
| 6672 | `bool \|\| → &&` | `spo2Adv: obj.spo2Advanced \|\| null,` |
| 6673 | `bool \|\| → &&` | `hrAdv: obj.hrAdvanced \|\| null,` |
| 6675 | `bool \|\| → &&` | `sbii: obj.sbii \|\| null,` |
| 6676 | `bool \|\| → &&` | `pred3p: obj.pred3p \|\| null,` |
| 6677 | `bool \|\| → &&` | `desSev: obj.desSev \|\| null,` |
| 6684 | `bool \|\| → &&` | `spo2Drift: (obj.newMetrics \|\| {}).spo2Drift \|\| null,` |
| 6685 | `bool \|\| → &&` | `odi2: (obj.newMetrics \|\| {}).odi2 \|\| null,` |
| 6686 | `bool \|\| → &&` | `spo2Over: (obj.newMetrics \|\| {}).spo2Overshoot \|\| null,` |
| 6687 | `bool \|\| → &&` | `spo2Ac1: (obj.newMetrics \|\| {}).spo2Ac1 \|\| null,` |
| 6688 | `bool \|\| → &&` | `hrFreq: (obj.newMetrics \|\| {}).hrFreqBands \|\| null,` |
| 6689 | `bool \|\| → &&` | `respRate: (obj.newMetrics \|\| {}).respRate \|\| null,` |
| 6690 | `bool \|\| → &&` | `hrAsym: (obj.newMetrics \|\| {}).hrAsymmetry \|\| null,` |
| 6691 | `bool \|\| → &&` | `hrQuart: (obj.newMetrics \|\| {}).hrQuartiles \|\| null,` |
| 6692 | `bool \|\| → &&` | `spo2HRLag: (obj.newMetrics \|\| {}).spo2HRLag \|\| null,` |
| 6693 | `bool \|\| → &&` | `spkDecay: (obj.newMetrics \|\| {}).spikeDecay \|\| null,` |
| 6694 | `bool \|\| → &&` | `spkUnder: (obj.newMetrics \|\| {}).spikeUndershoot \|\| null,` |
| 6695 | `bool \|\| → &&` | `spkRise: (obj.newMetrics \|\| {}).spikeRiseRate \|\| null,` |
| 6696 | `bool \|\| → &&` | `dataGaps: (obj.newMetrics \|\| {}).dataGaps \|\| null,` |
| 6697 | `bool \|\| → &&` | `hrFlat: (obj.newMetrics \|\| {}).hrFlatlines \|\| null,` |
| 6699 | `bool \|\| → &&` | `odri: (obj.newMetrics \|\| {}).odri \|\| null,` |
| 6700 | `bool \|\| → &&` | `spo2Pct: (obj.newMetrics \|\| {}).spo2Pct \|\| null,` |
| 6701 | `bool \|\| → &&` | `spo2Shape: (obj.newMetrics \|\| {}).spo2Shape \|\| null,` |
| 6702 | `bool \|\| → &&` | `hrCV: (obj.newMetrics \|\| {}).hrCV \|\| null,` |
| 6704 | `bool \|\| → &&` | `t88t85: (obj.newMetrics \|\| {}).t88t85 \|\| null,` |
| 6706 | `bool \|\| → &&` | `poincare: (obj.newMetrics \|\| {}).poincare \|\| null,` |
| 6707 | `bool \|\| → &&` | `o2hrEff: (obj.newMetrics \|\| {}).o2hrEff \|\| null,` |
| 6708 | `bool \|\| → &&` | `condSpo2: (obj.newMetrics \|\| {}).condSpo2 \|\| null,` |
| 6710 | `bool \|\| → &&` | `iei: (obj.newMetrics \|\| {}).iei \|\| null,` |
| 6711 | `bool \|\| → &&` | `recovCV: (obj.newMetrics \|\| {}).recovCV \|\| null,` |
| 6712 | `bool \|\| → &&` | `hrNadirT: (obj.newMetrics \|\| {}).hrNadirT \|\| null,` |
| 6713 | `bool \|\| → &&` | `spo2NadirT: (obj.newMetrics \|\| {}).spo2NadirT \|\| null,` |
| 6714 | `bool \|\| → &&` | `rmssdArc: (obj.newMetrics \|\| {}).rmssdArc \|\| null,` |
| 6715 | `bool \|\| → &&` | `spk50Rec: (obj.newMetrics \|\| {}).spk50Rec \|\| null,` |
| 6716 | `bool \|\| → &&` | `stageProxy: (obj.newMetrics \|\| {}).stageProxy \|\| null,` |
| 6722 | `bool && → \|\|` | `if (v.hrRest && v.hrMax && v.rmssdAdj != null) {` |
| 6722 | `bool && → \|\|` | `if (v.hrRest && v.hrMax && v.rmssdAdj != null) {` |
| 6723 | `num → 0` | `var base = +(15.3 * (v.hrMax / v.hrRest)).toFixed(1);` |
| 6724 | `bool \|\| → &&` | `var corrected = +(base + (v.dfaAdj \|\| 0) + v.rmssdAdj).toFixed(1);` |
| 6725 | `eq !== → ===` | `if (corrected !== v.vo2est) v = Object.assign({}, v, { vo2est: corrected` |
| 6729 | `bool \|\| → &&` | `bpProj: (obj.newMetrics \|\| {}).bpProj \|\| null,` |
| 6730 | `bool \|\| → &&` | `karv: (obj.newMetrics \|\| {}).karv \|\| null,` |
| 6758 | `bool && → \|\|` | `var o4r = obj.odi4 && obj.odi4.rate != null ? obj.odi4.rate : 0;` |
| 6759 | `bool && → \|\|` | `var ct90s = obj.ctPrecise && obj.ctPrecise.ct90s != null ? obj.ctPrecise` |
| 6767 | `bool && → \|\|` | `var o4r = obj.odi4 && obj.odi4.rate != null ? obj.odi4.rate : 0;` |
| 6768 | `bool && → \|\|` | `var o3r = obj.odi3 && obj.odi3.rate != null ? obj.odi3.rate : 0;` |
| 6771 | `bool && → \|\|` | `var dsev = obj.desSev && obj.desSev.desSev != null ? obj.desSev.desSev :` |
| 6772 | `bool && → \|\|` | `var t95 = obj.stats && obj.stats.t95pct != null ? obj.stats.t95pct : 0;` |
| 6828 | `bool \|\| → &&` | `message: 'This is a ' + (node \|\| 'non-OxyDex') + ' export \\u2014 open it` |
| 6828 | `bool \|\| → &&` | `message: 'This is a ' + (node \|\| 'non-OxyDex') + ' export \\u2014 open it` |
| 6834 | `bool && → \|\|` | `var carrier = Array.isArray(json.nights) ? json.nights : json.date && js` |
| 6843 | `eq === → !==` | `if (el && el.date === n.date && el.summary != null) n.summary = el.summa` |
| 6843 | `bool && → \|\|` | `if (el && el.date === n.date && el.summary != null) n.summary = el.summa` |
| 6843 | `bool && → \|\|` | `if (el && el.date === n.date && el.summary != null) n.summary = el.summa` |
| 6854 | `bool && → \|\|` | `generated: (json.schema && json.schema.generated) \|\| null,` |
| 6855 | `bool && → \|\|` | `derivedFrom: (json.schema && json.schema.derivedFrom) \|\| null,` |
| 6855 | `bool \|\| → &&` | `derivedFrom: (json.schema && json.schema.derivedFrom) \|\| null,` |
| 6856 | `bool \|\| → &&` | `kernel: json.kernel \|\| null,` |
| 6859 | `bool && → \|\|` | `scrubbed: !!(json.schema && json.schema.scrubbed),` |
| 6860 | `cmp > → >=` | `multiNight: nights.length > 1,` |
| 6879 | `eq === → !==` | `if (typeof DexExport !== 'undefined' && DexExport && typeof DexExport.sc` |
| 6879 | `bool && → \|\|` | `if (typeof DexExport !== 'undefined' && DexExport && typeof DexExport.sc` |
| 6879 | `eq !== → ===` | `if (typeof DexExport !== 'undefined' && DexExport && typeof DexExport.sc` |
| 6879 | `bool && → \|\|` | `if (typeof DexExport !== 'undefined' && DexExport && typeof DexExport.sc` |
| 6880 | `eq === → !==` | `if (typeof dexScrubExport === 'function') return dexScrubExport(envelope` |
| 6942 | `bool \|\| → &&` | `opts = opts \|\| {};` |
| 6943 | `eq !== → ===` | `var _prov = opts.provenance !== undefined ? opts.provenance : null;` |
| 6944 | `eq !== → ===` | `var _kernel = opts.kernel !== undefined ? opts.kernel : null;` |
| 6945 | `eq !== → ===` | `var _ecgFusion = opts.ecgFusion !== undefined ? opts.ecgFusion : null;` |
| 6946 | `eq !== → ===` | `var _ansAge = opts.ansAge !== undefined ? opts.ansAge : null;` |
| 6968 | `bool && → \|\|` | `windows: n.motion && n.motion.windows ? n.motion.windows : []` |
| 6998 | `bool \|\| → &&` | `spikeDecay: n.spkDecay \|\| null,` |
| 6999 | `bool \|\| → &&` | `spikeUndershoot: n.spkUnder \|\| null,` |
| 7020 | `bool \|\| → &&` | `spk50Rec: n.spk50Rec \|\| null,` |
| 7022 | `bool \|\| → &&` | `vo2est: n.vo2est \|\| null,` |
| 7023 | `bool \|\| → &&` | `bpProj: n.bpProj \|\| null,` |
| 7024 | `bool \|\| → &&` | `karv: n.karv \|\| null` |
| 7029 | `bool \|\| → &&` | `period: n.period \|\| null,` |
| 7080 | `bool \|\| → &&` | `var nSamp = stats.n \|\| 0;` |
| 7081 | `num → 0` | `var durMs = stats.durationMin != null ? stats.durationMin * 60000 : null` |
| 7082 | `bool && → \|\|` | `var dt = durMs && nSamp ? durMs / nSamp : 1000; // O2Ring ≈ 1 Hz (mirror` |
| 7082 | `num → 0` | `var dt = durMs && nSamp ? durMs / nSamp : 1000; // O2Ring ≈ 1 Hz (mirror` |
| 7085 | `bool && → \|\|` | `var devs = dp && Array.isArray(dp.events) ? dp.events : [];` |
| 7087 | `bool \|\| → &&` | `if (!d \|\| d.artifact) return; // self-gated artifacts are never on the b` |
| 7124 | `negate: drop !` | `if (!ep) return;` |
| 7127 | `eq !== → ===` | `var W = ep.windowSec != null ? ep.windowSec : (typeof CFG !== 'undefined` |
| 7127 | `bool && → \|\|` | `var W = ep.windowSec != null ? ep.windowSec : (typeof CFG !== 'undefined` |
| 7127 | `num → 0` | `var W = ep.windowSec != null ? ep.windowSec : (typeof CFG !== 'undefined` |
| 7127 | `bool \|\| → &&` | `var W = ep.windowSec != null ? ep.windowSec : (typeof CFG !== 'undefined` |
| 7130 | `cmp > → >=` | `var cycleLen = cross && cross > 0 ? +((2 * W) / cross).toFixed(1) : null` |
| 7130 | `bool && → \|\|` | `var cycleLen = cross && cross > 0 ? +((2 * W) / cross).toFixed(1) : null` |
| 7150 | `num → 0` | `var durB = (Math.min(dur, 60) / 60) * 0.05; // up to +0.05 for a ≥60 s s` |
| 7150 | `num → 0` | `var durB = (Math.min(dur, 60) / 60) * 0.05; // up to +0.05 for a ≥60 s s` |
| 7151 | `num → 0` | `var recB = rec > 0 ? 0.03 : 0; // +0.03 for a clean recovery to baseline` |
| 7167 | `cmp < → <=` | `for (var i = 0; i < arr.length; i++) {` |
| 7169 | `bool \|\| → &&` | `if (!r \|\| r.tMs == null) continue;` |
| 7170 | `bool \|\| → &&` | `out.push({ tMs: r.tMs, t: new Date(r.tMs), spo2: r.spo2, hr: r.hr, motio` |
| 7179 | `bool \|\| → &&` | `if (typeof input.text === 'string') return parseCSV(input.text, input.fi` |
| 7192 | `cmp < → <=` | `for (var i = 0; i < rows.length; i++) {` |
| 7194 | `bool \|\| → &&` | `if (!r \|\| r.tMs == null) continue;` |
| 7202 | `bool && → \|\|` | `if (typeof r.hr === 'number' && isFinite(r.hr)) b.hr.push(r.hr);` |
| 7203 | `bool && → \|\|` | `if (typeof r.motion === 'number' && isFinite(r.motion)) b.mo.push(r.moti` |
| 7279 | `bool \|\| → &&` | `if (!rows \|\| !rows.length \|\| t0Ms == null) return null;` |
| 7281 | `cmp >= → >` | `for (var i = rows.length - 1; i >= 0; i--)` |
| 7282 | `bool && → \|\|` | `if (rows[i] && rows[i].tMs != null) {` |
| 7286 | `cmp < → <=` | `if (last == null \|\| last < t0Ms) return null;` |
| 7288 | `cmp > → >=` | `if (n < 2 \|\| n > 48 * 3600) return null; // a >48 h grid means a broken ` |
| 7288 | `cmp < → <=` | `if (n < 2 \|\| n > 48 * 3600) return null; // a >48 h grid means a broken ` |
| 7291 | `cmp < → <=` | `for (var j = 0; j < rows.length; j++) {` |
| 7293 | `bool \|\| → &&` | `if (!r \|\| r.tMs == null) continue;` |
| 7295 | `cmp >= → >` | `if (k < 0 \|\| k >= n) continue;` |
| 7295 | `bool \|\| → &&` | `if (k < 0 \|\| k >= n) continue;` |
| 7297 | `bool && → \|\|` | `if (typeof r[field] === 'number' && isFinite(r[field])) out[k] = r[field` |
| 7306 | `bool && → \|\|` | `var n = nightsChrono && nightsChrono.length === 1 ? nightsChrono[0] : nu` |
| 7307 | `bool \|\| → &&` | `if (!n \|\| !Array.isArray(n.tchEpochs) \|\| !n.tchEpochs.length) return nul` |
| 7307 | `bool \|\| → &&` | `if (!n \|\| !Array.isArray(n.tchEpochs) \|\| !n.tchEpochs.length) return nul` |
| 7315 | `bool && → \|\|` | `if (Array.isArray(n.spo2Series) && n.spo2Series.length) {` |
| 7340 | `cmp < → <=` | `if (!rows \|\| rows.length < 1) return null;` |
| 7341 | `bool \|\| → &&` | `return processNight(rows, fname \|\| null);` |
| 7345 | `bool && → \|\|` | `var OxyDex = typeof OxyDex !== 'undefined' && OxyDex ? OxyDex : {};` |
| 7345 | `eq !== → ===` | `var OxyDex = typeof OxyDex !== 'undefined' && OxyDex ? OxyDex : {};` |
| 7348 | `bool && → \|\|` | `var fname = opts.fname \|\| (input && input.provenance && input.provenance` |
| 7348 | `bool \|\| → &&` | `var fname = opts.fname \|\| (input && input.provenance && input.provenance` |
| 7348 | `bool && → \|\|` | `var fname = opts.fname \|\| (input && input.provenance && input.provenance` |
| 7348 | `bool \|\| → &&` | `var fname = opts.fname \|\| (input && input.provenance && input.provenance` |
| 7348 | `bool \|\| → &&` | `var fname = opts.fname \|\| (input && input.provenance && input.provenance` |
| 7352 | `eq !== → ===` | `var el = oxyBuildNightElement(night, { provenance: opts.provenance !== u` |
| 7361 | `eq !== → ===` | `provenance: opts.provenance !== undefined ? opts.provenance : null,` |
| 7398 | `bool && → \|\|` | `if (night.rtcVerifiedAtMs != null && isFinite(night.rtcVerifiedAtMs)) _o` |
| 7453 | `cmp < → <=` | `for (var i = 0; i < lines.length; i++) {` |
| 7455 | `bool \|\| → &&` | `if (!ln \|\| ln.charCodeAt(0) === 0x50 /* 'P'hone header */) continue;` |
| 7463 | `bool \|\| → &&` | `if (!isFinite(a) \|\| !isFinite(b)) continue;` |
| 7485 | `cmp > → >=` | `var sl = sxx > 0 ? sxy / sxx : 0;` |
| 7488 | `cmp < → <=` | `for (i = 2; i < n - 2; i++) {` |
| 7513 | `cmp < → <=` | `if (!rec \|\| !rec.rows \|\| rec.rows.length < 5000) {` |
| 7513 | `num → 0` | `if (!rec \|\| !rec.rows \|\| rec.rows.length < 5000) {` |
| 7517 | `bool \|\| → &&` | `if (!spo2Rows \|\| !spo2Rows.length) {` |
| 7535 | `cmp > → >=` | `if (hist[k] > best) {` |
| 7546 | `num → 0` | `if (b.length < 90) return;` |
| 7546 | `cmp < → <=` | `if (b.length < 90) return;` |
| 7557 | `cmp <= → <` | `if (A.dc < 1 \|\| B.dc < 1 \|\| A.ac <= 0 \|\| B.ac <= 0) return;` |
| 7557 | `cmp <= → <` | `if (A.dc < 1 \|\| B.dc < 1 \|\| A.ac <= 0 \|\| B.ac <= 0) return;` |
| 7557 | `cmp < → <=` | `if (A.dc < 1 \|\| B.dc < 1 \|\| A.ac <= 0 \|\| B.ac <= 0) return;` |
| 7557 | `bool \|\| → &&` | `if (A.dc < 1 \|\| B.dc < 1 \|\| A.ac <= 0 \|\| B.ac <= 0) return;` |
| 7557 | `cmp < → <=` | `if (A.dc < 1 \|\| B.dc < 1 \|\| A.ac <= 0 \|\| B.ac <= 0) return;` |
| 7557 | `bool \|\| → &&` | `if (A.dc < 1 \|\| B.dc < 1 \|\| A.ac <= 0 \|\| B.ac <= 0) return;` |
| 7557 | `bool \|\| → &&` | `if (A.dc < 1 \|\| B.dc < 1 \|\| A.ac <= 0 \|\| B.ac <= 0) return;` |
| 7562 | `cmp > → >=` | `if (s != null && isFinite(R) && R > 0) pts.push({ tMs: tm, R: R, s: s })` |
| 7562 | `bool && → \|\|` | `if (s != null && isFinite(R) && R > 0) pts.push({ tMs: tm, R: R, s: s })` |
| 7562 | `bool && → \|\|` | `if (s != null && isFinite(R) && R > 0) pts.push({ tMs: tm, R: R, s: s })` |
| 7564 | `cmp < → <=` | `for (i = 0; i < d.length; i++) {` |
| 7565 | `cmp >= → >` | `if (Math.abs(d[i] - modal) >= 3) {` |
| 7594 | `cmp >= → >` | `for (kk in bins) if (bins[kk].R.length >= 8) B2.push({ tMs: med(bins[kk]` |
| 7598 | `cmp < → <=` | `if (B2.length < 40) {` |
| 7598 | `num → 0` | `if (B2.length < 40) {` |
| 7622 | `cmp > → >=` | `if (!(sRR > 0) \|\| !(sSS > 0)) {` |
| 7622 | `cmp > → >=` | `if (!(sRR > 0) \|\| !(sSS > 0)) {` |
| 7622 | `bool \|\| → &&` | `if (!(sRR > 0) \|\| !(sSS > 0)) {` |
| 7627 | `cmp >= → >` | `if (!(r >= 0.3)) {` |
| 7627 | `num → 0` | `if (!(r >= 0.3)) {` |
| 7635 | `num → 0` | `return { tMs: bb.tMs, spo2w: Math.max(60, Math.min(100, w)), dev: bb.s }` |
| 7650 | `cmp < → <=` | `return w < 90;` |
| 7650 | `num → 0` | `return w < 90;` |
| 7653 | `num → 0` | `1000` |
| 7654 | `num → 0` | `) / 10,` |
| 7655 | `num → 0` | `trackR: Math.round(r * 1000) / 1000,` |
| 7655 | `num → 0` | `trackR: Math.round(r * 1000) / 1000,` |
| 7688 | `cmp > → >=` | `if (span > 0 && span < 48 * 3600) {` |
| 7688 | `cmp < → <=` | `if (span > 0 && span < 48 * 3600) {` |
| 7688 | `bool && → \|\|` | `if (span > 0 && span < 48 * 3600) {` |
| 7691 | `cmp < → <=` | `for (i = 0; i < span; i++) {` |
| 7696 | `cmp < → <=` | `for (i = 0; i < span; i++) {` |
| 7700 | `cmp < → <=` | `if (nW < 8) continue; // a second whose window holds under 8 buffers ren` |
| 7702 | `num → 0` | `series.push({ sec: firstSec + i, spo2w: Math.max(60, Math.min(100, wv)) ` |
| 7721 | `cmp < → <=` | `if (dSpan > 0 && dSpan < 48 * 3600) {` |
| 7721 | `cmp > → >=` | `if (dSpan > 0 && dSpan < 48 * 3600) {` |
| 7721 | `bool && → \|\|` | `if (dSpan > 0 && dSpan < 48 * 3600) {` |
| 7724 | `cmp < → <=` | `for (i = 0; i < dSpan; i++) {` |
| 7732 | `cmp <= → <` | `if (hi2 <= lo2) return null;` |
| 7735 | `cmp >= → >` | `return nn2 >= (hi2 - lo2) / 2 ? (dpSum[hi2] - dpSum[lo2]) / nn2 : null;` |
| 7744 | `cmp >= → >` | `if (pairs.length >= 300) {` |
| 7744 | `num → 0` | `if (pairs.length >= 300) {` |
| 7756 | `cmp <= → <` | `if (Math.abs(e) <= 1) w1++;` |
| 7757 | `cmp <= → <` | `if (Math.abs(e) <= 2) w2c++;` |
| 7775 | `bool && → \|\|` | `var rHz = sWW > 0 && sDD > 0 ? sWD / Math.sqrt(sWW * sDD) : null;` |
| 7775 | `cmp > → >=` | `var rHz = sWW > 0 && sDD > 0 ? sWD / Math.sqrt(sWW * sDD) : null;` |
| 7775 | `cmp > → >=` | `var rHz = sWW > 0 && sDD > 0 ? sWD / Math.sqrt(sWW * sDD) : null;` |
| 7782 | `bool && → \|\|` | `for (i = kd * stepW; i < (kd + 1) * stepW && i < pairs.length; i++) errs` |
| 7782 | `cmp < → <=` | `for (i = kd * stepW; i < (kd + 1) * stepW && i < pairs.length; i++) errs` |
| 7782 | `cmp < → <=` | `for (i = kd * stepW; i < (kd + 1) * stepW && i < pairs.length; i++) errs` |
| 7798 | `num → 0` | `var tol = bestW != null ? Math.max(3 * bestW, 1.0) : Infinity;` |
| 7799 | `cmp > → >=` | `var nonUniform = bestW != null && worstW != null && worstW > tol;` |
| 7805 | `cmp <= → <` | `for (var kk2 = 0; kk2 <= byWindow.length; kk2++) {` |
| 7806 | `cmp < → <=` | `var wv2 = kk2 < byWindow.length ? byWindow[kk2] : null;` |
| 7807 | `cmp <= → <` | `if (wv2 != null && wv2 <= tol) {` |
| 7807 | `bool && → \|\|` | `if (wv2 != null && wv2 <= tol) {` |
| 7808 | `cmp < → <=` | `if (curFrom < 0) curFrom = kk2;` |
| 7810 | `cmp > → >=` | `if (curFrom >= 0 && kk2 - curFrom > bestLen) {` |
| 7810 | `cmp >= → >` | `if (curFrom >= 0 && kk2 - curFrom > bestLen) {` |
| 7810 | `bool && → \|\|` | `if (curFrom >= 0 && kk2 - curFrom > bestLen) {` |
| 7820 | `num → 0` | `bias: Math.round((sumE / pairs.length) * 1000) / 1000,` |
| 7820 | `num → 0` | `bias: Math.round((sumE / pairs.length) * 1000) / 1000,` |
| 7821 | `num → 0` | `mae: Math.round((sumAbs / pairs.length) * 100) / 100,` |
| 7822 | `num → 0` | `rmse: Math.round(Math.sqrt(sumSq / pairs.length) * 100) / 100,` |
| 7822 | `num → 0` | `rmse: Math.round(Math.sqrt(sumSq / pairs.length) * 100) / 100,` |
| 7823 | `num → 0` | `r: rHz != null ? Math.round(rHz * 1000) / 1000 : null,` |
| 7824 | `num → 0` | `within1Pct: Math.round((w1 / pairs.length) * 1000) / 10,` |
| 7824 | `num → 0` | `within1Pct: Math.round((w1 / pairs.length) * 1000) / 10,` |
| 7825 | `num → 0` | `within2Pct: Math.round((w2c / pairs.length) * 1000) / 10,` |
| 7825 | `num → 0` | `within2Pct: Math.round((w2c / pairs.length) * 1000) / 10,` |
| 7829 | `num → 0` | `tol: Math.round(tol * 100) / 100,` |
| 7829 | `num → 0` | `tol: Math.round(tol * 100) / 100,` |
| 8011 | `eq !== → ===` | `})(/** @type {any} */ (typeof globalThis !== 'undefined' ? globalThis : ` |
| 8011 | `eq !== → ===` | `})(/** @type {any} */ (typeof globalThis !== 'undefined' ? globalThis : ` |

## `ppgdex-dsp.js` — 789 survivor(s)

| line | operator | source |
|---:|---|---|
| 21 | `num → 0` | `return new Date(instantMs).getTimezoneOffset() * 60000;` |
| 35 | `bool \|\| → &&` | `if (dd.getUTCFullYear() !== y \|\| dd.getUTCMonth() !== mo0 \|\| dd.getUTCDa` |
| 35 | `bool \|\| → &&` | `if (dd.getUTCFullYear() !== y \|\| dd.getUTCMonth() !== mo0 \|\| dd.getUTCDa` |
| 36 | `eq === → !==` | `if (h === 24 && mi === 0 && se === 0 && ms === 0) return day0 + 86400000` |
| 36 | `eq === → !==` | `if (h === 24 && mi === 0 && se === 0 && ms === 0) return day0 + 86400000` |
| 36 | `num → 0` | `if (h === 24 && mi === 0 && se === 0 && ms === 0) return day0 + 86400000` |
| 36 | `eq === → !==` | `if (h === 24 && mi === 0 && se === 0 && ms === 0) return day0 + 86400000` |
| 36 | `num → 0` | `if (h === 24 && mi === 0 && se === 0 && ms === 0) return day0 + 86400000` |
| 37 | `cmp > → >=` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 37 | `cmp > → >=` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 37 | `cmp > → >=` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 37 | `cmp > → >=` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 37 | `bool \|\| → &&` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 37 | `bool \|\| → &&` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 37 | `bool \|\| → &&` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 37 | `bool \|\| → &&` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 37 | `bool \|\| → &&` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 37 | `bool \|\| → &&` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 37 | `bool \|\| → &&` | `if (h < 0 \|\| h > 23 \|\| mi < 0 \|\| mi > 59 \|\| se < 0 \|\| se > 59 \|\| ms < 0 ` |
| 41 | `bool \|\| → &&` | `opts = opts \|\| {};` |
| 49 | `num → 0` | `if (/^\\d{10,13}$/.test(s)) {` |
| 50 | `num → 0` | `let x = parseInt(s, 10);` |
| 51 | `cmp < → <=` | `if (x < 1e11) x *= 1000;` |
| 51 | `num → 0` | `if (x < 1e11) x *= 1000;` |
| 52 | `cmp > → >=` | `if (x < 1e11 \|\| x > 4e12) return null;` |
| 52 | `cmp < → <=` | `if (x < 1e11 \|\| x > 4e12) return null;` |
| 52 | `bool \|\| → &&` | `if (x < 1e11 \|\| x > 4e12) return null;` |
| 53 | `num → 0` | `return { tMs: x - tzOffset(x), offsetMin: -tzOffset(x) / 60000 };` |
| 68 | `eq === → !==` | `if (m[8] === 'Z') offsetMin = 0;` |
| 71 | `eq === → !==` | `offsetMin = (z[0] === '-' ? -1 : 1) * (parseInt(z.slice(1, 3), 10) * 60 ` |
| 71 | `num → 0` | `offsetMin = (z[0] === '-' ? -1 : 1) * (parseInt(z.slice(1, 3), 10) * 60 ` |
| 71 | `num → 0` | `offsetMin = (z[0] === '-' ? -1 : 1) * (parseInt(z.slice(1, 3), 10) * 60 ` |
| 71 | `num → 0` | `offsetMin = (z[0] === '-' ? -1 : 1) * (parseInt(z.slice(1, 3), 10) * 60 ` |
| 158 | `cmp < → <=` | `for (let i = 0; i < x.length; i++) {` |
| 171 | `cmp < → <=` | `for (let i = 0; i < x.length; i++) y[i] = x[x.length - 1 - i];` |
| 193 | `cmp < → <=` | `for (let i = 0; i < p; i++) ext[i] = 2 * x[0] - x[p - i];` |
| 195 | `cmp < → <=` | `for (let i = 0; i < p; i++) ext[p + n + i] = 2 * x[n - 1] - x[n - 2 - i]` |
| 256 | `cmp >= → >` | `if (ch.length >= 3) return { chIdx: [ch[0], ch[1], ch[2]], amb, ns, phon` |
| 266 | `cmp < → <=` | `for (let k = 0; k < p.length; k++) if (isFinite(parseFloat(p[k]))) nums.` |
| 268 | `cmp >= → >` | `if (n >= 5) return { chIdx: [nums[n - 4], nums[n - 3], nums[n - 2]], amb` |
| 269 | `eq === → !==` | `if (n === 3) return { chIdx: [nums[n - 1]], amb: -1, ns: 1, phone: 0 };` |
| 310 | `cmp < → <=` | `for (let i = 0; i < x.length; i++) {` |
| 315 | `cmp <= → <` | `for (let k = i - 2; k <= i + 2; k++) {` |
| 316 | `cmp >= → >` | `if (k === i \|\| k < 0 \|\| k >= x.length) continue;` |
| 316 | `cmp < → <=` | `if (k === i \|\| k < 0 \|\| k >= x.length) continue;` |
| 316 | `bool \|\| → &&` | `if (k === i \|\| k < 0 \|\| k >= x.length) continue;` |
| 316 | `bool \|\| → &&` | `if (k === i \|\| k < 0 \|\| k >= x.length) continue;` |
| 322 | `cmp > → >=` | `if (!cnt \|\| Math.abs(O2_BEAT_MARKER - sum / cnt) > O2_MARKER_ISOLATION) ` |
| 352 | `cmp < → <=` | `for (let li = 0; li < lines.length && li < 12; li++) {` |
| 383 | `bool && → \|\|` | `if (/timestamp/i.test(line) && !pcols) {` |
| 397 | `eq === → !==` | `if (nCh === 1) minFields = 3;` |
| 416 | `bool \|\| → &&` | `if (!isFinite(v1) \|\| !isFinite(v2)) continue;` |
| 421 | `cmp >= → >` | `const va = pc.amb >= 0 ? parseFloat(p[pc.amb]) : NaN;` |
| 422 | `cmp >= → >` | `if (pc.amb >= 0 && !isFinite(va)) continue;` |
| 432 | `cmp >= → >` | `const b = BigInt(p[pcols && pcols.ns >= 0 ? pcols.ns : 1].trim());` |
| 432 | `bool && → \|\|` | `const b = BigInt(p[pcols && pcols.ns >= 0 ? pcols.ns : 1].trim());` |
| 458 | `bool && → \|\|` | `if (at && isFinite(relNs)) axisAnchors.push({ devMs: relNs / 1e6, hostMs` |
| 467 | `num → 0` | `let fs = 176;` |
| 469 | `cmp < → <=` | `for (let i = 1; i < n; i++) {` |
| 471 | `cmp > → >=` | `if (isFinite(d) && d > 0) deltas.push(d);` |
| 471 | `bool && → \|\|` | `if (isFinite(d) && d > 0) deltas.push(d);` |
| 501 | `cmp >= → >` | `for (let li = lines.length - 1; li >= 0 && !t; li--) {` |
| 512 | `num → 0` | `if (deltas.length > 20) {` |
| 512 | `cmp > → >=` | `if (deltas.length > 20) {` |
| 514 | `cmp > → >=` | `if (md > 0) fs = 1e9 / md;` |
| 515 | `cmp > → >=` | `if (md > 0) {` |
| 517 | `cmp < → <=` | `for (let i = 0; i < deltas.length; i++) if (deltas[i] === md) same++;` |
| 524 | `cmp > → >=` | `if (firstTs && lastTs && lastTs.tMs > firstTs.tMs) {` |
| 524 | `bool && → \|\|` | `if (firstTs && lastTs && lastTs.tMs > firstTs.tMs) {` |
| 524 | `bool && → \|\|` | `if (firstTs && lastTs && lastTs.tMs > firstTs.tMs) {` |
| 532 | `bool && → \|\|` | `const hostAx = typeof DexClock !== 'undefined' && DexClock.hostAxis ? De` |
| 533 | `bool && → \|\|` | `if (hostAx.ok && isFinite(hostAx.ppm)) fs = fs / (1 + hostAx.ppm / 1e6);` |
| 540 | `cmp >= → >` | `const axisDrawn = quantizedShare != null && quantizedShare >= 0.99;` |
| 547 | `num → 0` | `if (deltas.length > 20) {` |
| 547 | `cmp > → >=` | `if (deltas.length > 20) {` |
| 548 | `cmp < → <=` | `for (let i = 0; i < n; i++) {` |
| 557 | `cmp < → <=` | `for (let i = 0; i < n; i++) relSec[i] = i / fs;` |
| 634 | `cmp > → >=` | `if (timebase === 'device-crystal' && site === 'finger' && n > 0) {` |
| 636 | `cmp > → >=` | `const maxStep = TIME_GAP_STEPS / (fs > 0 ? fs : O2_ADC_HZ); // gap detec` |
| 640 | `cmp < → <=` | `for (let i = 0; i < n; i++) {` |
| 641 | `cmp > → >=` | `if (i > 0 && relSec[i] - relSec[i - 1] > maxStep) {` |
| 641 | `cmp > → >=` | `if (i > 0 && relSec[i] - relSec[i - 1] > maxStep) {` |
| 700 | `bool && → \|\|` | `return lt && lt.tMs != null ? lt.tMs : null;` |
| 780 | `bool \|\| → &&` | `stability: axisSynthetic ? null : hostAx.stability \|\| null,` |
| 783 | `bool \|\| → &&` | `: { ok: false, reason: hostAx.reason \|\| 'no host anchors', drawn: axisSy` |
| 804 | `cmp > → >=` | `let replicated = nCh > 1;` |
| 805 | `cmp < → <=` | `for (let c = 1; replicated && c < nCh; c++) {` |
| 809 | `cmp < → <=` | `for (let i = 0; i < len; i++) {` |
| 833 | `num → 0` | `const win = Math.min(sig.length, Math.max(Math.round(fs * 90), Math.roun` |
| 833 | `num → 0` | `const win = Math.min(sig.length, Math.max(Math.round(fs * 90), Math.roun` |
| 835 | `cmp < → <=` | `if (s0 < 0) s0 = 0;` |
| 836 | `bool && → \|\|` | `const slice = s0 === 0 && win === sig.length ? sig : sig.subarray(s0, s0` |
| 837 | `num → 0` | `const pulse = bandpass(slice, fs, 0.7, 3.0);` |
| 837 | `num → 0` | `const pulse = bandpass(slice, fs, 0.7, 3.0);` |
| 838 | `num → 0` | `const noise = bandpass(slice, fs, 4.0, 8.0);` |
| 838 | `num → 0` | `const noise = bandpass(slice, fs, 4.0, 8.0);` |
| 840 | `bool \|\| → &&` | `ns = std(noise) \|\| 1e-6;` |
| 860 | `bool \|\| → &&` | `if (!a \|\| !b \|\| a.length !== b.length) return false;` |
| 860 | `bool \|\| → &&` | `if (!a \|\| !b \|\| a.length !== b.length) return false;` |
| 861 | `cmp < → <=` | `for (let i = 0; i < a.length; i++) if (a[i] !== b[i]) return false;` |
| 900 | `cmp <= → <` | `if (refRate == null \|\| !isFinite(refRate) \|\| refRate <= 0) return refIdx` |
| 900 | `bool \|\| → &&` | `if (refRate == null \|\| !isFinite(refRate) \|\| refRate <= 0) return refIdx` |
| 900 | `bool \|\| → &&` | `if (refRate == null \|\| !isFinite(refRate) \|\| refRate <= 0) return refIdx` |
| 902 | `cmp < → <=` | `for (let i = 0; i < rates.length; i++) if (i !== refIdx && rates[i] != n` |
| 902 | `cmp > → >=` | `for (let i = 0; i < rates.length; i++) if (i !== refIdx && rates[i] != n` |
| 905 | `cmp > → >=` | `if (!(medOther > 0)) return refIdx;` |
| 909 | `cmp >= → >` | `if (!(spread < 0.15 && refRate / medOther >= 1.5)) return refIdx;` |
| 909 | `cmp < → <=` | `if (!(spread < 0.15 && refRate / medOther >= 1.5)) return refIdx;` |
| 913 | `cmp < → <=` | `for (let i = 0; i < rates.length; i++) {` |
| 914 | `cmp >= → >` | `if (rates[i] == null \|\| Math.abs(rates[i] / medOther - 1) >= 0.15) conti` |
| 915 | `bool && → \|\|` | `const s = snr && snr[i] != null ? snr[i] : 0;` |
| 916 | `cmp > → >=` | `if (s > bestSnr) {` |
| 921 | `cmp >= → >` | `return pick >= 0 ? pick : refIdx;` |
| 963 | `num → 0` | `const ORIENT_SAMPLE_SEC = 120; // ~100 beats at 50 bpm — enough for a st` |
| 970 | `cmp > → >=` | `if (n < ORIENT_MIN_BEATS \|\| !(det.T > 0)) return null;` |
| 970 | `bool \|\| → &&` | `if (n < ORIENT_MIN_BEATS \|\| !(det.T > 0)) return null;` |
| 970 | `cmp < → <=` | `if (n < ORIENT_MIN_BEATS \|\| !(det.T > 0)) return null;` |
| 972 | `cmp < → <=` | `for (var k = 0; k < n; k++) {` |
| 974 | `bool && → \|\|` | `if (isFinite(d) && d > 0) r.push(d);` |
| 974 | `cmp > → >=` | `if (isFinite(d) && d > 0) r.push(d);` |
| 976 | `cmp < → <=` | `if (r.length < ORIENT_MIN_BEATS) return null;` |
| 984 | `cmp <= → <` | `if (!(want > 0) \|\| n <= want) {` |
| 992 | `bool && → \|\|` | `if (up == null && dn == null) return orient(bp); // undecidable → previo` |
| 995 | `cmp < → <=` | `return dn < up ? -1 : 1;` |
| 1005 | `cmp < → <=` | `for (let i = 1; i < bp.length; i++) d[i] = bp[i] - bp[i - 1];` |
| 1051 | `cmp < → <=` | `if (!signs \|\| signs.length < 2) return 0;` |
| 1054 | `cmp < → <=` | `for (let i = 0; i < signs.length; i++) {` |
| 1060 | `cmp > → >=` | `return neg > pos ? -1 : 1;` |
| 1111 | `num → 0` | `const SUBH_FRAC = 0.4;` |
| 1116 | `cmp < → <=` | `if (n < WIN + HOP) return null;` |
| 1125 | `cmp < → <=` | `for (let i = 0; i < m; i++) x[i] = lp[i * D];` |
| 1126 | `num → 0` | `const lagMin = Math.max(2, Math.round(fsd * 0.33)); // 180 bpm ceiling` |
| 1132 | `cmp <= → <` | `for (let s = 0; s + wd <= m; s += hd) {` |
| 1151 | `cmp > → >=` | `if (r > best) {` |
| 1173 | `cmp > → >=` | `if (bl && best > 0) {` |
| 1173 | `bool && → \|\|` | `if (bl && best > 0) {` |
| 1176 | `cmp < → <=` | `if (cand < lagMin) continue;` |
| 1184 | `cmp <= → <` | `for (let d = -1; d <= 1; d++) {` |
| 1186 | `cmp >= → >` | `if (c2 >= lagMin && c2 <= lagMax && corr[c2] > bv) {` |
| 1186 | `cmp <= → <` | `if (c2 >= lagMin && c2 <= lagMax && corr[c2] > bv) {` |
| 1186 | `cmp > → >=` | `if (c2 >= lagMin && c2 <= lagMax && corr[c2] > bv) {` |
| 1191 | `cmp >= → >` | `if (bv >= SUBH_FRAC * best) {` |
| 1206 | `cmp < → <=` | `for (let i = 0; i < n; i++) {` |
| 1207 | `cmp > → >=` | `while (k < ws.length - 2 && i > ws[k + 1]) k++;` |
| 1207 | `cmp < → <=` | `while (k < ws.length - 2 && i > ws[k + 1]) k++;` |
| 1208 | `cmp <= → <` | `if (i <= ws[0]) out[i] = ts[0];` |
| 1209 | `cmp >= → >` | `else if (i >= ws[ws.length - 1]) out[i] = ts[ts.length - 1];` |
| 1225 | `cmp < → <=` | `if (n < 3) return { peaks, feet: [], T: Math.round(fs * 0.85) };` |
| 1234 | `cmp < → <=` | `for (let i = 1; i < n; i++) {` |
| 1236 | `cmp > → >=` | `z[i] = dv > 0 ? dv * dv : 0;` |
| 1253 | `cmp < → <=` | `while (i < n) {` |
| 1256 | `cmp < → <=` | `while (j < n && maPeak[j] > maBeat[j]) j++;` |
| 1256 | `cmp > → >=` | `while (j < n && maPeak[j] > maBeat[j]) j++;` |
| 1257 | `cmp >= → >` | `if (j - i >= minW) {` |
| 1263 | `cmp < → <=` | `for (let k = i; k < hi; k++) {` |
| 1265 | `cmp > → >=` | `if (v > sv) {` |
| 1274 | `cmp >= → >` | `if (peaks.length === 0 \|\| sp - peaks[peaks.length - 1] >= refr) peaks.pu` |
| 1275 | `cmp > → >=` | `else if (bp[sp] > bp[peaks[peaks.length - 1]]) peaks[peaks.length - 1] =` |
| 1283 | `cmp > → >=` | `if (peaks.length > 2) {` |
| 1285 | `cmp < → <=` | `for (let k = 1; k < peaks.length; k++) dd.push(peaks[k] - peaks[k - 1]);` |
| 1287 | `cmp > → >=` | `if (md > 0) T = Math.round(md);` |
| 1303 | `cmp < → <=` | `if (bp[j] < mv) {` |
| 1310 | `cmp < → <=` | `for (let j = mi; j < p; j++) {` |
| 1312 | `cmp > → >=` | `if (dv > msv) {` |
| 1318 | `cmp > → >=` | `if (msv > 1e-9) {` |
| 1328 | `cmp < → <=` | `for (let i = 0; i < x.length; i++) y[i] = -x[i];` |
| 1366 | `cmp < → <=` | `if (nCh < 2) {` |
| 1370 | `num → 0` | `const tol = Math.max(1, Math.round(0.05 * fs)); // ±50 ms agreement wind` |
| 1397 | `cmp <= → <` | `while (j < ev.length && ev[j].s - ev[j - 1].s <= tol) {` |
| 1414 | `num → 0` | `const refr = Math.round(fs * 0.3);` |
| 1418 | `cmp < → <=` | `if (peaks.length && rawPeaks[k] - peaks[peaks.length - 1] < refr) {` |
| 1419 | `cmp > → >=` | `if (refBp[rawPeaks[k]] > refBp[peaks[peaks.length - 1]]) {` |
| 1428 | `num → 0` | `let T = Math.round(fs * 0.85);` |
| 1429 | `cmp > → >=` | `if (peaks.length > 2) {` |
| 1431 | `cmp < → <=` | `for (let k = 1; k < peaks.length; k++) dd.push(peaks[k] - peaks[k - 1]);` |
| 1433 | `cmp > → >=` | `if (md > 0) T = Math.round(md);` |
| 1441 | `cmp < → <=` | `for (let i = 0; i < x.length; i++) {` |
| 1450 | `cmp < → <=` | `for (let i = i0; i < i1; i++) if (a[i] > m) m = a[i];` |
| 1450 | `cmp > → >=` | `for (let i = i0; i < i1; i++) if (a[i] > m) m = a[i];` |
| 1463 | `num → 0` | `const pre = Math.round(fs * 0.2),` |
| 1468 | `cmp < → <=` | `for (let k = 0; k < n; k++) {` |
| 1472 | `cmp < → <=` | `for (let j = 0; j < L; j++) {` |
| 1474 | `cmp >= → >` | `if (idx < 0 \|\| idx >= bp.length) {` |
| 1474 | `cmp < → <=` | `if (idx < 0 \|\| idx >= bp.length) {` |
| 1474 | `bool \|\| → &&` | `if (idx < 0 \|\| idx >= bp.length) {` |
| 1487 | `bool \|\| → &&` | `const r = mx - mn \|\| 1;` |
| 1489 | `cmp < → <=` | `for (let j = 0; j < b.length; j++) o[j] = (b[j] - mn) / r;` |
| 1495 | `cmp < → <=` | `for (let j = 0; j < L; j++) {` |
| 1510 | `num → 0` | `let q = Math.max(0, corr) * (0.4 + 0.6 * motFactor);` |
| 1517 | `bool && → \|\|` | `else if (regular && regular[k] != null) {` |
| 1541 | `cmp < → <=` | `if (n < 4) return peaks.map(() => null); // no local cadence to judge ag` |
| 1553 | `num → 0` | `out.push(Math.max(0.6, 1 - Math.min(1, d / 0.5) * 0.4));` |
| 1571 | `cmp < → <=` | `for (let i = 0; i < y.length; i++) {` |
| 1582 | `cmp < → <=` | `for (let i = 0; i < y.length && first === null; i++) if (!gap[i]) first ` |
| 1582 | `eq === → !==` | `for (let i = 0; i < y.length && first === null; i++) if (!gap[i]) first ` |
| 1582 | `negate: drop !` | `for (let i = 0; i < y.length && first === null; i++) if (!gap[i]) first ` |
| 1582 | `bool && → \|\|` | `for (let i = 0; i < y.length && first === null; i++) if (!gap[i]) first ` |
| 1583 | `eq !== → ===` | `for (let i = 0; i < y.length && gap[i]; i++) y[i] = first !== null ? fir` |
| 1583 | `cmp < → <=` | `for (let i = 0; i < y.length && gap[i]; i++) y[i] = first !== null ? fir` |
| 1583 | `bool && → \|\|` | `for (let i = 0; i < y.length && gap[i]; i++) y[i] = first !== null ? fir` |
| 1621 | `bool \|\| → &&` | `if (rmssdMs == null \|\| sdnnRobustMs == null) return false;` |
| 1622 | `bool \|\| → &&` | `if (!isFinite(rmssdMs) \|\| !isFinite(sdnnRobustMs) \|\| sdnnRobustMs <= 0) ` |
| 1629 | `cmp > → >=` | `if (!relSec \|\| !feet \|\| feet.length < 2 \|\| !(fs > 0)) return out;` |
| 1629 | `cmp < → <=` | `if (!relSec \|\| !feet \|\| feet.length < 2 \|\| !(fs > 0)) return out;` |
| 1629 | `bool \|\| → &&` | `if (!relSec \|\| !feet \|\| feet.length < 2 \|\| !(fs > 0)) return out;` |
| 1635 | `cmp < → <=` | `for (let i = 1; i < n; i++) {` |
| 1636 | `cmp > → >=` | `if (relSec[i] - relSec[i - 1] > maxStep) run++;` |
| 1640 | `cmp < → <=` | `for (let k = 0; k < nIntervals; k++) {` |
| 1643 | `bool \|\| → &&` | `if (a == null \|\| b == null) continue;` |
| 1646 | `cmp > → >=` | `if (i1 > i0 && prefix[i1] > prefix[i0]) out[k] = true;` |
| 1663 | `bool \|\| → &&` | `if (!rec \|\| rec.t0Ms == null \|\| !isFinite(rec.t0Ms)) return null;` |
| 1663 | `bool \|\| → &&` | `if (!rec \|\| rec.t0Ms == null \|\| !isFinite(rec.t0Ms)) return null;` |
| 1666 | `cmp > → >=` | `if (!relSec \|\| !relSec.length \|\| !(fs > 0)) return null;` |
| 1666 | `bool \|\| → &&` | `if (!relSec \|\| !relSec.length \|\| !(fs > 0)) return null;` |
| 1666 | `bool \|\| → &&` | `if (!relSec \|\| !relSec.length \|\| !(fs > 0)) return null;` |
| 1672 | `cmp < → <=` | `for (let i = 1; i < n; i++) {` |
| 1673 | `cmp > → >=` | `if (relSec[i] - relSec[i - 1] > maxStep) {` |
| 1680 | `bool && → \|\|` | `return typeof DexExport !== 'undefined' && DexExport && DexExport.covera` |
| 1680 | `bool && → \|\|` | `return typeof DexExport !== 'undefined' && DexExport && DexExport.covera` |
| 1699 | `bool && → \|\|` | `const f = feet && feet[k] != null ? Math.floor(feet[k]) : p;` |
| 1702 | `cmp <= → <` | `for (let i = lo; i <= hi; i++)` |
| 1725 | `bool \|\| → &&` | `const den = Math.sqrt(sa * sb) \|\| 1e-9;` |
| 1740 | `bool \|\| → &&` | `winSec = winSec \|\| 60;` |
| 1743 | `bool \|\| → &&` | `const t0 = t0Ms \|\| 0;` |
| 1744 | `num → 0` | `const secAbs = (k) => Math.floor((t0 + (peaks[k] / fs) * 1000) / 1000);` |
| 1744 | `num → 0` | `const secAbs = (k) => Math.floor((t0 + (peaks[k] / fs) * 1000) / 1000);` |
| 1745 | `cmp < → <=` | `if (n < 20) {` |
| 1745 | `num → 0` | `if (n < 20) {` |
| 1746 | `cmp < → <=` | `for (let k = 0; k < n; k++) out.set(secAbs(k), 1);` |
| 1752 | `cmp < → <=` | `if (S < 1) return out;` |
| 1755 | `cmp < → <=` | `for (let k = 0; k < n; k++) {` |
| 1757 | `cmp >= → >` | `if (s >= 0 && s < S) {` |
| 1757 | `bool && → \|\|` | `if (s >= 0 && s < S) {` |
| 1757 | `cmp < → <=` | `if (s >= 0 && s < S) {` |
| 1759 | `bool && → \|\|` | `qsum[s] += sqi && Number.isFinite(sqi[k]) ? sqi[k] : 1;` |
| 1769 | `cmp < → <=` | `for (let i = 0; i < S; i++) {` |
| 1777 | `cmp < → <=` | `while (lo < a) {` |
| 1783 | `cmp > → >=` | `winSqi[i] = cAcc > 0 ? qAcc / cAcc : 0;` |
| 1786 | `cmp > → >=` | `for (let i = 0; i < S; i++) if (winCnt[i] > 0) active.push(i);` |
| 1786 | `cmp < → <=` | `for (let i = 0; i < S; i++) if (winCnt[i] > 0) active.push(i);` |
| 1801 | `bool \|\| → &&` | `const madC = 1.4826 * (cAbs.length ? cAbs[cAbs.length >> 1] : 0) \|\| 1;` |
| 1801 | `num → 0` | `const madC = 1.4826 * (cAbs.length ? cAbs[cAbs.length >> 1] : 0) \|\| 1;` |
| 1802 | `bool \|\| → &&` | `const madQ = 1.4826 * (qAbs.length ? qAbs[qAbs.length >> 1] : 0) \|\| 1e-6` |
| 1802 | `num → 0` | `const madQ = 1.4826 * (qAbs.length ? qAbs[qAbs.length >> 1] : 0) \|\| 1e-6` |
| 1804 | `cmp >= → >` | `const trust = (z) => (z <= 0 ? 1 : z >= C ? 0 : (1 - (z / C) * (z / C)) ` |
| 1804 | `cmp <= → <` | `const trust = (z) => (z <= 0 ? 1 : z >= C ? 0 : (1 - (z / C) * (z / C)) ` |
| 1805 | `cmp < → <=` | `for (let i = 0; i < S; i++) {` |
| 1806 | `cmp <= → <` | `if (winCnt[i] <= 0) continue;` |
| 1832 | `num → 0` | `const PPI_SPINE_MARGIN_PP = 10;` |
| 1853 | `cmp <= → <` | `const inRange = rr.filter((v) => v >= 300 && v <= 2000);` |
| 1853 | `cmp >= → >` | `const inRange = rr.filter((v) => v >= 300 && v <= 2000);` |
| 1892 | `cmp >= → >` | `if (runBad >= RESEED_AFTER) {` |
| 1896 | `cmp <= → <` | `for (let k = lo; k <= i && k < rr.length; k++) if (rr[k] >= 300 && rr[k]` |
| 1896 | `cmp >= → >` | `for (let k = lo; k <= i && k < rr.length; k++) if (rr[k] >= 300 && rr[k]` |
| 1896 | `cmp <= → <` | `for (let k = lo; k <= i && k < rr.length; k++) if (rr[k] >= 300 && rr[k]` |
| 1896 | `cmp < → <=` | `for (let k = lo; k <= i && k < rr.length; k++) if (rr[k] >= 300 && rr[k]` |
| 1896 | `bool && → \|\|` | `for (let k = lo; k <= i && k < rr.length; k++) if (rr[k] >= 300 && rr[k]` |
| 1896 | `bool && → \|\|` | `for (let k = lo; k <= i && k < rr.length; k++) if (rr[k] >= 300 && rr[k]` |
| 1896 | `num → 0` | `for (let k = lo; k <= i && k < rr.length; k++) if (rr[k] >= 300 && rr[k]` |
| 1905 | `cmp > → >=` | `if (v < 300 \|\| v > 2000) bad = true;` |
| 1905 | `cmp < → <=` | `if (v < 300 \|\| v > 2000) bad = true;` |
| 1905 | `bool \|\| → &&` | `if (v < 300 \|\| v > 2000) bad = true;` |
| 1905 | `num → 0` | `if (v < 300 \|\| v > 2000) bad = true;` |
| 1906 | `cmp > → >=` | `else if (Math.abs(v - ref) / ref > PPI_ECTOPY_THR) bad = true; // >30% o` |
| 1945 | `cmp < → <=` | `if (N < 60 \|\| !tt \|\| tt.length !== N) return { events: [], index: null }` |
| 1945 | `bool \|\| → &&` | `if (N < 60 \|\| !tt \|\| tt.length !== N) return { events: [], index: null }` |
| 1945 | `bool \|\| → &&` | `if (N < 60 \|\| !tt \|\| tt.length !== N) return { events: [], index: null }` |
| 1945 | `num → 0` | `if (N < 60 \|\| !tt \|\| tt.length !== N) return { events: [], index: null }` |
| 1948 | `cmp < → <=` | `if (M < 120) return { events: [], index: null }; // < 2 min → no apnea-b` |
| 1951 | `cmp < → <=` | `for (let s = 0; s < M; s++) {` |
| 1952 | `cmp < → <=` | `while (j < N - 1 && tt[j + 1] < s) j++;` |
| 1952 | `cmp < → <=` | `while (j < N - 1 && tt[j + 1] < s) j++;` |
| 1956 | `cmp < → <=` | `for (let s = 0; s < M; s++) {` |
| 1959 | `cmp <= → <` | `for (let k = -2; k <= 2; k++) {` |
| 1961 | `cmp >= → >` | `if (u >= 0 && u < M) {` |
| 1961 | `cmp < → <=` | `if (u >= 0 && u < M) {` |
| 1961 | `bool && → \|\|` | `if (u >= 0 && u < M) {` |
| 1971 | `cmp < → <=` | `for (let s = 0; s < M; s++) {` |
| 1974 | `cmp <= → <` | `for (let k = -half; k <= half; k++) {` |
| 1976 | `cmp >= → >` | `if (u >= 0 && u < M) {` |
| 1976 | `cmp < → <=` | `if (u >= 0 && u < M) {` |
| 1976 | `bool && → \|\|` | `if (u >= 0 && u < M) {` |
| 1988 | `cmp < → <=` | `for (let s = 0; s < M; s++) res[s] = hiCut[s] - lo[s];` |
| 1990 | `cmp < → <=` | `for (let s = 0; s < M; s++) {` |
| 1993 | `num → 0` | `for (let k = -12; k <= 12; k++) {` |
| 1993 | `cmp <= → <` | `for (let k = -12; k <= 12; k++) {` |
| 1993 | `num → 0` | `for (let k = -12; k <= 12; k++) {` |
| 1995 | `cmp >= → >` | `if (u >= 0 && u < M) {` |
| 1995 | `cmp < → <=` | `if (u >= 0 && u < M) {` |
| 1995 | `bool && → \|\|` | `if (u >= 0 && u < M) {` |
| 2002 | `num → 0` | `const ENV_ON = 2.6; // bpm — sustained-oscillation gate (matches ECGDex)` |
| 2004 | `num → 0` | `let lastT = -100;` |
| 2005 | `cmp < → <=` | `for (let s = 8; s < M - 8; s++) {` |
| 2006 | `cmp < → <=` | `if (env[s] < ENV_ON) continue;` |
| 2007 | `cmp <= → <` | `if (res[s] < res[s - 1] && res[s] <= res[s + 1] && res[s] < -2.4) {` |
| 2007 | `cmp < → <=` | `if (res[s] < res[s - 1] && res[s] <= res[s + 1] && res[s] < -2.4) {` |
| 2007 | `cmp < → <=` | `if (res[s] < res[s - 1] && res[s] <= res[s + 1] && res[s] < -2.4) {` |
| 2007 | `bool && → \|\|` | `if (res[s] < res[s - 1] && res[s] <= res[s + 1] && res[s] < -2.4) {` |
| 2007 | `bool && → \|\|` | `if (res[s] < res[s - 1] && res[s] <= res[s + 1] && res[s] < -2.4) {` |
| 2007 | `num → 0` | `if (res[s] < res[s - 1] && res[s] <= res[s + 1] && res[s] < -2.4) {` |
| 2010 | `cmp < → <=` | `for (let u = s + 8; u < Math.min(M, s + 48); u++) {` |
| 2011 | `cmp > → >=` | `if (res[u] > pk) {` |
| 2018 | `cmp >= → >` | `if (amp >= 5 && period >= 14 && period <= 46 && s - lastT > 14) {` |
| 2018 | `cmp >= → >` | `if (amp >= 5 && period >= 14 && period <= 46 && s - lastT > 14) {` |
| 2018 | `cmp <= → <` | `if (amp >= 5 && period >= 14 && period <= 46 && s - lastT > 14) {` |
| 2018 | `cmp > → >=` | `if (amp >= 5 && period >= 14 && period <= 46 && s - lastT > 14) {` |
| 2018 | `bool && → \|\|` | `if (amp >= 5 && period >= 14 && period <= 46 && s - lastT > 14) {` |
| 2018 | `bool && → \|\|` | `if (amp >= 5 && period >= 14 && period <= 46 && s - lastT > 14) {` |
| 2018 | `num → 0` | `if (amp >= 5 && period >= 14 && period <= 46 && s - lastT > 14) {` |
| 2018 | `bool && → \|\|` | `if (amp >= 5 && period >= 14 && period <= 46 && s - lastT > 14) {` |
| 2018 | `num → 0` | `if (amp >= 5 && period >= 14 && period <= 46 && s - lastT > 14) {` |
| 2025 | `cmp > → >=` | `const index = hours > 0 ? +(events.length / hours).toFixed(1) : 0;` |
| 2035 | `cmp < → <=` | `if (nn.length < 2) return null;` |
| 2037 | `cmp >= → >` | `const base = keep.length >= 2 ? keep : nn; // never let the omission emp` |
| 2055 | `cmp < → <=` | `if (cnt < 1) {` |
| 2060 | `cmp > → >=` | `if (Math.abs(d) > 50) nn50++;` |
| 2069 | `num → 0` | `const b = Math.round(v / 7.8125);` |
| 2073 | `cmp > → >=` | `for (const k in bins) if (bins[k] > mx) mx = bins[k];` |
| 2086 | `cmp < → <=` | `if (d.length < 2) {` |
| 2116 | `cmp < → <=` | `for (let i = 0; i < N; i++) y[i] -= slope * t[i] + icpt;` |
| 2117 | `num → 0` | `const bands = { vlf: [0.003, 0.04], lf: [0.04, 0.15], hf: [0.15, 0.4] };` |
| 2117 | `num → 0` | `const bands = { vlf: [0.003, 0.04], lf: [0.04, 0.15], hf: [0.15, 0.4] };` |
| 2117 | `num → 0` | `const bands = { vlf: [0.003, 0.04], lf: [0.04, 0.15], hf: [0.15, 0.4] };` |
| 2135 | `cmp > → >=` | `const fNyq = _meanPPIs > 0 ? 1 / (2 * _meanPPIs) : fmax;` |
| 2143 | `cmp <= → <` | `for (let f = fmin; f <= fCal; f += df) {` |
| 2169 | `cmp < → <=` | `if (f >= bands.vlf[0] && f < bands.vlf[1]) vlf += pw;` |
| 2170 | `cmp < → <=` | `else if (f >= bands.lf[0] && f < bands.lf[1]) lf += pw;` |
| 2170 | `cmp >= → >` | `else if (f >= bands.lf[0] && f < bands.lf[1]) lf += pw;` |
| 2171 | `cmp < → <=` | `else if (f >= bands.hf[0] && f < bands.hf[1]) {` |
| 2171 | `cmp >= → >` | `else if (f >= bands.hf[0] && f < bands.hf[1]) {` |
| 2227 | `cmp < → <=` | `if (nn.length < 50) return null;` |
| 2227 | `num → 0` | `if (nn.length < 50) return null;` |
| 2232 | `cmp < → <=` | `for (let i = 0; i < N; i++) {` |
| 2237 | `cmp <= → <` | `for (let s = 4; s <= 16; s++) scales.push(s);` |
| 2242 | `cmp < → <=` | `if (nWin < 1) continue;` |
| 2297 | `cmp > → >=` | `if (N > MAXN) {` |
| 2300 | `cmp < → <=` | `for (let i = 0; i < N; i += stride) dec.push(nn[i]);` |
| 2323 | `cmp > → >=` | `if (nA > 0) {` |
| 2325 | `cmp < → <=` | `for (let i = 0; i < nA; i++) ord[i] = i;` |
| 2329 | `cmp < → <=` | `for (let a = 0; a < nA; a++) {` |
| 2331 | `cmp < → <=` | `for (let b = a + 1; b < nA; b++) {` |
| 2333 | `cmp > → >=` | `if (nn[j] - nn[i] > tol) break; // ordered ⇒ every later j is further st` |
| 2336 | `cmp > → >=` | `if (Math.abs(nn[i + k] - nn[j + k]) > tol) {` |
| 2341 | `negate: drop !` | `if (!ok) continue;` |
| 2343 | `cmp <= → <` | `if (Math.abs(nn[i + m] - nn[j + m]) <= tol) A++;` |
| 2348 | `cmp < → <=` | `for (let j = 0; j < t; j++) {` |
| 2350 | `cmp < → <=` | `for (let k = 0; k < m; k++) {` |
| 2351 | `cmp > → >=` | `if (Math.abs(nn[t + k] - nn[j + k]) > tol) {` |
| 2363 | `bool \|\| → &&` | `if (!B \|\| !A) return null;` |
| 2378 | `bool \|\| → &&` | `var p = String(headerLine \|\| '').split(';');` |
| 2388 | `cmp >= → >` | `return idx.x >= 0 && idx.y >= 0 && idx.z >= 0 ? idx : null;` |
| 2388 | `cmp >= → >` | `return idx.x >= 0 && idx.y >= 0 && idx.z >= 0 ? idx : null;` |
| 2388 | `cmp >= → >` | `return idx.x >= 0 && idx.y >= 0 && idx.z >= 0 ? idx : null;` |
| 2388 | `bool && → \|\|` | `return idx.x >= 0 && idx.y >= 0 && idx.z >= 0 ? idx : null;` |
| 2388 | `bool && → \|\|` | `return idx.x >= 0 && idx.y >= 0 && idx.z >= 0 ? idx : null;` |
| 2394 | `cmp < → <=` | `for (var k = 0; k < p.length; k++) {` |
| 2397 | `cmp < → <=` | `if (nums.length < 3) return null;` |
| 2408 | `negate: drop !` | `if (/timestamp/i.test(t) && !cols) {` |
| 2408 | `bool && → \|\|` | `if (/timestamp/i.test(t) && !cols) {` |
| 2413 | `cmp < → <=` | `if (p.length < 5) continue;` |
| 2414 | `bool \|\| → &&` | `const c = cols \|\| xyzColsByTail(p);` |
| 2420 | `cmp >= → >` | `const b = BigInt(p[c.ns >= 0 ? c.ns : 1].trim());` |
| 2421 | `eq === → !==` | `if (ns0 === null) ns0 = b;` |
| 2424 | `cmp >= → >` | `const ts = parseTimestamp(p[c.phone >= 0 ? c.phone : 0]);` |
| 2460 | `cmp < → <=` | `for (let i = 0; i < n; i++) {` |
| 2469 | `cmp < → <=` | `if (grid[c] < thr) continue;` |
| 2471 | `cmp <= → <` | `for (let k = c - localBins; k <= c + localBins; k++) {` |
| 2472 | `cmp < → <=` | `if (k >= 0 && k < n && grid[k] > grid[c]) {` |
| 2472 | `cmp >= → >` | `if (k >= 0 && k < n && grid[k] > grid[c]) {` |
| 2479 | `cmp < → <=` | `if (sec - last < minGapSec) continue;` |
| 2495 | `cmp > → >=` | `const has = (accRows && accRows.length > 5) \|\| (gyroRows && gyroRows.len` |
| 2495 | `cmp > → >=` | `const has = (accRows && accRows.length > 5) \|\| (gyroRows && gyroRows.len` |
| 2512 | `cmp < → <=` | `if (!rows \|\| rows.length < 2) return true;` |
| 2514 | `cmp < → <=` | `for (let i = 0; i < rows.length; i++) {` |
| 2517 | `cmp < → <=` | `if (v < prev) return false;` |
| 2526 | `bool && → \|\|` | `if (r.tMs != null && t0Ms != null) return (r.tMs - t0Ms) / 1000;` |
| 2541 | `cmp < → <=` | `if (!rows \|\| rows.length < 3) return null;` |
| 2547 | `cmp > → >=` | `if (prev != null && t > prev) d.push(t - prev);` |
| 2547 | `bool && → \|\|` | `if (prev != null && t > prev) d.push(t - prev);` |
| 2553 | `cmp > → >=` | `return isFinite(dm) && dm > 0 ? 1 / dm : null;` |
| 2553 | `bool && → \|\|` | `return isFinite(dm) && dm > 0 ? 1 / dm : null;` |
| 2559 | `cmp > → >=` | `if (accRows && accRows.length > 5) {` |
| 2604 | `cmp > → >=` | `if (gyroRows && gyroRows.length > 5) {` |
| 2607 | `num → 0` | `const p10 = sortedGy[Math.floor(sortedGy.length * 0.1)];` |
| 2608 | `cmp > → >=` | `gyroBias = Math.min(isFinite(p10) && p10 > 0 ? p10 : 0, GY_FULL / 4);` |
| 2608 | `bool && → \|\|` | `gyroBias = Math.min(isFinite(p10) && p10 > 0 ? p10 : 0, GY_FULL / 4);` |
| 2626 | `cmp >= → >` | `if (g >= 0 && g < nG) {` |
| 2626 | `cmp < → <=` | `if (g >= 0 && g < nG) {` |
| 2626 | `bool && → \|\|` | `if (g >= 0 && g < nG) {` |
| 2634 | `cmp >= → >` | `if (g >= 0 && g < nG) {` |
| 2634 | `cmp < → <=` | `if (g >= 0 && g < nG) {` |
| 2634 | `bool && → \|\|` | `if (g >= 0 && g < nG) {` |
| 2650 | `cmp < → <=` | `for (let i = 0; i < nG; i++) {` |
| 2658 | `cmp >= → >` | `return g >= 0 && g < nG ? sm[g] : 0;` |
| 2658 | `cmp < → <=` | `return g >= 0 && g < nG ? sm[g] : 0;` |
| 2658 | `bool && → \|\|` | `return g >= 0 && g < nG ? sm[g] : 0;` |
| 2666 | `cmp >= → >` | `return g >= 0 && g < nG && covCell[g] === 1;` |
| 2666 | `cmp < → <=` | `return g >= 0 && g < nG && covCell[g] === 1;` |
| 2669 | `cmp < → <=` | `for (let i = 0; i < nG; i++) if (covCell[i]) _nCov++;` |
| 2674 | `num → 0` | `const stride = Math.max(1, Math.floor(nG / 600));` |
| 2675 | `cmp < → <=` | `for (let i = 0; i < nG; i += stride) series.push({ x: (i * dt) / 60, y: ` |
| 2675 | `num → 0` | `for (let i = 0; i < nG; i += stride) series.push({ x: (i * dt) / 60, y: ` |
| 2693 | `cmp > → >=` | `const magHas = !!(magRows && magRows.length > 5);` |
| 2701 | `bool \|\| → &&` | `magState.base = median(magState.mag.filter(isFinite)) \|\| 0; // session \|` |
| 2706 | `bool \|\| → &&` | `const gn = Math.hypot(g.x, g.y, g.z) \|\| 1,` |
| 2717 | `cmp < → <=` | `if (Math.abs(ux) < 0.9) ax = 1;` |
| 2717 | `num → 0` | `if (Math.abs(ux) < 0.9) ax = 1;` |
| 2722 | `bool \|\| → &&` | `const e1n = Math.hypot(e1x, e1y, e1z) \|\| 1;` |
| 2729 | `num → 0` | `return (Math.atan2(hx * e2x + hy * e2y + hz * e2z, hx * e1x + hy * e1y +` |
| 2731 | `num → 0` | `return (Math.atan2(My, Mx) * 180) / Math.PI;` |
| 2733 | `cmp > → >=` | `if (accRows && accRows.length > 5) {` |
| 2734 | `num → 0` | `const w = Math.max(3, Math.round((accRows.length / durSec) * 2.0)); // ~` |
| 2745 | `cmp < → <=` | `for (let i = 0; i < ss.length; i += gStride) {` |
| 2746 | `cmp >= → >` | `if (ss[i] != null && ss[i] >= s0 && ss[i] < s1) {` |
| 2746 | `cmp < → <=` | `if (ss[i] != null && ss[i] >= s0 && ss[i] < s1) {` |
| 2756 | `negate: drop !` | `if (!magState.has) return null;` |
| 2760 | `cmp < → <=` | `for (let i = 0; i < magState.ss.length; i++) {` |
| 2762 | `cmp >= → >` | `if (s != null && s >= s0 && s < s1) {` |
| 2762 | `cmp < → <=` | `if (s != null && s >= s0 && s < s1) {` |
| 2762 | `bool && → \|\|` | `if (s != null && s >= s0 && s < s1) {` |
| 2762 | `bool && → \|\|` | `if (s != null && s >= s0 && s < s1) {` |
| 2768 | `cmp < → <=` | `if (sx.length < 3) return null;` |
| 2774 | `negate: drop !` | `if (!magState.has) return false;` |
| 2776 | `cmp < → <=` | `for (let i = 0; i < magState.ss.length; i++) {` |
| 2778 | `cmp < → <=` | `if (s != null && s >= s0 && s < s1) v.push(magState.mag[i]);` |
| 2778 | `cmp >= → >` | `if (s != null && s >= s0 && s < s1) v.push(magState.mag[i]);` |
| 2778 | `bool && → \|\|` | `if (s != null && s >= s0 && s < s1) v.push(magState.mag[i]);` |
| 2778 | `bool && → \|\|` | `if (s != null && s >= s0 && s < s1) v.push(magState.mag[i]);` |
| 2780 | `cmp < → <=` | `if (v.length < 3) return false;` |
| 2783 | `bool \|\| → &&` | `bg = magState.base \|\| 1;` |
| 2784 | `cmp > → >=` | `return !!(sd > Math.max(0.03, 0.04 * bg) \|\| Math.abs(md - bg) / bg > 0.2` |
| 2784 | `bool \|\| → &&` | `return !!(sd > Math.max(0.03, 0.04 * bg) \|\| Math.abs(md - bg) / bg > 0.2` |
| 2784 | `cmp > → >=` | `return !!(sd > Math.max(0.03, 0.04 * bg) \|\| Math.abs(md - bg) / bg > 0.2` |
| 2784 | `num → 0` | `return !!(sd > Math.max(0.03, 0.04 * bg) \|\| Math.abs(md - bg) / bg > 0.2` |
| 2784 | `num → 0` | `return !!(sd > Math.max(0.03, 0.04 * bg) \|\| Math.abs(md - bg) / bg > 0.2` |
| 2784 | `num → 0` | `return !!(sd > Math.max(0.03, 0.04 * bg) \|\| Math.abs(md - bg) / bg > 0.2` |
| 2793 | `cmp < → <=` | `for (let s = 0; s < durSec; s += 30) {` |
| 2793 | `num → 0` | `for (let s = 0; s < durSec; s += 30) {` |
| 2794 | `num → 0` | `const g = gravMed(s, s + 30);` |
| 2795 | `negate: drop !` | `if (!g) continue;` |
| 2797 | `eq === → !==` | `if (base === 'supine' \|\| base === 'prone' \|\| base === 'upright') {` |
| 2797 | `eq === → !==` | `if (base === 'supine' \|\| base === 'prone' \|\| base === 'upright') {` |
| 2797 | `eq === → !==` | `if (base === 'supine' \|\| base === 'prone' \|\| base === 'upright') {` |
| 2797 | `bool \|\| → &&` | `if (base === 'supine' \|\| base === 'prone' \|\| base === 'upright') {` |
| 2797 | `bool \|\| → &&` | `if (base === 'supine' \|\| base === 'prone' \|\| base === 'upright') {` |
| 2798 | `num → 0` | `const h = headingAtSec(s, s + 30);` |
| 2800 | `num → 0` | `const rad = (h * Math.PI) / 180;` |
| 2807 | `num → 0` | `if (cnt > 0) refHeading = (Math.atan2(sumS, sumC) * 180) / Math.PI;` |
| 2807 | `cmp > → >=` | `if (cnt > 0) refHeading = (Math.atan2(sumS, sumC) * 180) / Math.PI;` |
| 2814 | `num → 0` | `const need = (accRows.length / durSec) * 30; // need ≥30 s of gravity sa` |
| 2815 | `cmp < → <=` | `if (!g \|\| g.n < need / gStride) return { position: 'unknown', conf: 0, h` |
| 2820 | `eq === → !==` | `if (base === 'lateral' && heading != null && refHeading != null && !magI` |
| 2820 | `bool && → \|\|` | `if (base === 'lateral' && heading != null && refHeading != null && !magI` |
| 2820 | `bool && → \|\|` | `if (base === 'lateral' && heading != null && refHeading != null && !magI` |
| 2820 | `bool && → \|\|` | `if (base === 'lateral' && heading != null && refHeading != null && !magI` |
| 2820 | `negate: drop !` | `if (base === 'lateral' && heading != null && refHeading != null && !magI` |
| 2822 | `cmp > → >=` | `while (d > 180) d -= 360;` |
| 2822 | `num → 0` | `while (d > 180) d -= 360;` |
| 2822 | `num → 0` | `while (d > 180) d -= 360;` |
| 2823 | `cmp < → <=` | `while (d < -180) d += 360;` |
| 2823 | `num → 0` | `while (d < -180) d += 360;` |
| 2823 | `num → 0` | `while (d < -180) d += 360;` |
| 2824 | `cmp >= → >` | `if (Math.abs(d) >= 30) base = d >= 0 ? 'lateral_R' : 'lateral_L';` |
| 2824 | `cmp >= → >` | `if (Math.abs(d) >= 30) base = d >= 0 ? 'lateral_R' : 'lateral_L';` |
| 2824 | `num → 0` | `if (Math.abs(d) >= 30) base = d >= 0 ? 'lateral_R' : 'lateral_L';` |
| 2827 | `bool \|\| → &&` | `const gn = Math.hypot(g.x, g.y, g.z) \|\| 1,` |
| 2829 | `num → 0` | `let conf = Math.max(0, Math.min(1, (dom - 0.577) / (1 - 0.577)));` |
| 2829 | `num → 0` | `let conf = Math.max(0, Math.min(1, (dom - 0.577) / (1 - 0.577)));` |
| 2830 | `num → 0` | `conf *= Math.max(0.3, Math.min(1, (g.n * gStride) / need));` |
| 2831 | `eq === → !==` | `if (magState.has && !magInterf && (base === 'lateral_L' \|\| base === 'lat` |
| 2831 | `eq === → !==` | `if (magState.has && !magInterf && (base === 'lateral_L' \|\| base === 'lat` |
| 2831 | `bool && → \|\|` | `if (magState.has && !magInterf && (base === 'lateral_L' \|\| base === 'lat` |
| 2831 | `bool && → \|\|` | `if (magState.has && !magInterf && (base === 'lateral_L' \|\| base === 'lat` |
| 2831 | `negate: drop !` | `if (magState.has && !magInterf && (base === 'lateral_L' \|\| base === 'lat` |
| 2831 | `bool \|\| → &&` | `if (magState.has && !magInterf && (base === 'lateral_L' \|\| base === 'lat` |
| 2831 | `num → 0` | `if (magState.has && !magInterf && (base === 'lateral_L' \|\| base === 'lat` |
| 2831 | `num → 0` | `if (magState.has && !magInterf && (base === 'lateral_L' \|\| base === 'lat` |
| 2832 | `num → 0` | `if (magInterf) conf *= 0.6;` |
| 2833 | `num → 0` | `return { position: base, conf: r2(conf), heading: heading != null ? Math` |
| 2833 | `num → 0` | `return { position: base, conf: r2(conf), heading: heading != null ? Math` |
| 2833 | `num → 0` | `return { position: base, conf: r2(conf), heading: heading != null ? Math` |
| 2854 | `cmp > → >=` | `gyroBiasDps: gyroRows && gyroRows.length > 5 ? r2(gyroBias) : null,` |
| 2861 | `bool && → \|\|` | `refHeadingDeg: magState.has && magState.refHeading != null ? Math.round(` |
| 2861 | `num → 0` | `refHeadingDeg: magState.has && magState.refHeading != null ? Math.round(` |
| 2861 | `num → 0` | `refHeadingDeg: magState.has && magState.refHeading != null ? Math.round(` |
| 2861 | `num → 0` | `refHeadingDeg: magState.has && magState.refHeading != null ? Math.round(` |
| 2872 | `cmp >= → >` | `if (Math.abs(uz) >= 0.7) return uz > 0 ? 'supine' : 'prone';` |
| 2872 | `cmp > → >=` | `if (Math.abs(uz) >= 0.7) return uz > 0 ? 'supine' : 'prone';` |
| 2873 | `cmp >= → >` | `if (Math.abs(uy) >= 0.55) return 'upright';` |
| 2888 | `negate: drop !` | `if (!t) continue;` |
| 2890 | `cmp < → <=` | `if (p.length < 2) continue;` |
| 2892 | `negate: drop !` | `if (!isFinite(ppi)) continue;` |
| 2942 | `cmp < → <=` | `for (let i = 0; i < vals.length; i++) {` |
| 2943 | `num → 0` | `acc += vals[i] / 1000;` |
| 2979 | `num → 0` | `const ALLAN_MIN_PAIRS = 64; // below this the octave ladder has too few ` |
| 2990 | `cmp < → <=` | `if (N < 3) return [];` |
| 2992 | `cmp < → <=` | `for (let j = 0; j < N; j++) pre[j + 1] = pre[j] + y[j];` |
| 2994 | `cmp <= → <` | `for (let m = 1; 2 * m + 1 <= N; m *= 2) {` |
| 3002 | `cmp < → <=` | `if (cnt < 8) break; // an estimate from a handful of terms is wider than` |
| 3012 | `num → 0` | `[-0.75, 'white/flicker-phase', 'jitter — averages away fast'],` |
| 3013 | `num → 0` | `[-0.25, 'white-frequency', 'benign; averaging helps as √N'],` |
| 3014 | `num → 0` | `[0.25, 'flicker-frequency', 'A FLOOR — more averaging buys nothing'],` |
| 3015 | `num → 0` | `[0.75, 'random-walk-frequency', 'wanders; a longer fit is worse than a s` |
| 3025 | `cmp > → >=` | `const pts = (points \|\| []).filter((p) => p.adev > 0 && p.tau > 0);` |
| 3025 | `cmp > → >=` | `const pts = (points \|\| []).filter((p) => p.adev > 0 && p.tau > 0);` |
| 3025 | `bool && → \|\|` | `const pts = (points \|\| []).filter((p) => p.adev > 0 && p.tau > 0);` |
| 3026 | `cmp < → <=` | `if (pts.length < 3) return null; // two points fit any line and cannot b` |
| 3038 | `cmp > → >=` | `if (!(sxx > 0)) return null;` |
| 3042 | `cmp < → <=` | `for (let i = 0; i < k; i++) {` |
| 3086 | `cmp > → >=` | `const half = se > 0 ? 1.96 * se : 0;` |
| 3086 | `num → 0` | `const half = se > 0 ? 1.96 * se : 0;` |
| 3087 | `cmp < → <=` | `const straddled = edges.filter((e) => sl - half < e && e < sl + half);` |
| 3087 | `cmp < → <=` | `const straddled = edges.filter((e) => sl - half < e && e < sl + half);` |
| 3091 | `cmp < → <=` | `if (sl < Number(ALLAN_NOISE[i][0])) {` |
| 3103 | `cmp < → <=` | `for (let i = 0; i < ALLAN_NOISE.length; i++) {` |
| 3104 | `eq === → !==` | `const lo = i === 0 ? -Infinity : Number(ALLAN_NOISE[i - 1][0]);` |
| 3105 | `cmp > → >=` | `if (sl - half < Number(ALLAN_NOISE[i][0]) && sl + half > lo) cands.push(` |
| 3105 | `cmp < → <=` | `if (sl - half < Number(ALLAN_NOISE[i][0]) && sl + half > lo) cands.push(` |
| 3105 | `bool && → \|\|` | `if (sl - half < Number(ALLAN_NOISE[i][0]) && sl + half > lo) cands.push(` |
| 3107 | `cmp > → >=` | `if (sl + half > Number(ALLAN_NOISE[ALLAN_NOISE.length - 1][0])) cands.pu` |
| 3123 | `cmp < → <=` | `if (!selfSec \|\| !fwSec \|\| selfSec.length < ALLAN_MIN_PAIRS \|\| fwSec.leng` |
| 3123 | `cmp < → <=` | `if (!selfSec \|\| !fwSec \|\| selfSec.length < ALLAN_MIN_PAIRS \|\| fwSec.leng` |
| 3123 | `bool \|\| → &&` | `if (!selfSec \|\| !fwSec \|\| selfSec.length < ALLAN_MIN_PAIRS \|\| fwSec.leng` |
| 3127 | `cmp < → <=` | `for (let k = 0; k < selfSec.length; k++) {` |
| 3133 | `cmp < → <=` | `if (fwSec[mid] < t) lo = mid + 1;` |
| 3138 | `cmp < → <=` | `if (lo > 0 && Math.abs(fwSec[lo - 1] - t) < best) {` |
| 3138 | `cmp > → >=` | `if (lo > 0 && Math.abs(fwSec[lo - 1] - t) < best) {` |
| 3142 | `cmp < → <=` | `if (best < tol) {` |
| 3147 | `cmp < → <=` | `if (ph.length < ALLAN_MIN_PAIRS) return null;` |
| 3149 | `cmp > → >=` | `if (!(tau0 > 0)) return null;` |
| 3151 | `cmp < → <=` | `if (curve.length < 3) return null;` |
| 3153 | `bool && → \|\|` | `const cls = classifyAllan(fit && fit.slope, fit && fit.se, fit && fit.nT` |
| 3153 | `bool && → \|\|` | `const cls = classifyAllan(fit && fit.slope, fit && fit.se, fit && fit.nT` |
| 3156 | `cmp < → <=` | `for (const p of curve) if (p.adev < best.adev) best = p;` |
| 3159 | `num → 0` | `pairedPct: r1((100 * ph.length) / selfSec.length),` |
| 3234 | `bool \|\| → &&` | `for (const t of trains) if (!t \|\| t.length < TCH_MIN_TRIPLES) return nul` |
| 3234 | `cmp < → <=` | `for (const t of trains) if (!t \|\| t.length < TCH_MIN_TRIPLES) return nul` |
| 3241 | `cmp < → <=` | `if (arr[m] < t) lo = m + 1;` |
| 3246 | `cmp < → <=` | `if (lo > 0 && Math.abs(arr[lo - 1] - t) < b) {` |
| 3246 | `cmp > → >=` | `if (lo > 0 && Math.abs(arr[lo - 1] - t) < b) {` |
| 3262 | `cmp < → <=` | `if (b.d < tol && c.d < tol) {` |
| 3262 | `bool && → \|\|` | `if (b.d < tol && c.d < tol) {` |
| 3262 | `cmp < → <=` | `if (b.d < tol && c.d < tol) {` |
| 3268 | `cmp < → <=` | `if (A.length < TCH_MIN_TRIPLES) return null;` |
| 3270 | `cmp > → >=` | `if (!(tau0 > 0)) return null;` |
| 3276 | `cmp < → <=` | `if (nT < 3) return null;` |
| 3284 | `cmp < → <=` | `for (let c = 0; c < 3; c++) if (split[c] < 0) bad = true;` |
| 3284 | `cmp < → <=` | `for (let c = 0; c < 3; c++) if (split[c] < 0) bad = true;` |
| 3286 | `cmp >= → >` | `for (let c = 0; c < 3; c++) per[c].push({ tau: ab[i].tau, adev: split[c]` |
| 3293 | `cmp > → >=` | `for (const q of curve) if (q.adev != null && q.adev > 0) pts.push({ tau:` |
| 3293 | `bool && → \|\|` | `for (const q of curve) if (q.adev != null && q.adev > 0) pts.push({ tau:` |
| 3321 | `eq === → !==` | `polarityUnanimous: opts && opts.signs ? opts.signs.every((s) => s === op` |
| 3321 | `bool && → \|\|` | `polarityUnanimous: opts && opts.signs ? opts.signs.every((s) => s === op` |
| 3326 | `bool \|\| → &&` | `if (!rr \|\| rr.length < 2) return 0;` |
| 3326 | `cmp < → <=` | `if (!rr \|\| rr.length < 2) return 0;` |
| 3340 | `cmp > → >=` | `const dev = devicePPI.filter((d) => d.ppi > 300 && d.ppi < 2000 && (d.bl` |
| 3340 | `cmp < → <=` | `const dev = devicePPI.filter((d) => d.ppi > 300 && d.ppi < 2000 && (d.bl` |
| 3340 | `num → 0` | `const dev = devicePPI.filter((d) => d.ppi > 300 && d.ppi < 2000 && (d.bl` |
| 3341 | `cmp < → <=` | `if (dev.length < 3 \|\| selfNN.length < 3) return { hasData: true, filePre` |
| 3341 | `cmp < → <=` | `if (dev.length < 3 \|\| selfNN.length < 3) return { hasData: true, filePre` |
| 3341 | `bool \|\| → &&` | `if (dev.length < 3 \|\| selfNN.length < 3) return { hasData: true, filePre` |
| 3347 | `cmp < → <=` | `if (self.length < 3 \|\| devc.length < 3) return { hasData: true, filePres` |
| 3347 | `cmp < → <=` | `if (self.length < 3 \|\| devc.length < 3) return { hasData: true, filePres` |
| 3347 | `bool \|\| → &&` | `if (self.length < 3 \|\| devc.length < 3) return { hasData: true, filePres` |
| 3354 | `num → 0` | `const pct = (a, b) => (b ? r1((100 * Math.abs(a - b)) / b) : null);` |
| 3355 | `num → 0` | `const agree = 100 * (1 - Math.min(1, Math.abs(sM - dM) / dM));` |
| 3397 | `cmp < → <=` | `if (nn.length < 2) return epochs;` |
| 3400 | `cmp < → <=` | `for (let e0 = 0; e0 < tEnd; e0 += epLen) {` |
| 3402 | `cmp < → <=` | `for (let i = 0; i < tt.length; i++) {` |
| 3403 | `cmp >= → >` | `if (tt[i] >= e0 && tt[i] < e0 + epLen) idx.push(i);` |
| 3403 | `cmp < → <=` | `if (tt[i] >= e0 && tt[i] < e0 + epLen) idx.push(i);` |
| 3405 | `cmp < → <=` | `if (idx.length < 5) continue;` |
| 3472 | `cmp < → <=` | `return (x < 10 ? '0' : '') + x;` |
| 3472 | `num → 0` | `return (x < 10 ? '0' : '') + x;` |
| 3499 | `num → 0` | `P(45, 'Ranking optical channels…');` |
| 3507 | `num → 0` | `P(55, 'Optical beat detection · 3-LED (systolic feet)…');` |
| 3512 | `eq === → !==` | `const perChannelAll = rec._preChannels && rec._preChannels.length === re` |
| 3560 | `bool && → \|\|` | `const _chRates = perChannel.map((pc) => (pc && pc.peaks && pc.peaks.leng` |
| 3560 | `cmp > → >=` | `const _chRates = perChannel.map((pc) => (pc && pc.peaks && pc.peaks.leng` |
| 3560 | `cmp > → >=` | `const _chRates = perChannel.map((pc) => (pc && pc.peaks && pc.peaks.leng` |
| 3560 | `bool && → \|\|` | `const _chRates = perChannel.map((pc) => (pc && pc.peaks && pc.peaks.leng` |
| 3560 | `bool && → \|\|` | `const _chRates = perChannel.map((pc) => (pc && pc.peaks && pc.peaks.leng` |
| 3560 | `num → 0` | `const _chRates = perChannel.map((pc) => (pc && pc.peaks && pc.peaks.leng` |
| 3560 | `num → 0` | `const _chRates = perChannel.map((pc) => (pc && pc.peaks && pc.peaks.leng` |
| 3561 | `bool && → \|\|` | `const _chSnr = keepIdx.map((c) => (sel.scores && sel.scores[c] ? sel.sco` |
| 3599 | `cmp < → <=` | `for (let k = 0, prevKept = -1; k < cons.peaks.length; k++) {` |
| 3601 | `cmp >= → >` | `if (prevKept >= 0) bridged.push(k - prevKept > 1); // >= 1 beat removed ` |
| 3612 | `num → 0` | `P(62, 'Motion gate (ACC + GYRO)…');` |
| 3616 | `num → 0` | `P(68, 'Per-beat SQI (× 3-LED agreement)…');` |
| 3631 | `num → 0` | `P(74, 'PPI + HRV…');` |
| 3660 | `num → 0` | `const _rateFoot = _ppiFoot.rr.length ? (100 * _corrFoot.nCorr) / _ppiFoo` |
| 3660 | `num → 0` | `const _rateFoot = _ppiFoot.rr.length ? (100 * _corrFoot.nCorr) / _ppiFoo` |
| 3661 | `num → 0` | `const _ratePeak = _ppiPeak.rr.length ? (100 * _corrPeak.nCorr) / _ppiPea` |
| 3661 | `num → 0` | `const _ratePeak = _ppiPeak.rr.length ? (100 * _corrPeak.nCorr) / _ppiPea` |
| 3662 | `cmp < → <=` | `const footSpineOK = !(_ratePeak < _rateFoot - PPI_SPINE_MARGIN_PP);` |
| 3665 | `bool && → \|\|` | `const ppiAgreement = _mFoot > 0 && _mPeak > 0 ? Math.min(_mFoot, _mPeak)` |
| 3665 | `cmp > → >=` | `const ppiAgreement = _mFoot > 0 && _mPeak > 0 ? Math.min(_mFoot, _mPeak)` |
| 3665 | `cmp > → >=` | `const ppiAgreement = _mFoot > 0 && _mPeak > 0 ? Math.min(_mFoot, _mPeak)` |
| 3686 | `bool \|\| → &&` | `rec.t0Ms \|\| 0` |
| 3688 | `bool \|\| → &&` | `const _t0 = rec.t0Ms \|\| 0;` |
| 3690 | `num → 0` | `const c = _pConfMap.get(Math.floor((_t0 + s * 1000) / 1000));` |
| 3690 | `num → 0` | `const c = _pConfMap.get(Math.floor((_t0 + s * 1000) / 1000));` |
| 3713 | `cmp < → <=` | `for (let i = 0; i < nn.length; i++) spansGap[i] = !!spansTime[i] \|\| !!(b` |
| 3716 | `cmp < → <=` | `for (let i = 0; i < nn.length; i++) {` |
| 3720 | `cmp >= → >` | `cleanMask[i] = corr.flags[i] === 0 && q0 >= 0.5 && q1 >= 0.5 && !spansGa` |
| 3720 | `cmp >= → >` | `cleanMask[i] = corr.flags[i] === 0 && q0 >= 0.5 && q1 >= 0.5 && !spansGa` |
| 3720 | `num → 0` | `cleanMask[i] = corr.flags[i] === 0 && q0 >= 0.5 && q1 >= 0.5 && !spansGa` |
| 3725 | `cmp < → <=` | `for (let i = 0; i < nn.length; i++) {` |
| 3728 | `bool && → \|\|` | `agreeI[i] = a0 != null && a1 != null ? (a0 + a1) / 2 : a0 != null ? a0 :` |
| 3750 | `cmp < → <=` | `for (let i = 0; i < nn.length; i++) {` |
| 3763 | `cmp > → >=` | `const perfWindow = () => (dc > 0 ? r2((100 * acAmp) / dc) : null);` |
| 3765 | `num → 0` | `P(80, 'Epochs…');` |
| 3770 | `cmp >= → >` | `const cleanBeats = sqi.filter((s) => s >= 0.5).length;` |
| 3772 | `cmp > → >=` | `const motionRejected = motion.hasData ? det.peaks.filter((p, k) => /** @` |
| 3772 | `num → 0` | `const motionRejected = motion.hasData ? det.peaks.filter((p, k) => /** @` |
| 3773 | `num → 0` | `const motionRejectedPct = det.peaks.length ? r1((100 * motionRejected) /` |
| 3774 | `num → 0` | `const correctionRate = rr.length ? r1((100 * corr.nCorr) / rr.length) : ` |
| 3780 | `bool && → \|\|` | `const magInterferencePct = motion.hasMag && magKnown.length ? Math.round` |
| 3780 | `num → 0` | `const magInterferencePct = motion.hasMag && magKnown.length ? Math.round` |
| 3816 | `bool && → \|\|` | `const segAll = epochs.map((e) => e.sdnn).filter((v) => v != null && isFi` |
| 3839 | `cmp >= → >` | `const gatedEp = epochs.filter((e) => e.sdnn != null && isFinite(e.sdnn) ` |
| 3839 | `cmp <= → <` | `const gatedEp = epochs.filter((e) => e.sdnn != null && isFinite(e.sdnn) ` |
| 3839 | `num → 0` | `const gatedEp = epochs.filter((e) => e.sdnn != null && isFinite(e.sdnn) ` |
| 3840 | `bool && → \|\|` | `const usable = gatedEp.length >= 3 ? gatedEp : epochs.filter((e) => e.sd` |
| 3852 | `bool && → \|\|` | `if (sd1 != null && sdnnRobust != null) sd2Robust = r1(Math.sqrt(Math.max` |
| 3854 | `bool && → \|\|` | `const lfA = usable.map((e) => e.lf).filter((v) => v != null && isFinite(` |
| 3855 | `bool && → \|\|` | `const hfA = usable.map((e) => e.hf).filter((v) => v != null && isFinite(` |
| 3856 | `bool && → \|\|` | `const lhA = usable.map((e) => e.lfhf).filter((v) => v != null && isFinit` |
| 3857 | `bool && → \|\|` | `const vlA = usable.map((e) => e.vlf).filter((v) => v != null && isFinite` |
| 3858 | `cmp >= → >` | `if (lfA.length >= 3) lfRobust = r1(median(lfA));` |
| 3859 | `cmp >= → >` | `if (hfA.length >= 3) hfRobust = r1(median(hfA));` |
| 3860 | `cmp >= → >` | `if (lhA.length >= 3) lfhfRobust = r2(median(lhA));` |
| 3861 | `cmp >= → >` | `if (vlA.length >= 3) vlfRobust = r1(median(vlA));` |
| 3863 | `bool && → \|\|` | `if (vlfRobust != null && lfRobust != null && hfRobust != null) tpRobust ` |
| 3863 | `bool && → \|\|` | `if (vlfRobust != null && lfRobust != null && hfRobust != null) tpRobust ` |
| 3867 | `num → 0` | `const MOT_STRICT = 0.15;` |
| 3875 | `cmp <= → <` | `const lowMot = motKnown.filter((e) => e.motionIndex <= MOT_STRICT);` |
| 3876 | `bool && → \|\|` | `const hfLM = lowMot.map((e) => e.hf).filter((v) => v != null && isFinite` |
| 3877 | `cmp >= → >` | `if (hfLM.length >= 3) hfRobustLowMotion = r1(median(hfLM));` |
| 3892 | `eq === → !==` | `if (epochs[i].position === 'unknown' \|\| epochs[i - 1].position === 'unkn` |
| 3892 | `bool \|\| → &&` | `if (epochs[i].position === 'unknown' \|\| epochs[i - 1].position === 'unkn` |
| 3892 | `eq === → !==` | `if (epochs[i].position === 'unknown' \|\| epochs[i - 1].position === 'unkn` |
| 3894 | `eq !== → ===` | `if (epochs[i].position !== epochs[i - 1].position) posShift++;` |
| 3897 | `num → 0` | `const durFactor = Math.max(0, Math.min(1, rec.durSec / 60 / 60)); // VLF` |
| 3897 | `num → 0` | `const durFactor = Math.max(0, Math.min(1, rec.durSec / 60 / 60)); // VLF` |
| 3908 | `cmp < → <=` | `motion: motKnown.length === 0 ? 'none' : motKnown.length < epochs.length` |
| 3909 | `cmp < → <=` | `posture: posPairs === 0 ? 'none' : posPairs < epochs.length - 1 ? 'parti` |
| 3917 | `num → 0` | `sdnn: qPosture == null ? null : c(qCov * qCorr * (0.6 + 0.4 * qPosture))` |
| 3917 | `num → 0` | `sdnn: qPosture == null ? null : c(qCov * qCorr * (0.6 + 0.4 * qPosture))` |
| 3921 | `num → 0` | `vlf: qPosture == null ? null : c(Math.min(0.7, qPosture * durFactor)),` |
| 3928 | `bool && → \|\|` | `motionCoveredFrac: motion.hasData && motion.motionCoveredFrac != null ? ` |
| 3941 | `cmp > → >=` | `ledAgree3of3Pct = cons.kept33 + cons.kept22 > 0 ? Math.round((100 * cons` |
| 3941 | `num → 0` | `ledAgree3of3Pct = cons.kept33 + cons.kept22 > 0 ? Math.round((100 * cons` |
| 3942 | `num → 0` | `const epLen = 300,` |
| 3948 | `cmp >= → >` | `if (c.nAgree >= 3) bins[e].c3++;` |
| 3949 | `eq === → !==` | `else if (c.nAgree === 2) bins[e].c2++;` |
| 3955 | `bool \|\| → &&` | `tot = b.c1 + b.c2 + b.c3 \|\| 1;` |
| 3987 | `bool \|\| → &&` | `const hrvLowConfidence = analyzablePct < 60 \|\| correctionRate > 20 \|\| hr` |
| 3987 | `cmp > → >=` | `const hrvLowConfidence = analyzablePct < 60 \|\| correctionRate > 20 \|\| hr` |
| 3987 | `cmp < → <=` | `const hrvLowConfidence = analyzablePct < 60 \|\| correctionRate > 20 \|\| hr` |
| 3987 | `num → 0` | `const hrvLowConfidence = analyzablePct < 60 \|\| correctionRate > 20 \|\| hr` |
| 3987 | `bool \|\| → &&` | `const hrvLowConfidence = analyzablePct < 60 \|\| correctionRate > 20 \|\| hr` |
| 3987 | `num → 0` | `const hrvLowConfidence = analyzablePct < 60 \|\| correctionRate > 20 \|\| hr` |
| 4002 | `cmp < → <=` | `if (durMin < 1) {` |
| 4004 | `cmp < → <=` | `} else if (durMin < 5) {` |
| 4006 | `cmp < → <=` | `} else if (durMin < 90) {` |
| 4006 | `num → 0` | `} else if (durMin < 90) {` |
| 4009 | `num → 0` | `const longRec = durMin >= 90;` |
| 4009 | `cmp >= → >` | `const longRec = durMin >= 90;` |
| 4039 | `cmp > → >=` | `if ((!ppiSeries \|\| !ppiSeries.length) && rec.beatMarkerSec && rec.beatMa` |
| 4039 | `negate: drop !` | `if ((!ppiSeries \|\| !ppiSeries.length) && rec.beatMarkerSec && rec.beatMa` |
| 4041 | `cmp < → <=` | `for (let i = 1; i < rec.beatMarkerSec.length; i++) {` |
| 4044 | `num → 0` | `mk.push({ ppi: (rec.beatMarkerSec[i] - rec.beatMarkerSec[i - 1]) * 1000,` |
| 4056 | `bool && → \|\|` | `if (validation && validation.usable && ppiSource === 'o2ring-marker' && ` |
| 4056 | `eq === → !==` | `if (validation && validation.usable && ppiSource === 'o2ring-marker' && ` |
| 4056 | `bool && → \|\|` | `if (validation && validation.usable && ppiSource === 'o2ring-marker' && ` |
| 4066 | `num → 0` | `P(92, 'Finalising…');` |
| 4067 | `bool && → \|\|` | `const dispHr = longRec && epochs.length ? median(epochs.map((e) => e.hr)` |
| 4070 | `bool \|\| → &&` | `source: rec.source \|\| 'file',` |
| 4071 | `bool \|\| → &&` | `fname: rec.fname \|\| '',` |
| 4183 | `bool \|\| → &&` | `siteSource: rec.siteSource \|\| 'device-default',` |
| 4206 | `bool \|\| → &&` | `sentinelRejected: rec.sentinelRejected \|\| 0,` |
| 4207 | `bool \|\| → &&` | `sentinelKept: rec.sentinelKept \|\| 0,` |
| 4247 | `bool \|\| → &&` | `if (!sqi \|\| !sqi.length \|\| !peakSec) return null;` |
| 4247 | `bool \|\| → &&` | `if (!sqi \|\| !sqi.length \|\| !peakSec) return null;` |
| 4247 | `negate: drop !` | `if (!sqi \|\| !sqi.length \|\| !peakSec) return null;` |
| 4247 | `negate: drop !` | `if (!sqi \|\| !sqi.length \|\| !peakSec) return null;` |
| 4247 | `negate: drop !` | `if (!sqi \|\| !sqi.length \|\| !peakSec) return null;` |
| 4250 | `cmp < → <=` | `for (let k = 0; k < peakSec.length; k++) {` |
| 4251 | `bool && → \|\|` | `if (peakSec[k] != null && Math.abs(peakSec[k] - relSec) < 5) {` |
| 4251 | `cmp < → <=` | `if (peakSec[k] != null && Math.abs(peakSec[k] - relSec) < 5) {` |
| 4260 | `bool \|\| → &&` | `const surgeConf = (ampBpm) => r2(Math.max(0.45, Math.min(0.9, 0.45 + Mat` |
| 4260 | `num → 0` | `const surgeConf = (ampBpm) => r2(Math.max(0.45, Math.min(0.9, 0.45 + Mat` |
| 4260 | `num → 0` | `const surgeConf = (ampBpm) => r2(Math.max(0.45, Math.min(0.9, 0.45 + Mat` |
| 4260 | `num → 0` | `const surgeConf = (ampBpm) => r2(Math.max(0.45, Math.min(0.9, 0.45 + Mat` |
| 4260 | `num → 0` | `const surgeConf = (ampBpm) => r2(Math.max(0.45, Math.min(0.9, 0.45 + Mat` |
| 4260 | `num → 0` | `const surgeConf = (ampBpm) => r2(Math.max(0.45, Math.min(0.9, 0.45 + Mat` |
| 4262 | `num → 0` | `const tMs = t0Ms != null ? t0Ms + Math.round(relSec * 1000) : null;` |
| 4263 | `bool \|\| → &&` | `ev.push({ t: fmtClockSec(tMs != null ? tMs : relSec * 1000), tMs, impuls` |
| 4263 | `num → 0` | `ev.push({ t: fmtClockSec(tMs != null ? tMs : relSec * 1000), tMs, impuls` |
| 4269 | `bool && → \|\|` | `if (a.rmssd && b.rmssd) {` |
| 4271 | `cmp > → >=` | `if (drop > 0.35)` |
| 4271 | `num → 0` | `if (drop > 0.35)` |
| 4273 | `num → 0` | `b.tMin * 60,` |
| 4275 | `num → 0` | `0.7,` |
| 4279 | `eq !== → ===` | `position: b.position && b.position !== 'unknown' ? b.position : null,` |
| 4279 | `bool && → \|\|` | `position: b.position && b.position !== 'unknown' ? b.position : null,` |
| 4283 | `num → 0` | `sqiAt(b.tMin * 60)` |
| 4286 | `cmp > → >=` | `if (a.hr && b.hr && b.hr - a.hr > 8) {` |
| 4286 | `bool && → \|\|` | `if (a.hr && b.hr && b.hr - a.hr > 8) {` |
| 4286 | `bool && → \|\|` | `if (a.hr && b.hr && b.hr - a.hr > 8) {` |
| 4289 | `num → 0` | `b.tMin * 60,` |
| 4294 | `eq !== → ===` | `position: b.position && b.position !== 'unknown' ? b.position : null,` |
| 4294 | `bool && → \|\|` | `position: b.position && b.position !== 'unknown' ? b.position : null,` |
| 4298 | `num → 0` | `sqiAt(b.tMin * 60)` |
| 4305 | `cmp < → <=` | `for (let k = 0; k < det.peaks.length; k++) {` |
| 4306 | `cmp > → >=` | `const hi = peakSec[k] != null && motion.motionAtSec(peakSec[k]) > 0.5;` |
| 4306 | `bool && → \|\|` | `const hi = peakSec[k] != null && motion.motionAtSec(peakSec[k]) > 0.5;` |
| 4306 | `num → 0` | `const hi = peakSec[k] != null && motion.motionAtSec(peakSec[k]) > 0.5;` |
| 4307 | `bool && → \|\|` | `if (hi && runStart === null) runStart = peakSec[k];` |
| 4307 | `eq === → !==` | `if (hi && runStart === null) runStart = peakSec[k];` |
| 4308 | `eq === → !==` | `if ((!hi \|\| k === det.peaks.length - 1) && runStart !== null) {` |
| 4308 | `bool && → \|\|` | `if ((!hi \|\| k === det.peaks.length - 1) && runStart !== null) {` |
| 4308 | `eq !== → ===` | `if ((!hi \|\| k === det.peaks.length - 1) && runStart !== null) {` |
| 4308 | `bool \|\| → &&` | `if ((!hi \|\| k === det.peaks.length - 1) && runStart !== null) {` |
| 4308 | `negate: drop !` | `if ((!hi \|\| k === det.peaks.length - 1) && runStart !== null) {` |
| 4309 | `num → 0` | `evt(runStart, 'motion_artifact_segment', 0.3, {}, sqiAt(runStart));` |
| 4327 | `bool && → \|\|` | `if (motion && motion.hasData && motion.grid && motion.dt) {` |
| 4327 | `bool && → \|\|` | `if (motion && motion.hasData && motion.grid && motion.dt) {` |
| 4327 | `bool && → \|\|` | `if (motion && motion.hasData && motion.grid && motion.dt) {` |
| 4328 | `bool \|\| → &&` | `const onsets = movementOnsets(motion.onsetGrid \|\| motion.grid, motion.dt` |
| 4333 | `num → 0` | `for (const sec of onsets) evt(sec, 'movement_onset', 0.6, { streams: str` |
| 4404 | `bool \|\| → &&` | `if (typeof Blob === 'undefined' \|\| typeof URL === 'undefined' \|\| !URL.cr` |
| 4404 | `eq === → !==` | `if (typeof Blob === 'undefined' \|\| typeof URL === 'undefined' \|\| !URL.cr` |
| 4404 | `bool \|\| → &&` | `if (typeof Blob === 'undefined' \|\| typeof URL === 'undefined' \|\| !URL.cr` |
| 4404 | `eq === → !==` | `if (typeof Blob === 'undefined' \|\| typeof URL === 'undefined' \|\| !URL.cr` |
| 4404 | `negate: drop !` | `if (typeof Blob === 'undefined' \|\| typeof URL === 'undefined' \|\| !URL.cr` |
| 4456 | `eq !== → ===` | `var url = typeof Worker !== 'undefined' ? _buildWorkerURL() : null;` |
| 4457 | `bool \|\| → &&` | `if (!url \|\| !nCh) {` |
| 4457 | `negate: drop !` | `if (!url \|\| !nCh) {` |
| 4457 | `negate: drop !` | `if (!url \|\| !nCh) {` |
| 4476 | `cmp < → <=` | `for (var c = 0; c < nCh; c++) {` |
| 4486 | `bool && → \|\|` | `if (++done === nCh && !settled) {` |
| 4486 | `eq === → !==` | `if (++done === nCh && !settled) {` |
| 4486 | `negate: drop !` | `if (++done === nCh && !settled) {` |
| 4502 | `negate: drop !` | `if (!settled) serialFallback();` |
| 4503 | `num → 0` | `}, 20000); // stall guard` |
| 4605 | `bool \|\| → &&` | `? { version: (opts.kernel.version != null ? opts.kernel.version : opts.k` |
| 4605 | `bool \|\| → &&` | `? { version: (opts.kernel.version != null ? opts.kernel.version : opts.k` |
| 4613 | `bool \|\| → &&` | `generated: opts.generated \|\| new Date().toISOString(),` |
| 4614 | `bool \|\| → &&` | `provenance: opts.provenance \|\| null,` |
| 4622 | `eq !== → ===` | `typeof SignalFrame !== 'undefined' && SignalFrame && SignalFrame.compute` |
| 4622 | `bool && → \|\|` | `typeof SignalFrame !== 'undefined' && SignalFrame && SignalFrame.compute` |
| 4622 | `bool && → \|\|` | `typeof SignalFrame !== 'undefined' && SignalFrame && SignalFrame.compute` |
| 4622 | `bool && → \|\|` | `typeof SignalFrame !== 'undefined' && SignalFrame && SignalFrame.compute` |
| 4622 | `bool && → \|\|` | `typeof SignalFrame !== 'undefined' && SignalFrame && SignalFrame.compute` |
| 4645 | `bool && → \|\|` | `durSec: r.durSec != null && isFinite(r.durSec) ? r.durSec : null,` |
| 4655 | `bool \|\| → &&` | `siteSource: r.siteSource \|\| 'device-default'` |
| 4745 | `eq === → !==` | `return v == null \|\| (typeof v === 'number' && !isFinite(v)) ? null : v;` |
| 4745 | `bool \|\| → &&` | `return v == null \|\| (typeof v === 'number' && !isFinite(v)) ? null : v;` |
| 4793 | `bool \|\| → &&` | `lowConfidenceReason: r.hrvLowConfidenceReason \|\| null,` |
| 4815 | `bool && → \|\|` | `const epochScale = r.lfRobust != null && r.hfRobust != null;` |
| 4837 | `bool && → \|\|` | `respRateMethod: fq && fq.respRate != null ? 'RSA (HF-peak of RR spectrum` |
| 4882 | `bool \|\| → &&` | `position: e.position \|\| 'unknown',` |
| 4892 | `bool && → \|\|` | `if (r.nn && r.nn.length && r.tt && r.tt.length === r.nn.length) {` |
| 4892 | `bool && → \|\|` | `if (r.nn && r.nn.length && r.tt && r.tt.length === r.nn.length) {` |
| 4892 | `bool && → \|\|` | `if (r.nn && r.nn.length && r.tt && r.tt.length === r.nn.length) {` |
| 4908 | `bool && → \|\|` | `r.ppiFlags && r.ppiFlags.length === r.nn.length` |
| 4921 | `bool && → \|\|` | `conf: r.ppiConf && r.ppiConf.length === r.nn.length ? r.ppiConf.slice() ` |
| 4949 | `cmp < → <=` | `for (var i = 0; i < n; i++) relSec[i] = i / (fs \|\| 1);` |
| 4949 | `bool \|\| → &&` | `for (var i = 0; i < n; i++) relSec[i] = i / (fs \|\| 1);` |
| 4953 | `bool \|\| → &&` | `amb: s.amb \|\| null,` |
| 4959 | `cmp > → >=` | `durSec: s.durSec != null ? s.durSec : n > 1 ? (n - 1) / (fs \|\| 1) : 0,` |
| 4959 | `bool \|\| → &&` | `durSec: s.durSec != null ? s.durSec : n > 1 ? (n - 1) / (fs \|\| 1) : 0,` |
| 4965 | `bool \|\| → &&` | `siteSource: s.siteSource \|\| 'device-default',` |
| 4966 | `bool \|\| → &&` | `acc: input.acc \|\| null,` |
| 4967 | `bool \|\| → &&` | `gyro: input.gyro \|\| null,` |
| 4968 | `bool \|\| → &&` | `magn: input.magn \|\| null,` |
| 4969 | `bool \|\| → &&` | `devicePPI: input.devicePPI \|\| null,` |
| 4970 | `bool \|\| → &&` | `markers: input.markers \|\| null` |
| 4976 | `eq === → !==` | `typeof input === 'string' ? input : input && typeof input.text === 'stri` |
| 4976 | `bool && → \|\|` | `typeof input === 'string' ? input : input && typeof input.text === 'stri` |
| 4976 | `bool && → \|\|` | `typeof input === 'string' ? input : input && typeof input.text === 'stri` |
| 4976 | `bool && → \|\|` | `typeof input === 'string' ? input : input && typeof input.text === 'stri` |
| 4981 | `bool && → \|\|` | `if (opts.fname && !rec.fname) rec.fname = opts.fname;` |
| 4981 | `negate: drop !` | `if (opts.fname && !rec.fname) rec.fname = opts.fname;` |
| 5047 | `bool && → \|\|` | `if (global.DexExport && typeof global.DexExport.scrubExport === 'functio` |
| 5047 | `eq === → !==` | `if (global.DexExport && typeof global.DexExport.scrubExport === 'functio` |
| 5048 | `eq === → !==` | `if (typeof global.dexScrubExport === 'function') return global.dexScrubE` |

## `pulsedex-dsp.js` — 271 survivor(s)

| line | operator | source |
|---:|---|---|
| 63 | `cmp < → <=` | `if (a.length < 2) return 0;` |
| 79 | `cmp < → <=` | `for (let i = 1; i < a.length; i++) if (Math.abs(a[i] - a[i - 1]) > 50) n` |
| 84 | `cmp < → <=` | `for (let i = 1; i < a.length; i++) if (Math.abs(a[i] - a[i - 1]) > 50) n` |
| 91 | `cmp < → <=` | `if (v < mn) mn = v;` |
| 92 | `cmp > → >=` | `if (v > mx) mx = v;` |
| 115 | `cmp <= → <` | `const amo50 = (a, mo) => (a.filter((v) => Math.abs(v - mo) <= 25).length` |
| 124 | `cmp < → <=` | `if (a.length < 3) return 0;` |
| 197 | `cmp <= → <` | `return elevM <= 1500 ? 1 : Math.max(0.55, 1 - ((elevM - 1500) / 300) * 0` |
| 204 | `cmp < → <=` | `if (N < 120) return null;` |
| 207 | `cmp < → <=` | `for (let i = 0; i < N; i++) {` |
| 208 | `num → 0` | `t.push(acc / 1000);` |
| 215 | `num → 0` | `const nf = 48,` |
| 217 | `cmp < → <=` | `for (let k = 0; k < nf; k++) {` |
| 223 | `cmp < → <=` | `for (let i = 0; i < N; i++) {` |
| 231 | `bool \|\| → &&` | `p += ((cs * cs) / (cc \|\| 1) + (ss * ss) / (sc2 \|\| 1)) * 0.5 * df;` |
| 231 | `num → 0` | `p += ((cs * cs) / (cc \|\| 1) + (ss * ss) / (sc2 \|\| 1)) * 0.5 * df;` |
| 231 | `bool \|\| → &&` | `p += ((cs * cs) / (cc \|\| 1) + (ss * ss) / (sc2 \|\| 1)) * 0.5 * df;` |
| 235 | `num → 0` | `const pb = band(0.01, 0.05),` |
| 235 | `num → 0` | `const pb = band(0.01, 0.05),` |
| 236 | `num → 0` | `lf = band(0.04, 0.15),` |
| 236 | `num → 0` | `lf = band(0.04, 0.15),` |
| 237 | `num → 0` | `tot = band(0.003, 0.4) \|\| 1;` |
| 237 | `bool \|\| → &&` | `tot = band(0.003, 0.4) \|\| 1;` |
| 237 | `num → 0` | `tot = band(0.003, 0.4) \|\| 1;` |
| 239 | `cmp > → >=` | `return { frac: +frac.toFixed(3), strong: frac > 0.4 && pb > lf }; // PB ` |
| 239 | `cmp > → >=` | `return { frac: +frac.toFixed(3), strong: frac > 0.4 && pb > lf }; // PB ` |
| 239 | `num → 0` | `return { frac: +frac.toFixed(3), strong: frac > 0.4 && pb > lf }; // PB ` |
| 239 | `bool && → \|\|` | `return { frac: +frac.toFixed(3), strong: frac > 0.4 && pb > lf }; // PB ` |
| 256 | `bool && → \|\|` | `return mo && mx ? amo / (2 * (mo / 1000) * (mx / 1000)) : null;` |
| 263 | `num → 0` | `return en * 0.4 + fo * 0.3 + co * 0.3;` |
| 263 | `num → 0` | `return en * 0.4 + fo * 0.3 + co * 0.3;` |
| 263 | `num → 0` | `return en * 0.4 + fo * 0.3 + co * 0.3;` |
| 271 | `bool && → \|\|` | `const ok = (v) => typeof v === 'number' && isFinite(v);` |
| 272 | `bool \|\| → &&` | `if (!ok(ps) \|\| !ok(sn) \|\| ps + sn === 0) return null;` |
| 272 | `bool \|\| → &&` | `if (!ok(ps) \|\| !ok(sn) \|\| ps + sn === 0) return null;` |
| 299 | `cmp < → <=` | `for (let i = 0; i < N; i++) {` |
| 322 | `cmp > → >=` | `if (F > 0) {` |
| 327 | `cmp < → <=` | `if (logn.length < 3) return null;` |
| 340 | `cmp < → <=` | `if (N < m + 2) return null;` |
| 342 | `cmp > → >=` | `if (N > MAXN) {` |
| 345 | `cmp < → <=` | `for (let i = 0; i < N; i += stride) dec.push(a[i]);` |
| 351 | `cmp < → <=` | `for (let i = 0; i < N - m; i++) {` |
| 361 | `bool \|\| → &&` | `if (B === 0 \|\| A === 0) return null;` |
| 372 | `cmp > → >=` | `const s = d.map((v) => (v > 0 ? 1 : v < 0 ? -1 : 0));` |
| 373 | `cmp < → <=` | `for (let i = 0; i < s.length; i++) {` |
| 401 | `cmp < → <=` | `for (let i = 0; i < runs.length; i++) {` |
| 404 | `cmp >= → >` | `if (altRun >= 4) pasNN += altRun;` |
| 423 | `cmp < → <=` | `for (let i = L; i < N - L; i++) {` |
| 424 | `cmp > → >=` | `const isAnchor = sign > 0 ? a[i] > a[i - 1] : a[i] < a[i - 1];` |
| 424 | `cmp < → <=` | `const isAnchor = sign > 0 ? a[i] > a[i - 1] : a[i] < a[i - 1];` |
| 426 | `cmp > → >=` | `if (Math.abs(a[i] - a[i - 1]) / a[i - 1] > 0.05) continue; // artifact g` |
| 426 | `num → 0` | `if (Math.abs(a[i] - a[i - 1]) / a[i - 1] > 0.05) continue; // artifact g` |
| 429 | `cmp < → <=` | `if (win.length < 3) return null;` |
| 431 | `cmp < → <=` | `for (let k = 0; k < 2 * L + 1; k++) {` |
| 448 | `cmp > → >=` | `if (f[k] > maxC) maxC = f[k];` |
| 470 | `cmp >= → >` | `return v >= 15 ? 'ok' : v >= 9 ? 'warn' : 'bad';` |
| 470 | `cmp >= → >` | `return v >= 15 ? 'ok' : v >= 9 ? 'warn' : 'bad';` |
| 501 | `cmp > → >=` | `const fNyq = _meanRRs > 0 ? 1 / (2 * _meanRRs) : fHi;` |
| 519 | `cmp < → <=` | `for (let kf = 0; kf < nfFull; kf++) {` |
| 551 | `cmp > → >=` | `if (P > gPeakP) {` |
| 555 | `cmp < → <=` | `if (f < 0.04) vlf += e;` |
| 556 | `cmp < → <=` | `else if (f < 0.15) lf += e;` |
| 559 | `cmp > → >=` | `if (P > peakP) {` |
| 567 | `cmp > → >=` | `const sc = tpFull > 0 ? variance / tpFull : 1; // calibrate ∫PSD = varia` |
| 582 | `cmp > → >=` | `lfhf: hf > 0 ? +(lf / hf).toFixed(3) : null,` |
| 584 | `cmp < → <=` | `peakBand: gPeakF < 0.04 ? 'VLF' : gPeakF < 0.15 ? 'LF' : 'HF',` |
| 584 | `num → 0` | `peakBand: gPeakF < 0.04 ? 'VLF' : gPeakF < 0.15 ? 'LF' : 'HF',` |
| 584 | `cmp < → <=` | `peakBand: gPeakF < 0.04 ? 'VLF' : gPeakF < 0.15 ? 'LF' : 'HF',` |
| 584 | `num → 0` | `peakBand: gPeakF < 0.04 ? 'VLF' : gPeakF < 0.15 ? 'LF' : 'HF',` |
| 585 | `cmp > → >=` | `peakBelowHF: gPeakF > 0 && gPeakF < 0.15, // dominant oscillation sits B` |
| 585 | `num → 0` | `peakBelowHF: gPeakF > 0 && gPeakF < 0.15, // dominant oscillation sits B` |
| 585 | `cmp < → <=` | `peakBelowHF: gPeakF > 0 && gPeakF < 0.15, // dominant oscillation sits B` |
| 585 | `bool && → \|\|` | `peakBelowHF: gPeakF > 0 && gPeakF < 0.15, // dominant oscillation sits B` |
| 586 | `cmp > → >=` | `respBelowHF: gPeakF > 0 && gPeakF < 0.15 && gPeakP > peakP * 2, // STRIC` |
| 586 | `cmp > → >=` | `respBelowHF: gPeakF > 0 && gPeakF < 0.15 && gPeakP > peakP * 2, // STRIC` |
| 586 | `cmp < → <=` | `respBelowHF: gPeakF > 0 && gPeakF < 0.15 && gPeakP > peakP * 2, // STRIC` |
| 586 | `bool && → \|\|` | `respBelowHF: gPeakF > 0 && gPeakF < 0.15 && gPeakP > peakP * 2, // STRIC` |
| 586 | `bool && → \|\|` | `respBelowHF: gPeakF > 0 && gPeakF < 0.15 && gPeakP > peakP * 2, // STRIC` |
| 586 | `num → 0` | `respBelowHF: gPeakF > 0 && gPeakF < 0.15 && gPeakP > peakP * 2, // STRIC` |
| 587 | `num → 0` | `respRate: +(peakF * 60).toFixed(1) // breaths/min from HF peak` |
| 609 | `cmp < → <=` | `for (let i = 0; i < parts.length; i++) {` |
| 610 | `bool \|\| → &&` | `if (pat.test(parts[i] \|\| '')) return i;` |
| 623 | `cmp >= → >` | `if (!headerLine \|\| c < 0 \|\| c >= headerLine.length) return false;` |
| 623 | `cmp < → <=` | `if (!headerLine \|\| c < 0 \|\| c >= headerLine.length) return false;` |
| 623 | `bool \|\| → &&` | `if (!headerLine \|\| c < 0 \|\| c >= headerLine.length) return false;` |
| 653 | `cmp < → <=` | `for (let i = 0; i < parts.length; i++) {` |
| 664 | `cmp < → <=` | `for (let c = 0; c < ncol; c++) {` |
| 671 | `num → 0` | `if (nums.length < rows.length * 0.5) continue;` |
| 671 | `cmp < → <=` | `if (nums.length < rows.length * 0.5) continue;` |
| 673 | `cmp <= → <` | `if (med >= 300 && med <= 2000) {` |
| 673 | `cmp >= → >` | `if (med >= 300 && med <= 2000) {` |
| 674 | `cmp >= → >` | `const inRange = nums.filter((v) => v >= 250 && v <= 2500).length / nums.` |
| 674 | `bool && → \|\|` | `const inRange = nums.filter((v) => v >= 250 && v <= 2500).length / nums.` |
| 674 | `cmp <= → <` | `const inRange = nums.filter((v) => v >= 250 && v <= 2500).length / nums.` |
| 674 | `num → 0` | `const inRange = nums.filter((v) => v >= 250 && v <= 2500).length / nums.` |
| 674 | `num → 0` | `const inRange = nums.filter((v) => v >= 250 && v <= 2500).length / nums.` |
| 726 | `bool && → \|\|` | `const delimHits = nonEmpty.filter((l) => /[;\\t]/.test(l) && /(\\d{4}-\\d{2` |
| 740 | `cmp >= → >` | `if (delimHits >= 2) {` |
| 744 | `cmp >= → >` | `if (p.length >= 2 && !isFinite(parseFloat((p[1] \|\| '').replace(',', '.')` |
| 744 | `bool && → \|\|` | `if (p.length >= 2 && !isFinite(parseFloat((p[1] \|\| '').replace(',', '.')` |
| 744 | `bool \|\| → &&` | `if (p.length >= 2 && !isFinite(parseFloat((p[1] \|\| '').replace(',', '.')` |
| 754 | `cmp < → <=` | `if (intervalCol < 0) {` |
| 756 | `cmp >= → >` | `const dataRows = rows.filter((p) => p !== headerLine && p.length >= 2 &&` |
| 756 | `bool && → \|\|` | `const dataRows = rows.filter((p) => p !== headerLine && p.length >= 2 &&` |
| 756 | `bool && → \|\|` | `const dataRows = rows.filter((p) => p !== headerLine && p.length >= 2 &&` |
| 777 | `cmp >= → >` | `const col = intervalCol >= 0 && intervalCol < parts.length ? intervalCol` |
| 777 | `cmp < → <=` | `const col = intervalCol >= 0 && intervalCol < parts.length ? intervalCol` |
| 783 | `cmp >= → >` | `if (blockerCol >= 0 && blockerCol < parts.length && parseFloat((parts[bl` |
| 783 | `cmp < → <=` | `if (blockerCol >= 0 && blockerCol < parts.length && parseFloat((parts[bl` |
| 818 | `cmp < → <=` | `for (let i = 0; i < vals.length; i++) {` |
| 820 | `cmp <= → <` | `if (isFinite(v) && v >= 250 && v <= 3000) nUsable++;` |
| 820 | `cmp >= → >` | `if (isFinite(v) && v >= 250 && v <= 3000) nUsable++;` |
| 838 | `cmp >= → >` | `if (tsOK && tsMs.length === vals.length && tsMs.length >= 10) {` |
| 838 | `bool && → \|\|` | `if (tsOK && tsMs.length === vals.length && tsMs.length >= 10) {` |
| 838 | `num → 0` | `if (tsOK && tsMs.length === vals.length && tsMs.length >= 10) {` |
| 838 | `bool && → \|\|` | `if (tsOK && tsMs.length === vals.length && tsMs.length >= 10) {` |
| 840 | `cmp > → >=` | `if (isFinite(spanMs) && spanMs > 0) {` |
| 840 | `bool && → \|\|` | `if (isFinite(spanMs) && spanMs > 0) {` |
| 842 | `cmp < → <=` | `for (let i = 0; i < vals.length; i++) {` |
| 844 | `cmp >= → >` | `if (isFinite(v) && v >= 250 && v <= 3000) sumMs += v;` |
| 844 | `cmp <= → <` | `if (isFinite(v) && v >= 250 && v <= 3000) sumMs += v;` |
| 844 | `bool && → \|\|` | `if (isFinite(v) && v >= 250 && v <= 3000) sumMs += v;` |
| 844 | `bool && → \|\|` | `if (isFinite(v) && v >= 250 && v <= 3000) sumMs += v;` |
| 844 | `num → 0` | `if (isFinite(v) && v >= 250 && v <= 3000) sumMs += v;` |
| 849 | `cmp > → >=` | `const timeImplausible = timeRatio != null && timeRatio > PD_MAX_TIME_RAT` |
| 852 | `cmp >= → >` | `const usable = nUsable >= 10;` |
| 860 | `cmp >= → >` | `else if (!vals.length) reason = headerLine \|\| delimHits >= 2 ? 'columns ` |
| 860 | `bool \|\| → &&` | `else if (!vals.length) reason = headerLine \|\| delimHits >= 2 ? 'columns ` |
| 862 | `eq === → !==` | `else if (!usable) reason = 'only ' + nUsable + ' usable interval' + (nUs` |
| 862 | `negate: drop !` | `else if (!usable) reason = 'only ' + nUsable + ' usable interval' + (nUs` |
| 886 | `cmp < → <=` | `for (let i = 0; i < a.length; i++) if (a[i] < m) m = a[i];` |
| 886 | `cmp < → <=` | `for (let i = 0; i < a.length; i++) if (a[i] < m) m = a[i];` |
| 891 | `cmp > → >=` | `for (let i = 0; i < a.length; i++) if (a[i] > m) m = a[i];` |
| 891 | `cmp < → <=` | `for (let i = 0; i < a.length; i++) if (a[i] > m) m = a[i];` |
| 914 | `cmp < → <=` | `for (let i = 0; i < n; i++) {` |
| 926 | `cmp >= → >` | `for (let j = lo; j < hi; j++) if (j !== i && vals[j] >= RR_MIN && vals[j` |
| 926 | `cmp < → <=` | `for (let j = lo; j < hi; j++) if (j !== i && vals[j] >= RR_MIN && vals[j` |
| 926 | `cmp <= → <` | `for (let j = lo; j < hi; j++) if (j !== i && vals[j] >= RR_MIN && vals[j` |
| 963 | `num → 0` | `const k = Math.max(1, Math.floor(n * 0.2));` |
| 966 | `cmp >= → >` | `if (durMin >= 20 * 60) {` |
| 968 | `num → 0` | `conf = 0.95;` |
| 969 | `num → 0` | `why = (durMin / 60).toFixed(1) + ' h continuous record';` |
| 970 | `cmp >= → >` | `} else if (durMin >= 90) {` |
| 973 | `num → 0` | `why = (durMin / 60).toFixed(1) + ' h' + (hour != null ? ' from ' + Strin` |
| 974 | `cmp > → >=` | `} else if (hr >= 100 \|\| rising > 15) {` |
| 974 | `cmp >= → >` | `} else if (hr >= 100 \|\| rising > 15) {` |
| 977 | `num → 0` | `why = 'mean HR ' + hr.toFixed(0) + ' bpm' + (rising > 15 ? ', HR rising ` |
| 977 | `cmp > → >=` | `why = 'mean HR ' + hr.toFixed(0) + ' bpm' + (rising > 15 ? ', HR rising ` |
| 978 | `cmp < → <=` | `} else if (durMin <= 8 && (hour == null \|\| hour < 11)) {` |
| 978 | `cmp <= → <` | `} else if (durMin <= 8 && (hour == null \|\| hour < 11)) {` |
| 998 | `cmp <= → <` | `for (let w0 = 0; w0 <= tEnd; w0 += winSec) {` |
| 1001 | `cmp < → <=` | `while (i < N && t[i] < w1) {` |
| 1001 | `cmp < → <=` | `while (i < N && t[i] < w1) {` |
| 1005 | `cmp >= → >` | `if (seg.length >= 20) {` |
| 1017 | `num → 0` | `const W = 680,` |
| 1018 | `num → 0` | `H = 150,` |
| 1019 | `num → 0` | `P = { l: 46, r: 14, t: 14, b: 26 },` |
| 1019 | `num → 0` | `P = { l: 46, r: 14, t: 14, b: 26 },` |
| 1019 | `num → 0` | `P = { l: 46, r: 14, t: 14, b: 26 },` |
| 1019 | `num → 0` | `P = { l: 46, r: 14, t: 14, b: 26 },` |
| 1027 | `cmp < → <=` | `if (p.y < ymn) ymn = p.y;` |
| 1028 | `cmp > → >=` | `if (p.y > ymx) ymx = p.y;` |
| 1029 | `cmp < → <=` | `if (p.x < xmn) xmn = p.x;` |
| 1030 | `cmp > → >=` | `if (p.x > xmx) xmx = p.x;` |
| 1032 | `eq === → !==` | `if (ymx === ymn) ymx = ymn + 1;` |
| 1033 | `eq === → !==` | `if (xmx === xmn) xmx = xmn + 1;` |
| 1039 | `cmp <= → <` | `for (let h = Math.ceil(xmn / 60) * 60; h <= xmx; h += 60) xt.push(h);` |
| 1039 | `num → 0` | `for (let h = Math.ceil(xmn / 60) * 60; h <= xmx; h += 60) xt.push(h);` |
| 1039 | `num → 0` | `for (let h = Math.ceil(xmn / 60) * 60; h <= xmx; h += 60) xt.push(h);` |
| 1039 | `num → 0` | `for (let h = Math.ceil(xmn / 60) * 60; h <= xmx; h += 60) xt.push(h);` |
| 1046 | `cmp < → <=` | `${xt.map((h) => `<text x="${sx(h).toFixed(1)}" y="${H - 8}" fill="#6F809` |
| 1046 | `num → 0` | `${xt.map((h) => `<text x="${sx(h).toFixed(1)}" y="${H - 8}" fill="#6F809` |
| 1046 | `cmp > → >=` | `${xt.map((h) => `<text x="${sx(h).toFixed(1)}" y="${H - 8}" fill="#6F809` |
| 1047 | `cmp > → >=` | `<path d="${line}" fill="none" stroke="${color}" stroke-width="1.6" strok` |
| 1047 | `num → 0` | `<path d="${line}" fill="none" stroke="${color}" stroke-width="1.6" strok` |
| 1047 | `cmp < → <=` | `<path d="${line}" fill="none" stroke="${color}" stroke-width="1.6" strok` |
| 1048 | `cmp > → >=` | `</svg>`;` |
| 1048 | `cmp < → <=` | `</svg>`;` |
| 1081 | `cmp > → >=` | `return d > 0 ? +(sxy / d).toFixed(3) : null;` |
| 1097 | `eq === → !==` | `if (tsMs && tsMs.length === vals.length && isFinite(tsMs[0])) return tsM` |
| 1100 | `cmp < → <=` | `for (let i = 0; i < vals.length; i++) {` |
| 1106 | `bool && → \|\|` | `const haveAbs = !!(primary.tsMs && reference.tsMs && isFinite(primary.ts` |
| 1116 | `cmp < → <=` | `for (let i = 0; i < B.length; i++) {` |
| 1117 | `cmp < → <=` | `while (j + 1 < A.length && Math.abs(ta[j + 1] - tb[i]) <= Math.abs(ta[j]` |
| 1117 | `cmp <= → <` | `while (j + 1 < A.length && Math.abs(ta[j + 1] - tb[i]) <= Math.abs(ta[j]` |
| 1119 | `num → 0` | `const tol = Math.max(120, 0.3 * Math.min(A[j], B[i]));` |
| 1119 | `num → 0` | `const tol = Math.max(120, 0.3 * Math.min(A[j], B[i]));` |
| 1120 | `cmp <= → <` | `if (dt <= tol) {` |
| 1139 | `cmp < → <=` | `if (nM < 5) {` |
| 1140 | `num → 0` | `return { matched: nM, matchRatePct: +((nM / mAll) * 100).toFixed(0), sta` |
| 1147 | `cmp <= → <` | `const within25 = (diffs.filter((d) => Math.abs(d) <= 25).length / nM) * ` |
| 1147 | `num → 0` | `const within25 = (diffs.filter((d) => Math.abs(d) <= 25).length / nM) * ` |
| 1157 | `cmp > → >=` | `const rmssdRatio = rmA > 0 ? +(rmB / rmA).toFixed(2) : null;` |
| 1160 | `cmp > → >=` | `const pttv = rmB > rmA ? +Math.sqrt(Math.max(0, rmB * rmB - rmA * rmA)).` |
| 1163 | `bool \|\| → &&` | `const kA = primary.kind \|\| 'auto',` |
| 1164 | `bool \|\| → &&` | `kB = reference.kind \|\| 'auto';` |
| 1165 | `eq === → !==` | `const refIsPulse = kB === 'ppi' \|\| (kB === 'auto' && kA === 'rr');` |
| 1165 | `eq === → !==` | `const refIsPulse = kB === 'ppi' \|\| (kB === 'auto' && kA === 'rr');` |
| 1165 | `bool \|\| → &&` | `const refIsPulse = kB === 'ppi' \|\| (kB === 'auto' && kA === 'rr');` |
| 1165 | `eq === → !==` | `const refIsPulse = kB === 'ppi' \|\| (kB === 'auto' && kA === 'rr');` |
| 1165 | `bool && → \|\|` | `const refIsPulse = kB === 'ppi' \|\| (kB === 'auto' && kA === 'rr');` |
| 1166 | `cmp <= → <` | `const agreeGrade = mad <= 10 && Math.abs(bias) <= 15 ? 'ok' : mad <= 20 ` |
| 1166 | `cmp <= → <` | `const agreeGrade = mad <= 10 && Math.abs(bias) <= 15 ? 'ok' : mad <= 20 ` |
| 1166 | `cmp <= → <` | `const agreeGrade = mad <= 10 && Math.abs(bias) <= 15 ? 'ok' : mad <= 20 ` |
| 1166 | `num → 0` | `const agreeGrade = mad <= 10 && Math.abs(bias) <= 15 ? 'ok' : mad <= 20 ` |
| 1166 | `num → 0` | `const agreeGrade = mad <= 10 && Math.abs(bias) <= 15 ? 'ok' : mad <= 20 ` |
| 1166 | `bool && → \|\|` | `const agreeGrade = mad <= 10 && Math.abs(bias) <= 15 ? 'ok' : mad <= 20 ` |
| 1166 | `num → 0` | `const agreeGrade = mad <= 10 && Math.abs(bias) <= 15 ? 'ok' : mad <= 20 ` |
| 1169 | `num → 0` | `const step = Math.max(1, Math.floor(nM / 400));` |
| 1172 | `eq === → !==` | `if (i % step === 0) ba.push([+((matchA[i] + matchB[i]) / 2).toFixed(0), ` |
| 1177 | `num → 0` | `matchRatePct: +((nM / mAll) * 100).toFixed(0),` |
| 1179 | `bool \|\| → &&` | `primaryLabel: primary.label \|\| 'Signal A',` |
| 1180 | `bool \|\| → &&` | `referenceLabel: reference.label \|\| 'Signal B',` |
| 1187 | `num → 0` | `loaLoMs: +(bias - 1.96 * sdDiff).toFixed(1),` |
| 1188 | `num → 0` | `loaHiMs: +(bias + 1.96 * sdDiff).toFixed(1),` |
| 1226 | `cmp < → <=` | `p = (n) => (n < 10 ? '0' : '') + n;` |
| 1252 | `cmp < → <=` | `if (!f.vals \|\| f.vals.length < 10) return null;` |
| 1252 | `num → 0` | `if (!f.vals \|\| f.vals.length < 10) return null;` |
| 1256 | `num → 0` | `if (N < 10) return null;` |
| 1256 | `cmp < → <=` | `if (N < 10) return null;` |
| 1277 | `num → 0` | `const durSec = times[N - 1] \|\| (N * mean(a)) / 1000;` |
| 1284 | `cmp > → >=` | `const coverage = spanSec != null && spanSec > 0 ? +Math.min(100, (rrSumS` |
| 1284 | `bool && → \|\|` | `const coverage = spanSec != null && spanSec > 0 ? +Math.min(100, (rrSumS` |
| 1306 | `cmp >= → >` | `if (win.wins.length >= 3) {` |
| 1319 | `cmp < → <=` | `for (let i = 0; i < rmA.length; i++) {` |
| 1321 | `cmp < → <=` | `if (d < bd) {` |
| 1337 | `bool && → \|\|` | `if (w.lfhf != null && isFinite(w.lfhf)) slh.push(w.lfhf); // §5.2 — per-` |
| 1338 | `cmp > → >=` | `if (w.respRate > 0) srr.push(w.respRate);` |
| 1386 | `eq === → !==` | `input && typeof input === 'object' && typeof input.contentId === 'string` |
| 1388 | `bool && → \|\|` | `: typeof SignalFrame !== 'undefined' && SignalFrame && SignalFrame.compu` |
| 1388 | `bool && → \|\|` | `: typeof SignalFrame !== 'undefined' && SignalFrame && SignalFrame.compu` |
| 1440 | `bool && → \|\|` | `const wins = (r.windows \|\| []).filter((w) => isFinite(w.rmssd) && isFini` |
| 1447 | `cmp < → <=` | `if (w.rmssd < 0.7 * med && w.rmssd < 25)` |
| 1447 | `bool && → \|\|` | `if (w.rmssd < 0.7 * med && w.rmssd < 25)` |
| 1447 | `cmp < → <=` | `if (w.rmssd < 0.7 * med && w.rmssd < 25)` |
| 1453 | `num → 0` | `conf: +Math.max(0.4, Math.min(0.92, (0.7 * med - w.rmssd) / (0.7 * med))` |
| 1456 | `cmp >= → >` | `if (st >= 70)` |
| 1462 | `num → 0` | `conf: +Math.max(0.4, Math.min(0.92, (st - 50) / 50)).toFixed(2),` |
| 1467 | `cmp >= → >` | `if (r.stress >= 70)` |
| 1473 | `num → 0` | `conf: +Math.max(0.4, Math.min(0.92, (r.stress - 50) / 50)).toFixed(2),` |
| 1473 | `num → 0` | `conf: +Math.max(0.4, Math.min(0.92, (r.stress - 50) / 50)).toFixed(2),` |
| 1473 | `num → 0` | `conf: +Math.max(0.4, Math.min(0.92, (r.stress - 50) / 50)).toFixed(2),` |
| 1473 | `num → 0` | `conf: +Math.max(0.4, Math.min(0.92, (r.stress - 50) / 50)).toFixed(2),` |
| 1476 | `cmp < → <=` | `else if (r.rmssd < 20) ev.push({ tMs: t0, t: _pdClockS(t0), impulse: 'hr` |
| 1476 | `num → 0` | `else if (r.rmssd < 20) ev.push({ tMs: t0, t: _pdClockS(t0), impulse: 'hr` |
| 1476 | `num → 0` | `else if (r.rmssd < 20) ev.push({ tMs: t0, t: _pdClockS(t0), impulse: 'hr` |
| 1569 | `bool \|\| → &&` | `message: 'This is a ' + (node \|\| 'non-PulseDex') + ' export \\u2014 open ` |
| 1569 | `bool \|\| → &&` | `message: 'This is a ' + (node \|\| 'non-PulseDex') + ' export \\u2014 open ` |
| 1581 | `negate: drop !` | `if (!evAll.length)` |
| 1586 | `bool && → \|\|` | `return ((a && a.tMs) \|\| 0) - ((b && b.tMs) \|\| 0);` |
| 1586 | `bool && → \|\|` | `return ((a && a.tMs) \|\| 0) - ((b && b.tMs) \|\| 0);` |
| 1586 | `bool \|\| → &&` | `return ((a && a.tMs) \|\| 0) - ((b && b.tMs) \|\| 0);` |
| 1586 | `bool \|\| → &&` | `return ((a && a.tMs) \|\| 0) - ((b && b.tMs) \|\| 0);` |
| 1596 | `bool && → \|\|` | `generated: (json.schema && json.schema.generated) \|\| null,` |
| 1597 | `bool \|\| → &&` | `derivedFrom: (json.schema && json.schema.derivedFrom) \|\| null,` |
| 1597 | `bool && → \|\|` | `derivedFrom: (json.schema && json.schema.derivedFrom) \|\| null,` |
| 1598 | `bool \|\| → &&` | `kernel: json.kernel \|\| null,` |
| 1602 | `bool && → \|\|` | `scrubbed: !!(json.schema && json.schema.scrubbed),` |
| 1603 | `cmp > → >=` | `multiNight: elements.length > 1,` |
| 1609 | `eq !== → ===` | `var PulseDex = typeof PulseDex !== 'undefined' && PulseDex ? PulseDex : ` |
| 1609 | `bool && → \|\|` | `var PulseDex = typeof PulseDex !== 'undefined' && PulseDex ? PulseDex : ` |
| 1614 | `eq !== → ===` | `return pdBuildNodeExport(r, { provenance: opts.provenance !== undefined ` |
| 1614 | `bool \|\| → &&` | `return pdBuildNodeExport(r, { provenance: opts.provenance !== undefined ` |
| 1614 | `bool \|\| → &&` | `return pdBuildNodeExport(r, { provenance: opts.provenance !== undefined ` |
| 1622 | `eq === → !==` | `if (typeof DexExport !== 'undefined' && DexExport && typeof DexExport.sc` |
| 1622 | `eq !== → ===` | `if (typeof DexExport !== 'undefined' && DexExport && typeof DexExport.sc` |
| 1622 | `bool && → \|\|` | `if (typeof DexExport !== 'undefined' && DexExport && typeof DexExport.sc` |
| 1622 | `bool && → \|\|` | `if (typeof DexExport !== 'undefined' && DexExport && typeof DexExport.sc` |
| 1623 | `eq === → !==` | `if (typeof dexScrubExport === 'function') return dexScrubExport(env);` |
| 1730 | `eq !== → ===` | `})(/** @type {any} */ (typeof globalThis !== 'undefined' ? globalThis : ` |
| 1730 | `eq !== → ===` | `})(/** @type {any} */ (typeof globalThis !== 'undefined' ? globalThis : ` |

