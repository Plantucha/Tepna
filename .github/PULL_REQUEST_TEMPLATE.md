**Summary:** <one line — what changed, and why>

**Plants:** <each regression planted → the test that caught it. A test that caught NO plant is listed as such, never omitted.>

**Gate:** <`npm run check` | `capture-host/check.sh`> → <verdict, with counts>

**Changeset:** <yes → `changes/<file>.md` | no → why none is owed>

Fleet-Session: <Name>
