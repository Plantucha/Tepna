{
  "clock.js": {
    "line": 386,
    "op": "cmp <= → <",
    "before": "if (x <= pts[0].d) return sm[0];",
    "after": "if (x < pts[0].d) return sm[0];",
    "killers": [
      "clock.js — wave 8: duplicate anchors, and an anchor that is not a number"
    ]
  },
  "cpapdex-cross.js": {
    "line": 44,
    "op": "negate: drop !",
    "before": "const r2 = (v) => (v == null || !isFinite(v) ? null : Math.round(v * 100) / 100);",
    "after": "const r2 = (v) => (v == null || isFinite(v) ? null : Math.round(v * 100) / 100);",
    "killers": [
      "Cross-night slope basis — an index slope is never a per-day slope (DEEP-AUDIT-II §9.4)"
    ]
  },
  "cpapdex-dsp.js": {
    "line": 37,
    "op": "eq !== → ===",
    "before": "var _EDF = typeof require !== 'undefined' ? /** @type {any} */ (require)('./cpapdex-edf.js') : root ",
    "after": "var _EDF = typeof require === 'undefined' ? /** @type {any} */ (require)('./cpapdex-edf.js') : root ",
    "killers": [
      "CPAPDex §6.2 — ODI is rated over ANALYZABLE oximeter time, not the therapy span"
    ]
  },
  "cpapdex-edf.js": {
    "line": 66,
    "op": "num → 0",
    "before": "var year = yy >= 85 ? 1900 + yy : 2000 + yy; // EDF clipping rule",
    "after": "var year = yy >= 85 ? 1900 + yy : 0 + yy; // EDF clipping rule",
    "killers": [
      "Leaf-module coverage — CPAPDex DSP/EDF self-tests + morphology"
    ]
  },
  "ecgdex-cross.js": {
    "line": 57,
    "op": "cmp < → <=",
    "before": "for (let i = 0; i < vals.length; i++) {",
    "after": "for (let i = 0; i <= vals.length; i++) {",
    "killers": [
      "Cross-night baseline mean/sd published (#7)"
    ]
  },
  "ecgdex-dsp.js": {
    "line": 68,
    "op": "num → 0",
    "before": "for (let i = 1; i < a.length; i++) if (Math.abs(a[i] - a[i - 1]) > 50) n++;",
    "after": "for (let i = 1; i < a.length; i++) if (Math.abs(a[i] - a[i - 1]) > 0) n++;",
    "killers": [
      "ECGDex rich export ≡ its committed golden — Integrator-facing surface"
    ]
  },
  "glucodex-dsp.js": {
    "line": 44,
    "op": "negate: drop !",
    "before": "if (!sorted.length) return null;",
    "after": "if (sorted.length) return null;",
    "killers": [
      "GlucoDex §5.1/§5.2 — a truncated grid says so, and the session span cannot overflow"
    ]
  },
  "hrvdex-dsp.js": {
    "line": 118,
    "op": "negate: drop !",
    "before": "if (v == null || typeof v !== 'number' || !isFinite(v)) return false;",
    "after": "if (v == null || typeof v !== 'number' || isFinite(v)) return false;",
    "killers": [
      "HRVDex — an absent column yields NaN, never a fabricated number (§1.1–1.9)",
      "HRVDex computeDerived — the 52 derived columns, pinned",
      "HRVDex computeDerived — the GUARDS, one seed absent at a time"
    ]
  },
  "integrator-dsp.js": {
    "line": 83,
    "op": "negate: drop !",
    "before": "if (c == null || !isFinite(c)) continue;",
    "after": "if (c == null || isFinite(c)) continue;",
    "killers": [
      "Integrator periodic-breathing corroboration (§2)",
      "Fusion algebra — combineConf laws (Phase 8)"
    ]
  },
  "manifest-gate.js": {
    "line": 201,
    "op": "eq === → !==",
    "before": "var hasCommitted = !!(committed && typeof committed === 'object');",
    "after": "var hasCommitted = !!(committed && typeof committed !== 'object');",
    "killers": [
      "ManifestGate — GATE A/B verdicts red on drift (provenance-meta)"
    ]
  },
  "motiondex-dsp.js": {
    "line": 48,
    "op": "negate: drop !",
    "before": "if (!a.length) return NaN;",
    "after": "if (a.length) return NaN;",
    "killers": [
      "MotionDex inferAccUnit — three gravity bands, all bounds exclusive (mutation bootstrap)",
      "MotionDex: rate, coverage and plausibility — §4",
      "MotionDex sampleHz is the NATIVE rate, not the coverage-depressed average — §4.1 at its own seam"
    ]
  },
  "oxydex-cross.js": {
    "line": 25,
    "op": "num → 0",
    "before": "const r2 = (v) => (v == null || !isFinite(v) ? null : Math.round(v * 100) / 100);",
    "after": "const r2 = (v) => (v == null || !isFinite(v) ? null : Math.round(v * 0) / 100);",
    "killers": [
      "Cross-night slope basis — an index slope is never a per-day slope (DEEP-AUDIT-II §9.4)"
    ]
  },
  "oxydex-dsp.js": {
    "line": 72,
    "op": "eq === → !==",
    "before": "if (h) for (var k in _ui) if (typeof h[k] === 'function') _ui[k] = h[k];",
    "after": "if (h) for (var k in _ui) if (typeof h[k] !== 'function') _ui[k] = h[k];",
    "killers": [
      "ODI-4 pilot — the paper Table 1 numbers, on committed bytes",
      "OxyDex publishes absence as absence — a missing sensor and an uncomputed correlation (DA-V §2.3)",
      "§17-§21 hygiene — no fabricated date, no 0-for-undefined, no pop-default-as-you, pooled indices, badged heroes"
    ]
  },
  "ppgdex-cross.js": {
    "line": 23,
    "op": "num → 0",
    "before": "const r2 = (v) => (v == null || !isFinite(v) ? null : Math.round(v * 100) / 100);",
    "after": "const r2 = (v) => (v == null || !isFinite(v) ? null : Math.round(v * 0) / 100);",
    "killers": [
      "Cross-night slope basis — an index slope is never a per-day slope (DEEP-AUDIT-II §9.4)"
    ]
  },
  "ppgdex-dsp.js": {
    "line": 31,
    "op": "bool || → &&",
    "before": "se = se || 0;",
    "after": "se = se && 0;",
    "killers": [
      "O2Ring device-crystal is invariant to a corrupted host clock — bad-host acceptance"
    ]
  },
  "pulsedex-cross.js": {
    "line": 23,
    "op": "num → 0",
    "before": "const r1 = (v) => (v == null || !isFinite(v) ? null : Math.round(v * 10) / 10);",
    "after": "const r1 = (v) => (v == null || !isFinite(v) ? null : Math.round(v * 10) / 0);",
    "killers": [
      "Cross §1 — per-node crossNightBlock + helpers (VARYING series)"
    ]
  },
  "pulsedex-dsp.js": {
    "line": 66,
    "op": "cmp < → <=",
    "before": "for (let i = 0; i < a.length; i++) {",
    "after": "for (let i = 0; i <= a.length; i++) {",
    "killers": [
      "PulseDex compareIntervalSeries — two signals, one heart (mutation bootstrap)"
    ]
  }
}
