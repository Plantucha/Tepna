[Unit]
Description=Tepna capture daemon (BLE -> vendor-layout files)
# Needs the Bluetooth stack and a synced clock (arrival stamps depend on the clock).
After=bluetooth.target network-online.target time-sync.target
Wants=bluetooth.target network-online.target
# Capture is unattended overnight — never rate-limit restarts (keep retrying across transient BLE drops).
# StartLimitIntervalSec lives in [Unit], not [Service], since systemd v230 — it was silently IGNORED in
# [Service] (a `systemctl status` warned "Unknown key … ignoring"), so the no-give-up intent never applied.
StartLimitIntervalSec=0

[Service]
# notify (not simple): the daemon sends READY=1 once capture is actually up, and heartbeats WATCHDOG=1
# from a live-event-loop task. WatchdogSec turns the box's signature HUNG-BUT-ALIVE failure (a wedged BLE
# stack that captures nothing while the process keeps running) into an automatic kill+restart — Restart
# alone never fires on a wedge because nothing crashes. sd_watchdog() pings at half this interval.
Type=notify
WatchdogSec=120
# This box's operator account. An earlier duplicate of this unit under systemd/ said `tepna`; no such user
# exists here, and everything under /opt/tepna and /srv/tepna is owned by vigil. That duplicate was DELETED
# 2026-08-05 rather than corrected — two files with one name is what made check-system-files.sh's first
# green a lie (it byte-compared the copy nobody installs), and its own `ambiguous()` check then held the
# gate at exit 1 on the live box permanently. THIS is the only copy; install-services.sh installs it.
User=vigil
# ARM THE WATCHDOG'S RECOVERY LADDER. Without CAP_NET_ADMIN the daemon runs prevention-only: the
# adapter watchdog's `hciconfig <hci> reset` and USB unbind/bind both fail silently, so a wedged radio
# has NO automatic recovery — the exact 2026-07-23 failure that cost ~110 min. `startup_defense_check`
# has been logging "capture has no CAP_NET_ADMIN" at every boot since; nothing read it, because the
# monitor filtered on syslog priority and capture logs WARNINGs at PRIORITY=6.
# Ambient (not just Bounding): the unit runs as an unprivileged User=, and ambient caps are the only
# ones an unprivileged process actually keeps across exec.
# ⚠️ DO NOT narrow CapabilityBoundingSet here, and do not set NoNewPrivileges=true. Both look like
# hardening and both DISABLE the daemon's sudo calls (tepna-rssi.sh, and the CPAP harvest's
# wpa_supplicant/ip). sudo must gain uid 0, which needs CAP_SETUID/CAP_SETGID in the BOUNDING set and
# is forbidden outright by NoNewPrivileges. Shipped 2026-07-26 and broke the CPAP association four
# commits after enabling it: sudo-rs panics with rc=101 ("panicked at src/system/audit"), which reads
# like a sudo bug rather than a unit-file mistake. AmbientCapabilities alone is what arms the watchdog
# ladder; the bounding-set narrowing added nothing but breakage.
AmbientCapabilities=CAP_NET_ADMIN

Group=vigil
WorkingDirectory=/opt/tepna/capture-host
# Use the venv's python (see README install).
ExecStart=/opt/tepna/capture-host/.venv/bin/python capture.py --config /opt/tepna/capture-host/config.yaml
Restart=always
RestartSec=5

# ── Writable paths ────────────────────────────────────────────────────────────────────────────────
# /srv/tepna              — the captures themselves.
# /opt/tepna/capture-host — config.yaml. The monitor PERSISTS here (/api/remember, /api/settings and the
#   Storage card all call webmon._save), and under ProtectSystem=strict the code tree is read-only, so
#   without this every save would 500. It must be the DIRECTORY, not the file: _save is an atomic
#   mkstemp+fsync+os.replace, so it creates a sibling temp file — that is the cost of the atomic write,
#   and it is worth paying (the old truncating write needed only the file, but could destroy it).
ReadWritePaths=/srv/tepna /opt/tepna/capture-host

# ⚠️ KEEP THIS `no`. NoNewPrivileges=yes makes sudo structurally impossible, so every helper the daemon
# reaches through `sudo -n` — tepna-clock.sh (Save&apply / Sync now / Set timezone), tepna-rssi.sh,
# tepna-btreset.sh (the watchdog's last recovery rung), and the CPAP harvest's wpa_supplicant/ip — fails
# with "the no new privileges flag is set".
NoNewPrivileges=no
# ⚠️ THE MONITOR'S CLOCK CONTROLS NEED THREE MORE PATHS OPENED, and they are NOT open here. If Save&apply
# or Sync now must write, add a DROP-IN (never edit this file, or a redeploy reverts it) — which is exactly
# what deploy/enable-clock-control.sh installs:
#
#   /etc/systemd/system/tepna-capture.service.d/clock-control.conf
#     [Service]
#     ReadWritePaths=-/etc/chrony -/etc/systemd/timesyncd.conf.d -/run/chrony
#
# All three were established by an actual failure on a real box (2026-07-25), not guessed: ProtectSystem=
# strict mounts the WHOLE hierarchy read-only except /dev, /proc and /sys — inherited by sudo'd children —
# so /etc/chrony blocks writing the servers and /run/chrony blocks chronyc from creating the reply socket
# it needs to talk to chronyd at all.
ProtectSystem=strict
# ProtectHome stays TRUE, so /home/vigil is invisible to the daemon. Consequence for the offload: the
# rsync SSH key must NOT live in ~/.ssh — put it under /srv/tepna/.ssh/ (mode 600) or the archive push
# will fail with a key it cannot see.
ProtectHome=true

[Install]
WantedBy=multi-user.target
