<!-- SPDX-License-Identifier: Apache-2.0 -->
---
bump: patch
type: added
nodes: [OxyDex]
brief: MUTATION-PROGRAM-FOLLOWUPS-2026-08-11-BRIEF.md
---
The third and largest file of the draft-adoption programme: 72 of the oxydex bank's 85 drafts
adopted as one compute*-floor group — every guard refuses absent input null-never-throw across
the spike/recovery/hypoxic/HR helper families, plus the honest count pins on present input.
Batch-verified green against the current tree before adoption, which is what caught the discards:
one draft pinned a recorded THROW as if it were a value, two pinned a vacuous
undefined-vs-"undefined" comparison, and one had gone stale against current code
(computeMOS(null) reads 1 now, not the drafted 3).

NOT adopted, deliberately: the fabrication-adjacent value pins — composite scores computed from
EMPTY input (sfi/nsi 0), sub-stats of 0 sitting beside a null primary (hl_meanDepth/hl_meanDurSec),
periodic:false claimed on no data, and kin. Pinning those would lock exactly the fabricated-absence
warts the suite's §B groups exist to catch; they are left as a fabricated-defaults follow-up, and
their mutants stay honestly on the survivor ledger.

The group grew to 80 assertions in verification: two sharpenings after first kill-verify (a
slopes object with a NULL recovery leg refuses — null, not NaN asymmetry; the epoch grid refuses
absent rows and a missing t0Ms anchor), and the two long-line MANUAL targets pinned by contract —
the filename date-anchor round-trip (month 13 day 45 is DATE UNKNOWN, never a rolled instant;
Clock Contract §2.7) and the nocturnal-dip label bands at their exact 10.0 % boundary.

Kill tally from re-applying every mutant: 37 first-pass + 3 sharpened + 3 hand-applied = 43 red.
The 12 still-surviving are the dropped drafts' mutants plus two text-degenerate non-targets —
honest residue on the ledger, not silent claims. The planted control (a sabotaged parseJSONL →
4 reds) proved the harness could see a kill before any verdict was trusted.
