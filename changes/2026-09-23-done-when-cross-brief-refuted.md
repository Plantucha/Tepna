---
bump: patch
type: added
brief: DONE-WHEN-CROSS-BRIEF-2026-09-23-BRIEF.md
---
A Done-when item can be answered by a SIBLING brief while neither file knows — `AXIS-LEG`'s ECG ppm item sat open for nine days after `FIDUCIAL-JITTER` §6 had measured it. `DONE-WHEN-CROSS-BRIEF` asks whether that is detectable and records a **measured negative**: a brief→brief detector keyed on a shared rare token reaches **sensitivity 1 of 2** on the class's two authentic instances (both replayed from git) and **precision 0 of 4** on the live corpus — **still 0 of 2** after both available by-construction exclusions, so "tune the grammar" is answered by measurement rather than opinion. It fails for a reason about the corpus, not the regex: a repeated number here means **citation** (both survivors quote one third source), while two briefs describing one change share **no token at all**. This is the THIRD refuted shape after `BRIEF-CODE-ANCHOR-CHECK`'s two, and the first whose sensitivity was actually measured — its §10.4 precondition (plant first) was met because a lexical criterion replays under git, unlike a consumed doc-search index. The detector is deliberately not shipped.
