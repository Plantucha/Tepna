---
bump: minor
type: added
brief: OPERATIONAL-MATURITY-ROADMAP-2026-08-27-BRIEF.md
---

The daemon writes each start into the night as STARTS.csv, and the night report carries the count beside the number of starts that fell inside a signal-carrying file's span — so the restart question is answered from the night instead of re-derived from journalctl, and the count never appears without its harm term.
