---
bump: patch
type: fixed
brief: PYTHON-TYPES-AND-FORMAT-2026-08-27-BRIEF.md
---

capture-host's mypy count drops **58 → 41**; `MYPY_BASELINE` banked. Re-measured before starting: **58 in
27 files** on `origin/main` `bf395e45`, matching the banked baseline — no drift.

Seventeen errors, five causes, and **one of them was a real code defect rather than a missing annotation**:

⚠️ **Two different tables shared the name `ALL_WRITERS` in one module.** `tests/test_writers_sidecars.py`
defines `(class, write-action lambda)` near the top and then, 140 lines later, `(class, constructor
kwargs)` under the same name — which **silently shadows the first for every use below it**. mypy typed the
name from the first assignment and reported six "incompatible list item" errors; that is the collision
seen from the type side. Renamed the second to `WRITER_CTOR_KWARGS` and repointed its two consumers.
Annotating it would have silenced the symptom and left two different tables answering to one name.

The other four are one declaration each:

* `bonding` — `proc.stdin` is `StreamWriter | None` in the stubs and non-None because the process was
  created with `stdin=PIPE`. Bound once instead of narrowed at three uses. **3 closed.**
* `probe_pmd_opcodes` — `entry` infers its value type from a literal holding one `str | None`, so a later
  bool and dict read as mismatches. **3 closed.**
* `oxyii` — `SET_CONFIG_FIELDS` rows mix ints with a nullable string, joined to `object`, so `spec["max"]`
  read as indexing an object. **3 closed.**
* `probe_verity_survey` — `best = []` gets its element type only from a reassignment inside the loop,
  which mypy cannot infer backwards from.

Gate: `capture-host/check.sh` all green — 7300 passed, coverage 100.00 %, mypy 41 at baseline 41. The
baseline edit is followed by a full re-run of the gate, not preceded by one.

**41 remain.**
