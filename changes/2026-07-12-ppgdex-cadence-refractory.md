<!-- SPDX-License-Identifier: Apache-2.0 -->
---
bump: minor
type: fixed
nodes: [PpgDex]
brief: PPGDEX-OPTICAL-DETECTOR-AND-SIGMA-REDERIVE-2026-07-11-BRIEF.md
---
Size the beat-detector refractory from a windowed-autocorrelation cadence — fixes the optical HR reading exactly 2× true when a prominent diastolic wave is counted as a second beat.

TERMA calls a beat wherever the short upstroke-energy average exceeds the long one — a bare
`maPeak > maBeat`, with **no amplitude discrimination**. A prominent DIASTOLIC (reflected) wave is a
genuine positive-slope event, so it raises its own block and is detected as a second "beat" about half
a cycle after systole. At the corpus' sleeping ~48 bpm that lands ~625 ms out — **twice** the fixed
0.30 s refractory — so nothing suppressed it and the optical HR read exactly 2× true on 4 of the 17
real trio nights. On three of them ALL THREE LEDs doubled together, so the 3-LED consensus vote
ratified the harmonic (2-of-3 agreement on a doubled beat is still 2-of-3) and no cross-channel check
could see it either. This is the residual §1 the PPI-spine arbiter (v1.5.0) flagged but could not fix:
when both halves of the beat are wrong, no choice of spine rescues them.

Fix: a dicrotic notch is a HARMONIC — it sits at T/2 — while the autocorrelation still peaks at the true
beat T. A WINDOWED ACF of the pulse band (0.5–3.0 Hz, decimated to ~25 Hz) recovers the true cadence on
every night, and that cadence sizes an ADAPTIVE refractory (0.60 × the local beat, floored at the old
0.30 s physiologic ceiling). TERMA still finds every peak; the existing amplitude arbitration (keep the
taller peak on a refractory conflict) then discards the smaller diastolic one.

⚠️ This is NOT a return to the retired global-period detector. That one used a WHOLE-RECORD
autocorrelation period to GATE detection and was the missed-beat root cause. Here the cadence is
windowed (it tracks HR drift and arousals across a night) and gates nothing — it only says how close
together two peaks may legitimately be.

Rejected en route, and recorded so they are not retried: (a) adapting TERMA's `W2` "one-beat" window to
the true cadence — re-running the doubled channel with W2 = the true 1230 ms beat still returned 94.5 bpm,
because the threshold has no amplitude offset; (b) a cross-channel cadence prior — it collapses precisely
where it is needed, since on 3 of the 4 nights all three LEDs double together and the median cadence is
doubled too.

Validated against the paired chest-ECG corner on all 17 concurrent trio nights: **17/17 HR-clean, 4
recovered (2026-06-29 100→50 vs ECG 49.7 · 07-01 94→48 vs 48.0 · 07-02 106→48 vs 48.4 · 07-05 110→58 vs
57.8), 0 regressions.** `ppiAgreementPct` rises to 99–100% on the recovered nights — the foot and peak
spines now agree, independently confirming the beat spine rather than one half compensating for the other.

This CHANGES the corrected NN beat series, so the committed `PpgDex_2026-06-27_equiv` fixture was
REGENERATED by re-running the app on its committed input (never hand-edited): `contentId`
7e887548f693→2c3b67527577, `outputHash` 69131ebdfe4d8ff1→eeaeb3ee20fe7764. `uploads/trio/` re-derived
with the fixed detector.
