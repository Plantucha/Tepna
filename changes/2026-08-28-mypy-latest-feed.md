---
bump: patch
type: fixed
brief: PYTHON-TYPES-AND-FORMAT-2026-08-27-BRIEF.md
---

`capture-host/check.sh` now persists mypy's output to `.mypy-latest.txt` — **the §P2 fix lane's work
queue had no writer.**

`qwen-idle-driver.sh` stage 5 reads that file and skipped on every cycle because nothing produced it:
`run_advisory` ran mypy and printed its output, and the consumer shipped **before** the producer. The
skip was loud by design — *"absent is not clean"* — which is why it surfaced within one driver tick
instead of reading as a healthy quiet lane. But a loud skip forever is still a lane that never runs.

⚠️ **Written by redirect and then echoed, never `| tee`.** A pipe would hand `run_advisory` tee's exit
status instead of mypy's, and mypy's is the number the advisory reports — CLAUDE.md §4b, the check that
runs and reports about something it never examined. The wrapper captures `$?` before printing.

⚠️ **No mypy ⇒ the feed is REMOVED, not left stale.** A stale queue is worse than an absent one: the
lane would propose fixes for errors that may already be gone, and its acceptance rate — the metric that
decides whether the lane survives at all — would be measured against a dead list. The not-installed
path already reported *"nothing was examined"*; now it also stops feeding the consumer.

`capture-host/.mypy-latest.txt` is gitignored, verified with `git check-ignore -v` rather than assumed:
it is machine-local and regenerated by every gate run, so committing it would ship one box's error list
as if it were the repo's.

The defect class is the mirror of *trace-to-the-consumer*: I traced the consumer and never asked who
writes. **A file path in a consumer is not evidence that anything produces it.**

## Verified end to end by a real gate run

`./check.sh` in a worktree: `ruff` ok · `pytest` ok · `unwired` ok · `format` *"0 changed .py files vs
origin/main — nothing in scope (not a pass, an empty scope)"* · **`shellcheck` exit 127 — the tool is
absent on this box, which is a missing tool and not a failing gate** (CI runs it).

The feed landed with **255 lines**, ending `Found 188 errors in 40 files (checked 277 source files)`.

⚠️ **188 is NOT a burn-down, and I first reported that it was.** The count came from a worktree off
`origin/main`; root measures **189**. The difference is exactly one line, and it is
`probe_rt_ppg_args.py:101` — an **untracked** file that exists only in root. Two trees, two
populations, one of them carrying a file the other has never seen; comparing the totals as if they
measured the same thing is the error, not the number. **The baseline stands at 189 and nothing has
been burned down.** Re-recording it off my 188 would have moved a pre-stated threshold on the strength
of an artifact.

And the empty-vs-clean discriminator, exit codes captured without a pipe:

```
empty feed                     → exit 2, refused
"Success: no issues found in…" → exit 0, proceeds
```

31 selftest assertions, all green. `.mypy-latest.txt` stays out of `git status` — gitignored, verified
with `git check-ignore -v`, not assumed.

