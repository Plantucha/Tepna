<!-- SPDX-License-Identifier: Apache-2.0 -->
---
bump: patch
type: fixed
nodes: [OxyDex, Integrator]
brief: DEEP-AUDIT-2026-07-11-BRIEF.md
---
Place desat events on their own row's wall clock, analyse the whole night instead of its first hour, and refuse to report a physiologically impossible REM estimate as a healthy finding.

**§8 — the event clock.** `parseCSV` drops rows (the device's `- -` no-reading, out-of-band values), so a
row INDEX is not a second-offset — yet two consumers rebuilt event time from the index anyway and
disagreed with each other AND with the row's own parsed stamp: the bus export used `t0 + idx·dt` (a uniform
stretch that smears a dropout evenly across the night) and `oxydex-fusion` used `t0 + idx·1000` (hard-coded
1 Hz). On a real lossy night the worst desat landed **422 s / 849 s** from its true time — against a
coincidence gate of LEAD 15 s / TRAIL 60 s, which made the desat↔surge "confirmed %" pure noise. Events now
carry `tMs`/`startTMs`/`endTMs` taken from their own rows; the index mapping survives only as a fallback for
a legacy stamp-less event. The equiv fixture is a CLEAN clip, which is exactly why the gate never saw this.

**§9 — whole-night windows.** `computeSpO2FFT` and `computeDFA` head-sliced to the first 3600 samples (~1 h
of a 6–10 h night) with no disclosure. Neither cap was performance-motivated (DFA is O(N), the probe
O(11·N)). The surfaced **FFT Cycle Length** — the periodic-breathing / Cheyne-Stokes cycle number — changed
on ~30 of 39 real nights; the committed `_0439` fixture moves 100 s → 143 s on this fix alone. This is the
same defect the sibling `computeHREntropy` already fixed with whole-night decimation; it was applied to one
of three siblings and these two were missed.

**§7 — REM plausibility.** The proxy's criteria ("still + low HR SD + HR near the night mean") describe the
bulk of quiet sleep, not REM (REM shows *increased* HR variability). All 39 real O2Ring nights return
39.6–87.8 % "REM" (median 77.5 %) against a norm of ~20–25 %, and **every one rendered as KPI colour
"good"** — the more the estimator over-fired, the healthier it looked. Re-deriving an oximetry REM detector
is research, not an audit fix; what the node must not do is assert an impossible number as a healthy
finding. It now self-reports `plausible:false` with a reason past a 30 % ceiling, the KPI never renders
'good' for it, and the Integrator refuses to fold an implausible proxy into the staging consensus —
re-checking the ceiling itself, so LEGACY exports are caught too. This is the node-side half of
INTEGRATOR-FUSION-ISSUES §T2, which was marked ✅ but only ever shipped the Integrator half, leaving a
standalone OxyDex user (the common case) with the bare number and nothing to disagree with it.

Both OxyDex fixtures regenerated by re-running `compute()` on their committed inputs (never hand-edited).
