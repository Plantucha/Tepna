---
bump: minor
type: added
brief: VERDICT-CONTRACT-2026-09-21-BRIEF.md
---

VERDICT-CONTRACT wave 2, capture-host lane: eight producers emit `tepna.verdict/1` through `verdict.py` (#2803) — `ble_sniff` (a link was followed) · `o2ring` (the selftest's three known-answer vectors) · `parse_dat` (trailer self-consistency, both pre-stated bands) · `probe_oxyii_0x03` (the 2 % candidate band: 112.9 / 125.0 Hz) · `probe_rtc_read` (a clock candidate per opcode, from the one `clock_candidates` rule `diff` prints) · `tools/ax210_postinstall` (the checklist) · `tools/scan_coexistence` (the matrix, INCONCLUSIVE → UNDERPOWERED) · `unseal` (converged onto `verdict.make`/`unknown`). Each builder is pure over the record its tool already computed, prints the object after its prose, exposes `--verdict-sample` (corpus-free, no radio), and its manifest row is `adopted` — 18 adopted / 18 validated under verdict.js. The two BLE probes guard their `bleak` import so the sample runs where no radio stack is installed; `unseal`'s row reads a committed sample file pinned to the live reader by test, because the static runner's python3 carries no `cryptography`.
