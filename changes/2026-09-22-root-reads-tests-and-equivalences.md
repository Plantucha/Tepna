---
bump: patch
type: fixed
brief: none
---

The tests that kill `root_reads`' surviving mutants, which #2872's widening merged ahead of: a real `git init` fixture exercising the production path (the fallback was the only branch any test reached), per-branch followers named in exactly one place so a `continue → break` is observable, a three-segment self-reference path, and an absolute literal pointing inside the root. Plus the eight equivalence entries for what cannot be killed — the `timeout` value, and the `split`/`rsplit` maxsplit and index variants — each with the probe that was run.
