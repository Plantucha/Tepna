<!-- SPDX-License-Identifier: Apache-2.0 -->
---
bump: patch
type: fixed
nodes: [integrator]
brief: CITATION-ATTRIBUTION-FOLLOWUPS-II-2026-08-08-BRIEF.md
---
Discharges §1, the debt the citation work created and could not settle itself. A citation lives in a DSP comment and a DSP is inside the compute closure, so `computeHash` moved on all five rebuilt bundles and §🔏 owed a re-verification that no machine without the corpus can run. Run against the real corpus: **the suite came back green and one fixture needed re-stamping** — `integrator_tch_golden.node-export.json`, `verifiedUnder f7173b2d12b9 → 95653b64ea78`; the other 13 were already current. Worth noting what the moved hash says: `f7173b2d12b9` is exactly the Integrator `computeHash` recorded immediately after the citation rebuild, so the closure has moved again since under other Integrator work, and this stamp records verification against the code as it stands rather than as it stood. No export byte moved — nothing needed regenerating — but that is now a **computed** result from a real corpus run rather than the "export-inert" assertion §🔏 abolished. `tools/release.mjs` no longer refuses on an unverified corpus-backed fixture.
