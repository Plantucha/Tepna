---
bump: patch
type: changed
brief: CAPTURE-HOST-2026-06-29-BRIEF.md
---

`CAPTURE-HOST-2026-06-29` stamped DONE after verifying every ☐ against the running appliance over SSH:
capture service active, 11 night directories, live `status.json`, all five how-to-collect notes, and —
the actual gate — all three live devices round-tripping (40 nights × ECGDex/OxyDex/PpgDex). Records the
divergence that matters: the box is an **x86_64 mini-PC, not the Raspberry Pi** §5 specifies, and `caddy`
serves in place of the named `tepna-web` unit.
