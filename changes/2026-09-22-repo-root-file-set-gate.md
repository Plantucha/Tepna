---
bump: minor
type: added
brief: none
---

`docs-ledger` check9 pins the repo root as an **equality over named classes** — `classified + unclassified = total` across the documented doc/OSS/config names and the runtime extensions CLAUDE.md §📁 lists. A zero-byte extensionless `Data` sat on `origin/main` for hours because every available shape of check fails open on it: it matches no bad pattern, and a file-count floor rises when a stray is ADDED. The root is now read from git (`rootTrackedFiles` — tracked paths, not `readdir`, because in a linked worktree `.git` is a file), and the self-tests plant the measured defect rather than a hypothetical one.
