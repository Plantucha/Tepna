<!-- SPDX-License-Identifier: Apache-2.0 -->
---
bump: patch
type: added
nodes: [GlucoDex]
brief: MUTATION-COVERAGE-SELECTION-2026-08-14-BRIEF.md
---
The kill lead the equivalence harvest surfaced, executed: 7 assertions pinning excursion meal
annotation — the path where a surviving mutant (`negate: drop !` in `matchMeal`) nulled the meal
attribution of every excursion precisely when meal markers EXIST, and nothing red because no
assertion ever drove an excursion with markers present.

The group reuses the postprandial group's designed lunch curve (trough 106 → peak 160: rise 54,
rate 1.2 — trips the detector by construction) but reads `res.excursions`, the markers' other
consumer: label, category and `annotated` all ride through; a no-markers control pins the honest
orphan (null, never fabricated) in both directions so hard-coding cannot pass; and a far-away
marker pins the −20…+75 min window rather than "any meal anywhere".

Verified by re-applying the exact surviving mutant: 3 assertions red (`got null · want "Lunch"`),
restored to 7/7 green.
