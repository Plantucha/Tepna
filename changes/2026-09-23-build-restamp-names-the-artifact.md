---
bump: patch
type: fixed
brief: none
---

build.mjs's re-stamp line names the artifact it wrote (the provenance fragment) and reports the embedded golden stamps it did NOT write, with the regen tool to run — the old count read as completion while covering one of two artifacts.
