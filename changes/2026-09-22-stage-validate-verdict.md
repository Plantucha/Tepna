---
bump: patch
type: changed
brief: VERDICT-CONTRACT-2026-09-21-BRIEF.md
---

tools/nsrr-stage-validate.mjs becomes a DECLARED pool scorer — `CRITERION` (pooled Cohen's κ ≥ 0.60, the E1 band pre-stated in SHHS-EXTERNAL-VALIDATION-2026-09-04 before its run) + `verdict()` mapping E1's three bands onto the closed enum, `suspect-shift` misalignment → UNKNOWN, < 10 records → UNDERPOWERED; `--dir` emits the same tepna.verdict/1 object, `--verdict-sample` for the adoption gate; manifest row adopted.
