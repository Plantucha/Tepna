---
bump: minor
type: added
brief: PAT-RELATIVE-REFRAME-2026-08-17-BRIEF.md
---

The ΔPAT validation seam, wired end to end: leave-self-out baseline in `patDipEvents` (a centered
median over a locally monotone stretch IS the centre element, so a self-inclusive baseline made
dev ≡ 0 exactly — first misdiagnosed as fiducial quantization), a `shiftFeetMs` surrogate mode inside
the detector, overlap-aware ankle pairing and a measured chance line in `pat-dip-index`, and
`tools/pat-dip-validate.mjs` — per-night CPAP clock anchoring on the independent desat pair, a
coverable-events Katz fraction, and an onset-shift null.

Three of its own first runs each caught a defect of the repo's recurring shape: an anchor criterion
that could not succeed on a treated night, a denominator counting uncoverable events, and a foot-shift
null so destructive the readability gate refused every surrogate.

First anchorable night (08-14, δ = −23.5 min): **Katz 7 % vs chance 36 %** — sub-chance, n = 1 night,
recorded without softening. The ankle leg's dip index itself is readable on 3 of 4 nights (floors
4.9–17.1 ms, analytic lifts 3.8–15.6).
