<!-- SPDX-License-Identifier: Apache-2.0 -->
---
bump: patch
type: changed
nodes: [suite]
brief: GENERATOR-FOLLOWUPS-III-2026-08-08-BRIEF.md
---
§1 asked whether parked briefs are parked for reasons that are still true — the brief's own motivating case being a §3 recorded as "DECIDED: deliberately single-recording" whose real cause was a renderer sitting in a worker file the app could not load, so nobody had decided anything. Six checkable park claims were re-checked against the tree and **two are stale**. `INTEGRATOR-THREE-CORNERED-HAT-FOLLOWUPS` parks §1/§2 as "blocked on upstream OxyDex per-epoch-HR export", but that export is DONE (2026-07-04) and `oxydex-dsp.js:6618` ships the per-epoch cross-node series under a comment naming this brief's own §2 — and the header is self-contradictory, stating both the block and, further along, that the code landed. `INTEGRATOR-PAT-VASCULAR` parks on the drift criterion being "unmeasurable with this instrument" and single-host vs phone-stamped capture being "indistinguishable"; both instruments now exist and postdate the park by five days — `tools/dual-clock-rate.mjs` measures device-vs-host rate directly from the two columns in every raw fragment, and `clock.js:356` publishes `independent = spreadMs > 2 ms`, precisely that discriminator. That park nonetheless **still stands on its other leg** (NO-GO on coupling, measured twice), which is what makes the stale sentence expensive: it is the one a future reader would cite when deciding whether to re-open. Upheld: PPGDex's missing `n0614a` companions (the capture corpus starts 2026-07-16, zero June directories) and EEGDex's absent EEG corpus. Reported, not fixed — re-affirming a park is cheap, executing what a refuted one hid is a work-unit each. Two method notes recorded: a capability phrase anywhere in a header is not a park reason, and a date matched inside a filename is not a date (`*0614*` matches the time field `…180614_PPG.txt`).
