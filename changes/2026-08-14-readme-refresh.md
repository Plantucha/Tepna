<!-- SPDX-License-Identifier: Apache-2.0 -->
---
bump: patch
type: changed
---
Bring `README.md` back in line with what the suite actually ships. **MotionDex was absent entirely** — it has its own bundle, DSP/registry modules and a `provenance/MotionDex.json` fragment, sits in `MANIFEST_BUNDLES` as one of the 9 GATE-A bundles, and is consumed by `integrator-dsp.js` (41 references) to motion-gate HRV, but the README still read "7 live · 1 planned". It is now in both roster tables, with its two genuine gaps stated rather than papered over: no reference guide, no landing-page tile. **`capture-host/` was also absent** — 189 Python files and a separate gate (`ruff` · `shellcheck` · `pytest` at a 100 % statement-and-branch floor over 3,200+ tests) that `npm run check` does not cover — so the Health Box now has a section saying it is a producer outside the browser suite and gated on its own lane. Replaces the `"75+ metrics"` framing with the measured count and its tier distribution (404 across the 8 node registries: 118 measured · 84 validated · 78 emerging · 96 experimental · 28 heuristic), which makes the surface-area-vs-confidence argument concrete instead of rhetorical. Also corrects the test figures against a real green run — **4,650+ assertions / 305 groups → 7,333 assertions / 470 groups** (`node tests/run-tests.mjs`, exit 0, 12 skipped) — the no-network scope (8 → 9 provenance-gated bundles), the doc count (~180 → ~400 briefs), and refreshes the momentum note from v2.0.0 to the 2.1→2.5 mutation-testing and host-axis arc. Every figure was re-measured from the tree, and the paper's headline numbers (47.7 ppm, 11 nights, 145,586 coupled beats) were re-verified against `papers/wearable-clock-drift.html` rather than carried forward.
