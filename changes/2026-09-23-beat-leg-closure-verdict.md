---
bump: patch
type: changed
brief: none
---

tools/beat-leg-closure.mjs emits its run as one tepna.verdict/1 object, and its adoption row stops claiming its words occur only in prose. The criterion is the tool's own contract bar, BL_MAX_BOUND_PPM: legC already refuses to return a ppm when the unexplained excursion implies a wider rate half-width, so PASS carries ppm and boundPpm together (never one without the other) and a bound above the max is UNDERPOWERED - a measurement too imprecise to quote, not a failure - as are too few beats, fewer than four blocks and no slope pairs 60 minutes apart; no seed is UNKNOWN; a beat-extraction failure is NOT_RUN. --pred prints a closure residual and declares no agreement verdict, so FAIL is reachable from nothing and criterion.name says so. The population unit is the night-pair, because the tool decides one thing per H10+Verity pair and its refusal object exposes no block count; blocks and beat counts ride in result. WO_CLAIM_RATCHET shrinks by one.
