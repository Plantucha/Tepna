---
bump: minor
type: added
brief: MEASUREMENT-PROVENANCE-ROADMAP-2026-08-26-BRIEF.md
---

PpgDex is the third `measurement` emitter (roadmap §12): whole-record `hr` and `rmssd` blocks with lineage on every export, schema 2.1, code identity from the bundle's `<html>` stamp via the app layer and the regen tool — and the rule the row carries: a `clock-seam` refusal ⇒ NO block, `measurement: null` with a `measurementReason` naming the seam (§∅ owner ruling 2026-09-17); six PpgDex fixtures regenerated.
