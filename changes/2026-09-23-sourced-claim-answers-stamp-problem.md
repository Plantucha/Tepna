---
bump: patch
type: changed
brief: none
---

Records that `2026-09-07-drain-stamp-asserted-a-count-it-never-read`'s mechanism question is already answered by §8d¹'s **sourced** `CLAIM <name> = <value> FROM <path>#<pointer>`, which re-resolves a written number rather than merely naming the query that produced it — strictly stronger than the candidate that row lists. Scoped to `**Status:**` headers only (526, not 526 documents) so it stays off the prose-scanner path §8d¹ refuses on measured grounds, with a second same-day instance from another session as the argument that opt-in marking alone is insufficient. Not built: the extension is a population change with its own false-positive profile.
