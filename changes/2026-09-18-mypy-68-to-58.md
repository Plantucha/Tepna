---
bump: patch
type: fixed
brief: PYTHON-TYPES-AND-FORMAT-2026-08-27-BRIEF.md
---

capture-host's mypy count drops **68 → 58**; `MYPY_BASELINE` banked 68 → 58. Re-measured before starting,
per the bar: **68 in 29 files** on `origin/main` `d5a9adaf`, matching the banked baseline exactly — no
drift since the last batch.

⚠️ **Four of the ten were mine, introduced by the previous batch.** `load_cases` returns `(cases, skipped)`
— a tuple of two lists — and I annotated it `list[...]`. mypy then errored the return AND the caller's
unpack. A wrong signature does not stay local: it convicts every correct call site downstream, so an
annotation that is confidently wrong costs more than no annotation at all. Caught by the checker rather
than by review, which is the argument for running it to zero.

The other six, each one declaration:

* `cpap_spool`'s `summary` literal is heterogeneous — two counters, a cursor, two nullable outcome slots.
  Un-annotated, mypy joins the value types, so `summary["bytes"] += len(body)` read as `None + int` **and**
  `str + int` on the same line. **4 closed.**
* `polar_psftp._client` is `BleakClient | None`, set on connect and used unguarded in `get`/`query`.
  Narrowed with `assert ... is not None` and a note that the methods are only reachable inside the
  `async with` — the house pattern already used at `writers.py:738` and `:948`. **2 closed.**

⚠️ **A stale number in prose, and it was the one handed between sessions.** `check.sh`'s header comment
still read *"baseline 99, 2026-09-13"* after the constant moved to 68 — I banked the ratchet last batch
and did not update the prose, and that stale figure was then relayed to me as the live count. The comment
now points at `MYPY_BASELINE` instead of repeating it: **a number written twice has one copy nobody
updates.**

**The hardcoded-literal class was audited, not just fixed.** `test_AN_IMPROVEMENT_SAYS_TO_BANK_IT` (fixed
last batch) hardcoded a count that the ratchet moved past, so banking progress reded the test written to
reward it. Searching the rest of the suite for the same shape found **no other instance**: the remaining
fixed numbers in tests (a 10 s timeout, 61.0 s, `sample_count == 18311`) are fixtures' own values, which no
ratchet moves. The class has exactly two members and both are closed — the test, and the prose above.

This batch banks the baseline and then **re-runs the full gate after that edit**, which is the step whose
omission reded CI last time: the local gate had been green for a tree that no longer existed.

Gate: `capture-host/check.sh` all green — 7282 passed, coverage 100.00 %, mypy 58 at baseline 58.

**58 remain**, largest cluster `arg-type` (~21).
