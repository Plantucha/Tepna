---
bump: patch
type: fixed
brief: none
---

Four shipped nodes' codegen manifests declared `"status": "planned"`. ECGDex, PpgDex, PulseDex and
GlucoDex are all in the canonical 9 of `provenance-ledger.js:30` — the same category as OxyDex and
CPAPDex, which already carry `alpha`. Corrected to match their siblings; no word was invented.

EEGDex is deliberately LEFT at `"planned"`, and the residue row that asked for it to change is
WITHDRAWN as premise-false. That row saw `eegdex-dsp.js` existing and inferred the status was stale —
invented consequence, the class that produced a wrong remedy elsewhere tonight. EEGDex has no app, no
render, no `.src.html`, no bundle, no provenance fragment and no GATE-A membership, and is PARKED for
want of a corpus (re-checked: still 0 files matching `*muse*`/`*eeg*`). A node parked for want of data
should read as not-shippable.

THE FIELD HAS EXACTLY ONE CONSUMER: `codegen/dex-gen.js:451` interpolates it as display text. Nothing
branches on it. A same-named `.status` at `overdex-app.js:568` is `fusion.kernelAudit` kernel drift —
an unrelated field, checked rather than assumed.

So the field is DEAD for the four corrected nodes, whose reference guides are hand-authored and carry
zero status strings, and LIVE for EEGDex alone, whose guide IS the generated page. Fixing dead strings
is still worth four edits: they are dead only because those nodes have hand-authored guides, and the
day one loses its guide — or anyone adds a second consumer — four wrong strings go live at once.

⚠️ NO GENERATED OUTPUT MOVES. Only EEGDex has anything under `codegen/generated/`, and its manifest is
unchanged, so nothing is regenerated by this PR.

⚠️ AND AN ABSENCE EXPLAINED RATHER THAN LEFT: codegen covers 7 manifests for 10 node-ish things.
HRVDex, MotionDex and Integrator have ZERO codegen presence, so the manifest set is a partial adoption
rather than a complete registry. Stated as measured; whether that is by design is not claimed here.

The surviving design question — `status` is one word for a spectrum, and whether the generated page
should list what EXISTS instead of a word — is logged as residue
`2026-09-19-manifest-status-is-one-flag-for-a-spectrum` and deliberately not answered by editing.
Inventing a sixth vocabulary value is the failure the brief-status rules already gate against.
