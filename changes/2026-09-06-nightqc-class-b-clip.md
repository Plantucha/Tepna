<!-- SPDX-License-Identifier: Apache-2.0 -->
---
bump: minor
type: added
nodes: [capture-host]
brief: none
---
Night-QC class-B quality signatures: `clip` regions pinned at a stream's own observed extremes (both
rails, no declared bound, no value list) and a computed `held` mark for zero-order-hold streams, both
emitted through the shared `emit_run` sidecar seam. Beat-marker values are declared as annotations and
stepped over before any shape is measured — without that a fifth of clip events fragment and the
population reads as two mechanisms instead of one (686/689 approach ramps read monotone with the ring's
`156` excluded, against 67-79 % with it left in, over six nights). No `collapse` rule: it was specified,
built, and measured away to 3-17 regions in 843,032 samples.
