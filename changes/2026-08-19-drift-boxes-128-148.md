---
bump: patch
type: fixed
brief: CROSS-DEVICE-DRIFT-FOLLOWUPS-2026-08-17-BRIEF.md
---

**Two boxes updated from evidence — one half-closed, one sharpened, and a premise corrected.**

**Box 128 — `[ ]` → `[~]`.** #1530 landed *sharing one implementation*: `dual-clock-rate.mjs` delegates
to `crystalVerdict`, its duplicate constant is a re-export, and 14 assertions pin that the two entry
points agree. **The other half — *reads uncertainties* — cannot be done from that side at all:** the
tool computes no per-fragment σ, while `device-stability.mjs` sources its own from σ_y via Allan
machinery this tool does not run. So the shared verdict takes its no-uncertainties branch by design,
and §2's *"protected today only by its length filter"* still stands. Recorded as half, not claimed as
whole.

**Box 148 — still open, now with a decision rule attached, and §4's premise corrected.**

⚠️ **§4 says `inverseVarianceWeights` is "exported" and idle. It is exported AND USED**, at
`integrator-tch.js:428` inside the three-cornered-hat decomposition. The true, narrower claim is that
**`fitClockOffsetPooled` does not use it** and the two sit one file apart.

⚠️ **`device-stability.mjs:152` declines to reuse it and claims to close this item** — *"(This is the
§3.4 open item, answered with a reason.)"* — **but it answers a different consumer's question.** Its
reason: the function **floors each σ² at 8 % of the largest**, and where the σ span two orders of
magnitude (2.4 ppm vs 376 ppm) *the smallest is the most trustworthy*, so the floor would discard
exactly the fragment carrying the answer. Decisive for `device-stability`; silent about
`fitClockOffsetPooled`, whose per-channel σ separation is the unmeasured quantity this box asks for.

**So the extraction now carries its own decision rule:** widely-separated σ ⇒ the floor makes
`inverseVarianceWeights` wrong there too, for the reason already written next door; comparable σ ⇒ the
floor is harmless and equal weighting was never costing much. Either way the σ decides it — which is
why the extraction, not the wiring, is the open item.

Not extracted here: it needs a corpus pass, and bulk traversal is one of the operations currently
wedging on this volume (18 processes in D-state).

**Box 127 deliberately untouched.** Its own brief says the `atShortestPpm`/`atLongestPpm` addition is a
three-line edit that re-stamps all 8 provenance fragments, and to *"land it with other spine work, not
alone."* There is no other spine work in flight, so landing it alone contradicts the brief's own cost
reasoning.
