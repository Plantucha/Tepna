<!-- SPDX-License-Identifier: Apache-2.0 -->
---
bump: patch
type: changed
brief: ACQ-EVIDENCE-CONTRACT-2026-08-24-BRIEF.md
---

The Acquisition Evidence Contract is **DONE — all 22 §21 criteria met**. #1809 closed the last one
("O2Ring LIVE acquisition can produce it"), and this re-audits and stamps the brief.

The earlier refusal to stamp DONE at 21/22 is **kept in the brief rather than tidied away**, because it
is the reusable part: three phases shipping was not the same as the contract being complete, and a DONE
over an unmet criterion would have been the exact false-completion this contract exists to prevent — an
envelope that says COMPLETE about an acquisition that was not. The stamp is honest now because the
criterion is met, not because the phases finished.

The original 🔴 finding is preserved under the ✅, since the SHAPE of the gap is what generalises:
half-built machinery reads as unstarted unless someone names it, which is why the audit called it out
explicitly instead of leaving it in an unchecked box.

Also records the §1-map ratification with its evidence (2026-08-24, two refinements folded in; every
phase since was assigned and executed against that map) rather than ticking it bare.
