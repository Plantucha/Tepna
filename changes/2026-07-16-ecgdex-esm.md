<!-- SPDX-License-Identifier: Apache-2.0 -->
---
bump: patch
type: changed
nodes: [ECGDex, suite]
brief: ESM-MIGRATION-FOLLOWUPS-2026-07-16-BRIEF.md
---
ECGDex → ES modules (fan-out node 3/7). `ecgdex-app.js` `import`s `{ ECGDSP }` from the DSP and `{ ECGUI }` from render (repointing the clean `const DSP = window.ECGDSP, UI = window.ECGUI` capture); `ecgdex-render.js` publishes `window.evBadge = evBadge` before scoping it into the module (the app's bare `evBadge(...)` calls + the classic profile's guarded calls still resolve — mirrors glucodex-render). `ecgdex-dsp.js` and `-render.js` are DUAL-MODE (keep `window.ECGDSP`/`ECGDex`/`ECGUI` attaches + export for the bundle); `ecgdex-morph`/`-profile`/`-cross`/`-registry` stay classic. The inlined `WORKER_SRC` in `ecgdex-app.js` is a hermetic template string (references no module binding), so the export does not touch the worker realm. Marked `ecgdex-dsp.js` `type=module` in both orchestrators (`Data Unifier`, `OverDex`) and re-bundled them + ECGDex.

**Worker co-load bridge extended.** `ecgdex-dsp.js` is `importScripts`-ed by four analysis-page workers — `pat-feasibility-worker.js`, `sensor-trio-worker.js`, `qrs-equiv-worker.js`, `qrs-yield-worker.js` — which a classic worker cannot do once the DSP carries a top-level `export`. Each now routes its co-load through the same inline `loadScript` fallback as cohort-worker (importScripts → on module-syntax error, fetch → `DexBuild.classicify` → eval; `build-core.js` is worker-safe). Verified in real Workers: pat-feasibility reports `ok:true` (ECGDSP + PPGDSP both loaded), the other three reach `ready` with no error. `tools/trio-batch.mjs` (vm realm) + `Dex-Test-Suite.html` (main() eval) also classicify the DSP; `tools/tch-reference-validation.mjs` already did (from the cpapdex step).

`computeHash` moved (`f9485d26588d → b2b1e77590cf`, `manifestHash → e5e0479c9492`) but is EXPORT-INERT BY VERIFICATION — the equiv leg reproduces byte-identical and `verify-fixtures.mjs` re-stamped the corpus-backed `ECGDex_2026-06-27_equiv` fixture after a green real-corpus run (`--check` 0 unverified). `ecgdex-globals.d.ts` stays until the classic co-load path retires (Phase 4). Full local gate sequence green incl. browser lane (`Dex-Test-Suite?full` all-green 2594 passed, `bootSkips:[]`; `verify-provenance` GATE A/B; `no-network`), biome + tsc clean; direct smoke: ECGDex.html boots + `evBadge` published, 0 page errors.
