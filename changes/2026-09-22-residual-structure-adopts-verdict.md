---
bump: minor
type: added
brief: none
---

`tools/pat-residual-structure.mjs` emits its run as one `tepna.verdict/1` object against its own pre-stated band (`rho1 >= BAND_STRUCTURED` 0.30), and adoption forced the population to be honest: five bare `continue`s used to drop a night out of the TALLY with no record, so `excluded` is now a named skip census and `checked + excluded = eligible`. Its `verdict-adoption.json` row moves from a copy-pasted `word-only` exemption to `decides`/`adopted`, shrinking `WO_CLAIM_RATCHET` from eight to seven.
