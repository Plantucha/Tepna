---
bump: patch
type: changed
brief: none
---

`event-coupling.js` states its FIFTH caveat — the circular-shift null cannot see SHARED MODULATION, so two independent streams driven by one process finer than the ±5–17 min shifts read as coupled (30 % of nights at the 5-min bout scale, 87.6 % at 90 s, measured over 400 SHHS1 records) — and the Integrator's published `apneaCoupling` explainer carries the same scale statement, since that is where a consumer of the export reads what a significant lift licenses. No per-record flag: the diagnostic was measured and rejected at AUC ≤ 0.76 against a pre-registered 0.8.
