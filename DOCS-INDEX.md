<!--
  DOCS-INDEX.md — Tepna
  Copyright 2026 Michal Planicka
  SPDX-License-Identifier: Apache-2.0
-->

# Tepna — Documentation Index & Site Map

**The single entry path over the suite's ~60 markdown documents.** The repo accreted a lot of
briefs, audits, and READMEs; this page is the map. Start at the top group and only go as deep as
your task needs.

> **Role legend** — *Authoritative* = a rule you must follow · *Spec* = a format/contract ·
> *Brief* = a build/work plan for one task · *Audit* = a findings report · *Reference* = a guide to
> read · *Narrative* = context, not spec · *Historical* = superseded/done, kept for the record.
> When a *Brief*/*Audit* and an *Authoritative* doc disagree, the authoritative doc wins.

---

## ⚡ At a glance — live surfaces (open these first)

**Here for the product, not the source tree?** These are the live HTML pages a reader actually wants
first — this index maps the ~60 *markdown* docs; the block below surfaces the *HTML*. Open any directly.

- **Front door:** [`index.html`](index.html) — the landing / device picker.
- **Per-node technical references** (what each Dex measures · every metric evidence-graded):
  [OxyDex](OxyDex%20Reference.html) · [PpgDex](PpgDex%20Reference.html) · [PulseDex](PulseDex%20Reference.html) · [ECGDex](ECGDex%20Reference.html) · [HRVDex](HRVDex%20Reference.html) · [GlucoDex](GlucoDex%20Reference.html) · [CPAPDex](CPAPDex%20Reference.html)
- **Architecture & science:** [`Architecture.html`](Architecture.html) · [`Science.html`](Science.html) · [How It's Wired — the Dex Suite](wiring/How%20It's%20Wired%20-%20the%20Dex%20Suite.html)
- **Papers:** [`papers/papers.html`](papers/papers.html) — working preprints, each regenerated from its tool.

> Guide filenames contain spaces (`OxyDex Reference.html`) → every target above is `%20`-encoded so
> crawlers resolve it. Those spaced names are **frozen cross-references** (the badge gate + deploy
> manifest fetch them verbatim) and are deliberately **not** renamed — see
> [`REPO-DISCOVERABILITY-2026-07-03-BRIEF.md`](briefs/REPO-DISCOVERABILITY-2026-07-03-BRIEF.md) §2.

---

## 0 · Read first (orientation)

> **Repo layout (2026-07-03).** Root holds only base/entry docs + standard OSS files + all runtime/build files (`*.js` / `*.html` / `*.src.html` / `*.css` / `*.json` — never move these). Archival docs live in **`briefs/`** (work-plans + pre-standard kickoffs/handoffs), **`audits/`** (findings, external reviews, fusion issues, validation status, audit prompts), and **`docs/`** (specs, derivations, analysis READMEs, `LEXICON.md`/`EVENT-LEXICON.md`, patterns, deploy + privacy statements, narrative). `ORIENTATION.md` stays in root (the test suite fetches it for the roster gate). New archival docs go straight into the right folder + get a row here.

| Doc | What it is | Role |
|---|---|---|
| [`CLAUDE.md`](CLAUDE.md) | Project conventions / constitution — the gates, the Clock Contract, the build-then-bundle rule, frozen names | **Authoritative** |
| [`ORIENTATION.md`](ORIENTATION.md) | **Start here** — the 60-second map: the Dex roster, the shared spine, the two gates, and where each fact actually lives (pointer-based, no variable catalog). Roster is gate-backed by a `tests/dex-tests.js` group | Reference *(living)* |
| [`AUDIT-PROMPT.md`](AUDIT-PROMPT.md) | Deep-audit charter for an AI/human auditor — this suite's specific bug-classes (units, clock, fabricated absence, silent fallbacks, differential drift, evidence honesty, provenance) + what NOT to flag + a reproduce-don't-trust method | Reference *(living)* |
| [`EFFICIENCY-AUDIT-PROMPT.md`](audits/EFFICIENCY-AUDIT-PROMPT.md) | Efficiency-audit charter (sibling of `AUDIT-PROMPT.md`, for the EFFICIENCY lane) — two lanes kept separate: **A** dev/agent-loop cost (gate scoping, the re-bundle→ledger dance, redundant bundling, doc navigation) and **B** app runtime (super-linear DSP, redundant recompute, worker use, bundle/DOM cost). Measure-don't-guess at real full-night scale; correctness always wins; carries an "already-addressed / out-of-scope" list so the 2026-07-01 scoped-runs + reconcile-reporter work isn't re-filed | Reference *(living)* |
| [`PRIVACY-SECURITY-AUDIT-PROMPT.md`](audits/PRIVACY-SECURITY-AUDIT-PROMPT.md) | Privacy & security audit charter (third sibling of `AUDIT-PROMPT.md` / `EFFICIENCY-AUDIT-PROMPT.md`) for a 100%-local, no-account health-signal tool — anchored to portable data-protection PRINCIPLES (minimization, storage-limitation, local control) for an **international** audience, **NOT** HIPAA/any single-nation regime. Flagship invariant: prove **NO-NETWORK egress**; also storage-minimization, export/PII leaks, untrusted-input→DOM (XSS), eval/CSP. Do-first = a two-layer `no-network` gate (static egress-grep + runtime throwing-trap probe) **with a required negative-control canary** — a planted-egress fixture the gate must catch — that promotes "100% local" from convention to enforced property | Reference *(living)* |
| [`MOBILE-AUDIT-PROMPT.md`](audits/MOBILE-AUDIT-PROMPT.md) | Mobile / small-screen behavior audit charter (fourth sibling of `AUDIT-PROMPT.md`) — the apps ship a correct viewport + `@media` CSS so a phone is an intended surface. Bug-classes highest-yield first: **body horizontal overflow** (#1), **tap targets < 44×44**, **sub-12 px readable text**, viewport-meta/zoom-lock, sticky occlusion, breakpoint-collapse & re-bundle regressions, touch-only (`:hover`) affordances. Carries a **ready-to-run Playwright harness** (local http server + populate via each app's "Generate synthetic" button + the measure closure) so the setup isn't rediscovered; presentation-only + EXPORT-INERT; out-of-scope list (chart-tick micro-labels, the known Integrator nav dup) | Reference *(living)* |
| [`TEST-AUDIT-PROMPT.md`](audits/TEST-AUDIT-PROMPT.md) | Test / CI audit charter (fifth sibling of `AUDIT-PROMPT.md`) — audits the **tests and gates themselves**, the orthogonal axis: *does each gate actually fail when the code it guards breaks?* Method is **mutation, not reading** — break the guarded code, confirm the gate reds; a surviving mutation IS the finding. Gate-failure classes highest-yield first: **a gate that passes on a dead path** (#1 — the PPG `worker≡serial` that passed on a dead pool), **static-listed ≠ executed ≠ asserted**, **fake coverage** (testing a copy not the shipped code), **circular known-answers**, **unreal fixtures** (integer-`fs` that hid the fractional-index bug), **silent skips that shrink the gate**, **asserted-not-computed claims** (the stale "EXPORT-INERT" that shipped), **partition/shard holes**, **two lanes that diverge**. Out-of-scope list names the classes a META-GATE already enforces (`expected-skips.json`, `verify-shard-union`, `FIXTURE-VERIFICATION-GATE`) so they aren't hand-re-filed — audit the meta-gate for holes instead. Cites the briefs each scar came from | Reference *(living)* |
| [`MOBILE-AUDIT-FINDINGS-2026-07-15.md`](audits/MOBILE-AUDIT-FINDINGS-2026-07-15.md) | **Report** — first run of the mobile charter (2026-07-15). **Layout is structurally sound: zero horizontal body-scroll on all 12 surfaces at 360/375/414, empty AND populated; viewport-meta correct on all 10; no mobile-only console errors; Integrator `.mobile-nav` dedupes to 1 at runtime.** Real defects are shared-class + fleet-wide: **F1** ~30–44 tap targets under 44×44 per analyzer (shared `.eb-btn` 104×32 · `.theme-toggle` 74×25 · `.synth-link` 124×18 · `.mode-btn` 48×22 · `select` h20 · `.sb-item` h38) — touch ergonomics; **F2** pervasive sub-12 px readable/finding/badge text (10–11.5 px) — legibility (global density, not a mobile regression); **F3 (low)** Integrator source triple `mobile-nav` (runtime-harmless). All CSS-only/EXPORT-INERT. Punch-list ends with "add a `browser-gates.mjs` 375 px no-overflow smoke". Coverage gaps: OxyDex populated + CPAPDex/DataUnifier/OverDex (file-only) not fully exercised | Findings *(report)* |
| [`PRIVACY-SECURITY-AUDIT-FINDINGS-2026-07-01.md`](audits/PRIVACY-SECURITY-AUDIT-FINDINGS-2026-07-01.md) | **Report** from running the privacy/security charter (2026-07-01). No-network invariant **HOLDS** (proven + now enforced by `no-network.html`). Demonstrated findings: **F1 (HIGH)** OxyDex stored-XSS via filename→`innerHTML`; **F2 (MED)** PulseDex reflected-XSS via comparison filename; **F4 (MED)** raw CSV + unscrubbed filename cached forever; **F5 (MED)** "Clear" doesn't clear + no complete "erase my data" path; **F7 (LOW–MED)** no CSP; **F3/F6 (LOW)**. Export posture already mature (not re-filed). Each finding → its own dated brief | Findings *(report)* |
| [`PRIVACY-SECURITY-AUDIT-FINDINGS-2026-07-13.md`](audits/PRIVACY-SECURITY-AUDIT-FINDINGS-2026-07-13.md) | **Report** — second run of the privacy/security charter (2026-07-13). **F1–F7 have held** (XSS removed/escaped, raw-CSV cache dropped, `dex-forget.js` erase-all inventory-gated, CSP + strict `script-src` on all 10 bundles, `no-network` gate now in CI). Three LOW / LOW–MED residues: **N1** the standalone analysis pages + `index.html` carry neither CSP nor no-network-gate coverage though they ingest recordings & persist checkpoints (which `dex-forget` already erases — a scope inconsistency); **N2** OxyDex `console.log`s the raw filename + raw CSV header bytes on every load (local-only, minimization); **N3** PpgDex's error toast is an unescaped `innerHTML` sink (F3-class, the node the escaper pass skipped). Each → its own dated brief | Findings *(report)* |
| [`no-network.html`](no-network.html) | **Gate** — the privacy analogue of `verify-provenance.html` (the charter's do-first). Origin-classified static egress grep of all 8 bundles + runtime throwing-trap boot + a **negative-control canary** (planted egress it must catch, benign local it must not) so a vacuous pass can't ship. Verdict via `window.__noNetworkOK`. Adds no app behavior (no re-bundle). **Follow-up:** wire into `tests/dex-tests.js` (both runners) | Gate |
| [`README.md`](README.md) | Front door — what Tepna measures well, the evidence ladder, the node list, the repo map | Reference |
| [`CONTRIBUTING.md`](CONTRIBUTING.md) | One-page orientation — read before the briefs | Reference |
| [`ADD-AN-ADAPTER.md`](docs/ADD-AN-ADAPTER.md) | The 5-minute contributor path — teach the suite one more vendor file (one new adapter, one gate, no node edit) | Reference |
| [`ARCHITECTURE-PRINCIPLES.md`](ARCHITECTURE-PRINCIPLES.md) | The durable design principles behind the suite — three layers, downhill deps, contracts-as-product, the epistemic/fusion principles, **plus §8 "Adding a new Dex" pointer-checklist** (the ordered index of where each rule lives, for EEGDex/SpiroDex; the adapter-only subset is `ADD-AN-ADAPTER.md`) | Authoritative |
| [`LEXICON.md`](docs/LEXICON.md) | Canonical terminology (Ganglior, Integrator, node, tier, …) | Authoritative |
| [`WHY-THIS-EXISTS.md`](docs/WHY-THIS-EXISTS.md) | The *why* — philosophy, constraints, jokes (styled mirror: `Why This Exists.html`) | Narrative |
| [`HANDOFF.md`](briefs/HANDOFF.md) | "Get current in one read" remediation handoff (dated — check the timestamp) | Reference |

---

## 1 · Cross-cutting contracts & specs (apply to every node)

| Doc | What it is | Role |
|---|---|---|
| [`CLOCK-UNIFY-BRIEF.md`](briefs/CLOCK-UNIFY-BRIEF.md) | How the Clock Contract (floating wall-clock ms + `getUTC*`) was unified across apps | Spec / Brief |
| [`CAPTURE-HOST-2026-06-29-BRIEF.md`](briefs/CAPTURE-HOST-2026-06-29-BRIEF.md) | The **Tepna Health Box** — a single bedside **Raspberry Pi** that auto-captures all 6 signals overnight, **serves** the bundled apps to the LAN (`http://tepna.local`, same-origin → shared profile/longitudinal store + runnable gates), and **stores** each night on an SSD (the durable source of truth). Three systemd roles (`tepna-capture` bleak supervisor · `tepna-web` static server · optional `tepna-analyze` nightly Node walk→compute→fusion). Per-device method (O2Ring onboard · H10 live PMD ECG mandatory · Verity onboard/PMD · Muse `muse-lsl`/`OpenMuse` for Athena · Lingo NOT raw → Health-export); TP-Link UB500 Plus / nRF52840 dongle bedside; emits the EXISTING vendor layouts so files route with NO new parser branch; Clock-Contract + device-id-filename companion-pairing contract; on-box privacy/backup-as-re-ingest ops. Hardware/daemon out-of-suite; the in-suite landing honors the gates | Brief *(PROPOSED 2026-06-29)* |
| [`HEALTH-BOX-VISION-2026-07-01-BRIEF.md`](briefs/HEALTH-BOX-VISION-2026-07-01-BRIEF.md) | **Tepna Vigil** — the **product-vision layer** on top of `CAPTURE-HOST` (which owns the *how*). Names the bedside box (**Vigil** — watchful, nocturnal; a product-surface label only, touches NO frozen identifier), fixes the **phased capability model** (v1 capture/serve/store → next auto-analyze/context → someday automate), gives the **live multi-signal view** the hero framing (raw traces + derived-metric strip, same node DSP as the apps, badges carry over), specifies the **context/night-story** surface ("why was last night bad?" — room · weather · life on one clock; fetched context is host-side, NOT app egress), and records the experience principles. Styled mirror: `Tepna Vigil — Health Box Vision.html`. Vision brief; ships no code | Brief *(PROPOSED 2026-07-01)* |
| [`CAPTURE-HOST-FOLLOWUPS-2026-07-16-BRIEF.md`](briefs/CAPTURE-HOST-FOLLOWUPS-2026-07-16-BRIEF.md) | **What the first real multi-sensor bring-up surfaced** (`capture-host/`, out-of-suite, PR #129). Took the scaffold to **three sensors streaming live at once** (H10 ECG · Verity PPG · Wellue O2Ring-S SpO₂/pulse) and built the `HEALTH-BOX-VISION §4` hero live-view (served monitor: device bond/remember + stream-aware scope + averaged-beat/pulse/oximetry panels). Fixes a **~10 % HR bug** — `writers.py` emitted `timestamp [ms]` integer+absolute, so ECGDex inferred fs=143 not 130 (real PSL is relative+fractional). Vendor-format quirks found: **Verity PPG START needs CHANNELS as a u8** (else err 0x0B/0x05) and streams **delta/compressed frames** (new `_decode_delta`); the **O2Ring is an OxyII/T8520 device, NOT legacy Viatom** — advertises `S8-AW`, ignores the legacy service, speaks a separate `0xA5`-framed protocol on the `e8fb` service (new `oxyii.py`: XOR auth → poll `cmd=0x04`, no bond/no AES); the **H10 needs a bonded PMD link**; BlueZ needs a **connect lock** for concurrent devices. `CAPTURE-HOST` stays PROPOSED (no real overnight round-trip / `how-to-collect` notes / Pi bring-up yet) | Brief *(PROPOSED 2026-07-16)* |
| [`MULTI-SENSOR-DERIVATIONS-2026-07-16-BRIEF.md`](briefs/MULTI-SENSOR-DERIVATIONS-2026-07-16-BRIEF.md) | **Scoping/agenda** — the derived values the Vigil multi-sensor capture (synchronized ECG · 4-ch PPG · dual ACC · GYRO/MAG · SpO₂ · RR) unlocks but the suite doesn't compute yet, each with its **published method source** + home node + evidence tier. Headline (Tier 1): **respiratory effort (chest ACC) → central-vs-obstructive apnea typing** (cross O2Ring desats) and **body position (chest-ACC gravity) → positional OSA**. Tier 2: cardiorespiratory/actigraphic **sleep staging**, fused **respiration rate**, **PAT** (not PTT), motion-gated cross-validated **HRV**. **Architectural blocker flagged:** ACC/GYRO/MAG have **no consumer node** yet (roster is single-signal) → needs a new motion/IMU node or Integrator ingestion. Compute in the apps only (never the box); each derivation becomes its own executable brief; extends `PAPERS-ROADMAP` §3. Cites Manoni'20, Ryser'22, Rocha'26, Fonseca'20, Schipper'24, Aktaruzzaman'17 et al. **Blocker RESOLVED 2026-07-17 (owner):** build a new motion node **MotionDex** → `MOTIONDEX-BUILD-2026-07-17`; the five IMU items route to future Integrator-fusion briefs that depend on it, PAT routes to the DONE `PAT-FEASIBILITY`. | Brief *(DONE 2026-07-17 — agenda routed)* |
| [`MOTIONDEX-BUILD-2026-07-17-BRIEF.md`](briefs/MOTIONDEX-BUILD-2026-07-17-BRIEF.md) | **Build brief for MotionDex — the fleet's motion / IMU node.** Spawned from `MULTI-SENSOR-DERIVATIONS` after the owner chose a new node over Integrator ingestion. Ingests the Polar Sensor Logger IMU streams (Verity ACC/GYRO/MAG + H10 chest ACC) on the Clock Contract and computes the **single-signal** motion metrics — **body position** (gravity-vector, Rocha'26), **actigraphy/activity counts**, a **thoraco-abdominal effort waveform** (chest ACC, Ryser'22), and motion-artifact SQI (→ Ganglior `conf`) — exporting `ganglior.node-export` (`node:"MotionDex"`). Cross-sensor fusions (apnea typing, staging, motion-gated HRV) stay in the Integrator and consume this export, NOT here. ESM from birth (fan-out complete); owned plain-inline bundle; full gate sequence incl. an equiv leg on a committed synthetic IMU fixture. **Unblocks** the five `MULTI-SENSOR-DERIVATIONS` Tier-1/2 fusion briefs. | Brief *(PROPOSED 2026-07-17)* |
| [`CAPTURE-HOST-FOLLOWUPS-II-2026-07-16-BRIEF.md`](briefs/CAPTURE-HOST-FOLLOWUPS-II-2026-07-16-BRIEF.md) | **Findings + concerns from the second bring-up** (out-of-suite, `645810d` on `claude/vigil-capture-parity` + uncommitted clock work). Full multi-stream capture (Verity PPG **4-ch** + **ACC/GYRO/MAG**, H10 **ACC**+**RR**, O2Ring motion/battery) via **PMD settings negotiation** + STOP-before-START + one multiplexed SSE; Tepna-unified monitor; **clock/NTP/timezone control** + **CLOCK_MONOTONIC-anchored** capture stamps. **To fix:** empty-vendor writer artifact; DST re-anchor non-monotonic edge; vestigial `incoming_subdir`. **Unvalidated:** GYRO/MAG/PPI decoders vs a PSL export (only delta path / motion-response seen), ACC frame-type, monotonic re-anchor vs a real NTP step, the sudo clock-apply path on a real box. **Deferred:** per-night clock provenance, `offsetMin` in exports, monitor lite-mode, and a **multi-sensor DERIVATIONS** agenda (respiratory-effort→central/obstructive apnea · body-position→positional OSA · PTT · motion-gated HRV · actigraphy) that should become its own brief | Brief *(PROPOSED 2026-07-16)* |
| [`O2RING-PROTOCOL-2026-07-17-BRIEF.md`](briefs/O2RING-PROTOCOL-2026-07-17-BRIEF.md) | **REFERENCE — Wellue O2Ring-S (T8520 "OxyII") BLE protocol + capabilities**, reverse-engineered + hardware-validated. It is **NOT legacy Viatom** (OxyII service `e8fb…`, advertises `S8-AW`, Random-Static MAC → resolve by name). `0xA5` frame envelope + **CRC-8 poly 0x07, no AES** (XOR auth w/ MD5 "lepucloud" salt). **Live** (auth 0xFF→setup 0x10→poll 0x04, 24-B header: SpO₂/HR/motion/battery/contact). **Stored-session download** — the `.dat` the phone app syncs: opcodes **0xF1 list · 0xF2 open+size · 0xF3 read-chunk · 0xF4 close**, needs **MTU≥517**; Format A = `[10B hdr][3B SpO₂,HR,status ×N @1Hz][48B trailer, O₂-score×10 @42]`. **Quirks:** advertises ONLY when worn, phone app grabs the single link (close it), short advertising burst (early-exit scan). **Validated:** pulled a 10 h night (SpO₂ 96.3%/HR 50), stored-vs-live SpO₂ MAE **0.76%** / HR **2.42 bpm**; **ring clock ~+151 s off NTP** → stored-`.dat` time needs correction. Code: `oxyii.py`, `pull_session.py` | Brief *(REFERENCE 2026-07-17)* |
| [`POLAR-OFFLINE-DOWNLOAD-2026-07-17-BRIEF.md`](briefs/POLAR-OFFLINE-DOWNLOAD-2026-07-17-BRIEF.md) | **Polar onboard offline-recording download (PS-FTP), wired into the Vigil monitor** — the Polar sibling of the O2Ring `.dat` puller; executes `POLAR-SDK-CAPTURE` Track A. Pulls the recording a Polar device stores in its own flash, over BLE, from the monitor's Devices view (**📥 Recordings** → list → Download). **USB is HID-only** (`0da4:0008` "Polar INW4J", `/dev/hidrawN`) — no mass storage, BLE is the only path. Protocol verbatim from the Polar BLE SDK: service `FEEE`, MTU char `FB005C51`, **RFC60** length prefix + **RFC76** air-packets (`bit0 next · bits1-2 MORE 0x06/LAST 0x02 · bits4-7 seq`), `PbPFtpOperation{GET,path}` → `PbPFtpDirectory`/raw file; hand-rolled protobuf, **GET-only**. Code: `polar_psftp.py` + `webmon.py` (`/api/recordings`,`/api/pull`) + `monitor.html`. **Bonding is mandatory** (un-bonded read → `UNLIKELY_ERROR 14`). **Hardware finding:** the Verity stores only an **exercise `.BPB` session (HR series, ~55-62 bpm)**, **NO raw-PPG `.REC`** — raw PPG is live-PMD-only; pulled + byte-verified a 7-file session. **Caveat:** trusted-device BlueZ auto-reconnect races the web pull (standalone CLI reliable after `bluetoothctl disconnect`); pause capture + idle device before pulling. Follow-ups: `SAMPLES.BPB`→CSV decoder, web-pull robustness, automate the SDK offline-recording button | Brief *(IN-PROGRESS 2026-07-17)* |
| [`TEST-AUDIT-FINDINGS-2026-07-18-BRIEF.md`](briefs/TEST-AUDIT-FINDINGS-2026-07-18-BRIEF.md) | **42 hollow gates found by mutation-testing the suite** (executes `audits/TEST-AUDIT-PROMPT.md`). 99 surgical defects planted in the shipped compute code + run against the full suite **with the real corpus** (`DEX_UPLOADS`); **42 stay green under a real defect** (27 high / 15 medium), 55 caught, 2 inconclusive. Each finding = exact `old→new` + why no gate sees it. Highlights: the Bessel **`÷N−1`→`÷N`** SD slip untested in **every** `*-cross.js`; boundary edges never planted (2000 ms beat, worst-10-min window on the last sample, coupling `t+hi`, ambiguous O2Ring night); **surfaced-but-unexported** metrics escape the equiv leg (Baevsky SI **×1000**, PI **×100**, SD1 `/√2`→`/2`); a Supine↔Prone posture flip; 3 provenance-verdict (`manifest-gate.js`) hollows that slip both node lanes. **Scope caveat: JS `run-tests.mjs` lane only — the Python `capture-host/` was NOT mutation-audited** (own pytest CI). Proposes 5 class-killing meta-gates instead of 42 one-offs | Brief *(PROPOSED 2026-07-18)* |
| [`SYNTH-TEXTURE-PAPERS-RERUN-FOLLOWUPS-2026-07-07-BRIEF.md`](briefs/SYNTH-TEXTURE-PAPERS-RERUN-FOLLOWUPS-2026-07-07-BRIEF.md) | **synth-gen v2.1 RR-texture re-fit** — running the parent paper-rerun at 20k surfaced 3 scale-only rMSSD-scatter artifacts (hard floor line, hard ceiling line, overplot vertical edge). Owner chose a full generator re-texture (no footnotes). **DONE:** `synth-gen/2.1` (`buildRR`: drop τ=2, white-noise 3→1.0, HRV-level-scaled `texF`) + `cohort-gen/1.9` (`rsaGainFor = clamp(√(t²−7²)/19.15, 0.06, 4.35)`) — floor spreads 8.6–19 ms, ceiling to ~76, bulk ±1 ms, DFA-α1 0.757 preserved; SubjectA re-fit; hrv-confound paper fully rerun (figures + prose). **REMAINING:** 5 more papers, SubjectA app-fixture regen + re-bundle + provenance, gates, changeset | Brief *(DONE 2026-07-11)* |
| [`POLAR-SDK-CAPTURE-2026-07-07-BRIEF.md`](briefs/POLAR-SDK-CAPTURE-2026-07-07-BRIEF.md) | **Polar BLE SDK as a capture foundation** — research/decision doc extending `CAPTURE-HOST`. The official SDK (`polarofficial/polar-ble-sdk`, BSD-3, Android/iOS only) is the authoritative decoder for the proprietary PMD streams (ECG/ACC/PPG/PPI) our hand-rolled `capture-host/polar_pmd.py` reverse-engineers with acknowledged compressed-frame caveats. **Track A (recommended first):** use the SDK as a *reference spec* to reimplement `polar_pmd.py`'s weak paths + prototype automated offline-recording fetch (upgrades the manual-button backstop). **Track B1:** an optional native Android **capture-only companion** — a cleaner replacement for third-party Polar Sensor Logger that emits the SAME vendor layouts + Clock-Contract stamps + device-id filenames (zero new parser branch); **B2** = companion-as-health-box (defers to Vigil). H10 robustness facts (skin-contact auto-disconnect, RR in 1/1024 s). Ships no code | Brief *(PROPOSED 2026-07-07)* |
| [`PAT-FEASIBILITY-2026-07-08-BRIEF.md`](briefs/PAT-FEASIBILITY-2026-07-08-BRIEF.md) | **PAT / vascular feasibility** — chest-ECG→ankle-PPG beat coupling; ~48 ppm inter-device clock drift over 11 nights; ACC-sync fails → single-host capture required (see `papers/wearable-clock-drift.html`). | Brief / DONE |
| [`TCH-REFERENCE-VALIDATION-2026-07-12-BRIEF.md`](briefs/TCH-REFERENCE-VALIDATION-2026-07-12-BRIEF.md) | **The three-cornered hat, measured against a TRUE reference for the first time.** The σ programme exists *because* there is no reference — so nobody could ever check the estimator. CPAP's PLD channel writes **measured respiration from a calibrated flow sensor**, so on the quad-modal nights there finally is one. Driven against it (67 epochs, 11 nights), TCH fails **two ways that are invisible without truth**: it is **blind to bias by construction** (ECG under-reads respiration by **1.35 br/min** — no reference-free method or artifact gate can ever see this), and its **independence assumption is violated** (ρ = **0.42** between the ECG and PPG error terms, because both read the *same* RSA proxy with the *same* estimator) — so it rates a calibrated flow sensor as noisy as an RSA-derived estimate. The repo's correlated solve **cannot** fix it: its `rho` is one **common-mode** term across all three pairs, not a pairwise structure. **Complements** `TRIO-ARTIFACT-GATE` (that brief found TCH is not robust to *artifact*, diagnosed by implausibility; this one has *truth*) — clean the epochs and both defects remain. Also finds two F1-class defects: `PPGDSP.lombScargle` never tracks the HF peak (hence `respRate: null`), and ECGDex computes respiration and exports neither estimate | Brief *(PROPOSED 2026-07-12)* |
| [`CPAP-REAL-CORPUS-2026-07-11-BRIEF.md`](briefs/CPAP-REAL-CORPUS-2026-07-11-BRIEF.md) | **First whole-SD-card run of CPAPDex** — ~180 real nights (1139 EDF, 222 sessions) driven through `CPAPDex.compute()`, 39 paired with O2Ring, **17 quad-modal** (CPAP+ECG+PPG+O2Ring). **Code-only by design** (the clinical read stays in a local gitignored log). Six defects synthetic fixtures cannot find: **the node-export drops 17 of 32 computed metrics** incl. the *entire* ventilation + flow-limitation lane (15 populated on ≥168/180 nights — and the dropped lane holds the corpus's strongest event-burden predictor, detrended r≈0.5, vs r≈0.16 for the pressure/leak fields we *do* export); the `mode` heuristic flips 57× on a bare `pressIqr>1.0` cut; **CPAPDex's "can't join the equivalence gate" blocker is FALSE** (binary multi-file EDF runs headless in Node — closes the fleet's last synthetic-only GATE-C, but the raw EDF headers carry a device identifier that must be scrubbed or synthesized around first); no ResMed ingest adapter; `buildLongitudinal` returns `crossNight:null` **silently** in any Node realm (inconsistent dep resolution); SA2 lane 0/222 real coverage. Proposes a shuffled-null **cross-node event-coupling primitive** (a chance baseline *inverts* the naive co-occurrence conclusion) and **CPAP flow as a respiration REFERENCE to validate the reference-free σ/TCH estimator** — the one experiment that programme can never otherwise run | Brief *(DONE 2026-07-12 — **P1 · P2 · P9 EXECUTED** (ventilation lane on the bus 15→30 metrics; binary-EDF equiv leg via a **committed synthetic** EDF set, no device serial published; and the discovery that **every** node's `compute() ≡ export` leg had been SKIPPING in CI because each input was a gitignored real recording — now every node has a committed synthetic input, so the suite's strongest correctness gate finally executes). **§P5 EXECUTED** — `event-coupling.js`, the shuffled-null coupling primitive, whose construction found **three defects in the prototype §M1's own numbers came from**, all biased toward believing: the null shift did not **wrap** (surrogates fell off the recording end → chance deflated → **lift inflated**, i.e. couplings manufactured); a **saturated** window (wider than B's inter-event interval) crushes lift to ~1.0 *by arithmetic* and was indistinguishable from "no coupling" (now flagged via the exact ceiling `maxLift = 100/chancePct`); and the whole-minute default shifts **resonate** with any round periodicity — a planted, *perfect* coupling scored **lift 1.006**, a false negative. §M1's conclusions survive; its magnitudes must be re-derived. **§P6 EXECUTED** → spawned `TCH-REFERENCE-VALIDATION-2026-07-12-BRIEF.md`: measured against a true reference for the first time, **the reference-free TCH estimator does not recover the true σ** — blind to bias, and its independence assumption fails (ρ = 0.42). **§P3 EXECUTED** — `adapters/resmed-edf.js` registered; CPAP was the fleet's only adapter-less signal type, and the premise ("EDF is binary, so no text-stream adapter") only ever ruled out a *text* adapter — it takes its bytes off the same `ctx` hatch `oxydex-spo2` already uses. The §F4 session-grouping rule is now gated **both ways**: both EVE files survive, AND the naive ±60 s-only rule *provably drops one*. **§P4 EXECUTED** — the `mode` heuristic was not badly tuned, it was **measuring the wrong thing**: the raw pressure IQR is dominated by **EPR** (a 1–3 cmH₂O drop on every *expiration*, sampled at 0.5 Hz), so a **fixed** machine with EPR=3 read "APAP". No constant could fix that. `mode` now rides a minutes-scale, EPR-immune P90 pressure envelope, **refuses to guess** (dead-band → `null`), and is **stability-guarded** at night level (every session must agree — the first session no longer names the night; the real 06-12 night now correctly reads `null`). Both surfaces shipping the old rule are gone, including a subtitle stating it to the user as fact. ⚠️ The two cut points are **not calibrated** against the ~180-night corpus (not in-repo) — the dead-band makes that safe, and calibration is carried in the follow-up)* |
| [`CPAP-REAL-CORPUS-FOLLOWUPS-2026-07-12-BRIEF.md`](briefs/CPAP-REAL-CORPUS-FOLLOWUPS-2026-07-12-BRIEF.md) | **What executing `CPAP-REAL-CORPUS` surfaced and did not close.** The `mode` estimator is now right but its **two cut points are set from physiology, not fitted to the corpus** (the fixtures agree — real nights 1.2/2.2, synthetic 1.08, fixed machine ~0 — but 1.08 sits uncomfortably close to the 1.0 cut; the dead-band makes the failure mode `null` rather than a wrong device setting, so this is an *unmeasured* gap, not a bug). `event-coupling.js` is gated but **dormant — no node consumes it yet**, and §M1's *magnitudes* were computed with the non-wrapping null and must be **re-derived** before publication (its conclusions survive). **`Dex-Test-Suite.html?full` has not been run since P4**, which changed two rendered surfaces. P7 (apnea→CVHR coupling on the quad-modal nights) and P8 (A4's dated step-changes as the first real test of `CPAPCross` change detection, which has only ever run on `sd:0` 'stable' synthetics) were never in the parent's Done-when and are now cheaper. Also: `build.mjs` re-stamps a fixture's `manifestHash` but does **not** recompute its `outputHash` — a fleet-wide gap `tools/regen-cpap-goldens.mjs` closes for CPAP only | Brief *(DONE 2026-07-12)* |
| [`EVENT-COUPLING-2026-07-13-BRIEF.md`](briefs/EVENT-COUPLING-2026-07-13-BRIEF.md) | **The `event-coupling.js` primitive — its contract, and the FOUR ways its null model lied.** The suite's "is it real or coincidence?" test: two frequent event streams co-occur *by construction*, so a raw co-occurrence rate is not evidence of anything — `coupling()` measures it against circular time-shift surrogates. Written up separately because the primitive was born inside a *CPAP* brief and corrected twice more in a CPAP *follow-up*, so its hard-won guards lived scattered across two documents about a subject it has nothing to do with — while the module still has **no node consumer**. **Every guard exists because it was learned the hard way, on real data, after the module shipped and its numbers had already been written up as findings:** a **non-wrapping shift** INFLATED lift (invented a ×3.3–10 coupling); **no coverage model** DEFLATED it (30% of apneas happened while the oximeter was *not recording*, and were scored as MISSES → manufactured *anti*-coupling at ×0.72); **no power floor** made a 16-event bucket look decisive (a ×0.0 published as *"provably no signal"* — chance alone predicts 0.9 hits there, so zero is a ~41% outcome); and **resonant whole-minute shifts** collapsed a planted, *perfect* coupling to ×1.006. THE RULE: read `lift` only when `underpowered` and `saturated` are both false. The synthetic self-tests were green through all four | Brief *(REFERENCE — living)* |
| [`CPAP-REAL-CORPUS-FOLLOWUPS-II-2026-07-13-BRIEF.md`](briefs/CPAP-REAL-CORPUS-FOLLOWUPS-II-2026-07-13-BRIEF.md) | **What the CPAP briefs left open — and one false DONE.** Opens by correcting itself: `CPAP-REAL-CORPUS-FOLLOWUPS` was stamped **DONE while its §5/§6 had never been executed** — *the same defect those briefs spent their length closing* (a claim made, nothing verifying it; the `docs-ledger` gate checks a status header's **format**, not its **truth**, exactly as GATE B checked a fixture's bytes but not whether anything reproduced them). The unexecuted work is carried out here so the parent's DONE becomes true. Open: **P7** — `event-coupling.js` is shipped, gated and **dormant** (no node consumes it, so the Integrator's "is it real?" question is still being answered by raw co-occurrence somewhere); **P8** — **`CPAPCross` has never been shown to detect a change at all** (it only ever runs on synthetic nights with `sd:0` and a `'stable'` label, while A4's two *dated* device-setting step-changes sit unused as labelled ground truth); a **demo-input gate** (nothing stops another node repeating the CPAPDex demo that had never worked on any fresh clone); and the **generated-list merge tax** (`changes-list.txt`/`docs-ledger-list.txt` are committed snapshots of the filesystem that every PR regenerates, so they conflict *by construction* — four rebases on one PR; they exist only because the browser lane can't list a directory, so make those gates Node-only and delete them) | Brief *(DONE 2026-07-17)* |
| [`CPAP-REAL-CORPUS-FOLLOWUPS-III-2026-07-17-BRIEF.md`](briefs/CPAP-REAL-CORPUS-FOLLOWUPS-III-2026-07-17-BRIEF.md) | **What closing §P8 surfaced, plus the §5 residue.** -II shipped `CPAPCross.pressureChangePoints()` — a cross-night device-SETTING change-point detector (robust L1-cost binary segmentation), validated on the real 180-night corpus: it flags the #151 epap95 step (10.7→6.8) with zero false positives and **honestly returns empty on the noise-dominated pressureEnvIqr**, correcting the brief's assumed #169 to *not a robust change-point*. The detector + additive `crossNight.pressureChangePoints` export field are live/gated. Carried here: **§1** a user-facing render banner for the change-points (deferred — opens the evidence-badge COVERAGE MANDATE + cohesion surface); **§2** apnea→**motion-arousal** coupling (the second `event-coupling` pairing -II §P7 left unwired); **§3** generalize the per-node goldens regen tools into `regen-goldens.mjs --node` (CPAP just gained the `outputHash` re-record they already had); **§4** `how-to-collect/resmed-edf.md` + the `pressureRange goodDirection` vocabulary question. **EXECUTED 2026-07-17:** §1 render banner shipped (badged, export-inert), §3 unified into `regen-goldens.mjs --node` over a shared core, §4 resolved (adapter id named; pressureRange/mode left with reasons); §2 RE-DEFERRED — no motion-arousal event stream exists to couple against (needs an ACC-derived arousal source first) | Brief *(DONE 2026-07-17)* |
| [`ARCHITECTURE-DEBT-REDUCTION-2026-07-14-BRIEF.md`](briefs/ARCHITECTURE-DEBT-REDUCTION-2026-07-14-BRIEF.md) | **Five maintainability wins after the checkJs type gate** — a phase-per-PR plan written for a local/cheaper model. **P1** convert the brittle source-text safety gates (regex-slicing a function body out of `tests/dex-tests.js`) to behavioral tests that call the function and assert the effect (strictly stronger; unblocks P4). **P2** turn on `strictNullChecks` one gated module at a time (fix the null/`{}`/`never` class at source, not with casts). **P3** split the single `BUILD-MANIFEST.json`/`FIXTURE-PROVENANCE.json` into per-app ledger fragments so parallel bundle PRs stop colliding (the §👥.3 "one session at a time" bottleneck). **P4** (after P1) reflow the whole tree and retire `biome.json`'s formatter-override list. **P5** ESM for the node realms — DEFERRED / research-only (the type gate already made the DSP↔render coupling visible, capturing much of the benefit). Ships no runtime change; includes an environment + gate-command cheat-sheet and the hard-won lessons from PRs #70–#79 | Brief *(DONE 2026-07-15 — P1·P2·P3·P4 merged; P5 deferred)* |
| [`ARCHITECTURE-DEBT-REDUCTION-FOLLOWUPS-2026-07-15-BRIEF.md`](briefs/ARCHITECTURE-DEBT-REDUCTION-FOLLOWUPS-2026-07-15-BRIEF.md) | **Follow-ups surfaced executing P4 (whole-tree Biome reflow).** **F1** (required before next release) — the reflow rewrote DSP text inside every app's compute closure, so `computeHash` moved and `verifiedUnder` on the 14 corpus-backed fixtures is stale; re-stamp with `DEX_UPLOADS=<full-corpus> node tools/verify-fixtures.mjs` (fail-closed in a partial-corpus env, hence owner action; `release.mjs` blocks until done). Export bytes are provably unchanged (0 output/input hashes moved), so no merge gate reds. **F2** process note — a reflow-safe gate audit must test source-mirror gates against reflowed bytes for files CI never format-checked (P4-prep's blind spot; ~14 gate groups hardened in the P4 PR). **F3** watch-out — Biome 2.5.3 formatter is non-idempotent on some JSDoc-cast constructs (needs a 2nd pass to the fixpoint) | Brief *(DONE 2026-07-17 — F1 discharged: all 14 fixtures verified under the current compute closure, `verify-fixtures --check` = 0 unverified; F2/F3 no code owed)* |
| [`ESM-MIGRATION-2026-07-15-BRIEF.md`](briefs/ESM-MIGRATION-2026-07-15-BRIEF.md) | **Make the DSP↔render coupling a real module boundary — spike first, decide, then fan out.** The executable spin-out of `ARCHITECTURE-DEBT-REDUCTION` **P5** (which stays deferred there). Each node is 4–5 plain global scripts sharing page scope; real `import`/`export` would delete the `<node>-globals.d.ts` scaffolding and the source-mirror gates that P4's reflow kept breaking. **Phase 0 is a throwaway inliner SPIKE on GlucoDex** (smallest node, no worker), behind a `--esm` flag writing a scratch `GlucoDex.esm.html`: prove the owned build can bundle ES-module source into ONE offline `Foo.html` that passes `no-network` + GATE A/B and boots under `file://` (the one cost local horsepower can't buy down — it's architectural). Everything after Phase 0 is CONDITIONAL on a GO. Written for a **local Ubuntu coder with the full corpus + headless Chromium** (runs the browser lane the cloud env can't). PpgDex's worker blob converts last. **Phase 0 (spike) → GO; Phase 1 (GlucoDex UI modules → ESM via `build-core.js esmBundle`) + Phase 2 (GlucoDex DSP → dual-mode ESM + the `classicify` co-load bridge for the orchestrators + both test runners) executed 2026-07-15**, export-inert by verification. **Phase 3 fan-out COMPLETE (all 7 nodes ESM) + Phase 4 executed in the owner-scoped middle path, 2026-07-16** (see `ESM-MIGRATION-FOLLOWUPS-2026-07-16`, DONE); stays IN-PROGRESS only on Done-when items 4–5 (d.ts + source-mirror-gate retirement), measured+parked in `ESM-MIGRATION-FOLLOWUPS-II`. | Brief *(IN-PROGRESS 2026-07-16)* |
| [`ESM-MIGRATION-FOLLOWUPS-2026-07-16-BRIEF.md`](briefs/ESM-MIGRATION-FOLLOWUPS-2026-07-16-BRIEF.md) | **The ESM-migration fleet fan-out — originally parked, then funded: ALL 7 NODES DONE; only Phase 4 remains.** Spawned after Phases 0–2 landed (GlucoDex fully ESM + the reusable `esmBundle`/`classicify` bridge). Phase 3 is NOT the mechanical sweep the parent anticipated: each remaining node has a structural blocker (indirect DSP consumption / no clean import edge in oxydex·hrvdex·pulsedex; no-IIFE `render`/`overview` in pulsedex; a Web-Worker blob in **ecgdex AND ppgdex**; edf/fusion/cross + non-orchestrator co-load in cpapdex; a load-order hazard making any DSP-only conversion a dead-export churn or a risky UI change). And the P5 payoff (delete `-globals.d.ts`, retire source-mirror gates) only lands once the fan-out is COMPLETE and the classic co-load path is retired — a partial fleet unlocks nothing. Captures the per-node blockers + the full-GlucoDex-treatment completion recipe. **Execution 2026-07-16: nodes 2–4 landed** (CPAPDex — the CJS `--selftest` path dropped; ECGDex — hermetic worker blob; PpgDex — toString-serialized pool worker), one node per fully-gated commit, plus fleet infrastructure: the worker co-load bridge in all five workers (repairing a latent `cohort-worker.js` GlucoDex regression) and the `browser-gates.mjs` timeout fix. **Execution II (same day): the deep 3 landed** — PulseDex set the template (window-publish blocks + defineProperty proxies for shared mutable state + side-effect imports), HRVDex applied it, OxyDex's 'hardest' DSP turned out IIFE-wrapped all along (two real bugs caught: the `var UP` non-configurable window property; a sixth un-bridged worker). **Phase 4 executed 2026-07-16 (owner-scoped middle path)**: app pages namespaced + explicit `_bare` helper-imports + mutable-state proxies — the import boundary is real and self-verifying (a missed helper throws); the spray stays as a documented test/worker-realm surface. The full-ambition remainder is scoped+parked in FOLLOWUPS-II. Brief DONE. | Brief *(DONE 2026-07-16)* |
| [`ESM-MIGRATION-FOLLOWUPS-II-2026-07-16-BRIEF.md`](briefs/ESM-MIGRATION-FOLLOWUPS-II-2026-07-16-BRIEF.md) | **What the Phase-4 middle path deliberately left open — measured and priced.** The rest of the original P5 ambition, scoped from real counts: (1) rewrite `tests/dex-tests.js`'s hundreds of deliberate bare-helper calls (`rmssd`×116, `parseTimestamp`×26 …) to namespace form — only then can the spray blocks be deleted; (2) qualify the six workers' ~20 bare calls; (3) invert the DSP→UI reach-ins (`setStatus`/`rerender`/`getProfile`… — dependency injection, a per-DSP behavioral refactor); (4) delete the seven `<node>-globals.d.ts` (unlocked by 3); (5) behavioralize the P4-hardened source-mirror gates; (6) then flip ARCHITECTURE-DEBT-REDUCTION §P5 + the parent brief DONE. Also records: Integrator + the orchestrator app layers are NOT ESM (fresh decision, not a leftover); the `<Node>` namespace attaches are permanent public API, not debt. Recommendation: stay parked — the middle path already bought the verified app-page boundary; this buys type-hygiene, not correctness. | Brief *(PROPOSED 2026-07-16)* |
| [`ESM-MIGRATION-SPIKE-FINDINGS-2026-07-15.md`](briefs/ESM-MIGRATION-SPIKE-FINDINGS-2026-07-15.md) | **Phase 0 inliner-spike findings for the ESM migration — verdict GO.** The throwaway go/no-go spike for `ESM-MIGRATION-2026-07-15-BRIEF.md`, run locally on GlucoDex with headless Chromium + the committed synthetic corpus, fully non-destructive. Proves the owned inliner CAN bundle ES-module source into one offline plain-inline `Foo.html`: a dependency-free module-registry bundler → one classic `<script>`, then `build-core.js` inlines it — **16 assets, zero network edges, real non-null manifestHash `023172618bf1`**. Boots under `file://` **and** `http://` with **0 errors/0 CSP violations**, full graph wired in-browser, `compute()` gives a real summary; export is **byte-identical ESM ≡ classic ≡ committed golden** on both inputs. Per-node edit is tiny (~23 changed lines; IIFEs stay intact, only ~7 inter-node symbols become `import`/`export`). Confirms inline modules boot offline but external `import` fails under `file://` (the CORS wall — why inlining is mandatory). Next likely go/no-go is Phase 2 (the co-load contract), not an inliner wall. **STOP for owner GO before Phase 1** | Findings *(REFERENCE 2026-07-15)* |
| [`LOCAL-DOWNLOAD-FILE-URL-FIX-2026-07-14-BRIEF.md`](briefs/LOCAL-DOWNLOAD-FILE-URL-FIX-2026-07-14-BRIEF.md) | **The science tools now run from a local download (file://), like the apps.** A user downloaded a "trio sigma" tool to disk and it did nothing — it only worked when served. Confirmed with headless Chromium under `file://`: the analysis tools (unlike the bundled apps) loaded logic from external `<script src>` siblings and spun `new Worker('x.js')`, which the browser **forbids under `file://`** (opaque origin → `SecurityError` on the worker; `Failed to fetch` on the data path) — so a downloaded shell had no compute. New **`tools/build-analysis.mjs`** bundles all 9 tools to self-contained single-file HTML: inline every script, and rewrite `new Worker('w.js')` → a **blob-URL worker** whose importScripts deps are inlined behind a hoisted DOM/window shim (mirrors `cohort-worker.js`). CSP unchanged (once inline, `'unsafe-inline'` + `blob:` cover it — same as the apps). Verified under file:// **and** http://: 0 page errors, every worker completes `init`→`ready`. Gated two ways: `build-analysis.mjs --check` in CI (staleness — the tools inline DSP code) + a Node-suite invariant group (no external `<script src>`, no `new Worker('file')`) | Brief *(DONE 2026-07-14)* |
| [`DEEP-AUDIT-2026-07-11-BRIEF.md`](briefs/DEEP-AUDIT-2026-07-11-BRIEF.md) | **`AUDIT-PROMPT.md` executed end-to-end — 21 reproducible defects, 11 of which mis-state a number the user sees.** Both gates were green before *and* after: these live in the space the gates don't cover. Headline: the **Clock Contract §3 file-level DMY/MDY lock is not implemented** (`clock.js:40` decides per-row) → an MDY-configured O2Ring night runs time *backward* → `durationMin = −254460`, **ODI-4 = 0/h, hypoxic burden = 0 — an apnea night reads perfectly healthy**; a Polar H10 **accelerometer** file is analyzed as a heart recording (HR 61.9 bpm, stress 100, 36 events — a gravity rail read as RR); HRVDex **`HF n.u.` = 125 000 000 %** (a 0–100 quantity) and **`MxDMn/MeanRR` 1000× low** (the un-guarded sibling of `guardBaevsky`); GlucoDex counts **gap-fill interpolation as measured glucose** (TIR 11 % vs a true 0 %) and **misses the real Lingo clip floor** (37 `nocturnal_hypo` @conf 0.97 off a 54 mg/dL rail); OxyDex's **REM estimate is impossible on 39/39 real nights** (median 77.5 %, coloured "good"); the Integrator says **"Sources agree … reliable"** when nothing was comparable, publishes a **glucose⟷autonomic coupling with no glucose in it**, **can never fuse HRVDex**, and its **kernel-drift audit is blind to 3 of 7 nodes**. **Why the gates missed it:** every `env.equiv` fixture is a *clean ~6-min clip*, so the long/lossy/differently-configured failures are structurally invisible. Also records the verified **clean bills** — the charter's own flagship ECG-transient suspicion is **refuted**, the Lomb–Scargle path is real (planted 800 ms² tone → 799), the ÷N-vs-÷N−1 SDNN bug stays fixed, and viewer-timezone independence holds | Brief *(**DONE — 2026-07-12 · all 21 findings executed.** §1 Clock DMY/MDY file-lock · §2 ACC-as-RR foreign-stream veto · §3/§4 HRVDex spectral presence-gates + Baevsky unit guard · §5/§6 GlucoDex gap-fill + the real Lingo clip floor · §7/§8/§9 OxyDex real event clock, whole-night windows, REM plausibility · §12–§16 Integrator read-set + kernel audit · §10/§11 one spectral time scale, no grid lottery · §17–§21 hygiene (export naming, null-not-zero, **profile origins — a population default no longer ships as your own data**, pooled ODI/T90, badge coverage incl. `.ev-corner`, which the mandate prescribed but the engine never defined). Every fix gated by a group **verified to red on the original code**. Final: headless 2125/2125 · GATE A 8/8 · GATE B green · `build --check` clean. Follow-up: `DEEP-AUDIT-FOLLOWUPS-2026-07-12-BRIEF.md`)* |
| [`FIXTURE-VERIFICATION-GATE-2026-07-14-BRIEF.md`](briefs/FIXTURE-VERIFICATION-GATE-2026-07-14-BRIEF.md) | **A fixture may not claim reproducibility nobody checked.** Specs the gate for the 2026-07-14 miss: `DEEP-AUDIT-2026-07-14 §1` moved GlucoDex's real export, the equiv leg that would have caught it **SKIPPED** (its input is a gitignored recording — absent in CI *and* on the executing session's machine), GATE B stayed green (it is static, it never re-runs the app), and **`build.mjs` re-stamped the fixture's `manifestHash`** — auto-authoring a reproducibility claim no gate had tested. Every gate green, brief DONE, release shipped, and the **served GlucoDex ran the pre-fix DSP against real users** until it was caught by hand. The defect is not "a leg skipped" but that *the skip leaves a false claim behind, written by a tool*. Four parts: **§1 `computeHash`** — a `manifestHash`-style projection restricted to the export's **compute closure** (the module set a headless realm needs to run `compute()`; the regen tools already encode it), so **"EXPORT-INERT" stops being a claim in a commit message and becomes a computed value** — render edits move `manifestHash` but not `computeHash`; a DSP edit moves both. **§2 `verifiedUnder`** — the fixture records the code that *actually re-ran the app and reproduced these bytes*; `build.mjs` is **forbidden** to write it (only the regen tools + a new `verify-fixtures.mjs` may), so the ledger can no longer assert what nobody checked. **§3** three placements: the suite *names* every UNVERIFIED fixture (a skip stops being silent) · **`release.mjs` refuses to cut a release with one** (the wall sits where harm materializes, and where the owner *has* the corpus — this alone would have blocked v1.10.1) · CI red, sequenced LAST (a corpus-less contributor cannot green it). **§4** finishes `DEEP-AUDIT-FOLLOWUPS-2026-07-12 §A`, which established committed-synthetic adversarial twins and shipped four for OxyDex but **left GlucoDex with none** — a 14 h-gap synthetic Lingo would have reddened this in CI, on the PR, with no corpus. Migration **verifies once on the corpus, then stamps — never back-fills** (that would fabricate the very claim being abolished); **that pass has been RUN (2026-07-14, v1.10.2): every corpus-backed leg reproduces — GlucoDex was the only stale one**, so the gate lands green rather than reding on arrival | Brief *(**DONE — 2026-07-15** — §1 computeHash · §2 verifiedUnder · §3.1 visibility · §3.2 the release WALL · §4 twins · §5 migration all EXECUTED; re-confirmed 10/10 green post-P3. The closure shipped as a **denylist, not the allowlist the brief sketched** — an allowlist that forgets a module fails OPEN and the gate goes blind; a denylist merely over-flags. Proven on the real bundle: a render-only edit moves manifestHash but NOT computeHash (gate stays green), a DSP edit moves both (gate reds) — and it catches the §1 scenario with NO corpus. Migration re-verified + stamped all 14 corpus-backed fixtures, so the gate landed GREEN. **§3.3 (CI-red) deliberately deferred** — a corpus-less contributor cannot green it; the release wall covers the harm)* |
| [`DEEP-AUDIT-2026-07-14-BRIEF.md`](briefs/DEEP-AUDIT-2026-07-14-BRIEF.md) | **Second deep audit — after ~15 PRs / 193 commits — with the recent churn cleared and 8 older latent defects found.** Four adversarial auditors, each verified by code trace. **The recent PR churn (checkJs / strictNullChecks casts + biome reflows) is a CLEAN BILL** — runtime-inert, GATE-B confirms no fixture moved; the findings are *residue of `DEEP-AUDIT-2026-07-11`'s incomplete fixes* (applied at the headline, not propagated to every consumer), not regressions. Correctness-first: **§1** GlucoDex counts long sensor-gap **interpolation as measured glucose** in ~9 metric families (CONGA/MODD/GVP/AGP/per-day/nocturnal-hypo/…) because the §5 `GAP_LONG` exclusion was wired only into `analyzableIndex` — the docstring says "every distribution metric," the code excludes it from one; **§2** the Integrator **silently drops a multi-night CPAP export's entire per-night payload** (device-scored AHI vanishes from fusion, no warning — the `nights[]` unwrap is OxyDex-only); **§3** PulseDex breaks the Task-Force identity `vlf+lf+hf==totalPower` on overnight (four independent medians vs the defined sum ECGDex/PpgDex carry); **§4** GlucoDex `detectSessions` can never see a sensor change (scans `FLAG.GAP`, gaps are `GAP_LONG`); **§5** OxyDex ODI rates on two time bases (`n/3600` vs elapsed span — needs an author call on which is canonical); **§6** Integrator resurrects the retired per-session CPAP `mode` (latent); **§7** CPAPDex `periodicBreathingPct:0`-not-null on zero-duration; **§8** SD1 estimator drift (negligible). Cleared: event-coupling, EDF clock, CPAP pooling, core HRV estimators (no SDNN ÷N regression), Baevsky guards. **ALL 8 EXECUTED 2026-07-14** — §5 by owner-ratified basis decision (SAMPLE, after a 37-night corpus measurement showed the divergence is inert on real data), the rest agent-executed; each with a failing-assertion gate verified RED-first and export-inertness verified via `computeHash`/`verify-fixtures` | Brief *(**DONE 2026-07-14**)* |
| [`DEEP-AUDIT-FOLLOWUPS-2026-07-12-BRIEF.md`](briefs/DEEP-AUDIT-FOLLOWUPS-2026-07-12-BRIEF.md) | **The residue of the deep audit, and the blind spot that hid it.** Headline (§A): **every `env.equiv.*` fixture is a clean ~6-minute clip**, which is *why* three Tier-1 findings (§1 clock lock, §8 event clock, §9 whole-night windows) were structurally invisible while both gates stayed green — the gates proved *reproducibility*, and both sides were wrong together. The fix is not another assertion but a **fixture with the failure modes in it**: a long, lossy, real night (≥6 h, `- -` no-reading rows, spanning midnight) from the 39-night O2Ring corpus. Also carries the deliberately-scoped-short work: PulseDex's two deeper fail-open layers under §2 (any column with a median in 300–2000 is accepted as RR; SDNN 9.5 ms over 3 h went unquestioned) · `_envToSeed`'s `0`-for-absent seed · the `||0` siblings of the HRV Score · the OxyDex **tail**-slice family (§9 fixed only the head-slice) · `remProxyPct` vs `remFraction` denominating on different clocks · the REM estimator itself, never re-derived · CPAPDex `apnea`/`hypopnea` feeding no fusion rule (and absent from `EVENT-LEXICON.md`) · and the process lesson from §6: **a long-standing assertion that a fixture was a "clean, unclamped file" was itself wrong** — the real Lingo export always railed at 54 mg/dL | Brief *(**IN-PROGRESS 2026-07-12** — **§A EXECUTED**: four **committed synthetic** adversarial inputs (MDY order · dropped rows · a full-length night) now run in the equiv gate as INVARIANTS, each with a control proving it bites. ⚠️ **This brief's own prescription for §A was WRONG, and the correction IS the lesson**: it asked for a *real* night — but real recordings are gitignored personal data, which is precisely *why* the GATE-C equivalence legs skip in CI. A real adversarial night would have been gitignored too: green on the maintainer's machine, CI as blind as before. The fixture had to be **synthetic so that it could be committed**. Verified in a fresh clone (what CI actually gets): all 16 asserts pass, and re-introducing the §9 head-slice reds it there. **§B EXECUTED** — an interval must CONSERVE TIME (19/19 real recordings sum to their elapsed span within 1%; the accelerometer read as RR claims 24.6×), which is a conservation law rather than the variability floor the brief prescribed — that floor would have MISSED the Verity rail entirely (its SDNN sits inside the real RR range) and risked rejecting real pathology. Absence is now null, not 0, across HRVDex seeds/siblings and OxyDex decoupling — including a fabricated green "Stress 0 · ok" the brief had assumed reached nobody. §C–§E open)* |
| [`REPO-DISCOVERABILITY-2026-07-03-BRIEF.md`](briefs/REPO-DISCOVERABILITY-2026-07-03-BRIEF.md) | **"First-look" clarity** — make the suite legible on first landing for humans, crawlers & LLMs *without* touching the local-first/no-CDN architecture or any gate. Front-door link block near the top of `DOCS-INDEX`/`README` (§1); **rejects** the source proposal's guide-rename (§2 — FROZEN filenames; encoded links + redirect stubs instead); static `sitemap.xml`/`robots.txt` (§3, public-vs-LAN caveat); GitHub topic manifest (§4); house additions (§5) — JSON-LD/`about.json`, one canonical roster with a discoverability-cohesion gate, `llms.txt`/`llms-full.txt`, on-page **SEO** (`<title>`/OG/canonical/structured data) + honest **SEM**/keyword map, **off-site signals** (backlink/registry submissions, **Zenodo DOI**, RSS feed, glossary/FAQ, PWA manifest), and a **crawlable `<noscript>` fallback for the JS-heavy app pages**. §1–§5.8 additive/**no re-bundle**; only §5.9 (app `<head>`) is gated | Brief *(IN-PROGRESS 2026-07-04 — §1 front-door links EXECUTED: DOCS-INDEX "⚡ At a glance" block + README Reference column + 🔬 Scientific Foundation section, all `%20`-encoded, no gate touched; §2 affirmed no-rename. §3 sitemap/robots + §4 topics + §5 house-additions + gated §5.9 → REPO-DISCOVERABILITY-FOLLOWUPS-2026-07-04)* |
| [`REPO-DISCOVERABILITY-FOLLOWUPS-2026-07-04-BRIEF.md`](briefs/REPO-DISCOVERABILITY-FOLLOWUPS-2026-07-04-BRIEF.md) | Remaining discoverability work after §1's front-door links landed: **§3** static `sitemap.xml`/`robots.txt` (**Allow** — `tepna.net` is public) generated by a committed `tests/*.mjs`-sibling script from the deploy manifest (don't hand-maintain); **§4** GitHub topics (off-repo settings action); **§5.1–5.8** JSON-LD/`about.json`, one canonical roster + a `discoverability-cohesion` gate in both runners, `llms.txt`/`llms-full.txt`, per-page `<title>`/meta/OG/canonical, off-site signals (registry backlinks, **Zenodo DOI**, RSS feed, glossary/FAQ, PWA manifest); **§5.9 (GATED, LAST)** crawlable `<noscript>` fallback in each app's `*.src.html` `<head>` → re-bundle + `BUILD-MANIFEST.json`. §1–§5.8 additive/no-gate; §5.9 is the sole re-bundle | Brief *(IN-PROGRESS 2026-07-04 — §3 sitemap/robots + generator `tools/build-docs.mjs`, §5.1 JSON-LD/`about.json`, §5.4 `llms.txt`/`llms-full.txt`, §5.6-text OG/canonical, §5.8 `feed.xml` shipped into `docs/` (served root) + `suite.manifest.json` canonical roster; `docs/index.html` re-synced. Deferred: og:image/PWA icons + §5.2 cohesion gate + §5.9 gated + §4 topics/Zenodo/registry)* |
| [`DOCS-LEDGER-GATE-2026-07-03-BRIEF.md`](briefs/DOCS-LEDGER-GATE-2026-07-03-BRIEF.md) | **Machine-check the brief lifecycle** — a `docs-ledger` group in `tests/dex-tests.js` (both runners, headless floor) enforcing `CLAUDE.md` §📌: no root-level briefs (all in `briefs/`), valid status header on every brief, every brief linked from this index, all index links resolve, `Superseded-by:`/`Supersedes:` symmetry, dated-filename form for new briefs. Pure static text — **no re-bundle, no provenance churn**. Motivated by the 2026-07-03 bulk relocation (127 links repointed with nothing to turn red if one was missed) | Brief *(✅ DONE 2026-07-04 — `docs-ledger` group live in both runners (checks 1–6 + list-staleness), all green; Phase 0 surfaced 55 pre-cutoff headerless briefs → grandfathered (not fabricated a status), 10 unindexed briefs → rows added, 1 out-of-vocab `DEFERRED` status → fixed to PROPOSED, + a latent `**Superseded-by:**` parser bug fixed. Residue → DOCS-LEDGER-GATE-FOLLOWUPS-2026-07-04)* |
| [`DOCS-LEDGER-GATE-FOLLOWUPS-2026-07-04-BRIEF.md`](briefs/DOCS-LEDGER-GATE-FOLLOWUPS-2026-07-04-BRIEF.md) | Residue from the docs-ledger gate (all LOW): **F1** decide whether `DEFERRED` should be a first-class status in the CLAUDE.md §📌 vocabulary (used in sub-item notes; one brief carried it as top-level status); **F2** extend check 4 link-integrity beyond `briefs/` to the whole tree (docs/, wiring/, root) — needs a generated path inventory; **F3** v2 status-column sync between the dashboard prose and brief headers | Brief *(DONE 2026-07-04)* |
| [`CONTROLLED-RELEASES-2026-07-05-BRIEF.md`](briefs/CONTROLLED-RELEASES-2026-07-05-BRIEF.md) | **Controlled releases, versioning & the changelog** — adds the missing *release identity* layer over the existing code (`manifestHash`) + document (dated briefs) identity: ONE suite SemVer (canonical in `suite.manifest.json`, the "maintenance number"), a root `CHANGELOG.md` (Keep a Changelog) + machine-readable `RELEASE-MANIFEST.json` history, a collision-free **changeset** flow (`changes/*.md` drops → one automated `tools/release.mjs` stamp) so parallel coders never hand-pick a number, and a `release-ledger` gate (sibling of `docs-ledger`) enforcing version↔history↔changelog parity + unique/increasing versions + "unreleased code needs an unreleased entry." Plus a 62304/13485-**aligned, not conformant** doc set (`docs/COMPLIANCE/`: lifecycle plan, safety class, config-management plan, SOUP list [runtime-empty by design], release SOP, document-control crosswalk). Backfills a reconstructed 1.0.0 baseline from the DONE brief corpus. Phases 1–4 no re-bundle | Brief *(✅ DONE 2026-07-05 — 1.0.0 baseline + `CHANGELOG.md` + `RELEASE-MANIFEST.json` + `changes/` flow + `tools/release.mjs` + `release-ledger` gate (both runners, 10/10 green) + `docs/COMPLIANCE/` set; no re-bundle. Executed collision-safe alongside OWN-THE-BUILD Part C (check 7 informational during rollout) + docs-ledger F2 (inventory regenerated). Residue → CONTROLLED-RELEASES-FOLLOWUPS-2026-07-05)* |
| [`CONTROLLED-RELEASES-FOLLOWUPS-2026-07-05-BRIEF.md`](briefs/CONTROLLED-RELEASES-FOLLOWUPS-2026-07-05-BRIEF.md) | Residue from executing `CONTROLLED-RELEASES` (all deferred/coordination): **F1** version stamp INTO bundles + `producer.suiteVersion` (re-bundle, sequence with OWN-THE-BUILD Part C); **F2** version propagation to docs surfaces via `build-docs.mjs`; **F3** strengthen `release-ledger` check 6 once surfaces declare a version; **F4** `CITATION.cff` version sync; **F5** decision: no retroactive `v0.x` tags; **F6/F7** changelog compare-links + docs-ledger F2 inventory coordination | Brief *(DONE 2026-07-06 — F2/F3/F4/F6/F8 executed, F5/F7 closed; F1 re-deferred to the next behavioral re-bundle)* |
| [`OPEN-BRIEFS-POST-TRIO-2026-07-06-BRIEF.md`](briefs/OPEN-BRIEFS-POST-TRIO-2026-07-06-BRIEF.md) | **Open-briefs snapshot after the tri-device σ experiment** — the narrative "what's next" map; DOCS-INDEX stays authoritative. | Brief *(REFERENCE 2026-07-06)* |
| [`SIGMA-PAPER-REWRITE-2026-07-06-BRIEF.md`](briefs/SIGMA-PAPER-REWRITE-2026-07-06-BRIEF.md) | **Rewrite sigma-no-reference paper + index** on the raw-ECG 10-night folder corpus; reconcile the Verity↔O2Ring noisy-corner reordering. | Brief *(DONE 2026-07-07)* |
| [`TRIO-METHODS-REUSE-2026-07-06-BRIEF.md`](briefs/TRIO-METHODS-REUSE-2026-07-06-BRIEF.md) | **Apply the trio-experiment learnings** — worker-DSP shim, folder ingest, quality gate, capture-provenance facts — across the suite. | Brief *(DONE 2026-07-07)* |
| [`REGISTRY-INVERSION-2026-07-03-BRIEF.md`](briefs/REGISTRY-INVERSION-2026-07-03-BRIEF.md) | **Manifests become the SOLE metric-metadata source; registries become generated build artifacts** — turn the forward-first codegen (`dex-registry-gen.js`, proven on EEGDex) backward onto the 7 shipped nodes: Phase-0 semantic parity audit (registry is current truth; every hand-registry caveat ported into the manifest losslessly), then flip generation on per node + a `check-registries.mjs` drift guard (hand edit to a generated registry = red). Retires the registry↔manifest drift pair; NO DSP generated, zero semantic change is the bar. Phase 3 re-bundle prefers sequencing after `OWN-THE-BUILD` Part A | Brief *(DONE 2026-07-04 — flip SUPERSEDED by REGISTRY-PROJECTION-2026-07-04 (direction A); Phase 0 audit DONE (`codegen/PARITY-AUDIT.md`): manifests carry 0% of the grade layer + diverge in ids/membership + HRVDex has none → no node flip-ready. Owner chose direction (A); successor `REGISTRY-PROJECTION-2026-07-04` — registry stays the grade truth, mirrors gated/generated, no flip)* |
| [`REGISTRY-PROJECTION-2026-07-04-BRIEF.md`](briefs/REGISTRY-PROJECTION-2026-07-04-BRIEF.md) | **Registry stays the grade truth; its mirrors become gated projections** — owner-selected direction (A) from `REGISTRY-INVERSION` Phase 0. Keeps each `<node>-registry.js` as THE source of `label·unit·goodDirection·depth·evidence·cite`; adds a `registry-defs-parity` gate (both runners) so the crossnight `*_DEFS`/`METRICS[]` mirror can't silently drift (catches the PpgDex evidence/cite-dropping map); optional Phase 2 generates `*_DEFS` from the registry (re-bundle). No grade invention, no flip; the Phase-1 gate ships with no re-bundle | Brief *(DONE 2026-07-05 — Phase 1 gate + Phase 2 DEFS-fix/re-bundle/golden-regen; `KNOWN_DRIFT` empty, all shared-id fields hard-gated; every REGISTRY-PROJECTION gate green)* |
| [`CROSSNIGHT-ENVELOPE-SPEC.md`](docs/CROSSNIGHT-ENVELOPE-SPEC.md) | `ganglior.crossnight` v1.0 — the standardized cross-night export shape | Spec |
| [`EVENT-LEXICON.md`](docs/EVENT-LEXICON.md) | The canonical `ganglior_events[].impulse` vocabulary fleet-wide (`desat_event` · `autonomic_surge` · `periodic_breathing` + node-scoped impulses) + the canonical-name + back-compat-alias policy, so a new node/demo can't re-fork a name. Records the dead `cvhr_surge`, the `autonomic_arousal` drop, and the deferred node migrations | Spec |
| [`SYSTEM-COHESION-BRIEF.md`](briefs/SYSTEM-COHESION-BRIEF.md) | Metric registry · disclosure tiers · epistemic hierarchy · lexicon — the cohesion layer | Spec / Brief |
| [`DEX-EVENT-UNIFY-AND-CSV-BRIEF.md`](briefs/DEX-EVENT-UNIFY-AND-CSV-BRIEF.md) | OxyDex DSP: unify the duplicated desaturation-event detection + make the human summary CSV export-only (Tasks A + B + C1 + C2 + C3 ALL DONE 2026-06-23) | Brief |
| [`DEX-EVENT-UNIFY-FOLLOWUPS-2026-06-23-BRIEF.md`](briefs/DEX-EVENT-UNIFY-FOLLOWUPS-2026-06-23-BRIEF.md) | Post-Task-A: ported the desat primitive to the 3 OxyDex mean-baseline families + dip3 loop; audited other nodes (DONE 2026-06-23, both gates green) | Brief |
| [`DEX-EVENT-UNIFY-FOLLOWUPS-II-2026-06-23-BRIEF.md`](briefs/DEX-EVENT-UNIFY-FOLLOWUPS-II-2026-06-23-BRIEF.md) | Round-II polish: perf-memoize the shared event set (blArr threaded through all 11 desat consumers), drop vestigial nadir-count guards, close-mode consistency documented, test-fixture currency declined | Brief (**DONE 2026-06-30** — OxyDex re-bundled, both gates green) |
| [`PPGDEX-DAWN-SQI-FOLLOWUPS-2026-06-30-BRIEF.md`](briefs/PPGDEX-DAWN-SQI-FOLLOWUPS-2026-06-30-BRIEF.md) | Residue from the 2026-06-30 LOW-remainder sweep: **FINDING+FIX** — `ppgBuildNodeExport` dropped the per-event `sqi` axis (vs ECGDex/R7), now preserved + byte-coverage; deferred committed `*_events` fixtures (rationale: 7 MB PPG input / non-deterministic GlucoDex genSynthetic → self-contained groups instead); co-load generation DECIDED keep gated-static | Brief *(DONE 2026-06-30)* |
| [`PPGDEX-BEAT-DETECTION-PERF-AND-HRV-FIDELITY-2026-07-02-BRIEF.md`](briefs/PPGDEX-BEAT-DETECTION-PERF-AND-HRV-FIDELITY-2026-07-02-BRIEF.md) | Two defects, one file: **perf** — `detectBeats` replaced its O(N·lag) whole-record autocorrelation + global-period scan with an O(N) TERMA detector run as a **3-LED consensus** (≥2 of 3 optical channels agree ±50 ms; single-LED beats dropped, not median-filled), windowed channel-SNR ranking, + a byte-identical Web-Worker pool; **HRV fidelity** — coverage-gated whole-record HRV (`lowConfidence`), clean-SQI-subset rMSSD/SD1/pNN50, + a 3-LED agreement ribbon. On the equiv clip: 338 beats, RMSSD 36.6 ms (vs paired ECG 37.4), correction 77.8 %→1.5 %. `PpgDex.html` re-bundled (manifestHash `d5fa185f6f97`), equiv fixture regenerated; both gates green | Brief *(DONE 2026-07-02)* |
| [`PPGDEX-BEAT-DETECTION-PERF-AND-HRV-FIDELITY-FOLLOWUPS-2026-07-02-BRIEF.md`](briefs/PPGDEX-BEAT-DETECTION-PERF-AND-HRV-FIDELITY-FOLLOWUPS-2026-07-02-BRIEF.md) | Residue from the parent (validation-depth / hardening): **§1** no overnight PPG in the equiv gate — the 137 ms→37 ms claim is proven only on a 6.5-min clip; commit a ≥90-min fixture; **§2** wire the Integrator to read `hrv.time.lowConfidence` + `ledAgreementPct` (emitted, not yet consumed); **§3** TERMA runs on slope-energy not Elgendi's amplitude square (device-driven; document or revisit); **§4** consensus 1/3-drop rate / foot-vs-peak clustering; **§5** orchestrate `compute()` is serial-only | Brief *(DONE 2026-07-02 — §2 Integrator consumer wired; §1/§3/§4 test-only; §5 declined)* |
| [`OXYDEX-HR-ARTIFACT-RUNAWAY-FIX-2026-07-03-BRIEF.md`](briefs/OXYDEX-HR-ARTIFACT-RUNAWAY-FIX-2026-07-03-BRIEF.md) | User report (*"100 bpm absolutely wrong"*): OxyDex read a night's HR as a flat **100 all night**. Two coupled fixes in `oxydex-dsp.js` — **Fix 1** `cleanArtifactHR`'s recovery search is **bounded** (`HR_ARTIFACT_MAX_RUN_SEC`=60) so a non-recovering jump no longer clamps the whole night to a stale anchor; **Fix 2** new `trimSensorWarmup` drops the O2Ring frozen warm-up placeholder (SpO₂ 84 / HR 100 held ~25 s until perfusion lock) that seeded **both** the runaway clamp and a false critical `minSpo2`. Validated over all 44 raw nights (100→**48.6 bpm** on the reported night, matching ECGDex; 4 warm-ups trimmed; both fixtures untouched). `OxyDex.html` re-bundled `a16db72bc689→91196f73460c`; both gates green | Brief *(DONE 2026-07-03)* |
| [`OXYDEX-HR-ARTIFACT-RUNAWAY-FIX-FOLLOWUPS-2026-07-03-BRIEF.md`](briefs/OXYDEX-HR-ARTIFACT-RUNAWAY-FIX-FOLLOWUPS-2026-07-03-BRIEF.md) | Residue the 44-night scan surfaced, OUT of the parent's frozen-placeholder scope: **§1 (MED)** gradual-ramp warm-ups (e.g. `20260615` SpO₂ 81→87 over ~20 s, not frozen) aren't caught and feed a false critical nadir — decide monotonic-ramp handling (flag + exclude-from-nadir, don't delete); **§2 (MED)** mid-night extreme-low SpO₂ artifacts (`20260613` 61, `20260623` 75) survive into `minSpo2`/`SPO2_CRITICAL_DIP` — gate the nadir on the self-gate / a robust percentile; **§3 (LOW)** re-check the critical-dip trigger rate after §1/§2 | Brief *(DONE 2026-07-04 — §1+§2 gated-nadir + §3 recheck landed & validated over 44 nights; OxyDex re-bundled, GATE-A + both fixtures reconciled (export-inert), both gates green)* |
| [`NODE-RESIDUE-FOLLOWUPS-2026-06-30-BRIEF.md`](briefs/NODE-RESIDUE-FOLLOWUPS-2026-06-30-BRIEF.md) | Closeout residue from the coordinated PpgDex+ECGDex+GlucoDex follow-up wave (2026-06-30): the EXECUTED items (PPGDEX §2 + GLUCODEX §3 Integrator light/end-to-end ingest gates, §11 buildHash-doc trap) are DONE in their parent briefs; this tracks what carries forward — ECGDEX-II §5 overnight-ECG orchestrate perf cap (DEFERRED, conditional/LOW, not inert), GLUCODEX §5 captureAppExport render-diff (DEFERRED, browser-rig-only + by-construction-safe), the Integrator PpgDex event.sqi weighting (§3 WIRED 2026-06-30 — categorical sqi-floor mirroring clamp-floor, Integrator re-bundled, both gates green), + fleet standing debt (live-UI drop, Node-CI) | Brief (§3 DONE 2026-06-30 · §1/§2/§4 deferred) |
| [`NODE-RESIDUE-FOLLOWUPS-II-2026-06-30-BRIEF.md`](briefs/NODE-RESIDUE-FOLLOWUPS-II-2026-06-30-BRIEF.md) | Residue from wiring NODE-RESIDUE §3 (the PpgDex sqi-floor): **§1 (decide)** ECGDex has NO symmetric categorical sqi-floor — parity vs intentional chest-strap-vs-limb-worn asymmetry; **§2** is `sqiFloor`/`clampFloor` load-bearing or audit-only (the `meta.derived` precedent); **§3** floor inert on non-fused PpgDex impulses; **§4** floor 0.3 / ×0.5 / effConf-stack unvalidated (PB_CVHR_MIN class); §5 Node-CI standing debt | Brief **(DONE 2026-07-02** — §1 DECIDED *intentional asymmetry*: keep the categorical SQI floor PpgDex-only (limb-worn optical reaches the unusable `sqi` tail; chest-strap ECGDex rarely does, `effConf`'s proportional taper covers it); §2 DECIDED `sqiFloor`/`clampFloor` are *audit-only* (the `conf ×0.5` is load-bearing) — both annotated in `integrator-dsp.js` + EVENT-LEXICON §6.10; §3/§4/§5 no-action. Integrator re-bundled `fe4c2c623820→be7e7aa83355`, GATE A updated, Integrator GATE A/B clean. ⚠ Gate check surfaced a PRE-EXISTING, unrelated OxyDex ledger drift → NODE-RESIDUE-FOLLOWUPS-III**) |
| [`NODE-RESIDUE-FOLLOWUPS-III-2026-07-02-BRIEF.md`](briefs/NODE-RESIDUE-FOLLOWUPS-III-2026-07-02-BRIEF.md) | Residue the -II closeout gate check surfaced: **§1 (⚠ headline, VERIFY→RE-RECORD)** a PRE-EXISTING OxyDex provenance drift — `OxyDex.html` `a16db72bc689` ≠ committed `e2ec6294e1ad`; the bundle REPRODUCES from its current loose sources (ledger stale, not bundle), fixtures `code-drift` (output bytes stable) → identify the post-2026-07-01 OxyDex source change, confirm `env.equiv.oxydex`, re-record (or regen if it reds) — NOT introduced by -II; **§2** parent §1/§2 are terminal (no residue); **§3** Node-CI / full-suite-boot standing debt | Brief **(DONE 2026-07-03** — §1 RESOLVED out-of-band: the OxyDex drift was the in-flight SELF-INGEST-2026-06-27 re-bundle (2026-07-02, export-inert), reconciled by that writer across ledger + both fixtures → `a16db72bc689`; confirmed via `manifest-gate.js`, did NOT hand-edit (concurrent-writer rule). An active fleet-reconciliation pass was observed converging mid-session (GlucoDex `62d38df70558`, HRVDex `93dd371ef306`); FINAL GATE A 8/8 + GATE B clean. §2/§3 terminal/env) |
| [`COHESION-ROLLOUT-BRIEF.md`](briefs/COHESION-ROLLOUT-BRIEF.md) | Rolling evidence badges + depth tiers out to all nodes | Brief |
| [`KERNEL-BUILD-BRIEF.md`](briefs/KERNEL-BUILD-BRIEF.md) | Physiology kernel + cross-deployment hash (DONE 2026-06-23 — all 5 steps verified, both gates green) | Brief |
| [`WORKER-POOL-PATTERN.md`](docs/WORKER-POOL-PATTERN.md) | The shared Web-Worker pool pattern used by the analysis tools | Reference |
| [`SIGNAL-ADAPTER-AND-FRONTIER-2026-06-23-BRIEF.md`](briefs/SIGNAL-ADAPTER-AND-FRONTIER-2026-06-23-BRIEF.md) | Forward-first plan for the vendor-adapter / `SignalFrame` ingest layer + cross-vendor unifier (Coospo/Ultrahuman/…), plus frontier hardening (property/metamorphic + differential testing, manifest-as-source, machine-checked contracts, content-addressed provenance, units-as-quantities, fusion algebra). Gate-sequenced; spine (Phases 0–2) is zero-gate-cost | Brief *(DONE 2026-06-30)* |
| [`SIGNAL-ADAPTER-FOLLOWUPS-PROVENANCE-2026-06-30-BRIEF.md`](briefs/SIGNAL-ADAPTER-FOLLOWUPS-PROVENANCE-2026-06-30-BRIEF.md) | Residue from executing AND-FRONTIER Phase 7 (content-addressed provenance), all LOW/MED, nothing blocks: **§1 (MED)** GATE C (regenerate-and-diff) is the only catcher of an output-moving code change whose fixture wasn't regenerated — but the Dex-Test-Suite equiv gate already IS that surface for code-gated fixtures (tie off the doc mapping); **§2** `buildHash` still stamped into exports by `ganglior-provenance.js` (inert — LEAVE IT, fold removal into a future fleet re-bundle); **§3** Node GATE B best-effort (`uploads/` gitignored → skips in CI); **§4** `outputHash` pins raw bytes not stripped content (recorded decision); **§5** 4 dropped pre-R1 samples remain in `uploads/`; **§6** parallelize the ~10 s GATE-B settle | Brief *(DONE 2026-06-30)* |
| [`SIGNAL-ADAPTER-FOLLOWUPS-2026-06-24-BRIEF.md`](briefs/SIGNAL-ADAPTER-FOLLOWUPS-2026-06-24-BRIEF.md) | Residue from executing the spine: unifier reaches into PulseDex internals (needs a public `compute()`), partial event parity, unifier-bundling is non-trivial, OxyDex/HRVDex-summary adapters still blocked on pure-parser extraction (Baevsky unit guard + black-box tagging live there), Coospo capture note + `SignalSpec` placeholders, + §9 Phase-10/OverDex residue (inherits the PulseDex reach-in via the extracted signal-orchestrate.js, no sealed-bundle driver yet, stays unbundled) | Brief *(DONE 2026-07-04 — live drop-through discharged: all 3 raw types emit via the real UI + OverDex fuses O2Ring live; fixed a newest-first Welltory `tsMs` adapter bug; residual §§5·6·8 external/node-gated)* |
| [`SIGNAL-ADAPTER-FOLLOWUPS-II-2026-06-24-BRIEF.md`](briefs/SIGNAL-ADAPTER-FOLLOWUPS-II-2026-06-24-BRIEF.md) | Round-II residue. NEW from the PulseDex/OxyDex Phase-9 execution: no automated gate asserts `compute()` ≡ the app's own export (highest), isolation-iframe proliferation (one hidden iframe per migrated node — §3 decision), no Node-side `compute()` CI floor, multi-signal `signal-orchestrate` naming drift, per-node display detail hand-branched. PLUS pre-registered HRVDex-leg items: give teeth to the `validateFrame` irregular-`tsMs` relaxation (cgm inherits it), ship `how-to-collect/` notes for the non-RR adapters (welltory-summary + oxydex-spo2), exercise the `SignalSpec.hrv`/spo2 dsp resolvers, refresh the stale Phase-9 DSP-purity allow-list reasons. Items 1 & 3 best land BEFORE HRVDex; rest zero gate cost | Brief *(DONE 2026-06-25)* |
| [`SIGNAL-ADAPTER-FOLLOWUPS-III-2026-06-24-BRIEF.md`](briefs/SIGNAL-ADAPTER-FOLLOWUPS-III-2026-06-24-BRIEF.md) | Round-III residue the HRVDex Phase-9 landing newly exposed (sibling of -II, which holds the PulseDex/OxyDex residue): **§1 (🔴 correctness)** the Baevsky ms-vs-s guard protects only the adapter path — HRVDex.html's own `computeDerived` SI/CSI (`hrvdex-dsp.js:341/378`) is still unguarded, so the app mis-reports SI on ms-unit files (NODE fix → re-bundle + GATE A/B); **§2** the black-box `meta.derived`/`heuristic` tag has NO Integrator fusion consumer (`effConf`/`gradeFor` ignore it) — audit-only today; **§3** activates the four -II HRVDex hooks + reinforces -II §1 (compute≡export gate now overdue at the 3rd node) | Brief *(DONE 2026-06-25)* |
| [`SIGNAL-ADAPTER-FOLLOWUPS-IV-2026-06-25-BRIEF.md`](briefs/SIGNAL-ADAPTER-FOLLOWUPS-IV-2026-06-25-BRIEF.md) | Round-IV residue from the §3 namespaced-build / iframe-removal pass: **§1 (🔴)** the OxyDex *isolation/headless* SpO₂ path was LATENTLY BROKEN pre-§3 (old `oxyHost` iframe loaded neither `oxydex-util.js` nor `upVO2category`, so isolated `OxyDex.compute` threw on every real ≥1 h overnight file) — fixed (co-load util + guard), now confirm the live Unifier/OverDex drop-zone UI end-to-end; **§2** the `compute()` functional floor is thin (OxyDex case is <1 h, skips vo2est) and missed §1 by construction; **§3** -II §1 (automated compute()≡app-export gate) is 4× overdue — §3 hand-wrote the regen-diff AGAIN; fold it in; **§4** no gate asserts a migrated DSP's headless path is self-contained ({self·kernel·util}); **§5** co-load list hand-synced across 4 files; **§6** `oxydex-util`/`-profile` still bare-global; **§7** Node CI (`run-tests.mjs`) not executed this pass — run it | Brief *(DONE 2026-07-04 — §1 live drop-through discharged + adapter sort bug fixed; §5 resolved by `dex-coload.js`; §6 util-namespacing + §7 Node-CI carried)* |
| [`SIGNAL-ADAPTER-FOLLOWUPS-V-2026-06-25-BRIEF.md`](briefs/SIGNAL-ADAPTER-FOLLOWUPS-V-2026-06-25-BRIEF.md) | Round-V residue from the HRVDex native-correctness pass (executed -III §1 + AND-FRONTIER §8 #1/#2/#5 in one re-bundle): **§1** the zero-seed fabrication reaches MORE composites than the three §8 #2 named (`d_pti`/`d_coh_energy`/`d_incoherent_stress`/`d_sdi`…) — gate them too; **§2** black-box tier consistency — the demotion was scoped to the brief's five, siblings (`pti`/`abs`/`coherence`/`focusEff`) still read `experimental` (decision); **§3** PulseDex `*Est` quarantine (the other half of §8 #5) still owed, per-node; **§4** carries forward -IV §7 (Node CI) + §1 (live drop-zone UI) verification debt — NOT discharged this pass | Brief *(DONE 2026-07-04 — §4 live-UI drop half discharged; §3 PulseDex `*Est` deferred to its own pass; §4 Node-CI half = fleet standing debt)* |
| [`SIGNAL-ADAPTER-FOLLOWUPS-VI-2026-06-25-BRIEF.md`](briefs/SIGNAL-ADAPTER-FOLLOWUPS-VI-2026-06-25-BRIEF.md) | Round-VI residue from landing the equivalence gate (-II §1 / -IV §3) + the -II–V execution pass: **§1 (⚠ highest)** the `compute() ≡ committed export` gate is OxyDex-ONLY — extend to PulseDex + HRVDex (needs a committed input→fixture pair + `FIXTURE-PROVENANCE.json` entry per node; budget for byte-identity not holding cleanly); **§2** the gate compares to the committed fixture, not the live app export (decision); **§3** the `env.equiv` Node-CI path is unverified (folds into the standing Node-CI debt); **§4** the -III §2 `meta.derived` note in `integrator-dsp.js` is source-only — fold into the next Integrator re-bundle; **§5** the render-coverage rig is unrunnable in a cross-origin preview sandbox (env limit, not a gate gap — document) | Brief *(DONE 2026-06-25)* |
| [`SIGNAL-ADAPTER-FOLLOWUPS-VII-2026-06-25-BRIEF.md`](briefs/SIGNAL-ADAPTER-FOLLOWUPS-VII-2026-06-25-BRIEF.md) | Round-VII residue the -VI equivalence-gate extension exposed: **§1 (🔴)** HRVDex `compute({text})` skips the `commitRows` ascending sort the app path applies → a real `compute() ≠ app-export` divergence (newest-first Welltory ⇒ `recording.startEpochMs` is the LAST day, `spanDays = -27`); first concrete instance of -VI §2's "fixture ≠ live app export" gap hiding a divergence — fix in the shared builder, re-bundle HRVDex, regen the equiv fixture; **§2** the equivalence gate exercises ZERO events on both new nodes (event `t`/ordering/meta byte-identity untested) — add inputs that emit ≥1 of each impulse; **§3** carries the Node-CI verification debt (-IV §7 / -V §4 / -VI §3) | Brief *(DONE 2026-06-25)* |
| [`SIGNAL-ADAPTER-FOLLOWUPS-VIII-2026-06-25-BRIEF.md`](briefs/SIGNAL-ADAPTER-FOLLOWUPS-VIII-2026-06-25-BRIEF.md) | Round-VIII residue the -VII row-sort + event-coverage pass exposed: **§1 (⚠ highest)** the -VI §2 gap is now PROVEN real (VII §1 was a genuine `compute() ≠ app-export` divergence the fixture-proxy gate did NOT catch) — add option (b), a direct live `appExport ≡ compute()` diff per node (browser `extraProbe` + a capture hook on the download-triggering export); VII §1 closed it for HRVDex by construction but OxyDex/PulseDex parity is still proxy-only; **§2** the `env.equiv` Node-CI path unverified — now 5 cases + the VII §1 group to eyeball; **§3** verify-provenance's fixture audit only enumerates `uploads/` same-origin, its hardcoded fallback list is hand-maintained sync-debt (best fixed by deriving it from `FIXTURE-PROVENANCE.json`) | Brief *(DONE 2026-06-25)* |
| [`SIGNAL-ADAPTER-FOLLOWUPS-IX-2026-06-25-BRIEF.md`](briefs/SIGNAL-ADAPTER-FOLLOWUPS-IX-2026-06-25-BRIEF.md) | Round-IX residue the -VIII live-export-diff pass exposed: **§1 (⚠ highest)** HRVDex's app export is LOSSY vs the headless path — `exportGanglior()` carries display-ROUNDED rmssd/sdnn (18 vs 18.2) + the filtered/accumulated `getFilteredRows()` view, while `compute({text})` is full-precision/full-recording, so an APP-produced and a UNIFIER-produced HRVDex export of the SAME file are NOT byte-identical (unlike OxyDex/PulseDex) — decide the fusion contract: (a) export full precision (HRVDex re-bundle + fixture regen + both gates) or (b) document the filtered-scope contract; **§2** -VIII §1's HRVDex leg tests SEAM parity (`compute({rows})`), not the text→parse path the other two nodes exercise — make it end-to-end once §1 lands; **§3** the render-coverage suite false-reds under CONCURRENT runs (ECGDex iframe-boot + OxyDex watchdog timeouts vanish on isolated re-run) — CONTRIBUTING note; **§4** standing Node-CI `env.equiv` verification debt | Brief *(DONE 2026-06-25)* |
| [`SIGNAL-ADAPTER-FOLLOWUPS-X-2026-06-25-BRIEF.md`](briefs/SIGNAL-ADAPTER-FOLLOWUPS-X-2026-06-25-BRIEF.md) | Round-X residue the -IX HRVDex export-scope fix carried/exposed (all LOW priority): **§1** the HRVDex §1 render-coverage diff still tests SEAM parity (`compute({rows})`), not text→parse end-to-end — isolate a single recording (snapshot/restore the running log) to make it end-to-end; **§2** confirm the human exports (`exportCSV`/`exportJSONL`) keeping the filtered-view scope is intentional (+ a visible "current N-day window" hint so truncation isn't silent); **§3** render-coverage false-reds under CONCURRENT runs (ECGDex iframe-boot + OxyDex watchdog) — CONTRIBUTING note; **§4** standing Node-CI `env.equiv` verification debt | Brief *(DONE 2026-06-25)* |
| [`SIGNAL-ADAPTER-FOLLOWUPS-XI-2026-06-25-BRIEF.md`](briefs/SIGNAL-ADAPTER-FOLLOWUPS-XI-2026-06-25-BRIEF.md) | Round-XI residue the -X end-to-end-test / export-hint pass exposed (all LOW priority, test-only): **§1** the X §1 snapshot/restore only fires if `extraProbe` reaches its `finally` — an early `return` at the compute-surface guard would leak the prep snapshot (restore-leak); decouple it from `extraProbe` control flow (wrap the whole leg / register a teardown); **§2** fleet-wide render-coverage storage-hygiene audit — other legs that drive a `localStorage`-backed app (OxyDex night store, Integrator longitudinal store) clobber without restore too; apply the same pattern where a leg persists; **§3** standing Node-CI `env.equiv` verification debt | Brief *(DONE 2026-06-25)* |
| [`SIGNAL-ADAPTER-FOLLOWUPS-XII-2026-06-25-BRIEF.md`](briefs/SIGNAL-ADAPTER-FOLLOWUPS-XII-2026-06-25-BRIEF.md) | Round-XII residue the -XI render-coverage storage-hygiene pass exposed (all LOW priority, test-only): **§1** IndexedDB-backed render-coverage state (Integrator longitudinal `ganglior_integrator` DB) is NOT covered by the `localStorage` whole-store snapshot — latent gap, harmless today (the prep ingests no `crossNight` envelope so nothing persists) but would clobber the operator's longitudinal store the moment a prep ingests one or clicks `genSynthetic`; guard it (assert the stores stay empty) or add an async IDB snapshot/restore; **§2** the whole-store `clear()`+restore is shared-origin-wide (net-zero today, optionally switch to a keyed reconcile); **§3** standing Node-CI `env.equiv` verification debt | Brief *(DONE 2026-06-25)* |
| [`SIGNAL-ADAPTER-PHASE9-REMAINING-NODES-2026-06-25-BRIEF.md`](briefs/SIGNAL-ADAPTER-PHASE9-REMAINING-NODES-2026-06-25-BRIEF.md) | Forward-execution handoff for the REMAINING Phase-9 node migrations (GlucoDex · ECGDex · PpgDex · CPAPDex) — distils the proven PulseDex/OxyDex/HRVDex recipe (namespaced build + headless `compute()` + adapter + `signal-orchestrate` emit + 4-site co-load + the re-bundle gate ritual), per-node gotchas, suggested order (GlucoDex first), and the error classes from rounds -I…-XII that WILL recur. Phase 7 (provenance) stays out, owner-gated, do last | Brief *(DONE 2026-06-28)* |
| [`CPAPDEX-PHASE9-FOLLOWUPS-2026-06-28-BRIEF.md`](briefs/CPAPDEX-PHASE9-FOLLOWUPS-2026-06-28-BRIEF.md) | Residue the CPAPDex Phase-9 leg (node 4/4) exposed: **§1** three legacy CPAPDex export fixtures stamp a stale buildHash + aren't code-gated (pre-existing drift, sandbox-invisible — a deliberate regen+register pass); **§2 (deferred)** CPAPDex has no live-host routing (binary EDF can't traverse the `readAsText` ingest boundary — needs a binary read path + binary-aware adapter if Unifier/OverDex CPAP ingest is ever wanted); **§3** `cross.js` self-describing crossnight evidence (fleet-wide gap); **§4** standing Node-CI debt | Brief *(DONE 2026-06-28)* |
| [`CPAPDEX-PHASE9-FOLLOWUPS-II-2026-06-28-BRIEF.md`](briefs/CPAPDEX-PHASE9-FOLLOWUPS-II-2026-06-28-BRIEF.md) | Post-DONE re-audit residue (gate-coverage of the new CPAPDex `compute()` path; no defect, both gates were green): **§1 (⚠ MEDIUM)** CPAPDex is the ONLY migrated node with no content/reference equivalence gate — the `compute() ≡ cpapBuildExport` parity pins the two code PATHS but neither to a committed output, so a silent regression in the shared builder drifts both together undetected → add a golden-export gate (or a binary-input equivalence variant); **§2** the `readEDF → compute()` seam isn't gated end-to-end (floor uses the synthetic decoded set); **§3** `compute()` drops `opts.ingest/fname/offsetMin` (latent — folds into -I §2 when live routing lands); **§4** `_synthEdfSet` is now load-bearing for two gates + a stale self-gate comment to tidy | Brief *(DONE 2026-06-28)* |
| [`CPAPDEX-PHASE9-FOLLOWUPS-III-2026-06-28-BRIEF.md`](briefs/CPAPDEX-PHASE9-FOLLOWUPS-III-2026-06-28-BRIEF.md) | Residue from executing -I + -II: **§1** the multi-night crossnight-export wrapper (`exportNight`'s ≥3-night array + `ganglior.crossnight` header) lost its only fixture when `cpapdex-multi17` was retired (its 17-night source EDFs were never committed) — add a DETERMINISTIC synthetic multi-night golden (≥3 `_synthEdfSet` nights → per-night `buildNight` → the multi-night wrapper / `CPAPCross.crossNightBlock`) + code-gate it; **§2** `compute(input, opts)` still DROPS `opts` (ingest/fname/offsetMin) — thread `opts.ingest` into `cpapBuildExport(night, opts)` IF/WHEN live-host EDF routing lands (-I §2, owner-gated; needs a re-bundle); **§3 (carried)** `cross.js` self-describing crossnight evidence (fleet-wide) + Node-CI `env.equiv` standing debt | Brief *(DONE 2026-06-28)* |
| [`CPAPDEX-PHASE9-FOLLOWUPS-IV-2026-06-28-BRIEF.md`](briefs/CPAPDEX-PHASE9-FOLLOWUPS-IV-2026-06-28-BRIEF.md) | Residue from executing -III (all LOW / hardening): **§1** `CPAPCross` is now runtime-co-loaded in both runners (the -III discovery) but still has NO DEDICATED runtime unit test — its `crossNight`/`crossNightBlock` math is covered only transitively (P12 source-drift + the shared ECGCross engine + the -III multi-night golden), so a CPAPCross-specific group (mirroring ECGCross's "Cross-night baseline mean/sd") would localize a failure to the node; **§2** the -III multi-night gate uses an in-test RECONSTRUCTION of `exportNight`'s INLINED envelope + a source-pin — lifting a shared `cpapBuildMultiNightExport(chrono)` the app delegates to (gate calls the SAME code → drop the source-pin; also lets `compute()`/orchestrate emit multi-night) remains a re-bundle-gated cleanup; **§3 (carried)** `compute()` opts-drop (owner-gated, -II §3 / -III §2) · `cross.js` self-describing evidence (fleet-wide) · Node-CI `env.equiv` standing debt | Brief *(DONE 2026-06-29)* |
| [`CROSS-MODULE-RUNTIME-COVERAGE-2026-06-28-BRIEF.md`](briefs/CROSS-MODULE-RUNTIME-COVERAGE-2026-06-28-BRIEF.md) | **FLEET-WIDE** (surfaced by `CPAPDEX-PHASE9-FOLLOWUPS-III`'s discovery that `cpapdex-cross.js`/`CPAPCross` was loaded into NEITHER runner): **§1 (LOW–MED)** only `ECGCross` is runtime-exercised among the `*-cross.js`; the others' NODE-SPECIFIC surface (`<NODE>_DEFS`, `crossNightBlock`, helpers like `nightOdi`/`compliancePct`) is runtime-UNTESTED — covered only by P12 source byte-identity (shared rule only) + the ECGCross engine group → runtime-co-load every `*-cross.js` in both runners + a per-node `crossNightBlock` group over a VARYING series (subsumes `CPAPDEX-PHASE9-FOLLOWUPS-IV §1`); **§2 (MED, root cause)** `dex-coload.js` covers only adapters + the 6 routable DSPs, so NO gate guarantees an app-BUNDLED aux module (`*-cross/-coimport/-fusion/-render/-edf/-registry.js`) is even loaded in the suite (exactly how `cpapdex-cross.js` shipped untested) → add a bundled-module runtime-presence gate (or extend `dex-coload.js` with a `nodeModules:` leg); **§3 (LOW)** Node↔browser co-load asymmetry (e.g. `cpapdex-coimport.js` loaded in the browser suite, not in `run-tests.mjs`). All test-infra, NO app re-bundle | Brief *(DONE 2026-06-29)* |
| [`CROSS-MODULE-RUNTIME-COVERAGE-FOLLOWUPS-2026-06-29-BRIEF.md`](briefs/CROSS-MODULE-RUNTIME-COVERAGE-FOLLOWUPS-2026-06-29-BRIEF.md) | Residue from executing the parent (both LOW–MED / hardening): **§1 (MED)** the new `dex-coload.js` `nodeModules:` leg is HAND-MAINTAINED → it narrows but doesn't CLOSE the parent's §2 structural blind spot (a new bundled aux module nobody hand-lists still slips); do the real per-bundle "bundled ⇒ runtime-present-or-`RUNTIME_EXEMPT`" gate (parse each bundle's `__bundler/manifest` or `.src.html` `<script src>`) or at least a `nodeModules:` ⊇ repo-glob exhaustiveness check; **§2 (LOW)** PpgDex is the LONE cross node still on the legacy raw shape — its `crossNightBlock` never calls `CrossNightEnvelope.build` (the other four migrated) → migrate for uniformity (re-bundle-gated) or document the choice. Test-infra; §2-with-migration re-bundles PpgDex | Brief *(DONE 2026-06-29)* |
| [`CROSS-MODULE-RUNTIME-COVERAGE-FOLLOWUPS-II-2026-06-29-BRIEF.md`](briefs/CROSS-MODULE-RUNTIME-COVERAGE-FOLLOWUPS-II-2026-06-29-BRIEF.md) | Lone residue from executing the parent's §1 (the lighter middle step): the `Co-load §1` exhaustiveness gate enforces "bundled ⇒ on the `nodeModules:` leg" ONLY for the **cross/coimport** suffix class, so the parent's full structural invariant ("ANY module bundled into App X is runtime-present-or-exempt in the suite") is still unchecked for other aux shapes (`*-fusion.js`, a new suffix, a second non-suffixed helper). **§1 (MED)** build the real per-bundle gate — derive each bundle's inlined `*-*.js` set (from `.src.html` `<script src>` / `__bundler/manifest`) and assert each is runtime-co-loaded (a `file→global` resolver) OR on a documented reason-stamped `RUNTIME_EXEMPT` list; the `nodeModules:` leg + `Co-load §1` then validate against it. Test-infra ONLY, NO re-bundle | Brief *(DONE 2026-06-29)* |
| [`GLUCODEX-FOLLOWUPS-2026-06-27-BRIEF.md`](briefs/GLUCODEX-FOLLOWUPS-2026-06-27-BRIEF.md) | Residue the GlucoDex Phase-9 leg exposed: **§1 (⚠ highest)** the `compute()`-shape gap (orchestrate hands `compute()` the canonical SignalFrame, not the node's parser shape — it threw on every routed CGM file; the `genSynthetic` floor hid it by construction) WILL recur on PpgDex/ECGDex/CPAPDex — add a generic adapter→emit→export gate or a per-node canonical-frame round-trip; **§2 (✅ DONE 2026-06-27)** GlucoDex node-export clamp-saturation honesty flag — `glucoBuildNodeExport` now emits `recording.clamp` + stamps clip-floor `nocturnal_hypo` `meta.clampFloor:true`, Integrator `adaptGlucoDex` surfaces `summary.clampSat` + down-weights those hypos (conf ×0.5); GlucoDex+Integrator re-bundled `d8bf3b24036c→5bd14ef5c05b` / `c991bd461fe5→026e1d6ef0e1`, equiv fixture regenerated, gates green 1176/75. **FINDING:** the committed Lingo CSV does NOT clamp-saturate (`detected:false`, min 54/max 169, vendor:null — natural hypo tail, not a clip pile-up; the brief's "37 hypos off the 55-floor" premise was an over-assumption), so the clamp-detected path is gated by a synthetic `r`, and the fixture's only delta is the added `recording.clamp:{detected:false}`; **§3** Integrator `adaptGlucoDex` ingest now exercised by the §2 test (clamp-export round-trips through the adapter); **§7** centralize the now-5-wide co-load/adapter hand-sync before the CPAPDex leg; LOW: live-UI drop, render-coverage probe + live-export≡compute() diff, `dawn_surge` event-byte coverage, standing Node-CI debt | Brief *(DONE 2026-06-30)* |
| [`PPGDEX-FOLLOWUPS-2026-06-27-BRIEF.md`](briefs/PPGDEX-FOLLOWUPS-2026-06-27-BRIEF.md) | Residue the PpgDex Phase-9 leg exposed (the §1 compute()-shape trap did NOT bite — `compute()` was built to accept the canonical ppg frame from the start): **§1 (⚠ highest, PpgDex-specific)** the orchestrate PPG export is COMPANION-LESS + light — the single-text adapter boundary drops the ACC/GYRO/MAGN motion gate + limb posture + device-PPI lane, and `compute()` emits only `recording`+`ganglior_events`, so a Unifier-routed PPG file loses Integrator posture + HRV-consensus (keeps event-based apnea fusion); decision owed (richer export / companion-bundle ingest / document event-only); **§2** Integrator `adaptPpgDex` ingest of the light export unverified; **§5** centralize the now-6-wide co-load hand-sync before ECGDex/CPAPDex; **§8** ECGDex forward note (single-channel `{samples:Float32Array,fs}` — do NOT copy PpgDex's packed-`samples` shape; no Worker); LOW: `sqi`+impulse event-byte coverage, un-capped O(N²) `sampEn`, live-UI drop, Node-CI debt | Brief *(DONE 2026-06-30)* |
| [`ECGDEX-FOLLOWUPS-2026-06-27-BRIEF.md`](briefs/ECGDEX-FOLLOWUPS-2026-06-27-BRIEF.md) | Residue the ECGDex Phase-9 leg exposed (the §1 compute()-shape trap did NOT bite — `compute()` was built to accept the canonical single-channel ecg frame from the start): **§1 (⚠ highest, ECGDex-specific)** the orchestrate ECG export is COMPANION-LESS + light — the single-text `*_ECG.txt` adapter boundary drops the device `*_RR/_HR/_ACC` cross-checks (self-RR↔device-RR validation, HR agreement, ACC posture/gait), and `emitEcgNodeExport` emits only `recording`+`ganglior_events` (no `hrv.time`, `morphology`, `sleepStages`, posture), so a Unifier-routed ECG file loses the rich `buildV2` payload the Integrator's `adaptEnvelopeNode` ECGDex branch reads (HRV consensus + posture series); decision owed (mirror of PPGDEX-FOLLOWUPS §1); **§2** Integrator `adaptEnvelopeNode('ECGDex')` ingest of the light export unverified; **§3** the light `exportGanglior` now strips `_sec` (harmonized with `buildV2`) — confirm no consumer relied on it; **§4** equiv-fixture event-byte coverage is 0-event (CVHR `autonomic_surge`/`stage_*` byte-shape — incl. the `sqi` axis + `meta.position` — untested by the gate); LOW: full-overnight `*_ECG.txt` perf on the orchestrate path, live-UI drop, Node-CI debt | Brief *(DONE 2026-06-27)* |
| [`ECG-RPEAK-SEED-FIX-2026-06-27-BRIEF.md`](briefs/ECG-RPEAK-SEED-FIX-2026-06-27-BRIEF.md) | User-reported: a real overnight Polar H10 `*_ECG.txt` that OPENS mid electrode-settling threw "Too few R-peaks detected". `detectPeaks` seeded the Pan-Tompkins threshold from `max(first 2 s)` — the ~5 kµV settling transient ≫ the ~600 µV overnight QRS (~18× in the squared integrate) — and `SPKI` only decays when a peak FIRES, so once seeded high it never recovered → <12 peaks → false "flat" throw on an otherwise-good night. Fixed: robust global-percentile seed (`_seedScale`), behaviour-preserving on clean records + transient-immune; regression gate (group 12c). Re-bundled `7c625af51078→bfa1aa934fcc` (buildHash unchanged); both gates green | Brief *(DONE 2026-06-27)* |
| [`ECGDEX-FOLLOWUPS-II-2026-06-27-BRIEF.md`](briefs/ECGDEX-FOLLOWUPS-II-2026-06-27-BRIEF.md) | Residue from the -I execution + the seed-fix pass: **§1 (⚠)** Pan-Tompkins SEARCH-BACK for the GENERAL mid-file threshold-stuck case (the seed fix covered only startup; `SPKI` still only decays on a detected peak); **§2 (⚠)** IMPLEMENT the -I §1 decision — richer gated export (a) + companion-bundle ingest (b) — coordinated with the still-undecided PPGDEX §1; §3 upgrade the §4 structural event gate to a committed byte-fixture (optional); §4 the co-load manifest GENERATION step (before/with CPAPDex); LOW: overnight orchestrate perf cap, live-UI drop + Node-CI debt | Brief *(DONE 2026-06-30)* |
| [`ECG-PPG-FOLLOWUPS-HANDOFF-2026-06-27-BRIEF.md`](briefs/ECG-PPG-FOLLOWUPS-HANDOFF-2026-06-27-BRIEF.md) | **Next-coder entry point** to execute `PPGDEX-FOLLOWUPS-2026-06-27` + `ECGDEX-FOLLOWUPS-II-2026-06-27` as ONE coordinated pass. Exists because the highest item in each (`PPGDEX §1` ≡ `ECGDEX-II §2` — the companion-less + light orchestrate export) is the SAME decision: the ECGDex -I pass DECIDED option (a) and deferred the code so both nodes land with ONE shared rich-export shape (gated so the apps' light exports stay byte-identical). Sequences the coupled (a)/(b) export work + each brief's node-independent remainder (ECGDex Pan-Tompkins search-back, co-load generation, PpgDex Integrator-verify/sampEn) + the per-node re-bundle gate ritual | Brief *(DONE 2026-06-30)* |
| [`HOST-EMIT-ALLOWLIST-2026-06-27-BRIEF.md`](briefs/HOST-EMIT-ALLOWLIST-2026-06-27-BRIEF.md) | The "discovered residue" from the HANDOFF §2(b) execution: both hosts (`data-unifier-app.js` `canEmit` / `overdex-app.js` auto-emit) gated their emit UI to a hardcoded `rr/spo2/hrv` literal written before the cgm→ppg→ecg migrations, so a dropped CGM/PPG/ECG file routed+validated+framed but never actually emitted a Ganglior export in the live UI (`emitNodeExport` already dispatched all six). Fix: ONE shared `SignalOrchestrate.canEmit(signalType)` over `{rr,spo2,hrv,cgm,ppg,ecg}`, both hosts gate on it (no second literal to drift; EEGDex adds `eeg` in one place). Loose `<script src>` → no re-bundle. New Dex-Test-Suite group 'Host emit allowlist'; gates green 1176/75 | Brief *(DONE 2026-06-27)* |
| [`ECG-INGEST-FOLLOWUPS-2026-06-28-BRIEF.md`](briefs/ECG-INGEST-FOLLOWUPS-2026-06-28-BRIEF.md) | User-reported ingest bugs, **executed & both gates green** (recorded in `BUILD-MANIFEST.json` `_note_ecg_ingest_magn_skip`, ECGDex `c8eb64808061→b8a195a082e1`): (1) a dropped Verity-Sense session (no `*_ECG.txt`) had its **magnetometer** analyzed as raw ECG; (2) a mixed **Sense+H10+O2Ring+duplicate-`_ECG`** drop "processed it all". Fixed in `ecgdex-app.js` (skip foreign `_MAGN/_GYRO/_PPG` + O2Ring/Wellue SpO₂ & Libre/CGM, first-line **header content-sniff**, **device-id companion pairing**, **same-night de-dupe**, clear "ignored …(breakdown)" messaging) — ingest-only, `compute()`/export inert. This brief = the discovered residue: **§1 (🔴)** `signal-orchestrate.pairCompanions` is **device-agnostic** (nearest-stamp only) → Unifier/OverDex can cross-pair a Sense sidecar onto an H10 ECG; lift the device-id filter there (loose JS, no re-bundle); **§2 (⚠)** SAME bug class in `ppgdex-app.js loadFiles` — mirror device-filter + foreign-signal reject + de-dupe (re-bundle PpgDex); **§3 (⚠)** the app ingest classifier is **untested** + per-node vendor regexes **duplicated** → promote to a testable surface / shared vendor registry (the AND-FRONTIER ask); **§4** cross-DROP device awareness; **§5** `_RR` vs `_PPI` last-wins; **§6** a manifest JSON was committed **invalid** (GlucoDex `FIXTURE-PROVENANCE` note, repaired this pass) → make `verify-provenance` hard-fail on a parse error; **§7** known-by-design notes (Polar-only scope, foreign-ACC-dropped, pre-parse de-dupe, name-OR-header SpO₂ detect) | Brief *(DONE 2026-06-28)* |
| [`ECG-INGEST-FOLLOWUPS-II-2026-06-28-BRIEF.md`](briefs/ECG-INGEST-FOLLOWUPS-II-2026-06-28-BRIEF.md) | Residue from executing FOLLOWUPS: **§1 (⚠)** fold `signal-orchestrate` onto the shared `dex-ingest.js` registry (it kept its OWN `deviceKey`/`foreignSignal` — the deferred AND-FRONTIER (b)); **§2** Polar-only de-dupe/device-filter scope (known-by-design, documented); **§3** add `dex-ingest.js` to `tsconfig`; **§4** extract a pure `planIngest()` so the `loadFiles` orchestration (not just the classifiers) is gate-backed | Brief *(DONE 2026-06-28)* |
| [`ECG-INGEST-FOLLOWUPS-III-2026-06-28-BRIEF.md`](briefs/ECG-INGEST-FOLLOWUPS-III-2026-06-28-BRIEF.md) | Lone residue from executing FOLLOWUPS-II §4: the new shared `DexIngest.planIngest` gates the **ECGDex** drop path only. **§1 (⚠)** PpgDex's `loadFiles` is **content-first** (reads all text up front, `mergeMultipart` on parsed objects) and its companion pick uses the **parsed `rec.t0Ms`** (not the filename stamp) for nearest-stamp → the `ecgGroups`-shaped `planIngest(fileNames)` can't own PPG's per-primary association; extend a PPG sibling (or factor the shared name-based dedupe + device-eligibility primitives) so PpgDex's mixed-Sense+H10 split + duplicate-`_PPG` set-aside are gate-backed too (re-bundle PpgDex, ingest-inert); **§2** `signal-orchestrate` keeps its own generic `streamKind`/`fnameStampMs` (no `DexIngest` equivalent — known-by-design) | Brief *(DONE 2026-06-28)* |
| [`ECG-INGEST-FOLLOWUPS-IV-2026-06-28-BRIEF.md`](briefs/ECG-INGEST-FOLLOWUPS-IV-2026-06-28-BRIEF.md) | Residue a post-execution re-audit of FOLLOWUPS-III surfaced (nothing shipped-broken; -III both gates green): **§1 (⚠)** the per-primary nearest-`t0Ms` companion PICK — deliberately kept app-side in `ppgdex-app.js loadFiles` (it needs the parsed `rec.t0Ms`) — is now the SOLE un-gated link in PPG companion association + PPG-UNIQUE (ECG loads all deduped companions per lane, no pick to mirror): two same-device sessions dropped together are separated ONLY by that untested pick → extract `DexIngest.pickNearestByStamp` (headless, both runners) or drive a crafted multi-file drop through render-coverage; **§2** option (a) left the name-based dedupe + device-eligibility primitives DUPLICATED across `planIngest`/`planIngestPpg` (two-copy drift trap) → factor shared `_dedupeBySession`/`_eligibleByDevice` (LOW, structural; `dex-ingest.js` bundles into ECGDex+PpgDex → batch the 2-app re-bundle with §1 or defer); **§3** standing live mixed-drop UI + Node-CI debt (documented); **§4** `eligibleByPrimary` name-keyed (known-by-design, safe-degrades) | Brief *(DONE 2026-06-28)* |
| [`ECG-INGEST-FOLLOWUPS-GATE-RATIFY-2026-06-28-BRIEF.md`](briefs/ECG-INGEST-FOLLOWUPS-GATE-RATIFY-2026-06-28-BRIEF.md) | **Verify-only backstop** — FOLLOWUPS was stamped DONE on HEADLESS evidence (shared-assertion suite green, both bundles boot, buildHash unchanged `146ac9c8b1bd`/`fff8fe8b1b68`, manifests consistent), but the cross-origin preview sandbox could NOT run the *canonical* gates' iframe-reach-in legs. Three checks to ratify on a **same-origin host**: (1) `node tests/run-tests.mjs` exit 0; (2) `Dex-Test-Suite.html` all-green incl. the **equivalence gate** (`env.equiv.ecgdex`/`ppgdex` byte-identical — the actual proof the `*_equiv` fixtures are export-inert, which was re-recorded on a *reasoned* basis, not re-run this session); (3) `verify-provenance.html` GATE A 8/8 (ECGDex `65e5eaaa152c`, PpgDex `71e712b0b87c`) + GATE B reproducible + the new §6 parse-banner. No code change expected if green | Brief *(DONE 2026-06-28)*
| [`GATE-LIVE-RUNNABILITY-2026-06-28-BRIEF.md`](briefs/GATE-LIVE-RUNNABILITY-2026-06-28-BRIEF.md) | **No code defect — verification *process* residue** from the RATIFY pass: the preview served **same-origin** here, so the iframe-reach-in gates the RATIFY brief + the `Dex-Test-Suite` ENV NOTE call BLOCKED actually ran **green** (`bodyHasBlocked` false; all 9 render-coverage groups + all 8 provenance bundle-boots passed). **§1 (⚠ process)** soften the ENV NOTE / `verify-provenance` preamble — the block is **host-specific**, not universal; default the workflow to *try-live-first* (the HEADLESS-DONE→RATIFY-backstop cycle is only needed when reach-in is *actually* blocked); partial-discharge the Node-CI `env.equiv` debt for the now-live-confirmed nodes. **§2** `ppgdex_20260610.json` reads `no provenance (pre-R1)`, not the predicted `buildHash-only/legacy` (wording fix). **§3** the §6 corruption teeth-test mutates the REAL sidecar → add an **in-memory** red-branch unit so nobody risks shipping a corrupted gate. **§4** suite reports green incrementally (70→77→79 over ~50 s) → only ✓all-green AFTER the count stabilises is a pass (reinforces `GENERIC-EMIT-GATE-FOLLOWUPS §3`). **§5** literal `node tests/run-tests.mjs` still not run (no shell) → fold into the standing Node-CI debt, don't re-open. Doc/harness only, no re-bundle | Brief *(DONE 2026-06-28)* |
| [`GATE-LIVE-RUNNABILITY-FOLLOWUPS-2026-06-28-BRIEF.md`](briefs/GATE-LIVE-RUNNABILITY-FOLLOWUPS-2026-06-28-BRIEF.md) | Residue from a post-DONE "did anything surface?" re-audit of the parent (**no code defect, no re-bundle**): **§1 (⚠ highest, self-inflicted + reproduced live)** the softened prose put the literal `Blocked a frame`/`opaque` tokens into `verify-provenance.html`'s preamble + `CONTRIBUTING.md`, so the *documented* same-origin check `bodyHasBlocked = /Blocked a frame\|cross-origin\|SecurityError\|opaque/.test(document.body.innerText)` now **FALSE-POSITIVES** (this session's own verify-provenance read returned `blocked:true` on a green 8/8 load) — **committed automated gates are immune** (`browser-gates.mjs` + page verdicts key off `.pill.fail`/`.pill.bad` cells, never innerText); fix = re-scope the documented detector to result-rows / the iframe-reach-in error signature; **§2** `CONTRIBUTING.md` now says BOTH "wait ~3 s" and "~50 s to settle" (reconcile to one canonical phrasing); **§3** the deferred ans-design.css pass's "flips every buildHash-legacy fixture" prediction (`AUDIT-FOLLOWUPS-II/III`) is wrong for the pre-R1 PulseDex/ppgdex fixtures (no provenance block → they don't flip; only `integrator_fusion_*` do) — corrected expectation carried forward here, no historical rewrite; **§4** the §3 red-branch unit MIRRORS verify-provenance's banner (two copies → drift risk) → optional shared-selector / real-DOM teeth. Doc/test-harness only | Brief *(DONE 2026-06-28)* |
| [`GENERIC-EMIT-GATE-FOLLOWUPS-2026-06-28-BRIEF.md`](briefs/GENERIC-EMIT-GATE-FOLLOWUPS-2026-06-28-BRIEF.md) | Residue from hardening **GLUCODEX-FOLLOWUPS §1** (the generic adapter→emit→export gate): its base was already landed in the ECGDex leg as `PPGDEX-FOLLOWUPS §10` (the §1 status was stale), hardened 2026-06-28 with a **DRIVER 2** that binds coverage to the EMIT ALLOWLIST (`canEmit` ∪ `SignalSpec.types()` ∪ parsed `_EMITTABLE`), not just the adapter registry, so CPAPDex can't go emittable via any path without a gated schema-valid provider (red-fires proof: `flow`→`[flow]`, bespoke `cpap`→`[cpap]`; today `[]`). **§1 (⚠ forward→CPAPDex)** writing that provider IS the EDF-event-vs-`SignalFrame`-shape decision (a `SignalFrame` has NO event carrier; CPAPDex's value is device-scored EVE/CSL events) + 3 gate hand-sync points (`providers`/`NODE_OF`/the orchestrate emit case) + co-load the synth source in both runners; **§2** DRIVER-2 soft-skip can pass a provider WITHOUT verifying its export when its generator isn't co-loaded; **§3** Dex-Test-Suite pass/group COUNTS are timing-dependent (render-coverage legs boot real bundles async — observed 68→72 groups / 1084→1146 passes in one session, all green) → only ✓all-green/0-fails is a baseline, recorded counts are snapshots; **§4** one logical gate was tracked under 3 section IDs (the stale-status cause) — one canonical section, others link it. Test-/harness-layer, no re-bundle | Brief *(DONE 2026-06-28)* |
| [`GENERIC-EMIT-GATE-FOLLOWUPS-II-2026-06-28-BRIEF.md`](briefs/GENERIC-EMIT-GATE-FOLLOWUPS-II-2026-06-28-BRIEF.md) | Residue from executing -I (all test-/doc-layer, no re-bundle, nothing blocks a node DONE): **§1 (⚠ process, low-risk)** the -I §2 soft-skip→RED change made the generic gate **realm-sensitive**, so RATIFY it under **Node CI** (`node tests/run-tests.mjs`) — confirmed by inspection that run-tests.mjs co-loads all 6 emittable providers' synth sources (`ECGDSP`/`GLUDSP`/`SYNTH.{renderPPG,pickWindow,buildTimelines}`/`PpgDex.parsePPG`/`SignalOrchestrate`) so it SHOULD stay green, but Node was not run this pass (standing -IV §7…-XII debt; same run discharges `env.equiv` byte-identity); **§2** the deferred -I §3(b) — make the Dex-Test-Suite summary deterministic so counts are CI-assertable (optional); **§3** DRIVER-2's `_EMITTABLE` source-regex `\{([^}]*)\}` truncates at the first nested `}` → could silently shrink the allowlist universe; harden (assert parsed-count ≡ live `_EMITTABLE`, or read the live object) **before CPAPDex edits `_EMITTABLE`**; **§4 (link-only)** the CPAPDex frame-shape decision stays owned by -I §1 — §2 now forces provider + synth-source co-load; note the 3 distinct reds (DRIVER-2 "NO provider" · §2 "synth frame-source NOT co-loaded" · "namespace co-loaded"). No re-bundle | Brief *(DONE 2026-06-28)*
| [`GENERIC-EMIT-GATE-FOLLOWUPS-III-2026-06-28-BRIEF.md`](briefs/GENERIC-EMIT-GATE-FOLLOWUPS-III-2026-06-28-BRIEF.md) | Residue from executing -II §3 (feedback on my own fix; all test-/doc-layer, no re-bundle, nothing blocks a node DONE): **§1 (⚠ highest, latent)** the -II §3 `_EMITTABLE` fallback parser is **brace-only — not string/comment-aware** — and **teeth A** only checks that *a* depth-0 close brace was found, so a `}` inside a comment/string in the literal silently truncates the parse with teeth A still green (exactly -II §3's named "trailing-comment brace" risk, closed for the live-accessor path but RE-OPENED on the fallback). Masked today only because the live `emittableTypes()` accessor is present (the universe unions from it; **teeth B** reds on a live-vs-parse mismatch) → the accessor is now **silently load-bearing** for full truncation-safety, with **nothing asserting its presence**; remove it (or load an older build) and the gate degrades to the weak fallback, where a comment-brace before a bespoke `_EMITTABLE`-only type (**the CPAPDex `cpap`**) silently drops it — the false-green -II §3 exists to kill. Fix before/with the CPAPDex `_EMITTABLE` edit: **(a)** make the fallback scanner string/comment-aware (durable), **(b)** RED on a source-declares-but-runtime-lacks-`emittableTypes` mismatch + WARN when falling back. **§2** the inert `emittableTypes()` is source-ahead-of-bundle (rides the next Data Unifier/OverDex re-bundle — expected, the `BADGE_CSS` rule; provenance untouched by construction; no action). **§3 (link-only)** a transient ECGDex render-coverage iframe-boot red recurred → reinforces the deferred -II §2 deterministic summary (don't re-track). **§4** literal Node-CI run stays the standing debt at `SIGNAL-ADAPTER-FOLLOWUPS-XII §3`. Test-/harness-/doc-layer, no re-bundle | Brief *(DONE 2026-06-28)* |
| [`SYNTH-TEXTURE-2026-06-24-BRIEF.md`](briefs/SYNTH-TEXTURE-2026-06-24-BRIEF.md) | RR within-night texture realism (1/f bank + RSA wander) → `rsaGainFor` re-fit + SubjectA arc re-tune; coupled synth-gen/cohort-gen v-bump (**DONE 2026-06-24** — arc lands night-2=18.0; 6 apps re-bundled, both gates green; oxy/CGM byte-identical) | Brief |
| [`SYNTH-TEXTURE-FOLLOWUPS-2026-06-24-BRIEF.md`](briefs/SYNTH-TEXTURE-FOLLOWUPS-2026-06-24-BRIEF.md) | The spun-out DSP audit from the texture brief: Lomb–Scargle resp blind &lt;0.15 Hz (**FIXED** — global-peak tracking + render caveat; corpus CSR ~0.017 Hz now surfaces), SampEn O(N²) defensive cap, `cleanArtifactHR` arousal-erase pipeline-order (**verified safe**) — detector-side; PulseDex re-bundled, both gates green (**DONE 2026-06-24**) | Brief |
| [`SYNTH-TEXTURE-FOLLOWUPS-II-2026-06-24-BRIEF.md`](briefs/SYNTH-TEXTURE-FOLLOWUPS-II-2026-06-24-BRIEF.md) | Residue from the DSP audit: `oxydex-dsp.js computeHREntropy` **verified already-bounded** (head-slice cap, not the feared unbounded O(N²)); ECG/PPG `lombScargle` global-peak port **audited & declined as intentional** (PPG has no peak/respRate path — defect absent; ECG is HF-peak-only by design with median-EDR respRate + dedicated `detectCVHR` apnea-band channel). Verify-not-fix — no re-bundle; 5 static guards added, suite green (**DONE 2026-06-24**) | Brief |
| [`SYNTH-TEXTURE-FOLLOWUPS-III-2026-06-24-BRIEF.md`](briefs/SYNTH-TEXTURE-FOLLOWUPS-III-2026-06-24-BRIEF.md) | Residue from FOLLOWUPS-II: OxyDex entropy caps were a **head-slice** (first ~16 min), not whole-night decimation. **DONE** — corpus showed head-slice materially under-states full-night irregularity (HR 0.42 vs 1.5); switched `computeHREntropy`/`computeSpO2Entropy` to whole-night decimation stride; OxyDex fixture regen + re-bundle (manifestHash a4df7d2281aa→1f2486a7358a); GATE A + Dex-Test-Suite green | Brief *(DONE 2026-06-24)* |
| [`SYNTH-TEXTURE-FOLLOWUPS-IV-2026-06-24-BRIEF.md`](briefs/SYNTH-TEXTURE-FOLLOWUPS-IV-2026-06-24-BRIEF.md) | Residue from FOLLOWUPS-III fixture regen: (1) `vo2est`/`karv` user-profile-coupled → **decided (b) strip-list** (GATE-C strip list + `_profileCoupledStripList` in `FIXTURE-PROVENANCE.json`); (2) 2nd OxyDex fixture (`_2026-06-17_2042`) stale (head-slice entropy + dropped `ansAge` block) & unreproducible → **RETIRED** to `docs-archive/retired-fixtures/`; (3) 3-byte CSV drift = benign UTF-8 **BOM** (sample-inert), stale integrator byte-ref left as historical snapshot. Doc/sidecar/move only — no re-bundle; GATE A PASS 8/8, zero reds | Brief *(DONE 2026-06-24)* |
| [`SYNTH-TEXTURE-PAPERS-RERUN-2026-06-24-BRIEF.md`](briefs/SYNTH-TEXTURE-PAPERS-RERUN-2026-06-24-BRIEF.md) | Re-run + rewrite the RR-derived papers after the texture v-bump; oxy/CGM legs byte-identical (DONE 2026-07-11 — all 6 papers rerun, released v1.1.0) | Brief |
| [`EXPORT-HYGIENE-2026-06-27-BRIEF.md`](briefs/EXPORT-HYGIENE-2026-06-27-BRIEF.md) | Unify export filenames behind one shared `exportName()` helper + fix the Clock-Contract bug in the copy-pasted `_exportTs()` (it stamps LOCAL export-click time, not the recording anchor `t0Ms` via `getUTC*` → wrong day, viewer-TZ-dependent). Controlled `kind` vocab (ganglior/summary/series/report), span-aware HRVDex names. Filename change is fixture-INERT (name not hashed) → per-node re-bundle + GATE A bump only. Sibling of EXPORT-IDENTITY | Brief *(DONE 2026-06-29)* |
| [`EXPORT-IDENTITY-2026-06-27-BRIEF.md`](briefs/EXPORT-IDENTITY-2026-06-27-BRIEF.md) | Export identity & PHI minimization: adopt a content-addressed `recording.contentId` handle (NOT a serial/patient/device-serial ID — that would be a privacy regression), scrub the PHI-bearing `provenance.files` filenames at the ingest boundary, optional opt-in pseudonymous subject key. CORE-layer Phases 0–1 zero-gate; node adoption (Phase 2) is fixture-MOVING. Sibling of EXPORT-HYGIENE | Brief *(DONE 2026-06-29)* |
| [`EXPORT-IDENTITY-FOLLOWUPS-2026-06-29-BRIEF.md`](briefs/EXPORT-IDENTITY-FOLLOWUPS-2026-06-29-BRIEF.md) | Residue from EXPORT-IDENTITY Phases 0–1: §1 Phase 2 node adoption of `recording.contentId` (fixture-MOVING; flips parent to DONE); §2 🔴 discovered `GangliorProvenance.stamp().inputs[].name` carries the raw filename into every app export's `schema.provenance` (same PHI leak, different pipe — fleet-wide `ganglior-provenance.js` re-bundle, fixture-MOVING); §3 Phase 3 opt-in subject key; §4 open product decisions (longitudinal linking? HIPAA/GDPR sign-off) | Brief *(DONE 2026-06-29)* |
| [`EXPORT-HYGIENE-FOLLOWUPS-2026-06-29-BRIEF.md`](briefs/EXPORT-HYGIENE-FOLLOWUPS-2026-06-29-BRIEF.md) | Residue from EXPORT-HYGIENE Phase 0 + PulseDex: §1 migrate the remaining 7 exporters to `exportName()` (fixture-INERT per-node re-bundle + GATE-A bump; audit un-grepped ppgdex/cpapdex/integrator; HRVDex is the `spanDays` case); §2 in-payload `generated:new Date().toISOString()` still non-deterministic (fixture-MOVING, separate); §3 wire optional `_<contentId>` suffix after EXPORT-IDENTITY Phase 2; §4 Welltory-compat exports intentionally not migrated | Brief *(DONE 2026-06-29)* |
| [`EXPORT-IDENTITY-FOLLOWUPS-II-2026-06-29-BRIEF.md`](briefs/EXPORT-IDENTITY-FOLLOWUPS-II-2026-06-29-BRIEF.md) | Residue from executing -I §1/§2: §1 the remaining 7 nodes surface `recording.contentId` opportunistically (fixture-MOVING per node; ⚠ stamp it on BOTH the compute() path AND the live app result, folding the same payload+t0Ms, or the render-coverage app≡compute parity gate reds — the PulseDex lesson); §2 recorded decision (SCRUB not drop `inputs[].name`, matching `provenance.files`); §3 opt-in subject key (product-gated, unbuilt); §4 longitudinal-linking? + HIPAA/GDPR sign-off — human product calls | Brief *(DONE 2026-06-29)* |
| [`EXPORT-IDENTITY-FOLLOWUPS-III-2026-06-29-BRIEF.md`](briefs/EXPORT-IDENTITY-FOLLOWUPS-III-2026-06-29-BRIEF.md) | Residue from the all-7 `recording.contentId` adoption: **§1 (⚠ MED)** gate-back the new Integrator `contentId` dedup (verified headlessly, not yet a committed Dex-Test-Suite assertion — the P1 group only exercises the no-contentId fallback); **§2** the pre-existing CPAPDex/Integrator manifestHash drift this pass reconciled — add a continuous headless GATE-A check so "re-bundled but forgot BUILD-MANIFEST" reds without opening verify-provenance; **§3** the 2 real-EDF CPAP fixtures' contentId was reconstructed (cross-checked equal to the builder) not EDF-re-run — gold path if an EDF host is available; **§4** `cpapdex-2026-06-16.json` byte change → its historical `integrator_fusion` fingerprint is stale-by-design (not a live gate); **§5** carried (in-payload `generated`, subject key, compliance sign-off, Node-CI) | Brief *(DONE 2026-06-29)* |
| [`EXPORT-IDENTITY-FOLLOWUPS-IV-2026-06-29-BRIEF.md`](briefs/EXPORT-IDENTITY-FOLLOWUPS-IV-2026-06-29-BRIEF.md) | Residue from gate-backing the Integrator dedup: **§1 (⚠ MED)** continuous `manifestHash`-drift detection (the reframed -III §2) — a headless/Node-CI check that every bundle's `manifestHashOf` == `BUILD-MANIFEST`, so a "re-bundled but forgot the manifest" drift (the kind -II found PRE-EXISTING on CPAPDex/Integrator, silently red until a human opened verify-provenance) reds automatically; keep it in the PROVENANCE lane, NOT the behavior suite (gate separation); **§2** the 2 real-EDF CPAP fixtures' reconstructed contentId confirmed by a real `readEDF→…→cpapBuildExport` re-run if an EDF host is available (verify-don't-trust); **§3** carried standing/product-gated (in-payload `generated`, opt-in subject key, HIPAA/GDPR sign-off, Node-CI `env.equiv`) | Brief *(DONE 2026-06-29)* |
| [`EXPORT-IDENTITY-FOLLOWUPS-V-2026-06-29-BRIEF.md`](briefs/EXPORT-IDENTITY-FOLLOWUPS-V-2026-06-29-BRIEF.md) | Residue from landing the continuous `manifestHash`-drift check (-IV §1): **§1** a Node-host run of BOTH headless runners (`run-tests.mjs` + the new `verify-manifest.mjs`) — host-gated (no Node host here; the standing `env.equiv` debt); one fix already applied in -IV (mid-file shebang → line 1); **§2** a `manifestHash` projection-stability observation (every bundle's committed hash had moved across-time with no source change) — owned by `PROVENANCE-NONDETERMINISM-FOLLOWUPS`, not re-opened; **§3** carried standing/product-gated (real-EDF CPAP re-run, in-payload `generated`, opt-in subject key, HIPAA/GDPR sign-off, Node-CI `env.equiv`) | Brief *(DONE 2026-06-30)* |
| [`PHI-SURFACE-STATEMENT.md`](docs/PHI-SURFACE-STATEMENT.md) | Per-export-channel PHI / data-handling surface statement — the evidence pack a compliance reviewer signs off ON (what each `ganglior.node-export` emits vs scrubs: identity-free `contentId`, floating wall-clock, scrubbed `provenance.files`/`inputs[].name`; no name/serial/DOB/geolocation). Records the engineered posture (100% local, research/personal, disclaimer-stamped); explicitly does NOT certify HIPAA/GDPR — that external sign-off stays open (`EXPORT-IDENTITY-FOLLOWUPS-II` §4(b)) | Reference |
| [`EXPORT-HYGIENE-FOLLOWUPS-II-2026-06-29-BRIEF.md`](briefs/EXPORT-HYGIENE-FOLLOWUPS-II-2026-06-29-BRIEF.md) | Residue from finishing the 7-node `exportName()` migration: §1 the `recording.contentId` filename suffix is now UNBLOCKED (PulseDex ships it) — wire optional `_<contentId>` into `exportName` + adopt where a node surfaces contentId (fixture-INERT); §2 in-payload `generated:new Date().toISOString()` still non-deterministic (fixture-MOVING, separate); §3 carry-forward note — a contentId adopter must stamp BOTH compute() + live app paths (parity) | Brief *(DONE 2026-06-29)* |
| [`OXYDEX-NODE-EXPORT-ENVELOPE-2026-06-27-BRIEF.md`](briefs/OXYDEX-NODE-EXPORT-ENVELOPE-2026-06-27-BRIEF.md) | Make EVERY OxyDex export (1..N nights) ONE schema-validated `ganglior.node-export` v2.0 envelope carrying a real `ganglior_events[]` (`desat_event` + `periodic_breathing`), closing the `<3`-night bare-array drift gap — the Integrator validates ONE shape, not two. Wrap unconditionally (crossNight block still ≥3-only); per-night `nights[]` byte-identical; tiers from `OXY_REGISTRY` (PB=experimental, never `measured`); `conf` continuous ≠ tier; Clock-Contract `t`+`tMs` | Brief *(DONE 2026-06-29)* |
| [`OXYDEX-NODE-EXPORT-ENVELOPE-FOLLOWUPS-2026-06-29-BRIEF.md`](briefs/OXYDEX-NODE-EXPORT-ENVELOPE-FOLLOWUPS-2026-06-29-BRIEF.md) | Residue from executing the envelope brief: **§1 (⚠ highest)** impulse-VOCABULARY split — OxyDex now emits `desat_event`/`periodic_breathing` while fusion + the synthetic generator still speak `spo2_desaturation`/`autonomic_surge`/`autonomic_arousal` (fusion bridges desat by gathering BOTH, but the vocab is forked); **§2** the v2.0 path DROPS OxyDex's synthesized `autonomic_arousal` (HR-spike arousals) — emit vs defer-to-cardiac; **§3** OxyDex `periodic_breathing` has NO fusion semantics yet (CPAPDex-PB corroboration / surfaced finding?); **§4** audit the other nodes for unconditional-envelope conformance; **§5** regenerate/retire the historical bare-array loose samples | Brief *(DONE 2026-06-29)* |
| [`OXYDEX-NODE-EXPORT-ENVELOPE-FOLLOWUPS-II-2026-06-29-BRIEF.md`](briefs/OXYDEX-NODE-EXPORT-ENVELOPE-FOLLOWUPS-II-2026-06-29-BRIEF.md) | Residue the event-lexicon pass deferred (each a deliberate per-node pass, never a blind batch): **§1** CPAPDex emitter `desat`→`desat_event` (re-bundle + golden regen); **§2** OxyDex `periodic_breathing` cross-node fusion FEATURE (corroboration / burden / finding); **§3** drop the dead `cvhr_surge` accept-type; **§4** `hrv_drop`/`hrv_low` + `stress_peak`/`stress_high` unify question; **§5** the multi-record carrier-key inconsistency (`nights`/`recordings`/`sessions`) | Brief *(DONE 2026-06-30)* |
| [`OXYDEX-NODE-EXPORT-ENVELOPE-FOLLOWUPS-III-2026-06-30-BRIEF.md`](briefs/OXYDEX-NODE-EXPORT-ENVELOPE-FOLLOWUPS-III-2026-06-30-BRIEF.md) | Residue from shipping cross-node periodic-breathing fusion (-II §2): **§1 (MED)** the longitudinal **PB-burden trend** — a NODE-side change (each PB node emits a PB metric in its `ganglior.crossnight` `metrics{}`; the generic `integrator-longitudinal.js` then trends + couples it automatically — CPAPDex `periodicBreathingPct`/night, OxyDex a PB index/night); **§2** ECGDex first-class `periodic_breathing` emit vs. the current `cvhrIndex` correlate channel (decision); **§3** `PB_CVHR_MIN` is a local heuristic — kernel-source or annotate Integrator-local; **§4** PB has no timeline band (window finding like its `staging`/`hrv_consensus` siblings — add a span, or leave) | Brief *(DONE 2026-06-30)* |
| [`OXYDEX-NODE-EXPORT-ENVELOPE-FOLLOWUPS-IV-2026-06-30-BRIEF.md`](briefs/OXYDEX-NODE-EXPORT-ENVELOPE-FOLLOWUPS-IV-2026-06-30-BRIEF.md) | Residue from executing `-III §1` (the longitudinal PB-burden trend): **§1 (LOW)** the one node `-III §1` left undone — an ECGDex per-night CVHR/CSR **cardiac-correlate** PB-burden crossnight metric (decision: build it per the per-node ritual, or record it intentionally absent since CVHR-as-index already corroborates at the same-night fusion layer); **§2 (LOW)** FINDING — `cite` is dropped in every node's def→envelope mapping (the builder plumbs `cite:m.cite\|\|null` but each `<node>-cross.js` omitted it); fixed for OxyDex+CPAPDex in `-III`, still latent in `ppgdex/ecgdex/pulsedex-cross.js` (inert today — none cite a crossnight metric) | Brief *(DONE 2026-06-30)* |
| [`OXYDEX-NODE-EXPORT-ENVELOPE-FOLLOWUPS-V-2026-06-30-BRIEF.md`](briefs/OXYDEX-NODE-EXPORT-ENVELOPE-FOLLOWUPS-V-2026-06-30-BRIEF.md) | Residue from executing `-IV` (a pure decision pass): the `cite`-in-mapping audit exposed a SUPERSET gap on the **`evidence`** field + pinned the exact PpgDex fold-in. **§1 (LOW)** `ppgdex-cross.js` `crossNightBlock`'s `Object.keys(PPG_DEFS).map(...)` drops BOTH `evidence` and `cite` (the lone real `-IV §2` drop — ECGDex/PulseDex pass `METRICS` literals so carry nothing to fix); fold the OxyDex/CPAPDex-parity one-liner into PpgDex's next rebuild; **§2 (LOW)** crossnight metric **`evidence` grade is absent for 3 of 5 nodes** (only OxyDex+CPAPDex plumb it; PpgDex/ECGDex/PulseDex surface `evidence:null` though their registries grade every metric) — inert until the Integrator Longitudinal view BADGES crossnight metrics (CLAUDE.md COVERAGE MANDATE), then a deliberate 3-node grading pass + a registry-parity test assertion | Brief *(DONE 2026-06-30)* |
| [`WELLTORY-COMPOSITES-ENDGAME-2026-07-01-BRIEF.md`](briefs/WELLTORY-COMPOSITES-ENDGAME-2026-07-01-BRIEF.md) | ADR-style terminal decision closing the "which way does the Welltory closed-input (black-box `Stress`/`Energy`/`Coherence`/`Focus`/`SNS`/`PSNS`) dependency end?" question — **option A: RATIFY QUARANTINE as terminal** (keep consuming, keep the composites visibly second-class via the already-shipped multi-layer quarantine); **option B (cut the dependency entirely) explicitly DECLINED**, with a recorded revisit-trigger (σ-paper provenance-purity requirement, Welltory terms change, or a post-`meta.derived`-down-weight mislead). Ratifies the existing machinery: adapter `provenance.derived`+`meta.derived`/heuristic tag, the FOLLOWUPS-V §2 registry tier-split, the FOLLOWUPS-V §1 zero-seed gate, COVERAGE-MANDATE badges | Brief *(DONE 2026-07-01)* |
| [`PROVENANCE-NONDETERMINISM-2026-06-29-BRIEF.md`](briefs/PROVENANCE-NONDETERMINISM-2026-06-29-BRIEF.md) | Provenance-gate non-determinism surfaced while executing OXYDEX-…-II §1/§3 + CPAPDEX-IV §2: **§1** the inliner (`super_inline_html`) keys manifest assets by RANDOM UUIDs → `manifestHash` changes on every re-bundle of identical source (proven: PulseDex re-bundled twice → `4b6ceb289138` then `1b12d2897389`), so GATE B "reds when code changes" really means "reds on any rebuild" (the `FIXTURE-PROVENANCE` "EXPORT-INERT re-record" churn is the symptom); **§2** the platform AUTO-REBUILDS bundles → `manifestHash` drifts with no human re-bundle → GATE A can spontaneously red; **§3** a live fleet-wide ledger drift (5 bundles) seen mid-session then re-synced; **§4** concurrent writers touched the workspace (files changed with no edit by the run); **§5** `buildHash` is runtime-helper-only, not statically recomputable from raw template text (doc trap) | Brief *(DONE 2026-06-29)* |
| [`PROVENANCE-NONDETERMINISM-FOLLOWUPS-2026-06-29-BRIEF.md`](briefs/PROVENANCE-NONDETERMINISM-FOLLOWUPS-2026-06-29-BRIEF.md) | Residue from making `manifestHash` deterministic: **§1 (MED)** `buildHash` non-determinism (`buildSource()` races injected badge/synth CSS) is now the LONE remaining provenance non-determinism — the real fix (snapshot `buildSource()` at module-eval, or content-hash the inliner keys) stays owner-gated (`GENERATOR-FOLLOWUPS-II §1`); **§2** the auto-rebuild / concurrent-writer / ledger-authorship model is documented-by-REASONING, not empirically proven (no watcher experiment run) — revisit if GATE A spontaneously reds; **§3** legacy buildHash-only fixtures (Integrator fusions + pre-R1 summaries) still lack the deterministic `manifestHash` teeth; **§4** stale per-fixture `note_*` lines quote pre-redefinition hashes (umbrella-note-clarified, optional prune); **§5** a continuous headless/Node-CI GATE-A drift check is now RELIABLE (cross-link `EXPORT-IDENTITY-FOLLOWUPS-IV §1`) | Brief *(DONE 2026-06-29)* |
| [`PROVENANCE-NONDETERMINISM-FOLLOWUPS-II-2026-06-29-BRIEF.md`](briefs/PROVENANCE-NONDETERMINISM-FOLLOWUPS-II-2026-06-29-BRIEF.md) | Residue from the decision pass (all LOW): **§1** ~5 legacy uploaded SAMPLE exports (`ECGDex_2026-06-13_1024`/`-06-17_2040` `446a8ecf3527`, `GlucoDex_2026-06-17_2121` `8ebee9986547`, `ppgdex_20260614`/`_20260616` `ab7d7d51ae21`) stamp OLD buildHashes → verify-provenance verdicts them red `stale build` ONLY on a host that can dir-list `uploads/`; the canonical sandbox can't → fallback set (`sidecar ∪ static-legacy`) never audits them (0 canonical reds, banner unaffected). Not curated fixtures, not byte-regenerable (don't code-gate), several are historical-fusion inputs (don't blind-delete) → decide leave-documented / scope-dir-listing-to-the-fallback-set / prune; **§2** `buildHash` lone non-determinism (owner-gated) + auto-rebuild model (revisit-if-bites) carried; **§3** continuous GATE-A check stays tracked at `EXPORT-IDENTITY-FOLLOWUPS-IV §1` | Brief *(DONE 2026-06-29)* |
| [`OWN-THE-BUILD-2026-06-30-BRIEF.md`](briefs/OWN-THE-BUILD-2026-06-30-BRIEF.md) | **Coder brief — retire the drift-suppression machinery by OWNING the build.** One thesis, three separable parts. **Part A (marquee):** a deterministic, dependency-free `tools/build.mjs` that plain-inlines the existing `*.js`/`.src.html` and emits `bundle + BUILD-MANIFEST + FIXTURE-PROVENANCE` as **build artifacts** — killing the `buildHash`/`manifestHash` nondeterminism class (supersedes the `PROVENANCE-NONDETERMINISM` workaround at the cause), the hand-edited ledgers + EXPORT-INERT re-record churn, and the hand-mirrored `ans-design.css`; `manifest-gate.js` gains one plain-inline branch, both gates verify instead of being hand-fed. **Phase 0 de-risk spike FIRST** (does the platform clobber the owned bundle? relocate sources if so). Takes the "own the inliner" path `GENERATOR-FOLLOWUPS-II §1` deliberately deferred. **Part B:** born-clean the next node (EEGDex/SpiroDex) — headless DSP + construction-enforced badges + `env.equiv` from commit one (forward-adopt per ARCH §7). **Part C:** badge-by-construction — make `MetricRegistry.badge()` the sole render path, one file at a time. No DSP math changes. ~5–7 focused days (Part A) | Brief *(**DONE 2026-07-05** — Part A Phases 0–2 landed: owned bundler (`tools/build-core.js` + `tools/build.mjs` + `tools/build.html`) + `manifest-gate.js` plain-inline dual-branch + `tests/build-core-tests.mjs` in CI; **Phase 0 verdict: ownership STICKS** (platform doesn't clobber the owned bundle — no relocation needed); **all 8 migrated, both gates green** (PulseDex cut over 2026-07-03 — stale captured woff2 dropped); ans-design single-sourcing + retire-now-unused-legacy-branch + doc rewrite (Phase 4) + Part-D.1 ESLint (`.github/workflows/lint.yml`) + Part-B born-clean gate all landed; CLAUDE.md §🔏 + ARCHITECTURE-PRINCIPLES §6 docs rewritten; GATE A + full `--check` re-verified green 2026-07-05. Parts C / D.2 / D.3 + Part-B new-node continue in the FOLLOWUPS brief)* |
| [`OWN-THE-BUILD-FOLLOWUPS-2026-07-03-BRIEF.md`](briefs/OWN-THE-BUILD-FOLLOWUPS-2026-07-03-BRIEF.md) | Residue + staged remainder from executing OWN-THE-BUILD Part A Phases 0–2 (2026-07-03; OxyDex owned end-to-end, both gates green). **§1 Phase 3** — migrate the remaining 7 bundles (uniform via `build.mjs`/run_script + the dual-branch; **PulseDex LAST** — owning the build drops its legacy captured IBM Plex Mono woff2 that the current src no longer references → owner decision). **§2 Phase 4** — ans-design single-sourcing (delete OxyDex/HRVDex `ANS-DESIGN-START` mirror), retire the legacy `__bundler/manifest` branch once 8/8 migrated, §🔏/ARCH§6/AUDIT-2a rewrite, drop inert `buildHash`. **§3 A5 single `clock.js` — EXECUTED 2026-07-03** (owner-ratified same day; 5 DSPs delegate to `DexClock`, 3 deliberate node-local variants stay, both gates green). **§4** records the preview-host fetch-injection + single-pass/terminator findings. **§5** Parts B/C/D (D.1 ESLint = cheapest next win) | Brief *(IN-PROGRESS 2026-07-05 — §1/§2/§3/§6 + Part-D.1 ESLint DONE; remaining: Part C (badge-by-construction, opportunistic), D.2 (checkJs), D.3 (Prettier), Part-B new-node build. §7 records the 2026-07-05 `OverDex.html` `--check` drift fix — stale `integrator-dsp.js` comment, rebuilt from source)* |
| [`DEV-TOOLCHAIN-2026-06-30-BRIEF.md`](briefs/DEV-TOOLCHAIN-2026-06-30-BRIEF.md) | **Coder brief — the formatter + house-invariant linter (the "black + flake8" to the existing "mypy").** The type-checker leg already shipped (Phase 4a `tsc --noEmit --checkJs` + `types.yml` + `dex-contracts.js` `@typedef`s; Phase 4b DSP-purity gate); this adds the two missing legs, same ships-nothing / CI-only / near-zero-dep constraints. **Part A (marquee, ZERO new dep):** new `tests/dex-tests.js` source-text gate groups — siblings of the purity gate — mechanizing **Clock-Contract** bans (`new Date(str)`/`Date.parse` on vendor stamps, non-`getUTC*` display getters, `new Date()` parser fallbacks), **SPDX-header** presence, **retired-vocabulary** ban (allow-listing the FROZEN `ganglior.*`/`fascia` verbatim), + folds in the orphaned -IV §1 reach-in allow-list. **Part B:** a `black`-equivalent (Biome preferred — one self-contained binary) **check-only in CI, on-touch only, NEVER big-bang** — a repo-wide reflow moves every `manifestHash` and churns every fixture (§🔏), so formatting only ever rides an independent on-touch re-bundle. **Part C:** widen `tsconfig.include` one de-DOMed module at a time. Continues AND-FRONTIER Phase 4; shares OWN-THE-BUILD's construction-enforcement thesis + Node-host reality | Brief *(IN-PROGRESS 2026-07-11 — Parts A+B complete; Part C carried)* |
| [`BIOME-FORMATTER-2026-07-11-BRIEF.md`](briefs/BIOME-FORMATTER-2026-07-11-BRIEF.md) | **Execution brief for DEV-TOOLCHAIN Part B** — adopt **Biome** (`@biomejs/biome`, one self-contained binary) as the code formatter. Whole design turns on the §B2 footgun: reflowing a shipped `*.js` moves every bundle's `manifestHash` → forces a fixture re-record for identical behavior, so Biome is **check-only in CI, on CHANGED files only** (`biome ci --changed`) and `--write` rides only net-new files + on-touch re-bundles — the legacy tree is never big-banged. **Phase 0** tune `biome.json` to the house style (2-space/single-quote/semicolons/wide lineWidth — measure the churn); **Phase 1** config + pinned `devDependency`+lockfile + `format.yml`, ZERO reformatting (prove `verify-provenance` untouched, 0 fixtures re-recorded); **Phase 2** on-touch application (rides the Phase-3 fleet re-bundle); **Phase 3 (deferred)** migrate lint off ESLint. Needs a Node host w/ npm (Biome binary — not runnable in-sandbox). Decisions: D1 format-only vs format+lint, D2 CI wiring, D3 changed-vs-whole-tree | Brief *(DONE 2026-07-11 — formatter + Phase 2 on-touch + Phase 3 lint migration; ESLint retired)* |
| [`DEX-TEST-SUITE-LAZY-FOLLOWUPS-2026-06-30-BRIEF.md`](briefs/DEX-TEST-SUITE-LAZY-FOLLOWUPS-2026-06-30-BRIEF.md) | Residue from making `Dex-Test-Suite.html` render-coverage **on-demand** (lazy) + tightening its waits, and the verify-provenance Tepna brand touch. **§1 (🔴 regression — FIX APPLIED):** the CI browser gate (`tests/browser-gates.mjs`) opened the suite WITHOUT `?full` → with lazy RC the "hang guard" group never appears → 5-min timeout / silent RC-skip; now navigates to `?full` (playwright CI re-run owed). **§2 (DONE 2026-07-01):** swept the live on-load-full-run assumptions to the `?full`/▶ ritual (CONTRIBUTING/AUDIT-PROMPT/ORIENTATION + `browser-gates.yml` comment + the suite eyebrow; the `__sameOriginOK` reader note now flags the SUITE's signal as post-RC-only). **§3 (DONE 2026-07-01):** no product-brand "Ganglior"/"ANS Intelligence" prose remained (verify-provenance title/h1 already Tepna this session); every hit is a frozen `ganglior.*`/bus-codename usage. **§4:** lazy-change hardening notes (no action). | Brief *(DONE 2026-07-04 — §2/§3 swept 2026-07-01; §1 resolved by the 2026-07-03 `browser-gates.mjs` hardening (`BROWSER-GATES-CI-TIMEOUT`: `?full` nav + `__rcState` wait + 15-min ceiling + crash diagnostics), predicate-equivalence verified in-browser; the literal `node tests/browser-gates.mjs` run superseded as a blocker (un-runnable here — closed on inspection); §4 notes-only. No `-II` follow-up)* |
| [`SELF-INGEST-2026-06-27-BRIEF.md`](briefs/SELF-INGEST-2026-06-27-BRIEF.md) | Reload a node's OWN `ganglior.node-export` back into its app as a print/PDF **clinical summary** (findings · KPIs · event timeline · evidence badges) to bring to a doctor WITHOUT the raw dataset — a faithful VIEW of the export-time computation: no recompute, no badge upgrade, no `buildHash` re-stamp; raw-chart panels greyed (never faked); foreign-node guard; optional **scrub-for-sharing** (strips device serials/filenames, keeps `contentId`). Unblocked by the v2.0 envelope (which is NOT self-reloadable today — `parseJSONL` reads the legacy array, not the `{…}` envelope). Pilot OxyDex | Brief *(DONE 2026-07-02 — OxyDex pilot shipped: reload→review-mode + clinical print + scrub + foreign-guard; §7 live in both runners; `OxyDex.html` re-bundled `a16db72bc689`, GATE A 8/8 + GATE B clean (export-inert re-record per `NODE-RESIDUE-FOLLOWUPS-III`). Six-node rollout → `SELF-INGEST-FOLLOWUPS-2026-07-03-BRIEF.md`)* |
| [`SELF-INGEST-FOLLOWUPS-2026-07-03-BRIEF.md`](briefs/SELF-INGEST-FOLLOWUPS-2026-07-03-BRIEF.md) | Roll `loadOwnExport` + the review-mode clinical summary out from the OxyDex pilot to the other six nodes, **one node per pass, each re-bundle+gate-gated**. Records the carrier reality the pilot surfaced: PulseDex=`recordings[]` + CPAPDex=`nights[]` are rich-carrier twins of OxyDex (mechanical, export-inert); GlucoDex/ECGDex single-record but rich (ECGDex rich only under `opts.rich`); **PpgDex + HRVDex exports are event-only + aggregate — NO per-element summary carrier**, so a faithful clinical view needs an export-enrichment decision FIRST (fixture-moving, not export-inert). §0 decisions: scrub shared-helper vs per-node duplicate; PpgDex/HRVDex accept-lean vs enrich. No DSP math changes | Brief *(DONE 2026-07-04 — CPAPDex pass DONE: `cpapLoadOwnExport` + shared `scrubExport` + 22-assert self-ingest group wired into both runners; CPAPDex `7136e9cd5642`, gates green. **D1 folded** (OxyDex `oxyScrubExport` → shared `dexScrubExport`, `313731f49b34`). **D2 decided**: PpgDex + HRVDex both → enrich-first (path b) — PpgDex reuse existing `opts.rich`, HRVDex add `measurements[]` carrier. **PulseDex pass DONE** (engine + review-mode UI; `pulseLoadOwnExport` + shared scrub + 23-assert §7 group + `pulseReviewView`, `3ecc15845d1b`, gates green). **GlucoDex pass DONE** (⚠ export was actually LIGHT not rich — enriched `glucoBuildNodeExport` with a `glucose` block, D2 path b; fixture regenerated + review UI; `861bcf0923eb`, gates green). **ECGDex pass DONE** (engine + review UI, export-inert; `1711e0b08098`, gates green). **HRVDex pass DONE** (enrich-engine + review UI; `measurements[]` carrier, D2 path b; `8c3150307a24`, gates green). **PpgDex pass DONE** (engine + review UI, export-inert — buildV2 already rich like ECGDex; `59ba60fc079b`, gates green). **✅ SELF-INGEST ROLL-OUT COMPLETE** — all 6 nodes (OxyDex pilot + Pulse/Gluco/ECG/HRV/Ppg) re-ingest their own export as a review-mode clinical view; `?full` 1869 passed / 123 groups, provenance A/B clean)* |
| [`SELF-INGEST-FOLLOWUPS-II-2026-07-04-BRIEF.md`](briefs/SELF-INGEST-FOLLOWUPS-II-2026-07-04-BRIEF.md) | Residue from executing the 6-node self-ingest roll-out. **F1 (🔴 privacy):** the shared `dexScrubExport` strips `out.recording` + `out.nights[].recording` but NOT the `recordings[]` (ECGDex/PulseDex) / `sessions[]` (PpgDex) multi carriers → a scrubbed MULTI export leaks device/serial per element; fix = generalize the sweep to all three carriers (fleet re-bundle, export-inert). **F2 (🟠 test):** the review-mode RENDER functions (`*ReviewView`) are exercised by no gate — add a render assertion. **F3 (🟡 doc):** the light(`exportGanglior`)/rich(`buildV2`/`exportSummary`) export DUALITY has no contract — 3 nodes were mis-Tiered in the parent; write an "export shapes per node" table for EEGDex. **F4/F5 (LOW):** ECG/PpgDex §7 use hand-built exports; 4/5 review renderers are app-IIFE-local not global | Brief *(✅ DONE 2026-07-04 — F1 scrub sweeps all 3 carriers + new multi-scrub §7 legs; F2 live review probe (rich+light) for 5 nodes; F3 done; F5 `<Node>.reviewView`/`.renderReview` namespace convention + source-mirror group. Fleet re-bundle, GATE-A + 13 fixtures re-recorded export-inert; verify-provenance/headless/no-network green. F2-OxyDex-live + F4 → -III)* |
| [`SELF-INGEST-FOLLOWUPS-III-2026-07-04-BRIEF.md`](briefs/SELF-INGEST-FOLLOWUPS-III-2026-07-04-BRIEF.md) | Residue from -II (both LOW, carried forward): **F1** OxyDex's live review render is NOT driven by the -II browser probe (its review is `renderAll`-integrated, not a standalone `renderReview`/card — only source-mirrored, not live-driven like the other 5); **F2** ECGDex/PpgDex §7 groups reload hand-built buildV2-shaped exports, not a real `buildV2` output (defer unless `buildV2` changes) | Brief *(✅ DONE 2026-07-04 — F1: OxyDex live review-render leg (single-night + multi-night) added to its `APP_COVERAGE` extraProbe, logic validated headlessly, test-only/no re-bundle; F2 deferred → -IV. Discovered: `OxyDex.reviewView` nights-default nit → -IV F1)* |
| [`SELF-INGEST-FOLLOWUPS-IV-2026-07-04-BRIEF.md`](briefs/SELF-INGEST-FOLLOWUPS-IV-2026-07-04-BRIEF.md) | Residue from -III (both LOW): **F1** `OxyDex.reviewView`'s `nights` arg defaults to `review.elements` but OxyDex reloads return `review.nights` (harmless — always called with explicit nights; fold on next OxyDex re-bundle + harden `oxyClinicalSummary` against empty nights); **F2** the standing ECGDex/PpgDex §7 hand-built-export fidelity defer (carried since -II F4; fires only if `buildV2` changes) | Brief *(✅ DONE 2026-07-04 — F1 executed standalone: reviewView nights-default elements→nights + honest empty-nights placeholder; OxyDex re-bundled render-only EXPORT-INERT cf8f4ceaf1fe→7fbedea9a12f (reconciled a stale out-of-band bd269931cae1 ledger write), GATE A/B green. F2 stays standing-defer in-place — no -V spawned)* |
| [`EXPORT-SHAPES.md`](docs/EXPORT-SHAPES.md) | **The light-vs-rich export contract per node** — every node emits a `ganglior.node-export`, but the LIGHT `exportGanglior` (`…BuildNodeExport`: recording + events, the byte-identical Integrator fusion stream) differs from the RICH `buildV2`/`exportSummary` (full derived layer, the clinical reload target). Per-node table (which builder, which fields, the `nights[]`/`recordings[]`/`sessions[]` multi-carrier key, what `loadOwnExport` reads) + the rules a new node must follow. Written after 3 nodes were mis-Tiered in the self-ingest roll-out; the reference EEGDex/SpiroDex fill in at build time (SELF-INGEST-FOLLOWUPS-II F3) | Spec *(living)* |

---

## 📦 Releases, versioning & compliance (IEC 62304 / ISO 13485-aligned)

> One suite **SemVer** (canonical in `suite.manifest.json` `version`) is the release identity. The
> current tree is **1.0.0** (`CONTROLLED-RELEASES-2026-07-05`). *Alignment, not conformance* — Tepna
> remains "Not a medical device"; every compliance doc carries that disclaimer.

| Doc | What it is | Role |
|---|---|---|
| [`CHANGELOG.md`](CHANGELOG.md) | The suite changelog (Keep a Changelog) — one section per controlled release; history below 1.0.0 is reconstructed from the DONE brief corpus | Reference *(living)* |
| [`RELEASE-MANIFEST.json`](RELEASE-MANIFEST.json) | Machine-readable release ledger — per release: version, date, contributing briefs, per-app `manifestHash` snapshot. The `release-ledger` gate keeps it ≡ the canonical `suite.manifest.json` version | Record *(ledger)* |
| [`SOFTWARE-LIFECYCLE-PLAN.md`](docs/COMPLIANCE/SOFTWARE-LIFECYCLE-PLAN.md) | Umbrella — SemVer bump semantics + the IEC 62304 / ISO 13485 clause crosswalk (with an honest gap column) | Reference *(living)* |
| [`SAFETY-CLASSIFICATION.md`](docs/COMPLIANCE/SAFETY-CLASSIFICATION.md) | IEC 62304 §4.3 software safety class — Class A rationale (non-device; retrospective descriptive summaries only) | Reference *(living)* |
| [`CONFIGURATION-MANAGEMENT-PLAN.md`](docs/COMPLIANCE/CONFIGURATION-MANAGEMENT-PLAN.md) | IEC 62304 §8 — configuration items, change control, status accounting (= CHANGELOG + RELEASE-MANIFEST), reproducibility | Reference *(living)* |
| [`SOUP-LIST.md`](docs/COMPLIANCE/SOUP-LIST.md) | IEC 62304 §5.3.3/§8.1.2 — runtime SOUP **empty by design**; build-time tooling only | Reference *(living)* |
| [`SOFTWARE-RELEASE-PROCEDURE.md`](docs/COMPLIANCE/SOFTWARE-RELEASE-PROCEDURE.md) | IEC 62304 §5.8 release SOP — the `tools/release.mjs` runbook, sign-off, reproducibility evidence | Reference *(living)* |
| [`DOCUMENT-CONTROL.md`](docs/COMPLIANCE/DOCUMENT-CONTROL.md) | ISO 13485 §4.2.4/§4.2.5 crosswalk — the brief lifecycle + gates **as** document/record control | Reference *(living)* |

---

## 2 · Per-node build briefs

| Doc | Node | Role |
|---|---|---|
| [`ECGDex-BUILD-BRIEF.md`](briefs/ECGDex-BUILD-BRIEF.md) | ECGDex — build brief & handoff | Brief |
| [`ECGDEX-REFERENCE-GUIDE-BRIEF.md`](briefs/ECGDEX-REFERENCE-GUIDE-BRIEF.md) | ECGDex reference-guide build | Brief |
| [`PPGDex-BUILD-BRIEF.md`](briefs/PPGDex-BUILD-BRIEF.md) | PpgDex — build brief & handoff | Brief |
| [`PPGDEX-BEAT-DETECTION-PERF-AND-HRV-FIDELITY-2026-07-02-BRIEF.md`](briefs/PPGDEX-BEAT-DETECTION-PERF-AND-HRV-FIDELITY-2026-07-02-BRIEF.md) | PpgDex — swap the O(N·lag) autocorrelation for an O(N) streaming detector (MSPTD/TERMA) run as a 3-LED consensus, Web-Worker-parallelised; gate the jitter-inflated whole-record HRV *(PROPOSED)* | Brief |
| [`GlucoDex-BUILD-BRIEF.md`](briefs/GlucoDex-BUILD-BRIEF.md) | GlucoDex — build brief & handoff | Brief |
| [`GLUCODEX-FUSION-WINDOWING-BRIEF.md`](briefs/GLUCODEX-FUSION-WINDOWING-BRIEF.md) | GlucoDex × Integrator — windowing CGM at the fusion layer | Brief |
| [`CPAPDEX-BUILD-BRIEF.md`](briefs/CPAPDEX-BUILD-BRIEF.md) | CPAPDex (ResMed AirSense 11) — build brief | Brief |
| [`CPAPDEX-KICKOFF.md`](briefs/CPAPDEX-KICKOFF.md) | CPAPDex — ordered handoff kickoff script | Brief |
| [`CPAPDEX-CODEGEN-HANDOFF.md`](briefs/CPAPDEX-CODEGEN-HANDOFF.md) | CPAPDex — codegen scaffold handoff | Brief |
| [`EEGDEX-BUILD-BRIEF.md`](briefs/EEGDEX-BUILD-BRIEF.md) | EEGDex (Muse, planned) — build brief | Brief |
| [`INTEGRATOR-BUILD-BRIEF.md`](briefs/INTEGRATOR-BUILD-BRIEF.md) | The Integrator (cross-node fusion layer) — build brief | Brief |
| [`INTEGRATOR-THREE-CORNERED-HAT-2026-07-02-BRIEF.md`](briefs/INTEGRATOR-THREE-CORNERED-HAT-2026-07-02-BRIEF.md) | Integrator — reference-free per-sensor error via correlated three-cornered-hat over ECG/PPG/Oxy → inverse-variance fusion weight + outlier attribution (extends `fuseHRVConsensus`). Estimator `integrator-tch.js` + §1/§3 wiring + §5 render card all shipped; `Integrator.html` re-bundled `ca7b872a68e8`; `Dex-Test-Suite?full` all-green (TCH 17/17 + wiring 9/9 + render-coverage 9/9) + verify-provenance GATE A/B clean. Deferred external-ρ / real-night-firing / τ-curve → `INTEGRATOR-THREE-CORNERED-HAT-FOLLOWUPS-2026-07-03-BRIEF.md` *(DONE 2026-07-03)* | Brief |
| [`INTEGRATOR-THREE-CORNERED-HAT-FOLLOWUPS-2026-07-03-BRIEF.md`](briefs/INTEGRATOR-THREE-CORNERED-HAT-FOLLOWUPS-2026-07-03-BRIEF.md) | Integrator TCH residue — (§1) consumer-side common-mode ρ estimate (needs a per-epoch motion series on ≥2 triplet nodes; only PpgDex emits one today), (§2) making TCH *fire* on a real night (upstream OxyDex per-epoch-HR export — both triplets currently lack a 3rd series-bearing node), (§3) per-sensor Allan-deviation τ-curve sparkline. All additive; all blocked on node-export data, not the estimator *(DONE — 2026-07-06 — HR-hat + external-ρ + τ-curve all shipped; the reference-night golden landed via FU-II §2 [first code-gated Integrator fixture, `cef329a4fec6`]. §4 N-cornered hat DEFERRED, blocked on a real ≥4-sensor co-recording + EEGDex → FU-III)* | Brief |
| [`OXYDEX-PER-EPOCH-HR-EXPORT-2026-07-04-BRIEF.md`](briefs/OXYDEX-PER-EPOCH-HR-EXPORT-2026-07-04-BRIEF.md) | OxyDex emits a top-level `timeseries.epochs[]` `{tMin, hr, motionIndex}` 5-min series — the TCH **HR-hat**'s 3rd corner (ECG+PPG+Oxy) + a 2nd per-epoch motion series for the correlated-TCH ρ. The named §2 prerequisite of the Integrator TCH follow-ups; export-inert for the OxyDex fixtures. **DONE 2026-07-04.** | Brief |
| [`INTEGRATOR-THREE-CORNERED-HAT-FOLLOWUPS-II-2026-07-04-BRIEF.md`](briefs/INTEGRATOR-THREE-CORNERED-HAT-FOLLOWUPS-II-2026-07-04-BRIEF.md) | Residue from landing the HR-hat + external-ρ (FU-I §1/§2): **§1 (⚠ correctness)** TCH aligns on node-relative `tMin` not absolute wall-clock — co-recorded devices that start minutes apart mis-align → biased σ²; **§2** the committed reference-night golden (first code-gated Integrator fixture); **§3** render `block.tchHR`; **§4** real-data ρ validation + motion comparability; **§5** document the quiet-sensor recovery-precision limit. *(DONE — 2026-07-06 — §1/§3/§5 done 2026-07-05; §2 golden landed = the FIRST code-gated Integrator fixture [`uploads/integrator_tch_golden.node-export.json`, `cef329a4fec6`]; §4 real-data-ρ DEFERRED, blocked on a real co-recorded night → FU-III)* | Brief |
| [`INTEGRATOR-THREE-CORNERED-HAT-FOLLOWUPS-III-2026-07-06-BRIEF.md`](briefs/INTEGRATOR-THREE-CORNERED-HAT-FOLLOWUPS-III-2026-07-06-BRIEF.md) | TCH residue after FU-I/FU-II closed: (§1) real-data ρ validation, (§2/§3) optional golden variants, (§4) N-cornered hat (3→N sensors). *(DONE — 2026-07-13 — **§1 CLOSED**: the real 24-night distribution + reference-anchored magnitude check landed through the committed harness [24/24 solve; median σ ECGDex 0.79 / OxyDex 1.08 / PpgDex 2.20 bpm — ranking preserved, Verity corner bracketed by the two independent anchors ≈1.8/≈2.8]; the rescue reproduced on 6 of 11 quiet-order nights, and the 5 failures **confirmed the §7 ρ-dilution hypothesis** — `_tchRhoFromMotion`'s mean-of-pairwise ρ under-reads exactly in the quiet-order shape, so the kernel falls through to its boundary-seeking min-ρ search and pins the quiet corner at ≈0. Write-up `docs/INTEGRATOR-TCH-REALDATA-VALIDATION-2026-07-06.md` §11. §2/§3 + §4 [N-cornered, still EEGDex/≥4-sensor-blocked] explicitly RE-DEFERRED → FU-IV)* | Brief |
| [`INTEGRATOR-THREE-CORNERED-HAT-FOLLOWUPS-IV-2026-07-13-BRIEF.md`](briefs/INTEGRATOR-THREE-CORNERED-HAT-FOLLOWUPS-IV-2026-07-13-BRIEF.md) | TCH residue after FU-III closed. **§1 (the payload, now INDICATED by real data)** coupled-pair-weighted `_tchRhoFromMotion` — the mean-of-pairwise ρ dilutes exactly in the quiet-order shape it exists to rescue, leaving 5 of 11 quiet-order nights with a corner pinned at σ≈0; acceptance = those five nights rescue without disturbing the other 19 (changes fusion output → golden regen + re-bundle + changeset). **§2** carried LOW golden polish. **§3** N-cornered hat, still blocked on a ≥4-sensor co-recording + EEGDex. **§4** two artifact nights handed to the artifact gate. **§5** already done by PR #81 (the 7 further trio nights are committed; corpus 17 → 24) *(PROPOSED — §1 unblocked now)* | Brief |
| [`INTEGRATOR-TCH-ML-ESTIMATOR-2026-07-11-BRIEF.md`](briefs/INTEGRATOR-TCH-ML-ESTIMATOR-2026-07-11-BRIEF.md) | **Principled negative-variance estimator** for the Integrator TCH — replace the heuristic min-ρ clamp (quiet-order regime) with a **maximum-likelihood TCH** (non-negative by construction + bootstrap uncertainties; Schatzman 2020) or **Groslambert/two-sample covariance** (GCOV; Calosso 2018, outperforms TCH on the negative-variance case). Bake-off FIRST on `tools/tch-multinight.mjs` + the real 07-06 night; ship additively (opt-in flag, N=3 golden byte-identical) only if it beats the clamp. Motivated by the docs §9 method-literature sweep | Brief *(DONE 2026-07-11 — bake-off `tools/tch-estimator-bakeoff.mjs` = NEGATIVE: no HR-only candidate beats the min-ρ clamp at N=3 [GCOV≡classic; quiet-MAE BASE 0.334 < GCOV 0.746/NNLS 0.719; only external-ρ 0.080 recovers], ML advantage deferred to FU-III §4 N≥4; no code/re-bundle. Docs §10)* |
| [`SECURITY-REMEDIATION-2026-07-11-BRIEF.md`](briefs/SECURITY-REMEDIATION-2026-07-11-BRIEF.md) | **Fix the open privacy/security audit findings** (`PRIVACY-SECURITY-AUDIT-FINDINGS-2026-07-01`, untouched for 10 days). **Phase A (urgent):** the live untrusted→DOM injections — **F1 (HIGH)** OxyDex stored-XSS (`oxydex-app.js:321` persisted filename→`innerHTML`, auto-executes on reload) + **F3** OxyDex error→DOM + **F2 (MED)** PulseDex reflected-XSS via comparison filename — fixed with ONE shared `escapeHTML`/`.textContent`, OxyDex+PulseDex re-bundle (EXPORT-INERT) + a "filename renders escaped" gate. **Phase B:** meta-**CSP** (F7) in all 8 `.src.html` (`connect-src 'none'` — browser-enforces no-network). **Phase C:** storage hygiene — **F4** raw-CSV/name cached forever, **F5** Clear-doesn't-clear + no erase-all path, **F6** legacy profile keys linger. A first (2 apps); B+C ride the Phase-3 fleet re-bundle. All EXPORT-INERT/PATCH. Owner decisions resolved: F4=drop, F7=baseline-CSP-defer-nonce. Phase A shipped in v1.2.0 (PR #9); B+C added on top. | Brief *(DONE 2026-07-11)* |
| [`SECURITY-REMEDIATION-FOLLOWUPS-2026-07-11-BRIEF.md`](briefs/SECURITY-REMEDIATION-FOLLOWUPS-2026-07-11-BRIEF.md) | Follow-ups discovered while executing the security remediation: **§1** nonce/hash-based `script-src` (deferred by F7 decision A — a hash-based strict CSP would make it an injection backstop, not just an egress control); **§2** erase-all doesn't cover the standalone analysis pages' checkpoint keys/DBs; **§3** the erase control isn't shown in CPAPDex/Integrator (data still erasable from the 6 profile apps); **§4** F1 is now closed by removal, not escaping (informational). | Brief *(DONE 2026-07-11 — §2/§3 executed; §1 executed via the strict-CSP brief)* |
| [`SECURITY-CSP-STRICT-SCRIPT-SRC-2026-07-11-BRIEF.md`](briefs/SECURITY-CSP-STRICT-SCRIPT-SRC-2026-07-11-BRIEF.md) | Executes FOLLOWUPS §1 — the deferred strict, hash-based `script-src`. Step-by-step plan to drop `'unsafe-inline'` from `script-src` fleet-wide: a shared **event-delegation dispatcher** (`dex-actions.js`) → convert all ~150 inline `on*=` handlers (`.src.html` + runtime `innerHTML`) to `data-act` → hash the inline `<script>` blocks in the owned bundler → drop `'unsafe-inline'` + verify every app's interactions headless (+ a negative-control injection-blocked test). Makes CSP an *injection* backstop, not just the egress control it is today. Large, EXPORT-INERT. | Brief *(DONE 2026-07-11)* |
| [`SECURITY-NONBUNDLE-CSP-COVERAGE-2026-07-13-BRIEF.md`](briefs/SECURITY-NONBUNDLE-CSP-COVERAGE-2026-07-13-BRIEF.md) | Executes `PRIVACY-SECURITY-AUDIT-FINDINGS-2026-07-13` N1 — extend the two backstops the 10 owned bundles carry (meta-CSP + `no-network` gate coverage) to the **unbundled, data-ingesting** surfaces the remediation left uncovered: the standalone analysis/research pages + `index.html`. Aligns the CSP/no-network scope with the erase-all scope `SECURITY-REMEDIATION-FOLLOWUPS §2` already set (`dex-forget` erases these pages' checkpoints). Baseline `connect-src 'none'\|'self'; default-src 'self'` per page + extend `no-network.html` `PAGES` / the `tests/dex-tests.js` CSP-presence leg. **No re-bundle** (pages are outside `BUILD-MANIFEST.json`), EXPORT-INERT. | Brief *(DONE 2026-07-13)* |
| [`SECURITY-LEAK-HYGIENE-2026-07-13-BRIEF.md`](briefs/SECURITY-LEAK-HYGIENE-2026-07-13-BRIEF.md) | Executes `PRIVACY-SECURITY-AUDIT-FINDINGS-2026-07-13` N2 + N3 (consolidated like F1/F2/F3). **N2** drop OxyDex's four unconditional `console.log`s that dump the raw filename + raw CSV header bytes on load (`oxydex-dsp.js:368/380/441/443`; local-only, minimization); **N3** escape PpgDex's `showErr`/`showOK` `innerHTML` error toast (`ppgdex-app.js:783-784`, the lone node the F1/F2/F3 escaper pass skipped). Two bundles (OxyDex + PpgDex), both `manifestHash`-only, EXPORT-INERT + source-mirror regression assertions. | Brief *(DONE 2026-07-13)* |
| [`LITERATURE-USE-POLICY-2026-07-11-BRIEF.md`](briefs/LITERATURE-USE-POLICY-2026-07-11-BRIEF.md) | **How Tepna uses published literature** — a decision/reference brief: three tiers (formulas/processes · reference statistics/priors · raw datasets) all ALLOWED with rules, the hard line (**no networked data in a bundle, ever**; no fabricated `validated`-tier authority; mandatory attribution), the routing rule (node validation doc · `PAPERS-ROADMAP` · an executable brief when literature changes code), and a living anchor index from the 2026-07-11 sweep. Consolidates implicit `CLAUDE.md` rules; ships no code | Brief *(PROPOSED 2026-07-11)* |
| [`AMBULATORY-MODE-BRIEF.md`](briefs/AMBULATORY-MODE-BRIEF.md) | Activity-aware mode — stop scoring a walk as a sleep | Brief |
| [`OXIMETER-SELFGATE-AND-CONSEQUENCE-COROBORATION.md`](docs/OXIMETER-SELFGATE-AND-CONSEQUENCE-COROBORATION.md) | Shared self-gate + consequence-corroboration reasoning upgrade | Brief |

---

## 3 · Audits, reviews & fix briefs

| Doc | What it is | Role |
|---|---|---|
| [`AUDIT.md`](audits/AUDIT.md) | Suite-wide health + friction pass (June 2026) — read before re-investigating known issues | Audit |
| [`DEEP-AUDIT-FINDINGS-2026-06-30.md`](audits/DEEP-AUDIT-FINDINGS-2026-06-30.md) | Deep-correctness pass per `AUDIT-PROMPT.md` (2026-06-30). 3 findings: **(MED)** PulseDex surfaces + `validated`-badges "VLF (night)"/"Total Pwr (night)" from the crude `spectral()` `rmssd²` proxy not Lomb–Scargle (closes `SIGNAL-ADAPTER §622`); **(LOW/process)** the new Integrator-PB render-coverage rig's cold iframe-boot watchdog hard-fails → non-deterministic all-green pill; **(LOW)** GlucoDex has no mmol/L display path (mg/dL-only) vs the metric-default mandate. Plus a verified-clean ledger (Clock Contract · Baevsky units · glycemic constants · fabricated-absence gates · PB fusion noisy-OR · provenance) | Audit |
| [`DEEP-AUDIT-FIXES-2026-06-30-BRIEF.md`](briefs/DEEP-AUDIT-FIXES-2026-06-30-BRIEF.md) | **Executes** the `DEEP-AUDIT-FINDINGS-2026-06-30` punch-list. **DONE — 2026-06-30 (all three §§; both gates green).** §1 (MED) dropped PulseDex's two `(night)` spectral rows + the dead `spectral()` proxy + the `validated`-borrowing aliases (PulseDex `1a8b99cf8a4c→4ad7ee5b9982`, export-inert; **closed `SIGNAL-ADAPTER §622`**); §2 (LOW/process) made the Integrator-PB render-coverage boot watchdog non-fatal (warm + retry-once) → deterministic all-green pill (test-infra only); §3 (LOW, Option A) added a read-only mg/dL⇄mmol/L **display-only** toggle to GlucoDex (default mg/dL; standardized mmol bands; compute stays mg/dL; GlucoDex `267987038e2f→650c1738827e`, export-inert). Gates: Dex-Test-Suite **1608/104** · verify-provenance **A 8/8 + B**. Residue → followups brief | Brief |
| [`DEEP-AUDIT-FIXES-FOLLOWUPS-2026-06-30-BRIEF.md`](briefs/DEEP-AUDIT-FIXES-FOLLOWUPS-2026-06-30-BRIEF.md) | Residue from executing `DEEP-AUDIT-FIXES` §3 (all LOW). **DONE — 2026-07-03 (per-surface).** The 3 GlucoDex mmol/L display-residue surfaces: (1) profile **target-INPUT** → KEEP mg/dL, no code change (field migrated into the SHARED `dex-profile.js` panel where mg/dL is canonical + the label is explicit; honoring the toggle = shared-module edit → fleet-wide re-bundle, out of scope for LOW); (2) Ganglior event-stream preview → KEEP mg/dL + honest clarifier (mirrors the raw node-export); (3) CSV hint → KEEP, harmonized to unit-neutral `timestamp, glucose`. Both edits display-copy → **EXPORT-INERT**; GlucoDex `25eaee49bd19→62d38df70558`, `GlucoDex_2026-06-27_equiv` fixture manifestHash re-recorded (output/input hashes UNCHANGED). Gates: **GATE A 8/8 + GATE B 15/15 reproducible**; Dex-Test-Suite GlucoDex source-mirror + equiv confirmed. +§2 docs note. No follow-up | Brief |
| [`DEEP-AUDIT-FINDINGS-2026-07-01.md`](audits/DEEP-AUDIT-FINDINGS-2026-07-01.md) | Deep-correctness pass per `AUDIT-PROMPT.md` (2026-07-01). 2 residue findings OUTSIDE the prior clean-ledger: **(MED)** HRVDex transparent HRV columns parsed a blank cell to a fabricated `0` (polluting the rolling SDNN baseline); **(LOW–MED)** ECGDex device cross-check loaders used `Date.parse`+`_floatNow()` (viewer-TZ-dependent). Both **EXECUTED** → see the fixes brief. | Audit |
| [`DEEP-AUDIT-FIXES-2026-07-01-BRIEF.md`](briefs/DEEP-AUDIT-FIXES-2026-07-01-BRIEF.md) | **Executes** the `DEEP-AUDIT-FINDINGS-2026-07-01` punch-list. **DONE — 2026-07-01 (both findings; both gates green).** Finding 1: HRVDex transparent cols parse absent→`null` (`numOrNull`) + symmetric SDNN rolling filter + `d_sdnn_z` presence gate (`6c4a8930b1cb→00578ca08503`, export-inert). Finding 2: ECGDex cross-check loaders delegate to the `ECGDSP.parseDevice*` twins; `parseRows`/`Date.parse`/`_floatNow` removed (`39388acbc7dd→ede9e04831c8`, export-inert). | Brief |
| [`DEEP-AUDIT-FIXES-FOLLOWUPS-2026-07-01-BRIEF.md`](briefs/DEEP-AUDIT-FIXES-FOLLOWUPS-2026-07-01-BRIEF.md) | Residue from executing `DEEP-AUDIT-FIXES-2026-07-01` (all LOW/latent — **DONE — 2026-07-01**): §1 ECGDex primary loader + RR/HRV exporters thread null (retired the `_floatNow()` now()-fallback; `ede9e04831c8→ac57ef19b66d`); §2 HRVDex pNN50 rolling slope → `Number.isFinite` (keeps real 0, drops null; `00578ca08503→936c007aed32`); §3 subjective-cols→null migration **decided no-go** (correct + gated as-is). Both re-bundles export-inert; both gates green. | Brief |
| [`DEEP-AUDIT-FIXES-FOLLOWUPS-II-2026-07-01-BRIEF.md`](briefs/DEEP-AUDIT-FIXES-FOLLOWUPS-II-2026-07-01-BRIEF.md) | Render-layer twin of the -I DSP nullability fixes (**DONE — 2026-07-03**, LOW–MED): §1 `hrvdex-render.js` — correlation scatter + weekday distribution + `_patPearson` → `Number.isFinite`, and `fmt0-4` null-guard. The audit caught TWO paths past the two named lines: the correlation HEATMAP feeds `_patPearson` RAW nullable `series` (the brief's "protected" note was wrong), and `renderTable`'s `fmt(_sdnn/_rmssd)` would `null.toFixed()`-crash on a nullable cell (a Finding-1 regression) — both fixed, a real 0 kept. HRVDex re-bundled `936c007aed32→93dd371ef306`, export-inert (clean render-only asset diff, 16/17 assets byte-identical). §2 ECGDex `stampEpochPositions`/`accAnalyze`/`accExtras` VERIFIED null-safe (`off=0` on null t0Ms; no code change/re-bundle); undated-export **decided = keep anchor-at-0**. Tests added both runners. GATE A 8/8 + GATE B HRVDex reproducible (disk-recompute); full node `run-tests.mjs` pending a node-CI env. | Brief |
| [`EFFICIENCY-AUDIT-FINDINGS-2026-07-13.md`](audits/EFFICIENCY-AUDIT-FINDINGS-2026-07-13.md) | Fresh efficiency pass (2026-07-13; baseline green), one day after 2026-07-12. **Closes the one blind spot both prior passes left — the Integrator fusion / longitudinal layer, never before measured, is measured (main realm, per M1) and CLEAN at real corpus scale:** `runFusion` over the whole 17-night trio corpus (265 desat + 657 surge events) = ~4–8 ms; `CpapEdf.readEDF` on a real 450 KB EDF = 1.14 ms; `OxyDex.compute` on the 25.2k-row synthetic longnight = 196 ms with each analyzer called once (no redundant recompute). **Measured & dismissed:** `fuseApneaEvents`'s O(D×S) match loop is real but capped by real n (corpus is 265×657, ~ms); the whole-corpus re-fusion is batched (one `recompute()` per drop). **No new accepted finding** — the actionable work is the still-UNEXECUTED 2026-07-12 backlog (D3 red-gate recap, X1 stale `manifestHash` para, the two PROPOSED PpgDex/hotspots briefs), pointed to not re-filed. Verdict: bottleneck is execution, not discovery | Audit |
| [`EFFICIENCY-AUDIT-FINDINGS-2026-07-12.md`](audits/EFFICIENCY-AUDIT-FINDINGS-2026-07-12.md) | Efficiency pass per `EFFICIENCY-AUDIT-PROMPT.md` (2026-07-12; baseline green). **The two headline results are not speedups.** **C1 (HIGH, correctness)** — `ppgdex-morph.js` steps a sample loop by a NON-INTEGER stride (`winSamp = 60 × fs`, `fs` is always a float on a real Polar file), so `raw[fractional]` → `undefined` → `NaN` → the surfaced per-window **perfusion-index trend is mostly `null`** (2 of 18 windows on a real capture); the `Math.round` fix is also **2.24×** and leaves both gated PpgDex fixtures byte-identical. **G1 (HIGH, gate integrity)** — **CI runs a WEAKER gate than local**: 2087 assertions/11 skipped vs 2107/2, and **GATE B verifies only 10 of 23 fixtures** ("uploads/ not served" — real recordings are gitignored); nothing pins the skip count, so a new skip is invisible. **M1 (HIGH, method)** — the Node `vm` realm distorts profiles up to **12.3×**, which is where `PROFILED-HOTSPOTS §3` came from: the "28% in `mean`" hoist measures **1.015×** end-to-end → **§3 RETRACTED**. Plus **P1** PpgDex parses 190k timestamps it never reads (**compute 2.01×**, byte-identical), **D1** local gate still single-threaded (102 s vs 28 s forked), **D2** the two generated list files conflict in 45% of commits and a conflicted PR gets **no CI at all**, **D3** a red gate prints 169 KB and names the failure once on line 1651, **X1–X4** `CLAUDE.md`'s `manifestHash` paragraph is stale + 4 DOCS-INDEX rows contradict their brief header. Dismissed: don't cache tsc/npm/Playwright (latency-neutral), don't shard past 4 | Audit |
| [`EFFICIENCY-AUDIT-FINDINGS-2026-07-01.md`](audits/EFFICIENCY-AUDIT-FINDINGS-2026-07-01.md) | Efficiency pass per `EFFICIENCY-AUDIT-PROMPT.md` (2026-07-01; baseline green). **Lane B clean** — full 8 h ECG (3.46M samples) traced: `analyze()` 760 ms, parse ~730 ms/night, all O(n), no recompute, envelope-decimated render, capped entropy; the one perf item (ECG orchestrate) is already-filed-LOW. **Lane A (2 findings):** A1 (HIGH) `DOCS-INDEX.md` is ~31k tokens / 31% inline changelog duplicating each brief's DONE header → trim rows to role+status+link; A2 (MED–HIGH) the fast-loop toolchain (`check-dex`/`reconcile-provenance`/`verify-manifest`/`run-tests`) is Node-only → no-Node-host agent re-derives "Node-CI debt" across ≥15 briefs → one canonical gate-equivalence note + a browser reconcile-reporter mode in `verify-provenance.html`. | Audit |
| [`DEX-SUITE-EXTERNAL-REVIEW.md`](audits/DEX-SUITE-EXTERNAL-REVIEW.md) | Independent external review (scored A–F + top-10) | Audit |
| [`DEX-SUITE-REVIEW-RESPONSE.md`](audits/DEX-SUITE-REVIEW-RESPONSE.md) | Point-by-point response & action plan to the review (WP-A executed) | Brief |
| [`DEX-METRIC-REMOVAL-AUDIT-BRIEF.md`](briefs/DEX-METRIC-REMOVAL-AUDIT-BRIEF.md) | Which metrics to remove/demote + 1:1 hero replacements (FULL removal of ANS Age + HRV/oximetry→BP + Metabolic Age across all 6 nodes + 🟡 welfare→Restoration Index — DONE 2026-06-23, both gates green) | Audit / Brief |
| [`DEX-METRIC-REMOVAL-FOLLOWUPS-2026-06-23-BRIEF.md`](briefs/DEX-METRIC-REMOVAL-FOLLOWUPS-2026-06-23-BRIEF.md) | Post-removal cleanup: regenerate 3 stale export fixtures, scrub codegen manifests, dead .readiness-ansage CSS, empty projection sections | Brief *(DONE 2026-06-29)* |
| [`REVIEW-FOLLOWUP-FIXES-BRIEF.md`](briefs/REVIEW-FOLLOWUP-FIXES-BRIEF.md) | Loose ends found during the review pass — provenance-gate hole, ECGDex clock copy, dead-code, test gaps (ordered, with gates) (DONE 2026-06-23: all P0–P3 substantive items verified shipped; gates green) | Brief |
| [`AUDIT-FOLLOWUPS-BRIEF.md`](briefs/AUDIT-FOLLOWUPS-BRIEF.md) | Open items after the 6-brief execution review (2026-06-22) — Integrator synthetic filter, GlucoDex HH:MM:SS, repo-wide re-bundle sweep, blank-on-print, badge coverage | Brief |
| [`AUDIT-FOLLOWUPS-II-BRIEF.md`](briefs/AUDIT-FOLLOWUPS-II-BRIEF.md) | Round II — the §6 correctness re-bundle sweep (DONE 2026-06-23: all 8 bundles synced, GATE A/B + suite green) | Brief *(Historical)* |
| [`AUDIT-FOLLOWUPS-III-2026-06-23-BRIEF.md`](briefs/AUDIT-FOLLOWUPS-III-2026-06-23-BRIEF.md) | Round III — §1 suite-wide blank-on-print fix shipped (entrance-guard.js bundled into all 7 nodes; DONE 2026-06-23). §2 governance/data items are standing owner-gated backlog | Brief |
| [`DEX-DSP-AUDIT-FREQ-HRV.md`](audits/DEX-DSP-AUDIT-FREQ-HRV.md) | Frequency-domain HRV estimator audit (Lomb–Scargle, all 3 RR nodes) + known-answer test | Audit |
| [`DEX-DSP-AUDIT-BEATS-ARTIFACT.md`](audits/DEX-DSP-AUDIT-BEATS-ARTIFACT.md) | Beat-detection + artifact/ectopy rejection audit (Malik rule, all 3 RR nodes) + known-answer test | Audit |
| [`DEX-VALIDATION-STATUS.md`](audits/DEX-VALIDATION-STATUS.md) | What's validated (internal/synthetic numbers + DSP known-answer tests) vs the external gold-standard gap | Audit |
| [`INGEST-AUDIT-BRIEF.md`](briefs/INGEST-AUDIT-BRIEF.md) · [`INGEST-AUDIT-FINDINGS.md`](audits/INGEST-AUDIT-FINDINGS.md) | Real-capture ingest hardening — brief + findings | Audit |
| [`REFERENCE-GUIDE-AUDIT-BRIEF.md`](briefs/REFERENCE-GUIDE-AUDIT-BRIEF.md) · [`REFERENCE-GUIDE-AUDIT-FINDINGS.md`](audits/REFERENCE-GUIDE-AUDIT-FINDINGS.md) | Reference-guide ↔ registry consistency — brief + findings | Audit |
| [`BADGE-COVERAGE-AUDIT-BRIEF.md`](briefs/BADGE-COVERAGE-AUDIT-BRIEF.md) · [`BADGE-COVERAGE-AUDIT-FINDINGS.md`](audits/BADGE-COVERAGE-AUDIT-FINDINGS.md) · [`BADGE-COVERAGE-AUDIT-FOLLOWUPS-2026-06-23-BRIEF.md`](briefs/BADGE-COVERAGE-AUDIT-FOLLOWUPS-2026-06-23-BRIEF.md) | Evidence-badge coverage & correctness sweep — brief + findings + followups (DONE 2026-06-23 — brief + R1–R10 followups all complete, gates green) | Audit / Brief |
| [`BADGE-PLACEMENT-SWEEP-2026-06-24-BRIEF.md`](briefs/BADGE-PLACEMENT-SWEEP-2026-06-24-BRIEF.md) · [`BADGE-PLACEMENT-SWEEP-FOLLOWUPS-2026-06-24-BRIEF.md`](briefs/BADGE-PLACEMENT-SWEEP-FOLLOWUPS-2026-06-24-BRIEF.md) · [`DEX-PILL-UNIFY-2026-06-24-BRIEF.md`](briefs/DEX-PILL-UNIFY-2026-06-24-BRIEF.md) | Badge PLACEMENT sweep — propagate OxyDex fixes (disc before label · missing surfaces: charts/hero/flagged cards · inline spacing · pill≠disc) to the other 7 nodes (**DONE 2026-06-24**; OxyDex = reference) + follow-ups (**§1+§2 DONE 2026-06-24** — resolver-alias grade fixes on GlucoDex/PulseDex, PpgDex Limb-position badged, cross-node/event-stream tiles ratified exempt; gates green) + `DEX-PILL-UNIFY` (§4 optional pill-class consolidation, **DEFERRED 2026-06-24** — single byte-identical base premise fails: 3 distinct shapes, only color axis is shared & already in `ans-design.css §11`; no cross-file dedup possible under hand-mirroring, so even color-axis-only ≈ full-fleet re-bundle for 0 visual change; revisit only if `ans-design.css` is centralized) | Brief |
| [`DEX-CITATION-FORMULA-AUDIT-BRIEF.md`](briefs/DEX-CITATION-FORMULA-AUDIT-BRIEF.md) | Citation/formula provenance audit | Audit |
| [`INTEGRATOR-FUSION-AUDIT.md`](audits/INTEGRATOR-FUSION-AUDIT.md) · [`INTEGRATOR-FUSION-ISSUES.md`](audits/INTEGRATOR-FUSION-ISSUES.md) | Integrator fusion defects — audit + paste-ready issues | Audit |
| [`CSV-EXPORT-AUDIT.md`](audits/CSV-EXPORT-AUDIT.md) | Every `*-app.js` CSV exporter across the nodes | Audit |
| [`FINDINGS-AND-FIXES-BRIEF.md`](briefs/FINDINGS-AND-FIXES-BRIEF.md) | Generator re-run pass — findings + fixes | Brief |
| [`PAPERS-AND-FIXES-BRIEF.md`](briefs/PAPERS-AND-FIXES-BRIEF.md) | Working-preprint backlog + pilot-finding fixes — continuation | Brief |
| [`OXYDEX-ODI-CEILING-FIX-BRIEF.md`](briefs/OXYDEX-ODI-CEILING-FIX-BRIEF.md) | OxyDex ODI-4 ceiling-baseline undercount fix | Brief |
| [`DSP-NITS-2026-07-03-BRIEF.md`](briefs/DSP-NITS-2026-07-03-BRIEF.md) | Three LOW nits from the 2026-07-03 code sweep: **(1)** `integrator-longitudinal` series sort mixes floating-`t0Ms` with `Date.parse` real-UTC + misreads `t0Ms===0` (edge misorder); **(2)** OxyDex `morningPct` reads a `'T'` time component its date-only field never has → always-0 dead code (HRVDex twin works); **(3)** stale `filterArtifactSpikes` comment. Land on each app's next re-bundle / the OWN-THE-BUILD pass — no dedicated re-bundle | Brief *(DONE 2026-07-04 — all 3 nits fixed + mixed dated/undated sort assertion; OxyDex `692a7b110ef9` + Integrator `45a7e6f15e49` + OverDex + Data Unifier re-bundled, both ledgers reconciled, gates green)* |
| [`REBUNDLE-PASS-BRIEF.md`](briefs/REBUNDLE-PASS-BRIEF.md) | The final re-bundle pass — continuation checkpoint | Brief |
| [`VERITY-SIGMA-CORNER-BRIEF.md`](briefs/VERITY-SIGMA-CORNER-BRIEF.md) · [`SIGMA-WINDOW-DERIVATION.md`](docs/SIGMA-WINDOW-DERIVATION.md) | Verity σ corner / window derivation | Brief / Spec |
| [`EFFICIENCY-AUDIT-FIXES-2026-07-01-BRIEF.md`](briefs/EFFICIENCY-AUDIT-FIXES-2026-07-01-BRIEF.md) | Executes the Lane A efficiency-audit punch-list | Brief *(DONE 2026-07-01)* |
| [`EXPORT-HARDENING-FOLLOWUP-BRIEF.md`](briefs/EXPORT-HARDENING-FOLLOWUP-BRIEF.md) | Export-hardening follow-ups for the next coder | Brief |
| [`FOLLOWUP-FINDINGS-BRIEF.md`](briefs/FOLLOWUP-FINDINGS-BRIEF.md) | Follow-up findings build brief | Brief |
| [`GENERATOR-FOLLOWUPS-BRIEF.md`](briefs/GENERATOR-FOLLOWUPS-BRIEF.md) | Follow-ups from the in-app patient-generator rollout | Brief |
| [`GENERATOR-FOLLOWUPS-II-BRIEF.md`](briefs/GENERATOR-FOLLOWUPS-II-BRIEF.md) | Patient-generator follow-ups, round II | Brief |
| [`GLUCODEX-HYPO-DISAMBIG-BRIEF.md`](briefs/GLUCODEX-HYPO-DISAMBIG-BRIEF.md) | GlucoDex — stop compression-rejection from eating genuine sharp nocturnal hypos | Brief *(DONE 2026-06-23)* |
| [`INTEGRATOR-EXPORT-FIX-BRIEF.md`](briefs/INTEGRATOR-EXPORT-FIX-BRIEF.md) | Integrator fusion-export completeness + ordering (+ session follow-ups) | Brief |
| [`PROFILE-DOM-READ-AUDIT-BRIEF.md`](briefs/PROFILE-DOM-READ-AUDIT-BRIEF.md) | Legacy `getElementById('profX').value` reads that bypass the unified profile panel | Brief *(DONE 2026-06-23)* |
| [`PROFILE-HANDOFF-BRIEF.md`](briefs/PROFILE-HANDOFF-BRIEF.md) | Wire the detected profile tier into compute (+ cleanup) | Brief *(DONE 2026-06-23)* |
| [`PROFILE-UNIFY-BRIEF.md`](briefs/PROFILE-UNIFY-BRIEF.md) | Profile unification build brief | Brief |

---

## 4 · Validation, cohort & papers

| Doc | What it is | Role |
|---|---|---|
| [`COHORT-VALIDATION-BRIEF.md`](briefs/COHORT-VALIDATION-BRIEF.md) | Ganglior cohort-validation harness (10k synthetic patients) | Brief |
| [`COHORT-VALIDATION-README.md`](docs/COHORT-VALIDATION-README.md) | The harness, frozen-seed reproducibility | Reference |
| [`COHORT-WORKFLOW-GUIDE.md`](docs/COHORT-WORKFLOW-GUIDE.md) | How synthetic patients are built + how to run the harness | Reference |
| [`SYNTHETIC-CORPUS-BRIEF.md`](briefs/SYNTHETIC-CORPUS-BRIEF.md) · [`SYNTHETIC-CORPUS-README.md`](docs/SYNTHETIC-CORPUS-README.md) | Cross-node-coherent synthetic overnight corpus | Brief / Reference |
| [`SENSOR-TRIO-NIGHTS-PAPER-BRIEF.md`](briefs/SENSOR-TRIO-NIGHTS-PAPER-BRIEF.md) | Paper: how many nights to measure a sensor (reference-free σ) | Brief |
| [`RERUN-OXYFIX-PILOTS-BRIEF.md`](briefs/RERUN-OXYFIX-PILOTS-BRIEF.md) | Re-run OxyDex-dependent pilots on the ceiling-baseline fix | Brief |
| [`HRV-CONFOUND-README.md`](docs/HRV-CONFOUND-README.md) | Paper apparatus: age confounds apnea-driven HRV suppression | Reference |
| [`ODI-BIAS-README.md`](docs/ODI-BIAS-README.md) · [`ODI-BIAS-ANALYSIS-README.md`](docs/ODI-BIAS-ANALYSIS-README.md) | Paper apparatus: rolling-baseline ODI-4 vs AHI bias | Reference |
| [`papers/`](papers/) | Working preprints (each regenerated from its `*-analysis.html` tool) — see `papers/PAPERS-AUDIT.md` | Reference |
| [`PAPERS-ROADMAP-2026-06-24-BRIEF.md`](briefs/PAPERS-ROADMAP-2026-06-24-BRIEF.md) | Forward papers agenda + new-deliverable potential: the real-validation front the adapter layer / OverDex / EEGDex / Ultrahuman·SpiroDex unlock (timestamp-pathology note, dead-ends paper, multi-vendor HRV agreement, EEG-anchored sleep validation, longitudinal σ drift). Complements `PAPERS-AUDIT.md` (per-paper triage) | Brief *(PROPOSED)* |
| [`PPGDEX-OPTICAL-DETECTOR-AND-SIGMA-REDERIVE-2026-07-11-BRIEF.md`](briefs/PPGDEX-OPTICAL-DETECTOR-AND-SIGMA-REDERIVE-2026-07-11-BRIEF.md) | §1 EXECUTED — the optical beat detector counted a prominent DIASTOLIC wave as a second beat (TERMA has no amplitude discrimination; at 48 bpm the intruder lands ~625 ms out, twice the fixed 0.30 s refractory), so optical HR read 2× true on 4 of 17 trio nights. Fixed by sizing the refractory from a windowed-autocorrelation cadence. §2 OPEN — re-derive the published Verity σ (6.2 bpm measured the bug; the corrected corpus gives 1.85) | Brief *(IN-PROGRESS)* |
| [`TRIO-ARTIFACT-GATE-AND-N15-POWER-2026-07-12-BRIEF.md`](briefs/TRIO-ARTIFACT-GATE-AND-N15-POWER-2026-07-12-BRIEF.md) | The three-cornered hat is a VARIANCE estimator with no robustness to artifact: 3 bad epochs of 86 (3.5%) inflated σ_H10 from 2.5 → 9.6 bpm. Two σ estimates were wrong in OPPOSITE directions — Verity's CI optimistic (the quality gate was censoring the hard nights), H10's pessimistic (one 15-min artifact burst). Validated fix: a cross-corner consensus gate (AF-safe by construction) | Brief *(PROPOSED)* |
| [`TCH-FUSED-ROBUST-HAT-2026-07-14-BRIEF.md`](briefs/TCH-FUSED-ROBUST-HAT-2026-07-14-BRIEF.md) | Executes `TRIO-ARTIFACT-GATE` with a better estimator. Prototyping on the real corpus showed the proposed hard consensus gate (and every single-cue fix) is unreliable or biases the noisiest corner. The validated answer: a **fused-weight hat** — per-second per-sensor confidence `c = density_trust × quality_trust` (DSP tier) drives a **weighted-variance** TCH. Ground truth: recovers 1.51/1.56/2.59 (planted 1.50/1.56/2.60), exact, unbiased, AF-safe, O(n), no corpus-tuned constant | Brief *(IN-PROGRESS)* |
| [`TCH-FUSED-ROBUST-HAT-FOLLOWUPS-2026-07-14-BRIEF.md`](briefs/TCH-FUSED-ROBUST-HAT-FOLLOWUPS-2026-07-14-BRIEF.md) | Where the fused-hat findings generalize across the fleet: `RMSSD`/`SDNN`/`CV`/`MAGE` are all variance-family estimators with the SAME 0-breakdown fragility, and `beatConfidence` is signal-agnostic. Transfer map — ECGDex-own-HRV (drop-in), PulseDex/HRVDex robust HRV, OxyDex ODI / CPAPDex AHI artifact-event suppression, GlucoDex robust variability, revive the dead `bSQI` (detector B) | Brief *(PROPOSED)* |
| [`R5-HR-TRIPLET-REFERENCE-2026-07-12-BRIEF.md`](briefs/R5-HR-TRIPLET-REFERENCE-2026-07-12-BRIEF.md) | R5 executed: the HR triplet's independence test is NOT RUNNABLE — a hat cannot be validated by one of its OWN corners (σ_ECG²(TCH) ≡ cov of the other two corners' errors, verified to 6.7e-14, so the test has exactly zero power). What IS measurable is bias: OxyDex under-reads by −0.36 bpm, invisible to every published σ. The fix is one cable — connect the ResMed oximeter | Brief *(PROPOSED)* |
| [`PROFILED-HOTSPOTS-CI-AND-DSP-2026-07-12-BRIEF.md`](briefs/PROFILED-HOTSPOTS-CI-AND-DSP-2026-07-12-BRIEF.md) | CPU-profiled ground truth, not op-counts. The CI `test` gate is ECGDex-bound and SINGLE-threaded — `lombScargle` alone is 35% of it. `DEX_GROUP` filters the REPORT, not the RUN, so CI sharding is not a workflow-only change. `PpgDex.compute()` spends 28% in `mean`, because `pearson()` recomputes `mean(tmpl)` once per beat. Plus §0: a synthetic microbenchmark lied by 18× — profile the real pipeline first | Brief *(PROPOSED — §2/§1a EXECUTED via CI-SHARDING; §3 RETRACTED (vm-realm profile artifact, EFFICIENCY-AUDIT §M1); §4 dismissed; **only §1b `lombScargle` optimise-or-leave decision open**)* |
| [`TRIO-POWER-N15-FINDINGS-2026-07-12-BRIEF.md`](briefs/TRIO-POWER-N15-FINDINGS-2026-07-12-BRIEF.md) | Attempted SENSOR-TRIO-NIGHTS §181 (the N=10→15 power re-run). **The σ re-fit is NOT landed** — the planted σ and the paper's Tables 1–3 are ONE ATOMIC UNIT (those tables *are* the Monte-Carlo at that σ), so changing one without the other silently publishes a simulation of a truth nobody chose. **Blocker 1:** a 50k/100k re-run does not complete — the WebGPU lane accelerates only the N-sweep; the ρ/duration sweeps are CPU-bound (ETA advanced 1 min in 25 min at 100k). The paper is also self-inconsistent: Table 1 says 50,000 trials, the abstract says 720, the tool defaults to 500. **Blocker 2 (FIXED here):** the planted σ is TRIPLICATED (page/CPU/GPU) and was ungated — a stale cache made the tool REPORT σ=2.72/1.86/1.94 while SIMULATING at 2.60/1.58/1.85, and briefly faked a GPU≠CPU divergence (they are bit-identical). Measured-but-unlanded: the 15-night hat is **2.60/1.58/1.85** and moves 2 cells (H10 ±0.25: 1→2 — a *quieter* corner needs MORE windows, because the TCH couples them; Verity ±0.15: 3→2) | Brief *(PROPOSED)* |
| [`WORKER-REALM-GATES-2026-07-12-BRIEF.md`](briefs/WORKER-REALM-GATES-2026-07-12-BRIEF.md) | Follows PPGDEX-WORKER-CLOSURE. The `cadenceSamples` bug was not bad luck — **nothing in this repo executed a worker**. **The trap:** `detectChannelsAsync` falls back to serial on `w.onerror`, so a throwing worker still returns CORRECT NUMBERS — any check comparing worker output to serial **passes on a completely dead pool**. Demonstrated: against the pre-fix DSP the new rig reds on `NO worker threw` while `worker output ≡ serial` **still passes**. **§1** the PPG blob now EXECUTES in the headless gates (`new Function` reproduces the empty worker realm in both lanes) — necessary because the STATIC check said `CLOSED` on code that still threw `REFR_CADENCE_FRAC is not defined` (a module `const` does not travel with `Function.toString()`). **§2** a REAL-Worker rig in the browser lane (spawned · none errored · ≡ serial). **§3 ⚠️** `ecgdex-app.js`'s `WORKER_SRC` carries a **FOURTH, ungated `parseTimestamp`** (`_ckPF`) that runs ONLY above the 5 MB worker threshold — a drift there would corrupt timestamps **exclusively on large overnight recordings** while every fixture stayed green; measured to agree with `DexClock` on all 12 vendor formats, now pinned. Audit also: 10 surfaces driven in a real browser → **0 uncaught errors**; H2 (OverDex/Data-Unifier missing `PPGMorph`) **resolved as a non-issue**. Gates only — no re-bundle | Brief *(DONE — 2026-07-12)* |
| [`PPGDEX-WORKER-CLOSURE-2026-07-12-BRIEF.md`](briefs/PPGDEX-WORKER-CLOSURE-2026-07-12-BRIEF.md) | `Uncaught ReferenceError: cadenceSamples is not defined` (reported from the live demo). The PPG worker is a `blob:` built from an INLINED source string whose deps array is **hand-maintained**, and the worker realm starts **empty** — the optical-detector fix gave `detectBeats` an adaptive refractory sourced from a new `cadenceSamples()` + `REFR_CADENCE_FRAC`, and **neither was shipped**. **Why no gate saw it:** `w.onerror` falls back to serial, so the NUMBERS stayed right — and the equiv/headless gates run `analyze()`/`compute()`, which are serial BY DESIGN and **never touch the worker**. Symptoms: a console error, and all 3 LED channels detected on the MAIN THREAD (the exact thing the pool exists to prevent). A module `const` **does not travel with `Function.toString()`** — a static call-graph check missed `REFR_CADENCE_FRAC`; only RUNNING the worker realm found it. Verified worker ≡ serial on 3 real captures × 3 channels (9/9, byte-identical). New gate re-derives the closure from the DSP's own source text; reds against `origin/main` naming both symbols | Brief *(DONE — 2026-07-12)* |
| [`PPGDEX-PI-AND-PARSE-2026-07-12-BRIEF.md`](briefs/PPGDEX-PI-AND-PARSE-2026-07-12-BRIEF.md) | Executes `EFFICIENCY-AUDIT-FINDINGS-2026-07-12` §C1+§P1. **A shipped app was showing a wrong number.** `ppgdex-morph.js` stepped its sample loop by `winSamp = winSec × fs`, and `fs` is the median sensor-ns delta — so on a real Polar file it is NEVER integral (176.26 → 10575.6). A fractional `Float32Array` index returns `undefined` → `NaN` → **`pi` silently `null`**: the surfaced per-window perfusion-index trend was plotting **1/2, 1/8, 2/7** windows on real captures (now 2/2, 8/8, 7/7). Rounding ONLY the sample bounds leaves beat→window assignment untouched (proved: `analyze()` hashes identically with `pi` stripped) and is **2.24×** faster. Plus §P1: `parsePPG` ran `parseTimestamp` on all ~190k rows for a `lastTs` only a degenerate fallback reads → **2.08×**, byte-identical. The audit's 3rd finding was **REJECTED** — it would have broken PAT feasibility (`pat-feasibility-worker.js` reads `tMs` on those rows). Pinned by a regression group using a NON-INTEGER `fs` (an integer `fs` passes even against the bug — which is why the suite never caught it) | Brief *(DONE — 2026-07-12)* |
| [`PPGDEX-PI-AND-PARSE-FOLLOWUPS-2026-07-12-BRIEF.md`](briefs/PPGDEX-PI-AND-PARSE-FOLLOWUPS-2026-07-12-BRIEF.md) | Residue from executing PPGDEX-PI-AND-PARSE. **§1** the companion-parse 2.08× (`parseSensorXYZ`), deferred not dismissed — needs a back-compat-safe `opts.stamps` shape because `pat-feasibility-worker.js:117` reads `r.tMs`. **§2** drop `ppgdex-morph.js` from biome's formatter-exempt list once it is genuinely reflowed. **§3** sweep the fleet for other float-stepped sample loops used as array indices — the failure mode is SILENT (`undefined`→`NaN`→`null`, no throw); candidate house-invariant lint. **§4** the synthetic fixtures all used an integer `fs`, so they could only prove the code works on inputs that never occur — audit the other nodes' generators for the same tidiness | Brief *(PROPOSED — deferred; §3/§4 EXECUTED; §1 blocked on gitignored ~600k-row companions; §2 conditional on a morph re-bundle)* |
| [`GATE-INTEGRITY-AND-DEVLOOP-2026-07-12-BRIEF.md`](briefs/GATE-INTEGRITY-AND-DEVLOOP-2026-07-12-BRIEF.md) | Executes `EFFICIENCY-AUDIT-FINDINGS-2026-07-12` §G1+§D1+§D2. **§G1** a ⊘ SKIP is neither pass nor fail, so the gate could shrink in SILENCE — the raw corpus is gitignored, so on a fresh clone **all 8 real-recording equivalence legs (the whole GATE-C surface) have never run on the merge path** (CI 2093/11-skipped, GATE B 10 of 23; local 2113/2, 23/23) and nothing pinned the skip count. Every skip must now be DECLARED in `tests/expected-skips.json` — an undeclared skip FAILS, fail-closed — plus a loud COVERAGE report and `DEX_UPLOADS=<path>` (which also makes CI's coverage reproducible locally). **§D1** `--jobs=N` forks the CI partition locally: **100.7 s → 24.9 s**, identical verdict (`npm run test:par`; `npm run check` uses it). **§D2** the two committed ledgers were a conflict ENGINE (45% of commits, 10 merge-resolutions in 48 h) because scalar `count`/`generated` changed on BOTH sides of any disjoint add — and a conflicted PR gets **NO CI AT ALL** (GitHub builds `pull_request` against the merge commit; it hit PR #43 AND #46 in one session). Now line-oriented text + `merge=union`, single-sourced in `tests/list-format.js`; proved in a scratch repo to auto-merge where the JSON conflicted, with the `list == fs` staleness assertion as the backstop | Brief *(DONE — 2026-07-12)* |
| [`TRIO-BATCH-O2RING-DAT-2026-07-13-BRIEF.md`](briefs/TRIO-BATCH-O2RING-DAT-2026-07-13-BRIEF.md) | **Six analyzable nights were invisible because the oximeter's CSV export stopped.** `trio-batch` discovered the oxy corner with `.csv` only — and *anchors the whole night on it*, so a night with a complete ECG+PPG pair was dropped for want of an anchor. The O2Ring writes a **native `.dat` beside the vendor CSV**, and when the export stops (app not opened, phone not synced) the `.dat` is all that survives: **20 CSV-less `.dat`s** sat in the capture folder, 6 of them (07-07 … 07-12) with full concurrent Polar streams. **OxyDex has read this format all along** (`isO2RingBin` / `decodeO2RingBinToCSV`, live on the browser drop path) — only the *headless* corner was blind, because `compute()` takes `{samples\|rows\|text}` and never bytes. The fix **exposes the existing decoder rather than copying it** (a second implementation of the 3-byte layout / `0xFF 0xFF` trailer / `motion×2` scale would drift), prefers the vendor CSV when both files exist, and ranks the anchor by **duration, not bytes** — bytes stopped being comparable the moment a `.dat` (~10× denser) could compete with a CSV. Equivalence is *proved*, not asserted: 2026-07-06 has both files, and same-code CSV-path ≡ `.dat`-path with **zero diffs** across every metric, event and timeseries value. Failure was **silent by construction** — one `⊘` among hundreds of `·` lines, exit 0; the silent-drop ergonomics are deliberately left to their own brief | Brief *(DONE — 2026-07-13)* |
| [`CI-SHARDING-2026-07-12-BRIEF.md`](briefs/CI-SHARDING-2026-07-12-BRIEF.md) | Executes PROFILED-HOTSPOTS §2→§1a. The 4-minute `test` gate was ONE single-threaded step at 104% CPU on a multi-core runner; now 4 parallel shards — **4m05s → ~1m10s** (101.3 s → 25.3 s makespan, 4.00x). `group()` now skips *execution*, not just the report (so `--group` finally means what it claimed). The shards **partition** the suite — index-based, never name-based, because a pattern shard silently drops any group no pattern claims and every shard still goes green. `verify-shard-union.mjs` proves the partition on every push (0.08 s) and `--deep` proves the shard-union is assertion-for-assertion identical to the unsharded run. `genSynthetic` memoisation deliberately NOT taken: those groups regenerate on purpose to assert byte-reproducibility, so a cache would speed the gate up by weakening it | Brief *(DONE — 2026-07-12)* |
| [`TEST-COVERAGE-FOLLOWUPS-2026-07-17-BRIEF.md`](briefs/TEST-COVERAGE-FOLLOWUPS-2026-07-17-BRIEF.md) | Two-pass coverage audit — modules that **ship but are never asserted**, outside the well-covered DSP/clock/contract/provenance backbone. **Item 1 (landing here):** the five per-node **profile engines** (~2,850 lines of *independent* cited physiology — Tanaka HRmax, Uth–Sørensen VO₂, age-norm RMSSD/RHR, CVHR apnea bands, GMI↔A1c/eAG bias) had **zero behavioral assertions**; `ECGProfile`/`GLUProfile`/`PPGProfile` are now surfaced into both runners' `env` and pinned by a `personalize()` known-answer group (test-only, no re-bundle). **Item 2:** `nsrr-adapter.js` (204) PSG ingest parser — untested. **Item 3:** 4 analysis kernels (`qrs-yield`/`qrs-equiv`/`pat-feasibility`/`cohort-regression`) appear only in `.html` static lists, their math never executed. **Item 4:** worker≡serial proven for only 2 of 6 (`cohort`/`pat-feasibility`/`qrs-*` workers unguarded). **Item 5 (minor):** `overdex-walk` untested; `support.js` is an orphan (source `dc-runtime/` absent, inlined nowhere) → cleanup not test. Item 1b (HRVDex/OxyDex profile seam, re-bundle) + 2–5 land independently | Brief *(IN-PROGRESS 2026-07-17)* |
| [`TEST-COVERAGE-FOLLOWUPS-II-2026-07-17-BRIEF.md`](briefs/TEST-COVERAGE-FOLLOWUPS-II-2026-07-17-BRIEF.md) | Continues the parent's **items 3/4/1b**, which execution showed are **NOT test-only** (each needs a source change). **Item 3** (analysis kernels `qrs-yield`/`qrs-equiv`/`pat-feasibility`/`cohort-regression`): DOM page-controllers with no export seam that duplicate their own `pearson`/`ba`/`olsR2` — a tested copy in `analysis-stats.js` would be *fake coverage*; plan = **Route A** expose-the-shipped-function + DOM-guard + known-answer (lower risk), or **Route B** extract-to-shared-kernel + delegate (also dedups, touches page HTML/CSP gates). **Item 4** (worker≡serial): the 4 workers are separate FILES with **no serial twin** to diff — plan = a known-answer **real-Worker rig** (`WORKER-REALM-GATES §2`), not a hollow ≡-serial gate. **Item 1b** (HRVDex/OxyDex profile seam): DOM-only, no namespace — add an additive pure surface → **re-bundle + provenance regen** (the one item that pays that cost). Direct application of `TEST-AUDIT-PROMPT.md` ("test the shipped code, never a copy") | Brief *(PROPOSED 2026-07-17)* |

---

## 5 · Licensing & attribution

| Doc | What it is | Role |
|---|---|---|
| [`LICENSE`](LICENSE) · [`NOTICE`](NOTICE) | Apache-2.0 license + attribution notice | Authoritative |
| [`CITATION.cff`](CITATION.cff) · [`THIRD-PARTY.md`](THIRD-PARTY.md) | How to cite + third-party inventory | Authoritative |
| [`licensing/LICENSING-BRIEF.md`](licensing/LICENSING-BRIEF.md) | The pass that licensed/stamped every file; `Tepna` brand vs frozen `Ganglior` codename | Brief |

---

## 6 · Folders (not single docs)

| Folder | What's inside |
|---|---|
| [`codegen/`](codegen/) | Manifest → scaffold toolchain (`dex-gen.js`, manifests). See `codegen/README.md`. |
| [`tests/`](tests/) | Shared assertions + Node runner behind `Dex-Test-Suite.html`. See `tests/README.md`. |
| [`wiring/`](wiring/) | "How It's Wired" architecture reference set — start at *How It's Wired - the Dex Suite.html*. |
| [`templates/`](templates/) | Design-system spec, theme + evidence-badge previews. |
| [`how-to-collect/`](how-to-collect/) | Device-by-device capture instructions (mirrors `How to Collect Data.html`). |
| [`papers/`](papers/) | Working preprints + `PAPERS-AUDIT.md`. |
| [`docs-archive/`](docs-archive/) | **Historical** — superseded/done briefs (e.g. the completed modularize refactor). Kept for the record; not current guidance. |

---

## Maintaining this index

When you add a root-level `*.md`, add one row here in the matching group with its real H1 and a
role tag. When a brief is fully executed and superseded, move it to `docs-archive/` and drop its row
(or relabel it *Historical*). This file is the one place a newcomer should land before opening any
single brief.

Every relative link in this file is gate-checked (`docs-ledger` check 4 — CLAUDE.md §📌): a `briefs/` link resolves against the real brief set (4a), and every other `docs/·audits/·wiring/·root` link resolves
against a whole-tree path inventory (4b) the Node lane recomputes from disk on each run. If you move or
rename a linked file, just fix the link — there is no committed inventory to regenerate (the gate reads the
tree straight from the filesystem; CPAP-REAL-CORPUS-FOLLOWUPS-II §4).
